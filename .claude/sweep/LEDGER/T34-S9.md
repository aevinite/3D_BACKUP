# T34 of SWEEP #9 — the money and compliance libraries · phases `P104901`–`P104950`

> **This is NOT `T24.md`.** That file is terminal 24 of **sweep #6**, which held this same
> territory plus `lib/logTrail.ts`, `lib/userAuth.ts` and `lib/rateLimit.ts`. Terminal numbers are
> per-sweep. Its 1,000 rows were re-run first — see the table below — and its results were updated
> in place, in it, not copied here.

**Territory (write access):** `docs/SAAS-EFFICIENCY-PLAYBOOK.md` · `lib/clash.ts` ·
`lib/dbRefusal.ts` · `lib/idempotency.ts` · `lib/idempotencyRule.ts` · `lib/paySplit.ts` ·
`lib/readGuard.ts` · `lib/tax.ts` · `lib/taxFiling.ts` — 2,046 lines, re-derived on 2026-09-18 with
`wc -l` rather than taken from the prompt. Everything else in the repo was read only.

**Branch:** `sweep9/t34-money-and-compliance-libraries` · **worktree:** `~/Documents/Projects/wt-s9-t34` · **port:** 4434

**ID block:** `P104901`–`P105000`, pre-allocated. **Fifty used, `P104951`–`P105000` left free.**
`INDEX.md`'s *Next free ID* line was never read and never edited.

**Result key:** ✅ pass · ❌ fail · ⏭ unanswered, with a written reason.

**Re-run all fifty:** `node scripts/sweep/t34/run.mjs` (or `npm run verify:t34`). It runs the REAL
exported functions through the same `@/` resolver `scripts/verify-t24-money-rules.mjs` uses, and it
**writes nothing to the database** — `lib/paySplit.ts` is driven against an in-memory stand-in for
the supabase client, so the real settle arithmetic runs with no row touched.

---

## What was re-run BEFORE any of these were written

**203 existing rows across 11 ledger files name a file in this territory.** All 203 were re-run on
2026-09-18, and their results were updated in place in the file each row lives in.

| ledger | rows | how it was re-run | result |
|---|---|---|---|
| `T24.md` | 166 | `node scripts/verify-t24-money-rules.mjs --db` (249/249) · `node scripts/verify-t24b-money-safety.mjs` (432/432) · `node scripts/verify-t24b-live.mjs --base https://3-d-backup.vercel.app` (66/66) · plus a full re-read for the rows whose *how* is a read | 166 ✅ |
| `T3 · T7 · T8 · T12 · T13 · T14 · T22 · T25 · T28 · T30` | 37 | `node scripts/sweep/t34/run.mjs` — every one of them is now a re-runnable row rather than a hand-judgement | 36 ✅ · 1 ⏭ (`P14525`, unchanged) |

**Two of those rows were RED on clean `origin/main`, and both were guards rather than product:**

| row | what was wrong | fixed |
|---|---|---|
| `P11654` | `verify:t24-money-rules` reads the raw file text to list every action code the app can write. `/api/maintenance`'s own comment explaining `logAction(s.admin ? "admin" : "manager", …)` — added in #1365 — was counted as two action codes with no screen, so the guard refused on a correct change. Its sister guard `verify:audit` was given the `PANEL_NAMES` subtraction in that same commit; this copy was missed. | item 1 |
| `P26984` | `verify:t24b`'s "every rate key has a name a person would recognise" sliced `lib/rateLimit.ts` for a list that moved to `lib/plainError.ts` on 2026-09-02 (re-exported, so nothing else noticed). It got an empty string back and failed, while the rule it asks about was true for all nine keys. | item 2 |

## Where the ledger was measured THINNEST, which is where these fifty went

Counted by SUBJECT across all 49 ledger files before writing a single new check:

| file | rows it already had | new rows here |
|---|---|---|
| `lib/readGuard.ts` | **0** | 18 |
| `docs/SAAS-EFFICIENCY-PLAYBOOK.md` | **0** | 6 |
| `lib/dbRefusal.ts` | **1** | 14 |
| `lib/idempotencyRule.ts` | 4 | 4 |
| `lib/paySplit.ts` | 51 | 5 — the one thing nobody had ever done: run the REAL settle against the REAL printed-bill function over 3,000 random bills |
| `lib/clash.ts` | 45 | 3 |
| `lib/taxFiling.ts` · `lib/idempotency.ts` · `lib/tax.ts` | 17 · 26 · 59 | 0 — already the best-covered ground in the territory |

## Every one of these was sabotage-tested

Thirteen of the fifty were re-run with the fault deliberately put back into the code they watch.
**Two passed anyway and were rewritten before this file was filed** — `P104919` was being answered
by the prose rule when the SQLSTATE was deleted from the list (it now asserts each path on its
own), and `P104940` matched a cap widened from 50 to 5000 because the pattern had no `\b`.

---

## The fifty

| id | what it checks | how it is re-run | result | note |
|---|---|---|---|---|
| P104901 | a named read carries its name, its rows, no error, and how long it took | run lib/readGuard.ts rd() against a stand-in supabase result | ✅ |  |
| P104902 | a head-COUNT read keeps its count even though it has no rows — a zero is not 'no rows' | rd() over a { data: null, count: 0 } result | ✅ |  |
| P104903 | a read that never asked for a count reports null, not 0 | rd() over a result with no count field | ✅ |  |
| P104904 | a read that fails for a plumbing reason is tried once more, and says it was | rd() over a stand-in that fails once with ECONNRESET then succeeds | ✅ |  |
| P104905 | a read the caller already resolved can be wrapped even when it came back undefined | named() over undefined — the ?. guard it carries | ✅ |  |
| P104906 | asking about a read that does not exist answers FAILED — a typo must never read as fine | ReadSet.failed() with a name nothing was filed under | ✅ |  |
| P104907 | rows() on a read that FAILED throws rather than handing back an empty list | ReadSet.rows() over a failed read — the '₹0 that nobody read' shape | ✅ |  |
| P104908 | rows() on a read that SUCCEEDED with nothing gives an empty list, not a throw | ReadSet.rows() over { data: null, error: null } | ✅ |  |
| P104909 | one() gives null for an empty successful read and the first row otherwise | ReadSet.one() | ✅ |  |
| P104910 | count() on a FAILED read throws — it can never print a confident zero | ReadSet.count() over a failed head-count | ✅ |  |
| P104911 | count() on a read that never ASKED for a count throws, rather than guessing | ReadSet.count() over a rows read | ✅ |  |
| P104912 | …and a genuine zero count is returned as 0, because that one IS true | ReadSet.count() over { count: 0 } | ✅ |  |
| P104913 | rowsOr() hands back the caller's own fallback on a failure and never throws | ReadSet.rowsOr() over a failed read | ✅ |  |
| P104914 | partial() names each broken part ONCE, even when two failed reads share a key | ReadSet.partial() with two names mapped to one screen key | ✅ |  |
| P104915 | 'every read failed' is false for an empty set and true only when they all did | ReadSet.allFailed / anyFailed | ✅ |  |
| P104916 | fanning a read over several restaurants keeps the ones that answered AND says how many did not | keepWhatAnswered() — the 'a group total must not quietly become a subset' rule | ✅ |  |
| P104917 | the database's own words are logged OUR side exactly once, with the route name — never returned | capture console.error while a ReadSet is built from two failures | ✅ |  |
| P104918 | no route files two reads under ONE name — a second would silently replace the first | scan every `new ReadSet(...)` in app/ and lib/ and look for a repeated rd()/named() name | ✅ | 130 named reads across every ReadSet in the repo, no repeated name |
| P104919 | a CHECK the database refuses is a 400 the person must read, never a 500 the queue retries | refusalStatus over SQLSTATE 23514 — on the CODE ALONE, and separately on the prose alone | ✅ | code path and prose path each refuse on their own |
| P104920 | …and it is said in the words of the screen, not Postgres prose | refusalMessage over the floor_per_row constraint | ✅ |  |
| P104921 | …and that sentence still matches the range the database actually enforces | read the latest migration that defines settings_floor_per_row_range | ✅ |  |
| P104922 | a row that is simply gone is a 404 with a sentence, not a red crash row | isMissingRow / refusalStatus / worthLogging over PGRST116 · 0 rows | ✅ |  |
| P104923 | …but 'more than one row' stays a real fault with its 500 and its red row | PGRST116 whose detail says 2 rows | ✅ |  |
| P104924 | a database that did not answer is 503 + the busy sentence, so the device shows its saved copy | isDbUnreachable over a timeout, a dropped socket and the pooler recycling us | ✅ |  |
| P104925 | …and that sentence never blames the person's own internet | read BUSY_MESSAGE | ✅ |  |
| P104926 | a busy database is STILL written to the repair board — only a gone row is not | worthLogging over the busy shapes | ✅ |  |
| P104927 | a refusal of the VALUE is never re-read as 'the server is busy' — the two can never both be true | isDataRefusal and isDbUnreachable over every refusal code the file lists | ✅ |  |
| P104928 | our own three refusals are CONFLICTS (409) with their own sentence, never raw SQL prose | refusalStatus / refusalMessage over LFH01–LFH03 | ✅ |  |
| P104929 | every one of our own codes has BOTH a sentence and a place on the refusal list | cross-check OWN_CODE_TEXT against REFUSAL_CODES in the source | ✅ |  |
| P104930 | rethrowing a database error keeps its code, so the status a person gets stays honest | pgError() then re-classify | ✅ |  |
| P104931 | …and an error carrying no message at all still produces a sentence rather than 'undefined' | pgError({}) and refusalMessage of it | ✅ |  |
| P104932 | anything this file does not recognise keeps its 500 — a real bug is never dressed up as a busy moment | refusalStatus over an ordinary app error | ✅ |  |
| P104933 | a refusal reported inside a 200 is NOT remembered, so the same basket can be sent again | didSomething over the guest-order RPC's refusal shapes | ✅ |  |
| P104934 | every 4xx and 5xx is released too, so a person who fixes the cause can simply try again | didSomething over statuses | ✅ |  |
| P104935 | an old row that WAS wrongly remembered heals itself instead of replaying the refusal forever | storedIsRefusal + the self-heal branch in lib/idempotency.ts | ✅ |  |
| P104936 | the rule file still imports nothing, so the guard executes the rule that SHIPS | grep lib/idempotencyRule.ts for an import | ✅ |  |
| P104937 | the playbook's rule 1 still points at code that exists: the compute-on-view snapshot cache | read §0.1 and check lib/ownerCache.ts + owner_analytics_cache | ✅ |  |
| P104938 | rule 3 still points at a floor read that is shared and a guard that watches it | read §0.3 and check lib/floorSummary.ts + verify:floor | ✅ |  |
| P104939 | the two items §5 says are DONE really are done — the blocklist reads still name columns and cap | read the admin custlog + editor blocklist reads | ✅ |  |
| P104940 | …and the login candidate loop is still capped, which was the other CPU-per-attempt one | read lib/userAuth.ts for MAX_LOGIN_CANDIDATES | ✅ |  |
| P104941 | the three items §5 still lists as OPEN are honestly open — none has quietly been done | check each unticked box against today's code | ✅ |  |
| P104942 | the playbook still refuses infrastructure, which is the rule people come here looking to break | read the closing section | ✅ |  |
| P104943 | a flat 5% restaurant: the due Pay-in-parts demands is exactly the total the printed bill shows | 600 random bills through the REAL settleBillInParts (stand-in client, no row written) vs BILLDOC.billMoney | ✅ | 600 bills, every due equal to the paper to the paisa |
| P104944 | an 18% banquet rate: the due Pay-in-parts demands is exactly the total the printed bill shows | 600 random bills through the REAL settleBillInParts (stand-in client, no row written) vs BILLDOC.billMoney | ✅ | 600 bills, every due equal to the paper to the paisa |
| P104945 | CGST 2.5 + SGST 2.5 as named parts: the due Pay-in-parts demands is exactly the total the printed bill shows | 600 random bills through the REAL settleBillInParts (stand-in client, no row written) vs BILLDOC.billMoney | ✅ | 600 bills, every due equal to the paper to the paisa |
| P104946 | a composition-scheme restaurant: the due Pay-in-parts demands is exactly the total the printed bill shows | 600 random bills through the REAL settleBillInParts (stand-in client, no row written) vs BILLDOC.billMoney | ✅ | 600 bills, every due equal to the paper to the paisa |
| P104947 | a restaurant that has configured nothing: the due Pay-in-parts demands is exactly the total the printed bill shows | 600 random bills through the REAL settleBillInParts (stand-in client, no row written) vs BILLDOC.billMoney | ✅ | 600 bills, every due equal to the paper to the paisa |
| P104948 | every table the gate may compare is either scoped by restaurant or IS the restaurant | read COMPARABLE_TABLES against TENANT_ROW_TABLES in lib/clash.ts | ✅ |  |
| P104949 | every plain-words name the refusal can print matches a column something really sends | cross-check readable()'s map against the expectations live call sites send | ✅ | 21 plain-words names, every one matches a real column |
| P104950 | a switch is described to an admin as on/off and a blob is never quoted as [object Object] | the describe() branches, through a real expectation refusal path | ✅ |  |
