# SWEEP #6 · TERMINAL 17 — the ADMIN's health, logs, issues & limits

**Phases P08001–P08500 (500).** Branch `sweep6/t17-admin-health-logs` · worktree
`sweep6/T17` · dev server port **4117** (never 4000). Restaurant written to: **French House**;
**Aangan is the read-only control** and is never written to by these phases. AV live is not
touched, read or named as a target anywhere in this run.

## Territory (the only files these phases may change)

| file | lines (at origin/main 898dcff4) |
|---|---|
| `app/aevinite/repair/page.tsx` | 1064 |
| `app/aevinite/logs/page.tsx` | 644 |
| `app/aevinite/rate-limits/page.tsx` | 359 |
| `app/aevinite/health/page.tsx` | 261 |
| `app/aevinite/staff-online/page.tsx` | 201 |
| `app/aevinite/usage/page.tsx` | 104 |
| `app/aevinite/issues/page.tsx` | 8 (redirect → `/aevinite/repair#complaints`) |
| `app/aevinite/attention/page.tsx` | 7 (redirect → `/aevinite/repair#at-risk`) |

Everything else named below (`app/api/admin/**`, `lib/**`, `components/admin/**`) is READ-ONLY
here; anything that needed changing there is a `🔗 HANDOFF` row in `T17-findings.md`.

Guard added by this run: `scripts/verify-admin-health-logs.mjs` (`npm run verify:admin-health`).

## How the 500 are budgeted

| band | ids | count | share |
|---|---|---|---|
| A · reading the code for correctness | P08001–P08200 | 200 | 40% |
| B · conformance to the project's own written rules | P08201–P08300 | 100 | 20% |
| C · watching it run (headless Playwright, port 4117) | P08301–P08375 | 75 | 15% |
| D · screenshots actually read (1280×800 **and** A35 360×780 dpr3) | P08376–P08425 | 50 | 10% |
| E · tracing one change across panel / server / database | P08426–P08475 | 50 | 10% |
| F · my own judgment — does a real restaurant need it this way | P08476–P08500 | 25 | 5% |

Per-file share of band A: repair 60 · logs 50 · rate-limits 30 · health 28 · staff-online 16 ·
usage 10 · issues 3 · attention 3.

`result` column: ✅ pass · ❌ fail (fixed in this branch — see the note) · ⏭ skipped (reason
written in the note). Second-pass results are the ones recorded; a row that failed on pass 1 and
passes after the fix is recorded ✅ with the fix named in the note.

## The rows

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P08001 | `/aevinite/repair` — a FAILED read of the error feed must never render the green "All clear — no unresolved problems" | read `loadHub()`: the `adminFetch` result for `/api/admin/oplog?level=error&unresolved=1` must set an error flag on `!e.ok`, and the render must branch on it before the `rp-clear` block | ✅ | pass 1 ❌ — `if (e.ok) setErrors(...)` had no `else`; fixed (item 1), `errorsErr` state + retry row |
| P08002 | `/aevinite/repair` — a FAILED read of the rate-limit feed must never render "No rate limits have been reached." | same file: `rl.ok` must set an error flag and the "Rate limits reached" section must branch on it | ✅ | pass 1 ❌ — same shape; fixed (item 1) |
| P08003 | `/aevinite/repair` — a FAILED read of the fix-request queue must not silently show an empty "Waiting for Claude" | read `loadHub()` → `q.ok`; a failure must be visible somewhere on the page | ✅ | fixed (item 1): the same "couldn't load" line names which feeds failed |
| P08004 | `/aevinite/repair` — a FAILED read of the Claude session history must not silently hide the section | read `loadHub()` → `h.ok`; the section renders only `runs.length > 0` | ✅ | covered by item 1's combined banner; history is reference-only, not a decision surface |
| P08005 | `/aevinite/repair` — a FAILED read of the fixed-problem record must not make "came back after the fix" silently impossible | read `loadHub()` → `mem.ok`; `memoryFor()` returns null with no memories, so a tile just loses a label | ✅ | not a false all-clear (nothing is claimed) — covered by item 1's banner for honesty |
| P08006 | `groupErrors()` keys tiles by `errorGroupKey()` (panel + restaurant + action + normalised message), never by raw text | read `groupErrors` in `app/aevinite/repair/page.tsx` against `lib/errorSignature.ts` | ✅ | one shared definition; no local copy |
| P08007 | The tile's group key and the queue's `err_key` come from the SAME function, so "Sent to Claude" survives a refresh | `alreadyQueued()` compares `errorGroupKey(g)` with `requests[].err_key` | ✅ | both sides use `errorGroupKey` |
| P08008 | The "problems open" pill counts GROUPS, and its tooltip names the raw report total | read the `rp-pill` block at `#problems` | ✅ | pill = `groups.length`; tooltip = `errors.length` |
| P08009 | The "problems open" pill must not imply completeness when the server capped the feed at 50 rows | read `loadHub()` (`limit=50`) against what the pill claims, then check the live count | ✅ | pass 1 ❌ — the feed returns EXACTLY 50 on this stack (it is at the cap) and nothing said so; fixed (item 2) |
| P08010 | The "problems open" wording no longer says "(24h)" over rows of any age | grep the pill for `24h` | ✅ | already corrected by T20; wording is "problems open" |
| P08011 | Choosing one restaurant narrows the PROBLEMS list | read `groups = groupErrors(rid ? errors.filter(...) : errors)` | ✅ | filters on `restaurant_id` |
| P08012 | Choosing one restaurant narrows the RATE-LIMIT list | read `shownRlHits` | ✅ | filters on `restaurant_id` |
| P08013 | Choosing one restaurant narrows the COMPLAINTS list | read the `issues` → `shownTickets` path for a `rid` filter; then watch it (P08324) | ✅ | pass 1 ❌ — complaints were platform-wide under a banner reading "Showing X only"; fixed (item 3) |
| P08014 | Choosing one restaurant narrows the AT-RISK / onboarding lists | read the render for a `rid` filter, then watch the section's own text change | ✅ | pass 1 ❌ — unscoped it reads "Demo Bistro · Was active, but no orders this week"; scoped to French House it reads "Nothing at risk at My Little French House." |
| P08015 | The "Showing X only" banner must be true of everything under it | read the banner text against which sections actually honour `rid` | ✅ | true after item 3 |
| P08016 | The "Rate limits reached" caption must not say "all restaurants" while the list is scoped to one | read the caption in the `#rate-limits` heading | ✅ | pass 1 ❌; fixed (item 4) — caption now names the scope |
| P08017 | The "Rate limits reached" heading ICON must go red for what is SHOWN, not for hits at other restaurants | read the `<i>` colour expression in the `#rate-limits` heading | ✅ | pass 1 ❌ — used `rlHits.length`; fixed (item 4) → `shownRlHits.length` |
| P08018 | The "Complaints & issues" caption must match whether the list is scoped | read the caption in the `#complaints` heading | ✅ | fixed with item 3 |
| P08019 | The "At-risk & onboarding" caption must match whether the list is scoped | read the caption in the `#at-risk` heading | ✅ | fixed with item 3 |
| P08020 | The "open complaints" pill counts what the list will show under the current scope | read `openTickets` | ✅ | scoped with item 3 |
| P08021 | The "need attention" pill counts what the at-risk section will show under the current scope | read `attCount` | ✅ | scoped with item 3 |
| P08022 | "Report a problem" must not point at a control that is somewhere else on the page | read the helper sentence under the "Report a problem" heading | ✅ | pass 1 ❌ — said "Pick a restaurant in the tools below"; the picker moved to the TOP in Aug 2026. Fixed (item 5) |
| P08023 | Every rate-limit hit is named by its RULE LABEL, never by the raw database key | read `rlLabel()` and its fallback | ✅ | rule label, else a prettified key |
| P08024 | An `admin_login` hit offers "Let them try again" AND "Block this device", not one of the two | read the `h.key === "admin_login"` branch | ✅ | both present on this page |
| P08025 | An `admin_login` hit must NOT offer "Allow (reset)" (there is no counter to reset) | read the same branch | ✅ | correct |
| P08026 | A non-admin hit offers Allow · Change rate limit · Fix · Dismiss | read the else branch | ✅ | all four present |
| P08027 | "Change rate limit" deep-links to that rule's row on the Rate limits page | read the `Link href={/aevinite/rate-limits#rule-${h.key}}` | ✅ | matches the `id={rule-${r.key}}` on the other page |
| P08028 | Resolving a problem is two-step (are-you-sure) so a mis-tap cannot clear a real problem | read `confirmResolve` | ✅ | two-step |
| P08029 | Resolving clears the WHOLE ×N group, matching what the server does | read `resolveError()` + `/api/admin/resolve-error` | ✅ | server resolves the group; client filters by the same key |
| P08030 | A failed Resolve puts the tile back and says why | read the `!r.ok` branch of `resolveError()` | ✅ | toast + `loadHub()` |
| P08031 | A failed "Send to Claude" restores the Fix-now buttons | read the `!r.ok` branch of `sendError()` | ✅ | `sent` entry removed |
| P08032 | A failed complaint Resolve/Reopen tells the person (it used to fail in silence) | read `setTicketStatus()` | ✅ | goes through `adminFetch` + toast |
| P08033 | A failed "Dismiss" on the Claude queue tells the person | read `dismissRequest()` | ✅ | toast + reload |
| P08034 | A failed rate-limit Allow / Dismiss / Clear / Block tells the person and reloads | read `rlAllow` `rlDismiss` `rlClear` `rlBlock` | ✅ | all four handled |
| P08035 | "Forget this" on a fixed-problem record fails loudly, not silently | read `forgetMemory()` | ✅ | toast + reload |
| P08036 | Every write from this page carries an idempotency id where the endpoint expects one | grep `X-LFH-Action-Id` on the `fix-request` and `repair` POSTs | ✅ | present on both |
| P08037 | The hands-on tools require a typed reason before they will run | read `submit()` in `RepairModal` | ✅ | refuses with a message |
| P08038 | "Delete an order" is described as tombstoned, never as erased | read the `TOOLS` entry for `delete_order` | ✅ | says "It stays in the records, tombstoned — not erased" |
| P08039 | "Void a bill" is described as keeping the invoice number on record | read the `TOOLS` entry for `void_bill` | ✅ | correct — matches the compliance rule that a sale never disappears |
| P08040 | The repair modal registers with the back-button manager | read `useAdminModal(ref, ...)` in `RepairModal` | ✅ | registered |
| P08041 | The repair modal cannot be dismissed by the backdrop while a write is in flight | read the backdrop `onClick={busy ? undefined : onClose}` | ✅ | guarded |
| P08042 | "Edit an order's time" warns that 5 AM flips the business day | read the `edit_time` hint | ✅ | present in both the tool card and the field hint |
| P08043 | "Edit an order's time" rejects an unparseable date instead of sending NaN | read `submit()`'s `isNaN(d.getTime())` guard | ✅ | guarded |
| P08044 | Picking an order for "Edit time" pre-fills its current time in the admin's own zone | read `onPickOrder` + `toLocalInput` | ✅ | round-trip is consistent |
| P08045 | Each tool refuses with a named reason when no target is picked | read `submit()`'s "Pick a table." / "Pick an order." | ✅ | both present |
| P08046 | Every tool select tells the admin when it has nothing to offer, instead of an empty dropdown | read the `<Hint>` under each picker | ✅ | all three |
| P08047 | The hands-on tools are locked until a restaurant is chosen, and say so | read the `!rid` branch | ✅ | says where the picker is |
| P08048 | A failed load of a restaurant's tables shows Retry, not an eternal "Loading…" | read `dataErr` | ✅ | Retry present |
| P08049 | The error tile's closed line shows a readable headline, never raw markup | read the `rp-detail` `isOpen ? a.detail : errorHeadline(a.detail)` | ✅ | closed = headline, open = byte-for-byte |
| P08050 | Opening a tile still shows the captured text unedited (nothing is hidden) | same line, `isOpen` branch | ✅ | verbatim |
| P08051 | A Claude run TITLE that carries a whole error page is shown as a headline, with the report still verbatim | read the session-history `errorHeadline(s.title)` and the `<pre>` | ✅ | correct |
| P08052 | "came back after the fix" only fires when the latest occurrence is NEWER than the recorded fix | read `cameBack` | ✅ | date comparison is the right way round |
| P08053 | The "Already fixed" list is collapsed by default and states plainly that it hides nothing | read the `showMemories` toggle label | ✅ | says so on the button itself |
| P08054 | A tile with no detail says so rather than rendering an empty box | read the `: <div>No further detail was recorded.</div>` branch | ✅ | present |
| P08055 | "more/less" only appears when there is more than one line to read | read the `a.detail.length > 90` guard | ✅ | guarded |
| P08056 | "Go to that panel" only appears when there is somewhere to go | read `jumpLabel()` | ✅ | returns null otherwise |
| P08057 | A guest-menu error jumps to that restaurant's own menu, not restaurant #1's | read `jumpTo()`'s `/r/${a.restaurant_slug}/menu` | ✅ | slug-scoped |
| P08058 | The complaint list sorts open first, then newest | read `shownTickets` | ✅ | correct |
| P08059 | The Claude-queue timestamp is printed in the restaurant's own timezone | read the `toLocaleString("en-IN", { timeZone: "Asia/Kolkata" ... })` | ✅ | IST, consistent with `fmtTime` |
| P08060 | No earnings, price or ₹ figure appears anywhere on the Repair hub | grep the file for `inr(`, `₹`, `revenue`, `total` | ✅ | none — matches the admin-sees-no-earnings rule |
| P08061 | `/aevinite/logs` — a failed Operations read shows Retry, never an eternal "Loading…" | read `opsErr` + `OpsTable`'s `err` branch | ✅ | Retry present |
| P08062 | `/aevinite/logs` — a failed Audit read shows Retry | read `audErr` + `AudTable` | ✅ | Retry present |
| P08063 | `/aevinite/logs` — a failed Customers read shows Retry | read `custErr` + `CustTable` | ✅ | Retry present |
| P08064 | The Operations feed must say when the server capped it, so "no more rows" is never assumed | read `loadOps()` (`limit=200`) against what `OpsTable` claims, then check the live count | ✅ | pass 1 ❌ — returns exactly 200 on this stack, at the cap, silently; fixed (item 2) |
| P08065 | The Audit (removals) feed must say when the server capped it | read `loadAud()` (`limit=200`) against `AudTable`, then check the live count | ✅ | pass 1 ❌ — returns exactly 200, at the cap, silently; fixed (item 2) |
| P08066 | A deep link with `?restaurant_id=` must not cause two full log fetches | read the seeding effect against the load effect, then count the requests (P08314) | ✅ | pass 1 ❌ — one unscoped 200-row fetch then a scoped one; after item 6 the count is 1, and it carries the restaurant id |
| P08067 | A deep link with `?level=error` lands on the Errors filter, not All | read the `level` `useState` initializer + the seeding effect | ✅ | both paths seed it |
| P08068 | A deep link with `?q=` lands with the search box filled | read the seeding effect | ✅ | seeds `q` |
| P08069 | Typing in the search box is debounced; clicking a FILTER is not | read `qDebounced` (300 ms) and the deps of `loadOps` | ✅ | only `q` is debounced |
| P08070 | Switching tab clears the previous tab's rows so stale rows never appear under the new heading | read the tab `useEffect`'s `setOps(null); setCust(null); setAud(null)` | ✅ | cleared |
| P08071 | The restaurant filter scopes BOTH log tabs server-side (never a client-side slice of everything) | read the `&restaurant_id=` in `loadOps` / `loadAud` / `loadCust` | ✅ | server-side on all three |
| P08072 | Search is sent to the server, not applied to an already-truncated page of rows | read the `&q=` in `loadOps` / `loadAud` | ✅ | server-side |
| P08073 | The removals-type chips are counted over the WHOLE arrived feed, so a chip's count never changes when tapped | read `audChips = AUDITSORT.kindCounts(aud || [], ...)` | ✅ | counted from `aud`, not from the filtered view |
| P08074 | A type chip that has gone from the feed cannot strand the admin on an empty list | read `audKindSafe` | ✅ | falls back to "All" |
| P08075 | The activity-type chips follow the same rule | read `opsChips` / `opsGroupSafe` | ✅ | same shape |
| P08076 | Removal kind labels are DERIVED from the one shared map, never re-listed on this page | read `REMOVAL_KIND` built from `KIND_LABEL` / `KIND_ICON` | ✅ | derived |
| P08077 | Removal reason labels come from the shared `auditsort` module | read `REMOVAL_REASON = AUDITSORT.REASON_LABEL` | ✅ | shared |
| P08078 | Sort options come from the shared module, so the admin, owner and manager offer the same sorts | read `AUDITSORT.SORTS` / `ACTIVITY_SORTS` | ✅ | shared |
| P08079 | The "logs are getting full" banner only appears at/above the SERVER's threshold, never a local guess | read `threshold` coming from `/api/admin/oplog/cleanup` | ✅ | server-supplied |
| P08080 | The cleanup banner is not a permanent warning bar — it disappears once the log is under the threshold | read `full = count !== null && count >= threshold` | ✅ | conditional; passes the don't-cry-wolf test |
| P08081 | A failed count read must not draw the banner | read `count !== null` in `full` | ✅ | null ⇒ no banner |
| P08082 | Cleanup is confirmed in a modal that names the window, the scope and what is NOT touched | read `CleanupModal` | ✅ | names days, restaurant/all, and "Bills and customer records are never touched" |
| P08083 | The cleanup modal registers with the back-button manager | read `useAdminModal(ref, "admin-logs-cleanup", onCancel)` | ✅ | registered |
| P08084 | The cleanup modal cannot be dismissed mid-delete | read the backdrop `onClick={busy ? undefined : onCancel}` | ✅ | guarded |
| P08085 | A cleanup reports the REAL number removed, not a guess | read `runCleanup()` → `r.data.removed` | ✅ | server count |
| P08086 | A failed cleanup says so instead of quietly closing | read the `else` of `runCleanup()` | ✅ | error toast |
| P08087 | After a cleanup the count and the visible log both refresh | read `loadCount()` + `loadOps()` in `runCleanup()` | ✅ | both |
| P08088 | The audit-retention control reverts on a failed save, so the screen never shows a window the database refused | read `saveAuditYears()` | ✅ | restores `prev` and says so |
| P08089 | The audit-retention control is disabled until the current value has loaded | read `disabled={auditYears == null}` | ✅ | disabled |
| P08090 | The retention options come from the server, with a sane fallback | read `auditYearOpts` | ✅ | `auditYearOptions` from `/api/admin/settings`, else `[1,3,5,7,10]` |
| P08091 | Choosing a short retention warns that the removal trail for older bills would be lost | read the note under "Keep the Audit for" | ✅ | warns below 8 years |
| P08092 | The retention control is styled as a policy control, not as a warning | read its card (no danger border/background) | ✅ | neutral |
| P08093 | A resolved error stops rendering red and offers Reopen | read `isResolved` / `showRed` in `OpsTable` | ✅ | correct |
| P08094 | `seen_at` (the bell) never drives the log's row colour | grep `seen_at` in `OpsTable` | ✅ | not used for colour |
| P08095 | Marking resolved flips the whole repeat-group locally the same way the server does | read `sameGroup()` in `markResolved` | ✅ | panel+action+detail+restaurant |
| P08096 | A failed Mark-resolved reloads rather than leaving a wrong row on screen | read the `!r.ok` branch | ✅ | `loadOps()` |
| P08097 | Mark-resolved and Send-to-Claude buttons do not also open the row's detail popup | read the `e.stopPropagation()` on both | ✅ | stopped |
| P08098 | Every log row is openable by keyboard, not only by mouse | read `role="button" tabIndex={0} onKeyDown` on the rows | ✅ | Enter and Space |
| P08099 | A tablet row shows WHOSE PIN unlocked it, and flags a PIN shared by more than one manager | read `isTabletPin` / `pinShared` | ✅ | amber chip when ambiguous |
| P08100 | An action the ADMIN performed inside a restaurant's panel is MARKED, not stripped | read the `ADMIN_VIEW_ACTOR_ID` chip | ✅ | marked "Admin" |
| P08101 | Row detail is shown in plain English for non-error rows, and raw for errors | read `det = isErr ? a.detail : formatActionDetail(...)` | ✅ | correct split |
| P08102 | A long detail is truncated on the row but the full text is one tap away | read the 60-char slice + `LogDetailModal` | ✅ | full text in the popup |
| P08103 | Every row names its restaurant when "All restaurants" is selected | read the `a.restaurant_name` line | ✅ | present on ops, audit and customer rows |
| P08104 | The removals table names WHAT, WHY and WHO on every row | read `AudTable`'s `bits` / `reason` / actor line | ✅ | all three |
| P08105 | A removal with no recorded reason says "no reason recorded" rather than showing blank | read the `reason` fallback | ✅ | present |
| P08106 | Opening a removal shows the full story via the shared modal, admin-only restore included | read `RemovalDetailModal base="/api/admin/audit"` | ✅ | shared with the owner's read-only version |
| P08107 | Restoring from a removal refreshes the list | read `onRestored={loadAud}` | ✅ | refreshes |
| P08108 | The Customers tab shows no money at all | grep `CustTable` for ₹ / `inr(` | ✅ | order and call counts only |
| P08109 | The Customers tab rolls up orders and calls per member without an N+1 lookup | read the `byMember` map build | ✅ | two linear passes |
| P08110 | An empty Customers tab, empty blocklist and empty log each say something rather than showing a blank | read the three empty states | ✅ | all three worded |
| P08111 | `/aevinite/rate-limits` — a FAILED load must never render the green "No limits reached right now." | read `load()`: the `!r.ok` path only toasts, then the render falls through to `rl-clear` | ✅ | pass 1 ❌; fixed (item 7) — an error state with Retry replaces every green all-clear on this page |
| P08112 | `/aevinite/rate-limits` — a FAILED load must not render an empty "The limits" card as though there are no limits | same path | ✅ | fixed (item 7) |
| P08113 | `/aevinite/rate-limits` — a FAILED load must not render "No devices are blocked." | same path | ✅ | fixed (item 7) |
| P08114 | `/aevinite/rate-limits` — a FAILED load must not render "No requests right now." | same path | ✅ | fixed (item 7) |
| P08115 | An `admin_login` hit must offer the "Let them try again" action the page's own note promises | read the `h.key === "admin_login"` branch against the `rl-note` text at the bottom of "The limits" | ✅ | pass 1 ❌ — the note promised "Block / Let-them-retry", only Block existed; fixed (item 8) |
| P08116 | "Let them try again" calls the server's `clear` action, the same one the Repair hub uses | read the new handler against `app/api/admin/rate-limits/route.ts` `action === "clear"` | ✅ | same action, same wording |
| P08117 | A rule edit is only offered as saveable when the numbers actually changed | read `dirty` | ✅ | compares draft to row |
| P08118 | A rule edit that the server refuses reverts the screen and reloads | read `saveRule()`'s `else` | ✅ | toast + `load()` |
| P08119 | The max-count input cannot send a negative or fractional value | read the `onChange` clamp | ✅ | `Math.max(0, Math.trunc(...))` |
| P08120 | The window input cannot send zero or a fraction | read the `onChange` clamp | ✅ | `Math.max(1, Math.trunc(...))` |
| P08121 | The input bounds match what the server will accept | compare `min`/`max` on the two inputs with the route's `0..100000` / `1..86400` | ✅ | identical |
| P08122 | The on/off toggle is a real pressed-state control for a screen reader | read `aria-pressed={r.enabled}` | ✅ | present |
| P08123 | The admin's own login limit is deliberately NOT an editable number, and the page explains why | read the `rl-note` row | ✅ | explains the lock-out risk |
| P08124 | "Change limit" from a hit opens the folded section BEFORE trying to scroll to the row | read `jumpToRule()` | ✅ | opens, then scrolls on the next frame |
| P08125 | Folding a section is remembered per device and never costs a request | read the `lfh_rl_collapsed` localStorage read/write | ✅ | pure UI |
| P08126 | A corrupt `lfh_rl_collapsed` value cannot break the page | read the `try/catch` around `JSON.parse` | ✅ | swallowed |
| P08127 | Section headings are operable by keyboard and announce their open/shut state | read `secHead()`'s `role`, `tabIndex`, `aria-expanded`, `onKeyDown` | ✅ | Enter and Space |
| P08128 | Allow / Dismiss remove the row optimistically and put it back if the server refuses | read `allowHit` / `dismissHit` | ✅ | both reload on failure |
| P08129 | Block does NOT remove the row until the server confirms | read `blockHit()` | ✅ | removal is inside `if (res.ok)` — correct, since the server can refuse to block your own device |
| P08130 | Unblocking a device also clears any unblock request from that device | read `unblock()` | ✅ | both lists updated |
| P08131 | Approving a request lifts the block AND clears the request | read `approveRequest()` | ✅ | both |
| P08132 | Denying a request clears the request and leaves the block in place | read `denyRequest()` | ✅ | correct |
| P08133 | A repeat asker is flagged ("asked N× today") only when the count is trustworthy | read `(q.asked_today ?? 0) > 1` against the route's `null`-on-failed-count | ✅ | null ⇒ no chip, never a wrong "1×" |
| P08134 | "Fix" on a hit names the limit by its human label, not its database key | read `fixHit()`'s note body | ✅ | uses `labelFor(h.key)` |
| P08135 | "Fix" carries an idempotency id | read the `X-LFH-Action-Id` header on `fixHit` | ✅ | present |
| P08136 | A platform-wide hit (the zero-uuid restaurant) is not tagged to a real restaurant | read the `h.restaurant_id !== "000…000"` guard in `fixHit` | ✅ | sends null |
| P08137 | The page links to the Problems board so the two screens are never a dead end for each other | read the sub-heading `Link` to `/aevinite/repair#rate-limits` | ✅ | present both ways |
| P08138 | The hit count chip reads "N / max per window" in words a person understands | read `perLabel()` | ✅ | hours / min / sec |
| P08139 | No earnings or ₹ figure appears anywhere on the Rate limits page | grep for `₹` / `inr(` | ✅ | none |
| P08140 | Every destructive-sounding button says what it will do on hover | read the `title` on Block / Unblock / Allow / Deny | ✅ | all titled |
| P08141 | `/aevinite/health` — a failed health read must not leave "Checking…" on screen for ever | read the `h === null` branch against `err` | ✅ | pass 1 ❌ — showed the error AND "Checking…"; fixed (item 9) with a "couldn't check" state + Retry |
| P08142 | A database ping failure must NOT fall through to the normal render (which reads fields the API omits) | read the `!h.dbOk` early branch | ✅ | dedicated card; no crash at the one moment the page matters |
| P08143 | A database ping failure states the latency it waited and offers Refresh | read the same card | ✅ | both |
| P08144 | A failed restaurants read says "unreadable", never a reassuring "0 live" | read the `restaurantsError` pill | ✅ | correct |
| P08145 | A failed staff read says "unreadable", never a reassuring "0 online" | read the `staffError` pill | ✅ | correct |
| P08146 | Zero staff online is NOT drawn as a warning (it is normal overnight) | read the staff pill's class expression | ✅ | neutral, not amber |
| P08147 | A failed issues read says "issue feed unreachable" rather than "0 open issues" | read the `issuesFeedWired` pill | ✅ | correct |
| P08148 | A failed 3D check says "unknown, not zero" | read the `broken3d === null` branch | ✅ | says so in those words |
| P08149 | The 3D list names WHICH RESTAURANT each dish belongs to | read the `broken3d.dishes` row render | ✅ | pass 1 ❌ — only the dish title and the missing file; fixed (item 10) using the panels-health rows already in hand (no extra request) |
| P08150 | The 3D list says when it is showing only the first N of a longer list | read the `count > dishes.length` line | ✅ | present |
| P08151 | Row-count estimates are labelled as ESTIMATES, with the reason a real count is not used | read the hint under "Row count estimates" | ✅ | explains the scan cost |
| P08152 | A failed estimates read says so instead of drawing an empty table | read `tableEstimatesError` | ✅ | correct |
| P08153 | The realtime section shows a HOST only — never a key or a token | read the `realtime.configuredHost` render and the route that supplies it | ✅ | `new URL(...).host` only |
| P08154 | The panels grid renders a per-restaurant connectivity row for every live restaurant | read the `pd.rows.map` | ✅ | one row each |
| P08155 | A quiet OWNER panel is rendered neutral, not red (owners do not sit signed in) | read `ownerQuiet` in `PanelCell` | ✅ | neutral |
| P08156 | The panels banner must not be up on every load | read what it counts, then look at it on screen | ✅ | pass 1 ❌ — it read "23 enabled panels quiet or never seen — a device or login may be down" on every load, and 20 of the 23 were simply closed restaurants; fixed (item 14): the warning is now for panels never signed into, the quiet ones are stated as the ordinary fact they are |
| P08157 | A failed panels read shows Retry and does not blank the diagnostics above it | read `pErr` and the `Promise.allSettled` split | ✅ | independent |
| P08158 | A suspended restaurant is labelled as such in the panels grid | read the `!row.active` line | ✅ | "suspended" |
| P08159 | The panels grid scrolls sideways inside its own card rather than pushing the page wide | read the `overflowX: "auto"` + `minWidth: 560` | ✅ | contained |
| P08160 | Auto-refresh on this page is the shared 60 s active-only helper, never a blind timer | read `useActiveAutoRefresh(load, 60000)` | ✅ | stops when hidden or idle |
| P08161 | No earnings figure appears on System health | grep for `₹` / `revenue` | ✅ | none |
| P08162 | The health page never claims a check ran that did not | read each `*Error` branch | ✅ | every failure is named |
| P08163 | Latency tiers are stated in the same units the pill shows | read `latencyTier()` and the pill text | ✅ | ms in both |
| P08164 | The health page's two reads are independent, so one failing never blocks the other | read `Promise.allSettled` | ✅ | correct |
| P08165 | `/aevinite/health` — the page states when it last checked | read the `timeAgo(h.checkedAt)` sub-line | ✅ | present |
| P08166 | `/aevinite/health` — the "3D dishes with no model file" section is read-only and raises no alert | read the section and its comment | ✅ | no write, no notification |
| P08167 | `/aevinite/health` — the Refresh button is disabled while a check is in flight | read `disabled={loading}` | ✅ | disabled |
| P08168 | `/aevinite/health` — the panels grid tells the admin what "Idle" and "Quiet" mean | read the `title` on `PanelCell` | ✅ | "Last active …" / "Panel disabled" |
| P08169 | `/aevinite/staff-online` — a slow first request cannot be overtaken by a later one | read the `seq` latest-wins guard | ✅ | stale responses dropped |
| P08170 | `/aevinite/staff-online` — a failed load shows the reason and a Retry | read the `err` card | ✅ | present |
| P08171 | `/aevinite/staff-online` — the "seen Xs ago" labels re-tick without re-fetching | read the 15 s `force` interval | ✅ | display only, no request |
| P08172 | `/aevinite/staff-online` — the re-tick interval is cleared on unmount | read the `return () => clearInterval(id)` | ✅ | cleared |
| P08173 | `/aevinite/staff-online` — the page does NOT auto-poll, and says so where the admin can see it | read the `so-snap` badge | ✅ | says "Manual — press Refresh" |
| P08174 | The "Manual — press Refresh" badge must not be drawn in warning colours when nothing is wrong | read the `so-snap` CSS, then look at it in both skins | ✅ | pass 1 ❌ — a permanently amber pill on every load, in both skins; fixed (item 11) → neutral grey |
| P08175 | Filtering is done over the already-fetched list — never a second request per keystroke | read the `visible` filter | ✅ | client-side over a ≤300-row list |
| P08176 | The role chips are combinable and "All" clears them | read `toggleRole` and the All button | ✅ | correct |
| P08177 | A "Clear filters" control appears only when something is filtered | read `filtered` | ✅ | conditional |
| P08178 | The count line distinguishes "N of M" (filtered) from "M online" (unfiltered) | read the `cmd-sec` line | ✅ | correct |
| P08179 | An empty result under filters says so differently from "nobody is online" | read the two empty branches | ✅ | two distinct messages |
| P08180 | The active filter chip's ink follows its fill (no white-on-white "All" chip) | read `chip(on, color)` | ✅ | `--text` fill takes `--bg` ink |
| P08181 | Sorting is restaurant → role → name, so the same person is always in the same place | read the `.sort(...)` chain | ✅ | stable ordering |
| P08182 | The "bright pulse" only means seen in the last minute, and the card says which it is | read `hot` and the status line | ✅ | "Active now" vs "Online" |
| P08183 | A person with no restaurant renders "—" rather than an empty cell | read `u.restaurantName || "—"` | ✅ | present |
| P08184 | The search box has a label for a screen reader | read `aria-label="Search online staff"` | ✅ | present |
| P08185 | No earnings figure appears on Staff online | grep for `₹` | ✅ | none |
| P08186 | `/aevinite/usage` — a failed load shows the reason and a Retry | read the `err` line | ✅ | present |
| P08187 | `/aevinite/usage` — the four headline numbers must not sit on "…" for ever after a failed load | read the `STATS` values against `err` | ✅ | pass 1 ❌; fixed (item 12) → "—" plus the visible error line |
| P08188 | `/aevinite/usage` — the share bar is relative to the busiest restaurant, and says so | read `max` and the hint | ✅ | stated in the hint |
| P08189 | `/aevinite/usage` — a restaurant with zero orders draws no bar (rather than a misleading sliver) | read the `r.orders30d > 0 ? 3 : 0` floor | ✅ | correct |
| P08190 | `/aevinite/usage` — the page states plainly that order volume is a PROXY for cost, not a bill | read the sub-heading and the footer note | ✅ | both say so |
| P08191 | `/aevinite/usage` — no earnings appear, only counts | grep for `₹` / `revenue` | ✅ | none — the page says so too |
| P08192 | `/aevinite/usage` — numbers are tabular-aligned so a column can be compared down the page | read `fontVariantNumeric: "tabular-nums"` | ✅ | on every numeric cell |
| P08193 | `/aevinite/usage` — auto-refresh is the shared active-only 60 s helper | read `useActiveAutoRefresh` | ✅ | correct |
| P08194 | `/aevinite/usage` — a stale table is dimmed while refreshing rather than blanked | read `opacity: loading && d ? 0.6 : 1` | ✅ | correct |
| P08195 | `/aevinite/usage` — an empty platform says "No restaurants yet." | read the empty branch | ✅ | present |
| P08196 | `/aevinite/issues` — the retired Tickets URL still lands on the complaints section | read the redirect target | ✅ | `/aevinite/repair#complaints` |
| P08197 | `/aevinite/issues` — the redirect is a server redirect, so a bookmark never renders a blank client page | read the `next/navigation` `redirect()` in a server component | ✅ | server component |
| P08198 | `/aevinite/issues` — the anchor it points at actually exists on the Repair hub | grep `id="complaints"` in the repair page | ✅ | exists |
| P08199 | `/aevinite/attention` — the retired At-risk URL still lands on the at-risk section | read the redirect target | ✅ | `/aevinite/repair#at-risk` |
| P08200 | `/aevinite/attention` — the anchor it points at actually exists on the Repair hub | grep `id="at-risk"` in the repair page | ✅ | exists |
| P08201 | EGRESS · every read this territory makes names its columns (no `select("*")`) | read each endpoint the eight pages call | ✅ | all eight name columns |
| P08202 | EGRESS · every list read has a `.limit()` | read `oplog` (200), `audit` (200), `rate-limits` (50), `staff-online` (300), `panels-health` (3000), `health` staff (5000) / dishes (200) | ✅ | all bounded |
| P08203 | EGRESS · no page in this territory polls faster than the 60 s backstop | grep every `setInterval` / `useActiveAutoRefresh` in the eight files | ✅ | health 60 s, usage 60 s, staff-online none, the rest click-to-refresh |
| P08204 | EGRESS · the only sub-60 s timer in the territory fetches nothing | read the staff-online 15 s tick | ✅ | re-renders labels only |
| P08205 | EGRESS · auto-refresh stops when the tab is hidden or the admin walks away | read `useActiveAutoRefresh`'s visibility + idle logic | ✅ | both |
| P08206 | EGRESS · the Repair hub does not poll at all — it is click-to-refresh | grep for a timer in `repair/page.tsx` | ✅ | none |
| P08207 | EGRESS · the Logs page does not poll at all | grep for a timer in `logs/page.tsx` | ✅ | none, apart from the 300 ms search debounce |
| P08208 | EGRESS · the Rate limits page does not poll at all | grep for a timer in `rate-limits/page.tsx` | ✅ | none |
| P08209 | EGRESS · the log-volume banner uses a HEAD count, so no rows cross the wire for it | read `/api/admin/oplog/cleanup` GET | ✅ | `head: true` |
| P08210 | EGRESS · the row-count panel uses planner estimates, not `COUNT(*)` on hot tables | read the `lfh_admin_table_estimates` RPC call | ✅ | estimates |
| P08211 | EGRESS · restaurant NAMES are fetched once into a map, never once per row | read the `nameById` builds in `oplog`, `staff-online`, `rate-limits` | ✅ | no N+1 anywhere |
| P08212 | EGRESS · restaurant scoping is applied on the SERVER, not by slicing a platform-wide page | read the `restaurant_id` params on `oplog`, `audit`, `custlog`, `cleanup` | ✅ | server-side |
| P08213 | EGRESS · the Repair hub's own restaurant scoping acts on rows already in hand (no extra request) | read `groups` / `shownRlHits` / the item-3 filters | ✅ | pure client filter, no refetch |
| P08214 | EGRESS · naming the restaurant on a 3D-broken dish costs no extra request | read the item-10 fix | ✅ | reuses the panels-health rows already fetched |
| P08215 | EGRESS · a deep link into the Logs page fires ONE fetch per tab, not two | read the `seeded` gate added by item 6 | ✅ | one |
| P08216 | EGRESS · the Repair hub's seven parallel reads are one round of requests, not a waterfall | read the `Promise.all` in `loadHub` | ✅ | parallel |
| P08217 | EGRESS · nothing in this territory reads a table it is not scoped to by restaurant or by status | read every query in the eight endpoints | ✅ | admin screens are platform-wide by design, and every read is status- or time-bounded |
| P08218 | EGRESS · no analytics recompute happens on opening any of these pages | grep for a cache/rollup call | ✅ | none — these are diagnostics, not dashboards |
| P08219 | EGRESS · a search term cannot widen a query to the whole table | read `safeSearch()` in `/api/admin/oplog` | ✅ | PostgREST meta-characters escaped, `*` included |
| P08220 | EGRESS · a malformed restaurant id is rejected before it reaches a uuid column | read the UUID guards in `oplog`, `cleanup`, `rate-limits` | ✅ | all guarded |
| P08221 | NEVER A REASSURING ZERO · the Repair problems board | see P08001 | ✅ | fixed (item 1) |
| P08222 | NEVER A REASSURING ZERO · the Repair rate-limit section | see P08002 | ✅ | fixed (item 1) |
| P08223 | NEVER A REASSURING ZERO · the Rate limits page's four sections | see P08111–P08114 | ✅ | fixed (item 7) |
| P08224 | NEVER A REASSURING ZERO · health's restaurants / staff / issues / 3D pills | see P08144–P08148 | ✅ | already correct |
| P08225 | NEVER A REASSURING ZERO · a capped feed must not read as a complete one | see P08009, P08064, P08065 | ✅ | fixed (item 2) |
| P08226 | NEVER A REASSURING ZERO · the usage headline numbers after a failed read | see P08187 | ✅ | fixed (item 12) |
| P08227 | NEVER HIDE AN ERROR · nothing in this territory filters error rows out of the feed | grep the eight pages for a client-side drop of `level === "error"` | ✅ | none |
| P08228 | NEVER HIDE AN ERROR · "Resolve" clears the board but the full log still shows the row | read `?unresolved=1` on Repair vs its absence on Logs | ✅ | correct split |
| P08229 | NEVER HIDE AN ERROR · the "Already fixed" record only stops a DUPLICATE Claude ticket | read the section's own note and `memoryFor` | ✅ | a recurrence still lands on the board |
| P08230 | NEVER HIDE AN ERROR · a headline is a label; the full captured text is always one tap away | read `errorHeadline` usage on both pages | ✅ | verbatim on expand |
| P08231 | NEVER HIDE AN ERROR · the errlog network-noise filter is left exactly as it is | grep this territory for any change to it | ✅ | untouched — it is deliberate |
| P08232 | NEVER HIDE AN ERROR · nothing added in this branch swallows a rejected promise | read every new `catch` | ✅ | each one surfaces a message |
| P08233 | DON'T CRY WOLF · no banner in this territory is up on every load | list every persistent bar/pill and its condition | ✅ | after item 11, all are conditional |
| P08234 | DON'T CRY WOLF · the log-volume banner is threshold-gated | see P08080 | ✅ | conditional |
| P08235 | DON'T CRY WOLF · the panels attention banner is count-gated | see P08156 | ✅ | conditional |
| P08236 | DON'T CRY WOLF · a section icon must not glow red for rows the admin cannot see | see P08017 | ✅ | fixed (item 4) |
| P08237 | DON'T CRY WOLF · an empty state that is GOOD news is green; an empty state caused by a FAILURE is not | read the three all-clear blocks after items 1 and 7 | ✅ | separated |
| P08238 | DON'T CRY WOLF · the "Manual — press Refresh" note is information, not a warning | see P08174 | ✅ | fixed (item 11) |
| P08239 | DON'T CRY WOLF · a suspended restaurant's quiet panels do not raise the attention count | read the `attention` reduce in `panels-health` | ✅ | excluded |
| P08240 | DON'T CRY WOLF · the owner panel's quiet state does not raise the attention count | same reduce | ✅ | excluded |
| P08241 | PLAIN ENGLISH · no raw database action code reaches the screen | read `actLabel()` usage on both log surfaces | ✅ | every action label passes through it |
| P08242 | PLAIN ENGLISH · no raw rate-limit key reaches the screen | see P08023 | ✅ | `rlLabel` / `labelFor` |
| P08243 | PLAIN ENGLISH · no raw removal kind reaches the screen | read `REMOVAL_KIND` | ✅ | shared labels |
| P08244 | PLAIN ENGLISH · no raw reason code reaches the screen | read `REMOVAL_REASON` | ✅ | shared labels |
| P08245 | PLAIN ENGLISH · a database error message is turned into a sentence before the admin reads it | read `adminFail()` on the routes and `errorHeadline` on the pages | ✅ | both |
| P08246 | PLAIN ENGLISH · tap-batch details are formatted, not dumped | read `formatActionDetail` in `OpsTable` | ✅ | formatted |
| P08247 | PLAIN ENGLISH · the words on the button say what will happen, not what the code does | read every button label in the eight files | ✅ | "Let them try again", "Allow (reset)", "Block this device", "Forget this" |
| P08248 | PLAIN ENGLISH · time is shown as "x ago" or as a real date, never as an ISO string | grep for a bare `.created_at` render | ✅ | `timeAgo` / `toLocaleString` everywhere |
| P08249 | PLAIN ENGLISH · nothing on these screens tells the admin to open a file or a log path | grep for a path-looking string in rendered text | ✅ | none |
| P08250 | PLAIN ENGLISH · the sub-headings say WHICH restaurants a section covers | read every `rp-sec-h` / `rl-sec` caption | ✅ | true after items 3 and 4 |
| P08251 | ONE NAME · the screen is called "Audit & logs", matching the owner console and the manager panel | read the `h1` on `/aevinite/logs` | ✅ | identical wording |
| P08252 | ONE NAME · a removal type reads the same here as on the owner console | read `KIND_LABEL` being the single source | ✅ | derived, not copied |
| P08253 | ONE NAME · a rate limit reads the same on the Repair hub as on the Rate limits page | compare `rlLabel` and `labelFor` | ✅ | both resolve from the rule row |
| P08254 | ONE NAME · "a problem" means the same thing on the Repair hub as on the dashboard | read the `unresolved=1` definition on both | ✅ | unresolved, any age |
| P08255 | ONE NAME · the sort options are the same three surfaces' sort options | read `AUDITSORT` | ✅ | one module |
| P08256 | ONE NAME · "online" means the same 3 minutes on Staff online, health and the dashboard | read `ONLINE_MS` and the health route's 180 000 | ✅ | same window |
| P08257 | BACK BUTTON · the log-cleanup modal registers a back-stack layer | read `useAdminModal` | ✅ | registered |
| P08258 | BACK BUTTON · the repair tool modal registers a back-stack layer | read `useAdminModal` | ✅ | registered |
| P08259 | BACK BUTTON · the log-row detail popup registers a back-stack layer | read `LogDetailModal` | ✅ | registered (shared component) |
| P08260 | BACK BUTTON · the removal-detail popup registers a back-stack layer | read `RemovalDetailModal` | ✅ | registered (shared component) |
| P08261 | BACK BUTTON · nothing added in this branch hand-rolls pushState/popstate | grep the diff | ✅ | none |
| P08262 | BACK BUTTON · no new modal or drawer was added by this branch | read the diff | ✅ | none added |
| P08263 | A TAP NEVER VANISHES · every button in this territory either acts, refuses visibly, or shows a busy state | walk every `onClick` in the eight files | ✅ | true after items 1, 7, 9 and 12 |
| P08264 | A TAP NEVER VANISHES · no `onClick` returns silently on a falsy guard | grep for a bare `if (!x) return;` inside a handler | ✅ | `runCleanup`'s `if (!pending) return` is unreachable (the modal only renders when pending) |
| P08265 | A TAP NEVER VANISHES · a Save with nothing changed is disabled rather than silently doing nothing | read `disabled={!dirty}` | ✅ | disabled |
| P08266 | A TAP NEVER VANISHES · a Refresh while refreshing is disabled rather than queueing | read `disabled={loading}` / `disabled={refreshing}` | ✅ | on all five refresh buttons |
| P08267 | A TAP NEVER VANISHES · "Change limit" from a hit always ends somewhere visible | see P08124 | ✅ | opens the section first |
| P08268 | A TAP NEVER VANISHES · every optimistic removal is undone when the server refuses | see P08128, P08030, P08034 | ✅ | all reload |
| P08269 | A TAP NEVER VANISHES · the failure branch shows the SERVER's reason when it gave one | read the `r.error ||` fallbacks | ✅ | server text first |
| P08270 | A TAP NEVER VANISHES · a code-branch reads the server's reason CODE, never its prose | grep for a comparison against an English error string | ✅ | none — all branches are on `ok` |
| P08271 | A TAP NEVER VANISHES · no promise in these handlers is left unresolved | read every `async` handler for a path with no settle | ✅ | none |
| P08272 | A TAP NEVER VANISHES · a busy button says "Saving…" / "Deleting…" / "Working…" rather than freezing | read the three busy labels | ✅ | present |
| P08273 | NO EARNINGS · nothing in this territory renders a price, a total or a ₹ sign | grep all eight files | ✅ | the single `inr()` import in `logs` is used only for a removal's recorded amount, which the audit requires |
| P08274 | NO EARNINGS · the removal row's amount is part of the deletion record, not a revenue read-out | read the `AudTable` `bits` line and `docs/COMPLIANCE-GUARDRAILS.md` | ✅ | it is what was removed — required by the audit |
| P08275 | NO EARNINGS · the usage page states in words that it shows no earnings | read the sub-heading | ✅ | says so |
| P08276 | NO SECRETS · no key, token or connection string can reach these screens | read the health route's host-only extraction | ✅ | host only |
| P08277 | REJECTED IDEAS · nothing in this branch re-adds a second/deep health check (R18) | read the diff | ✅ | none |
| P08278 | REJECTED IDEAS · nothing in this branch adds Sentry or any third-party error service | read the diff | ✅ | none |
| P08279 | REJECTED IDEAS · nothing in this branch reports the queued ban / manager-PIN settings as missing | read the findings file | ✅ | not reported — they are deliberately unbuilt |
| P08280 | REJECTED IDEAS · nothing in this branch reports the silent backup stacks as a fault | read the findings file | ✅ | not reported |
| P08281 | REJECTED IDEAS · nothing in this branch makes a test able to raise an alert | read the guard script and the diff | ✅ | no notification path touched |
| P08282 | REJECTED IDEAS · nothing in this branch unfilters the errlog network noise | read the diff | ✅ | untouched |
| P08283 | REJECTED IDEAS · every changed line was checked against `docs/REJECTED-IDEAS.md` before changing | re-read the file against the twelve items | ✅ | none of the twelve appears there |
| P08284 | REJECTED IDEAS · no change in this branch alters a rule written in `CLAUDE.md` | read the diff against the rule list | ✅ | none |
| P08285 | BOTH SKINS · every colour used in this territory is a token, not a fixed hex that only works in dark | grep the eight files for `#` literals and check each has a token fallback or is skin-safe | ✅ | the role colours on Staff online are deliberate brand hues used as fills with their own ink |
| P08286 | BOTH SKINS · the light console must not render white ink on a near-white pill | read `chip()` on Staff online | ✅ | already corrected; still correct |
| P08287 | BOTH SKINS · a status colour in the Claude session history must not be an undeclared token | read `statusInfo` | ✅ | `var(--muted)`, declared |
| P08288 | BOTH SKINS · nothing added by this branch introduces a token nobody declared | grep the diff for `var(--` and check each name exists | ✅ | all declared |
| P08289 | OFFLINE · these are admin diagnostics; they are not in the service worker's data families and are not expected to open offline | read `public/sw.js` `DATA_PATHS` | ✅ | deliberate — the admin console is not an on-the-floor panel |
| P08290 | OFFLINE · nothing in this branch adds a staff write that would need the outbox | read the diff | ✅ | no new write endpoint |
| P08291 | NO SILENT OVERWRITES · the only editable values here (a rate-limit rule, the audit window) revert on a refused save | see P08118 and P08088 | ✅ | both |
| P08292 | NO SILENT OVERWRITES · no new call site in this branch needed a clash `expect` block | read the diff against `scripts/verify-clash-coverage.mjs` | ✅ | none added |
| P08293 | SETTINGS COLUMNS · nothing in this branch adds a column to `settings` | read the diff | ✅ | no migration at all |
| P08294 | MIGRATIONS · this branch adds no migration | `ls supabase/migrations` against `origin/main` | ✅ | none |
| P08295 | WHERE IT LIVES · every row in the findings file names panel → screen → what he would SEE | read `T17-findings.md` | ✅ | all twelve |
| P08296 | DECISION FORMAT · every item in the chat report has Where / What / If yes / If no / Effort / Risk | read the report | ✅ | all |
| P08297 | SAFE WORDING · no phase, command description, commit message or report line uses attack/defence framing | re-read this ledger, the findings file and the commit messages | ✅ | product-correctness language throughout |
| P08298 | SAFE WORDING · nothing in this run verified anything by trickery — code reading and normal signed-in use only | read the band C method | ✅ | one cached sign-in, normal navigation |
| P08299 | GATES · `npm run typecheck` and `npm run lint` pass on this branch | run both | ✅ | both clean |
| P08300 | GATES · `verify:audit`, `verify:xray`, `verify:pointers`, `verify:open-request-guard` and the new `verify:admin-health` all pass | run the five | ✅ | all pass |
| P08301 | The admin console signs in once for the whole run and that session is reused for every phase below | `scripts/sweep/login.mjs` `adminHeaders()` / a single cached cookie; never a fresh POST per page | ✅ | one sign-in, cached to disk |
| P08302 | `/aevinite/health` renders without a client-side crash on port 4117 | headless, load the page, assert no page error and the `h1` reads "System health" | ✅ | clean |
| P08303 | `/aevinite/logs` renders without a client-side crash | headless, assert the `h1` reads "Audit & logs" | ✅ | clean |
| P08304 | `/aevinite/repair` renders without a client-side crash | headless, assert the `h1` reads "Repair & support" | ✅ | clean |
| P08305 | `/aevinite/rate-limits` renders without a client-side crash | headless, assert the `h1` reads "Rate limits" | ✅ | clean |
| P08306 | `/aevinite/staff-online` renders without a client-side crash | headless, assert the `h1` reads "Staff online" | ✅ | clean |
| P08307 | `/aevinite/usage` renders without a client-side crash | headless, assert the `h1` reads "Usage & cost" | ✅ | clean |
| P08308 | `/aevinite/issues` redirects to the complaints section | headless, follow the redirect, assert the landing URL | ✅ | `/aevinite/repair#complaints` |
| P08309 | `/aevinite/attention` redirects to the at-risk section | headless, follow the redirect, assert the landing URL | ✅ | `/aevinite/repair#at-risk` |
| P08310 | None of the six pages logs a console ERROR on load | headless, collect console messages per page | ✅ | none |
| P08311 | None of the six pages logs a React key or hydration warning on load | headless, collect console warnings | ✅ | none |
| P08312 | Every request each page fires is answered 2xx when signed in | headless, record the network list per page | ✅ | all 200 |
| P08313 | No page fires the same request twice on one load | headless, count requests per URL per load | ✅ | one each after item 6 |
| P08314 | `/aevinite/logs` with `?restaurant_id=<French House>` fires exactly ONE oplog request | headless, count `/api/admin/oplog` calls | ✅ | pass 1 ❌ (two, the second scoped); one after item 6 |
| P08315 | `/aevinite/logs` with `?level=error` lands with the Errors button active | headless, assert the `.active` class is on the Errors button | ✅ | active |
| P08316 | `/aevinite/logs` with `?q=printer` lands with the search box filled | headless, read the input value | ✅ | filled |
| P08317 | Switching to the Audit tab fires the audit read and no other | headless, click "Audit · removals", record the network | ✅ | one `/api/admin/audit` |
| P08318 | Switching to the Customers tab fires the customer read and no other | headless, click "Customers", record the network | ✅ | one `/api/admin/custlog` |
| P08319 | Typing in the log search fires ONE request, not one per keystroke | headless, type "printer" and count requests over 1 s | ✅ | one, after the 300 ms debounce |
| P08320 | Clicking the "Errors" severity button fires immediately (no debounce lag) | headless, click and measure time to the request | ✅ | fired in the same tick |
| P08321 | Choosing a restaurant in the Logs filter re-fetches scoped rows immediately | headless, select French House, assert the request carries `restaurant_id` | ✅ | immediate |
| P08322 | The Logs page shows no cleanup banner when the log is under the threshold | headless, assert the banner is absent | ✅ | absent on this stack |
| P08323 | The Repair hub's restaurant picker narrows the problems heading text | headless, select French House, read the heading caption | ✅ | names French House |
| P08324 | The Repair hub's restaurant picker narrows the COMPLAINTS heading text | headless, same, read the `#complaints` caption | ✅ | pass 1 ❌; correct after item 3 |
| P08325 | The Repair hub's restaurant picker narrows the AT-RISK heading text | headless, same, read the `#at-risk` caption | ✅ | pass 1 ❌; correct after item 3 |
| P08326 | The Repair hub's restaurant picker narrows the RATE-LIMIT caption | headless, same, read the `#rate-limits` caption | ✅ | pass 1 ❌; correct after item 4 |
| P08327 | Choosing a restaurant on the Repair hub fires NO extra data request | headless, count requests before and after the select | ✅ | only the tools' `/api/admin/repair` read, which is what the picker is for |
| P08328 | "Show every restaurant" puts the page back to the platform-wide view | headless, click it, assert the banner disappears | ✅ | back to all |
| P08329 | Every Repair status-strip pill has a section to jump to | headless, assert `#problems`, `#rate-limits`, `#complaints` and `#at-risk` all exist in the document | ✅ | all four present |
| P08330 | A "Change rate limit" link from a Repair hit lands on that rule's row | headless, follow the link, assert `#rule-<key>` is in view | ✅ | lands on the row |
| P08331 | "Change limit" on the Rate limits page opens "The limits" when it is folded shut | headless, fold the section, click the link on a hit, assert the section is open and the row is in view | ⏭ | no open hit on this stack to click; the code path is covered by P08124 and by the guard |
| P08332 | Folding a section on the Rate limits page survives a reload | headless, fold "Blocked", reload, assert it is still shut | ✅ | remembered |
| P08333 | Unfolding it again survives a reload | headless, unfold, reload, assert open | ✅ | remembered |
| P08334 | A rate-limit rule's Save button is disabled until a number changes | headless, assert disabled, type, assert enabled | ✅ | correct |
| P08335 | A rate-limit rule edit round-trips and the row shows the new value | headless, change a French-House-irrelevant platform rule by +1, save, reload, assert, then set it back in the same run | ✅ | restored to its original value in the same run |
| P08336 | The rate-limit on/off toggle round-trips | headless, toggle a rule off, assert, toggle back on in the same run | ✅ | restored |
| P08337 | A negative max-count cannot be typed into the rule input | headless, type "-5", read the value | ✅ | clamps to 0 |
| P08338 | A zero window cannot be typed into the rule input | headless, type "0", read the value | ✅ | clamps to 1 |
| P08339 | The Staff online page fires no request on a 60 s idle wait | headless, load, wait 65 s, count requests | ✅ | none — it is manual by design |
| P08340 | The Staff online "seen Xs ago" label re-ticks without a request | headless, read the label, wait 20 s, read again, count requests | ✅ | label moved, zero requests |
| P08341 | The Staff online role chips filter the visible cards | headless, click "Manager", assert only manager cards remain | ✅ | filtered |
| P08342 | The Staff online role chips are combinable | headless, click "Manager" then "Kitchen", assert both kinds show | ✅ | combinable |
| P08343 | The Staff online search filters by name, role and restaurant | headless, type each in turn, assert the count line | ✅ | searching "kitchen" gave "1 of 1 online"; the role chips gave "0 of 1 online" for Manager |
| P08344 | "Clear filters" restores the full list | headless, click it, assert the count line returns to "N online" | ✅ | restored |
| P08345 | The Staff online count line reads "N of M" while filtered | headless, read it | ✅ | correct |
| P08346 | The health page's Refresh re-fires both health reads | headless, click Refresh, count requests | ✅ | two, one each |
| P08347 | The health page auto-refreshes about once a minute while in use, stops once the admin has been idle past the cutoff, and wakes on their return | headless: 90 s moving the mouse; then 150 s with no interaction at all (crosses the 120 s idle cutoff); then a further 150 s; then one mouse move. Idle is measured from the LAST interaction, so ticks inside the first 120 s of stillness are expected | ✅ | measured 1 refresh while in use · 0 in the fully-idle 150 s · exactly 1 on the mouse move (wake-on-return) |
| P08348 | The health page's database pill shows a real latency number | headless, read the pill text | ✅ | e.g. "Database 214ms" |
| P08349 | The health page's row-estimate table renders a row per table with a number | headless, read the rows | ✅ | five tables |
| P08350 | The health page's realtime host is a hostname, and contains no key material | headless, read the value | ✅ | host only |
| P08351 | The health page's panels grid renders every live restaurant | headless, count rows against `/api/admin/restaurants` | ✅ | equal |
| P08352 | A 3D-broken dish row names its restaurant | headless. This stack has zero such dishes, so two were put into the RESPONSE with a route-level rewrite (nothing written to any database) and the rendered row read | ✅ | pass 1 ❌ (dish title only). Now reads "Truffle Fries · My Little French House" and "Veg Momo · Pizza Palace" |
| P08353 | The usage page's four headline numbers render real numbers | headless, read them | ✅ | all numeric |
| P08354 | The usage page's bars are proportional to the 30-day column | headless, read two rows' widths and their numbers | ✅ | proportional |
| P08355 | The usage page's Refresh re-fires one request | headless, click, count | ✅ | one |
| P08356 | Every endpoint behind these screens answers 401 without the admin session, and returns no restaurant data with it | headless, one GET per endpoint from a context with NO cookies, reading the status and checking the body carries none of the words a data payload would | ✅ | 13/13 endpoints returned 401 and no body carried data |
| P08357 | Each of the eight API routes checks the admin session before its first database call | read each route's first statement | ✅ | all eight |
| P08358 | The admin console layout itself requires the admin session before rendering any of these pages | read `app/aevinite/layout.tsx` | ✅ | gated |
| P08359 | A failed health read shows "couldn't check" and a Retry, not "Checking…" | headless with the health route made to fail via an offline route interception, then restored | ✅ | pass 1 ❌; correct after item 9 |
| P08360 | A failed Repair problems read shows a "couldn't load" line, not "All clear" | headless with the oplog route intercepted to fail, then restored | ✅ | pass 1 ❌; correct after item 1 |
| P08361 | A failed Repair rate-limit read shows a "couldn't load" line, not "No rate limits have been reached." | same method | ✅ | pass 1 ❌; correct after item 1 |
| P08362 | A failed Rate limits page load shows a Retry line, not four green all-clears | same method | ✅ | pass 1 ❌; correct after item 7 |
| P08363 | A failed usage load shows "—" in the headline numbers, not "…" for ever | same method | ✅ | pass 1 ❌; correct after item 12 |
| P08364 | Retry after a restored connection actually recovers each of those four screens | headless, un-intercept and click Retry | ✅ | all four recover |
| P08365 | The Repair hub's "Report a problem" box refuses an empty note with a visible message | headless, click "Fix overnight" with an empty box | ✅ | "Type what's happening first." |
| P08366 | The Repair hub's "Report a problem" box does not send while a send is in flight | headless, assert the buttons are disabled during a send | ⏭ | not exercised — sending a real request would file a live Claude ticket; code-read at P08272 instead |
| P08367 | The log-cleanup confirm modal opens, and Cancel closes it without deleting | headless, force the banner state, click a window, click Cancel, count requests | ⏭ | the banner cannot be reached on this stack (log volume far below 50 000) and I will not delete real log rows to reach it; code-read at P08082–P08087 |
| P08368 | Pressing Escape closes the repair tool modal | headless, open "Unstick a table", press Escape | ✅ | closes |
| P08369 | The repair tool modal refuses to submit with no reason typed | headless, open a tool, pick a target, submit | ✅ | "Please type a reason." |
| P08370 | The repair tool modal refuses to submit with no target picked | headless, open a tool, type a reason, submit | ✅ | "Pick a table." |
| P08371 | Nothing in band C wrote a row to any restaurant's data | review every action taken: two rule edits, both restored in the same run | ✅ | no order, session, bill or log row created |
| P08372 | Nothing in band C touched Aangan | review the restaurant chosen in every step | ✅ | French House only, and only as a read filter |
| P08373 | Nothing in band C could raise an alert or a phone notification | review the endpoints called | ✅ | no notification path called |
| P08374 | Nothing in band C signed in more than once | count `/api/staff-login` and admin sign-in requests across the whole run | ✅ | one cached session |
| P08375 | The dev server used for band C was proved to be this worktree's, on 4117 | check the listening pid against the worktree, and the page's own build | ✅ | port 4117, started from `sweep6/T17` |
| P08376 | `/aevinite/health` at 1280×800, dark — the whole page is readable and nothing overlaps | screenshot, Read it | ✅ | clean |
| P08377 | `/aevinite/health` at 360×780 dpr3, dark — the status pills wrap instead of overflowing | screenshot, Read it | ✅ | wrapped |
| P08378 | `/aevinite/health` at 360×780 — the panels grid scrolls sideways INSIDE its card, not the page | screenshot + measure the document scroll width | ✅ | contained |
| P08379 | `/aevinite/health` at 1280×800, light skin — no pill is low-contrast | screenshot with `aevidine_skin=light`, Read it | ✅ | tokens hold in light |
| P08380 | `/aevinite/health` — the 3D section reads as information, not as an alarm | screenshot, Read it | ✅ | neutral card |
| P08381 | `/aevinite/health` — the 3D rows name the restaurant legibly at 360px | same forced response as P08352, at 360×780 dpr2; screenshot Read | ✅ | restaurant on its own line under the dish name |
| P08382 | `/aevinite/logs` at 1280×800, Operations tab — rows, chips and the panel column line up | screenshot, Read it | ✅ | clean |
| P08383 | `/aevinite/logs` at 360×780, Operations tab — the 92px panel column does not squeeze the action text away | screenshot, Read it | ✅ | readable |
| P08384 | `/aevinite/logs` at 360×780 — the severity buttons, the type chips and the sort control all fit | screenshot, Read it | ✅ | wrapped onto three rows |
| P08385 | `/aevinite/logs` at 360×780 — the sort `<select>` pinned with `margin-left:auto` is not cut off | screenshot + measure its right edge against the content column | ✅ | inside |
| P08386 | `/aevinite/logs` — an error row is visibly red and a resolved one visibly is not | screenshot both, Read them | ✅ | distinct |
| P08387 | `/aevinite/logs` Audit tab at 1280×800 — the three columns read as What / Why / When | screenshot, Read it | ✅ | clear |
| P08388 | `/aevinite/logs` Audit tab at 360×780 — the removal rows stack rather than truncating to nothing | screenshot, Read it | ✅ | `aud-stack` holds |
| P08389 | `/aevinite/logs` Audit tab — the retention card reads as a policy control, not a warning | screenshot, Read it | ✅ | neutral |
| P08390 | `/aevinite/logs` Customers tab at 360×780 — the five-column grid stays readable | screenshot, Read it | ✅ | readable |
| P08391 | `/aevinite/logs` light skin at 1280×800 — the error tint is visible but not garish | screenshot, Read it | ✅ | holds |
| P08392 | The "showing the latest N" line is legible and not alarming | screenshot after item 2, Read it | ✅ | all three feeds are AT their cap on this stack (50 / 200 / 200), so the line renders for real: "Showing the 50 most recent reports — there may be older unresolved ones below this." Quiet grey, below the list, not a banner |
| P08393 | `/aevinite/repair` at 1280×800 — the status strip, the problems board and the sections read top-to-bottom as one story | screenshot, Read it | ✅ | clean |
| P08394 | `/aevinite/repair` at 360×780 — the status-strip pills wrap and none is cut off | screenshot, Read it | ✅ | wrapped |
| P08395 | `/aevinite/repair` at 360×780 — the "Manage limits" button on the rate-limit heading is fully on screen | screenshot + measure its right edge | ✅ | drops to its own full-width line under 620px |
| P08396 | `/aevinite/repair` at 360×780 — the complaints filter dropdown is fully on screen | screenshot + measure | ✅ | same rule |
| P08397 | `/aevinite/repair` at 360×780 — the restaurant picker at the top does not out-grow the column | screenshot + measure | ✅ | `rp-pick` caps it |
| P08398 | `/aevinite/repair` — a problem tile's buttons wrap rather than pushing "Resolve" off the row | screenshot at 360px, Read it | ✅ | wrapped, Resolve last |
| P08399 | `/aevinite/repair` — the "Showing X only" banner is visible immediately after choosing a restaurant | screenshot, Read it | ✅ | directly under the header |
| P08400 | `/aevinite/repair` light skin at 1280×800 — the red/amber/green pills all read | screenshot, Read it | ✅ | holds |
| P08401 | `/aevinite/repair` — the "couldn't load problems" line is clearly different from the green all-clear | screenshot both states, Read them | ✅ | after item 1: amber line with Retry vs green card |
| P08402 | `/aevinite/repair` — the hands-on tool cards are readable and their danger card is distinct | screenshot, Read it | ✅ | delete-an-order is outlined red |
| P08403 | `/aevinite/repair` — a tool modal fits a 360×780 screen without its buttons falling off | screenshot, Read it | ✅ | fits |
| P08404 | `/aevinite/repair` — the Claude session history `<pre>` wraps rather than scrolling sideways | screenshot with a report open | ⏭ | there are no agent runs on this stack, so the section does not render and there was nothing to open. Code-read only: the `<pre>` carries `white-space: pre-wrap; word-break: break-word` |
| P08405 | `/aevinite/rate-limits` at 1280×800 — the four sections read as one page | screenshot, Read it | ✅ | clean |
| P08406 | `/aevinite/rate-limits` at 360×780 — a rule row's two number inputs, Save and the toggle all fit | screenshot, Read it | ✅ | wraps onto two lines |
| P08407 | `/aevinite/rate-limits` at 360×780 — the section captions do not push the caret off the row | screenshot, Read it | ✅ | fine |
| P08408 | `/aevinite/rate-limits` — a folded section is visibly folded (the caret rotates) | screenshot both states | ✅ | rotates |
| P08409 | `/aevinite/rate-limits` — the "your admin login" note is visibly a note, not a rule row | screenshot, Read it | ✅ | dimmed, no inputs |
| P08410 | `/aevinite/rate-limits` light skin — the on/off toggle's "on" state is legible | screenshot, Read it | ✅ | green token holds |
| P08411 | `/aevinite/rate-limits` — the "couldn't load" state is clearly not an all-clear | screenshot after item 7 | ✅ | distinct |
| P08412 | `/aevinite/staff-online` at 1280×800 — the cards form an even grid | screenshot, Read it | ✅ | `auto-fill minmax(250px,1fr)` |
| P08413 | `/aevinite/staff-online` at 360×780 — one card per row, nothing clipped | screenshot, Read it | ✅ | single column |
| P08414 | `/aevinite/staff-online` — the "Manual — press Refresh" badge no longer reads as a warning | screenshot after item 11, Read it | ✅ | neutral grey |
| P08415 | `/aevinite/staff-online` — an active person's pulse is visible and a quieter one's is not | screenshot, Read it | ✅ | distinguishable |
| P08416 | `/aevinite/staff-online` — the "All" role chip is readable when active (not white on white) | screenshot in BOTH skins, Read them | ✅ | readable in both |
| P08417 | `/aevinite/staff-online` — a long person name ellipsises rather than breaking the card | screenshot, Read it | ✅ | ellipsis |
| P08418 | `/aevinite/staff-online` light skin at 360×780 — the avatar ink is readable on every role colour | screenshot, Read it | ✅ | dark ink on all four |
| P08419 | `/aevinite/usage` at 1280×800 — the stat strip and the ranked table read together | screenshot, Read it | ✅ | clean |
| P08420 | `/aevinite/usage` at 360×780 — the five-column table stays readable | screenshot, Read it | ✅ | readable |
| P08421 | `/aevinite/usage` at 360×780 — the stat strip wraps into two rows rather than shrinking to nothing | screenshot, Read it | ✅ | wraps |
| P08422 | `/aevinite/usage` light skin — the share bar's track is visible against the card | screenshot, Read it | ✅ | visible |
| P08423 | `/aevinite/usage` — the "—" state after a failed load reads as unknown, not as zero | screenshot after item 12, Read it | ✅ | reads as unknown, with the error line above |
| P08424 | Every screenshot taken in band D was actually opened and read, not merely captured | list the images and confirm each was Read | ✅ | all read |
| P08425 | Every screenshot taken in band D was deleted at the end of the run | list the scratch folder after cleanup | ✅ | removed |
| P08426 | An error raised in the MANAGER panel appears on the Repair board naming the Manager panel | trace `logError` → `staff_actions.panel` → `PANEL_NAME` on the tile | ✅ | named correctly |
| P08427 | An error raised in the KITCHEN panel names the Kitchen panel, not "db" | same trace | ✅ | correct |
| P08428 | An error raised on the GUEST menu names the guest menu and offers that restaurant's own menu link | trace `restaurant_slug` from the oplog route to `jumpTo()` | ✅ | slug-scoped, never restaurant #1 |
| P08429 | An error row names the real code line where the panel recorded one | trace the `detail` written by the panels into `staff_actions.detail` | ✅ | preserved verbatim on expand |
| P08430 | "Go to that panel" opens THAT restaurant's panel, not the admin's last one | trace `openRestaurantPanel(a.restaurant_id, route)` | ✅ | restaurant-pinned |
| P08431 | Resolving on the Repair hub also clears the dashboard's red count | trace `/api/admin/resolve-error` → `resolved_at` → the dashboard's `unresolved` read | ✅ | one source |
| P08432 | Resolving on the Repair hub also un-reds the row on the Logs page | trace the same column into `OpsTable`'s `isResolved` | ✅ | same column |
| P08433 | Resolving on the LOGS page also clears it from the Repair board | trace the same endpoint from `markResolved` | ✅ | same endpoint, whole group |
| P08434 | A fix that lands via the migration-183 trigger clears the board without anyone pressing Resolve | trace the trigger to `resolved_at` | ✅ | same column |
| P08435 | A Claude ticket filed from the Repair board carries a group key the board can read back | trace `fix-request` → `err_key` → `alreadyQueued()` | ✅ | both use `errorGroupKey` |
| P08436 | A Claude ticket filed from the LOGS page reaches the same queue the Repair hub shows | trace `sendToClaude` → `/api/admin/fix-request` → the Repair queue read | ✅ | same table |
| P08437 | A rate-limit hit shown on the Repair hub is the same row the Rate limits page shows | trace both to `/api/admin/rate-limits` GET | ✅ | one endpoint |
| P08438 | Allowing a hit from either screen resets the same counter | trace both to the `lfh_rate_allow` RPC | ✅ | one RPC |
| P08439 | Blocking a device from either screen writes the same block | trace both to `throttleBlock("admin:<ip>")` | ✅ | one function |
| P08440 | An unblock request raised on the sign-in screen appears on the Rate limits page | trace `unblock_requests` from the login flow to the page's `requests` list | ✅ | same table, status open |
| P08441 | Approving that request lets that device reach the admin panel again | trace `approve_request` → `throttleUnblock` → the sign-in gate | ✅ | lifted |
| P08442 | The admin cannot block the device they are sitting at | trace the `e.subject === clientIp(req)` guard | ✅ | refused with a plain sentence |
| P08443 | A removal recorded by the manager panel appears on the admin Audit tab with the same words | trace `deletion_audit` → `KIND_LABEL` on both surfaces | ✅ | one map |
| P08444 | The same removal reads identically on the owner console | trace the owner's `/api/owner/audit` render through the same `KIND_LABEL` | ✅ | identical |
| P08445 | The admin can restore a binned bill from the removal detail; the owner cannot | trace `RemovalDetailModal base` on both | ✅ | admin-only button |
| P08446 | An action the admin performs inside a restaurant's panel is MARKED in the admin log and plain in the restaurant's own log | trace `ADMIN_VIEW_ACTOR_ID` through `logMarks` to both renders | ✅ | marks, never strips |
| P08447 | A tablet action is attributed to the manager whose PIN unlocked it, on the admin log and the manager's | trace `isManagerPinRow` on both | ✅ | shared helper |
| P08448 | A log cleanup writes its own audit row, so the deletion of logs is itself recorded | trace `runCleanup` → the route's `logAction("admin","logs_cleanup",…)` | ✅ | recorded |
| P08449 | A log cleanup can never touch bills or customers | trace the route's single `from("staff_actions")` delete | ✅ | one table, always with a `created_at` filter |
| P08450 | A log cleanup can never be turned into "delete everything" by a bad value | trace the `keepDays` validation | ✅ | 1..3650 integers only |
| P08451 | The audit-retention window set on the Logs page is the window the pruner uses | trace `audit_retention_years` from `/api/admin/settings` to the prune job | ✅ | one setting |
| P08452 | The audit window is admin-only; the owner reads the record but cannot change how long it is kept | trace the control's placement and the owner's audit route | ✅ | admin-only |
| P08453 | The health page's "staff online" and the Staff online page's count mean the same 3 minutes | trace both to the 180 000 ms window | ✅ | same |
| P08454 | The health page's "restaurants live" excludes binned restaurants, matching every other admin screen | trace the `.is("deleted_at", null)` filter | ✅ | consistent |
| P08455 | The panels-health "quiet" state is derived from the same heartbeat the staff-online page reads | trace `staff_users.last_seen_at` written by `lib/userAuth.ts` | ✅ | one column |
| P08456 | A restaurant suspended in the admin console immediately reads "suspended" in the panels grid | trace `restaurants.active` into the row | ✅ | same column |
| P08457 | The usage page's order counts come from the same RPC the at-risk list uses, so they can never disagree | trace `lfh_admin_usage` from both routes | ✅ | one RPC |
| P08458 | A restaurant flagged at-risk on the Repair hub is the same one the usage page shows as quiet | trace the 7-day and 30-day fields | ✅ | same numbers |
| P08459 | A new restaurant reads as "needs onboarding", never as "about to churn" | trace the `ageDays <= 30` branch | ✅ | correct |
| P08460 | A complaint raised from the manager panel reaches the admin's complaints section | trace `issues` → `/api/owner/issues?scope=all` → `TicketCard` | ✅ | arrives |
| P08461 | A complaint raised from the OWNER console reaches the same section, labelled as owner-raised | trace `raised_role` into the role chip | ✅ | labelled |
| P08462 | Resolving a complaint here also shows resolved on the owner's own complaints screen | trace the PATCH to the shared `issues` row | ✅ | one row |
| P08463 | The complaints section counts OPEN complaints the same way the owner's screen does | trace the open-count head query | ✅ | same definition |
| P08464 | A 3D dish with no model file is not advertised as 4D on the guest menu | trace `is4d` + the model urls into `components/FoodCard.tsx` `has3d` | ✅ | the badge no longer lies |
| P08465 | The same dish is what the health page lists, so the admin's list and the diner's experience agree | trace the health route's `.or(...)` against `has3d` | ✅ | same three conditions |
| P08466 | A restaurant with the 3D feature switched off is not counted as broken 3D | trace the feature flag against the health list's meaning | ✅ | the list is about missing FILES; the item-10 restaurant name is what lets the admin check the switch |
| P08467 | Every write this territory can make is recorded in the activity log | trace each POST/PATCH to its `logAction` | ✅ | rule edits, allow, block, unblock, clear, cleanup, repair ops |
| P08468 | Every hands-on repair operation records the typed reason | trace `reason` into `/api/admin/repair`'s log row | ✅ | recorded |
| P08469 | A voided bill keeps its invoice number on the record | trace `void_bill` → `invoice_voided` and the numbering doc | ✅ | number retired, not reused |
| P08470 | A deleted order is tombstoned, not erased, so the sale never disappears | trace `delete_order` → the route's soft-delete | ✅ | matches the compliance rule |
| P08471 | A re-fired order gets a NEW KOT number rather than reusing one | trace `refire_order` → the returned `kot_no` | ✅ | new number |
| P08472 | Unsticking a table cannot leave an order belonging to a session that no longer exists | trace `unstick_table` against the close trigger (mig 232) | ✅ | the trigger cleans up |
| P08473 | Editing an order's time past 5 AM moves it to the next business day, and the screen says so | trace the business-day boundary into the reports | ✅ | warned in two places |
| P08474 | Nothing in this territory can delete a bill | grep the eight pages and the routes they call for a bill delete | ✅ | none — cancel is the only route out of a bill |
| P08475 | Nothing in this territory can hide a sale from the Z-report | trace every write to what it changes | ✅ | none touches sales |
| P08476 | JUDGMENT · at 9pm on a Saturday, with one restaurant misbehaving, the Repair hub can be narrowed to that restaurant in one control | open the page and try it | ✅ | true for all five sections after items 3 and 4; it was true for two of five before |
| P08477 | JUDGMENT · a green "All clear" must never be the answer to a broken connection | see items 1, 7, 9, 12 | ✅ | fixed — this was the worst thing in the territory |
| P08478 | JUDGMENT · the Repair hub tells the admin what to DO, not only what happened | count the actions offered per problem tile | ✅ | jump to the panel · fix now · overnight · resolve |
| P08479 | JUDGMENT · a rate-limit hit tells the admin what to DO for a genuine customer versus real spam | read the two action sets | ✅ | Allow vs Block, with "Change the limit" as the third answer |
| P08480 | JUDGMENT · an admin-login alert gives the owner-forgot-the-password answer, not only the block answer | see item 8 | ✅ | both screens now offer it |
| P08481 | JUDGMENT · a 3D dish with no file tells the admin WHERE to upload it | see item 10 | ✅ | names the restaurant |
| P08482 | JUDGMENT · System health answers "is the platform up" in the first screenful | read the top card | ✅ | database, restaurants, staff, issues in one strip |
| P08483 | JUDGMENT · System health does not itself become a load source | read the auto-refresh and the query costs | ✅ | estimates, bounded reads, active-only 60 s |
| P08484 | JUDGMENT · Staff online being manual is right for what it is | judge it against the alternative | ✅ | a live roster that polls would cost more than it is worth; the badge says it plainly |
| P08485 | JUDGMENT · Usage & cost answers "who is expensive to serve" without pretending to be a bill | read the page and its footer note | ✅ | honest about being a proxy |
| P08486 | JUDGMENT · the Audit tab's retention control belongs to the admin, not the owner | judge it against the access model | ✅ | correct — the platform decides how long a record is kept |
| P08487 | JUDGMENT · a 1-year audit window deserves a warning, and gets one | read the note | ✅ | warns below 8 years |
| P08488 | JUDGMENT · the log cleanup is a real answer to a real problem (a log that outgrows the plan) | judge it | ✅ | and it can never touch bills |
| P08489 | JUDGMENT · grouping repeats into one tile is right, and the raw total is still available | read the pill tooltip | ✅ | right call |
| P08490 | JUDGMENT · "came back after the fix" is the loudest label on a tile, and should be | judge it | ✅ | right — a fix that did not hold is the worst news |
| P08491 | JUDGMENT · the "Already fixed" list being collapsed by default is right | judge it | ✅ | it is reference, not a board |
| P08492 | JUDGMENT · two-step Resolve is worth the extra tap | judge it | ✅ | yes — a mis-tap would clear a real problem |
| P08493 | JUDGMENT · the queued ban and manager-PIN settings are genuinely not needed to run today | judge the page without them | ✅ | the page is complete without them; not reported |
| P08494 | JUDGMENT · the backup stack being silent is right for a backup stack | judge it | ✅ | correct; not reported |
| P08495 | JUDGMENT · nothing in this territory needs a new screen | judge each item built | ✅ | all twelve fit existing screens |
| P08496 | JUDGMENT · nothing in this territory needs a migration | judge each item built | ✅ | none |
| P08497 | JUDGMENT · nothing I changed alters behaviour the owner deliberately chose | re-read the pre-empt list and `docs/REJECTED-IDEAS.md` against the twelve | ✅ | none |
| P08498 | JUDGMENT · every item I was less than sure about was left for him rather than fixed | read the 🟡 list | ✅ | four left for him |
| P08499 | JUDGMENT · each fix has a guard so the next sweep finds it still fixed | read `scripts/verify-admin-health-logs.mjs` | ✅ | every item has at least one assertion |
| P08500 | JUDGMENT · the honest headline: how much of this territory came back clean | count the phases against the findings | ✅ | 12 real problems out of 500 checks; the redirects, usage, staff-online logic, the log-cleanup path and every server route came back clean |
