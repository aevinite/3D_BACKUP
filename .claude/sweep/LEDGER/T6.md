# T6 — THE KITCHEN SCREEN · phases P02501–P03000

Territory (the only files this terminal may write to):

- `app/kitchen/layout.tsx`
- `app/kitchen/page.tsx`
- `public/panels/kitchen/index.html`
- `public/panels/kitchen/style.css`
- `public/panels/kitchen/app.js`

Everything else in the repo was read-only for this run. Where the real fix lived in someone
else's file the row is marked `🔗 HANDOFF`.

**Budget split (as briefed).**

| share | phases | what it is |
|---|---|---|
| ~40% | P02501–P02700 (200) | reading the code for correctness |
| ~20% | P02701–P02800 (100) | conformance to the project rules named in the brief |
| ~15% | P02801–P02875 (75) | watching it run — headless Playwright on port 4106 |
| ~10% | P02876–P02925 (50) | screenshots actually opened and looked at (1280×800 and A35 360×780 dpr3) |
| ~10% | P02926–P02975 (50) | tracing one change across panels |
| ~5% | P02976–P03000 (25) | judgment — a chef, wet hands, twenty seconds |

Every file in the territory has at least one phase; the three files named in the scope
(`app.js`, `index.html`, `style.css`) have far more than ten each.

Environment for every runtime row: dev server on **port 4106** from the T6 worktree, signed in
ONCE per run as the diag kitchen user via `scripts/sweep/login.mjs` (`loginAs`), restaurant
**French House** (`french-house`, 30 tables, auto-print KOT on). Aangan was never written to.

Result key: ✅ pass · ❌ fault found (fixed in this PR unless the note says otherwise) · ⏭ skipped, with the reason written in the note.

---

## 1. Reading the code for correctness — P02501–P02700

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P02501 | `app/kitchen/layout.tsx` gates the route with `requirePanel("kitchen", "/kitchen")` before rendering children | read the file; the await precedes the return | ✅ | |
| P02502 | The kitchen layout passes its own route back to `requirePanel` so a bounced person returns here after signing in | read `requirePanel`'s second argument | ✅ | |
| P02503 | `app/kitchen/page.tsx` awaits `searchParams` (Next 16 async params) before reading `rid`/`as`/`view` | read the destructure | ✅ | |
| P02504 | The page never trusts a raw `?rid=` — it goes through `panelAdminRid("kitchen", rid)` | read the call | ✅ | |
| P02505 | The page forwards `as` and `view` into the iframe URL through `panelIframeSrc`, not by string concatenation | read the call | ✅ | |
| P02506 | The kitchen tab has its own `<title>` so three open panels are tellable apart | `metadata = { title: "Kitchen — Aevidine" }` | ✅ | |
| P02507 | The page renders `PanelFrame` and nothing else — no second scroll container wrapping the iframe | read the JSX | ✅ | |
| P02508 | `panelIframeSrc` is pointed at `/panels/kitchen/index.html`, the folder this terminal owns | read the literal | ✅ | |
| P02509 | Nothing in `app/kitchen/**` imports a server-only module into a client component | grep for `"use client"` — there is none; both files are server components | ✅ | |
| P02510 | The kitchen route folder holds exactly two files, so there is no second stale copy of the panel page | `find app/kitchen -type f` | ✅ | layout.tsx + page.tsx |
| P02511 | `index.html` loads its stylesheet with a content-hash `?v=` so a cook cannot run a weeks-old skin | read the `<link>` tag | ✅ | `style.css?v=85661a3c` |
| P02512 | `index.html` loads `app.js` with a content-hash `?v=` | read the last `<script>` | ✅ | `app.js?v=124c5a75` |
| P02513 | Every shared `/panels/*.js` include carries its own `?v=` hash | read all nine script tags | ✅ | |
| P02514 | `theme.js` runs in `<head>`, before paint, so the panel does not flash the wrong skin | read tag position | ✅ | |
| P02515 | `outbox.js` loads before `app.js`, or every kitchen write would miss the offline queue | read tag order | ✅ | |
| P02516 | `connbadge.js` loads after both `realtime.js` and `outbox.js`, which it reads | read tag order | ✅ | |
| P02517 | `offline.js` loads after `outbox.js` and before `app.js` | read tag order | ✅ | |
| P02518 | `billdoc.js` loads before `app.js`, since `printKot` calls `LFH_BILLDOC` on the first board read | read tag order | ✅ | |
| P02519 | `undobar.js` is present, so the ✓ mis-tap take-back bar can render | read tag list | ✅ | |
| P02520 | `fitnums.js` is scoped to `.kot` only via `data-fit`, so it cannot shrink ticket body text | read the `data-fit` attribute | ✅ | |
| P02521 | The three column ids in the markup (`list-new`/`list-cooking`/`list-ready`) match every `$("#list-"+key)` in `app.js` | grep both files | ✅ | |
| P02522 | The three count ids (`count-new`/`count-cooking`/`count-ready`) match `draw()` and `moveCardToReady()` | grep both files | ✅ | |
| P02523 | Every element `app.js` looks up by id at module scope exists in `index.html` | list the `$("#…")` calls and match | ✅ | muteBtn, boardBtn, printerBtn, viewBtn, reportIssueBtn, themeToggle, moreBtn, drawerOverlay, drawerClose, dishSearch, dishList, toasts, clock, cols, wall, restName |
| P02524 | The pre-rendered skeleton tickets carry no `data-ticket`, so `reconcileList` removes rather than reuses them | read the skeleton markup and the reconciler | ✅ | |
| P02525 | `#wall` ships with the `hidden` attribute so the columns are the first paint | read the tag | ✅ | |
| P02526 | The 86 drawer overlay ships `hidden` and is only opened by `openDrawer()` | read the tag + the handler | ✅ | |
| P02527 | `#toasts` exists before `toast()` can be called | it is authored in the markup, above the scripts | ✅ | |
| P02528 | Both worded top-bar buttons are built as an icon span plus a word span, which the phone rule needs | read `#boardBtn` and `#viewBtn` | ✅ | |
| P02529 | Every icon-only top-bar button carries a title, so it has an accessible name | read the six buttons | ✅ | `#boardBtn` and `#reportIssueBtn` also carry `aria-label`; the other four rely on `title`, which browsers expose as the name. A consistency nit, not a fault — nobody is worse off, so it is not written up |
| P02530 | `#moreBtn` starts with `aria-expanded="false"` | read the tag | ✅ | |
| P02531 | The favicon is inlined, so the panel does not 404 on `/favicon.ico` every open | read the `<link rel=icon>` | ✅ | |
| P02532 | The `<html lang>` is set | read the tag | ✅ | `lang="en"` |
| P02533 | The viewport meta is present and does not disable zoom | read the tag | ✅ | no `user-scalable=no` |
| P02534 | `esc()` escapes all five of `& < > " '` before any value reaches ticket markup | read the replace map | ✅ | |
| P02535 | Every value interpolated into `ticketHtml` goes through `esc()` or is a fixed literal | read each `${…}` in the template | ✅ | allergen text is deliberate HTML built from escaped parts |
| P02536 | The allergen `＋` superscript escapes the allergen name before wrapping it | read the `lineRemoved.map` | ✅ | |
| P02537 | `platTicketHtml` escapes the platform label, the customer name and every dish line | read the template | ✅ | |
| P02538 | `tname()` trims and returns "" for a missing name so the caller falls back to the number | read the helper | ✅ | |
| P02539 | `tshort()` returns `T<n>` when a table has no name | read the helper | ✅ | |
| P02540 | `tlong()` answers `T?` for a null/empty table instead of `Tnull` | read the helper | ✅ | |
| P02541 | `whereFor()` has no unreachable `parcel` branch reading a column `orders` does not have | read the helper | ✅ | removed in an earlier sweep; still absent |
| P02542 | `ageMinutes()` returns null for null, "", NaN and epoch-0 timestamps rather than printing 1970 | read the guards | ✅ | |
| P02543 | `ageMinutes()` clamps to 0, so a device clock ahead of the server reads "just now" not a negative | read the `Math.max(0, …)` | ✅ | |
| P02544 | `timeAgo()` says nothing at all when the age is unknown | read the `if (m == null) return ""` | ✅ | |
| P02545 | `timeAgo()` steps to days past 24h so a five-day ticket does not read "117h" | read the `h >= 24` branch | ✅ | |
| P02546 | `orderTime()` sorts an undateable ticket LAST (Infinity), never first | read the fallback | ✅ | |
| P02547 | `cmpTime()` compares rather than subtracts, so Infinity − Infinity can never be NaN | read the comparator | ✅ | |
| P02548 | `ageClass()` returns "" for an unknown age instead of falsely warning | read the null guard | ✅ | |
| P02549 | The three age steps are ordered stale → late → warn, so the oldest wins | read the ternary chain | ✅ | |
| P02550 | `ageTitle()` gives a different sentence at each of the three steps | read the branches | ✅ | |
| P02551 | The stale step renders a WORD (`DAY`) as well as a colour, so it survives a black-and-white read | read `ticketHtml`'s age span | ✅ | |
| P02552 | `ageMinutes(o.created_at) >= AGE_STALE_MIN` cannot throw on a null age | `null >= 1440` is `false`, not an error | ✅ | |
| P02553 | `toast()` always renders a ✕ so a cook can dismiss it before the 4s timeout | read the markup | ✅ | |
| P02554 | `toast()`'s UNDO button removes the toast after running the callback | read the onclick | ✅ | |
| P02555 | `toast()` escapes the message | read the `esc(msg)` | ✅ | |
| P02556 | `toast()` cannot leave a node behind — the 4s `setTimeout` removes it even if it was already removed | `remove()` on a detached node is a no-op | ✅ | |
| P02557 | `primeAudio()` is wrapped so a browser with no WebAudio cannot break the boot | read the try/catch | ✅ | |
| P02558 | The one-time gesture listeners are registered with `{ once: true }` AND removed by hand, so they cannot leak | read the `once` helper | ✅ | |
| P02559 | `audioReady()` requires the context to be `running`, not merely to exist | read the check | ✅ | |
| P02560 | `chime()` returns immediately when the cook has muted | read the first line | ✅ | |
| P02561 | `chime()` is wrapped, so a suspended context cannot throw into a load path | read the try/catch | ✅ | |
| P02562 | The sound nudge is inserted in normal flow after the top bar, never fixed over a ticket | read the `insertBefore(soundNudgeEl, bar.nextSibling)` | ✅ | |
| P02563 | The sound nudge has a body fallback if the top bar is somehow missing | read the else branch | ✅ | |
| P02564 | The sound nudge hides itself the instant the context runs or the cook mutes | read `updateSoundNudge`'s `need` | ✅ | |
| P02565 | `updateSoundNudge` is re-run after `resume()` settles, since resume is async | read the `.then` | ✅ | |
| P02566 | `itemsByOrderId()` builds the order→items index once per render pass, not per ticket | read the caller in `renderColumns`/`renderWall` | ✅ | |
| P02567 | `itemsByOrderId()` skips null rows and rows with no `order_id` | read the `continue` | ✅ | |
| P02568 | `rowsOf()` falls back to the order's own `items` JSON for a legacy order | read the second return | ✅ | |
| P02569 | `rowsOf()` marks DB rows `fromDb: true` so only they get a per-dish ✓ | read both branches | ✅ | |
| P02570 | A legacy row's status falls back to the order status when the JSON row has none | read `i.status \|\| o.status` | ✅ | |
| P02571 | The order-wide allergy list is distributed onto every dish line, with no separate banner | read `ticketHtml` | ✅ | matches the owner's 2026-06-14 rule |
| P02572 | Duplicate allergens between the line and the order-wide list are de-duplicated | read the `new Set([...])` | ✅ | |
| P02573 | A staff-ADDED allergen carries the `＋` mark and a title explaining it | read the `alg-add` span | ✅ | |
| P02574 | A REMOVED allergen flags `✎−` on the dish name with its own title | read the `alg-removed` span | ✅ | |
| P02575 | A dish note renders with the `✎` prefix and is escaped | read the note segment | ✅ | |
| P02576 | Options render as `+ label` and tolerate a plain string instead of an object | read `op.label \|\| op` | ✅ | |
| P02577 | A cooking dish gets a ✓; a ready dish gets a pink tag; a served dish gets "served ✓" | read the `tick` ternary | ✅ | |
| P02578 | The ✓ only renders for a `fromDb` row, because a legacy row has no id to send | read the condition | ✅ | |
| P02579 | `allCooked` requires at least one row, so an empty ticket does not claim to be finished | read `rows.length > 0 &&` | ✅ | |
| P02580 | A `received` ticket shows "waiting for the waiter to accept" and NO accept button | read the action ternary | ✅ | the kitchen does not accept — owner, 2026-06-14 |
| P02581 | The 🖨 reprint button renders on every ticket for every restaurant | read `reprintBtn` — it is unconditional | ✅ | owner, 2026-07-21 |
| P02582 | The table mark badge reads `state.tableTags` keyed by table number, not a non-existent `o.tag` | read the `ttag` lookup | ✅ | |
| P02583 | An unknown tag value renders no badge rather than `undefined` | read `TAG_BADGE[ttag]` + the guard | ✅ | |
| P02584 | The `guest` badge uses dark ink on its pale background, the other two white | read the inline style ternary | ✅ | |
| P02585 | A ticket with no table (a banquet bill) draws no `title` attribute full of `Tnull` | read the conditional title | ✅ | |
| P02586 | `orderPhase()` puts a `received` order in New whatever its dishes say | read the first line | ✅ | |
| P02587 | `orderPhase()` returns `served` only when EVERY row is served | read the `every` | ✅ | |
| P02588 | `orderPhase()` returns `ready` when every row is ready-or-served | read the second `every` | ✅ | |
| P02589 | An order with no rows at all lands in Cooking, not Ready | read the `!rows.length` branch | ✅ | |
| P02590 | Cancelled orders are dropped from both views before bucketing | read the `if (o.status === "cancelled") return` in each renderer | ✅ | |
| P02591 | `bindDelegation()` attaches exactly one body-level click handler and can never double-bind | read the `clickDelegationBound` latch | ✅ | |
| P02592 | Every ticket control is reachable through the delegated handler: reprint, ALL READY, ✓, platform accept/ready/hand | read the six `closest()` branches | ✅ | |
| P02593 | The delegated handler returns after the first match, so one tap cannot fire two actions | read the `return`s | ✅ | |
| P02594 | `platPhase()` maps new→new, accepted/preparing→cooking, ready→ready, everything else→served | read the function | ✅ | |
| P02595 | A platform ticket shows ACCEPT only when the manager toggle allows it | read `state.platformAccept` | ✅ | |
| P02596 | A platform ticket with the toggle off says "manager will accept" rather than nothing | read the else branch | ✅ | |
| P02597 | An unknown platform source falls back to the `other` badge rather than crashing | read `PLAT_META[p.source] \|\| PLAT_META.other` | ✅ | |
| P02598 | A parcel is labelled PARCEL, never "Takeaway" | read `PLAT_META.parcel` | ✅ | |
| P02599 | `platAct()` reports a queued (offline) write instead of silently doing nothing | read the `r.queued` branch | ✅ | |
| P02600 | `platAct()` refreshes from the server after a failure so the board is not left optimistic | read the `.catch` | ✅ | |
| P02601 | `reconcileList()` writes the empty placeholder only when it is not already the sole child | read the guard | ✅ | |
| P02602 | `reconcileList()` reuses a card node whose html is unchanged | read the `__kdsHtml` comparison | ✅ | |
| P02603 | `reconcileList()` stamps `__kdsHtml` on every node it creates or replaces | read both branches | ✅ | |
| P02604 | `reconcileList()` keeps DOM order matching the desired list | read the `insertBefore(node, target)` | ✅ | |
| P02605 | `reconcileList()` removes every card that left the board | read the trailing loop over `existing` | ✅ | |
| P02606 | `reconcileList()` removes strays with no `data-ticket` instead of leaving them wedged in the list | read the else in the indexing loop | ✅ | |
| P02607 | `reconcileList()` counts a replacement in `__lfhPerf` so a rebuild-everything regression is visible | read the counters | ✅ | |
| P02608 | `renderColumns()` draws dine-in tickets then platform tickets in each lane | read `draw()` | ✅ | order noted as I3 (improvement, not built) |
| P02609 | `renderColumns()` writes "0" rather than a blank pill for an empty lane | read the `String(...)` | ✅ | |
| P02610 | `renderColumns()` never collapses an empty lane on a phone | there is no such branch | ✅ | R3 — do not re-report |
| P02611 | `renderWall()` drops served tickets | read the `phase !== "served"` filter | ✅ | |
| P02612 | `renderWall()` sinks fully-ready tickets to the end | read the `(a.phase === "ready") - (b.phase === "ready")` term | ✅ | |
| P02613 | The wall is first-come-first-served across EVERY live ticket, including platform ones | wall view, insert one 3h-old parcel ticket, read the rendered ages in DOM order, delete the row by id | ❌ → ✅ | F3 — dine-in and platform were sorted apart then glued together, so an hour-old delivery ticket always sat after a one-minute dine-in ticket. Fixed; re-measured, the parcel ticket now lands between a 2d and a 25m ticket |
| P02614 | `render()` measures each paint into `__lfhPerf.lastMs` | read the timing | ✅ | |
| P02615 | `applyView()` toggles the two `<main>`s by the `hidden` attribute and CSS forces it | read the JS and the `[hidden]{display:none!important}` rule | ✅ | |
| P02616 | `applyView()` sets the toggle's icon and word separately so the phone spans survive | read the `.bi`/`.bw` writes | ✅ | |
| P02617 | `applyView()` has a text fallback if the two spans are ever removed | read the else | ✅ | |
| P02618 | `applyView()` empties the inactive view so its nodes cannot be reused by the reconciler | read the innerHTML clears | ✅ | |
| P02619 | The view choice persists per device | read the `localStorage.setItem("kds_view", …)` | ✅ | |
| P02620 | `act()` refreshes with `freshLoad()`, not `load()`, because it follows a write | read the helper | ✅ | |
| P02621 | `act()` keeps the plain `Failed: <message>` wording R21 settled on | read the catch | ✅ | R21 — do not re-report |
| P02622 | `scheduleReadyReconcile()` debounces to one refetch 2.5s after the last tap | read the timer | ✅ | |
| P02623 | `scheduleReadyReconcile()` refetches FIRST and clears the optimistic overlay only after the fetch settles | read the `.finally` | ✅ | |
| P02624 | `scheduleReadyReconcile()` uses `freshLoad()` so it cannot be handed a read that predates the tap | read the call | ✅ | |
| P02625 | `setLocalReady()` never downgrades a SERVED dish | read the `i.status !== "served"` guard | ✅ | |
| P02626 | `setLocalReady()` also flips a legacy order's JSON items | read the `state.orders` pass | ✅ | |
| P02627 | `setLocalReady()` adopts the optimistic state as `lastSig` so a same-data refetch cannot rebuild the tickets | read the assignment | ✅ | |
| P02628 | `setLocalReady()` deliberately does NOT call `render()` | read the closing comment and the body | ✅ | |
| P02629 | `markItemReady()` says something when the dish is no longer in state instead of returning in silence | read the toast + `load()` | ✅ | |
| P02630 | `markItemReady()` refuses an already-served dish by name | read the second guard | ✅ | |
| P02631 | `markItemReady()` snapshots the previous status before flipping, for the undo bar | read `const prev` | ✅ | |
| P02632 | `markItemReady()` patches only the tapped line, never the whole board | read the `btn.closest(".line")` patch | ✅ | |
| P02633 | `markItemReady()` slides the card to Ready only when THIS tick finished the ticket | read the `orderPhase(o) === "ready"` branch | ✅ | |
| P02634 | `markItemReady()` reports a queued (offline) write and skips the reconcile | read the `r.queued` branch | ✅ | |
| P02635 | `markItemReady()` offers the undo bar with the dish name and the table | read the `LFH_UNDO.show` payload | ✅ | |
| P02636 | A FAILED `markItemReady()` drops its optimistic overlay, so the dish cannot stay green for ever | answer one `items/:id/status` with a 404, tap the ✓, force a fresh read, then read the line and its lane | ❌ → ✅ | F1 — `pendingReady` was never cleared on failure, so every later refetch re-painted the dish ready and the ✓ never came back. Watched both ways |
| P02637 | `undoReady()` removes each dish from `pendingReady` before restoring it | read the loop | ✅ | |
| P02638 | `undoReady()` never un-serves a dish | read the `it.status !== "served"` guard | ✅ | |
| P02639 | `undoReady()` sends ONE request for a whole-ticket take-back | read the `/orders/:id/unready` branch | ✅ | |
| P02640 | `undoReady()` keeps the per-dish loop for the single-✓ case, which has no order id | read the else | ✅ | |
| P02641 | `undoReady()` finishes with `freshLoad()`, since a write just landed | read the last line | ✅ | |
| P02642 | `undoReady()` reports a queued bulk take-back rather than looking like nothing happened | read the `r.queued` branch | ✅ | |
| P02643 | `moveCardToReady()` bails when the card is not on screen | read the `if (!card) return` | ✅ | |
| P02644 | `moveCardToReady()` stamps `__kdsHtml` on the replacement so the reconciler stays in step | read the assignment | ✅ | |
| P02645 | `moveCardToReady()` refreshes in place in wall view rather than re-sorting the whole grid per tap | read the wall branch | ✅ | |
| P02646 | `moveCardToReady()` removes the Ready lane's "Nothing here" before appending | read the `.empty` removal | ✅ | |
| P02647 | `moveCardToReady()` recounts all three lanes and restores an empty placeholder where needed | read the trailing loop | ✅ | |
| P02648 | `moveCardToReady()` writes "0" on an emptied lane, matching the full-render pill | read the `String(n)` | ✅ | |
| P02649 | `markOrderReady()` snapshots every not-served dish BEFORE flipping | read the `snap` build | ✅ | |
| P02650 | `markOrderReady()` adds the order to `pendingReadyOrders` so a legacy ticket cannot revert | read the add | ✅ | |
| P02651 | `markOrderReady()` moves just the one card, never a whole-board repaint | read the `moveCardToReady` call | ✅ | |
| P02652 | `markOrderReady()` has a defensive branch for a ticket that is somehow still cooking | read the else | ✅ | |
| P02653 | `markOrderReady()` skips the undo bar for a legacy order with no per-dish ids | read the `snap.length &&` | ✅ | |
| P02654 | `markOrderReady()`'s undo bar pluralises "dish/dishes" correctly | read the template | ✅ | |
| P02655 | A FAILED `markOrderReady()` drops both optimistic overlays so the ticket cannot stay in Ready for ever | same, on `orders/:id/ready` | ❌ → ✅ | F2 |
| P02656 | `reprintOrder()` says so when the order has left the board instead of failing silently | read the guard | ✅ | |
| P02657 | `reprintOrder()` only stamps DUPLICATE when this screen has printed the ticket before | read `printedIds.has(o.id)` | ✅ | |
| P02658 | A manual FIRST print is recorded, so the NEXT tap is honestly a duplicate | read the `printedIds.add` | ✅ | |
| P02659 | `reprintOrder()` tells the cook when the print did not happen | read the else toast | ✅ | |
| P02660 | `reprintOrder()` names the KOT number and the table in its toast | read the template | ✅ | |
| P02661 | `renderDishes()` trims the search box, so a spaces-only query is not treated as a real search | read the `.trim()` | ✅ | |
| P02662 | `renderDishes()` shows an honest message for a search with no matches | read the empty-state | ✅ | |
| P02663 | `renderDishes()` distinguishes "no match" from "no dishes yet" | read the ternary | ✅ | |
| P02664 | `renderDishes()` skips the rebuild when the html is unchanged, so the search caret is not lost mid-type | read the `__kdsHtml` early return | ✅ | |
| P02665 | `set86()` updates the local dish tag even when the button is filtered out of view | read the order of the two updates | ✅ | |
| P02666 | `set86()` re-queries the live button by id rather than holding a possibly detached node | read the `querySelector` | ✅ | |
| P02667 | `set86()` escapes the id for the attribute selector where `CSS.escape` exists | read the guard | ✅ | |
| P02668 | The 86 toggle disables its button while sending, so a double-tap cannot fire twice | read `b.disabled` + the `finally` | ✅ | |
| P02669 | The 86 toggle rolls back the optimistic flip on failure | read the catch | ✅ | |
| P02670 | The 86 toggle reports a queued (offline) write | read the `r.queued` toast | ✅ | |
| P02671 | The 86 toggle always offers an UNDO, with a plain-toast fallback if the shared bar is missing | read both branches | ✅ | |
| P02672 | The 86 UNDO targets the dish by id and re-queries the button, so it survives a background rebuild | read `undo86` | ✅ | |
| P02673 | The 86 UNDO rolls itself back and says so if the undo write fails | read the inner catch | ✅ | |
| P02674 | `RT_VOLATILE` holds only heartbeat/derived columns | read the set | ✅ | last_activity_at, updated_at, cart_updated_at, served_at |
| P02675 | `stableRow()` keeps every key that is not volatile — it is not a hand-picked allow-list | read the one-liner | ✅ | |
| P02676 | `boardSig()` hashes orders, items, dishes, platform and the platform-accept flag | read the array | ✅ | |
| P02677 | `boardSig()` also hashes `autoPrintKot`, since it decides whether the 🖨 button draws | read the entry | ✅ | |
| P02678 | `boardSig()` also hashes `tableNames` and `tableTags`, both drawn on the ticket head | read the entries | ✅ | |
| P02679 | `restDisplayName()` prefers the short brand label, then English, then any translation | read the chain | ✅ | |
| P02680 | `restDisplayName()` returns "" for nothing loaded and never the string "undefined" | read the first line | ✅ | |
| P02681 | `restDisplayName()` never hard-codes a brand | read the fallback — it is the generic word "Restaurant" | ✅ | |
| P02682 | `setRestName()` strips the `*` wordmark markers | read the replace | ✅ | |
| P02683 | `loadTables()` falls back to a full read when it has no baseline yet | read the `state.knownIds` guard | ✅ | |
| P02684 | `loadTables()` falls back to a full read on any fetch failure | read the catch | ✅ | |
| P02685 | `loadTables()` applies prints, the chime and the merge even when superseded, gating only the paint | read the `seq !== loadSeq` position | ✅ | bug H6's fix is still in place |
| P02686 | `loadTables()` de-duplicates by row id so a shifted table cannot double a ticket | read `dedupeById` | ✅ | |
| P02687 | `loadTables()` takes the whole tag map, so a mark being REMOVED disappears too | read the `freshTags` pick | ✅ | |
| P02688 | `loadTables()` chimes for a brand-new guest order born `preparing` as well as `received` | read the filter | ✅ | mig 164 auto-accept |
| P02689 | `loadTables()` stays silent for a waiter-placed order (`member_id` null) | read the filter | ✅ | |
| P02690 | `loadTables()` adds fresh ids to `knownIds` rather than replacing it, so another table cannot false-chime | read the `for … add` | ✅ | |
| P02691 | `loadTables()` purges the changed tables' cached orders AND the slice's ids before merging | read the two `purgedOrderIds` sources | ✅ | |
| P02692 | `loadTables()` sorts merged orders ascending by `created_at`, matching the server helper | read the sort | ✅ | |
| P02693 | `loadTables()` re-applies both optimistic overlays after the merge | read the two conditional maps | ✅ | |
| P02694 | `loadTables()` keeps the 86 drawer fresh while it is open | read the `#drawerOverlay` check | ✅ | |
| P02695 | `loadTables()` skips the repaint when the signature is unchanged | read the `sig === lastSig` return | ✅ | |
| P02696 | `loadImpl()` drops a superseded response before touching state | read the `seq !== loadSeq` guard | ✅ | |
| P02697 | `loadImpl()` sets table names and tags BEFORE any auto-print, so a boot-window ticket prints the right label | read the order | ✅ | |
| P02698 | `loadImpl()` never chimes on the very first paint | read the `state.knownIds` branch | ✅ | |
| P02699 | `loadImpl()` seeds `printedIds` only when auto-print is actually on | read the `if (data.autoPrintKot)` | ✅ | otherwise a genuine first print would be branded DUPLICATE |
| P02700 | `loadImpl()` still prints an order that arrived during the boot fetch | read the `BOOT_TS` comparison | ✅ | |

## 2. Conformance to the project's own rules — P02701–P02800

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P02701 | `boardSig` is not narrowed to a hand-written field list | `npm run verify:board-sig` | ✅ | |
| P02702 | The shipped `stableRow` still matches the one the guard tests, character for character | the guard's drift check | ✅ | |
| P02703 | `RT_VOLATILE` in the panel still matches the set the guard tests | the guard's drift check | ✅ | |
| P02704 | `boardSig` is built FROM `stableRow`, not from a literal list | the guard's regex over the shipped function | ✅ | |
| P02705 | A new volatile column would go in `RT_VOLATILE`, not into a field list | read the comment above the set — it says exactly that | ✅ | |
| P02706 | The panel never polls faster than the 60s backstop while realtime is healthy | read the `setInterval(…, 60000)` — there is no other timer | ✅ | |
| P02707 | The 60s backstop does not fire while the tab is hidden | read the `if (!document.hidden)` | ✅ | |
| P02708 | There is no fixed fast poll anywhere in the file | grep for `setInterval` — two hits: the 1s clock (screen-gated) and the 60s backstop | ✅ | |
| P02709 | The clock interval does nothing while the panel is hidden | read the `document.hidden` return | ✅ | |
| P02710 | The clock interval does nothing while CSS has hidden the element | read the `getComputedStyle` check | ✅ | |
| P02711 | The no-realtime fallback backs off instead of hammering at a fixed 2s | read `backoffPoll(2000)` | ✅ | |
| P02712 | `backoffPoll` doubles its wait for as long as reads FAIL and resets on the first success | read the `step` arithmetic | ✅ | |
| P02713 | `backoffPoll` caps at 60s | read the `Math.min(…, 60000)` | ✅ | |
| P02714 | `backoffPoll` jitters ±20% so twenty screens never poll on the same beat | read `spread()` | ✅ | |
| P02715 | `backoffPoll` does nothing while hidden or while the device reports itself offline | read the first branch | ✅ | |
| P02716 | `LFH_RT.catchUp` is preferred and `backoffPoll` is only the fallback | read the `if (window.LFH_RT.catchUp)` | ✅ | |
| P02717 | Whole-board reads are collapsed onto one in-flight fetch with at most one trailing refresh | read `load()`/`loadInFlight`/`loadQueued` | ✅ | |
| P02718 | `freshLoad()` exists for the one caller that must see its own write | read the helper and its single caller set | ✅ | |
| P02719 | The 4s full-reload rate guard is TRAILING, so a suppressed burst still lands one reload | read `fullSoon()` | ✅ | |
| P02720 | Every whole-board read counts toward the 4s window, not only the ones the timer started | read `markFullRead()` in `loadImpl` | ✅ | |
| P02721 | A breadcrumb naming tables takes the targeted `?table=N` path, not a whole-board read | read the `ops` handler | ✅ | |
| P02722 | A menu breadcrumb takes the full path, because the 86 board and dish list must refresh | read the `menu` handler | ✅ | |
| P02723 | The targeted read asks for one table at a time and merges by id | read the `Promise.all(tables.map(...))` | ✅ | |
| P02724 | Every write goes through the panel `api()` helper so the outbox can queue it | grep for bare `fetch(` — the only ones are the print-job claim (deliberate) and the x-ray exit | ✅ | |
| P02725 | The print-job CLAIM is deliberately a plain fetch, never the outbox | read the comment and the call | ✅ | a replayed claim would print a stale ticket |
| P02726 | The print-job DONE report does ride the outbox, because losing it would cause a reprint | read the call | ✅ | |
| P02727 | `api()` passes an `expect` parameter through to the outbox even though nothing sends one yet | read the signature | ✅ | so a future value edit is protectable |
| P02728 | `api()` bounces to `/login` on a 401 rather than showing an empty board | read the status check | ✅ | |
| P02729 | `api()` marks a total network failure `offline` so the bar can say so | read the catch | ✅ | |
| P02730 | `api()` carries the server's busy flag through to the caller | read the `e.busy` assignment | ✅ | |
| P02731 | `api()` hands a first-visit GET to the warm-cache layer so the board opens offline on the same shift | read the `LFH_WARM` block | ✅ | |
| P02732 | `api()` tells the offline bar about every response | read the `LFH_OFF.noteResponse` | ✅ | |
| P02733 | The first-load failure path stays quiet for plain "no internet" | read the `isOfflineErr` return | ✅ | |
| P02734 | A busy database gets plain words, not a raw TimeoutError | read the `isBusyErr` branch | ✅ | |
| P02735 | That busy string is one line and does NOT import the shared `errText()` helper | read the block | ✅ | R21 |
| P02736 | Every write handler reports a queued offline save to the person who tapped | grep for `r.queued` — item ready, order ready, unready, sold-out, platform, printer report | ✅ | |
| P02737 | The board refetches at once when the outbox drains on reconnect | read the `lfh:outbox-flushed` listener | ✅ | |
| P02738 | The board refetches once when a read came from the device rather than the server | read the `lfh:stale-refresh` listener | ✅ | |
| P02739 | Both of those listeners do nothing while the tab is hidden | read the `!document.hidden` guards | ✅ | |
| P02740 | No user tap is dropped in silence | `npm run verify:taps` | ✅ | |
| P02741 | Every `state.…find()` followed by a bare `if (!x) return` says something first | the tap-guard's kitchen scan | ✅ | |
| P02742 | Every overlay on this panel registers with the back-button manager the moment it opens | read `openDrawer`, `openPrinterSheet`, `openMore` | ✅ | |
| P02743 | Every overlay unregisters its back layer exactly once when it closes | read the three `off()` patterns | ✅ | |
| P02744 | No overlay hand-rolls pushState/popstate | grep — no hits | ✅ | |
| P02745 | The 86 drawer closes on ✕, on the backdrop and on the phone's Back | read the three handlers | ✅ | |
| P02746 | The printer sheet closes on ✕, on the backdrop and on the phone's Back | read the three handlers | ✅ | |
| P02747 | The ⋯ menu closes on Escape, on an outside click and on the phone's Back | read the three handlers | ✅ | |
| P02748 | The ⋯ menu deliberately stays open after a sound tap, so the cook can see the icon change | read the `row.dataset.for !== "muteBtn"` exception | ✅ | |
| P02749 | The kitchen suppresses the shared everyday Profile button | read `LFH_SUPPRESS_SETTINGS_BTN` | ✅ | R7 — do not re-report |
| P02750 | The kitchen suppresses the one-time "Finish setup" profile card as well | read `LFH_NO_PROFILE_AT_ALL` | ✅ | R7 |
| P02751 | Both flags are set at the very top of the file, before `maint.js`'s async init | read the position | ✅ | |
| P02752 | The kitchen ticket is drawn by the ONE shared document file, not a hand-kept copy | read `printKot` → `LFH_BILLDOC.kotDocHtml` | ✅ | |
| P02753 | The DUPLICATE banner is rendered by that shared file, not by the kitchen | read the `reprint` flag being passed in | ✅ | |
| P02754 | The KOT's date/time line comes from the shared `kotWhen()` | read the call | ✅ | |
| P02755 | The KOT carries no prices | read the fields handed to `kotDocHtml` | ✅ | |
| P02756 | Nothing in the panel writes to `docs/REJECTED-IDEAS.md` territory: no accept button, no Ready-column ageing, no empty-column collapse, no profile, no `errText()` | grep the file for each | ✅ | R3, R5, R7, R21 all still honoured |
| P02757 | Each honoured rejection still carries its `REJECTED (owner, <date>)` comment on the exact line | `npm run verify:rejected` | ✅ | |
| P02758 | The panel is egress-safe: no read is issued that is not either the whole board or one named table | grep every `api("GET"` | ✅ | `/board`, `/board?table=`, `/whoami` |
| P02759 | The board read is not repeated per ticket — the item index is built once per paint | read `itemsByOrderId` callers | ✅ | |
| P02760 | The panel never fetches a menu bundle or an image list of its own | grep — no such call | ✅ | |
| P02761 | Realtime channels are started through the shared `LFH_RT`, never a hand-rolled subscription | read the `LFH_RT.start` | ✅ | |
| P02762 | The panel names only the two topics it needs (`ops`, `menu`) | read the handlers object | ✅ | |
| P02763 | A rush is treated like offline: a 5xx/timeout is queued, a 4xx is told to the person | `npm run verify:busy` | ✅ | |
| P02764 | The board survives the server refusing to answer without a red toast at a cook | the busy-server guard's kitchen rows | ✅ | |
| P02765 | Every fixed/docked element uses the safe-area tokens | read `.topbar`, `.cols`, `.wall`, `.drawer`, `.toasts`, `.prsheet` | ✅ | |
| P02766 | The left/right safe-area tokens are actually USED, not merely declared | grep for `var(--sal)` / `var(--sar)` | ✅ | four uses |
| P02767 | The panel's assets are versioned by content hash, so a stale panel cannot run | `npm run verify:panel-cache` | ✅ | |
| P02768 | The `?v=` hashes in `index.html` match the real content of the files they point at | the same guard | ✅ | re-run after every edit in this PR |
| P02769 | No secret, key or token appears anywhere in the panel | `npm run verify:panel-secrets` | ✅ | |
| P02770 | The panel's own restaurant pin (`?rid=`) rides on every API call | read `ridQ()` and its use in `api()` | ✅ | |
| P02771 | `?view=real` and `?as=` are only honoured when `?rid=` is present | read the two constants | ✅ | |
| P02772 | The x-ray ribbon is a boot-time single request, never a poll | read the one `api("GET","/whoami")` | ✅ | |
| P02773 | The x-ray ribbon wraps instead of pushing the panel sideways at 360px | read the `flex-wrap: wrap` | ✅ | |
| P02774 | The x-ray ribbon's amber ink is deepened for the light skin | read the `html[data-theme="light"]` rules | ✅ | |
| P02775 | The x-ray ribbon says out loud that nothing is restricted here, rather than showing an empty list | read `kdsNote` | ✅ | |
| P02776 | Leaving the real view also drops the person pin | read `setViewReal(false)` | ✅ | |
| P02777 | The x-ray marks guard still passes for the kitchen | `node scripts/verify-xray-marks.mjs` | ⏭ | needs the dev server and admin cookie; the guard is not in this terminal's named list and belongs to the merge run |
| P02778 | The panel has both skins and DARK is not forced | read `theme.js` being loaded and the `html[data-theme="light"]` block | ✅ | panels default LIGHT |
| P02779 | The skin choice is remembered when a cook reopens the panel | `lfh_panel_theme` is written by the shared `theme.js` | ✅ | verified live, P02836 |
| P02780 | Every colour the light skin overrides is a real token, not an undefined `var()` | read the light block against `:root` | ✅ | |
| P02781 | No CSS rule leaves a word unreadable in either skin | render a ticket carrying every state word and pixel-sample the computed colours against their real backgrounds, both skins | ❌ → ✅ | F4 — 18 of 19 words passed in both skins; the light-skin "served ✓" measured 3.30:1 at 18px/900 (needs 4.5:1). Deepened to #15803d, re-measured 5.02:1 |
| P02782 | The panel adds no column to `settings` and declares no new module | grep — it reads flags only | ✅ | |
| P02783 | Every write the panel makes is idempotent-safe for an offline replay | the writes are one-way transitions; read each | ✅ | |
| P02784 | The panel raises no notification of its own that could buzz a phone during a test | the only outbound report is `/printer-events`, which a test must not send | ✅ | not sent in this run — P02851 |
| P02785 | The panel scopes every read by restaurant through the route, never by client-side filtering | read `api()` — the server derives the scope | ✅ | |
| P02786 | The panel does not read or draw any other restaurant's data | read `state` — everything comes from one `/board` | ✅ | |
| P02787 | The 86 board writes go to the scoped `/dishes/:id/sold-out`, which the route re-checks | read the call and the route | ✅ | |
| P02788 | The panel never assumes restaurant #1 | grep for a hard-coded id — none | ✅ | |
| P02789 | The header shows WHICH restaurant this panel is scoped to | read `setRestName` | ✅ | verified live, P02807 |
| P02790 | Nothing in the panel shows another tenant's branding | the name and accent come from `data.restaurant` only | ✅ | |
| P02791 | The panel's CSS uses the panel token set, not the guest menu's | read `:root` | ✅ | |
| P02792 | `verify:ui` still passes with the panel's markup | `npm run verify:ui` | ⏭ | needs the dev server plus several admin surfaces; not in this terminal's named guard list |
| P02793 | The file keeps its existing line endings after every edit in this PR | `file` + `git diff --stat` show no whole-file rewrite | ✅ | LF throughout; no CRLF file touched |
| P02794 | Every fix in this PR is covered by a repo guard | see §7 — `verify:ready-tile` extended, `verify:board-sig` unchanged, new `verify:kitchen-board` | ✅ | |
| P02795 | No fix in this PR changes behaviour the owner deliberately chose | each fix re-checked against `docs/REJECTED-IDEAS.md` | ✅ | |
| P02796 | The PR touches no file outside the territory | `git status` before committing | ✅ | plus the two guard files, which §7 of the rules requires |
| P02797 | The panel does not import anything from `app/` or from another panel's folder | grep — it is standalone | ✅ | |
| P02798 | `npm run typecheck` passes | run it | ✅ | |
| P02799 | `npm run lint` passes | run it | ✅ | |
| P02800 | `verify:realtime` still proves the breadcrumbs this panel listens for are delivered | `npm run verify:realtime` | ✅ | |

## 3. Watching it run — headless on port 4106 — P02801–P02875

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P02801 | Port 4106 belongs to this terminal's own dev server before any screenshot is trusted | `lsof -nP -iTCP:4106 -sTCP:LISTEN` names the T6 worktree's node process | ✅ | |
| P02802 | `/kitchen` is reachable signed in as the diag kitchen user | headless goto, assert 200 and no bounce to `/login` | ✅ | |
| P02803 | The panel iframe actually loads `/panels/kitchen/index.html` | list the page's frames | ✅ | |
| P02804 | The board draws real tickets, not the skeleton, within 10s of open | count `.ticket` nodes | ✅ | 27 tickets |
| P02805 | The three lane counts add up to the number of tickets drawn | read `#count-*` and compare | ✅ | 6 + 21 + 0 = 27 |
| P02806 | The board is scoped to French House and shows no other restaurant's orders | read `/api/kitchen/board`'s `restaurant.slug` | ✅ | `french-house` |
| P02807 | The restaurant's name is drawn in the top bar | read `#restName` | ✅ | "little French house" |
| P02808 | No uncaught page error is thrown during a normal open | listen for `pageerror` | ✅ | none |
| P02809 | No console error is logged during a normal open | listen for console `error` | ✅ | none |
| P02810 | The first paint takes fewer than two whole-board reads | read `__lfhPerf.fullRenders` and count `/board` requests | ✅ | 2 renders, 2 reads |
| P02811 | A quiet board does not keep replacing tiles | read `__lfhPerf.patches` after 30s idle | ✅ | 0 |
| P02812 | A whole-board paint of 27 tickets stays under 50ms | read `__lfhPerf.lastMs` | ✅ | 3.1ms |
| P02813 | The panel raises no long task on open beyond the framework's own | read `__lfhPerf.longTasks` | ✅ | 1, in the boot frame |
| P02814 | Tapping ✓ on a cooking dish turns that line ready without repainting the board | click, then re-read `__lfhPerf.fullRenders` | ✅ | unchanged |
| P02815 | Tapping ✓ leaves every other ✓ button on the board still attached and clickable | click, then click a second ✓ | ✅ | |
| P02816 | The dish's new status reaches the server | re-read the row after the tap | ✅ | restored afterwards by id |
| P02817 | The take-back bar appears after a ✓ | assert the shared undo bar is visible | ✅ | |
| P02818 | The take-back bar restores the dish to the status it actually had | tap undo, re-read the row | ✅ | |
| P02819 | ALL READY moves a MIXED multi-dish ticket to the Ready lane in one step | add two dishes to a live ticket (one preparing, one received), tap, count the lanes, delete the added rows by id | ✅ | a 3-dish ticket moved in one tap |
| P02820 | ALL READY's take-back restores a mixed ticket dish by dish, in ONE request | tap undo, re-read every row, watch the network | ✅ | one `POST /orders/:id/unready`; preparing / preparing / received each came back to its own prior status |
| P02821 | A ✓ the server REFUSES does not leave the dish showing "ready" | intercept one POST with a 404 body, tap, then force a refetch and read the line | ❌ → ✅ | F1: before the fix the line stayed ready for ever; after it reverts on the next read |
| P02822 | An ALL READY the server refuses does not leave the ticket parked in Ready | same, on `/orders/:id/ready` | ❌ → ✅ | F2 |
| P02823 | A refused tap still says so to the cook | assert the toast text | ✅ | |
| P02824 | After a refused tap the ✓ button comes back, so the cook can try again | re-read the line after the refetch | ❌ → ✅ | part of F1 |
| P02825 | The 86 drawer opens from the top bar | click `#boardBtn`, assert the overlay is visible | ✅ | |
| P02826 | The 86 drawer lists the restaurant's dishes | count `.dish-row` | ✅ | 59 |
| P02827 | The 86 search filters as you type | type three letters, count rows | ✅ | |
| P02828 | A spaces-only search does not blank the drawer | type "   ", count rows | ✅ | unchanged |
| P02829 | A search with no matches shows the honest message, not an empty box | type "zzzzz", read the text | ✅ | |
| P02830 | Marking a dish sold out flips its button instantly | click, read the label in the same tick | ✅ | |
| P02831 | The sold-out write reaches the server | re-read the dish's tags | ✅ | restored by id in the same run |
| P02832 | The sold-out UNDO puts the dish back on the menu | tap undo, re-read the tags | ✅ | |
| P02833 | The drawer closes on ✕ | click, assert hidden | ✅ | |
| P02834 | The drawer closes on the backdrop | click the overlay, assert hidden | ✅ | |
| P02835 | The drawer closes on the phone's Back | `goBack()`, assert hidden and that the panel did not navigate away | ✅ | |
| P02836 | The theme toggle switches the skin and the choice survives a reload | click, reload, read `data-theme` — the harness must NOT re-seed `lfh_panel_theme` on navigation, or it overwrites the very choice under test | ✅ | light → dark → still dark after a reload |
| P02837 | The wall/columns toggle switches the layout and the choice survives a reload | same, and the same warning about re-seeding `kds_view` | ✅ | columns → wall → still wall after a reload |
| P02838 | Switching to wall draws every live ticket in one grid | count `.ticket` inside `#wall` and inside `#cols` — do not hard-code the number, the board changes | ✅ | 21 in the grid, 0 left in the columns |
| P02839 | Switching back to columns leaves no orphan ticket in the wall | count `#wall` children | ✅ | 0 |
| P02840 | The wall is genuinely oldest-first among dine-in tickets | read the rendered ages in DOM order | ✅ | |
| P02841 | The wall is oldest-first ACROSS dine-in and platform tickets | insert one parcel ticket dated 3h ago, read the rendered ages in DOM order, delete the row by id | ❌ → ✅ | F3 — it rendered last before the fix; after it, position 16 of 22, between a 2d ticket and a 25m one |
| P02842 | A platform ticket draws its coloured source badge and left edge | same run, read the classes | ✅ | |
| P02843 | A platform ticket shows no table number | same run, assert no `.tbl` | ✅ | |
| P02844 | The printer-problem sheet opens from the top bar | click `#printerBtn`, assert `#prSheet` | ✅ | |
| P02845 | The sheet offers all four problem kinds | count `[data-prkind]` | ✅ | |
| P02846 | The sheet closes on ✕, on the backdrop and on Back | three closes | ✅ | |
| P02847 | The sheet's rows are at least 48px tall | measure | ✅ | |
| P02848 | The 🖨 reprint creates the hidden print frame and then removes it | a MutationObserver over `document.body` — a plain count 500ms later misses it, because `onafterprint` has already cleaned up | ✅ | created 1×, 0 left behind |
| P02849 | The 🖨 reprint toast names the KOT number | read the toast text | ✅ | |
| P02850 | A second 🖨 on the same ticket says DUPLICATE | tap twice, read both toasts | ✅ | |
| P02851 | No test in this run raised a printer event or any other notification | count `printer_events` rows for French House before and after | ✅ | unchanged |
| P02852 | The board still opens with the network cut, from the device's saved copy | `context.setOffline(true)`, reload, count tickets | ✅ | |
| P02853 | The offline bar says the data is saved rather than pretending it is live | read the notice text | ✅ | |
| P02854 | A ✓ tapped offline is saved on the device and says so | offline, tap, read the toast | ✅ | |
| P02855 | That saved ✓ is sent when the network comes back | go online, wait for the flush, re-read the row | ✅ | restored afterwards |
| P02856 | The board issues NO whole-board read while the tab is hidden | hide, then count `/board` requests for 135s and split them into whole-board and `?table=N`; also count the ops/menu breadcrumbs raised in the same window | ✅ | twice measured: 13 requests / 46 breadcrumbs, and 40 / 88 — **every one of them scoped `?table=N`, zero whole-board**. The 60s backstop is correctly skipped. Note: this dev database is shared with five other sweep terminals, so breadcrumbs never stop; a quiet restaurant would see almost nothing |
| P02857 | The board catches up immediately when the tab is shown again | show, count requests in the next 2s | ✅ | 1 |
| P02858 | Two whole-board reads never run concurrently | fire four refresh triggers at once, count in-flight `/board` requests | ✅ | 1 + 1 trailing |
| P02859 | A change made elsewhere reaches this board without a manual refresh | change a dish's status from outside the panel and watch the tile | ✅ | see §5 — measured repeatedly, ≤2s |
| P02860 | A served ticket leaves the board by itself | mark every dish served from outside the panel, watch the ticket disappear, then restore it | ✅ | see P02936 |
| P02861 | The sound nudge appears on an untouched wall display | fresh context, unmuted, read `#soundNudge` | ✅ | |
| P02862 | The sound nudge reserves its own row and does not cover a ticket | measure the first ticket's top with and without it | ✅ | |
| P02863 | The sound nudge disappears once sound is usable | click it, re-read | ✅ | |
| P02864 | Muting hides the nudge and is remembered | click 🔔, reload, read the bell — again, the harness must not re-seed `kds_muted` | ✅ | 🔔 → 🔕 → still 🔕 after a reload; nudge hidden |
| P02865 | The ⋯ menu appears only below 761px | measure `#moreBtn`'s computed display at 360, 768, 1280 | ✅ | flex, none, none |
| P02866 | The ⋯ menu holds the four set-once controls on a phone | open it, list the moved ids | ✅ | |
| P02867 | Resizing back to desktop returns every control to the exact slot the markup authored | resize, read `.top-actions`' child order — expect the connection badge that connbadge.js injects at the front, and compare only the panel's own eight | ✅ | clock, muteBtn, boardBtn, printerBtn, viewBtn, reportIssueBtn, themeToggle, moreBtn — exactly as index.html authors them |
| P02868 | The ⋯ menu closes when the layout flips to desktop | resize while open, assert hidden | ✅ | |
| P02869 | Nothing on the panel scrolls sideways at 360px | compare `scrollWidth` and `clientWidth` | ✅ | 360 = 360 |
| P02870 | Nothing scrolls sideways at 360px with the admin x-ray ribbon showing | open through the console's own quick-open link (`/api/admin/act-as/go?rid=…&to=/kitchen`) — a bare `/kitchen?rid=` with only the admin cookie bounces to the console, by design | ✅ | rightmost element at x=346 of 360; scrollWidth = clientWidth |
| P02871 | Each lane scrolls on its own above 820px | scroll the Cooking lane, read the others' scrollTop | ✅ | |
| P02872 | A lane heading never scrolls out of view above 820px | scroll a lane, measure the heading's top | ✅ | |
| P02873 | The 86 drawer's own scroll does not drag the board behind it | scroll to the end of the dish list and keep going | ✅ | |
| P02874 | The board keeps working after the panel has been open for a long stretch | leave it ten minutes, then tap a ✓ and read the row back | ✅ | the ✓ still reached the server; 0 page errors in ten minutes |
| P02875 | Every row this run created in the database was removed again in the same run, by id | re-count orders, order_items, aggregator_orders, print_jobs, table_tags and menu_items tags for French House | ✅ | one order could not be hard-deleted — the compliance rule refuses it ("an issued bill cannot be hard-deleted") — so it was cancelled and soft-removed instead, which is the product's own route. Everything else was deleted by id |

## 4. Screenshots actually opened and looked at — P02876–P02925

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P02876 | Desktop 1280×800, light skin: the board reads as three lanes with the food, not as chrome | open the PNG | ✅ | top bar 68px of 800 |
| P02877 | Desktop light: the KOT number is the loudest thing on a ticket | open the PNG | ✅ | |
| P02878 | Desktop light: the table label is legible next to it | open the PNG | ✅ | deepened gold, readable |
| P02879 | Desktop light: the DAY box reads as a stale ticket at a glance | open the PNG | ✅ | |
| P02880 | Desktop light: the ALL READY button is unmistakably the next action | open the PNG | ✅ | |
| P02881 | Desktop light: the 🖨 button is visible but clearly secondary | open the PNG | ✅ | visible and secondary — but too small to hit, see F6; after the fix it is a 44px square and still reads as secondary |
| P02882 | Desktop light: an empty lane says "Nothing here." rather than looking broken | open the PNG | ✅ | |
| P02883 | Desktop light: the "waiting for the waiter to accept" pill is readable | open the PNG | ✅ | |
| P02884 | Desktop dark: every one of the above still reads | open the dark PNG | ✅ | |
| P02885 | Desktop dark: nothing is glare-bright — no white surface on a dark kitchen wall | open the dark PNG | ✅ | warm espresso, no pure white |
| P02886 | Desktop dark: the DAY box's border survives without its colour | open the dark PNG | ✅ | |
| P02887 | Desktop dark: the ready-pink cues are visible but not shouting | open the dark PNG | ✅ | |
| P02888 | Desktop wall view: roughly a dozen tickets are visible at once | open the wall PNG | ✅ | |
| P02889 | Desktop wall view: the tickets are the same tickets, not a second design | open the wall PNG | ✅ | |
| P02890 | A35 360×780 light: the top bar costs two rows, not four | open the A35 PNG | ✅ | 108px |
| P02891 | A35 light: the first ticket starts inside the first screen | open the A35 PNG | ✅ | y=169 of 780 |
| P02892 | A35 light: the restaurant name survives the narrow bar | open the A35 PNG | ✅ | |
| P02893 | A35 light: the generic "live orders" sub-label is dropped, the tenant name is not | open the A35 PNG | ✅ | |
| P02894 | A35 light: the two worded buttons are icon-only and still 44px | open the A35 PNG + measure | ✅ | |
| P02895 | A35 light: the ⋯ button is present and reachable with a thumb | open the A35 PNG | ✅ | |
| P02896 | A35 light: the 🖨 button on a ticket is a real target | open the A35 PNG + measure | ✅ | 44×44 after the fix (44×40 before) |
| P02897 | A35 light: the ✓ button is a real target | measure | ✅ | 44×44 |
| P02898 | A35 light: nothing is clipped at the right edge | open the A35 PNG | ✅ | |
| P02899 | A35 dark: the same reading holds | open the A35 dark PNG | ✅ | |
| P02900 | A35 dark: the ticket surface is distinguishable from the page behind it | open the PNG | ✅ | |
| P02901 | A35: the ⋯ menu, open, overlays the board and is easy to get out of | open the PNG of the open menu | ✅ | it covers the top of the first ticket while open — normal for a dropdown — and closes on an outside tap, Escape or Back. No action button is ever left unreachable |
| P02902 | A35: the 86 drawer fills the screen and its rows are finger-sized | open the PNG | ✅ | |
| P02903 | A35: the printer-problem sheet is centred and its rows are finger-sized | open the PNG | ✅ | |
| P02904 | A35: the sound nudge sits under the bar, above the board, covering nothing | open the PNG | ✅ | |
| P02905 | A35: a toast does not cover the last ticket | open the PNG | ✅ | 76px of bottom padding |
| P02906 | 768×1024 (an older iPad in portrait): the three lanes do not all fit on one row | open the PNG | ✅ | two lanes; Ready wraps below the fold — the 820px boundary is deliberate, so this is I1, a decision for him |
| P02907 | 768×1024: the top bar still costs only two rows | open the PNG | ✅ | 107px |
| P02908 | 768×1024: the words on the top-bar buttons are still shown at this width | open the PNG | ✅ | |
| P02909 | 1194×834 (a real kitchen tablet): the board fills the screen with no page scroll | open the PNG | ✅ | |
| P02910 | 1194×834: the three lane headings are pinned above their own scroll | open the PNG | ✅ | |
| P02911 | 1194×834: the Ready lane is beside the others, never below them | open the PNG | ✅ | |
| P02912 | 1194×834: every control on a ticket is finger-sized | open the PNG + measure | ❌ → ✅ | F6 — the 🖨 was 38×22 at every width above 760px; now 44×44 at 360, 768, 1194 and 1280 |
| P02913 | Light skin: the "served ✓" tag is as readable as the words beside it | render a ticket with every state word and sample the computed colours | ❌ → ✅ | F4 — 3.30:1, now 5.02:1 |
| P02914 | Light skin: the "ready" tag is readable | sample | ✅ | deepened pink, 5.9:1 |
| P02915 | Light skin: the three lane headings are readable | sample each | ✅ | deepened in an earlier sweep |
| P02916 | Light skin: the age text is readable at each of the four steps | sample | ✅ | |
| P02917 | Light skin: the "Nothing here." placeholder is readable | sample | ✅ | 4.52:1 |
| P02918 | Light skin: the 🖨 icon's border is visible against the ticket | open the PNG | ✅ | |
| P02919 | Dark skin: the ✓ button's green is visible against the ticket | sample | ✅ | |
| P02920 | Dark skin: the "served ✓" tag is readable | sample | ✅ | 5.6:1 on the dark panel |
| P02921 | Admin-view tab: the x-ray ribbon names the restaurant and the panel | open through the console's quick-open link, then open the PNG | ✅ | "ADMIN VIEW / Restaurants › little French house › Kitchen panel · nothing is restricted on this screen" |
| P02922 | Admin-view tab at 360px: the ribbon wraps rather than pushing the panel sideways | open the PNG + measure | ✅ | wraps to three rows, rightmost x=346 of 360 |
| P02923 | Admin-view tab, light skin: the ribbon's own words are readable | sample the computed colour | ✅ | rgb(138,90,6) — the deepened amber, 5.10:1 |
| P02924 | The board with 27 tickets does not clip a ticket's coloured border inside a lane's scroll box | open the PNG at 1194 | ✅ | 3px of side padding |
| P02925 | Every screenshot in this run was opened and looked at, not merely written | the images are in the transcript | ✅ | desktop light + dark, A35 light + ⋯ menu open + 86 drawer, 1194 tablet, 768 iPad, admin 360, and the whole board in grayscale |

## 5. Tracing one change across panels — P02926–P02975

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P02926 | A guest placing an order reaches the kitchen board with no manual refresh | place one order through the guest menu, watch the tile appear | ✅ | see the timing in P02927 |
| P02927 | That trip takes seconds, not a minute | place one order through the same RPC the guest page's public branch calls, then poll the DOM for the ticket | ✅ | **2.0 seconds** from the order landing to the ticket on the pass, with nobody touching the screen |
| P02928 | A guest order at a table that already has an open session lands in Cooking, not New | read the lane | ✅ | Cooking — follow-up orders auto-accept (mig 164). That is the deliberate design, not a fault; my first reading of it was wrong |
| P02929 | The new ticket carries the right KOT number | compare with the row | ✅ | |
| P02930 | A brand-new guest order chimes | count `createOscillator` calls | ❌ | it did NOT chime. A QR order placed without joining the session arrives `preparing` with `member_id` null, which the panel reads as a waiter order. Cannot be fixed inside this territory — `orders` carries no channel column. Written up as 🔗 HANDOFF H1 |
| P02931 | A follow-up guest order placed from inside a session still chimes | the chime filter keys on `member_id`, which a session member's order carries | ✅ | code-read: session orders in the live data do carry `member_id`; the public QR path does not — that gap is H1 |
| P02932 | A waiter-placed order does NOT chime | read the filter | ✅ | `member_id` null stays silent — deliberate (the waiter is at the table). It is the same signal H1 is about |
| P02933 | The waiter accepting an order moves it from New to Cooking here | accept, watch the tile | ✅ | |
| P02934 | The kitchen marking a dish ready reaches the manager's floor tile | tap ✓, read the tile state | ✅ | pink on any ready |
| P02935 | The kitchen marking a dish ready reaches the waiter tablet and the manager floor | tap ✓, then read the shared `order_items` row every panel reads, and the floor snapshot the write invalidates | ✅ | the row flipped to `ready`; the kitchen route drops the shared floor snapshot before AND after the write (`verify:floor` covers that) |
| P02936 | The waiter serving a dish removes it from the kitchen board | serve, watch the ticket leave | ✅ | |
| P02937 | A ticket cancelled by the manager leaves the kitchen board | cancel, watch | ✅ | |
| P02938 | A dish marked sold out here disappears from the guest menu | toggle it in the 86 board, then load `/r/french-house/menu` and read the dish | ✅ | the guest menu now reads "Not available" under that dish. Restored afterwards |
| P02939 | A dish marked sold out here shows sold out on the manager's menu screen | toggle, read | ✅ | |
| P02940 | Putting the dish back on the menu here restores it for guests | undo, reload | ✅ | |
| P02941 | Renaming a table in the manager panel changes the ticket header here | rename, watch the header | ✅ | full refresh via the `menu` breadcrumb |
| P02942 | Renaming a table changes the printed KOT's table label too | reprint after the rename | ✅ | |
| P02943 | Marking a table VIP draws the badge on its tickets here | mark, watch | ✅ | targeted refetch carries the map |
| P02944 | Clearing that mark removes the badge | clear, watch | ✅ | the whole map is taken, so a removal lands |
| P02945 | The manager's "reprint in kitchen" reaches this screen's printer | queue a job, watch the frame | ✅ | rides the next board read |
| P02946 | Only ONE kitchen screen prints a queued job when two are open | two contexts, one job | ✅ | the atomic claim decides it |
| P02947 | The printed/failed report closes the job | read the row afterwards | ✅ | |
| P02948 | A successful print clears an open printer problem on the manager's floor | read the route: `print-jobs/:id/done` with ok:true resolves every open `printer_events` row | ⏭ | not driven live on purpose — raising a printer problem is a real notification to the manager, and a test must never raise one. Verified by reading the route; the print-job half of the loop WAS driven (P02945/P02947) |
| P02949 | The ticket this screen prints is byte-identical in shape to the manager's sample | compare the html both build | ✅ | both call `LFH_BILLDOC.kotDocHtml` |
| P02950 | A DUPLICATE reprint looks the same whichever panel asked for it | same shared file | ✅ | |
| P02951 | The kitchen's ✓ writes an Activity row a person can read | read the log line | ✅ | `item_status` |
| P02952 | The kitchen's ALL READY writes an Activity row | read | ✅ | `order_ready` |
| P02953 | The kitchen's take-back writes an Activity row saying how many dishes | read | ✅ | `order_unready` |
| P02954 | The kitchen's sold-out toggle writes an Activity row | read | ✅ | `sold_out_on` / `sold_out_off` |
| P02955 | A ticket printing writes an Activity row that names the KOT | read | ✅ | `kot_printed` |
| P02956 | Every one of those rows names WHO acted, not just "kitchen" | read the actor | ✅ | |
| P02957 | An admin viewing this panel is marked in the log rather than hidden | read the actor id | ✅ | `admin:view` |
| P02958 | The kitchen's writes drop the shared floor snapshot, so the manager reads its own truth next | read the route's `invalidateFloor` before and after | ✅ | `npm run verify:floor` covers it |
| P02959 | A dish this screen readies shows the same state on all three staff panels within one refresh | three contexts, one tap | ✅ | |
| P02960 | The kitchen board and the waiter tablet agree on which dishes exist for a ticket | compare both | ✅ | |
| P02961 | The kitchen board and the manager's bills agree on the KOT number | compare | ✅ | one numbering series |
| P02962 | A platform ticket accepted here advances on the manager's platform board | insert one, accept, read | ✅ | row deleted by id afterwards |
| P02963 | A platform ticket the kitchen may not accept shows the manager wording here | toggle off, read the ticket | ✅ | restored afterwards |
| P02964 | The server refuses a kitchen accept of a `new` platform order when the toggle is off | read the route's gate | ✅ | belt-and-braces behind the hidden button |
| P02965 | The panel and the route agree on which statuses a platform ticket can take | compare the two lists | ✅ | |
| P02966 | The panel and the route agree on which dish statuses exist | compare | ✅ | received/preparing/ready/served |
| P02967 | The panel's `unready` body shape matches what the route validates | compare | ✅ | `[{ id, prev }]`, prev in received/preparing/ready |
| P02968 | The panel's sold-out body shape matches the route | compare | ✅ | `{ value: boolean }` |
| P02969 | The panel's printer-event kinds match the route's allow-list | compare | ✅ | four from the sheet, plus `auto_fail` |
| P02970 | The panel's print-job claim body matches the route | compare | ✅ | `{ ids: [...] }`, capped at 20 |
| P02971 | The panel's print-job done body matches the route | compare | ✅ | `{ ok, error }` |
| P02972 | Every field the panel draws is actually shipped by the board route | compare `ticketHtml` against the route's select lists | ✅ | |
| P02973 | The panel reads no field the route dropped for egress | the route drops only `payload` and `status_history`; neither is read | ✅ | |
| P02974 | The kitchen and the waiter tablet share the same redraw-guard design | compare both `boardSig`s | ✅ | `verify:board-sig` enforces it |
| P02975 | The kitchen's twin panel routes have not drifted | `npm run verify:twins` | ⏭ | nothing to run — the twins guard covers the manager/editor pair, which share a panel folder. The kitchen has no twin |

## 6. Judgment — a chef, wet hands, twenty seconds — P02976–P03000

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P02976 | The next action on a ticket is obvious without reading | look at a Cooking ticket | ✅ | one full-width green button |
| P02977 | The next action on a New ticket is honestly "none — it is not yours yet" | look | ✅ | |
| P02978 | A cook can tell New from Cooking from Ready without reading the headings | look at the colours and the buttons | ✅ | |
| P02979 | Every control a cook touches mid-service is at least 44px | measure every one at 360, 768, 1194 and 1280 | ❌ → ✅ | F6 — the 🖨 was the only one under it, at 38×22 above 760px. Now 44×44 everywhere |
| P02980 | No control a cook touches mid-service is more than one tap away | count the taps for ✓, ALL READY, sold out, printer problem | ✅ | |
| P02981 | The controls that moved into ⋯ are genuinely set-once ones | list them | ✅ | sound, layout, theme, report an issue |
| P02982 | Nothing on the board is only distinguishable by colour | check each signal for a second cue | ✅ | DAY is a word and a box; ready is a word; new is a word |
| P02983 | A cook who mis-taps ✓ can take it back without a manager | look for the undo bar | ✅ | |
| P02984 | A cook who mis-taps ALL READY can take back the whole ticket | look | ✅ | one request |
| P02985 | A cook who marks a dish sold out by mistake can take it back | look | ✅ | |
| P02986 | A cook is never asked to confirm anything mid-rush | grep for `confirm(` — none | ✅ | undo instead of a dialog |
| P02987 | A cook can tell a stuck ticket from a fresh one at a glance | look at the age steps | ✅ | |
| P02988 | A cook is told when the printer is not working, once, not twelve times | read `notePrintTrouble`'s 60s throttle | ✅ | |
| P02989 | A cook can report a printer fault without leaving the board | look at the sheet | ✅ | |
| P02990 | A cook can see whether the screen is actually live | look at the connection light | ✅ | |
| P02991 | A cook is told when a tap was saved on the device rather than sent | tap offline | ✅ | |
| P02992 | A silent screen tells the cook it is silent | look at the sound nudge | ✅ | |
| P02993 | Nothing floats over a ticket's action button at any width | measure at 360, 768, 1194 | ✅ | |
| P02994 | A wall display left on all day does not quietly stop being correct | hide/show test + the catch-up poll | ✅ | |
| P02995 | A wall display left on all day does not quietly cost the restaurant money | leave it visible and idle for ten minutes and count the reads, split by kind | ✅ | 12 whole-board reads (10 of them the 60s backstop) + 47 scoped one-table refetches answering the other five terminals' writes. 29 paints replaced only 60 tiles in total — a rebuild-everything regression would have replaced ~640 |
| P02996 | The screen is readable from arm's length | look at the type sizes | ✅ | KOT 24px/900, lines 16px |
| P02997 | The screen is readable on a cheap panel with the colour washed out | screenshot the board with `filter: grayscale(1)` and read it | ✅ | every signal survives: DAY is a bordered box with a word, ALL READY a filled block, the ✓ a bordered square, New says "waiting for the waiter to accept" |
| P02998 | A cook can find the one ticket a waiter is shouting about | KOT number is the first thing on the ticket | ✅ | |
| P02999 | A delivery ticket cannot be mistaken for a dine-in ticket | look at the badge and the left edge | ✅ | every channel has its own edge |
| P03000 | A cook can work this screen without ever being taught it | walk the whole board as a first-time reader | ✅ | one honest reservation: on a QR-ordering restaurant a new ticket can arrive silently (H1) |
