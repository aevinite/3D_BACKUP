# SWEEP #6 · TERMINAL 14 — the owner's Customers, Pay Later (khata), Inventory, Complaints & Manager mode

**Phases P06501–P07000 (500).** Branch `sweep6/t14-owner-customers-money` · worktree
`sweep6/T14` · dev server port **4114** (never 4000). Restaurant written to: **French House**
(id `…0001`); **Aangan is read-only** and never touched by these phases.

## Territory (the only files these phases may change)

| file | lines (at origin/main af4d57db) |
|---|---|
| `app/owner/customers/page.tsx` | 335 |
| `app/owner/khata/page.tsx` | 166 |
| `app/owner/issues/page.tsx` | 349 |
| `app/owner/inventory/page.tsx` | 54 |
| `app/owner/manager/page.tsx` | 91 |

Guards added/repaired by this run: `scripts/verify-owner-money-screens.mjs`
(`npm run verify:owner-money`) and `scripts/verify-customer-erase.mjs`.

## How the 500 are budgeted

| band | ids | count | share |
|---|---|---|---|
| A · reading the code for correctness | P06501–P06700 | 200 | 40% |
| B · conformance to the project's own written rules | P06701–P06800 | 100 | 20% |
| C · watching it run (headless Playwright, port 4114) | P06801–P06875 | 75 | 15% |
| D · screenshots actually read (1280×800 **and** A35 360×780 dpr3) | P06876–P06925 | 50 | 10% |
| E · tracing one change across panels / server / DB | P06926–P06975 | 50 | 10% |
| F · my own judgment — does a real restaurant need it this way | P06976–P07000 | 25 | 5% |

Per-file coverage inside band A: customers 70 · khata 40 · issues 45 · inventory 22 ·
manager 23. Every file in the territory carries far more than one phase.

`result` is the **second pass** (every phase re-run from the top after the fixes landed).
A row that failed on the first pass says so in its note, with the item number that fixed it.

---

---

## ↻ SWEEP #7 SECOND PASS — every row above re-executed, 2026-08-27

**Branch `sweep7/t14-owner-customers`, worktree `../wt-s7-t14`, port 4214, against `origin/main`
d7af8f63.** Restaurant written to: **French House** only — two fixture rows (phones
`9000021601` / `9000021602`), both deleted by their own key in the same run, plus one existing
rating handled/noted/reopened and then restored field-by-field to what it was found as. **Aangan was
never written to. AV live was never read.** One owner sign-in for the whole run (`loginAs` cache).

**All 500 rows `P06501`–`P07000` were re-run, not sampled.** How:

| how a row was re-executed | rows |
|---|---|
| a literal assertion against the file AS IT IS TODAY, run as code rather than eyeballed | 194 |
| a `verify:*` guard run in this worktree — owner-money 26 · customer-erase 15 · personal-data 16 · owner-panel 61 · owner-territory 49 · owner-screen 61 · owner-clash 11 · taps 33 · customers 65 · recycle-name · ui · server-only · clash-coverage · typecheck · lint | 40 |
| driven live and headless on port 4214 — five screens, the guest record, an erase end to end, a rating handled + noted + reopened, both skins, 1280×800 and A35 360×780 dpr3 | 168 |
| a screenshot taken and then LOOKED AT | 50 |
| traced by reading across page → route → RPC → the panel at the other end | 48 |

### What the second pass found

| | |
|---|---|
| rows re-run | **500** |
| **regressions (was ✅, now ❌)** | **0** |
| rows that were ✅ and are ✅ | **486** |
| rows filed ✅ on a claim that was never true → now ❌ and FIXED on this branch | **1** — `P06616` |
| expectations that MOVED because the product legitimately changed | **4** — `P06585` `P06738` `P06921` `P06962`, all four the same reversal: the owner rejected sweep-6 item 4 (`docs/REJECTED-IDEAS.md` R34) |
| `⏭` rows CLOSED this run | **4** — `P06542` `P06846` `P06922` `P06990` |
| `⏭` rows still reserved, each with its reason on the row | **3** — `P06677` `P06697` `P06923` |

**Two things this pass paid for twice. Both are recorded because both cost real time.**

1. **A `mustNot` that reads comments accuses the sentence explaining the fix.** Sweep-7 item 19's new
   rule forbids `summary.total > summary.shown` — the fix removed it from the code and explained it
   in a comment above, and `verify:owner-money` went red pointing at its own obituary.
   `scripts/verify-owner-money-screens.mjs` now strips comments before testing any `mustNot`. My own
   re-run checker had the identical fault: **five of its first six "failures" were it matching a
   comment**, not the code. This is the ledger's own "a red guard can be asserting a retired rule"
   lesson arriving from a new direction.
2. **A panel's service worker eats a fault-injected reply.** Crafting a server answer with Playwright
   worked for a page's FIRST request and silently stopped working for every later one — the guest
   record's request went to the real server and the crafted reply never arrived, which read exactly
   like a bug in the drawer. The browser context must be created with `serviceWorkers: "block"`.

**A note on method, because it closed four `⏭` rows sweep 6 could not.** Several states this
territory has to get right cannot be produced by the dev database without writing something that
must not be written — a long credit book needs open pay-later bills (an issued sale), a capped
ratings list needs 381 ratings, a switched-off section needs another restaurant's entitlements
flipped while other terminals are reading it. Those were driven instead against a **crafted server
reply** (`route.fulfill`), which renders the REAL page, the REAL stylesheet and the REAL branch
while writing nothing at all. Rows closed that way say so on the row.


## A · READING THE CODE FOR CORRECTNESS (P06501–P06700)

### A1 · `app/owner/customers/page.tsx` (P06501–P06570)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P06501 | The file is a `"use client"` component and never imports a server-only module | read line 1; grep the import block for `lib/ownerScope`, `lib/supabaseAdmin` | ✅ | imports only `components/owner/*`, `lib/ownerPin`, `lib/backStack` |
| P06502 | `scopePin` is read ONCE from `?rid` at mount, into state, not on every render | read the `useState(() => …)` initialiser | ✅ | lazy initialiser, no re-read |
| P06503 | `scopePin` is null when `window` is undefined (server render) | read the same initialiser | ✅ | guards `typeof window === "undefined"` |
| P06504 | Every list request carries `scope=` + `asSuffix()` when the admin pinned a restaurant | read `load()`'s `qs` array | ✅ | |
| P06505 | The search term is URL-encoded before it goes in the query string | read `q=${encodeURIComponent(s)}` | ✅ | |
| P06506 | An empty/whitespace search sends NO `q=` at all (never `q=` matching everything) | read `const s = sv.trim()` then `s ? …` | ✅ | |
| P06507 | The restaurant filter is only sent when a restaurant is actually chosen | read `rv ? \`restaurant_id=${rv}\` : ""` | ✅ | |
| P06508 | Default segment/sort are omitted from the URL, so the default read is the cheapest one | read `gv !== "all"` / `sov !== "last_seen_at"` | ✅ | |
| P06509 | The list fetch is `cache: "no-store"` — an owner never sees a stale browser copy | read the fetch options | ✅ | |
| P06510 | `j.disabled` renders the "isn't enabled — contact Aevidine" card, not an empty list | read the `disabled` branch | ✅ | matches the API's 403 `disabled:true` |
| P06511 | `j.error` throws so the error card shows, instead of rendering zero guests | read `if (j.error) throw new Error(j.error)` | ✅ | |
| P06512 | A failed load leaves `customers` at `null`, so "Loading…" ≠ "no customers" | read the `err`/`customers === null` branches | ✅ | |
| P06513 | The filters live in a ref (`filt.current`) so `load` is stable and the 60s timer isn't rebuilt each keystroke | read `filt.current = {…}` and `load`'s deps | ✅ | dep is `[scopePin]` only |
| P06514 | First load fires immediately; later loads are debounced 350ms | read the `firstRun` ref effect | ✅ | one effect, no double-fetch at mount |
| P06515 | The debounce timer is cleared on unmount / on the next keystroke | read the effect's `return () => clearTimeout(t)` | ✅ | |
| P06516 | The 60s backstop is an interval, not a faster poll (egress rule) | read `setInterval(…, 60_000)` | ✅ | |
| P06517 | The backstop skips a tick while the tab is hidden | read `if (!document.hidden) load()` | ✅ | |
| P06518 | The backstop is torn down on unmount and its listener removed | read the effect's cleanup | ✅ | both `clearInterval` and `removeEventListener` |
| P06519 | Coming back to a hidden tab reloads immediately AND restarts the timer | read `onVis` | ✅ | |
| P06520 | `start()` cannot stack two intervals if called twice | read `if (!t)` | ✅ | |
| P06521 | The erase asks for confirmation before anything is sent | read `window.confirm` guard | ✅ | |
| P06522 | The confirm names WHAT IS DELETED and WHAT SURVIVES (bills keep the name) | read the confirm text | ✅ | the T7 I6 wording is still there |
| P06523 | The erase is keyed by `restaurant_id:phone`, so the same number at two restaurants is two rows | read `const key = …` | ✅ | |
| P06524 | Only the row being erased is disabled, never the whole table | read `disabled={erasing === key}` | ✅ | |
| P06525 | `erasing` is cleared in a `finally`, so a failed erase never leaves a dead button | read the `finally` block | ✅ | |
| P06526 | A non-2xx erase response is turned into a visible error, not swallowed | read `if (!r.ok \|\| j.error) throw` | ✅ | |
| P06527 | The 409 "still owes money" message from the server is shown verbatim to the owner | read the `throw new Error(j.error)` path | ✅ | server sends the ₹ and bill count |
| P06528 | A successful erase removes exactly that one row from the list, not every row with that phone | read the `setCustomers(prev => …filter…)` predicate | ✅ | matches on rid AND phone |
| P06529 | The erase button stops the row click from opening the record behind the confirm | read `e.stopPropagation()` | ✅ | |
| P06530 | The erase button has an accessible name that includes the guest | read `aria-label={\`Erase ${…}\`}` | ✅ | |
| P06531 | **After a successful erase the four tiles are recounted** | read whether `load()` runs after the filter, and with what | ✅ | ❌ on pass 1 — tiles kept the erased guest; **fixed by item 1** |
| P06532 | **The Refresh button forces a live recount (`?refresh=1`), per the snapshot-cache rule** | read the Refresh `onClick` | ✅ | ❌ on pass 1 — sent no `refresh` param; **fixed by item 1** |
| P06533 | Opening one guest's record does NOT also pay for the four tiles | read the API's early return before `cachedOwnerPayload` | ✅ | server-side, confirmed by reading the route |
| P06534 | The record drawer closes on Escape | read the `keydown` effect | ✅ | |
| P06535 | The Escape listener is only mounted while the drawer is up | read the effect's early `return` | ✅ | |
| P06536 | The Escape listener is removed on close | read the cleanup | ✅ | |
| P06537 | **Escape/Back/✕/backdrop close the drawer even while the record is still loading** | click a row and press Escape immediately | ✅ | ❌ on pass 1 — the tap did nothing and the record opened anyway; **fixed by item 2** |
| P06538 | The phone's hardware Back closes the drawer instead of leaving the page | `useBackClose("owner-customer-detail", …)` present + driven headless | ✅ | |
| P06539 | The back layer's id is unique to this drawer | grep `useBackClose(` across `app/owner` | ✅ | only this page uses that id |
| P06540 | The drawer is a real dialog for a screen reader | read `role="dialog"` + `aria-modal` + `aria-label` | ✅ | |
| P06541 | Clicking inside the drawer does not close it | read the inner `onClick={(e) => e.stopPropagation()}` | ✅ | |
| P06542 | Two quick row clicks can never show the FIRST guest's record under the second guest's name | click two rows in quick succession, headless | ✅ | ↻ S7 CLOSED (was ⏭). Driven 2026-08-27: tapping row 1 then row 2 within 2.5s — the second click is REFUSED (the modal overlay intercepts the pointer) and the drawer shows the FIRST guest, which is the one that was asked for. The sequence guard (item 2) is what makes CLOSING stick; the blocking is the overlay. Both behaviours are right. |
| P06543 | The drawer's phone number is grouped `XXXXX XXXXX` like the table's | read the drawer header expression | ✅ | |
| P06544 | Money in the drawer is rounded, never a raw float with 11 decimals | read `Math.round(detail.lifetime)` etc. | ✅ | |
| P06545 | A guest with no bills gets a sentence, not an empty box | read the `!detail.bills.length` branch | ✅ | |
| P06546 | When the guest has more bills than the page shows, the drawer says so | read the `detail.bill_count > detail.bills.length` line | ✅ | |
| P06547 | The multi-restaurant line only appears when the number really is at 2+ restaurants | read `detail.rows.length > 1` | ✅ | |
| P06548 | Each restaurant row in the drawer is keyed by `restaurant_id` (no React key collision) | read the `.map` key | ✅ | |
| P06549 | Each bill row is keyed by `session_id` | read the `.map` key | ✅ | |
| P06550 | The table rows are keyed by `restaurant_id:phone`, unique across restaurants | read the `<tr key>` | ✅ | |
| P06551 | A blocked guest's row is visibly dimmed AND carries a "blocked" chip | read `opacity` + the chip branch | ✅ | belt and braces, not colour alone |
| P06552 | "regular" vs "new" is decided by the server's `returning`, not re-derived on the page | read the chip branch | ✅ | |
| P06553 | A guest with no name shows "Guest", never an empty cell | read `c.name \|\| <span>Guest</span>` | ✅ | |
| P06554 | A missing phone shows "—", never "undefined" | read `showPhone` | ✅ | |
| P06555 | `showPhone` only groups a real 10-digit number, and passes anything else through | read the length check | ✅ | |
| P06556 | Dates are rendered in India time on every row | read `fmt` → `timeZone: IST` | ✅ | |
| P06557 | `first_seen_at`/`last_seen_at` can never be null, so no "Invalid Date" is possible | read mig `014_sessions_v2_schema.sql` — both are `NOT NULL DEFAULT NOW()` | ✅ | |
| P06558 | Visit counts are tabular figures so the column lines up | read `fontVariantNumeric` | ✅ | |
| P06559 | The consent tick only shows for a guest who actually consented | read `c.consent && …` | ✅ | |
| P06560 | The consent tick has a title AND an aria-label (not colour-only meaning) | read the `<i>` attributes | ✅ | |
| P06561 | During a search the footer says "N matches", never "the N most recent of TOTAL" | read the `search ? … :` branch | ✅ | the 2026-07-09 correction is intact |
| P06562 | With no search, the footer only appears when the list really is shorter than the total | read `summary.total > summary.shown` | ✅ | ↻ S7 ✅ for exactly what it asks — but the row asked the wrong question. `summary.total` is the whole-scope head-count, so on a GROUP tab the line read “the 2 most-recent of 26” with both blocked guests on screen. Widened by `P21630`–`P21634`; fixed as sweep-7 item 1. |
| P06563 | "1 match" is singular, "2 matches" plural | read the `rows.length === 1` ternary | ✅ | |
| P06564 | The empty state distinguishes "no match for your search" from "no customers yet" | read the two empty strings | ✅ | |
| P06565 | The restaurant picker only appears for an owner with more than one restaurant | read `rests.length > 1` | ✅ | |
| P06566 | The restaurant picker's option list comes from the API's whole scope, not the shown page | read the route's `restaurantList` (built from `ids`) | ✅ | |
| P06567 | The segment and sort strips are real tabs for a screen reader | read `role="tablist"` / `role="tab"` / `aria-selected` | ✅ | |
| P06568 | The search box has a label | read `aria-label="Search customers"` | ✅ | |
| P06569 | **The page reacts when the server says a figure could not be read (`partial`)** | read `load()` for any use of `j.partial` | ✅ | ❌ on pass 1 — ignored; **fixed by item 5** |
| P06570 | No `any`, no `@ts-ignore`, no stray `console.log` in the file | grep the file | ✅ | |

### A2 · `app/owner/khata/page.tsx` — the credit book (P06571–P06610)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P06571 | The page is client-side and imports `partialNote` from `lib/partialRead`, NOT from `lib/ownerScope` | read the import block + its comment | ✅ | the 2026-08-06 "supabaseKey is required" crash cannot come back here |
| P06572 | Nothing in this file's import graph reaches `lib/supabaseAdmin` | `npm run verify:server-only` | ✅ | guard passes |
| P06573 | The page is READ-ONLY — it never POSTs, PATCHes or DELETEs | grep the file for `method:` | ✅ | collecting stays in the manager panel, as the header comment says |
| P06574 | Every request carries the admin scope pin when there is one | read `qs = scopePin ? … : ""` | ✅ | |
| P06575 | The load is `cache: "no-store"` | read the fetch options | ✅ | |
| P06576 | `collectedToday`/`collectedMonth` are typed `number \| null`, not `number` | read the `Summary` type | ✅ | null = "couldn't read", never a claim about the till |
| P06577 | A figure that could not be read renders "—", never "₹0" | read the two tiles' ternaries | ✅ | `inr(0)` and "unread" must never look the same |
| P06578 | "Outstanding now" and "People who owe" come from the server aggregate, not from summing the rows | read the route's `agg` block | ✅ | mig 309 aggregate; a capped list cannot shrink the headline |
| P06579 | The unread-figure banner names WHICH figure, in plain words | read `partialNote(partial)` | ✅ | shared wording, one voice |
| P06580 | The banner offers a retry that actually re-requests | read the "Try again" `onClick={() => load()}` | ✅ | |
| P06581 | `partial` is cleared on every load, so a passing blip disappears by itself | read `setPartial(Array.isArray(j.partial) ? j.partial : [])` | ✅ | |
| P06582 | When the person list is capped, the page says so and says the tiles still count everyone | read the `shown` paragraph | ✅ | T7 F13's fix is intact |
| P06583 | The capped note is a plain statement, not a scary warning bar | read its class (`adm-page-sub`) | ✅ | the "don't cry wolf" rule |
| P06584 | `listCapped` is only rendered when the server actually said so | read `j.listCapped ? … : null` | ✅ | |
| P06585 | **A restaurant with pay-later switched OFF is shown the book anyway — this page never decides whether Pay Later exists (R34)** | read how `j.moduleOff` is handled | ✅ | ↻ S7 EXPECTATION MOVED. The owner REVERSED sweep-6 item 4 on 2026-08-18 — *“if the feature is on by me, it will stay on”* (`docs/REJECTED-IDEAS.md` R34). The page must now do the OPPOSITE. Re-run: `moduleOff` is deliberately unread, the R34 note is in the file, `setModuleOff` appears nowhere, and no “isn’t enabled” sentence exists. Same id, inverted expectation. |
| P06586 | The 60s backstop is paused while the tab is hidden | read the visibility effect | ✅ | |
| P06587 | The backstop interval is cleared on unmount | read the cleanup | ✅ | |
| P06588 | Coming back to the tab reloads immediately | read `onVis` | ✅ | |
| P06589 | The search filters name AND phone, case-insensitively | read the `rows` filter | ✅ | |
| P06590 | The search is client-side over the already-loaded list — it fires no new request | read the filter (no fetch in the path) | ✅ | deliberate: the list is already bounded |
| P06591 | **A search that matches nobody says whether it searched the WHOLE book or just the shown part** | read the `q ? … : …` empty state against `shown` | ✅ | ❌ on pass 1 — "No one matches that search" was unconditional; **fixed by item 6** |
| P06592 | A person's own bills all sit under that person (bounded by PERSON, not by bill) | read the route's `PEOPLE_SHOWN` comment + `byCust` grouping | ✅ | so a person's figure is never half a figure |
| P06593 | Each person's outstanding total is rounded to paise, not floated | read `Math.round((c.outstanding + amt) * 100) / 100` | ✅ | |
| P06594 | The oldest bill date per person is the MINIMUM, not the first row seen | read `if (r.khata_at < c.oldestKhataAt)` | ✅ | |
| P06595 | People are listed biggest debt first | read the route's `.sort((a,b) => b.outstanding - a.outstanding)` | ✅ | |
| P06596 | The restaurant chip only appears for a multi-restaurant estate | read `const multi = …` | ✅ | |
| P06597 | `multi` is computed from the rows actually returned, so it can't claim a brand the owner can't see | read the `new Set(...).size > 1` | ✅ | |
| P06598 | A person with no mobile reads "no mobile", never a blank | read `c.phone \|\| "no mobile"` | ✅ | |
| P06599 | Expanding one person doesn't collapse another | read the `Set`-based `toggle` | ✅ | |
| P06600 | The expander is a real button with `aria-expanded` | read the `<button aria-expanded>` | ✅ | |
| P06601 | A long name is ellipsised rather than pushing the money off the row | read the `textOverflow: "ellipsis"` block | ✅ | |
| P06602 | Money is tabular-figures in both the row and the bill lines | read `fontVariantNumeric` on both | ✅ | |
| P06603 | "1 bill" is singular, "2 bills" plural | read `c.billCount === 1 ? "" : "s"` | ✅ | |
| P06604 | A tab opened today reads "today", not "0 days" | read `ageDays` | ✅ | |
| P06605 | A tab one day old reads "1 day", not "1 days" | read `ageDays` | ✅ | |
| P06606 | A clock skew (future date) cannot print "-3 days" | read `d <= 0 ? "today"` | ✅ | |
| P06607 | **An OLD unpaid tab is visually distinct from a fresh one** | look at the age text's styling | ✅ | ❌ on pass 1 — every age looked the same; **fixed by item 10** |
| P06608 | A bill with no bill number still renders a row ("Bill · date · T?") | read the `b.bill_no != null` ternary | ✅ | |
| P06609 | The last bill in a person's list has no dangling divider | read the `i < c.bills.length - 1` border rule | ✅ | |
| P06610 | Dates in the credit book are India time | read `fmt` → `timeZone: IST` | ✅ | |

### A3 · `app/owner/issues/page.tsx` — guest ratings + staff complaints (P06611–P06655)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P06611 | Both tabs are scoped by the same `scp` suffix, so one tab can't read another restaurant's rows | read `const scp = …` and both loaders | ✅ | |
| P06612 | The suffix sends BOTH `scope=` (auth pin) and `rid=` (narrowing), like the Reports page | read the `scp` template | ✅ | |
| P06613 | A real owner (no `?rid`) sends no suffix at all and gets their whole estate | read `scopePin ? … : ""` | ✅ | |
| P06614 | A tab the admin switched off hides itself instead of showing a broken panel | read `ratingsOff` / `issuesOff` | ✅ | |
| P06615 | Both off → one clear sentence, not two empty tabs | read the `bothOff` branch | ✅ | |
| P06616 | The first available tab is chosen once, and never fights the owner's later click | DRIVE it: ratings 403 `disabled`, complaints 200 — assert the complaint actually RENDERS | ❌ → ✅ fixed | ↻ S7 **FILED GREEN ON A CLAIM THAT WAS NEVER TRUE** — the row READ the `decided` ref instead of driving it. Driven 2026-08-27 with Guest ratings off and Complaints on: `decided.current` latched on the FIRST render, before either request answered, so the tab never moved and the screen showed a lone “Complaints · 1” button with NOTHING under it, an open complaint hidden behind it. Untouched since PR #199. **Fixed by sweep-7 item 4** and re-driven green. Not a regression — it never worked. |
| P06617 | A load failure is told apart from "nothing here" on the ratings tab | read the `rErr && ratings === null` branch | ✅ | "this is a loading error, not 'no ratings'" |
| P06618 | Same on the complaints tab | read the `iErr && issues === null` branch | ✅ | "not 'all clear'" |
| P06619 | Both loaders run together at mount rather than in series | read `Promise.all` in `loadAll` | ✅ | |
| P06620 | The 60s backstop refreshes both tabs and pauses while hidden | read the visibility effect | ✅ | |
| P06621 | Marking a rating handled updates the row instantly (optimistic) | read `setRatings(...)` before the fetch | ✅ | |
| P06622 | …and the "To handle · N" badge moves with it, only when the state really flipped | read the `setSummary` guard `if (was === acknowledged) return s` | ✅ | |
| P06623 | …and can never go below zero | read `Math.max(0, …)` | ✅ | |
| P06624 | A failed "Mark handled" is surfaced AND the true state is restored | read the `!res.ok` throw + `await loadRatings()` in both paths | ✅ | no false optimistic tick |
| P06625 | The note editor only closes after the save actually succeeded | read the ordering in `saveNote` | ✅ | a failed PATCH used to wipe the typed note |
| P06626 | The note save sends `X-LFH-Expect` so a co-owner who typed first wins | read the header | ✅ | the no-silent-overwrites rule |
| P06627 | The expectation is what the note said when the editor OPENED, not what it says now | read `const was = (ratings \|\| []).find(...)` | ✅ | |
| P06628 | A clash is reported in the server's plain words plus what to do | read the `c?.plain` / `c?.todo` branch | ✅ | |
| P06629 | The note box is length-capped on the client too | read `maxLength={500}` | ✅ | server caps at 1000 as well |
| P06630 | Resolve/Reopen rolls the row back on failure and says why | read the `catch` in `setIssueStatus` | ✅ | |
| P06631 | Every action button disables itself while its own row is busy | read `disabled={busy === id}` on all four | ✅ | |
| P06632 | `busy` is cleared in a `finally` on all three writers | read the three `finally` blocks | ✅ | |
| P06633 | **The "Complaints · N" badge is the server's true open count, not a count of the shown page** | read where `openCount` comes from | ✅ | ❌ on pass 1 — recomputed from a 300-row page; **fixed by item 3** |
| P06634 | **The ratings list says so when it is showing only part of them** | 381 ratings exist on French House, the list caps at 200 | ✅ | ❌ on pass 1 — no line at all; **fixed by item 3** |
| P06635 | **The complaints list says so when it is showing only part of them** | read the `issues.length` cap note | ✅ | ❌ on pass 1 — none; **fixed by item 3** |
| P06636 | "To handle" asks the SERVER for unhandled rows, so old ones stay reachable | read the `filter=unhandled` suffix | ✅ | audit 2026-07-07 fix intact |
| P06637 | The rating distribution bars can't divide by zero | read `summary.total ? … : 0` | ✅ | |
| P06638 | The distribution reads 5→1 top to bottom | read the `[5,4,3,2,1].map` | ✅ | |
| P06639 | A low rating is marked by a coloured left edge AND the star count, not colour alone | read `borderLeft` + `<Stars>` | ✅ | |
| P06640 | The stars carry a text alternative ("N out of 5") | read the `aria-label` on `Stars` | ✅ | |
| P06641 | The empty stars are drawn in a border colour, so 5★ and 2★ can't be confused at a glance | read the second `.repeat(5 - n)` span | ✅ | |
| P06642 | A guest comment can never break the card layout | read `wrap` (`overflowWrap: anywhere`) on every free-text block | ✅ | applied to comment, note, subject, body |
| P06643 | A staff photo only renders when the URL is really http(s) | read `/^https?:\/\//i.test(i.image_url)` | ✅ | a non-http value can never become a clickable link |
| P06644 | Same rule for the voice note | read the audio branch | ✅ | |
| P06645 | The photo opens in a new tab with `rel="noopener noreferrer"` | read the `<a>` | ✅ | |
| P06646 | The voice note is `preload="none"` so an owner with 30 complaints doesn't download 30 clips | read the `<audio>` | ✅ | egress |
| P06647 | Every timestamp on both tabs is India time | read both `toLocaleString(… timeZone: IST)` calls | ✅ | |
| P06648 | "handled by X" only appears when it really was handled and by whom | read the `r.acknowledged && r.acknowledged_by` guard | ✅ | ↻ S7 ✅, and the database id it used to print is gone. `acknowledged_by` holds the owner’s uuid whenever the cockpit writes it, so the card read “handled by c0af7b5b-…”. Sweep-7 item 5 stops an id reaching the screen; the one-word repair at source is in `/api/owner/ratings`, another lane’s file — reported, not edited. |
| P06649 | A resolved complaint shows when it was resolved | read the `i.resolved_at` branch | ✅ | |
| P06650 | The open/all and all/to-handle strips are the same control language as the rest of the panel | read the `own-range` wrappers | ✅ | |
| P06651 | A complaint with no body still renders its subject and actions | read `i.body && …` | ✅ | |
| P06652 | A complaint with no raiser reads "—", never "undefined" | read `i.raised_by \|\| "—"` | ✅ | |
| P06653 | **The page reacts when the server says a figure could not be read (`partial`)** | read the loaders for any use of `j.partial` | ✅ | ❌ on pass 1 — ignored; **fixed by item 5** |
| P06654 | Neither tab writes anything the compliance rules forbid (no sale is edited or hidden here) | read every `fetch` in the file | ✅ | ratings + issue status only |
| P06655 | No `any`, no `@ts-ignore`, no stray `console.log` | grep the file | ✅ | |

### A4 · `app/owner/inventory/page.tsx` — the stock & expenses door (P06656–P06677)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P06656 | This is a SERVER component — no `"use client"`, so the service-role client stays server-side | read line 1 and the imports | ✅ | |
| P06657 | `searchParams` is awaited (Next 16 async params) | read `await searchParams` | ✅ | |
| P06658 | `cookies()` is awaited before any `.get()` | read `const store = await cookies()` | ✅ | |
| P06659 | A real owner only ever sees restaurants they own | read `enabledOwnedRestaurantIds(u.id)` | ✅ | |
| P06660 | …and that helper already drops deleted restaurants and owner-panel-off restaurants | read `lib/panelAccess.ts` → `.is("deleted_at", null)` + `enabled_panels.owner` | ✅ | |
| P06661 | …and a failed read there throws rather than saying "you have no restaurants" | read `staleOrThrow` in the same helper | ✅ | |
| P06662 | The inventory MODULE is re-checked here, so a typed URL can't open a switched-off feature | read the `inventoryLadder(id).effective` filter | ✅ | hiding the nav is never the only guard |
| P06663 | **The module check for N restaurants costs ONE settings read, not N** | read the loop | ✅ | ❌ on pass 1 — a sequential `for` loop; **fixed by item 9** (`inventoryEffectiveByRid`) |
| P06664 | The batch helper's formula is identical to the per-restaurant ladder's | compare `inventoryEffectiveByRid` with `moduleLadder` in `lib/tableTags.ts` | ✅ | `allowed && (!ownerControl \|\| enabled)` both ways |
| P06665 | `?rid=` is only honoured when the owner actually has that restaurant | read `withModule.includes(qRid)` | ✅ | can narrow, never widen |
| P06666 | With no `?rid` the first entitled restaurant is opened | read `: withModule[0]` | ✅ | |
| P06667 | The admin act-as branch requires a VALID admin token, not just the act-as cookie | read `tokenIsValid(store.get(AUTH_COOKIE)?.value)` | ✅ | |
| P06668 | The admin act-as branch resolves the restaurant it was sent to | read `qRid \|\| ADMIN_ACT_COOKIE` | ✅ | |
| P06669 | A restaurant that doesn't resolve leaves `selected` empty and shows the plain message | read `if (row)` then the `!selected` branch | ✅ | |
| P06670 | The "not switched on" screen uses the panel's own heading class | read `className="adm-page-h"` | ✅ | |
| P06671 | The restaurants list is one scoped `.in()` read with named columns | read the `select("id, name")` | ✅ | never `select("*")` |
| P06672 | A restaurant whose name is missing reads "Restaurant", never "undefined" | read `nameById.get(id) \|\| "Restaurant"` | ✅ | |
| P06673 | The cockpit skin is read from the `aevidine_skin` cookie, defaulting DARK | read the `skin` line | ✅ | dark is the intended default |
| P06674 | The skin is passed into the client component rather than re-read there | read the `<OwnerInventory skin={skin}>` prop | ✅ | one writer for the embed skin |
| P06675 | Nothing on this page recomputes on every open — the report rides the snapshot cache | read `/api/owner/inventory` → `cachedOwnerPayload` | ✅ | standing analytics rule |
| P06676 | …and its ↻ forces a live recompute (`?refresh=1`) | read `components/owner/OwnerInventory.tsx` → `load(force)` | ✅ | the pattern the Customers page was missing |
| P06677 | Stage-2+ inventory being absent is deliberate and is NOT reported as a fault | prompt's pre-empt list + `docs/CLAUDE-DETAIL.md` | ⏭ | deliberate by decision — nothing to check |

### A5 · `app/owner/manager/page.tsx` — the owner running their own floor (P06678–P06700)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P06678 | Server component, async `searchParams`, awaited `cookies()` | read the top of the handler | ✅ | |
| P06679 | A real owner's estate comes from the owner links, then is narrowed by the admin's `manager_mode` switch | read `entitledSubset(await enabledOwnedRestaurantIds(u.id), "manager_mode")` | ✅ | a typed URL is gated too |
| P06680 | An owner with exactly one restaurant lands straight on its floor | read `ids.length === 1 ? ids[0] : ""` | ✅ | confirmed on screen |
| P06681 | An owner with several restaurants gets the picker first, never a guessed floor | read the same expression | ✅ | owner's ask, 2026-08-02 |
| P06682 | `?rid=` is honoured only when the owner owns it | read `qRid && ids.includes(qRid)` | ✅ | |
| P06683 | The admin act-as branch requires a valid admin token | read `tokenIsValid(...)` | ✅ | |
| P06684 | The admin sees the SAME estate the real owner would, so the switcher isn't hidden from them | read the `restaurant_owners` lookup | ✅ | admin view marks, never strips |
| P06685 | That sibling lookup is PAGED, so an owner past the 50th link keeps every floor | read the `PAGE = 1000` loop | ✅ | T19's fix, 2026-08-14 |
| P06686 | The paging loop ends on a short batch AND on an error, never spins | read `if (q.error \|\| batch.length < PAGE) break` | ✅ | |
| P06687 | The paged read is ordered, so pages can't overlap or skip | read `.order("restaurant_id")` | ✅ | |
| P06688 | Deleted restaurants never appear in the admin's switcher | read `.is("deleted_at", null)` | ✅ | |
| P06689 | The admin lands on the restaurant the console sent them to | read `if (restaurants.some(r => r.id === rid)) selected = rid` | ✅ | |
| P06690 | …and falls back to the first available one rather than a blank screen | read the `else if (restaurants.length)` | ✅ | |
| P06691 | Each restaurant carries its own accent colour into the embed | read the `accentColor` mapping | ✅ | every restaurant is genuinely different |
| P06692 | A missing accent colour becomes `undefined`, not the empty string | read `\|\| undefined` | ✅ | so the embed falls back to its own default |
| P06693 | A missing name reads "Restaurant" | read `\|\| "Restaurant"` | ✅ | |
| P06694 | Restaurant columns are named, never `select("*")` | read the two selects | ✅ | |
| P06695 | **The "nothing available" fallback heading uses a class the stylesheet actually defines** | grep `adm-page-title` in `app/globals.css` | ✅ | ↻ S7 EXPECTATION MOVED. The heading is GONE with the screen: a section he has not been given is a redirect now, not a page that names the feature (item 8 / R36). The whole-app walk for the dead `adm-page-title` class still runs and is still green. |
| P06696 | The embed is the same engine as the real manager panel (no second implementation to drift) | read the header comment + `components/owner/OwnerManagerMode.tsx` | ✅ | `public/panels/editor` with `?ownermode=1` |
| P06697 | Settings / Ratings / Audit / Menu are deliberately absent from the embed — not a fault | read the header comment | ⏭ | deliberate; the owner panel has its own of each |
| P06698 | Every write from the embed is logged as the OWNER — no shadow manager account | read the header comment + `app/api/editor` → `editorScope` | ✅ | |
| P06699 | The embed's `?rid` is re-validated server-side against the owner's estate | read `editorScope` in `app/api/editor/[...path]/route.ts` | ✅ | the page widens nothing |
| P06700 | The cockpit skin reaches the embedded floor | read the `skin` prop + `useOwnerSkin` | ✅ | one writer, by postMessage |

---

## B · CONFORMANCE TO THE PROJECT'S OWN WRITTEN RULES (P06701–P06800)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P06701 | Every popup in the territory registers with the back-button manager the moment it is built | grep `useBackClose` / `LFH_BACK.layer` against every overlay in the five files | ✅ | one overlay exists (the guest record) and it is registered |
| P06702 | No hand-rolled `pushState` / `popstate` anywhere in the territory | grep the five files | ✅ | none |
| P06703 | The guest-record back layer id is unique across the whole app | grep every `useBackClose("` | ✅ | 20 layers, no duplicate id |
| P06704 | No user action in the territory ends in a silent `return` | read every `onClick` in the five files | ✅ | ❌ on pass 1 for close-while-loading; **fixed by item 2** |
| P06705 | Nothing leaves a promise unresolved | read every `async` handler for a `finally` | ✅ | |
| P06706 | Client code branches on server reason CODES, not on prose | read the erase's `reason: "khata_outstanding"` and the clash's `plain`/`todo` | ✅ | |
| P06707 | No poll anywhere in the territory is faster than the 60s backstop | grep `setInterval` in the five files | ✅ | three intervals, all 60_000 |
| P06708 | Every backstop pauses while the tab is hidden | read the three visibility effects | ✅ | egress rule |
| P06709 | Every list read is column-named and `.limit()`ed server-side | read the four owner routes these pages call | ✅ | customers 300, ratings 200, issues 300, khata 500 people |
| P06710 | No screen in the territory does a full-scan analytic on open | read the routes for aggregates outside the snapshot cache | ✅ | the only aggregates are cached or DB-side |
| P06711 | Aggregate tiles ride the compute-on-view snapshot cache | read `cachedOwnerPayload` in `/api/owner/customers` | ✅ | |
| P06712 | …and "Refresh" forces live, as the standing rule says | drive the Refresh button and read the request URL | ✅ | ↻ S7 re-driven on 4214: Refresh sends `?refresh=1`, an erase sends `?q=…&refresh=1`. Measured tile 26 → insert a guest → still 26 (the 5-minute snapshot, by design) → Refresh → 27 = the live database count. |
| P06713 | No new `settings` column is introduced by anything here | `git diff` the branch for `supabase/migrations` | ✅ | no migration in this PR at all |
| P06714 | No new screen, module or permission is introduced | review the diff | ✅ | every change is inside an existing screen |
| P06715 | Nothing in the territory can erase, hide or edit an ISSUED sale | grep the five files for any bill/sale write | ✅ | the pages never write a sale |
| P06716 | The guest erase leaves the sales rows alone by design, and says so to the owner | read the confirm text + `lib/personalData.ts` (`sessions → keep`) | ✅ | CGST §132 / DPDP both honoured |
| P06717 | A guest who still owes pay-later money cannot be erased | read the route's 409 `khata_outstanding` branch | ✅ | owner's call, 2026-08-12 |
| P06718 | …and the owner is told how much and what to do instead | read the message text | ✅ | |
| P06719 | The erase is written to the Activity log | read `logAction(ownerLogPanel(scope), "customer_erase", …)` | ✅ | |
| P06720 | …and to the removals record the owner would actually go looking in | read the `deletion_audit` insert | ✅ | |
| P06721 | Neither record stores the number that was just erased | read `phone.slice(-4)` in both | ✅ | |
| P06722 | `npm run verify:personal-data` passes | run it | ✅ | 16 checks |
| P06723 | `npm run verify:customer-erase` passes | run it | ✅ | ↻ S7 re-run in this worktree: 15 passed, 0 failed. |
| P06724 | `npm run verify:customers` passes | run it against port 4114 | ✅ | ↻ S7 re-run against 4214: 65/65 checks passed. |
| P06725 | `npm run verify:recycle-name` passes | run it against port 4114 | ✅ | ↻ S7 re-run against 4214: all checks passed. (“2 zz* rows left” are another terminal’s fixtures — deliberately not touched.) |
| P06726 | `npm run typecheck` passes | run it | ✅ | |
| P06727 | `npm run lint` passes | run it | ✅ | |
| P06728 | `npm run verify:server-only` passes (no client file reaches the service-role client) | run it | ✅ | protects the khata page's 2026-08-06 crash |
| P06729 | `npm run verify:ui` passes | run it | ✅ | |
| P06730 | `npm run verify:taps` passes | run it | ✅ | |
| P06731 | `npm run verify:clash-coverage` passes | run it | ✅ | the ratings note keeps its expectation |
| P06732 | `npm run verify:everything` is NOT run from this terminal | check the command history of this run | ✅ | pid-locked, belongs to the merge terminal |
| P06733 | Nothing in the territory hard-codes restaurant #1's branding | grep the five files for a restaurant name or slug | ✅ | none |
| P06734 | Every restaurant's own name reaches every row on a multi-restaurant estate | read the `restaurantName` chips on all three list screens | ✅ | |
| P06735 | Admin act-as MARKS the owner's screen, it never strips it | read the manager page's estate-mirroring branch | ✅ | |
| P06736 | The admin is never gated by an owner entitlement | read `if (!scope.all && !scope.admin)` in the routes these pages call | ✅ | admin = top power |
| P06737 | An owner IS gated by the admin's per-restaurant switch on every one of these screens | read `entitledSubset(...)` in customers / ratings / issues, and the ladders in khata / inventory / manager | ✅ | five for five |
| P06738 | A switched-off section renders nothing rather than dead UI | read each `disabled` / `off` branch | ✅ | ↻ S7 EXPECTATION MOVED for Pay Later ONLY (R34 — see `P06585`). Customers, Guest ratings, Complaints, Inventory and Manager mode all still render nothing when their section is off; Pay Later deliberately does not, on the owner’s word. |
| P06739 | Feature state is ADMIN-controlled, never owner-controlled, on all five | read the same gates | ✅ | |
| P06740 | The four backend-only flags never appear in any of these screens | grep `verification\|payments\|aggregators\|gst_invoice` in the five files | ✅ | absent, as intended |
| P06741 | Every list screen tells the owner when it is showing only part of the data | read the three footers | ✅ | ❌ on pass 1 for ratings + complaints; **fixed by item 3** |
| P06742 | Every money figure that could not be read shows a dash, never a zero | read the khata tiles | ✅ | |
| P06743 | Every screen that CAN report `partial` shows the note | read all three loaders | ✅ | ❌ on pass 1 for customers + complaints; **fixed by item 5** |
| P06744 | Warning bars are used only for something actually wrong (no crying wolf) | read the khata `partial` bar and the new notes' styling | ✅ | the capped-list notes are plain text, not warnings |
| P06745 | Error text is in plain words, never a database message | read every `catch` in the five files | ✅ | the routes send owner-facing sentences |
| P06746 | Every error state offers a way back (a Try again that really refetches) | read the four error cards | ✅ | |
| P06747 | A loading state is never confusable with an empty one | read every `=== null` branch | ✅ | |
| P06748 | Dark is the default skin and the pages don't fight it | read the `skin` defaults on the two server pages | ✅ | |
| P06749 | Both skins are legible on all five screens | read the light + dark screenshots | ✅ | see band D |
| P06750 | No colour is the only carrier of meaning | read the chips, the stars and the left edges | ✅ | each has a word or a count beside it |
| P06751 | Every `var(--token, #fallback)` in the territory names a token the stylesheet declares | grep each token against `app/globals.css` | ✅ | `--card2` is the one exception, and its fallback is a neutral translucent grey that reads correctly in both skins — recorded, not a defect |
| P06752 | Every CSS class used in the territory exists in the stylesheet | grep each `className` literal | ✅ | ❌ on pass 1 — `adm-page-title` is declared nowhere; **fixed by item 7** |
| P06753 | No `!important` is added anywhere in the territory | grep the diff | ✅ | |
| P06754 | No styled-jsx block is introduced (it loses twice over on these surfaces) | grep the diff for `<style jsx` | ✅ | |
| P06755 | No `backdrop-filter` is hand-prefixed | grep the five files | ✅ | one unprefixed line in the drawer |
| P06756 | The territory adds no `<iframe>` in JSX (it would eat a Back press) | grep the five files | ✅ | the two embeds are mounted imperatively in their components |
| P06757 | Nothing in the territory writes to AV live or names it as a target | grep the branch diff | ✅ | dev stack only |
| P06758 | Nothing here reads `.env.AV.live` | grep the branch diff | ✅ | |
| P06759 | Aangan is never written to by any phase in this run | review every script this run executed | ✅ | the one fixture was French House, removed by id in the same run |
| P06760 | Every row this run created was deleted in the same run | read the fixture scripts' `finally` blocks + a post-run count | ✅ | one customers row, phone 9000014114, gone |
| P06761 | No test in this run could raise a real alert or notification | grep the scripts for any alert/notify path | ✅ | reads + one fixture insert only |
| P06762 | This run signed in ONCE per role | count `loginAs` cache misses across the run | ✅ | one owner sign-in, cached to disk and reused |
| P06763 | Nothing was ever POSTed to `/api/staff-login` by this run | grep the scripts | ✅ | |
| P06764 | The dev server used was 4114, never 4000 | check the process and every script's BASE | ✅ | |
| P06765 | The dev server is stopped when this run ends | check the port after teardown | ✅ | |
| P06766 | `.claude/deploy.lock` was never taken | check the file's mtime across the run | ✅ | |
| P06767 | Nothing was pushed to `main` and nothing was merged | `git log origin/main` unchanged | ✅ | |
| P06768 | The worktree was created from `origin/main`, not from the shared folder | `git log -1` at branch point | ✅ | af4d57db |
| P06769 | No file was copied into the worktree | review the setup commands | ✅ | `npm install` was real; `.env.local` is a symlink, not a copy, and is gitignored |
| P06770 | Only files this run actually changed are staged | `git status` before the commit | ✅ | never `git add -A` |
| P06771 | Each numbered item is its own commit, so a veto can be honoured cleanly | `git log --oneline` on the branch | ✅ | |
| P06772 | Every changed file is inside the declared territory, or is a guard script §7 asks for | `git diff --name-only origin/main` | ✅ | five pages + two guard scripts + package.json + this ledger |
| P06773 | Nothing in `app/api/**` was touched | `git diff --name-only` | ✅ | handed off instead |
| P06774 | Nothing in `components/**` was touched | `git diff --name-only` | ✅ | |
| P06775 | Nothing in `public/panels/**` was touched | `git diff --name-only` | ✅ | |
| P06776 | No migration was added | `git diff --name-only supabase/` | ✅ | |
| P06777 | Nothing in `docs/REJECTED-IDEAS.md` was re-proposed | read the 30 rejections against every item in this run | ✅ | none of R1–R30 touches these five screens |
| P06778 | No decision recorded in `CLAUDE.md` is changed by this run | re-read the rule index against the diff | ✅ | |
| P06779 | Every problem listed says WHERE it lives (panel → screen → what he'd see) | read the chat report | ✅ | |
| P06780 | Every item he must choose from uses the decision format | read the chat report | ✅ | Where / What / If yes / If no / Effort / Risk |
| P06781 | Nothing in the report tells him to open a file | read the chat report | ✅ | |
| P06782 | No extra summary document was written for him | list the files this run created | ✅ | ledger + findings + improvements are working machinery only |
| P06783 | Every fix has a permanent guard or a written manual check | read §7 of this ledger's items | ✅ | `verify:owner-money` carries a rule for every item in this PR |
| P06784 | The new guard is fast and needs no database | run it and time it | ✅ | static source checks, well under a second |
| P06785 | The new guard cleans up after itself | read it | ✅ | it creates nothing |
| P06786 | The new guard is registered in `package.json` | read the scripts block | ✅ | `verify:owner-money` |
| P06787 | The repaired erase guard still asserts the compliance rule it exists for | read its FORBIDDEN table list | ✅ | no sales/session/invoice/audit table may be deleted |
| P06788 | …and now also checks the DECLARED list, so a forbidden table can't arrive through `lib/personalData.ts` | read the new check | ✅ | |
| P06789 | …and still checks that only the last 4 digits are recorded | read the check | ✅ | |
| P06790 | …and still checks the erase is traceable | read the check | ✅ | now matches `ownerLogPanel(scope)` as well |
| P06791 | Every guest write path in the territory goes through the panel `api()` or is a plain owner-console fetch, per the offline guide | read `docs/OFFLINE-SYNC.md` against the five files | ✅ | the owner console is not a staff panel; its writes are not queued, deliberately |
| P06792 | The `/api/owner/*` family is already covered by the service worker's saved-data list | read `public/sw.js` → `DATA_PATHS` | ✅ | `/^\/api\/owner\//` present |
| P06793 | The offline strip is shown on these screens by the app shell, not per page | read `app/layout.tsx` → `<OfflineNotice />` | ✅ | |
| P06794 | Nothing in the territory fabricates a bill line while offline | grep the five files | ✅ | none of them draws a bill |
| P06795 | Realtime channels are not opened by any of these screens | grep the five files for a channel subscribe | ✅ | the 60s backstop is the whole mechanism |
| P06796 | The territory adds no new `settings` column and no new module bag entry | review the diff | ✅ | |
| P06797 | Wording discipline held for the whole run (product-correctness language only) | re-read this ledger and the report | ✅ | |
| P06798 | Nothing was verified by trickery — only by reading code and normal signed-in use | review every script this run ran | ✅ | no id swapping, no signed-out calls |
| P06799 | Every finding names a person who is worse off and the conditions it happens under | read the findings file | ✅ | |
| P06800 | No finding was manufactured to fill a quota | read the findings file against the four-test gate | ✅ | eight problems, all of them cost the owner something real |

---

## C · WATCHING IT RUN — headless Playwright, port 4114, ONE owner sign-in (P06801–P06875)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P06801 | The dev server on 4114 is this terminal's own | `curl /api/health` = 200 and `lsof -ti:4114` is the pid this run started | ✅ | |
| P06802 | `/owner/customers` renders for a signed-in owner | headless goto, assert the `<h1>` reads "Customers" | ✅ | |
| P06803 | `/owner/khata` renders | assert the `<h1>` reads "Pay Later" | ✅ | |
| P06804 | `/owner/issues` renders | assert the `<h1>` reads "Feedback & complaints" | ✅ | |
| P06805 | `/owner/inventory` renders | assert the `<h1>` reads "Inventory & expenses" | ✅ | |
| P06806 | `/owner/manager` renders | assert the manager floor is inside the embed | ✅ | 31 tiles drawn |
| P06807 | No console error on any of the five screens | collect `console`/`pageerror` on each goto | ✅ | zero across all five |
| P06808 | The four Customers tiles show real numbers, not skeletons that never resolve | read the rendered tile values | ✅ | 23 / 10 / 10 / 2 |
| P06809 | The Customers table draws one row per guest | count `tbody tr` against the API's row count | ✅ | 23 = 23 |
| P06810 | Typing in the search fires ONE request, not one per keystroke | type 5 characters, count `/api/owner/customers` requests | ✅ | debounced to 1 |
| P06811 | A search actually narrows the table | search a known name, assert the row count drops | ✅ | |
| P06812 | A search that matches nothing shows the "no customers match" sentence | search gibberish | ✅ | |
| P06813 | Clearing the search restores the full list | clear the box and re-count | ✅ | |
| P06814 | The "Regulars" tab sends `seg=regulars` | watch the request URL | ✅ | |
| P06815 | The "First-timers" tab sends `seg=new` | watch the request URL | ✅ | |
| P06816 | The "Blocked" tab sends `seg=blocked` | watch the request URL | ✅ | |
| P06817 | "Everyone" sends no `seg` at all | watch the request URL | ✅ | cheapest default |
| P06818 | The "Blocked" tab's row count matches the Blocked tile | compare the two on screen | ✅ | 2 = 2 |
| P06819 | "Most visits" sends `sort=visits` and reorders the table | watch the URL, read the first row | ✅ | |
| P06820 | "Recent" sends no sort param | watch the URL | ✅ | |
| P06821 | Clicking a guest row opens the record drawer | click, assert `[role=dialog]` | ✅ | |
| P06822 | The drawer shows that guest's name and number | read the drawer's text | ✅ | |
| P06823 | The drawer's three figures render (bills / spent / average) | read them | ✅ | |
| P06824 | Escape closes the drawer once it has loaded | press Escape after load | ✅ | |
| P06825 | **Escape closes the drawer while it is still loading** | press Escape immediately after the click | ✅ | ❌ on pass 1 — it stayed open and then opened anyway; **item 2** |
| P06826 | The phone's Back closes the drawer and stays on the page | `page.goBack()`, assert the dialog is gone and the path is unchanged | ✅ | |
| P06827 | **Back also closes it while it is still loading** | goBack immediately after the click | ✅ | ❌ on pass 1; **item 2** |
| P06828 | The ✕ closes the drawer | click it | ✅ | |
| P06829 | Clicking the dark backdrop closes the drawer | click at the far left of the overlay | ✅ | |
| P06830 | Clicking inside the drawer does NOT close it | click the drawer's heading | ✅ | |
| P06831 | A second guest cannot be tapped while a record is open | click a row, then try to click another within 2.5s | ✅ | the drawer intercepts the pointer — so the crossed-replies path does not exist on this screen |
| P06832 | The erase button asks before doing anything | intercept `window.confirm`, assert it fired | ✅ | |
| P06833 | Cancelling the confirm sends NO delete request | dismiss the dialog, watch the network | ✅ | zero requests |
| P06834 | The confirm text says what is kept as well as what goes | read the dialog message | ✅ | |
| P06835 | **The Refresh button asks the server for a live recount** | click it, read the request URL | ✅ | ❌ on pass 1 — plain `/api/owner/customers`; **item 1** |
| P06836 | **A guest added while the page is open eventually appears in the TILES, not just the list** | fixture guest, then compare tile vs row count | ✅ | ❌ on pass 1 — tile said 23, list showed 24; **item 1** |
| P06837 | `?refresh=1` really does correct the tile | request it and compare | ✅ | 24 |
| P06838 | The fixture guest is gone afterwards and the tile is back to 23 | delete by phone, re-read | ✅ | |
| P06839 | The page makes no request at all while it sits idle for 5s | watch the network with the tab visible | ✅ | no fast poll |
| P06840 | The page makes no request while the tab is hidden | dispatch `visibilitychange` with `hidden`, wait, watch | ✅ | |
| P06841 | Pay Later's four tiles render | read them | ✅ | ₹0 / 0 / ₹0 / ₹1,323 |
| P06842 | Pay Later's empty state is a sentence, not a blank card | read it | ✅ | |
| P06843 | Pay Later's search box filters without a network request | type, watch the network | ✅ | client-side by design |
| P06844 | Pay Later's Refresh re-requests | click, watch | ✅ | |
| P06845 | Pay Later never issues a write of any kind | watch every request method for 30s of use | ✅ | GET only |
| P06846 | A person row expands to show their own bills | seed nothing — use the manager panel's own khata data when present | ✅ | ↻ S7 CLOSED (was ⏭). French House still has no open tab, so this was driven against a CRAFTED server reply (nothing written to the database): a person row expands to their own bills — `#100 · 24 Aug · T1  ₹2,000` — one row per bill, no dangling divider on the last. |
| P06847 | The complaints tab switches and shows the open list | click it | ✅ | 1 open |
| P06848 | The ratings tab shows the average and the distribution | read them | ✅ | 4.2, 201/96/52/20/12 = 381 |
| P06849 | "To handle" asks the server with `filter=unhandled` | click it, read the URL | ✅ | |
| P06850 | "All" goes back to the unfiltered request | click it, read the URL | ✅ | |
| P06851 | Marking a rating handled updates the row immediately | click, read the row before the reply lands | ✅ | |
| P06852 | …and the "To handle" count drops by one at the same moment | read the badge | ✅ | |
| P06853 | …and the server really stored it | reload and re-read the row | ✅ | |
| P06854 | Reopening the same rating restores it exactly | click Reopen, reload, compare | ✅ | the fixture rating is left as it was found |
| P06855 | Saving an internal note stores it and closes the editor | type, save, reload | ✅ | |
| P06856 | Clearing that note restores the rating to how it was found | save an empty note, reload | ✅ | state restored |
| P06857 | Resolving a complaint updates the row and the count | click Resolve, read both | ✅ | |
| P06858 | Reopening it restores the original state | click Reopen, reload | ✅ | left exactly as found |
| P06859 | **The complaints badge equals the server's own open count** | compare the badge with `/api/owner/issues` → `openCount` | ✅ | ❌ on pass 1 — recomputed on the client; **item 3** |
| P06860 | **The ratings list says it is showing only the newest 200 of 381** | read the footer line | ✅ | ❌ on pass 1 — no line; **item 3** |
| P06861 | The complaints list says the same when it is capped | read the footer line | ✅ | ↻ S7 driven with a crafted 300-complaint reply: *“Showing the 300 most recent complaints. 420 are still open, so some open ones are past the end of this page.”* |
| P06862 | Inventory says it isn't switched on for this restaurant, in plain words | read the sentence | ✅ | the module really is off for French House |
| P06863 | …and the nav does not offer Inventory when it is off | read the sidebar | ✅ | union-gated, and the page agrees with it |
| P06864 | Manager mode lands straight on the floor for a one-restaurant owner | read the embed | ✅ | no launcher shown |
| P06865 | The embedded floor is live (tiles carry real table state) | read the tiles | ✅ | 19/31 occupied, 8 to pay |
| P06866 | The embedded floor is the manager engine, not a second copy | read the embed's URL | ✅ | `/panels/editor/index.html?…ownermode=1` |
| P06867 | The cockpit's skin reaches the embedded floor | flip to light, read the embed | ✅ | |
| P06868 | Nothing in the territory opens a realtime channel | watch the websocket list on all five | ✅ | none |
| P06869 | Every page still works after a hidden→visible cycle | dispatch both events, assert a reload happened | ✅ | |
| P06870 | Leaving a page tears its timer down (no request after navigating away) | navigate away, watch for 3s | ✅ | |
| P06871 | The guest record drawer opens for a guest with NO bills without erroring | open one | ✅ | shows the "no bills carry this number yet" sentence |
| P06872 | The guest record drawer opens for a guest WITH bills | open one | ✅ | bill numbers, table, date, amount |
| P06873 | A second erase attempt while one is running is refused by the disabled button | double-click the erase | ✅ | |
| P06874 | Every screen renders at 360px without a horizontal page scrollbar | measure `document.documentElement.scrollWidth` at 360 | ✅ | ↻ S7 re-measured at 360px: `scrollWidth` 360 = `clientWidth` 360, and there is no table on the page at all any more — the phone list is CARDS (his 2026-08-18 ask). The old 🟡 item 1 is built. |
| P06875 | No fixture, note, rating or complaint state is left changed by this run | re-read every row this run touched | ✅ | all restored |

---

## D · SCREENSHOTS ACTUALLY READ — 1280×800 and A35 360×780 dpr3 (P06876–P06925)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P06876 | Customers, desktop, dark — the tiles, filters and table all fit and line up | read the shot | ✅ | |
| P06877 | Customers, desktop, dark — the erase button is present but quiet, not competing with the row | read the shot | ✅ | |
| P06878 | Customers, desktop, dark — chips ("new", "regular", "blocked") are legible on the dark card | read the shot | ✅ | |
| P06879 | Customers, desktop, light — nothing turns white-on-white | read the shot | ✅ | |
| P06880 | Customers, desktop, light — the trash icon is still visible against the pale row | read the shot | ✅ | |
| P06881 | Customers, desktop, light — the "REGULAR" chip keeps its green tint | read the shot | ✅ | |
| P06882 | Customers, A35 — the heading, sub-line and four tiles stack in a 2×2 without clipping | read the shot | ✅ | |
| P06883 | Customers, A35 — the filter strips wrap instead of overflowing the card | read the shot | ✅ | |
| P06884 | Customers, A35 — the table scrolls inside its wrapper, and the PAGE does not | read the shot + measure | ✅ | ↻ S7 re-shot at 360×780 dpr3: cards, not a table. Nothing scrolls sideways and no column is off-screen. |
| P06885 | Customers, A35 — a long guest name wraps rather than truncating to nothing | read the shot | ✅ | "T15 Audit Proof" over three lines |
| P06886 | Customers, A35 — phone numbers stay on one line | read the shot | ✅ | monospace, `nowrap` |
| P06887 | The guest record drawer, desktop — it is a right-hand sheet, full height, scrollable | read the shot | ✅ | |
| P06888 | The drawer's three figures are readable and rounded | read the shot | ✅ | |
| P06889 | The drawer's ✕ is reachable at the top right | read the shot | ✅ | |
| P06890 | The drawer's backdrop dims the page behind it | read the shot | ✅ | |
| P06891 | Pay Later, desktop, dark — four tiles, then the search card | read the shot | ✅ | |
| P06892 | Pay Later, desktop, dark — "Collected this month ₹1,323" sits beside "Outstanding ₹0" without confusion | read the shot | ✅ | two different questions, two different tiles |
| P06893 | Pay Later, desktop, light | read the shot | ✅ | |
| P06894 | Pay Later, A35 — tiles stack 2×2, the empty sentence is readable | read the shot | ✅ | |
| P06895 | Feedback, desktop, dark — the average, the stars and the distribution bars | read the shot | ✅ | |
| P06896 | Feedback, desktop, dark — the coloured left edge tells a 1★ card from a 5★ card | read the shot | ✅ | |
| P06897 | Feedback, desktop, light — the amber bars stay amber and the text stays dark | read the shot | ✅ | |
| P06898 | Feedback, A35 — the tab strip, the big average and the bars all fit 360px | read the shot | ✅ | |
| P06899 | Feedback, A35 — "Note" and "Mark handled" are both tappable, side by side | read the shot | ✅ | ~44px tall each |
| P06900 | Feedback, A35 — a rating card's meta line wraps instead of clipping | read the shot | ✅ | |
| P06901 | Complaints tab, desktop — subject, restaurant chip, status chip and the action button on one line | read the shot | ✅ | |
| P06902 | Complaints tab, A35 — the same row wraps to two lines rather than overflowing | read the shot | ✅ | |
| P06903 | Inventory, desktop — the "isn't switched on" sentence is a normal page, not an error | read the shot | ✅ | |
| P06904 | Inventory, A35 — the same sentence is readable | read the shot | ✅ | |
| P06905 | Manager mode, desktop — the manager's own header sits under the cockpit's | read the shot | ✅ | |
| P06906 | Manager mode, desktop — the floor gets the full width | read the shot | ✅ | 12 tiles per row at 1280 |
| P06907 | Manager mode, desktop — the legend, the counts and the KOT/QO-P pair all render | read the shot | ✅ | |
| P06908 | Manager mode, A35 — the floor drops to 2 tiles per row | read the shot | ✅ | matches the fixed per-screen counts |
| P06909 | Manager mode, A35 — "+ Take order" and "BILL" are full-width and tappable | read the shot | ✅ | |
| P06910 | Manager mode, light — the embed follows the cockpit's skin | read the shot | ✅ | |
| P06911 | No page in the territory shows an unstyled flash before its CSS lands | read the shots taken immediately after `domcontentloaded` | ✅ | |
| P06912 | No text is clipped by a fixed-height container on any screen | read all 20 shots | ✅ | |
| P06913 | No overlay covers a control the owner needs on any screen | read all 20 shots | ✅ | |
| P06914 | The cockpit's own nav is never hidden behind a page's content | read the shots | ✅ | |
| P06915 | The bottom-left brand mark never sits on top of a row's controls | read the A35 shots | ✅ | it overlaps only empty gutter |
| P06916 | Numbers use tabular figures everywhere they are compared down a column | read the shots | ✅ | |
| P06917 | The rupee sign is attached to its number, never orphaned on the previous line | read the shots | ✅ | |
| P06918 | Star glyphs render as stars, not as tofu boxes, at both sizes | read the shots | ✅ | |
| P06919 | The Font-Awesome icons all resolve (no empty squares) | read the shots | ✅ | |
| P06920 | **The ratings list's new "showing 200 of 381" line reads naturally and is not styled as a warning** | read the shot after the fix | ✅ | plain muted text, like the other two screens |
| P06921 | **Pay Later's new "isn't switched on" state looks like the Customers one** | read the shot with the module forced off | ✅ | ↻ S7 RETIRED AS AN EXPECTATION (R34). There is no “Pay Later isn’t switched on” state to photograph any more, and there must not be one. Re-run as its inverse: the page renders the book unconditionally. See `P06585`. |
| P06922 | **The new old-tab emphasis is visible without shouting** | read the shot of a person who owes | ✅ | ↻ S7 CLOSED (was ⏭). Photographed against a crafted reply: `oldest 3 days` muted grey, `oldest 35 days` amber `rgb(251,191,36)`, `oldest 92 days` red `rgb(248,113,113)`, `oldest today` muted. The words are unchanged; the colour is emphasis only. |
| P06923 | **The Manager-mode fallback heading now matches every other page heading** | read the shot of the "no restaurant available" state | ⏭ | that state needs an owner with no entitled restaurant, which this dev stack does not have; producing one means writing to another restaurant's entitlements. Asserted from the source: the class is now `adm-page-h`, which `app/globals.css` defines at 22px/800 (`verify:owner-money` item 7). |
| P06924 | The fixes changed nothing else on any of the five screens | compare before/after shots pair by pair | ✅ | |
| P06925 | Both skins were checked after the fixes, not just before | read the post-fix light shots | ✅ | |

---

## E · TRACING ONE CHANGE ALL THE WAY ACROSS (P06926–P06975)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P06926 | A guest named at billing in the manager panel reaches the owner's Customers list | follow `sessions.cust_phone` → `customers` (mig 212) → `/api/owner/customers` → the table | ✅ | |
| P06927 | …and their bill total reaches the guest record drawer through the same phone | follow `lfh_owner_customer_bills` → `detail.bills` → the drawer | ✅ | |
| P06928 | …computed with the discount taken off BEFORE tax, like every other money view | read mig 228's `gross - disc` and compare with `lib/billMath` | ✅ | one arithmetic, everywhere |
| P06929 | …and a bill whose orders were ALL cancelled is not shown as a ₹0 visit | read the RPC's `HAVING … > 0` | ✅ | |
| P06930 | …and a soft-deleted session never appears | read `ses.deleted_at IS NULL` | ✅ | |
| P06931 | The lifetime figures cover ALL of a guest's bills, not just the 20 shown | read the RPC's second aggregate | ✅ | |
| P06932 | The visit count on the row is the real counter, not a guess from two timestamps | read mig 212 + `REPEAT_MIN` | ✅ | |
| P06933 | "Regular" on the owner's row means the same thing as the tile's "Regulars" count | compare `returning` (page) with the `visits >= 2` head-count (route) | ✅ | one definition |
| P06934 | Erasing a guest also empties their PAY-LATER person record | follow the route's `ERASABLE` walk → `khata_customers` anonymise | ✅ | |
| P06935 | …so their name stops appearing in the manager panel's "Collect later" person picker | follow `khata/customers?q=` in `app/api/tablet/[...path]` | ✅ | it reads name/phone, both cleared |
| P06936 | …while the bill that points at that person keeps a valid reference | read the FK note in the route | ✅ | anonymise, never delete — a delete would take a sale with it |
| P06937 | …and the sale itself is untouched | `lib/personalData.ts` → `sessions: keep` + `verify:personal-data` | ✅ | |
| P06938 | …and both the Activity log and the removals record show it happened | follow `logAction` → `/owner/audit`, and `deletion_audit` → the owner's Recycle bin screen | ✅ | |
| P06939 | …and neither of those two records stores the erased number | read both `phone.slice(-4)` sites | ✅ | |
| P06940 | …and a guest who still owes money is refused BEFORE anything is deleted | read the 409 branch's position in the handler | ✅ | the checks run first |
| P06941 | A pay-later bill parked in the manager panel reaches the owner's Pay Later list | follow `lfh_khata_outstanding` (mig 184/309) → `/api/owner/khata` → the row | ✅ | same RPC both panels use |
| P06942 | …so the manager's figure and the owner's figure can never disagree | read the header comment + the shared RPC | ✅ | |
| P06943 | …and collecting it in the manager panel moves it into "Collected today" here | follow `lfh_khata_collected(p_from = businessDayStartIso())` | ✅ | by COLLECTION day, not by bill day |
| P06944 | …with the business day (not midnight UTC) deciding what "today" means | read `lib/businessDay.ts` | ✅ | |
| P06945 | …and the month figure uses the IST calendar month | read `istMonthStartIso()` | ✅ | |
| P06946 | Paying a bill in parts never re-implements the split — it calls `lib/paySplit.ts` | grep `paySplit` across the writers | ✅ | the territory never touches it |
| P06947 | A complaint raised from the manager/kitchen/tablet 🚩 reaches the owner's Complaints tab | follow `/api/editor/issue` → `issues` → `/api/owner/issues` → the card | ✅ | |
| P06948 | …with its photo and voice note, signed as short-lived links | follow `signRows("issue-media", …)` → the `<img>`/`<audio>` | ✅ | |
| P06949 | …and resolving it here is written to the Activity log as the OWNER | follow `logAction(ownerLogPanel(scope), "issue_resolved")` | ✅ | |
| P06950 | …and an ADMIN act-as resolving it is logged as "admin", not as the borrowed owner | read the `scope.admin` branch | ✅ | |
| P06951 | A star rating a guest leaves after a bill reaches the owner's Guest ratings tab | follow `feedback` (mig 037/138) → `/api/owner/ratings` → the card | ✅ | |
| P06952 | …its average and distribution come from the DB, never from the shown page | read `lfh_ratings_summary` | ✅ | |
| P06953 | …and "handled" is stamped with who did it | read `acknowledged_by` | ✅ | |
| P06954 | An expense entered in the embedded inventory panel reaches the owner's Overview | follow `/api/inventory` → `/api/owner/inventory` → `OwnerInventory` | ✅ | same engine both ways |
| P06955 | …through the snapshot cache, with ↻ forcing live | read `cachedOwnerPayload` + `refresh=1` | ✅ | |
| P06956 | An order taken in owner Manager mode is logged as the owner, not as a shadow manager | read `editorScope` + the page's header comment | ✅ | |
| P06957 | …and appears live on the kitchen and waiter screens with no extra wiring | read the embed's `ownermode=1` + the shared realtime | ✅ | same engine |
| P06958 | The admin switching "Customers" off for a restaurant hides the nav item AND refuses the page | follow `entitledSubset(…, "customers")` → the 403 `disabled` → the card | ✅ | |
| P06959 | The admin switching "Ratings" off hides only that tab, leaving Complaints usable | follow both gates | ✅ | |
| P06960 | The admin switching "Issues" off hides only that tab | follow both gates | ✅ | |
| P06961 | The admin switching the pay-later MODULE off removes the nav item… | read `OwnerShell` → `ent: "khata_book"` | ✅ | |
| P06962 | …and now the page itself says so instead of "no one owes anything" | read the `moduleOff` branch after the fix | ✅ | ↻ S7 EXPECTATION MOVED — see `P06585` / `P06738`. It now says the opposite, on the owner’s word (R34). |
| P06963 | The admin switching the inventory MODULE off removes the nav item and the page agrees | read `inventoryLadder` on both sides | ✅ | |
| P06964 | The admin switching "Manager mode" off removes the nav item and the page refuses | read `entitledSubset(…, "manager_mode")` | ✅ | |
| P06965 | A restaurant added to an owner appears in the Customers restaurant picker without a code change | follow `restaurantNames(ids)` → `restaurantList` | ✅ | |
| P06966 | …and in the Manager-mode switcher, even past the 50th link | read the paged `restaurant_owners` loop | ✅ | |
| P06967 | …and in Pay Later, but only if that restaurant has the module | read the `moduleIds` filter | ✅ | |
| P06968 | A restaurant put in the recycle bin disappears from all of these screens | follow `.is("deleted_at", null)` through `enabledOwnedRestaurantIds` | ✅ | |
| P06969 | …and `verify:recycle-name` still passes with these changes | run it | ✅ | |
| P06970 | The owner console's skin change reaches the two embeds by message, not by reloading them | follow `aevidine_skin` → `useOwnerSkin` → `postMessage` | ✅ | one writer |
| P06971 | Nothing in the territory can reach a restaurant the caller doesn't own — every id is re-checked server-side | read the five gates | ✅ | never trusted from the client |
| P06972 | The `?as=` person pin rides along on every call from these screens | grep `asSuffix()` in the territory | ✅ | customers, khata, issues |
| P06973 | The `?rid` scope pin is per-TAB, never a shared cookie | read the `scopePin` initialisers | ✅ | a second admin tab cannot repaint this one |
| P06974 | The `openCount` the server computes is the number the badge shows | follow the route → the page after the fix | ✅ | ❌ on pass 1 — the server's number was computed and thrown away; **item 3** |
| P06975 | The `partial` list the server computes is words the owner actually sees | follow `partialNote` → the banner on all three screens | ✅ | ❌ on pass 1 for two of the three; **item 5** |

---

## F · MY OWN JUDGMENT — does a real restaurant need it to work this way (P06976–P07000)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P06976 | Does the credit book make an unpaid tab hard to forget? | open Pay Later as an owner would, look for what pulls the eye | ✅ | ❌ on pass 1 — every age read the same, so an old ₹120 sat quietly under a fresh ₹4,000; **item 10** gives age its own colour |
| P06977 | Is the headline the number an owner would actually ask for? | read the four tiles | ✅ | "Outstanding now" is the right first number |
| P06978 | Would an owner mistake the capped list for the whole book? | read the capped note | ✅ | it says both numbers |
| P06979 | Would an owner mistake "couldn't read" for "nothing collected"? | read the dash rule | ✅ | this is done properly and is worth keeping |
| P06980 | Is there anywhere on these screens a restaurant could hide a sale? | read every write path | ✅ | none — and the erase deliberately refuses to touch a bill |
| P06981 | Is the erase honest about what survives? | read the confirm text | ✅ | it names the bills that keep the name, and why |
| P06982 | Is the erase reversible enough to be safe? | read the guard rails | ✅ | it is irreversible by design, so it is confirmed, gated, logged twice and refused while money is owed — that is the right shape |
| P06983 | Would an owner know WHY they can't erase someone? | read the 409 message | ✅ | it names the amount and the number of bills |
| P06984 | Is "Regulars" a number an owner can act on? | read the definition | ✅ | a real visit count, not a timestamp guess |
| P06985 | Does the guest record answer "is this person worth looking after"? | open one | ✅ | bills, lifetime spend, average, and every restaurant they use |
| P06986 | Is anything on these screens a number an owner would argue with the till about? | read the money figures | ✅ | only Pay Later's collected figures, and they refuse to invent a value |
| P06987 | Would a complaint ever go unnoticed? | read the badge and the default filter | ✅ | ❌ on pass 1 — the badge could undercount; **item 3** |
| P06988 | Would a 1★ rating ever get lost in a long list? | read the "To handle" filter | ✅ | it asks the server, so old ones stay reachable |
| P06989 | …but would an owner know there ARE older ones? | read the list footer | ✅ | ❌ on pass 1 — 381 ratings, 200 shown, silence; **item 3** |
| P06990 | Is the Customers table usable on the owner's own phone? | read the A35 shot | ✅ | ↻ S7 CLOSED (was ⏭). He answered it on 2026-08-18 and it is built: at ≤640px the list is cards carrying name, number, visits and the chip, with the dates moved into the guest’s own record. Nothing is off-screen, nothing scrolls sideways. |
| P06991 | Is Manager mode genuinely the same panel, or a lookalike that will drift? | read the embed | ✅ | the same engine — nothing to drift |
| P06992 | Is Inventory's "not switched on" message useful or a dead end? | read it | ✅ | it names who to ask |
| P06993 | Does anything here need a migration to be right? | review all eight fixes | ✅ | none of them does |
| P06994 | Does anything here need a new screen? | review the eight | ✅ | no |
| P06995 | Does anything here need a new permission? | review the eight | ✅ | no |
| P06996 | Is any fix in this run a matter of taste rather than a fault? | re-read each against the four-test gate | ✅ | the eight fixes are faults; the taste calls are in the 🟡 list |
| P06997 | Would any fix here change something the owner deliberately chose? | check each against `docs/REJECTED-IDEAS.md` and `CLAUDE.md` | ✅ | none |
| P06998 | Is any improvement here just tidier code nobody would notice? | re-read item 9 | ✅ | it is one database read instead of N — invisible on one restaurant, real on twenty; kept because it also removes an uncapped fan-out |
| P06999 | Is the territory, as a whole, in good shape? | the run as a whole | ✅ | it is — these screens were swept hard before, and most of what is here is careful work; seven of the eight faults are the same shape — the server did the careful thing and the screen ignored it |
| P07000 | Is anything left unfinished in this territory? | the run as a whole | ✅ | nothing: eight problems fixed, two improvements built, three left to him as 🟡 with reasons, one handed off |

---

# ✦ SWEEP #7 · THE 500 NEW CHECKS — `P21601`–`P22100`

**Designed by this terminal after reading its own territory, not handed down.** Written against
`origin/main` **d7af8f63** on 2026-08-27, executed on branch `sweep7/t14-owner-customers`, port
**4214**.

The five files are bigger than sweep 6 recorded them, because his 2026-08-18 asks landed in
between: customers 335 → 512 lines, khata 166 → 219, issues 349 → 434, inventory 54 → 60, manager
91 → 97. So band A does not repeat sweep 6's reading — it covers **what is new or what sweep 6's
rows only read rather than drove**.

| band | ids | count | what it is |
|---|---|---|---|
| A · reading the code for correctness | `P21601`–`P21780` | 180 | the parts that arrived after sweep 6, and the parts sweep 6 read but never ran |
| B · the project's own written rules | `P21781`–`P21860` | 80 | egress, taps, back button, offline, clash, compliance, isolation, the guards |
| C · watching it run, headless, on 4214 | `P21861`–`P21960` | 100 | the rendered thing, never "the source contains it" |
| D · screenshots taken and then LOOKED at | `P21961`–`P22020` | 60 | 1280×800 and A35 360×780 dpr3, both skins |
| E · crafted server replies — the states the dev database cannot make | `P22021`–`P22060` | 40 | a long book, a capped list, an unread figure, a section switched off |
| F · tracing one change across panels, routes and the database | `P22061`–`P22085` | 25 | does it reach every screen that must show it, and none that must not |
| G · my own judgment — would a real restaurant want it this way | `P22086`–`P22100` | 15 | technically right and still wrong |

`result` is what happened when the row was executed. A row that found a fault names the sweep-7
item number that fixed it.

---

## A · READING THE CODE FOR CORRECTNESS (P21601–P21780)

### A1 · `app/owner/customers/page.tsx` — the parts sweep 6 never saw (P21601–P21670)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P21601 | The four tiles are a shared `Tile` component, so a tappable tile and a plain one cannot drift apart in looks | read `function Tile` — one `body`, two wrappers | ✅ | the only difference is `<div>` vs `<button>` |
| P21602 | A tile WITHOUT a `seg` is exactly the element it always was | read `if (!seg \|\| !pick) return <div className="adm-stat">` | ✅ | "New (last 30 days)" is still a plain div |
| P21603 | A tappable tile is a real `<button type="button">`, so it is reachable by keyboard and by a screen reader | read the JSX | ✅ | `type="button"` present — it can never submit a form |
| P21604 | …and it says which one is active to a screen reader, not only to the eye | read `aria-pressed={active}` | ✅ | |
| P21605 | …and to the eye as well, so meaning is never carried by the pointer alone | read the `outline` on the active tile | ✅ | 2px accent outline, inset |
| P21606 | A tile only becomes a filter where the tile's number and the segment's number are THE SAME QUESTION | compare each `seg` with the head-count feeding it | ✅ | Total⇄all, Regulars⇄visits≥2, Blocked⇄blocked |
| P21607 | "New (last 30 days)" is deliberately NOT tappable, because it counts a different set from "First-timers" | read the comment + the missing `seg` | ✅ | first-seen-in-30-days ≠ has-been-once; a tile that opened a list with a different count in it would be worse than one you cannot tap |
| P21608 | The tile keeps the panel's own font and colour rather than the browser's button defaults | read `font: "inherit", color: "inherit"` | ✅ | |
| P21609 | The tile's text stays left-aligned like the plain tile's | read `textAlign: "left"` | ✅ | a `<button>` centres by default |
| P21610 | Tapping a tile moves the SEGMENT STRIP too, so the two controls never disagree | drive it: tap Blocked, read `aria-selected` | ✅ | strip shows Blocked selected, tile shows `aria-pressed` |
| P21611 | The phone list is chosen by a media QUERY, not by a CSS breakpoint | read `window.matchMedia("(max-width: 640px)")` | ✅ | every style on this page is inline, so a CSS breakpoint could not reach them |
| P21612 | …and it listens for the width CHANGING, so rotating a phone re-shapes the list | read `mq.addEventListener("change", sync)` | ✅ | |
| P21613 | …and removes that listener on unmount | read the effect's cleanup | ✅ | |
| P21614 | …and there is no wrong-shape flash, because the list only renders after the first load | read the `customers === null` branch above it | ✅ | |
| P21615 | The phone card's whole name area is ONE button, so a fingertip cannot miss it | read the `<button aria-label={`Open …`}>` wrapper | ✅ | full row width, `flex: 1` |
| P21616 | …with an accessible name that says whose record it opens | read the `aria-label` | ✅ | |
| P21617 | The phone card's erase button is at least 40×40, which is a real fingertip | read `minWidth: 40, minHeight: 40` | ✅ | the desktop one is smaller on purpose — a mouse is precise |
| P21618 | The phone card's erase button cannot open the record behind it | read `e.stopPropagation()` | ✅ | |
| P21619 | The phone card tells you the dates are one tap away rather than just dropping them | read "Tap for their visits, dates and bills" | ✅ | the columns were not deleted, they moved |
| P21620 | …and the record it opens really does carry both dates | read the drawer's `first … · last …` line | ✅ | added in the same change |
| P21621 | The phone card shows the restaurant chip ONLY on a multi-restaurant estate | read `rests.length > 1 &&` | ✅ | ↻ S7 · HIS DECISIONS, 2026-08-31 — BUILT as item 7. The desktop table now drops the Restaurant column for a single-restaurant owner, the same condition the phone card list uses. Driven: one restaurant → 7 headers with no Restaurant; two → it is back. |
| P21622 | A blocked guest's phone card is dimmed AND carries the word | read `opacity` + the chip | ✅ | never colour alone |
| P21623 | A guest with no name reads "Guest" on the phone card too | read `c.name \|\| <span…>Guest</span>` | ✅ | |
| P21624 | Visits are singular/plural on the phone card | read `visit{(c.visits ?? 0) === 1 ? "" : "s"}` | ✅ | |
| P21625 | A missing visit count reads 0 rather than "undefined" | read `c.visits ?? 0` | ✅ | |
| P21626 | The phone card's phone number keeps the monospace grouping | read the `fontFamily` | ✅ | so it can be read back to a guest |
| P21627 | Each phone card is keyed by restaurant+phone, unique across the estate | read the `key` | ✅ | |
| P21628 | The phone list and the desktop table render from the SAME `rows`, so they can never show different guests | read both `.map`s | ✅ | one source |
| P21629 | …and now the SAME footer, so they cannot drift either | read `listFoot(gap)` called from both | ✅ | built by sweep-7 item 1 |
| P21630 | **The line under the list is about the GROUP on screen, not about every guest in scope** | read `segTotal` | ❌ → ✅ | **sweep-7 item 1.** It read `summary.total`, the whole-scope head-count. Measured on French House: Regulars said "the 13 most-recent of 26", Blocked said "the 2 most-recent of 26" with both blocked guests on screen |
| P21631 | …and "Regulars" asks the head-count that means the same thing | read `summary.returning` (visits ≥ 2) vs the route's `seg=regulars` (`gte("visits", 2)`) | ✅ | one definition, both ends |
| P21632 | …and "Blocked" likewise | read `summary.blocked` vs `eq("blocked", true)` | ✅ | |
| P21633 | …and "First-timers" is EXACT, not a guess | read `total − returning`: both are head-counts over the same scope, and `visits < 2` is the complement of `visits ≥ 2` | ✅ | no third count needed |
| P21634 | …and the line only appears when the read actually HIT its cap, so a stale tile cannot invent a hidden guest | read `rows.length < LIST_PAGE` | ✅ | the tiles ride a 5-minute snapshot while the list is live |
| P21635 | The cap the page names is the cap the route applies | compare `LIST_PAGE = 300` with `.limit(300)` in `app/api/owner/customers/route.ts` | ✅ | same pattern the issues page already uses for 200/300 |
| P21636 | **A search box holding only wildcards is not a search** | read `safeSearch(search)` | ❌ → ✅ | **sweep-7 item 2.** `*` and `%%%` fetched the whole list and labelled it "26 matches for “*”" |
| P21637 | …and two spaces do not produce "26 matches for “”" | same | ❌ → ✅ | sweep-7 item 2 |
| P21638 | …and the footer quotes what the SERVER looked for, not what the box holds | read `for “{searched}”` | ✅ | `Ra%` now reads "matches for “Ra”" |
| P21639 | The cleaner on the page is the same one the route runs | both call `safeSearch` from `lib/searchText.ts` | ✅ | one function, so the two cannot disagree again |
| P21640 | Importing that cleaner into a client file reaches nothing server-only | `npm run verify:server-only` | ✅ | `lib/searchText.ts` has zero imports |
| P21641 | `listFoot` is a plain function, not a component declared inside render | read the comment + `const listFoot = (gap: number) =>` | ✅ | a component created in render is a new type each render — React remounts it and the lint fails it |
| P21642 | The "no match" empty state still tells a failed search from an empty book | read the two strings | ✅ | |
| P21643 | The erase confirmation still names what SURVIVES as well as what goes | read the confirm text | ✅ | re-read verbatim from the live dialog on 2026-08-27 |
| P21644 | The erase request carries the chosen-owner pin | read `asSuffix()` on the DELETE | ✅ | |
| P21645 | The erase request does NOT carry `scope=`, and that is harmless | read `ownerScope()`: with no pin an admin resolves to the all-restaurants view, and the handler still re-checks `ownIds.includes(restaurantId)` | ✅ | admin = top power; a real owner is unaffected (they never have a pin) |
| P21646 | Only the row being erased is disabled, on the phone card as well as the table row | read both `disabled={erasing === …}` | ✅ | |
| P21647 | The guest record's sequence counter is bumped by CLOSING as well as by opening | read `closeDetail` | ✅ | this is what makes closing-while-loading stick |
| P21648 | A reply that arrives after the drawer closed sets nothing at all | read `if (mine !== detailSeq.current) return` | ✅ | including the `catch` and the `finally` |
| P21649 | The ✕ is available WHILE the record loads, not only after | read the `!detail` branch | ✅ | a visible way out that does not need Escape or the phone's Back |
| P21650 | **A guest's bills say which restaurant they are from when the guest uses more than one** | read the bill row | ❌ → ✅ | **sweep-7 item 3.** Bill numbers are a per-restaurant daily series, so two of his restaurants can both issue #41 on the same day |
| P21651 | …and a single-restaurant owner sees exactly what they saw before | read `detail.rows.length > 1 &&` | ✅ | |
| P21652 | …and the chip's name comes from the record's own rows, so it costs no extra read | read the `.find` | ✅ | |
| P21653 | …and an unmatched restaurant reads "—", never "undefined" | read the `\|\| "—"` | ✅ | |
| P21654 | The bill row wraps instead of overflowing now that it carries one more thing | read `flexWrap: "wrap"` | ✅ | |
| P21655 | The drawer's per-restaurant rows carry first AND last visit, because the phone list no longer does | read the `first … · last …` span | ✅ | |
| P21656 | A blocked-at-one-restaurant guest is marked per restaurant in the record | read `r.blocked &&` inside the rows map | ✅ | |
| P21657 | The record's money is rounded to whole rupees, never a raw float | read the three `Math.round` calls | ✅ | |
| P21658 | The record opens for a guest the current list does NOT contain | read the route's own `mineRead` (it re-reads by phone, not from the page) | ✅ | search for one guest, open an older one |
| P21659 | The tiles are read once per load and never per row | read the route: the head-counts sit behind `cachedOwnerPayload` and after the drawer's early return | ✅ | opening a record pays for no aggregate |
| P21660 | The list read names its columns and never `select("*")` | read `COLS` in the route | ✅ | |
| P21661 | The segment filter is applied in the DATABASE, not on the page | read the route's `seg` branches | ✅ | so the payload stays small however many guests exist |
| P21662 | The sort is applied in the database too | read `.order(sort, …)` | ✅ | |
| P21663 | The restaurant narrowing can only NARROW, never widen | read `onlyRid && ids.includes(onlyRid) ? [onlyRid] : ids` | ✅ | |
| P21664 | …and the tiles narrow with it, so tile and list always answer about the same restaurant | read `scopeIds` | ✅ | |
| P21665 | A count that FAILED is never printed as a confident zero | read `ReadSet.count()` — it throws | ✅ | the caller turns it into a retryable answer |
| P21666 | …and a failed count is never STORED in the snapshot | read `isPartial` in `lib/ownerCache.ts` | ✅ | a blip would otherwise outlive itself by hours |
| P21667 | The page has no `any`, no `@ts-ignore`, no stray `console.log` after this run's changes | grep the file | ✅ | |
| P21668 | `npm run typecheck` passes with the changes | run it | ✅ | exit 0 |
| P21669 | `npm run lint` introduces no new ERROR | run it, compare with the branch point | ✅ | 686 warnings both sides, 0 errors — the first draft added 2 and they were removed (see `P21641`) |
| P21670 | Nothing in this file writes, hides or edits a sale | grep every `fetch` | ✅ | one DELETE, and it is the guest record, never the bill |

### A2 · `app/owner/khata/page.tsx` — the credit book, as it is today (P21671–P21700)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P21671 | The R34 reversal is recorded ON the line someone would otherwise change | read the comment above the `<h1>` | ✅ | the project's "every rejection goes in the code" rule, honoured |
| P21672 | …and it names the row in `docs/REJECTED-IDEAS.md`, so the two records point at each other | read "R34" in the comment | ✅ | |
| P21673 | …and `npm run verify:rejected` still agrees the two match | run it | ✅ | |
| P21674 | The page really does ignore `moduleOff`, rather than reading it and doing nothing | grep the file | ✅ | the word appears only in the explaining comment |
| P21675 | There is no "Pay Later isn't enabled" sentence anywhere in the file | grep | ✅ | guarded by `verify:owner-money` item 4, whose rule is INVERTED on purpose |
| P21676 | The "oldest first" control he refused is likewise recorded on the line, not just in a doc | read the R35 comment beside the search box | ✅ | |
| P21677 | `OldestTab` is a component declared at MODULE level, not inside render | read where it sits | ✅ | the trap `listFoot` had to avoid on the Customers page |
| P21678 | It computes the age itself rather than parsing the words `ageDays` produced | read the second `Math.floor(...)` | ✅ | a colour must never come from re-reading a sentence |
| P21679 | …which means the words and the colour can never disagree, because both come from the same date | read both | ✅ | |
| P21680 | A fresh tab carries NO colour at all, so a coloured one stands out | read `tone === null` | ✅ | emphasis, not decoration |
| P21681 | …and no colour means no `title` either, so a fresh row grows no tooltip | read the ternary on `title` | ✅ | |
| P21682 | The two thresholds are 30 and 60 days, and 60 wins | read `d >= 60 ? danger : d >= 30 ? warn : null` | ✅ | order matters and is right |
| P21683 | The colour never carries the meaning on its own — the words still say "oldest 92 days" | read the span's text | ✅ | |
| P21684 | The tiles come from the server aggregate, never from summing the shown rows | read the route's `agg` block | ✅ | mig 309; a capped list cannot shrink the headline |
| P21685 | The list is bounded BY PERSON, so a person's figure is never half a figure | read `PEOPLE_SHOWN` + the `byCust` grouping | ✅ | |
| P21686 | The capped note says both numbers AND that the tiles count everyone | read the sentence | ✅ | so the tiles and the rows can never seem to contradict each other |
| P21687 | The capped note is plain text, not a warning bar | read `className="adm-page-sub"` | ✅ | "don't cry wolf" |
| P21688 | The unread-figure note IS a warning bar, because something really is wrong | read the `--adm-warn` card | ✅ | the distinction is deliberate and correct |
| P21689 | A figure that could not be read shows "—", and `₹0` is reserved for a real zero | read the two ternaries | ✅ | a claim about the till must never be invented |
| P21690 | The search filters the loaded list and fires NO request | read the filter — no `fetch` in that path | ✅ | the list is already bounded; a round trip would buy nothing |
| P21691 | …and a search that finds nobody says WHERE it looked when the book is capped | read the three-way empty state | ✅ | |
| P21692 | …and says the ordinary sentence when it is not | read the `: "No one matches that search."` arm | ✅ | |
| P21693 | Blank spaces in the search box are not a search | read `search.trim().toLowerCase()` | ✅ | the Customers page had to be corrected to match this; Pay Later was already right |
| P21694 | Expanding one person cannot collapse another | read the `Set` | ✅ | |
| P21695 | A person with no note does not render a dangling "·" | read `c.note ? \` · ${c.note}\` : ""` | ✅ | |
| P21696 | The restaurant chip only appears on a genuinely multi-restaurant book | read `multi` | ✅ | computed from the rows returned, so it cannot name a brand he cannot see |
| P21697 | Money is tabular-figures in the row AND in each bill line, so a column of rupees lines up | read both | ✅ | |
| P21698 | The page issues no write of any kind | grep for `method:` | ✅ | collecting stays in the manager panel, as the header says |
| P21699 | The page opens no realtime channel | grep | ✅ | 60s backstop only |
| P21700 | Nothing here can hide, edit or erase a sale | read every path | ✅ | it is a read-only view of a liability |

### A3 · `app/owner/issues/page.tsx` — guest ratings + staff complaints (P21701–P21745)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P21701 | **The tab you are left on can never be a tab the admin switched off** | read the tab effect | ❌ → ✅ | **sweep-7 item 4.** The old `decided` ref latched on the FIRST render, before either request answered — see `P06616` |
| P21702 | …and the new effect can never fight a real click, because a switched-off tab has no button to click | read both arms | ✅ | each arm requires the OTHER tab to be on |
| P21703 | …and it cannot ping-pong when both are off | read: each arm needs `!theOtherOff` | ✅ | both off → `bothOff` card, neither arm fires |
| P21704 | …and it leaves a deliberate click alone when both tabs are available | drive it: click Complaints with both on, wait 3s | ✅ | stays on Complaints |
| P21705 | The `decided` ref is GONE, not merely unused | grep `decided.current` | ✅ | guarded by `verify:owner-money` item 22's `mustNot` |
| P21706 | **A rating card never prints a database id where a person's name belongs** | read `handledBy` | ✅ | ↻ S7 SUPERSEDED, 2026-08-31. T12's sweep landed the real repair while this branch was open: the owner routes record the login NAME (`ownerScope` → `ownerActorName`), and `lib/ownerActor.ts` renders a legacy uuid row as an em dash with the reference in the hover text. T14's local `handledBy()` was DELETED rather than left beside it — a new way replaces the old one. |
| P21707 | …and a real name still reads exactly as before | drive it with `acknowledged_by: "Ravi"` | ✅ | ↻ S7 re-driven against `lib/ownerActor.ts`: a real name prints unchanged. |
| P21708 | …and "admin" still reads as admin | drive it | ✅ | ↻ S7 re-driven: "admin" prints unchanged. |
| P21709 | …and a uuid reads as "the owner" rather than vanishing, so the sentence still means something | drive it | ✅ | ↻ S7 EXPECTATION MOVED with the shared fix: a bare id now renders as "—" with the id in the hover text, not as "the owner". Nothing is invented — a name we were never given is not guessed. |
| P21710 | The uuid test is anchored, so a NAME that merely contains hex is untouched | read `^…$` in the pattern | ✅ | |
| P21711 | The root cause is named honestly and left to the lane that owns the file | read the comment | ✅ | ↻ S7 CLOSED — he authorised the file on 2026-08-31 and T12's sweep had already landed it. Nothing left to hand off. |
| P21712 | The two writers of that column disagree, and the disagreement is written down where the next person will find it | read the comment | ✅ | ↻ S7 CLOSED. The two writers agree now: both record a name. |
| P21713 | The complaints badge is the server's own head-count | read `openSrv ?? shownOpen` | ✅ | |
| P21714 | …and falls back to counting the page ONLY when the server says its own count failed | read the `partial.includes("openCount")` branch | ✅ | |
| P21715 | …and moves with an optimistic Resolve, then settles on the server's answer | read `setOpenSrv` + `loadIssues()` | ✅ | |
| P21716 | …and rolls back with the row when the write fails | read the `catch` | ✅ | both the row and the count |
| P21717 | …and can never go below zero | read `Math.max(0, …)` | ✅ | |
| P21718 | The ratings cap note names the right total for the filter you are on | read `ratingsOf` | ✅ | "All" → total, "To handle" → unhandled |
| P21719 | …and only appears when the list really is at its cap AND there is more | read `ratingsShown >= RATINGS_PAGE && ratingsOf > ratingsShown` | ✅ | the same two-part test the Customers footer needed |
| P21720 | …and tells him where the older ones are, rather than just admitting the cap | read the two sentences | ✅ | "“To handle” reaches the older ones that still need you" |
| P21721 | The complaints cap note is honest about whether an OPEN one can be hidden | read the `openCount > ISSUES_PAGE` branch | ✅ | |
| P21722 | …and that claim is true, because the route really does order open first | read `.order("status", { ascending: true })` — "open" sorts before "resolved" | ✅ | traced to the route |
| P21723 | The page's two cap constants match the route's two limits | compare 200/300 with `.limit(200)` / `.limit(300)` | ✅ | both named with the route beside them |
| P21724 | "To handle" asks the SERVER, so a rating older than the newest 200 stays reachable | read the `filter=unhandled` suffix | ✅ | |
| P21725 | Both tabs feed ONE "couldn't read that" note, so he reads one sentence not two | read `[...new Set([...rPartial, ...iPartial])]` | ✅ | |
| P21726 | …and it de-duplicates, so a figure named by both routes is not said twice | read the `Set` | ✅ | |
| P21727 | An internal note is protected from a co-owner's simultaneous save | read `X-LFH-Expect` | ✅ | |
| P21728 | …and the expectation is what the box said when it OPENED | read `was` | ✅ | |
| P21729 | …and a clash is reported in the server's plain words plus what to do next | read `c?.plain` / `c?.todo` | ✅ | |
| P21730 | …and the editor only closes after the save really succeeded | read the ordering | ✅ | a failed PATCH used to wipe what he typed |
| P21731 | Marking handled is a TRANSITION and deliberately sends no expectation | read the `ackRating` fetch | ✅ | first tap wins; a second tap is the same answer |
| P21732 | Every action button disables itself while its own row is busy | read the four `disabled={busy === …}` | ✅ | |
| P21733 | `busy` is cleared in a `finally` on all three writers | read them | ✅ | three, counted |
| P21734 | A staff photo only becomes a link when the URL really is http(s) | read the test | ✅ | |
| P21735 | …and opens with `rel="noopener noreferrer"` | read the anchor | ✅ | |
| P21736 | A voice note is `preload="none"`, so 30 complaints do not download 30 clips | read the `<audio>` | ✅ | egress |
| P21737 | Every free-text block wraps rather than breaking the card | read `...wrap` on all four | ✅ | comment, note, subject, body |
| P21738 | A complaint's subject is capped server-side as well as on screen | read the route's `.slice(0, 200)` | ✅ | a direct call cannot store a blob that reloads on every list fetch |
| P21739 | The distribution bars cannot divide by zero | read `summary!.total ? … : 0` | ✅ | |
| P21740 | The empty half of a star row is drawn in the declared COLOUR token, not the border shorthand | read `var(--border-c, #ccc)` | ✅ | the 2026-08-18 five-gold-stars fault cannot come back this way |
| P21741 | …measured, not assumed: filled and empty compute to different colours in BOTH skins | read `getComputedStyle` on each half | ✅ | dark: amber `rgb(245,166,35)` vs `rgb(29,36,48)`. Light: `color(srgb 0.50 0.34 0.07)` vs `rgb(229,232,238)` — both measured 2026-08-27 |
| P21742 | A low rating is marked by a coloured left edge AND the star count | read both | ✅ | |
| P21743 | Every timestamp on both tabs is India time | read the three `timeZone: IST` | ✅ | |
| P21744 | Neither tab can write anything the compliance rules forbid | read every `fetch` | ✅ | ratings PATCH ×2, issue status PATCH ×1 — no sale is touched |
| P21745 | No `any`, no `@ts-ignore`, no stray `console.log` | grep | ✅ | |

### A4 · `app/owner/inventory/page.tsx` — the stock & expenses door (P21746–P21765)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P21746 | Still a SERVER component, so the service-role client never reaches a browser | read line 1 + `npm run verify:server-only` | ✅ | 113 client files checked, none reaches it |
| P21747 | `searchParams` and `cookies()` are both awaited (Next 16 async) | read both | ✅ | |
| P21748 | The owner branch resolves the estate through the owner links, not through a restaurant column | read `enabledOwnedRestaurantIds` | ✅ | |
| P21749 | …and that helper drops a binned restaurant and one whose owner panel the admin turned off | read `lib/panelAccess.ts` | ✅ | `.is("deleted_at", null)` + `enabled_panels.owner` |
| P21750 | …and THROWS rather than reporting "you have no restaurants" when the read fails | read `staleOrThrow` | ✅ | |
| P21751 | The module gate is re-applied here, so a typed URL cannot open a switched-off feature | read the `effective[id]` filter | ✅ | hiding the nav is never the only guard |
| P21752 | …in ONE settings read for the whole estate | read `inventoryEffectiveByRid(ids)` | ✅ | sweep 6 item 9; still one read |
| P21753 | …and its formula still matches the per-restaurant ladder exactly | compare with `moduleLadder` in `lib/tableTags.ts` | ✅ | `allowed && (!ownerControl \|\| enabled)` both ways |
| P21754 | `?rid=` can narrow, never widen | read `withModule.includes(qRid)` | ✅ | |
| P21755 | With no `?rid` the first entitled restaurant opens | read `: withModule[0]` | ✅ | |
| P21756 | The admin branch needs a VALID admin token, not just the act-as cookie | read `tokenIsValid` | ✅ | |
| P21757 | …and it deliberately does NOT re-apply the module gate, because admin = top power | read the branch | ✅ | consistent with every other admin gate in the panel; recorded so nobody "fixes" it |
| P21758 | …and it deliberately does not filter `deleted_at`, for the same reason | read the branch | ✅ | the manager page does filter it, because that branch MIRRORS what the owner would see |
| P21759 | A restaurant id that is not a uuid cannot crash the page | read `.eq("id", rid).maybeSingle()` → `row` null → the plain message | ✅ | driven with `?rid=not-a-uuid`: the "isn't switched on" page renders, no error |
| P21760 | The "not switched on" screen is a normal page, not an error card | read the classes | ✅ | ↻ S7 EXPECTATION MOVED (item 8, owner 2026-08-31). There is no "not switched on" screen any more — a real owner is sent to his dashboard and shown nothing, because naming a feature he has not been given is R36. Re-run as its inverse. |
| P21761 | …and it names who to ask | read "ask your administrator" | ✅ | ↻ S7 EXPECTATION MOVED with `P21760`: it must NOT name who to ask, because it must not exist. The sentence is deleted. |
| P21762 | The restaurants read names its columns | read `select("id, name")` | ✅ | |
| P21763 | A missing name reads "Restaurant" | read the `\|\|` | ✅ | |
| P21764 | The skin is read from `aevidine_skin` and defaults DARK | read the line | ✅ | never `lfh_theme`, which does nothing on `/owner` |
| P21765 | …and is passed INTO the client component rather than re-read there | read the prop | ✅ | one writer for the embed skin |

### A5 · `app/owner/manager/page.tsx` — the owner running their own floor (P21766–P21780)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P21766 | Server component, async params, awaited cookies | read the top | ✅ | |
| P21767 | The owner's estate is narrowed by the admin's `manager_mode` switch, so a typed URL is gated | read `entitledSubset(…, "manager_mode")` | ✅ | |
| P21768 | One restaurant → straight onto the floor | read `ids.length === 1 ? ids[0] : ""` | ✅ | driven: no launcher for diago1 |
| P21769 | Several → the picker first, never a guessed floor | same expression | ✅ | his ask, 2026-08-02 |
| P21770 | `?rid=` honoured only for a restaurant he owns | read `ids.includes(qRid)` | ✅ | |
| P21771 | The admin act-as branch MIRRORS the real owner's estate rather than stripping it | read the `restaurant_owners` lookup | ✅ | admin view marks, never strips |
| P21772 | …paged, so an owner past the 50th link keeps every floor | read `PAGE = 1000` | ✅ | |
| P21773 | …and the loop ends on a short batch AND on an error | read the `break` | ✅ | it can never spin |
| P21774 | …and is ordered, so pages cannot overlap or skip | read `.order("restaurant_id")` | ✅ | |
| P21775 | …and a binned restaurant never appears in the switcher | read `.is("deleted_at", null)` | ✅ | |
| P21776 | Each restaurant carries its OWN accent colour into the embed | read the mapping | ✅ | every restaurant is genuinely different |
| P21777 | A missing accent becomes `undefined`, not "" | read `\|\| undefined` | ✅ | so the embed falls back to its own default rather than to no colour |
| P21778 | The fallback heading uses a class the stylesheet defines | grep `adm-page-title` across `app/` | ✅ | ↻ S7 EXPECTATION MOVED: the fallback heading is gone with its screen (item 8). The class walk is what enforces this now. |
| P21779 | The embed is the manager ENGINE, not a second implementation | read the embed URL | ✅ | `public/panels/editor` with `?ownermode=1` |
| P21780 | Every write from the embed is re-validated server-side against the owner's estate | read `editorScope` in `app/api/editor/[...path]/route.ts` | ✅ | this page widens nothing |

---

## B · THE PROJECT'S OWN WRITTEN RULES (P21781–P21860)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P21781 | Every overlay in the territory registers with the back-button manager the moment it is built | grep `useBackClose` / `LFH_BACK.layer` against every overlay | ✅ | one overlay exists (the guest record) and it is registered |
| P21782 | …and its layer id is unique across the whole app | grep every `useBackClose("` | ✅ | no duplicate |
| P21783 | …and it is registered while LOADING too, not only once the record has arrived | read `!!detail \|\| detailBusy` | ✅ | |
| P21784 | No hand-rolled `pushState` / `popstate` anywhere in the five files | grep | ✅ | none |
| P21785 | No user action in the territory ends in a silent `return` | read every `onClick` | ✅ | |
| P21786 | `npm run verify:taps` passes | run it | ✅ | 33 checks |
| P21787 | Every async handler resolves — nothing is left hanging | read every `try/finally` | ✅ | |
| P21788 | The client branches on server reason CODES, not on prose | read `reason: "khata_outstanding"` and `clash.plain`/`todo` | ✅ | |
| P21789 | No poll in the territory is faster than the 60-second backstop | grep every `setInterval` | ✅ | three, all `60_000` |
| P21790 | Every backstop pauses while the tab is hidden | read the three visibility effects | ✅ | |
| P21791 | …and is torn down on unmount, listener included | read the three cleanups | ✅ | |
| P21792 | …and re-loads immediately when the tab comes back | read the three `onVis` | ✅ | |
| P21793 | Every list read is column-named and `.limit()`ed server-side | read the four routes | ✅ | customers 300 · ratings 200 · issues 300 · khata 500 people |
| P21794 | No screen here runs a full-scan analytic on open | read the routes | ✅ | every aggregate is either DB-side or cached |
| P21795 | Aggregate tiles ride the compute-on-view snapshot cache | read `cachedOwnerPayload` | ✅ | |
| P21796 | …and Refresh forces a live recompute | drive it and read the request URL | ✅ | `?refresh=1` |
| P21797 | …and a snapshot that admits it is incomplete is never STORED | read `isPartial` in `lib/ownerCache.ts` | ✅ | a blip would otherwise be served for hours |
| P21798 | The page tells the truth about how fresh a tile is, or the difference is bounded | read `DEFAULT_MAX_AGE_SEC = 300` | ✅ | ↻ S7 · HIS DECISIONS, 2026-08-31 — BUILT as item 13. The tiles now say when they were counted — "Counted at 10:44 pm · Refresh to count again." — so the 5-minute lag is stated instead of left to be discovered. |
| P21799 | No new `settings` column is added by anything on this branch | `git diff` for `supabase/migrations` | ✅ | no migration in this PR at all |
| P21800 | No new screen, module or permission is introduced | review the diff | ✅ | every change is inside an existing screen |
| P21801 | `npm run verify:settings-columns` passes | run it | ✅ | |
| P21802 | Nothing in the territory can erase, hide or edit an ISSUED sale | grep every write | ✅ | the pages never write a sale |
| P21803 | The guest erase leaves the sales rows alone by design, and says so to the owner | read the confirm text + `lib/personalData.ts` | ✅ | CGST §132 and DPDP both honoured |
| P21804 | …and a guest who still owes pay-later money cannot be erased | read the 409 branch | ✅ | |
| P21805 | …and that question is asked about THIS person, not about the top 500 debtors | read the exact `orders` read | ✅ | a cap cannot be raised out of an exactness question |
| P21806 | …and it is asked BEFORE anything is deleted | read the order of the handler | ✅ | |
| P21807 | …and a failed check REFUSES rather than proceeding | read `dbFail` on the balance read | ✅ | erasing on doubt is irreversible; waiting is not |
| P21808 | The erase is written to the Activity log | read `logAction(ownerLogPanel(scope), "customer_erase", …)` | ✅ | |
| P21809 | …and to the removals record | read the `deletion_audit` insert | ✅ | **driven end to end 2026-08-27**: row 1243, `item_title: "Guest ending 1601"`, `actor_role: "owner"` |
| P21810 | …and neither record stores the number that was just erased | read both `phone.slice(-4)` | ✅ | measured on the real row: `meta.phone_last4: "1601"` and nothing else |
| P21811 | …and the audit says honestly which table was ANONYMISED rather than removed | read `meta.anonymised` | ✅ | `["khata_customers"]` on the real row |
| P21812 | …and a failed audit line does not un-erase the guest, but is shouted about our side | read the `.then` | ✅ | |
| P21813 | `npm run verify:personal-data` passes | run it | ✅ | 16 checks |
| P21814 | `npm run verify:customer-erase` passes | run it | ✅ | 15 passed |
| P21815 | `npm run verify:owner-money` passes, with the five new rules | run it | ✅ | 26 passed, 0 failed |
| P21816 | `npm run verify:customers` passes against 4214 | run it | ✅ | 65/65 |
| P21817 | `npm run verify:recycle-name` passes against 4214 | run it | ✅ | and it left another terminal's `zz*` rows alone |
| P21818 | `npm run verify:owner-panel` passes | run it | ✅ | 61 passed |
| P21819 | `npm run verify:owner-territory` passes | run it | ✅ | 49 passed |
| P21820 | `npm run verify:owner-screen` passes | run it | ✅ | 61 checks |
| P21821 | `npm run verify:owner-clash` passes | run it | ✅ | 11 passed |
| P21822 | `npm run verify:clash-coverage` passes | run it | ✅ | |
| P21823 | `npm run verify:server-only` passes | run it | ✅ | protects the khata page's 2026-08-06 crash, and now the new `safeSearch` import |
| P21824 | `npm run verify:ui` passes | run it | ✅ | |
| P21825 | `npm run verify:rejected` passes | run it | ✅ | 27 citations land on a row that names their own file |
| P21826 | `npm run verify:ledger-index` passes with this ledger's new block | run it | ✅ | 29 ledgers, no id collisions |
| P21827 | `npm run typecheck` passes | run it | ✅ | |
| P21828 | `npm run lint` adds no new error | run it | ✅ | 0 errors, 686 warnings — the branch point's own number |
| P21829 | `npm run verify:everything` is NOT run from this terminal | check this run's history | ✅ | pid-locked; it belongs to the merge terminal |
| P21830 | Nothing in the territory hard-codes one restaurant's branding | grep the five files, comments stripped | ✅ | the only occurrences of "French House" are in explaining comments |
| P21831 | Every restaurant's own name reaches every row on a multi-restaurant estate | read the chips on all three list screens | ✅ | and now on each BILL in the guest record too (item 3) |
| P21832 | Admin act-as MARKS the owner's screen, never strips it | read the manager page's estate-mirroring branch | ✅ | |
| P21833 | The admin is never gated by an owner entitlement | read `if (!scope.all && !scope.admin)` in all four routes | ✅ | admin = top power |
| P21834 | A real owner IS gated by the admin's per-restaurant switch on all five screens | read each gate | ✅ | five for five |
| P21835 | A switched-off section renders nothing rather than dead UI — except Pay Later, on his word | read each branch + R34 | ✅ | see `P06585` |
| P21836 | Feature state is ADMIN-controlled, never owner-controlled | read the same gates | ✅ | |
| P21837 | The four backend-only flags appear in none of these screens | grep | ✅ | |
| P21838 | Every list screen says when it is showing only part of the data | read the four footers | ✅ | and now says it about the GROUP, not the scope (item 1) |
| P21839 | Every money figure that could not be read shows a dash, never a zero | read the khata tiles | ✅ | |
| P21840 | Every screen that CAN report `partial` shows the note | read all four loaders | ✅ | |
| P21841 | A warning bar is used only where something really is wrong | compare the capped notes (plain) with the unread notes (bar) | ✅ | no crying wolf |
| P21842 | Error text is in plain words, never a database message | read every `catch` | ✅ | |
| P21843 | Every error state offers a way back that really refetches | read the four Try-again buttons | ✅ | |
| P21844 | A loading state is never confusable with an empty one | read every `=== null` branch | ✅ | |
| P21845 | The screens open offline and say so | `public/sw.js` → `DATA_PATHS` covers `/api/owner/` | ✅ | no new API family is introduced by this branch |
| P21846 | No write on these screens needs the guest outbox, because only two writers exist and both are staff-side idempotent-by-transition | read the erase and the two PATCHes | ✅ | |
| P21847 | The one destructive write asks first and cannot be double-fired | read the confirm + the disabled button | ✅ | driven: a double-click sends one DELETE |
| P21848 | No colour is the only carrier of meaning anywhere in the territory | read the chips, the stars, the left edges, the age | ✅ | each has a word or a count beside it |
| P21849 | No `!important` is added | grep the diff | ✅ | |
| P21850 | No styled-jsx block is added | grep the diff | ✅ | it loses twice over on these surfaces |
| P21851 | No `backdrop-filter` is hand-prefixed | grep | ✅ | one unprefixed line in the drawer |
| P21852 | No `<iframe>` is added in JSX | grep | ✅ | the two embeds are mounted imperatively |
| P21853 | No colour is taken from `--border`, which is a whole border | `verify:owner-money` item 16 across all three client files | ✅ | |
| P21854 | Every CSS class the territory names exists in the stylesheet | grep each literal against `app/globals.css` | ✅ | `adm-page-h`, `adm-page-sub`, `adm-card`, `adm-stat`, `adm-stats`, `adm-chip`, `adm-btn`, `adm-input`, `adm-empty`, `adm-muted`, `own-range`, `adm-tablewrap`, `hue-ink`, `hint` |
| P21855 | Nothing on this branch writes to AV live or names it as a target | grep the diff | ✅ | dev stack only |
| P21856 | Nothing on this branch reads `.env.AV.live` | grep | ✅ | |
| P21857 | Aangan was never written to by this run | review every script this run executed | ✅ | two fixture rows, both French House, both removed by their own key |
| P21858 | Every row this run created was deleted in the same run | re-read the two phones afterwards | ✅ | `9000021601` and `9000021602`, both gone |
| P21859 | The one rating this run changed was restored to exactly what it was found as | re-read the row afterwards | ✅ | `acknowledged: false`, `staff_note: null`, `acknowledged_by: null` |
| P21860 | Nothing this run did could raise a real alert, and it signed in ONCE | read the scripts + `loginRequestCount` | ✅ | one owner sign-in, cached to disk and reused by every later context |

---

## C · WATCHING IT RUN — headless, port 4214, ONE owner sign-in (P21861–P21960)

Every row here asserts the **rendered** thing — visible text, a live count, a computed style, a
request URL. Never "the source contains it".

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P21861 | The dev server on 4214 is this terminal's own | `curl /api/health` = 200, and `lsof -ti:4214` is the pid this run started | ✅ | pid 49059 |
| P21862 | Port 4000 was never touched by this run | review every BASE in every script | ✅ | 4214 throughout |
| P21863 | `/owner/customers` renders for a signed-in owner | assert the `<h1>` | ✅ | "Customers" |
| P21864 | `/owner/khata` renders | assert the `<h1>` | ✅ | "Pay Later" |
| P21865 | `/owner/issues` renders | assert the `<h1>` | ✅ | "Feedback & complaints" |
| P21866 | `/owner/inventory` renders | assert the `<h1>` | ✅ | "Inventory & expenses" |
| P21867 | `/owner/manager` renders the live floor | assert the embed | ✅ | 30 tables, 1 occupied |
| P21868 | NO console error or page error on any of the five | collect `console`/`pageerror` on each goto | ✅ | zero across all five |
| P21869 | The four Customers tiles show real numbers, not a skeleton that never resolves | read the rendered values | ✅ | 26 / 13 / 13 / 2 |
| P21870 | The table draws one row per guest returned | count `tbody tr` against the reply | ✅ | 26 = 26 |
| P21871 | The tile figures agree with the database | compare with a scoped head-count | ✅ | 26 = 26 after Refresh |
| P21872 | Typing fires ONE request, not one per keystroke | type 5 characters, count the requests | ✅ | debounced to 1 |
| P21873 | A search really narrows the table | search "T5" | ✅ | 26 → 2 |
| P21874 | …and the footer agrees with the table in the same render | read both in ONE `page.evaluate` | ✅ | 2 rows, "2 matches for “T5”" — an earlier apparent mismatch was my own read race, not the page |
| P21875 | Clearing the search restores the full list | clear and re-count | ✅ | back to 26 |
| P21876 | A search that matches nothing shows the sentence, not an empty table | search gibberish | ✅ | "No customers match that search." |
| P21877 | **A search of only wildcards no longer claims matches** | type `*`, read the footer | ✅ | ❌ before item 2 — "26 matches for “*”"; now no line at all |
| P21878 | **A search of only spaces no longer says "26 matches for “”"** | type two spaces | ✅ | ❌ before item 2 |
| P21879 | …and sends no `q=` at all in that case | read the request URL | ✅ | `/api/owner/customers` bare |
| P21880 | "Most visits" sends `sort=visits` | watch the URL | ✅ | `?sort=visits` |
| P21881 | "Recent" sends no sort param | watch the URL | ✅ | the cheapest default |
| P21882 | "Regulars" sends `seg=regulars` | watch the URL | ✅ | |
| P21883 | "First-timers" sends `seg=new` | watch the URL | ✅ | |
| P21884 | "Blocked" sends `seg=blocked` | watch the URL | ✅ | |
| P21885 | "Everyone" sends no `seg` at all | watch the URL | ✅ | |
| P21886 | **The Regulars tab no longer claims older regulars exist** | click it, read the footer | ✅ | ❌ before item 1 — "the 13 most-recent of 26" with all 13 on screen |
| P21887 | **Nor the First-timers tab** | click it | ✅ | ❌ before item 1 |
| P21888 | **Nor the Blocked tab** | click it | ✅ | ❌ before item 1 — "the 2 most-recent of 26" with both on screen |
| P21889 | …and "Everyone" says nothing either, when everybody fits | read the footer | ✅ | |
| P21890 | …while a genuinely capped list still says so | drive a crafted 300-row reply | ✅ | "Showing the 300 most-recent of 4,210. Search to find an older guest." |
| P21891 | Tapping the "Blocked" TILE puts the list on blocked guests | click the tile | ✅ | 26 → 2 |
| P21892 | …and the segment strip moves with it | read `aria-selected` | ✅ | "Blocked" selected |
| P21893 | …and the tile says it is the active one | read `aria-pressed` | ✅ | |
| P21894 | The "New (last 30 days)" tile is NOT a button | read its tag | ✅ | `DIV`, cursor `auto` |
| P21895 | …while the other three are | read their tags | ✅ | `BUTTON`, cursor `pointer` |
| P21896 | The filter strips are real tabs for a screen reader | count `role=tablist` / `role=tab` | ✅ | 2 lists, 6 tabs, exactly 1 selected in each |
| P21897 | The page makes NO request at all while it sits idle for 6 seconds | watch the network | ✅ | 0 — no fast poll |
| P21898 | …and none while the tab is hidden | drive a real `visibilitychange` | ✅ | 0 over 2.5s |
| P21899 | …and exactly one when it becomes visible again | same | ✅ | 1 |
| P21900 | …and the same is true of Pay Later | same | ✅ | 0 hidden, 1 on return |
| P21901 | …and of Feedback & complaints, which reloads BOTH tabs | same | ✅ | 0 hidden, 2 on return |
| P21902 | Leaving the page tears its timer down | navigate away, watch for 4s | ✅ | 0 further customers requests |
| P21903 | Clicking a guest row opens the record | click, assert `[role=dialog]` | ✅ | |
| P21904 | Escape closes it once loaded | press Escape | ✅ | |
| P21905 | Escape closes it WHILE it is still loading | press Escape immediately after the click | ✅ | and the record does not open afterwards |
| P21906 | The phone's Back closes it and stays on the page | `page.goBack()` | ✅ | dialog gone, path still `/owner/customers` |
| P21907 | The ✕ closes it | click it | ✅ | |
| P21908 | The dark backdrop closes it | click far left | ✅ | |
| P21909 | Clicking INSIDE does not close it | click the drawer heading | ✅ | still open |
| P21910 | A second guest cannot be tapped while a record is open | click row 1, then try row 2 within 2.5s | ✅ | the click is refused; the drawer shows the FIRST guest |
| P21911 | The record's three figures render | read them | ✅ | bills / spent / average |
| P21912 | A guest with no bills gets a sentence, not an empty box | open one | ✅ | |
| P21913 | The erase asks before doing anything | intercept the confirm | ✅ | fired |
| P21914 | …and its words name what is KEPT as well as what goes | read the live dialog text | ✅ | quoted verbatim into `P21643` |
| P21915 | …and cancelling sends no delete request | dismiss it, watch the network | ✅ | zero |
| P21916 | **An erase really removes the guest from the database** | fixture `9000021601`, erase through the screen, re-read | ✅ | 1 row → 0 |
| P21917 | …and removes exactly that row from the list | re-count | ✅ | |
| P21918 | …and forces a live recount | read the request URL | ✅ | `?q=P21601&refresh=1` |
| P21919 | …and writes the removals record | re-read `deletion_audit` | ✅ | row 1243, "Guest ending 1601" |
| P21920 | …storing only the last four digits | read the row | ✅ | `phone_last4: "1601"`, nothing else |
| P21921 | …and naming the pay-later row as ANONYMISED, not removed | read `meta.anonymised` | ✅ | `["khata_customers"]` |
| P21922 | A double-tap on the erase sends ONE delete, not three | click three times in a row | ✅ | 1 DELETE |
| P21923 | Refresh asks the server for a live recount | click it, read the URL | ✅ | `?refresh=1` |
| P21924 | A guest added while the page is open reaches the LIST straight away | insert, reload | ✅ | 26 rows → 27 |
| P21925 | …and reaches the TILES on the next Refresh | click Refresh | ✅ | 26 → 27 = the live database count |
| P21926 | …and the gap between them is the 5-minute snapshot, not a fault | read `DEFAULT_MAX_AGE_SEC` | ✅ | ↻ S7 · HIS DECISIONS, 2026-08-31 — item 13: the gap is now SAID on screen, not just true. |
| P21927 | Pay Later's four tiles render | read them | ✅ | ₹0 / 0 / ₹0 / ₹1,323 |
| P21928 | Pay Later's empty state is a sentence, not a blank card | read it | ✅ | |
| P21929 | Pay Later's search fires no network request | type, watch | ✅ | 0 |
| P21930 | Pay Later's Refresh re-requests | click, watch | ✅ | one GET |
| P21931 | Pay Later issues no write to its own API | watch every method for the session | ✅ | only GET reaches `/api/owner/khata` |
| P21932 | The ratings tab shows the average and the distribution | read them | ✅ | 4.0, and 29 of 29 at four stars |
| P21933 | …and the distribution's bars sum to the stated total | add the five counts | ✅ | 0+29+0+0+0 = 29 |
| P21934 | …and the average agrees with the distribution | 29 × 4 ÷ 29 | ✅ | 4.0 |
| P21935 | "To handle" asks the server with `filter=unhandled` | click it, read the URL | ✅ | |
| P21936 | "All" goes back to the unfiltered request | click it | ✅ | |
| P21937 | **Marking a rating handled really stores it** | click, then re-read the row from the database | ✅ | `acknowledged: true` |
| P21938 | …and the badge moves in the same instant, before the reply lands | read it 300ms after the click | ✅ | 29 → 28 |
| P21939 | …and settles on the server's own number | read it after the reload | ✅ | 28 |
| P21940 | **Saving an internal note really stores it** | type, save, re-read the row | ✅ | "T14 P21603 note probe" |
| P21941 | …and the editor closes only after the save succeeded | assert the input is gone | ✅ | |
| P21942 | Reopening restores the count | click Reopen | ✅ | 28 → 29 |
| P21943 | …and the row was put back exactly as it was found | re-read every field | ✅ | `acknowledged: false`, `acknowledged_by: null`, `staff_note: null` |
| P21944 | Inventory says it is not switched on for this restaurant, in plain words | read the sentence | ✅ | ↻ S7 EXPECTATION MOVED (item 8): Inventory says nothing at all — a real owner lands on `/owner`. Driven 2026-08-31: `/owner/inventory` → `/owner`. |
| P21945 | …and the sidebar does not offer Inventory at all | read the nav | ✅ | ↻ S7 unchanged and still right: the sidebar never offered it, and now the page agrees by not existing for him. |
| P21946 | Manager mode lands straight on the floor for a one-restaurant owner | read the embed | ✅ | no launcher |
| P21947 | The embed is the manager ENGINE with the owner flag | read its `src` | ✅ | `/panels/editor/index.html?rid=…&ownermode=1&skin=dark` |
| P21948 | …carrying the restaurant id, so it can never be pointed elsewhere by the client alone | read the `src` + `editorScope` | ✅ | |
| P21949 | …and the cockpit's skin | read the `skin=` param | ✅ | `skin=dark` by default |
| P21950 | The embedded floor is live | read the tiles | ✅ | 1/30 occupied, 0 to pay |
| P21951 | Nothing in the territory opens a realtime socket | watch for `ws:`/`wss:` on all five | ✅ | none |
| P21952 | A junk `?rid=` cannot break the Inventory page | drive `?rid=not-a-uuid` | ✅ | ↻ S7 re-driven with `?rid=not-a-uuid`: 200, and the owner lands on `/owner` — the junk id is ignored and no feature is named. |
| P21953 | A junk `?rid=` cannot break Manager mode | same | ✅ | ↻ S7 re-driven: 200, the junk id is ignored, and his own floor opens (he IS entitled to Manager mode). |
| P21954 | A junk `?rid=` cannot break Customers | same | ✅ | 200, the list renders; a real owner's pin is ignored without an admin token |
| P21955 | **With Guest ratings off, the complaint is visible** | craft ratings 403 + complaints 200 | ✅ | ↻ S7 re-driven after item 8: ratings off + complaints ON still shows the complaint. Only BOTH-off redirects. |
| P21956 | With Complaints off, the ratings tab still works | craft the reverse | ✅ | stars, distribution, both filters |
| P21957 | With both off, one clear sentence | craft both 403 | ✅ | "This section isn't enabled for your restaurant — contact Aevidine." |
| P21958 | A deliberate click on Complaints is NOT undone by the new tab effect | click it, wait 3.5s | ✅ | still on Complaints |
| P21959 | **A rating handled from the cockpit no longer prints a database id** | craft `acknowledged_by` as a uuid | ✅ | ❌ before item 5 — "handled by c0af7b5b-c0d8-40f6-b831-f475e48bab53" |
| P21960 | …while a real name and "admin" read exactly as before | craft both | ✅ | "handled by Ravi", "handled by admin" |

---

## D · SCREENSHOTS TAKEN AND THEN LOOKED AT (P21961–P22020)

1280×800 and Samsung A35 (360×780, dpr 3), **both skins**, `aevidine_skin` driven — never
`lfh_theme`, which does nothing on `/owner`. 26 shots; every one below was opened and read.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P21961 | Customers, desktop, DARK — heading, four tiles, filters and table all fit and line up | read the shot | ✅ | |
| P21962 | Customers, desktop, dark — the active tile carries a visible outline, so the tap-to-filter is discoverable | read the shot | ✅ | green outline on "Total customers" |
| P21963 | Customers, desktop, dark — the erase icon is present but quiet, never competing with the row | read the shot | ✅ | muted trash, no border until hover |
| P21964 | Customers, desktop, dark — "REGULAR" and "NEW" are both legible on the dark row | read the shot | ✅ | |
| P21965 | Customers, desktop, LIGHT — nothing turns white-on-white | read the shot | ✅ | |
| P21966 | Customers, desktop, light — the trash icon is still visible against the pale row | read the shot | ✅ | |
| P21967 | Customers, desktop, light — the "REGULAR" chip keeps its green tint | read the shot | ✅ | |
| P21968 | Customers, desktop, both — phone numbers are monospace and column-aligned | read both shots | ✅ | |
| P21969 | Customers, desktop, both — dates never wrap mid-date | read both shots | ✅ | `whiteSpace: nowrap` holding |
| P21970 | Customers, A35, dark — the four tiles stack 2×2 without clipping | read the shot | ✅ | |
| P21971 | Customers, A35, dark — the two filter strips wrap rather than overflowing | read the shot | ✅ | "First-timers" wraps inside its pill, still readable |
| P21972 | Customers, A35 — the list is CARDS, and there is no table at all | read the shot | ✅ | the 2026-08-18 ask, built |
| P21973 | Customers, A35 — nothing is off the right edge, and the page does not scroll sideways | read the shot + measure | ✅ | `scrollWidth` 360 = `clientWidth` 360 |
| P21974 | Customers, A35 — each card shows name, chip, number, visits, and the "tap for…" hint | read the shot | ✅ | |
| P21975 | Customers, A35 — the erase target is a real fingertip beside the card, not inside the text | read the shot | ✅ | ~40×40 |
| P21976 | Customers, A35, LIGHT — the same, legible | read the shot | ✅ | |
| P21977 | Customers, A35 — the bottom-left brand mark overlaps a card's edge but never its text or its controls | read the shot | ✅ | |
| P21978 | The guest record, desktop — a right-hand sheet, full height, scrollable | read the shot | ✅ | |
| P21979 | …its three figures are readable and rounded | read the shot | ✅ | ₹9,877 / ₹1,097 |
| P21980 | …"SPENT WITH YOU" wraps to two lines, so its number sits lower than the other two | read the shot | ✅ | ↻ S7 · HIS DECISIONS, 2026-08-31 — BUILT as item 10. Each figure is a column with its number pinned to the bottom, so all three share a baseline whether a label wraps or not. Measured: tops at 118/118/118, previously 118/132/118. |
| P21981 | …the ✕ is reachable at the top right | read the shot | ✅ | |
| P21982 | …the backdrop dims the page behind it | read the shot | ✅ | |
| P21983 | …the per-restaurant rows carry visits and both dates | read the shot | ✅ | |
| P21984 | …a restaurant where the guest is blocked carries the chip | read the shot | ✅ | "Spice Route  BLOCKED" |
| P21985 | **…and each BILL now carries the restaurant it was issued at** | read the shot | ✅ | ❌ before item 3 |
| P21986 | …a bill with no number reads "—" rather than "#null" | read the shot | ✅ | |
| P21987 | …a bill with no table simply omits the table, without leaving a stray "·" | read the shot | ✅ | |
| P21988 | Pay Later, desktop, dark — four tiles, then the search card | read the shot | ✅ | |
| P21989 | Pay Later, desktop, dark — "Collected this month" sits beside "Outstanding now" without confusion | read the shot | ✅ | two different questions, two tiles |
| P21990 | Pay Later, desktop, dark — an unread figure shows "—" and the bar explains which one | read the crafted shot | ✅ | "Couldn't read money collected today just now" |
| P21991 | …and that bar is amber-bordered, i.e. a real warning, unlike the plain capped note above it | read the shot | ✅ | the distinction is visible at a glance |
| P21992 | Pay Later, desktop — a person row expands to their own bills, indented under them | read the crafted shot | ✅ | `#100 · 24 Aug · T1  ₹2,000` |
| P21993 | …and the amounts are right-aligned in a tabular column | read the shot | ✅ | |
| P21994 | …and an old tab is amber, a very old one red, a fresh one plain | read the shot | ✅ | 35 days amber, 92 days red |
| P21995 | …and the colour never replaces the words | read the shot | ✅ | "oldest 92 days" is still written |
| P21996 | …and a person with no mobile reads "no mobile" | read the shot | ✅ | |
| P21997 | Pay Later, desktop, LIGHT | read the shot | ✅ | |
| P21998 | Pay Later, A35, both skins — tiles stack 2×2, the empty sentence is readable | read both shots | ✅ | |
| P21999 | Feedback, desktop, dark — the average, the stars and the five bars | read the shot | ✅ | |
| P22000 | Feedback, desktop, dark — the FILLED half of a star row is amber and the EMPTY half is not | read the shot + the computed colour | ✅ | the 2026-08-18 five-gold-stars fault has not come back |
| P22001 | Feedback, desktop, light — the same, and the empty star is a pale grey against dark text | read the shot | ✅ | `rgb(229,232,238)` |
| P22002 | Feedback, desktop, light — the amber bars stay amber and the track stays grey | read the shot | ✅ | |
| P22003 | Feedback, desktop — a card's coloured left edge matches its star count | read the shot | ✅ | green at 4★ |
| P22004 | Feedback, A35 — the tab strip, the big average and the five bars all fit 360px | read the shot | ✅ | |
| P22005 | Feedback, A35 — "Note" and "Mark handled" are both real fingertips, side by side | read the shot | ✅ | |
| P22006 | Feedback, A35 — a card's meta line wraps rather than clipping | read the shot | ✅ | |
| P22007 | **Feedback — "handled by" never shows a database id** | read the crafted shot | ✅ | ↻ S7 re-driven against the shared `lib/ownerActor.ts`: a bare id renders as "—" with the reference in the hover text. |
| P22008 | **Feedback with ratings off — the complaint is on screen, not an empty card** | read the two shots, before and after | ✅ | ❌ before item 4; the "before" shot is kept as the evidence for item 4 |
| P22009 | Complaints tab, desktop — subject, restaurant chip, status chip and the action button on one line | read the shot | ✅ | |
| P22010 | Complaints tab, A35 — the same row wraps to two lines rather than overflowing | read the shot | ✅ | |
| P22011 | Inventory, desktop, both skins — the "isn't switched on" sentence is a normal page, not an error | read both shots | ✅ | ↻ S7 EXPECTATION MOVED (item 8): there is no such page to photograph. Re-run as its inverse — `/owner/inventory` lands on `/owner`. |
| P22012 | Inventory, A35 — the same sentence is readable, but the screen below it is entirely empty | read the shot | ✅ | ↻ S7 · HIS DECISIONS, 2026-08-31 — BUILT as item 8, and better than the card I offered: the screen does not exist at all now, so there is no empty phone screen to look at. |
| P22013 | Manager mode, desktop — the manager's own header sits under the cockpit's, and both are usable | read the shot | ✅ | |
| P22014 | Manager mode, desktop — the floor gets the full width | read the shot | ✅ | 7 tiles per row at 1280 |
| P22015 | Manager mode, desktop — the legend, the three counts and the KOT/QO-P pair all render | read the shot | ✅ | |
| P22016 | Manager mode, A35 — the floor drops to 2 tiles per row and stays readable | read the shot | ✅ | |
| P22017 | Manager mode, LIGHT — the embed follows the cockpit's skin rather than its own | read the shot | ✅ | one writer, by `?skin=` and postMessage |
| P22018 | Manager mode — a print-helper prompt appears INSIDE the owner's cockpit | read both shots | ✅ | ↻ S7 CLOSED — already gone upstream. He had the "should this screen print?" band removed everywhere on 2026-08-29/30 ("NO LONGER RENDERED ANYWHERE", mig 372: there is no mode toggle). Re-driven 2026-08-31 inside the cockpit's Manager mode: the prompt is absent. |
| P22019 | No screen in the territory shows leaked code text | read all 26 shots for `-->`, `${`, `undefined`, `NaN`, `[object Object]` | ✅ | none |
| P22020 | No screen shows a blank area with no honest message behind it | read all 26 shots | ✅ | every empty state carries a sentence |

---

## E · CRAFTED SERVER REPLIES — the states the dev database cannot make (P22021–P22060)

The real page, the real stylesheet, the real branch — with the server's answer supplied by
Playwright's `route.fulfill`. **Nothing is written to the database by any row in this band.** This
is how four of sweep 6's `⏭` rows were finally closed: a long credit book needs open pay-later
bills (an issued sale), a capped ratings list needs 381 ratings, and a switched-off section needs
another restaurant's entitlements flipped while nine other terminals are reading it.

**The trap, recorded because it cost an hour:** a panel's **service worker** swallows the crafted
reply for every request after a page's first one, so the guest record's fetch went to the real
server and looked like a bug in the drawer. The context must be created with
`serviceWorkers: "block"`.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P22021 | A 512-person credit book renders, ordered biggest debt first | craft it | ✅ | |
| P22022 | …and the capped note names both numbers and says the tiles count everyone | read it | ✅ | "Showing the 500 people who owe the most, of 512. The figures above count everyone." |
| P22023 | …and it is plain text, not a warning bar | read the class | ✅ | |
| P22024 | …while a genuinely unread figure IS a warning bar | craft `partial: ["collectedToday"]` | ✅ | amber border, an icon and a Try again |
| P22025 | …with the right words for that key | read the sentence | ✅ | "Couldn't read money collected today just now — everything else on this page is up to date." |
| P22026 | …and TWO unread figures read as a list, not as two bars | craft both keys | ✅ | "…money collected today and money collected this month…" |
| P22027 | …and both tiles show "—" while the other two show real figures | read the tiles | ✅ | the page never invents a zero |
| P22028 | A person row expands to their own bills | craft it | ✅ | closes `P06846` |
| P22029 | …ordered oldest-first within the person, with a bill number, a date and a table | read the rows | ✅ | |
| P22030 | …and no dangling divider under the last bill | read the shot | ✅ | |
| P22031 | An old tab is amber past 30 days and red past 60 | craft 3 / 35 / 92 days | ✅ | closes `P06922`; measured `rgb(251,191,36)` and `rgb(248,113,113)` |
| P22032 | …and a tab opened today reads "oldest today", in the plain colour | craft day 0 | ✅ | |
| P22033 | A search that matches nobody on a CAPPED book says where it looked | craft `listCapped` + search | ✅ | "…among the 500 people who owe the most. There are 512 people on the book in all." |
| P22034 | A Pay Later load failure shows an error card with a Try again, and the tiles stay "…" not "₹0" | craft a 500 | ✅ | |
| P22035 | A 300-row Customers list says it is the most-recent 300 of 4,210 | craft it | ✅ | |
| P22036 | …and the restaurant picker lists every restaurant in scope, not just the ones on the page | craft two restaurants | ✅ | |
| P22037 | …and the "couldn't read which restaurant" note appears with a Try again | craft `partial` | ✅ | |
| P22038 | …and every row still renders, with "—" where the brand should be | read the rows | ✅ | the list is not withheld because one lookup failed |
| P22039 | A guest record spanning two restaurants names both, with per-restaurant visits and dates | craft it | ✅ | |
| P22040 | …and marks the restaurant where the guest is blocked | craft `blocked` on one | ✅ | |
| P22041 | **…and names the restaurant on each BILL** | craft two bills at two restaurants | ✅ | ❌ before item 3 |
| P22042 | …and says "Showing their 2 most recent of 9 bills" when there are more | craft `bill_count: 9` | ✅ | |
| P22043 | …and rounds the money to whole rupees | craft `9876.543` | ✅ | ₹9,877 |
| P22044 | The Customers section switched off shows the "contact Aevidine" card and no list | craft a 403 `disabled` | ✅ | ↻ S7 EXPECTATION MOVED (item 8): the card is deleted; a real owner is sent to `/owner` and shown nothing. Driven with a crafted 403. |
| P22045 | A retryable 503 shows an error card that offers Try again, and the tiles do not print zeros | craft a 503 | ✅ | tiles render their labels with no number |
| P22046 | A failed TILE count shows the same, rather than four confident zeros | craft a 500 | ✅ | |
| P22047 | A genuinely empty guest list says "No customers yet", with four real zeros | craft an empty reply | ✅ | a real zero and an unread figure look different |
| P22048 | 200 of 381 ratings says so, and says where the older ones are | craft it | ✅ | closes the "not triggered today" gap on `P06860` |
| P22049 | …and on "To handle" it names the unhandled total instead | craft `unhandled: 250` | ✅ | "Showing the 200 newest of 250 still to handle." |
| P22050 | 300 of 420 open complaints says some open ones are past the end | craft it | ✅ | closes `P06861` |
| P22051 | A ratings load failure says it is a loading error, "not “no ratings”" | craft a 500 | ✅ | |
| P22052 | A complaints load failure says it is a loading error, "not “all clear”" | craft a 500 | ✅ | |
| P22053 | …and each is offered separately, so one failing tab does not blank the other | craft one at a time | ✅ | |
| P22054 | `partial` from BOTH tabs is merged into ONE sentence | craft overlapping keys | ✅ | |
| P22055 | …and de-duplicated, so a key named by both routes is not said twice | craft `restaurantNames` on both | ✅ | said once |
| P22056 | **…but a key with no plain words falls through as the code word itself** | craft `partial: ["openCount"]` | ✅ | ↻ S7 · HIS DECISIONS, 2026-08-31 — BUILT as item 12. `openCount` now reads "how many complaints are still open". Driven with a crafted `partial: ["restaurantNames","openCount"]`. |
| P22057 | Guest ratings off + Complaints on → the complaint is visible | craft a 403 on ratings | ✅ | ❌ before item 4 |
| P22058 | Complaints off + Guest ratings on → the ratings tab works fully | craft the reverse | ✅ | |
| P22059 | Both off → one sentence, no empty tabs | craft both | ✅ | ↻ S7 EXPECTATION MOVED (item 8): both off is a redirect now, not a sentence. Driven with two crafted 403s. |
| P22060 | A rating handled by a uuid, by a name, and by "admin" all read as English | craft all three | ✅ | ❌ for the uuid before item 5 |

---

## F · TRACING ONE CHANGE ALL THE WAY ACROSS (P22061–P22085)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P22061 | A guest named at billing in the manager panel reaches the owner's Customers list | follow `sessions.cust_phone` → `customers` (mig 212) → the route → the table | ✅ | |
| P22062 | …and their bill total reaches the guest record through the same number | follow `lfh_owner_customer_bills` → `detail.bills` | ✅ | |
| P22063 | …computed with the discount taken off BEFORE tax, like every other money view | compare mig 228 with `lib/billMath` | ✅ | one arithmetic everywhere |
| P22064 | …and a bill whose orders were all cancelled is not shown as a ₹0 visit | read the RPC's `HAVING` | ✅ | |
| P22065 | …and a soft-deleted session never appears | read `ses.deleted_at IS NULL` | ✅ | |
| P22066 | Erasing a guest also empties their PAY-LATER person record | follow `ERASABLE` → the `khata_customers` anonymise | ✅ | proven on the real erase: `meta.anonymised = ["khata_customers"]` |
| P22067 | …so their name stops appearing in the manager panel's "Collect later" person picker | follow `khata/customers?q=` in `app/api/tablet/[...path]` — it reads name and phone, both cleared | ✅ | |
| P22068 | …while the bill that points at that person keeps a valid reference | read the FK note | ✅ | a delete would either fail or take a sale with it |
| P22069 | …and the sale itself is untouched | `lib/personalData.ts` → `sessions: keep` + `verify:personal-data` | ✅ | |
| P22070 | …and the erasure appears in BOTH the Activity log and the owner's Recycle bin | follow `logAction` and the `deletion_audit` insert | ✅ | the real row was read back |
| P22071 | A pay-later bill parked in the manager panel reaches the owner's Pay Later list | follow `lfh_khata_outstanding` (mig 184/309) | ✅ | the same RPC both panels use, so the two figures cannot disagree |
| P22072 | …and collecting it there moves it into "Collected today" here, by COLLECTION day | follow `lfh_khata_collected(businessDayStartIso())` | ✅ | |
| P22073 | …with the business day, not midnight UTC, deciding what "today" means | read `lib/businessDay.ts` | ✅ | |
| P22074 | A complaint raised from a panel's 🚩 reaches the owner's Complaints tab | follow `/api/editor/issue` → `issues` → the card | ✅ | |
| P22075 | …with its photo and voice note as short-lived signed links | follow `signRows("issue-media", …)` | ✅ | |
| P22076 | …and resolving it here is logged as the OWNER | follow `logAction(ownerLogPanel(scope), "issue_resolved")` | ✅ | |
| P22077 | …and an ADMIN act-as resolving it is logged as "admin", not as the borrowed owner | read the `scope.admin` branch | ✅ | admin = top power, invisibly |
| P22078 | A star rating reaches the owner's Guest ratings tab, with the DB's own average | follow `feedback` → `lfh_ratings_summary` | ✅ | never from the shown page |
| P22079 | **…and "handled by" is written by TWO writers that disagree about what goes in that column** | compare `app/api/editor/[...path]` (`g.user?.name \|\| username \|\| "Manager"`) with `app/api/owner/ratings` (`scope.ownerId`) | ✅ | ↻ S7 CLOSED — fixed upstream by T12's sweep (2026-08-29) while this branch was open. Both writers record a name; `lib/ownerActor.ts` covers the rows already written with an id. |
| P22080 | An expense entered in the embedded inventory panel reaches the owner's Overview | follow `/api/inventory` → `/api/owner/inventory` | ✅ | same engine both ways |
| P22081 | An order taken in owner Manager mode is logged as the owner, not a shadow manager | read `editorScope` | ✅ | |
| P22082 | …and appears live on the kitchen and waiter screens with no extra wiring | the embed is the same engine and the same realtime | ✅ | |
| P22083 | The admin switching any of the five sections off removes the nav item AND refuses the page | follow `entitledSubset` / the module ladders on all five | ✅ | five for five — Pay Later's PAGE deliberately excepted (R34) |
| P22084 | A restaurant put in the recycle bin disappears from every one of these screens | follow `.is("deleted_at", null)` through `enabledOwnedRestaurantIds` | ✅ | |
| P22085 | The owner console's skin reaches both embeds by message, not by reloading them | follow `aevidine_skin` → `useOwnerSkin` → `postMessage` | ✅ | one writer; `?skin=dark` on the embed URL, measured |

---

## G · MY OWN JUDGMENT — would a real restaurant want it this way (P22086–P22100)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P22086 | Would an owner trust a number this screen tells him? | read every figure against its source | ✅ | ↻ S7 and still ✅ after his eight decisions: every figure on these screens now also says how fresh it is (item 13). |
| P22087 | Would he ever be shown a screen with nothing on it and no reason? | drive every switched-off combination | ❌ → ✅ | yes — ratings off, complaints on, an open complaint, and an empty card. **Item 4** |
| P22088 | Is there anything on these screens a person cannot read? | look at every rendered string | ✅ | ↻ S7 · HIS DECISIONS, 2026-08-31 — both are closed. The uuid was fixed upstream by T12; `openCount` was fixed here as item 12. |
| P22089 | Does a number on this screen mean the same thing as the same number elsewhere? | compare "Regulars" with `seg=regulars`, "Blocked" with `seg=blocked` | ✅ | one definition each, both ends |
| P22090 | Is the credit book still the thing that stops money being forgotten? | open it as an owner would | ✅ | biggest debt first, and age now carries its own colour |
| P22091 | Would he mistake a capped list for the whole thing? | read all four cap notes | ✅ | each names both numbers |
| P22092 | Would he mistake "couldn't read it" for "nothing"? | read the dash rule and the two note styles | ✅ | this is done properly and is worth keeping exactly as it is |
| P22093 | Is the erase honest about what survives, and hard enough to do by accident? | read the confirm, the 409 and the two audit rows | ✅ | confirmed, gated, logged twice, and refused while money is owed |
| P22094 | Is anything on these screens a place a sale could be hidden? | read every write path | ✅ | none — and the erase deliberately refuses to touch a bill |
| P22095 | Does a multi-restaurant owner ever have to guess which restaurant something belongs to? | read every list and the record | ❌ → ✅ | the guest record's bills named none of them. **Item 3** |
| P22096 | …and does a SINGLE-restaurant owner get a column of the same name 26 times? | read the desktop table | ✅ | ↻ S7 · HIS DECISIONS, 2026-08-31 — BUILT as item 7. |
| P22097 | Do three tiles that can be tapped look different from the one that cannot? | look at the shot | ✅ | ↻ S7 · HIS DECISIONS, 2026-08-31 — BUILT as item 9 — the three that work carry a filter mark and one muted line says what the mark means. |
| P22098 | Is any fix on this branch a matter of taste rather than a fault? | re-read each against the four-test gate | ✅ | five faults; every taste call is in Part 4, unbuilt |
| P22099 | Would any fix here undo something he deliberately chose? | check each against `docs/REJECTED-IDEAS.md` and `CLAUDE.md` | ✅ | none. R34 and R35 are both respected and both re-verified |
| P22100 | Is anything left unfinished in this territory? | the run as a whole | ✅ | ↻ S7 2026-08-31: he answered all eight open items in one message. Seven are built (7, 8, 9, 10, 12, 13 here; 11 and 14 were already done upstream and were verified rather than rebuilt). Nothing in this territory is left open. |
