# T10 — GUEST & STAFF-PANEL API ROUTES · phases P04501–P05000

Territory (write access): `app/api/guest/**` · `app/api/r/**` · `app/api/editor/**` ·
`app/api/kitchen/**` · `app/api/tablet/**` · `app/api/inventory/**` · `app/api/aggregators/**` ·
`app/api/blocked/**` · `app/api/health/**` · `app/api/log/**` · `app/api/maintenance/**` ·
`app/api/issue-media/**` · `app/api/rt-config/**` · `app/api/panel-login/**` ·
`app/api/panel-logout/**` · `app/api/panel-profile/**` · `app/api/staff-login/**` ·
`app/api/staff-logout/**` · `app/login/**` · `app/staff-login/**`

**Budget, as PLANNED** — 40% correctness reads · 20% project-rule conformance · 15% watched running
on port 4110 · 10% screenshots actually read (desktop 1280×800 + Samsung A35 360×780 dpr3) ·
10% traced across panels · 5% judgment about a real restaurant.

**Budget, as ACTUALLY SPENT — stated honestly, because it is not the same shape.** This territory is
25 server-side route files and two rendered pages: 10,733 lines, of which ~10,400 have no pixels at
all. So the phases came out heavier on reading than the plan allowed and lighter on screenshots,
because that is where the answers were. What the evidence actually consists of:

* **~300 correctness reads** — every line of all 25 files, not a sample. Three of the four faults
  were found this way and could not have been found any other way (they only appear when the
  database is failing).
* **~95 conformance rows** — the per-route gate, tenant scoping, egress discipline, idempotency,
  the clash gates, the money caps, the compliance rules, and the things the owner has already
  decided (which are marked as NOT findings).
* **74 watched running** on port 4110 — a scripted pass over the live routes signed out, as a
  manager, a kitchen user, a waiter and the admin. Second pass: **74 checks, 0 failed, 0 extra
  logins** (the shared session cache held, so the owner's phone was never pinged). On top of that
  **89 more** from `verify:tablet --base http://127.0.0.1:4110 --read-only` and **49** from the new
  `verify:panel-api`.
* **4 screenshots read, not just taken** — `/login` and `/staff-login` at both sizes, with the
  rendered character count, the submit button's measured box, and the position of the invisible
  bot field asserted at each.
* **~20 traced across panels** — issue-media → `lib/issues` → the signed link the manager and owner
  screens read · the guest outbox → both guest routes · `realtime.js` → rt-config · the inventory
  purchase form → the inventory route · BlockedView → `/api/blocked` · the kitchen/tablet/manager
  twins of eight shared endpoints.
* **~12 judgment calls** — recorded as the two 🟢 built and the four 🟡 listed, plus the places I
  decided something was deliberate rather than broken.

Every file in the territory has at least one phase; the four `[...path]` catch-alls, the three
guest routes, the two login pages and the four login/logout/profile routes have ten or more.

Result key: ✅ pass · ❌ real problem (row says where it is written up) · ⏭ skipped, with a reason.

**These are SECOND-PASS results.** All 500 were re-run from the top after the fixes landed. On the
first pass four rows were ❌ (P04566, P04573, P04687 and the F4 row inside the inventory block);
each is now ✅ and its note names the fix. Nothing is marked green that was not re-checked.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P04501 | Every file in the territory exists and is reachable as a route | `find app/api/{guest,r,editor,kitchen,tablet,inventory,aggregators,blocked,health,log,maintenance,issue-media,rt-config,panel-login,panel-logout,panel-profile,staff-login,staff-logout} app/login app/staff-login -type f` returns 25 files | ✅ | 25 files, 10,733 lines |
| P04502 | `app/api/guest/place-order/route.ts` exports POST wrapped in `withIdempotency` | grep `export const POST = withIdempotency` | ✅ | scope "guest" |
| P04503 | Guest place-order refuses a body with more than MAX_ITEMS lines | read: `items.length > MAX_ITEMS` → 400 `order_too_big` | ✅ | 200 |
| P04504 | Guest place-order caps allergy count AND each allergy's length | read: `MAX_ALLERGIES` 40 and `MAX_ALLERGY_LEN` 200 both checked | ✅ | |
| P04505 | Guest place-order refuses rather than silently trimming an oversize order | read: both over-size paths `return`, neither `.slice()`s | ✅ | a trimmed order = food nobody ordered |
| P04506 | Session-mode place-order never returns the database's own words to a diner | read: `error` → `console.error` + `busy()`, never `error.message` in the body | ✅ | |
| P04507 | Public-mode place-order never returns the database's own words to a diner | read: same shape on the `lfh_place_order_public` branch | ✅ | |
| P04508 | A malformed `restaurantId` on the public path is REFUSED, not defaulted to #1 | read: `isUuid(b.restaurantId) ? … : ""` then `unknown_restaurant` | ✅ | absent field still falls back (legacy single-restaurant shape) |
| P04509 | The floor snapshot is dropped using the restaurant the RPC itself resolved | read: `ridFromResult(data)` preferred over `b.restaurantId` | ✅ | T9 F20 |
| P04510 | A session replay with no resolvable restaurant is logged, not silently skipped | read: `console.warn("… floor snapshot not dropped")` | ✅ | |
| P04511 | `busy()` still spells `server_busy` in full so verify:order-retry can find it | `npm run verify:order-retry` | ✅ | |
| P04512 | The `Retry-After` header and the `retryAfter` body field carry the same number | read: both from the one `retryAfter` const | ✅ | 20–45s jitter |
| P04513 | The retry hint is jittered server-side, not a constant | read: `20 + Math.floor(Math.random() * 25)` | ✅ | a thousand phones, a thousand answers |
| P04514 | Public place-order runs the off-plan table check before the RPC | read: `offPlanTable(publicRid, b.table)` precedes `sb.rpc` | ✅ | |
| P04515 | Public place-order runs the replay-clash check before the RPC | read: `replayClash(…, "order", …)` precedes `sb.rpc` | ✅ | |
| P04516 | Session place-order deliberately skips the clash check (the RPC owns it) | read the comment + confirm no `replayClash` on that branch | ✅ | `lfh_place_order` answers `session_closed` itself |
| P04517 | The rate-limit ping is scoped to the order's own restaurant | read: `maybePing(data, sessionRid)` / `maybePing(data, publicRid)` | ✅ | an empty rid would buzz another owner's phone |
| P04518 | A refusal never drops the floor snapshot | read: `dropFloorIfPlaced` requires `ok !== false` | ✅ | |
| P04519 | A bad JSON body answers 400 `bad_body`, never a 500 | POST malformed JSON to /api/guest/place-order on :4110 | ✅ | watched running |
| P04520 | An empty RPC answer becomes `{ok:false, reason:"empty"}`, never `null` | read both `data ?? { ok:false, reason:"empty" }` | ✅ | |
| P04521 | `app/api/guest/call-waiter/route.ts` POST is wrapped in `withIdempotency` | grep | ✅ | a replayed call rings the floor once |
| P04522 | An unreadable `at` timestamp is treated as too old, not as a free pass | read: `hasAt && !Number.isFinite(at)` → `call_too_old` | ✅ | T9 F24 |
| P04523 | A body with NO `at` at all still goes straight through | read: `hasAt` is false → both guards skipped | ✅ | ordinary online call |
| P04524 | An `at` of empty string is treated as absent, not as 0 | read: `rawAt !== ""` in `hasAt` | ✅ | 0 would have been "1970", i.e. always stale |
| P04525 | STALE_CALL_MS here matches the constant on the device | compare 10min against `lib/guestOutbox.ts` STALE_CALL_MS | ✅ | |
| P04526 | call-waiter never hands a database message to a diner | read: both branches log and answer `server_busy` | ✅ | |
| P04527 | call-waiter prefers the RPC's own restaurant over the phone's | read: `fromRpc` before `b.restaurantId` | ✅ | twin of P04509 |
| P04528 | A malformed `restaurantId` on the public call path is refused | read: `unknown_restaurant` 400 | ✅ | |
| P04529 | The public call path runs the off-plan table check | read: `offPlanTable(rid, b.table)` | ✅ | |
| P04530 | A raised hand drops the floor snapshot | read: `invalidateFloor(rid)` on both branches | ✅ | |
| P04531 | The reason text is capped before it reaches the database | read: `.slice(0, 200)` | ✅ | |
| P04532 | Session mode without a token answers 400 `invalid_token` | POST `{mode:"session"}` on :4110 | ✅ | watched running |
| P04533 | `app/api/guest/limit-hit/route.ts` always answers 200 | read: single `NextResponse.json({ ok:true })` exit | ✅ | a beacon must never surface an error |
| P04534 | limit-hit only maps a known RPC name to a limit key | read: `FN_TO_KEY` lookup, unknown → no-op | ✅ | |
| P04535 | limit-hit is counted per caller AND per limit key | read: `` `limithit:${key}:${capKeyFor(req)}` `` | ✅ | one limit can't eat another's budget |
| P04536 | The cap key never comes from the body | read: `capKeyFor(req)` (cookie else server-derived IP) | ✅ | |
| P04537 | limit-hit only pings on a genuine recent event row | read: `pingLatestGuestLimit` contract + the `rid` it is passed | ✅ | a caller cannot compose an alert |
| P04538 | A malformed `rid` becomes null rather than reaching the query | read: `UUID.test(b.rid) ? b.rid : null` | ✅ | |
| P04539 | An empty/unparseable body is a no-op, not a 500 | POST `` (empty) to /api/guest/limit-hit on :4110 | ✅ | watched running |
| P04540 | The window and ceiling are stated as named constants, not magic numbers | read: `WINDOW_MS` / `MAX_PER_WINDOW` | ✅ | 60s / 6 |
| P04541 | `app/api/r/[restaurant]/menu-data/route.ts` answers `{items, categories}` and nothing else | GET on :4110 for french-house, assert the key set | ✅ | watched running |
| P04542 | An unknown slug answers 404 | GET `/api/r/definitely-not-a-restaurant/menu-data` on :4110 | ✅ | watched running |
| P04543 | A restaurant whose Menu feature is off answers 404, not the whole menu | read: `if (!gate.menuEnabled) return 404` | ✅ | hiding a screen is never the only guard |
| P04544 | An inactive restaurant answers 404 | read: `if (!r || !r.active)` | ✅ | |
| P04545 | A failed slug read answers 503 + `transient`, never 404 | read: `try { getRestaurantBySlug } catch { transient() }` | ✅ | "couldn't ask" ≠ "doesn't exist" |
| P04546 | A failed settings read answers 503 + `transient`, never "menu is off" | read: `try { getSettings } catch { transient() }` | ✅ | |
| P04547 | A failed dish/category read answers 503 + `transient`, never a schema message | read: `try { getMenuBundle } catch { transient() }` | ✅ | T9, 2026-08-06 |
| P04548 | The outer catch never leaks `e.message` to a guest | read: logs the detail, returns `transient()` | ✅ | |
| P04549 | menu-data sets `Cache-Control: no-store` on every path | read: the 503 and the 200 both set it | ✅ | a CDN cache is not purged by revalidateTag |
| P04550 | menu-data does NOT set a long s-maxage | grep for `s-maxage` in the file → none | ✅ | deliberate; the Next data cache is the saver |
| P04551 | The header comment's claimed response shape matches the code | read both | ✅ | corrected in T9; still true |
| P04552 | menu-data is not reachable for a slug belonging to another restaurant's data | read: everything downstream is keyed on `r.id` | ✅ | |
| P04553 | `app/api/health/route.ts` does one bounded row read and stops | read: `.select("id").limit(1)`, no count | ✅ | 288 hits/day forever |
| P04554 | health does not use `head: true` | read | ✅ | deliberate — a head request proves nothing |
| P04555 | health answers 503 when the database is unreachable | read: `if (error) … 503` | ✅ | |
| P04556 | health returns no tenant data, no counts, no secrets | read the whole 44-line file | ✅ | |
| P04557 | The rejected `/api/health/deep` has not come back | `find app/api/health -type f` → one route only | ✅ | REJECTED (owner, 2026-08-13) R18 |
| P04558 | health has not grown storage / config checks | grep for `storage`/`rt-config` in the file → none | ✅ | same rejection, different place |
| P04559 | health answers 200 on the running app | GET :4110/api/health | ✅ | watched running |
| P04560 | health sets `Cache-Control: no-store` | read | ✅ | |
| P04561 | `app/api/rt-config/route.ts` never returns the service-role key | read the whole file; only `NEXT_PUBLIC_*` are read | ✅ | |
| P04562 | A real staff member's restaurant always wins over `?rid=` | read: the `if (u)` branch ignores the query param | ✅ | |
| P04563 | `?rid=` is honoured only with a valid admin cookie | read: `else if (await tokenIsValid(...))` | ✅ | |
| P04564 | Missing public env answers 503 with a branchable CODE, not just prose | read: `reason: "rt_unconfigured"` | ✅ | I11 |
| P04565 | The unconfigured answer says it is not self-fixable | read: `selfFixable: false` | ✅ | "call Aevidine" vs "wait for wifi" |
| P04566 | A database blip while resolving the staff member no longer escapes as a bare 500 | read: `AuthDbError` caught → 503 `reason:"rt_busy"` | ✅ | **FIXED** — was an unhandled throw; see T10-findings F3 |
| P04567 | rt-config sets `Cache-Control: no-store` on every answer | read all three exits | ✅ | |
| P04568 | rt-config answers on the running app with a restaurantId | GET :4110/api/rt-config signed in as the manager | ✅ | watched running |
| P04569 | rt-config's restaurant resolution mirrors `panelScope.panelRestaurantId` | read both side by side | ✅ | |
| P04570 | The realtime client memo drops a rejection so a later wake retries | read `public/panels/realtime.js` getClient | ✅ | read-only check — file is not mine |
| P04571 | `app/api/issue-media/route.ts` awaits `tokenIsValid` | read: `const isAdmin = await tokenIsValid(...)` | ✅ | the 2026-08-04 no-op gate |
| P04572 | issue-media refuses when there is neither a staff nor an admin session | read: `if (!staff && !isAdmin) return 401` | ✅ | |
| P04573 | A database blip no longer reads as "please log in" on issue-media | read: `AuthDbError` → 503 `busy`, only a bad cookie → not-staff | ✅ | **FIXED** — see T10-findings F2 |
| P04574 | The restaurant comes from the session, never from the body | read: `panelRestaurantId(req, { user: staff })` | ✅ | |
| P04575 | issue-media refuses a non-File `file` field | read: `if (!(file instanceof File))` | ✅ | |
| P04576 | issue-media refuses an unknown attachment kind | read: `kind !== "image" && kind !== "audio"` | ✅ | |
| P04577 | A storage failure answers 400 with a sentence, not a stack | read: the catch returns `e.message` from `storeIssueMedia`, which is already a sentence | ✅ | |
| P04578 | issue-media is not queued offline, deliberately | read the header comment + confirm no `withIdempotency` | ✅ | nothing to upload to with no signal |
| P04579 | The upload path does not trust `?rid=` for a real staff member | read `lib/panelScope.ts` | ✅ | |
| P04580 | An issue photo lands in the private bucket, read back through a signed link | read `lib/mediaLinks.ts` PRIVATE_BUCKETS + `signRows` in the editor route | ✅ | traced across panels |
| P04581 | `app/api/blocked/route.ts` derives the IP server-side, never from the body | read: `clientIp(req)` | ✅ | |
| P04582 | The GET fails OPEN on an unreadable count | read: `(blocked ? await usedToday(ip) : 0) ?? 0` | ✅ | the page must still render |
| P04583 | The POST fails CLOSED on an unreadable count | read: `if (used === null) return 503 try_later` | ✅ | T9 F25 |
| P04584 | `usedToday` returns null (not 0) when the read genuinely failed | read | ✅ | the two callers want opposite things |
| P04585 | The unblock request is capped at 3 per IP per 24h | read: `MAX_PER_DAY` + the `gte(created_at, since)` count | ✅ | |
| P04586 | A caller who is no longer blocked is told to reload, not given a row | read: `not_blocked` | ✅ | |
| P04587 | The GET has its own ceiling now | read: `withinMemoryCap("blocked:get:…", 60_000, 20)` | ✅ | I3 |
| P04588 | The throttled GET degrades to the same page, not an error | read: it answers 200 with `throttled: true` | ✅ | |
| P04589 | The unblock message is capped at 200 chars | read: `.slice(0, 200)` | ✅ | |
| P04590 | /api/blocked writes ONLY to `unblock_requests` — it never pings a phone | grep the file for `sendOwnerAlert`/`recordAlert` → none | ✅ | owner's call: scroll-only |
| P04591 | The blocked page's Retry and Request buttons both handle a network failure | read `app/staff-login/BlockedView.tsx` catch branches | ✅ | |
| P04592 | The blocked page's Request button words the transient 503 as "try again" | read: the `else` branch text | ✅ | matches `reason:"try_later"` |
| P04593 | An unblocked device is bounced back to /staff-login by BOTH paths | read: `refresh()` and `retry()` and the `not_blocked` branch | ✅ | |
| P04594 | The blocked page never shows the raw IP or device id to the visitor | read the whole component | ✅ | |
| P04595 | `app/api/log/client-error/route.ts` caps the raw body at 2 kB | read: `raw.length > 2048` | ✅ | |
| P04596 | The `taps` branch has its own ceiling | read: `MAX_TAPS_PER_DEVICE_10MIN` 40 | ✅ | T9 — this branch used to return before the cap |
| P04597 | The `error` branch keeps its ceiling | read: `MAX_ERRORS_PER_DEVICE_10MIN` 5 | ✅ | |
| P04598 | The cap applies with OR without a device cookie | read: `capKeyFor(req)` outside any `if (device)` | ✅ | 2026-08-04 |
| P04599 | Rows are written under the SAME key the cap counts by | read: `device_id: capKey` on both inserts | ✅ | else the cap counts zero forever |
| P04600 | The panel name is allow-listed | read: `PANELS.has(panel)` | ✅ | |
| P04601 | A malformed `rid` becomes null rather than reaching the insert | read: `UUID.test(ridRaw)` | ✅ | |
| P04602 | The owner alert runs inside `after()` so the platform does not freeze it | read: `after(sendOwnerAlert(...))` | ✅ | proven flaky 2026-07-24 |
| P04603 | The error-logger never surfaces an error of its own | read: the outer catch answers `{ok:true, skipped:"internal"}` | ✅ | |
| P04604 | Message / where / detail are each length-capped before the insert | read: 300 / 120 / 500 / 1500 | ✅ | |
| P04605 | client-error answers 200 for a junk body on the running app | POST `{}` to :4110/api/log/client-error | ✅ | watched running |
| P04606 | `app/api/maintenance/route.ts` requires a manager (or the admin) for the READ | read: `scope(req)` → `requireRole(req,"manager")` | ✅ | |
| P04607 | The WRITE additionally checks the Access → Menu → maintenance rung | read: `scope(req, true)` and the `access_config.maintenance` block | ✅ | owner, 2026-08-01 |
| P04608 | The manager rung distinguishes `owner` from `owner_manager` | read: `who !== "owner_manager"` | ✅ | |
| P04609 | Maintenance is scoped to the ACTING restaurant, never `id='site'` | read: `.eq("restaurant_id", s.rid)` on both | ✅ | fixed 2026-07-08 |
| P04610 | A read failure answers a sentence, never `r.error.message` | read | ✅ | T9 |
| P04611 | A write that matched zero rows answers 409 and logs nothing | read: `if (!r.data?.length)` before `logAction` | ✅ | a record of something that didn't happen |
| P04612 | Taking the menu down is recorded in the Activity log | read: `logAction("manager", on ? "maintenance_on" : …)` | ✅ | sweep 2026-08-04 |
| P04613 | A DB blip on the gate answers 503, not 401 | read: `g.transient` branch | ✅ | |
| P04614 | The admin super-user (no staff cookie) may always write | read: `if (forWrite && g.user)` — admin skips | ✅ | |
| P04615 | Maintenance answers the running app for a signed-in manager | GET :4110/api/maintenance with the manager cookie | ✅ | watched running |
| P04616 | `app/api/aggregators/webhook/[source]/route.ts` answers `{disabled:true}` while the flag is off | read: `aggregatorsEnabled()` | ✅ | dormant by design |
| P04617 | An unknown source answers 404 | read: `source !== "zomato" && source !== "swiggy"` | ✅ | |
| P04618 | `verifyWebhook` is awaited | read: `!(await verifyWebhook(...))` | ✅ | the issue-media lesson |
| P04619 | The webhook body is capped at 64 kB | read: `raw.length > 64_000` → 413 | ✅ | I4 |
| P04620 | An oversize order is refused, not truncated | read: it returns before parsing | ✅ | a trimmed order is food somebody doesn't get |
| P04621 | The restaurant is RESOLVED from the outlet mapping, never defaulted to #1 | read: `resolveWebhookRestaurant` | ✅ | T9 F11 |
| P04622 | An unmapped outlet answers 404 so the platform stops retrying | read | ✅ | |
| P04623 | A duplicate external_id answers 200, not 5xx | read: `duplicate:true` still 200 | ✅ | else Zomato retries forever |
| P04624 | The catch never hands our schema to an outside company | read: `lib/aggregators.ts` turns a DB error into a sentence | ✅ | |
| P04625 | The `aggregators` flag stays invisible in every UI | grep the panels for `aggregators` → backend only | ✅ | deliberate, not a finding |
| P04626 | `app/api/staff-login/route.ts` runs the bot check BEFORE the throttle read | read the order | ✅ | untargeted traffic costs nothing |
| P04627 | The bot check refuses only on a present-and-wrong signal | read `lib/botCheck.ts` `botVerdict` | ✅ | fails open by design |
| P04628 | A bot refusal reuses the ORDINARY 401, never "you look like a bot" | read: `return bad({})` | ✅ | no free feedback |
| P04629 | A bot refusal does not count against the human lockout | read: no `throttleFail` on that branch | ✅ | junk traffic can't lock the owner out |
| P04630 | The password compare is constant-time via `safeEqual` | read: `safeEqual(await sha256hex(password), await sha256hex(expected))` | ✅ | |
| P04631 | The length cap sits AHEAD of the hash | read: `tooLong` short-circuits before `safeEqual` | ✅ | |
| P04632 | An empty `ADMIN_PASSWORD` can never match | read: `!!expected &&` | ✅ | |
| P04633 | A deliberate block and a wrong-tries lockout are different answers | read: `throttleIsBlocked` → `{blocked}` vs `{locked}` | ✅ | one offers a way back |
| P04634 | The redirect target only accepts a same-site relative path | read: `startsWith("/") && !startsWith("//")` | ✅ | no open redirect |
| P04635 | Both cookies are `secure` in production | read: `const secure = NODE_ENV === "production"` on AUTH and FLAG | ✅ | sweep 2026-08-05 |
| P04636 | The auth cookie is httpOnly; the flag cookie deliberately is not | read | ✅ | the UI reads the flag |
| P04637 | Every attempt (ok / wrong / blocked / bot) writes an admin log line | read: four `logAction` calls | ✅ | |
| P04638 | After N wrong tries the admin is ALERTED but never locked out | read: `ADMIN_ALERT_AT` → `recordAlert`, lock stays at `ADMIN_MAX_FAILS` | ✅ | mig 208 |
| P04639 | A correct password clears the counter | read: `throttleReset` | ✅ | |
| P04640 | The JSON path and the no-JS redirect path give the same verdict | read: `bad()` covers both shapes | ✅ | |
| P04641 | The route never echoes the typed password anywhere | grep the file for `password` in a log/detail string → none | ✅ | |
| P04642 | `app/staff-login/LoginForm.tsx` posts url-encoded so the no-JS form and the fetch agree | read: `new URLSearchParams({ password, next, [BOT_TRAP_FIELD], [BOT_ELAPSED_FIELD] })` | ✅ | |
| P04643 | The bot fields are read BEFORE the first await | read: `botFields(e.currentTarget)` above `await fetch` | ✅ | React clears currentTarget |
| P04644 | `<BotTrap/>` is LAST in the form | read: it is the final child before `</form>` | ✅ | first would refuse real people |
| P04645 | The wrong-password message auto-clears after 3s; a lockout does not | read the `useEffect` | ✅ | owner 2026-07-26 |
| P04646 | The typed password survives a wrong attempt | read: no `setPassword("")` on failure | ✅ | |
| P04647 | "N attempts left" is shown once the person starts missing | read: the `attemptsLeft` span | ✅ | |
| P04648 | This door says "admin console", not "staff sign in" | read the subtitle | ✅ | owner, 2026-08-13 |
| P04649 | There is a plain link to the staff door for someone at the wrong one | read: `<a href="/login">Staff sign in →</a>` | ✅ | a link, not a redirect |
| P04650 | The `100vh` on this page is left alone | read the REJECTED comment | ✅ | REJECTED (owner, 2026-08-13) R19 |
| P04651 | The SVG brand mark is left alone | read the REJECTED comment | ✅ | REJECTED (owner, 2026-08-14) R24 |
| P04652 | `app/staff-login/page.tsx` fails OPEN if the block check errors | read `lib/loginThrottle.throttleIsBlocked` | ✅ | a blip must not trap a real user |
| P04653 | `?blocked=1` also renders the blocked view | read: `|| blocked === "1"` | ✅ | the no-JS redirect path |
| P04654 | `?bad=1` / `?locked=1` become the right initial error | read: `initialError` | ✅ | |
| P04655 | `next` defaults to `/aevinite` | read the destructure default | ✅ | |
| P04656 | `app/api/panel-login/route.ts` runs the bot check before the rate limit | read the order | ✅ | junk can't use up a real allowance |
| P04657 | An unknown slug gets the SAME generic message as bad credentials | read: `"Wrong name or password."` | ✅ | never confirm which slugs exist |
| P04658 | The rate limit is counted per username (+ restaurant when the door names one) | read: `` `${restaurantId \|\| "*"}:${subjectFor(uname)}` `` | ✅ | mig 205 |
| P04659 | `describe` only runs when the wall is actually hit | read: it is a callback, not a value | ✅ | a normal login pays nothing |
| P04660 | A DB blip on the credential lookup answers 503, never 401 | read: `r.transient ? 503 : 401` | ✅ | 2026-07-03 stress test |
| P04661 | The real refusal reason goes to the ADMIN log, never to the person | read: `logAction(... detail ...)` vs `r.error` | ✅ | no account enumeration |
| P04662 | A transient blip writes NO login-failed row | read: `if (!r.transient && …)` | ✅ | a DB hiccup isn't an attempt |
| P04663 | A successful login clears the login counter | read: `rateResetOnSuccess` | ✅ | shared tablets |
| P04664 | An owner with no owner-panel-enabled restaurant is refused with a reason | read: `ownerPanelEnabled(u.id, false)` → 403 sentence | ✅ | |
| P04665 | The owner's entitlement is read UNCACHED at the door | read: the `false` second argument | ✅ | must reflect an admin flip at once |
| P04666 | A binned restaurant blocks every non-owner role | read: `isRestaurantDeleted` before the panel check | ✅ | mig 128 |
| P04667 | A disabled panel refuses the login with an actionable sentence | read: `isPanelEnabled` → "Ask your admin to turn it on." | ✅ | mig 106 |
| P04668 | The login log row carries the person's OWN restaurant_id | read: `restaurant_id: u.restaurant_id ?? null` | ✅ | else every tenant filed under #1 |
| P04669 | The login log row carries the stable `actor_id` | read | ✅ | a rename must not orphan past rows |
| P04670 | The login log detail does not repeat the name three times | read: only the username when it differs | ✅ | T15 sweep |
| P04671 | The user cookie is httpOnly + sameSite lax + secure in production | read the `cookies.set` options | ✅ | |
| P04672 | The cookie's 7-day age is also enforced in the signature | read `lib/userAuth.ts` TOKEN_TTL_MS | ✅ | |
| P04673 | `needsProfile` is derived from `profile_confirmed`, not from empty fields | read | ✅ | |
| P04674 | A 401 from panel-login on the running app carries the generic sentence | POST wrong credentials once on :4110 (single attempt, own test user) | ✅ | watched running; cleaned up |
| P04675 | `app/login/LoginForm.tsx` reads the bot fields before the first await | read | ✅ | |
| P04676 | `<BotTrap/>` is LAST in this form too | read | ✅ | |
| P04677 | A new sign-in clears the previous account's owner snapshots | read: `clearOwnerSnaps()` | ✅ | |
| P04678 | A new sign-in tells the service worker to wipe saved screens | read: `postMessage({ type: "LFH_CLEAR_DATA" })` | ✅ | shared tablet |
| P04679 | `?next` is honoured only when it equals this user's own panel home | read: `next === home ? next : home` | ✅ | open-redirect guard |
| P04680 | The scoped door lands on the scoped panel URL | read: `` `/r/${restaurantSlug}${base}` `` | ✅ | the address bar keeps saying which restaurant |
| P04681 | ROLE_HOME here matches `lib/panelGate.ts` ROLE_HOME | compare both maps | ✅ | |
| P04682 | The `100vh` on this page is left alone | read the REJECTED comment | ✅ | REJECTED R19 |
| P04683 | The ✦ text mark is left alone | read the REJECTED comment | ✅ | REJECTED R24 |
| P04684 | The sign-in button is disabled while a request is in flight | read: `disabled={busy \|\| !username \|\| !password}` | ✅ | no double submit |
| P04685 | A network failure shows a retryable sentence and re-enables the button | read the catch | ✅ | |
| P04686 | `app/login/page.tsx` redirects an already-signed-in person to their panel | read: `redirect(ROLE_HOME[u.role])` | ✅ | |
| P04687 | A database blip on `/login` no longer renders an error page instead of the form | read: `AuthDbError` caught → fall through to `<LoginForm/>` | ✅ | **FIXED** — see T10-findings F1 |
| P04688 | The fallthrough is safe: the form itself is public | read — nothing on the page needs the cookie | ✅ | |
| P04689 | `next` is passed through as a string only | read: `typeof next === "string" ? next : ""` | ✅ | |
| P04690 | `/login` renders the form on the running app, at both sizes | GET :4110/login signed out at 1280×800 and A35 360×780 dpr3, READ both images | ✅ | 128 rendered chars; Sign in measured 322×43 @ y=499 (desktop) and 273×43 @ y=481 (A35 — well above the 780 fold); the bot field renders LAST and invisibly in both |
| P04691 | `app/api/panel-logout/route.ts` is POST-only | grep for `export async function GET` → none | ✅ | T9 improvement 13 |
| P04692 | A logout never depends on the database answering | read: `userFromCookie` in a try/catch | ✅ | T17 F8 |
| P04693 | The cookie is cleared whether or not the audit line was written | read: the clear is after the try | ✅ | |
| P04694 | The logout redirects 303 so the browser follows with a GET | read | ✅ | a form POST must not re-post |
| P04695 | `app/api/staff-logout/route.ts` is POST-only | grep | ✅ | sweep 2026-08-05 |
| P04696 | staff-logout clears AUTH, FLAG and the act-as restaurant cookie | read all three `cookies.set(..., maxAge: 0)` | ✅ | |
| P04697 | staff-logout lands on the open guest menu, not a password screen | read: `redirect(new URL("/menu", req.url), 303)` | ✅ | |
| P04698 | Neither logout route touches data | read both files | ✅ | |
| P04699 | Both logout routes answer 303 on the running app | POST each on :4110 with a throwaway session | ✅ | watched running |
| P04700 | `app/api/panel-profile/route.ts` GET answers 200 `{staff:false}` for the admin | read: the `if (!u)` branch | ✅ | a 401 fed the error log a fake problem |
| P04701 | POST still answers 401 when nobody is signed in | read | ✅ | writing a profile needs a login |
| P04702 | A DB blip answers 503 + `busy`, on BOTH GET and POST | read: `whoIsAsking` | ✅ | T17 F9 |
| P04703 | The busy answer carries the `X-LFH-Busy` header the offline layer reads | read | ✅ | |
| P04704 | The three independent reads start together | read: `tablesP` / `ladderP` / `rowP` | ✅ | T9 improvement 14 |
| P04705 | `rowP` attaches its rejection handler immediately | read: `.then(x => x, e => …)` on the same expression | ✅ | a floating rejection can kill the invocation |
| P04706 | POST is wrapped in `withIdempotency` | read | ✅ | |
| P04707 | POST runs ONE `expectClash` gate covering every branch | read: the block before the password branch | ✅ | |
| P04708 | The password change re-authenticates with the current password | read: `verifySecret(current, row?.password_hash)` | ✅ | |
| P04709 | The password change is rate-limited PER ACCOUNT, not per device | read: `rateAllowed("password_change", u.id, …)` | ✅ | a guesser can clear a cookie |
| P04710 | The wall is checked BEFORE `verifySecret` so a wrong guess counts | read the order | ✅ | |
| P04711 | A successful change clears the counter | read: `rateResetOnSuccess` | ✅ | |
| P04712 | The password write checks BOTH the error and the row count | read: `.select("id")` then `pw.error` and `!pw.data?.length` | ✅ | T9 2026-08-06 |
| P04713 | A failed password write does NOT log `password_change` | read: the log is after both checks | ✅ | |
| P04714 | `token_version` is bumped so every existing session ends | read | ✅ | |
| P04715 | The self-profile patch is whitelisted by `SELF_PROFILE_FIELDS` | read: `mergeProfilePatch(cur, p, SELF_PROFILE_FIELDS)` | ✅ | nobody gives themselves a raise |
| P04716 | The profile branch checks its write error | read: `if (error) return 500 sentence` | ✅ | |
| P04717 | The name/phone/PIN write checks BOTH error and row count | read: `saved.error` and `!saved.data?.length` | ✅ | a PIN only bites later |
| P04718 | The username uniqueness check is scoped to THIS restaurant | read: `.eq("restaurant_id", u.restaurant_id)` | ✅ | sweep 2026-08-05 |
| P04719 | The uniqueness check excludes the person's own row and soft-deleted rows | read: `.neq("id", u.id).is("deleted_at", null)` | ✅ | |
| P04720 | A PIN must be 4–8 digits and is stored as a salted slow hash | read: `/^\d{4,8}$/` + `hashSecret` | ✅ | |
| P04721 | A person who may not self-set a PIN is refused with a sentence | read: `can_self_set_pin` | ✅ | |
| P04722 | A person who may not self-reset a password is refused with a sentence | read: `can_self_reset` | ✅ | |
| P04723 | Pay is only sent when the person is ON the pay list AND allowed to see it | read: `can_see_own_pay !== false && in_payroll === true` | ✅ | mig 221 |
| P04724 | The job fields are sent read-only, whatever the pay switch says | read the `job` object + the comment | ✅ | a shift is not money |
| P04725 | The payments read is scoped by staff_id AND restaurant_id, with a limit | read: `.eq("staff_id").eq("restaurant_id").limit(40)` | ✅ | |
| P04726 | The person's own row id is returned so the clash gate has something to name | read: `id: u.id` in `base` | ✅ | |
| P04727 | Kitchen has NO profile and this route does not invent one | read: `hasProfile(u.role)` from `lib/staffProfile` | ✅ | ruled three times |
| P04728 | panel-profile GET answers on the running app for the manager | GET :4110/api/panel-profile with the manager cookie | ✅ | watched running |
| P04729 | `app/api/kitchen/[...path]/route.ts` gates every request with `requireRole(req,"kitchen")` | read: `gate()` is the first line of GET and postImpl | ✅ | |
| P04730 | A DB blip on the kitchen gate answers 503, not 401 | read: `g.transient` | ✅ | keeps the panel logged in |
| P04731 | Kitchen refuses without a restaurant scope | read: `if (!rid) return err(…, 400)` | ✅ | |
| P04732 | The kitchen board read names its columns and carries a limit | read: `menu_items` 2000, `aggregator_orders` 500 | ✅ | egress rule |
| P04733 | The platform read drops `payload` and `status_history` | read the column list | ✅ | the two big JSON columns |
| P04734 | A targeted `?table=` refetch returns only orders + items + tags | read the `if (tbl)` branch | ✅ | |
| P04735 | A non-numeric `?table=` means "full board", never a 500 | read: `/^\d{1,6}$/` else null | ✅ | |
| P04736 | Table tags ride along on the TARGETED path too | read: `Promise.all([liveOrdersAndItems, tableTagMap])` | ✅ | T4 — else the badge waits 60s |
| P04737 | `tableTagMap` returns `{}` when the module is off | read: `tableTagsLadder(rid).effective` | ✅ | a mark nobody can clear |
| P04738 | `tableTagMap` swallows its own failure so the pass still arrives | read the catch | ✅ | losing a badge is survivable |
| P04739 | `tableTagMap` is bounded | read: `.limit(500)` | ✅ | |
| P04740 | Platform tickets are filtered to channels that are actually live | read: `kSources` built from `platL`/`parcL` + `platform_channels` | ✅ | mig 209 |
| P04741 | The reprint ride-along only runs when jobs exist | read: `if (jobs.length)` | ✅ | the common board read pays nothing |
| P04742 | A soft-deleted order is excluded from the reprint ride-along | read: `.is("deleted_at", null)` | ✅ | T7 F11 |
| P04743 | A print job stuck 'printing' for over 2 minutes is offered again | read: `staleIso` in the `.or(...)` | ✅ | a tab died mid-print |
| P04744 | Every kitchen write drops the floor snapshot BEFORE and AFTER | read: `invalidateFloor(rid)` + `invalidateFloorAfter` | ✅ | |
| P04745 | The after-wrapper carries rid on a WeakMap keyed by the request | read | ✅ | concurrent requests never mix |
| P04746 | The after-wrapper uses `finally` so a throwing handler still drops it | read | ✅ | |
| P04747 | Kitchen POST is wrapped in `withIdempotency` | read | ✅ | |
| P04748 | An empty id segment is refused before it reaches a uuid query | read: `emptyIdSegment(b) \|\| emptyIdSegment(c)` | ✅ | |
| P04749 | A blocked device is refused, scoped to THIS restaurant | read: `deviceBlocked(dev, rid)` | ✅ | not platform-wide |
| P04750 | The replay-clash gate runs before every kitchen action | read: `replayClash(req, rid, a, b, c, body)` | ✅ | |
| P04751 | The expect-clash gate runs before every kitchen action | read: `expectClash(req, rid)` | ✅ | |
| P04752 | `orders/:id/accept` is scoped `.eq("restaurant_id", rid)` on read AND write | read | ✅ | service-role bypasses RLS |
| P04753 | `orders/:id/accept` 404s when the order has left the board | read: `if (!cur)` | ✅ | |
| P04754 | `orders/:id/ready` never un-serves an already-served dish | read: `i.status === "served" ? "served" : "ready"` | ✅ | |
| P04755 | `orders/:id/ready` leaves the ORDER at 'preparing' | read | ✅ | serving is the waiter's action |
| P04756 | `orders/:id/unready` restores each dish to the status it actually had | read: the `snap` / `groups` logic | ✅ | a blanket write would promote the others |
| P04757 | `unready` refuses an unknown `prev` value | read: `VALID.includes(d.prev)` | ✅ | |
| P04758 | `unready` caps the snapshot at 200 lines | read: `.slice(0, 200)` | ✅ | |
| P04759 | `unready` only touches rows that belong to THIS order in THIS restaurant | read: `.eq("order_id", b).eq("restaurant_id", rid).in("id", …)` | ✅ | |
| P04760 | `unready` leaves a SERVED dish alone | read: `if (wasNow === "served") continue` | ✅ | un-serving is a different action |
| P04761 | `unready` groups by target status so a mixed ticket costs ≤3 updates | read | ✅ | the whole point of the endpoint |
| P04762 | `unready` clears `served_at` on every row it moves back | read: `served_at: null` | ✅ | |
| P04763 | `unready` refuses out loud when nothing is left to move | read: `if (!groups.size) return 409` | ✅ | |
| P04764 | `unready` rolls the parent order up from what the rows now say | read the rollup | ✅ | identical to items/:id/status |
| P04765 | `items/:id/status` 404s when the update matched no row | read: `if (!item)` | ✅ | sweep 2026-08-04 |
| P04766 | `items/:id/status` clears `served_at` for any pre-served status | read: `patch.served_at = status === "served" ? nowIso() : null` | ✅ | |
| P04767 | `items/:id/status` is logged | read: `logAction("kitchen","item_status", …)` | ✅ | the most frequent kitchen action |
| P04768 | `platform/:id/status` confirms the row belongs to this restaurant first | read: `owns` | ✅ | the RPC has no tenant scope |
| P04769 | The kitchen may not advance a still-'new' platform order by ANY status without the toggle | read: `if (owns.status === "new")` | ✅ | audit 2026-07-07 |
| P04770 | `dishes/:id/sold-out` is scoped on read and write | read both `.eq("restaurant_id", rid)` | ✅ | |
| P04771 | The 86 board busts the guest menu cache | read: `revalidateTag(menuTag(rid), { expire: 0 })` | ✅ | T13 — the backstop is 24h |
| P04772 | The bust uses `expire: 0`, not the "max" profile | read | ✅ | "max" hands the OLD bundle to the next reader |
| P04773 | A bust failure never fails the 86 itself | read: the try/catch | ✅ | |
| P04774 | `print-jobs/claim` is an atomic single UPDATE with a status filter | read | ✅ | two tabs cannot both print |
| P04775 | `print-jobs/claim` caps the id list at 20 | read: `.slice(0, 20)` | ✅ | |
| P04776 | `print-jobs/:id/done` ok:true resolves every open printer problem | read | ✅ | the auto-solve the owner asked for |
| P04777 | A printed ticket writes an `info`-level Activity row naming the KOT | read: `kot_printed` | ✅ | owner, 2026-08-14 |
| P04778 | A failed print re-queues with a counted attempt and parks after 5 | read: `parked = attempts >= 5` | ✅ | |
| P04779 | The parked failure is `warn`; a retry is `info` | read: `level: parked ? "warn" : "info"` | ✅ | don't cry wolf |
| P04780 | `printer-events` merges an already-open event of the same kind | read: `count + 1`, never a duplicate row | ✅ | a rush must not bury the floor |
| P04781 | `printer-events` allow-lists the problem kind | read: `kinds.includes(kind)` | ✅ | |
| P04782 | Kitchen route errors are logged with the endpoint that failed | read: `logError(... detail: \`POST ${path}\`)` | ✅ | |
| P04783 | Kitchen route errors go through `panelFailure` | read both catches | ✅ | a busy DB is told apart from a bug |
| P04784 | `worthLogging(e)` keeps ordinary refusals out of the error log | read | ✅ | |
| P04785 | The kitchen board loads on the running app | GET :4110/api/kitchen/board with the kitchen cookie | ✅ | watched running |
| P04786 | The kitchen `whoami` marks an admin view without telling the cook | read: `higherView: !g.user && !simulate` | ✅ | admin invisible |
| P04787 | `?view=real` on an admin tab answers as the real kitchen | read: `simulate` | ✅ | |
| P04788 | `?as=` names the person on the ribbon only — the KDS has no per-person settings | read the comment + `personLabel(asPerson)` | ✅ | owner, 2026-08-02 |
| P04789 | `app/api/tablet/[...path]/route.ts` gates every request with `requireRole(req,"tablet")` | read | ✅ | |
| P04790 | A DB blip on the tablet gate answers 503, not 401 | read | ✅ | |
| P04791 | `managerPinGate` lets the admin through | read: `tokenIsValid` first | ✅ | |
| P04792 | `managerPinGate` stays open until ANY manager has a PIN | read: `anyManagerHasPin(rid)` | ✅ | bootstrap |
| P04793 | The PIN throttle key is per restaurant AND per device | read: `` `pin:${rid}:${dev}` `` | ✅ | |
| P04794 | The configurable PIN limit only counts a real PIN attempt | read: `body?.managerPin ? … : false` | ✅ | never every gated action |
| P04795 | A locked PIN answers 429 with `locked:true` | read | ✅ | |
| P04796 | A missing PIN answers 403 with `needPin:true` | read | ✅ | a code, not prose |
| P04797 | `pinActorFrom` names EVERY manager a shared PIN could belong to | read: `names.join(" / ")` and `actor_id: null` when shared | ✅ | no single truth |
| P04798 | `tabletPerm` refuses `WAITER_NEVER` for a real waiter only | read: `user.role === "tablet" && WAITER_NEVER.includes(key)` | ✅ | a manager may still issue an invoice |
| P04799 | `tabletPerm` checks the FEATURE half before any per-person override | read: `WAITER_FEATURE_OF[key]` block | ✅ | sweep T6 |
| P04800 | `tabletPerm` reads the cap through `waiterCapValue`, not `settings[key] \|\| "off"` | read | ✅ | 8 of 9 restaurants were wrongly off |
| P04801 | `closeUnpaidGate` has its own Access row, not `void_bills` | read: `waiterConfigCapValue("close_unpaid", cfg)` | ✅ | mig 268 |
| P04802 | `closeUnpaidGate` defaults to 'pin' when unset | read: the final `return managerPinGate(...)` | ✅ | |
| P04803 | `overlayUserPerms` applies the Feature half LAST | read: `waiterFeatureOffCols(accessConfig)` after the overrides | ✅ | the restaurant beats one person |
| P04804 | An override can never grant a `WAITER_NEVER` cap | read: `if (asWaiter && WAITER_NEVER.includes(k)) continue` | ✅ | |
| P04805 | The admin is never blocked by a waiter rung | read: `if (user && accessConfig !== undefined)` | ✅ | X-ray honesty |
| P04806 | `narrowSummary` narrows tiles, calls, requests and joiners | read | ✅ | mig 222 |
| P04807 | `narrowSummary` also narrows `order_count` and `latest_order_table` | read | ✅ | restaurant-wide fields |
| P04808 | `narrowSummary` deliberately leaves `blocklist` alone | read the comment | ✅ | the waiter needs to see a blocked guest |
| P04809 | A restricted viewer narrows a structuredClone, never the shared snapshot | read: `myTables ? structuredClone(shared) : shared` | ✅ | live bug, 2026-08-02 |
| P04810 | `merges` rides on a shared key so 20 targeted calls issue one read | read: `sharedFloorSummary(\`merges:${rid}\`, …)` | ✅ | |
| P04811 | `merges` is attached to a COPY, never written into the snapshot | read: `{ ...summary, merges }` | ✅ | |
| P04812 | The whole-floor summary read is shared for 1.5s; a targeted one is not | read the ternary | ✅ | |
| P04813 | `?nomenu=1` omits the `dishes` KEY entirely (not `[]`) | read: `if (dishesP) body.dishes = …` | ✅ | "not sent" ≠ "empty" |
| P04814 | The settings row is stripped through `panelSafeSettings` | read | ✅ | T17 — `platform_channels` holds keys |
| P04815 | `access_config` is peeled off the restaurant row before it is sent | read: `Object.fromEntries(... k !== "access_config")` | ✅ | sweep T6 |
| P04816 | The KOT ▾ rung is resolved from the already-fetched settings row | read: `tableOpsEffectiveFromRow` | ✅ | no extra query |
| P04817 | Parcel is permanent — the retired toggle is not read back | read: `parcelEffectiveFromRow = () => true` | ✅ | owner, 2026-08-03 |
| P04818 | `my_tables` is sent so the client can grey the rest of the floor | read | ✅ | a missing tile would look free |
| P04819 | `menu-sig` hashes only the slim identifying columns | read: `id,title,price,tags` + categories | ✅ | T4 improvement 7 |
| P04820 | `menu-sig` answers in about forty bytes | GET :4110/api/tablet/menu-sig, measure the body | ✅ | watched running |
| P04821 | `customer-recognize` never lists customers | read: it returns `{known,name,visits,blocked}` | ✅ | |
| P04822 | `customer-search` needs at least 3 digits and returns at most 6 | read: `q.length < 3`, `p_limit: 6` | ✅ | |
| P04823 | `khata/customers` runs the ladder AND the tri-state before searching | read | ✅ | |
| P04824 | `khata/customers` search text goes through `safeSearch` | read | ✅ | `,` and `)` end a PostgREST term |
| P04825 | `banquet-items` checks the ladder and the cap, and is bounded | read: `.limit(200)` | ✅ | |
| P04826 | `/state` with no `?table=` is refused with a pointer, not a 404 | read: the 400 sentence | ✅ | T4 improvement 4 |
| P04827 | `/state?table=` outside the waiter's section answers as an EMPTY table | read: `if (!allows(limit, tbl)) return ok({…})` | ✅ | a 403 would spin a reload loop |
| P04828 | `/state?table=` scopes members by the table's open session ids | read: `.in("session_id", sids)` | ✅ | |
| P04829 | Every tablet write drops the floor snapshot before AND after | read | ✅ | |
| P04830 | The waiter-section gate is ONE check covering all ~38 branches | read: `blockedReason(sectionLimit, a, b, c, body)` | ✅ | a forgotten branch is a silent hole |
| P04831 | The section gate checks BOTH ends of a move | read `lib/tableAssign.blockedReason` | ✅ | |
| P04832 | A blocked device can do nothing from the tablet, scoped to this restaurant | read: `deviceBlocked(dev, rid)` | ✅ | |
| P04833 | The off-plan table check runs for `order` and `sessions/open` | read the condition | ✅ | |
| P04834 | `POST /order` checks the module rung then the waiter cap | read the two gates | ✅ | mig 179 / 178 |
| P04835 | `POST /order` refuses a table outside 1..table_count | read | ✅ | a typo would float a phantom order |
| P04836 | The double-tap signature folds in options AND per-line allergens | read: `optSig` / `remSig` | ✅ | else the guard never fired for add-ons |
| P04837 | The double-tap guard is overridable with `confirmDuplicate` | read | ✅ | two guests, same drink |
| P04838 | `confirmDuplicate` is passed through to the RPC's atomic guard | read: `p_confirm_duplicate` | ✅ | the JS pre-check races |
| P04839 | The RPC's own duplicate answer is surfaced as the SAME 409 shape | read: `duplicateWarning` branch | ✅ | |
| P04840 | An RPC refusal (`ok:false`) is surfaced as an error, not a success | read: `editErrMsg(reason)` | ✅ | a tap that vanished in silence |
| P04841 | A waiter-placed order skips the kitchen accept step | read: the `placedId` block | ✅ | owner, 2026-06-16 |
| P04842 | `placed_by_id` / `placed_by` ride on the SAME update | read | ✅ | no extra round trip |
| P04843 | `POST /parcel` enforces the module for the admin too | read: no `actor &&` | ✅ | mig 259 |
| P04844 | Parcel refuses an unresolvable dish instead of dropping the line | read: `if (!d) return err(editErrMsg("unknown_item"))` | ✅ | a line short = food not delivered |
| P04845 | Parcel honours the 86 board by hand | read: `d.tags.includes("sold-out")` | ✅ | it prices itself |
| P04846 | Parcel refuses an open-price line with no price | read: `if (price <= 0)` | ✅ | |
| P04847 | Parcel clamps qty to 1..99 and price to 0..100000 | read | ✅ | |
| P04848 | Parcel freezes `tax_mode` onto every line | read: `resolveTaxMode(d.tax_mode, parcelSet)` | ✅ | exactly as lfh_price_order does |
| P04849 | Parcel's stored total is subtotal + tax, matching the paper | read: `splitBill(picked, parcelSet, 0).total` | ✅ | fixed 2026-08-02 |
| P04850 | Only the taxable part of a parcel is taxed | read: `splitBill` | ✅ | mig 270 |
| P04851 | Parcel's post-insert patch is scoped by id AND restaurant | read | ✅ | |
| P04852 | `banquet/place` checks the ladder then the cap | read | ✅ | |
| P04853 | `banquet/place` surfaces the RPC's `ok:false` as a 400 with a sentence | read: `banquetErrMsg` | ✅ | |
| P04854 | `requests/:id/resolve` only resolves a STILL-PENDING request | read: `.eq("status","pending")` | ✅ | T4 — else a stale Approve seats a party |
| P04855 | A repeat of your OWN answer is idempotent; a DIFFERENT answer is refused | read: the `!reqRow` block | ✅ | twin-parity, 2026-08-07 |
| P04856 | Approving an `open` request opens the table race-tolerantly | read: `openTableSession` | ✅ | 2026-07-30 |
| P04857 | `calls/:id/attend` 404s when the call is gone | read: `if (!row[0])` | ✅ | T4 |
| P04858 | `calls/:id/reopen` 404s when the call is gone | read | ✅ | an undo that undid nothing |
| P04859 | `members/:id/approve` 404s when the guest has left | read | ✅ | |
| P04860 | `members/:id/make-head` refuses when the table is not open | read | ✅ | |
| P04861 | `members/:id/remove` 404s and is logged | read | ✅ | |
| P04862 | `members/:id/ban` needs a manager PIN | read: `managerPinGate` | ✅ | |
| P04863 | A ban carries the guest's DEVICE id as well as their phone | read | ✅ | mig 077 — the wall must stick |
| P04864 | A ban row carries `restaurant_id`, never the #1 default | read | ✅ | fixed 2026-07-07 |
| P04865 | `orders/:id/discount` is scoped by restaurant as well as gated | read the comment + `.eq("restaurant_id", rid)` | ✅ | a feature gate is not a tenant gate |
| P04866 | Per-ticket and whole-bill discount are mutually exclusive | read: the `sessions.discount > 0` check | ✅ | mig 143 |
| P04867 | The discount is clamped to the TAXABLE base, not the subtotal | read: `Number(cur.taxable_base ?? cur.subtotal)` | ✅ | mig 270 — an MRP price is final |
| P04868 | The per-role %-cap is enforced server-side | read: `overDiscountCap(amount, base, cap)` | ✅ | |
| P04869 | A waiter's discount writes a Removals audit row | read: `recordRemoval({ kind: "discount_given" })` | ✅ | 2026-08-03 |
| P04870 | Removing a discount (amount 0) is NOT recorded as a removal | read: `if (amount > 0)` | ✅ | it puts money back |
| P04871 | `sessions/:id/bill-discount` clamps to Σ taxable base over unpaid orders | read: `maxBase` | ✅ | |
| P04872 | `sessions/:id/invoice` confirms the session is this restaurant's first | read: `ownsGen` | ✅ | the RPC has no tenant param |
| P04873 | The tablet invoice path saves the bill customer before issuing | read: `saveBillCustomer` | ✅ | mig 227 |
| P04874 | A re-issue reason is captured, not hardcoded null | read: `genReasonT` | ✅ | |
| P04875 | A settled bill's invoice cannot be reopened (LFH01 → 409) | read | ✅ | mig 189 |
| P04876 | `tables/:t/restart` resolves a merged child to its parent | read: `mergeParentTable` | ✅ | |
| P04877 | A restart needs a PIN only when there is cooking food or an unpaid bill | read: `needsPin` | ✅ | |
| P04878 | A restart records how much was outstanding | read: `owed` in the log detail | ✅ | |
| P04879 | A restart archives, never cancels | read: `status: "served", archived: true` | ✅ | a completed order kept in revenue |
| P04880 | A restart clears the table's calls and pending requests | read: `clearTableSignals` | ✅ | no ghost 🔔 |
| P04881 | A restart CLOSES the party rather than leaving an invisible open session | read | ✅ | owner, table 30, 2026-08-01 |
| P04882 | `sessions/:id/shift` is table-ops gated | read: `tableOpsTabletAllowed` + `tabletPerm` | ✅ | |
| P04883 | A refused shift is surfaced as a 4xx, not a 200 | read: `shiftErrMsg` + 409 | ✅ | else the optimistic move snaps back |
| P04884 | A shift from the tablet is logged | read: `log("table_shift", …)` | ✅ | twin-parity 2026-08-07 |
| P04885 | `sessions/:id/merge` validates the target table against table_count | read | ✅ | |
| P04886 | A merge records WHO joined the tables | read: `p_actor` / `p_actor_id` | ✅ | mig 308 |
| P04887 | A PIN-approved merge names the MANAGER who approved it | read: `pin?.actor` first | ✅ | that is the PIN pill's point |
| P04888 | `order-items/:id/move` is table-ops gated and target-validated | read | ✅ | |
| P04889 | `items/:id/status` 404s when the update matched no row | read | ✅ | sweep 2026-08-05 |
| P04890 | The order-level status stays coarse (never exposes 'ready') | read the rollup comment | ✅ | |
| P04891 | `orders/:id/serve-all` refuses a CANCELLED ticket | read: `if (cur.status === "cancelled") return 409` | ✅ | sweep 2026-08-04 |
| P04892 | `orders/:id/allergies` 404s when the order is gone | read: `if (!prev)` | ✅ | |
| P04893 | `orders/:id/allergies` stamps `edited_at` and the per-dish ＋/✎− marks | read | ✅ | identical to the manager's |
| P04894 | `items/:id/delete` confirms the dish is this restaurant's first | read: `gone` | ✅ | the RPC looks it up by id alone |
| P04895 | `items/:id/delete` writes a Removals row with the dish's own worth | read: `unit_price * qty` | ✅ | |
| P04896 | `items/:id/delete` stamps `edited_at` | read: `stampEdited` | ✅ | twin-parity 2026-08-07 |
| P04897 | `items/:id/qty` records a REDUCTION only | read: `if (qty < wasQty)` | ✅ | raising adds money |
| P04898 | `items/:id/note` confirms the dish is this restaurant's first | read: `ownNote` | ✅ | the note RPC takes no restaurant |
| P04899 | Annotating a SETTLED bill writes a `bill_annotated` row | read: `if (data?.settled)` | ✅ | owner, 2026-08-13 |
| P04900 | `items/:id/removed` refuses a ready/served dish | read: `if (item.status === "ready" \|\| "served") 409` | ✅ | |
| P04901 | `items/:id/removed` refuses a paid or cancelled order | read | ✅ | |
| P04902 | `orders/:id/add-item` confirms the order is this restaurant's first | read: `ownAdd` | ✅ | |
| P04903 | A waiter-added dish is pushed straight to preparing only if the parent is accepted | read: `parent.status !== "received"` | ✅ | |
| P04904 | `orders/:id/delete` refuses a PAID order | read: 409 | ✅ | |
| P04905 | `orders/:id/delete` is a SOFT delete | read: `softDeleteOrders` | ✅ | mig 188 |
| P04906 | `orders/:id/delete` writes a Removals row with the bill's worth | read | ✅ | 2026-08-03 |
| P04907 | `orders/:id/move` is table-ops gated and validates the target | read | ✅ | |
| P04908 | `tables/:t/pay-split` reads the SAME permission as a plain settle | read: `tabletPerm("tablet_mark_paid", …)` | ✅ | splitting is not a way round it |
| P04909 | `tables/:t/pay-split` delegates the arithmetic to `lib/paySplit` | read | ✅ | one money path |
| P04910 | `tables/:t/pay` resolves a merged child to the parent | read: `mergeParentTable` | ✅ | found live 2026-08-03 |
| P04911 | `tables/:t/pay` refuses while any order is still 'received' | read: 409 | ✅ | owner 2026-06-29 |
| P04912 | The update itself also excludes 'received' orders | read: `.neq("status","received")` | ✅ | belt and suspenders |
| P04913 | A settle that settled NOTHING answers 409, not `{ok:true,count:0}` | read | ✅ | T17 F2 |
| P04914 | The split branch delegates to the same shared helper | read: `settleBillInParts` | ✅ | the second implementation is gone |
| P04915 | The split branch keeps the stricter table-ops permission | read | ✅ | delegating loosened nothing |
| P04916 | `payment_method` is validated against `PAYMENT_METHODS` | read | ✅ | |
| P04917 | `tables/:t/customer-capture` books the visit onto the table's OPEN session | read: `capSess` | ✅ | mig 233 |
| P04918 | customer-capture never hands a database message to a waiter | read: `console.error` + a sentence | ✅ | T17 F10 |
| P04919 | customer-capture is gated by the `customers` entitlement | read: `getOwnerEntitlements` | ✅ | |
| P04920 | `tables/:t/tag` validates the tag against `isTableTag` | read | ✅ | |
| P04921 | `tables/:t/on-the-house` requires a comp mark on ANY member of the party | read: `partyKids` + `COMP_TAGS` | ✅ | |
| P04922 | on-the-house reads the settle permission, not just the tag ladder | read: `tabletPerm("tablet_mark_paid", …)` | ✅ | |
| P04923 | on-the-house refuses when nothing is unpaid | read: 409 | ✅ | |
| P04924 | on-the-house writes one Removals row per order | read the loop | ✅ | the largest money-lowering action |
| P04925 | `tables/:t/khata` refuses while food is still cooking | read: 409 | ✅ | |
| P04926 | khata never hands a database message to a waiter | read: the insert error branch | ✅ | T17 F10 |
| P04927 | khata closes the session through the shared `closeSession` | read | ✅ | |
| P04928 | `tables/:t/unpay` refuses outside the 30-minute grace window | read: `if (!paid.length) return 409` | ✅ | sweep C2 |
| P04929 | unpay clears the tip as well as the payment | read: `tip: 0` | ✅ | else the Z-report counts it twice |
| P04930 | unpay strips the 100% discount from an on-the-house settle | read: `onHouseIds` | ✅ | |
| P04931 | unpay REVERSES split legs, never deletes them | read: `reverseSplitLegs` | ✅ | mig 285 |
| P04932 | unpay reverses THIS party's customer visit | read: `p_session: openSess.id` | ✅ | mig 233 |
| P04933 | unpay writes a Removals row per order | read | ✅ | 2026-08-03 |
| P04934 | `sessions/:id/close` with `force` runs the close-unpaid gate | read | ✅ | |
| P04935 | `sessions/open` clears a still-pending "asked to open" request | read | ✅ | fixed 2026-07-07 |
| P04936 | `sessions/open` tolerates the concurrent-open race | read: `openTableSession` | ✅ | 2026-07-30 |
| P04937 | The tablet catch never leaks the internal message to a waiter's toast | read: `panelFailure(e, { unknown: … })` | ✅ | NB2 |
| P04938 | The tablet floor loads on the running app | GET :4110/api/tablet/summary with the tablet cookie | ✅ | watched running |
| P04939 | The waiter floor still passes its own suite | `node scripts/verify-tablet-floor.mjs --base http://127.0.0.1:4110 --read-only` | ✅ | 89 checks, 0 failed. NOTE for the next sweep: `npm run verify:tablet` defaults to port **4000**, the owner's window — always pass `--base` |
| P04940 | `app/api/editor/[...path]/route.ts` gates every method with `requireRole(req,"manager")` | read GET/POST/PATCH/DELETE | ✅ | four entry points |
| P04941 | `editorScope` validates an owner's `?rid=` against their own portfolio | read: `enabledOwnedRestaurantIds` | ✅ | a forged rid is refused |
| P04942 | Non-owner staff stay pinned to their own restaurant | read: `panelRestaurantId` | ✅ | |
| P04943 | `managerCan` checks the FEATURE half before any override | read: `access_config[flag].on === false` | ✅ | owner, 2026-08-01 |
| P04944 | An owner's menu editing cascades from the ADMIN rung | read: the `flag !== "edit_menu"` branch | ✅ | T19 — the cascade was lost and is back |
| P04945 | That cascade fails OPEN on a read error | read: `!== false` | ✅ | a blip must not lock an owner out mid-service |
| P04946 | An absent manager grant means the row's DEFAULT, not "no" | read: `managerGrantValue` | ✅ | |
| P04947 | `canViewLogs` is non-breaking: only an explicit false removes it | read | ✅ | |
| P04948 | `logPartAllowed` treats an absent sub-option as ON | read: `opts[part] !== false` | ✅ | |
| P04949 | `canDeleteBill` returns true ONLY for the Aevidine admin console | read: `return !g.user` | ✅ | owner, 2026-08-16 |
| P04950 | Nothing in this file resurrects a per-manager delete-bill resolution | grep for `delete_bill` → comments only | ✅ | compliance |
| P04951 | `tabGate` matches the menu EDITOR, never a live order's dish action | read: `ORDER_ITEM_ACTION` exclusion | ✅ | fixed 2026-08-02, Aangan Garden |
| P04952 | `tabGate` checks BOTH the tab list and the power behind it | read: `managerTabOn(cfg, hit.tab) && granted` | ✅ | verify:access, 2026-08-01 |
| P04953 | POST /audit is deliberately NOT behind the log tab | read: `method === "GET"` in the log matcher | ✅ | the compliance record must land |
| P04954 | `resolveMenuParts` treats an unstored key as the row's default | read: `MENU_PART_DEFAULTS` | ✅ | sweep T6 — 7 of 9 restaurants were wrong |
| P04955 | `edit_3d` defaults off for a manager and is not hard-wired admin-only | read | ✅ | the switch decides |
| P04956 | `menuPartsFor` resolves all nine in ONE read | read | ✅ | a dish save asks about five |
| P04957 | `tableOpsGate` runs the module rung then the manager grant | read | ✅ | |
| P04958 | `tableOpsGate` lets the admin super-user through | read: `if (!g.user) return null` | ✅ | X-ray honesty |
| P04959 | `discountBaseOf` matches the SQL and billMath, both cases | read the two branches against mig 270/271 | ✅ | three copies of a money rule |
| P04960 | `billTaxOf` taxes each order at its OWN rate | read: `BILLDOC.orderTaxRate(o, settingsRate)` | ✅ | T7 F8 — a banquet shares a bill |
| P04961 | `billTaxOf` rounds ONCE PER RATE, never per order | read the bucket loop | ✅ | ±½ paise an order drifts |
| P04962 | `dishPhotoUpload` is gated by edit_menu AND the edit_dish sub-part | read | ✅ | no new Access row |
| P04963 | `dishPhotoUpload` refuses SVG | read: `PHOTO_EXT` has no `image/svg+xml` | ✅ | an SVG can carry a script |
| P04964 | `dishPhotoUpload` caps at 4 MB and says WHICH formats | read | ✅ | |
| P04965 | `dishPhotoUpload` only removes a `replaces` inside OUR bucket and THIS restaurant's folder | read: `mine.startsWith(\`${rid}/\`) && !mine.includes("..")` | ✅ | |
| P04966 | `dishPhotoUpload` never hands a storage message to a person at a file picker | read | ✅ | |
| P04967 | `dish-photo` is handled BEFORE `readBody` consumes the stream | read: `if (path[0] === "dish-photo")` above the try | ✅ | |
| P04968 | `invoiceLockedByOrder` / `ByItem` freeze a bill while a live invoice stands | read | ✅ | work-checker 2026-06-21 |
| P04969 | `GET /table-sections` needs the `table_assign` power | read | ✅ | |
| P04970 | `GET /table-sections` returns only the roster fields the editor needs | read the column list | ✅ | not phone numbers, not permissions |
| P04971 | `GET /customer-search` is prefix-anchored, ≤6 rows | read | ✅ | fired while typing |
| P04972 | `GET /whoami` folds the Feature half into `effectivePowers` | read: `hasFeature && granted` | ✅ | |
| P04973 | `GET /whoami` sends `menuSubTint` so an admin can see what a manager lacks | read | ✅ | owner, 2026-08-03 |
| P04974 | `GET /whoami` sends `discountCapPct` so the modal stops offering a refused number | read | ✅ | T7 I5 |
| P04975 | `GET /whoami`'s simulate branch answers exactly as a real manager | read: the `simulate ? "manager" : …` resolutions | ✅ | |
| P04976 | `GET /whoami` `canDeleteBill` is false for a simulated manager | read | ✅ | owner, 2026-08-16 |
| P04977 | `GET /banquet/bills` search goes through `safeSearch` | read | ✅ | T9 F15, applied 2026-08-16 |
| P04978 | `GET /banquet/bills` names its columns and caps at 100 | read | ✅ | |
| P04979 | `GET /khata` groups from ONE RPC shared with the owner panel | read: `lfh_khata_outstanding` | ✅ | the two panels cannot disagree |
| P04980 | `GET /onhouse` needs the dashboard power and the tag ladder | read | ✅ | |
| P04981 | `GET /onhouse` clamps `days` to 1..365 and caps rows at 1000 | read | ✅ | |
| P04982 | `GET /all` bounds its four reads | read: 5000 / 500 / 500 / single | ✅ | |
| P04983 | `GET /all` strips `platform_channels` from the settings row | read: `panelSafeSettings` | ✅ | T17 |
| P04984 | `GET /ratings` needs `view_ratings` and is bounded at 200 | read | ✅ | |
| P04985 | `GET /orders?bills=1` clamps to the Bills window from the Access screen | read: `billsReach` + `windowStartIso` | ✅ | owner, 2026-08-03 |
| P04986 | Every `?history=` search is clamped to the same window | read: `wantsWindow` | ✅ | no typed URL reaches further |
| P04987 | The plain live-floor read is deliberately NOT clamped | read the comment | ✅ | an open table's food must not vanish |
| P04988 | A soft-deleted bill leaves the manager panel entirely | read: `oq.is("deleted_at", null)` | ✅ | owner, 2026-08-04 |
| P04989 | The delete filter is NOT applied to /stats, /zreport or the GST report | grep those branches → no `deleted_at` | ✅ | hiding a sale from the RECORDS is the illegal one |
| P04990 | The invoice-number search matches the LAST run of digits | read: `/(\d+)(?!.*\d)/` | ✅ | a pasted INV/2025-26/000042 |
| P04991 | The date search builds its window in IST, not the server's zone | read: `+05:30` | ✅ | sweep 2026-08-04 |
| P04992 | The `?table=` floor slice returns only the party sitting there NOW | read: the `liveSess` / `since` logic | ✅ | owner report, 2026-07-30 & 07-31 |
| P04993 | A merged child's slice resolves to the PARENT's session | read: `mergedParent` | ✅ | mig 249 |
| P04994 | With sessions ON and no open party, only session-less rows are excluded correctly | read the `setRow?.sessions_enabled` branch | ✅ | |
| P04995 | The bills read names its columns (`BILLS_COLS`) | read | ✅ | including taxable_base / nontax_amount |
| P04996 | The live floor read keeps `select("*")` deliberately | read the comment | ✅ | RT_VOLATILE / boardSig need the full shape |
| P04997 | `GET /platform` refuses only when BOTH modules are off | read | ✅ | |
| P04998 | `GET /platform` names its columns and never selects `payload` | read: the `discount:payload->discount` JSON paths | ✅ | polled path |
| P04999 | `GET /zreport` pages past PostgREST's ~1000-row cap | read the paging loop | ✅ | owner 2026-07-06 |
| P05000 | `GET /zreport` advances by the ACTUAL page length | read: `from += page.length` | ✅ | a fixed +1000 would undercount |
</content>
</invoke>
