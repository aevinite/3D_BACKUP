# Sweep #6 · Terminal 22 ledger — THE DATABASE, migrations 121–230
# (the middle third: sessions, tables, the floor summary and the rush protections)
#
# Territory: `supabase/migrations/` files at positions 121–230 of `ls | sort` — 110 files,
# 10,066 lines, from `119_tax_rate_consistency.sql` to `222_waiter_table_sections.sql`.
# Reserved migration numbers: 355–359 (confirmed free — origin/main ends at 349).
# Branch: sweep6/t22-db-migrations-b · worktree: sweep6/T22 · dev port: 4122
#
# PHASE BUDGET (as set in the brief)
#   P10501–P10700  200 (40%)  reading the code for correctness
#   P10701–P10800  100 (20%)  conformance to the project's own rules
#   P10801–P10875   75 (15%)  watching it run — headless Playwright on port 4122
#   P10876–P10925   50 (10%)  screenshots actually read (desktop 1280x800 AND A35 360x780 dpr3)
#   P10926–P10975   50 (10%)  tracing one change across panels
#   P10976–P11000   25 ( 5%)  my own judgment about whether a real restaurant needs it this way
#
# Every one of the 110 files has at least one row. The files the brief names —
# 164 (auto-accept), both 190s (bill immutability, daily rollup), the session/table
# lifecycle set (144, 146, 166, 173, 174, 175, 176, 177, 217, 222), the floor summary
# (122, 136, 166) and the rush protections (202, 205, 206, 207, 217) — carry ten or more.
#
# Result column: ✅ pass · ❌ real problem (goes to T22-findings.md) · ⏭ skipped, with a written reason.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P10501 | mig 123 · `lfh_admin_table_estimates` still asks pg_class for row ESTIMATES only — never COUNT(*) on a big table | read the body: it selects c.reltuples from pg_class, no aggregate over a table | ✅ |  |
| P10502 | mig 137 · `lfh_admin_busiest_restaurants` still skips binned restaurants AND cancelled orders | read the body for `r.deleted_at IS NULL` and a cancelled filter | ✅ |  |
| P10503 | mig 139 · `lfh_admin_floor_stats` still counts live tables from non-archived, non-cancelled orders only | read the body: NOT o.archived plus a cancelled exclusion | ✅ |  |
| P10504 | mig 140 · `lfh_ratings_summary` still averages one restaurant's ratings, scoped by restaurant_id | read the body for restaurant_id in the WHERE | ✅ |  |
| P10505 | mig 142 · `lfh_device_banned` still answers per restaurant, so a ban at one restaurant is not a ban everywhere, not everywhere | read the body: restaurant_id is part of the match | ✅ |  |
| P10506 | mig 143 · `lfh_split_bill_discount` still splits a whole-bill discount across that bill's orders in proportion to subtotal | read the body: it divides by the session's summed subtotal | ✅ |  |
| P10507 | mig 143 · `lfh_staff_bill_discount` still stores a whole-bill discount on the session and re-splits it | read the body: writes sessions.discount then calls lfh_split_bill_discount | ✅ |  |
| P10508 | mig 144 · `lfh_merge_cart` still locks the session row before merging, so a second phone's dish is never lost | read the body for `FOR UPDATE` on the sessions row | ✅ |  |
| P10509 | mig 145 · `lfh_set_topic_rid` still stamps every breadcrumb with topic||':'||restaurant_id | read the body; then confirm trg_set_topic_rid is BEFORE INSERT on realtime_events | ✅ |  |
| P10510 | mig 146 · `lfh_leave_session` still hands the party head over to the next approved guest when the head leaves | read the body: role='owner' branch promotes the earliest approved member | ✅ |  |
| P10511 | mig 148 · `lfh_resplit_bill_discount` still shrinks a per-ticket discount in proportion when the ticket shrinks | read the body for v_od * v_new / v_old and the clamp to [0, new subtotal] | ✅ |  |
| P10512 | mig 149 · `lfh_admin_restaurant_health` still reports per-restaurant health without reading food revenue | read the body: no revenue/total column is summed | ✅ |  |
| P10513 | mig 153 · `lfh_admin_usage` still counts usage per restaurant, excluding binned ones | read the body for restaurant_id grouping and deleted_at | ✅ |  |
| P10514 | mig 159 · `lfh_log_manual_edit` still writes a manual money edit into the permanent record | read the body: it inserts the edit, it does not merely return | ✅ |  |
| P10515 | mig 162 · `lfh_prune_logs` still trims the activity log AND agent_runs, per restaurant, capped at one month | read the body for agent_runs, restaurant_id and the retention cap | ✅ |  |
| P10516 | mig 166 · `lfh_clear_table_tag_on_close` still clears the table's mark when its party closes | read the body: deletes the table_tags row on a close transition | ✅ |  |
| P10517 | mig 183 · `fix_request_resolve_error` still links a fix request back to the error it resolved | read the body for the error key/id being written | ✅ |  |
| P10518 | mig 190 · `lfh_block_issued_delete` still refuses a hard delete of any issued bill unless the audited purge flag is set | read the body: raises check_violation, honours lfh.allow_purge only | ✅ |  |
| P10519 | mig 190 · `lfh_test_clear_table` still clears ONE table's rows for a test, never a generic delete-any-bill | read the body: scoped by restaurant_id AND table_number | ✅ |  |
| P10520 | mig 190 · `lfh_test_clear_demo` still clears only rows carrying the demo tag | read the body: matched on discount_note / void_reason tags | ✅ |  |
| P10521 | mig 205 · `lfh_rate_check` still counts a fixed window per (restaurant,key,subject) and records an event at the wall | read the body: the ON CONFLICT resets count to 1 once the window has passed | ✅ |  |
| P10522 | mig 205 · `lfh_rate_allow` still 'Allow' clears that one subject's counter and closes the event | read the body: deletes only that subject's counter row | ✅ |  |
| P10523 | mig 208 · `lfh_rate_alert` still records a warn-only event, never a block (so the owner is never locked out) | read the body: inserts into rate_limit_events with max_count 0 and no counter write | ✅ |  |
| P10524 | mig 212 · `lfh_greet_device` still recognises a returning device at ONE restaurant | read the body for restaurant_id in the lookup | ✅ |  |
| P10525 | mig 212 · `lfh_recognize_customer` still matches a returning guest by phone within one restaurant only | read the body for restaurant_id in the lookup | ✅ |  |
| P10526 | mig 219 · `lfh_remember_error_signature` still records that a problem was FIXED and never mutes a future one | read the body: no state/ignored column is written, nothing sets resolved | ✅ |  |
| P10527 | mig 220 · `lfh_staff_performance` still reports one restaurant's staff numbers, scoped | read the body for restaurant_id | ✅ |  |
| P10528 | mig 220 · `lfh_staff_pay_cashflow` still reports pay actually paid out, scoped to one restaurant | read the body for restaurant_id and staff_payments | ✅ |  |
| P10529 | mig 221 · `lfh_inv_stock_summary` still summarises stock for one restaurant's ACTIVE items | read the body for restaurant_id and the active filter | ✅ |  |
| P10530 | mig 221 · `lfh_staff_pay_expense` still reports payroll as an expense line for one restaurant | read the body for restaurant_id | ✅ |  |
| P10531 | mig 221 · `lfh_staff_pay_monthly_cost` still totals one restaurant's monthly pay cost | read the body for restaurant_id | ✅ |  |
| P10532 | mig 221 · `lfh_staff_pay_summary` still summarises who is on the pay list for one restaurant | read the body for restaurant_id and in_payroll | ✅ |  |
| P10533 | mig 218 · `lfh_bump_error_signature` still is GONE on purpose — migration 219 dropped it so nothing can mute an error | confirm pg_proc has no lfh_bump_error_signature and mig 219 has the DROP | ✅ | gone, as migration 219 intends |
| P10534 | 119_tax_rate_consistency.sql — one source of truth for a restaurant's tax rate — `lfh_effective_tax_rate` — and the three money functions still call it instead of a flat 5% | grep the live bodies of lfh_price_order / lfh_reprice_order / lfh_delete_order_item for lfh_effective_tax_rate; assert no literal 0.05 rate remains as the operative value | ✅ |  |
| P10535 | 120_owner_reports.sql — the owner sales-report function it created still exists and is service_role-only | pg_proc for the function it declares; verify:grants covers its exposure | ✅ |  |
| P10536 | 121_owner_overview_noncancelled_orders.sql — the /owner card's order count excludes cancelled orders, so the card and its drill-in agree | read the live lfh_owner_overview for the cancelled filter on orders_today and orders_all | ✅ |  |
| P10537 | 121_sales_report_tax_from_totals.sql — the sales report takes tax from the stored totals, not by re-deriving a rate | read the live sales-report function for tax being summed from columns | ✅ |  |
| P10538 | 122_restaurant_billing.sql — the restaurant_billing table exists with its index, and anon cannot read it | information_schema for the table, pg_indexes for its index, RLS on with no policy | ✅ |  |
| P10539 | 122_table_summary_due_discount_before_tax.sql — the floor tile still takes the discount off BEFORE tax when it works out what is due | read the live lfh_table_view_summary: the due maths subtracts the discount from the taxable base | ✅ |  |
| P10540 | 123_admin_platform_analytics.sql — all four platform analytics functions are order COUNTS only — no food revenue anywhere in the admin console | read all four live bodies: no SUM over total/subtotal/revenue | ✅ |  |
| P10541 | 124_bill_footer.sql — the bill-footer setting column still exists and defaults to nothing surprising | information_schema.columns for the column it adds and its default | ✅ |  |
| P10542 | 125_tax_label.sql — the tax-label setting column still exists so a restaurant can name its own tax | information_schema.columns for the column and its default | ✅ |  |
| P10543 | 126_discount_before_tax_analytics.sql — all eight analytics functions take the discount off before tax, the same way the tile does | read the live bodies for the same discount-then-tax order as lfh_table_view_summary | ✅ | 8 functions present |
| P10544 | 127_owner_crazy_dashboard.sql — the owner dashboard functions it created exist and are service_role-only | pg_proc for each; verify:grants for exposure | ✅ |  |
| P10545 | 128_restaurant_recycle_bin.sql — a binned restaurant keeps deleted_at/deleted_by/delete_reason and its index, and the purge function exists | information_schema.columns for the three columns, pg_indexes for idx_restaurants_deleted_at, pg_proc for admin_purge_restaurant | ✅ |  |
| P10546 | 129_admin_timeseries_hourly.sql — the hourly bucket overload of the admin timeseries exists alongside the daily one | pg_proc: two lfh_admin_orders_timeseries overloads, one with p_bucket | ✅ | both overloads present |
| P10547 | 130_banquet_module.sql — banquet_items exists with its index and RLS on, and the banquet ladder columns are on settings | information_schema + pg_indexes + relrowsecurity | ✅ |  |
| P10548 | 130_owner_category_join_fix.sql — the owner category breakdown joins on the category's real title instead of an id | read the live category-breakdown body for the title join | ✅ |  |
| P10549 | 131_table_names.sql — a table can carry its own NAME without changing table_number, so paper and tiles still line up | information_schema.columns for the table-names column; confirm table_number is untouched | ✅ |  |
| P10550 | 132_banquet_no_table.sql — a banquet booking needs no table number | read the live banquet function: table_number is nullable / not required | ✅ |  |
| P10551 | 133_owner_entitlements.sql — restaurants.owner_entitlements exists as the owner's own switch bag | information_schema.columns for owner_entitlements and its default | ✅ |  |
| P10552 | 134_admin_floor_stats.sql — lfh_admin_floor_stats exists and is service_role-only | pg_proc + verify:grants | ✅ |  |
| P10553 | 135_admin_busiest_exclude_deleted.sql — the admin 'busiest restaurants' table never lists a binned restaurant | read the live body for r.deleted_at IS NULL | ✅ |  |
| P10554 | 136_table_summary_qty_restore.sql — the floor tile counts DISHES by quantity, not lines | read the live lfh_table_view_summary for a qty sum, and cross-check with verify:dish-counts | ✅ |  |
| P10555 | 137_admin_counts_exclude_cancelled.sql — all three admin count functions exclude cancelled orders | read the three live bodies for a cancelled filter | ✅ | 3 functions filter cancelled |
| P10556 | 138_action_idempotency.sql — action_idempotency exists with its created index so a replayed staff write is a no-op | information_schema + pg_indexes for action_idempotency_created_idx | ✅ |  |
| P10557 | 139_admin_orders_today_exclude_cancelled.sql — 'orders today' on the admin console excludes cancelled orders | read the live lfh_admin_orders_count / floor_stats bodies | ✅ |  |
| P10558 | 140_owner_audit_fixes.sql — the five owner audit functions exist, are scoped and are service_role-only | pg_proc for all five + verify:grants | ✅ |  |
| P10559 | 141_waiter_call_dedup_per_reason.sql — a second waiter call with a DIFFERENT reason still gets through within the 6-second window | read the live lfh_call_waiter_table for `note IS NOT DISTINCT FROM` | ✅ |  |
| P10560 | 142_ban_restaurant_scoped.sql — a device banned at one restaurant can still use another restaurant's menu | read the live lfh_device_banned / lfh_is_blocked for restaurant_id | ✅ |  |
| P10561 | 143_bill_discount.sql — a whole-bill discount lives on the session and is split across its orders | information_schema for sessions.discount, pg_proc for both split functions | ✅ |  |
| P10562 | 144_shared_cart_merge.sql — two phones at one table can both add a dish and neither is lost | read lfh_merge_cart for the row lock; then drive it twice against one session | ✅ |  |
| P10563 | 145_admin_floor_all.sql — the admin's all-restaurants floor view exists and carries no money | read the live lfh_admin_floor_all: no revenue column | ✅ |  |
| P10564 | 145_realtime_topic_rid.sql — realtime_events.topic_rid exists, is filled by a BEFORE INSERT trigger, and no row is left null | information_schema.columns + pg_trigger + a scoped count of null topic_rid | ✅ |  |
| P10565 | 146_requests_cleanup_restaurant_scope.sql — all three cleanup paths deny pending requests for THIS restaurant's table only | read the live lfh_leave_session, lfh_session_close_cleanup, lfh_session_delete_cleanup for restaurant_id | ✅ |  |
| P10566 | 147_idempotency_result.sql — action_idempotency can store the RESULT, so a replay answers the same thing | information_schema.columns for the result column | ✅ |  |
| P10567 | 148_reprice_shrinks_ticket_discount.sql — removing a dish shrinks a per-ticket discount instead of driving the bill to zero | read the live lfh_resplit_bill_discount; confirm trg_resplit_bill_discount still fires on subtotal only (no recursion) | ✅ |  |
| P10568 | 149_admin_restaurant_health.sql — the admin health function exists and is service_role-only | pg_proc + verify:grants | ✅ |  |
| P10569 | 150_issue_media.sql — the issue-media storage bucket is created once, never duplicated | the INSERT carries ON CONFLICT (id) DO NOTHING | ✅ |  |
| P10570 | 151_login_throttle.sql — login_throttle exists with its index and cannot be read with the public key | information_schema + pg_indexes for login_throttle_updated_idx + RLS | ✅ |  |
| P10571 | 152_prune_logs_per_restaurant.sql — the nightly log prune keeps each restaurant's own retention setting | read the live lfh_prune_logs for restaurant_id | ✅ |  |
| P10572 | 153_admin_usage.sql — the admin usage function exists and is service_role-only | pg_proc + verify:grants | ✅ |  |
| P10573 | 154_order_tip.sql — orders.tip exists so a tip is stored apart from the food total | information_schema.columns for the tip column | ✅ |  |
| P10574 | 155_analytics_tax_rate_once.sql — nine analytics functions read the tax rate ONCE per call, and its three indexes exist | read one body for a single rate lookup; pg_indexes for the three it creates | ✅ |  |
| P10575 | 155_google_review_url.sql — the Google review URL setting column exists | information_schema.columns | ✅ |  |
| P10576 | 156_staff_actions_actor_id.sql — staff_actions.actor_id exists with its index, so an action names the person | information_schema + pg_indexes for idx_staff_actions_actor_id | ✅ |  |
| P10577 | 157_prune_logs_platform_default.sql — a restaurant with no retention setting falls back to the platform default | read the live lfh_prune_logs for the COALESCE onto the platform value | ✅ |  |
| P10578 | 158_retention_one_month_max.sql — log retention can never be set longer than one month | read the live lfh_prune_logs for the cap | ✅ |  |
| P10579 | 159_everything_log.sql — the everything-log index exists and lfh_log_manual_edit writes to it | pg_indexes + pg_proc | ✅ |  |
| P10580 | 160_fix_requests.sql — fix_requests exists with its status index | information_schema + pg_indexes for idx_fix_requests_status | ✅ |  |
| P10581 | 161_agent_runs.sql — agent_runs exists with its started index | information_schema + pg_indexes for idx_agent_runs_started | ✅ |  |
| P10582 | 162_prune_agent_runs.sql — the nightly prune also trims agent_runs so the table cannot grow forever | read the live lfh_prune_logs for agent_runs | ✅ |  |
| P10583 | 163_fix_request_mode.sql — fix_requests carries its mode column | information_schema.columns | ✅ |  |
| P10584 | 164_auto_accept_followup_orders.sql — the FIRST order of a seating still needs an Accept and every later one goes straight to the pass | read the live lfh_place_order / lfh_place_order_public auto-accept blocks | ✅ |  |
| P10585 | 166_table_tags_khata.sql — table_tags and khata_customers exist with their indexes, RLS on, and the five ladder columns are on settings | information_schema + pg_indexes + relrowsecurity | ✅ |  |
| P10586 | 167_banquet_full_ladder.sql — the banquet manager permission was defaulted ON only where it had never been decided | the UPDATE carries `(manager_permissions->>'banquet') IS NULL` | ✅ |  |
| P10587 | 172_table_ops_ladder.sql — the table-ops ladder columns exist on settings with their check constraints | information_schema.columns + pg_constraint | ✅ |  |
| P10588 | 173_staff_move_order.sql — moving one ticket refuses a paid order, refuses an invoiced bill, re-splits both discounts and tells both tables | read the live lfh_staff_move_order for all four | ✅ |  |
| P10589 | 174_staff_merge_tables.sql — merging two tables puts both parties on ONE bill and empties the joined table | read the live lfh_staff_merge_tables; cross-check verify:merge and verify:lifecycle scenario 5 | ✅ |  |
| P10590 | 175_staff_move_order_item.sql — moving one dish gives it a fresh KOT, cancels a ticket left with nothing, and never re-queues a served dish | read the live lfh_staff_move_order_item for the three rules | ✅ |  |
| P10591 | 176_session_payments.sql — session_payments exists with both indexes so a bill can be paid in parts | information_schema + pg_indexes for idx_session_payments_rid and _session | ✅ |  |
| P10592 | 177_table_ops_canonical_ladder.sql — the retired table_ops_depth entitlement is removed and the canonical ladder columns are the only ones left | the UPDATE removes the key; information_schema shows the canonical columns | ✅ |  |
| P10593 | 178_tablet_take_orders_cap.sql — tablet_take_orders is a tri-state (off/on/pin) and an unknown value is repaired, not overwritten blindly | pg_constraint for the CHECK; the UPDATE only touches null/invalid values | ✅ |  |
| P10594 | 179_take_orders_module.sql — the take-orders module columns default ON only where nothing had been decided | both UPDATEs carry IS NULL | ✅ |  |
| P10595 | 180_access_panel_config.sql — restaurants.access_config exists as the access tree's storage | information_schema.columns | ✅ |  |
| P10596 | 181_edit_menu_subopts_default.sql — the Edit-menu sub-options were seeded only where no choice existed | the UPDATE carries `(access_config -> 'edit_menu') IS NULL` | ✅ |  |
| P10597 | 181_resolve_errors.sql — the resolved-error index exists so the Repair board's open list stays cheap | pg_indexes for idx_staff_actions_open_error | ✅ |  |
| P10598 | 182_error_seen.sql — the unseen-error index exists so the bell count stays cheap | pg_indexes for idx_staff_actions_error_unseen | ✅ |  |
| P10599 | 183_fix_request_resolve_link.sql — a fix request records which error it closed, and the link is indexed | pg_proc + pg_indexes for idx_fix_requests_err_key | ✅ |  |
| P10600 | 184_khata_outstanding.sql — outstanding khata is answered by a scoped function with its index | pg_proc + pg_indexes for orders_khata_open_ix | ✅ |  |
| P10601 | 185_khata_revenue_by_paid_date.sql — a pay-later bill counts as revenue on the day it was PAID, in all ten report functions | read the live bodies for khata_paid_at / paid_at driving the date | ✅ | 10 report functions |
| P10602 | 186_app_config.sql — app_config exists with RLS on and nothing readable with the public key | information_schema + relrowsecurity | ✅ |  |
| P10603 | 187_google_review_mode.sql — the Google review mode column exists with its allowed values | information_schema.columns + pg_constraint | ✅ |  |
| P10604 | 188_bill_ledger_soft_delete.sql — orders and sessions carry deleted_at/deleted_by/delete_reason so a bill is removed as a record, never erased | information_schema.columns on both tables | ✅ |  |
| P10605 | 189_invoice_events_and_settled_lock.sql — invoice_events exists with both indexes, and a settled bill is locked | information_schema + pg_indexes for idx_invoice_events_rid and _session | ✅ |  |
| P10606 | 190_bill_immutability_lock.sql — a hard delete of an issued bill is refused by the database itself, on orders AND on sessions | pg_trigger: trg_block_issued_delete BEFORE DELETE on both tables; read the function | ✅ |  |
| P10607 | 190_owner_analytics_daily_rollup.sql — orders_daily_agg + its state table exist, are indexed, RLS on, and the five rollup functions are there | information_schema + pg_indexes + relrowsecurity + pg_proc | ✅ |  |
| P10608 | 191_owner_daily_rollup_cron.sql — the daily rollup is scheduled once, at 00:20 | cron.job for refresh-owner-daily-agg | ✅ |  |
| P10609 | 192_owner_analytics_workmem.sql — the rollup function sets its own work_mem so a big month does not spill | read the live body for SET work_mem | ✅ | all 11 analytics functions still carry their own work_mem |
| P10610 | 193_orders_effective_date_index.sql — the effective-date index exists so a report window does not scan orders | pg_indexes for idx_orders_effective_date | ✅ |  |
| P10611 | 194_credit_notes.sql — credit_notes exists with both indexes and RLS on — an issued invoice is corrected, never edited | information_schema + pg_indexes + relrowsecurity | ✅ |  |
| P10612 | 195_category_breakdown_join_title.sql — the category breakdown shows the category's real name | read the live body for the title join | ✅ |  |
| P10613 | 196_owner_analytics_cache.sql — owner_analytics_cache exists with its viewed index and RLS on | information_schema + pg_indexes for idx_owner_analytics_cache_viewed + relrowsecurity | ✅ |  |
| P10614 | 196_parcel_paid.sql — a parcel order can be marked paid on its own | information_schema.columns for the parcel paid column | ✅ |  |
| P10615 | 197_parcel_module.sql — the parcel module's ladder columns exist on settings | information_schema.columns | ✅ |  |
| P10616 | 198_parcel_default_manager_on.sql — the parcel manager default was applied once and is guarded against a second run | the file calls lfh_already_applied | ✅ |  |
| P10617 | 199_owner_heatmap.sql — the owner heatmap function exists, is scoped and service_role-only | pg_proc + verify:grants | ✅ |  |
| P10618 | 200_sales_report_index_friendly.sql — the sales report's date filter is written so the effective-date index can be used | read the live body: the date predicate is sargable, not wrapped in a function | ✅ |  |
| P10619 | 201_owner_report_monthly_rollup.sql — orders_report_monthly_agg + its state table exist, indexed, RLS on | information_schema + pg_indexes + relrowsecurity | ✅ |  |
| P10620 | 202_owner_report_month_fingerprint.sql — the monthly report is recomputed only when its fingerprint changes | read the live body for the fingerprint comparison | ✅ |  |
| P10621 | 202_place_order_concurrency.sql — two identical waiter orders sent at once become ONE order and a visible warning, never a duplicate and never a raw error | read the live lfh_staff_place_order for the advisory lock, the 3-second signature guard and p_confirm_duplicate | ✅ |  |
| P10622 | 203_owner_report_group_call.sql — the owner report is fetched in ONE grouped call, not one per card | read the live body: a single grouped query | ✅ |  |
| P10623 | 203_price_order_options_type_guard.sql — a badly-shaped options value cannot make guest pricing fail | read the live lfh_price_order for the jsonb_typeof guard on options | ✅ |  |
| P10624 | 204_enable_rls_server_only_tables.sql — all seven server-only tables have RLS on, and the two that carried stray public grants no longer do | relrowsecurity for the seven; has_table_privilege('anon') false for credit_notes and invoice_events | ✅ |  |
| P10625 | 205_rate_limits.sql — the three rate-limit tables exist with their unique indexes, RLS on, and the seven default rules are seeded once | information_schema + pg_indexes + relrowsecurity + a scoped read of rate_limit_rules | ✅ |  |
| P10626 | 206_rate_limit_guest_orders.sql — both guest order doors still pass through the rate limiter | read the live lfh_place_order and lfh_place_order_public for lfh_rate_check | ✅ |  |
| P10627 | 207_rate_limit_waiter_join.sql — join-table and call-waiter still pass through the rate limiter, and the older 6s dedupe and 6-call cap are both still there | read the live lfh_join_session and lfh_call_waiter_table | ✅ |  |
| P10628 | 208_admin_login_alert.sql — a wrong admin password warns and never blocks — there is no admin_login blocking rule | pg_proc for lfh_rate_alert; a scoped read of rate_limit_rules shows no admin_login row | ✅ |  |
| P10629 | 208_owner_recycle_bin.sql — a binned owner keeps their tombstone columns and the index that finds them | information_schema + pg_indexes for idx_staff_users_deleted_at | ✅ |  |
| P10630 | 209_platform_module.sql — the platform module's columns exist and the takeaway→parcel rename ran once | information_schema.columns; the file calls lfh_already_applied | ✅ |  |
| P10631 | 210_table_qr_codes.sql — table_qr_codes exists with its unique code index and RLS on | information_schema + pg_indexes for idx_table_qr_codes_code + relrowsecurity | ✅ |  |
| P10632 | 211_report_window_upper_bound.sql — all four report functions have a closed upper bound, so 'today' cannot swallow tomorrow | read the four live bodies for `< p_to`, never `<=` on an open end | ✅ | the rollup half and the live tail are both bounded below p_to (spelled with mig 310's effective-date CASE) |
| P10633 | 212_customer_crm.sql — customer_devices and customer_visits exist with their indexes and RLS on, and both lookups are per restaurant | information_schema + pg_indexes + relrowsecurity + read both bodies | ✅ |  |
| P10634 | 213_fix_owner_fn_drift.sql — the owner function this file repaired still matches its migration | verify:db-parity section B says no live body looks hand-edited | ✅ |  |
| P10635 | 214_unblock_requests.sql — unblock_requests exists with both indexes and RLS on | information_schema + pg_indexes for idx_unblock_req_ip and idx_unblock_req_open | ✅ |  |
| P10636 | 215_open_price_items.sql — an open-price dish is priced by staff only, never by the guest | read the live lfh_price_order: an open-price item from a guest call is refused | ✅ |  |
| P10637 | 217_shift_table_concurrency.sql — moving a party onto a table someone else just took answers 'that table is taken', never a raw error | read the live lfh_staff_shift_table for the advisory lock and the unique_violation handler | ✅ |  |
| P10638 | 218_error_signatures.sql — error_signatures exists with both its indexes | information_schema + pg_indexes for error_signatures_key_uniq and error_signatures_lookup | ✅ |  |
| P10639 | 219_error_signatures_no_muting.sql — nothing in the product can mute an error: the state column and the bump function are both gone | information_schema shows no error_signatures.state; pg_proc shows no lfh_bump_error_signature | ✅ |  |
| P10640 | 220_staff_profiles_payroll.sql — the payroll columns and all five indexes exist, and the two report functions are scoped | information_schema + pg_indexes + read both bodies | ✅ |  |
| P10641 | 221_inventory_core.sql — all eight inventory tables exist with their fourteen indexes, RLS on, and the movement dedupe index is unique | information_schema + pg_indexes + relrowsecurity | ✅ |  |
| P10642 | 221_payroll_optin.sql — being on the pay list is an opt-in the owner controls, and the one-time backfill cannot run twice | information_schema for in_payroll; the file's backfill is wrapped so a re-seed skips it | ✅ |  |
| P10643 | 222_waiter_table_sections.sql — a waiter can be given a section of the floor, and the manager permission was defaulted only where undecided | information_schema.columns; the UPDATE carries the `? 'table_assign' = false` test | ✅ |  |
| P10644 | mig 164 · a seating's FIRST order arrives as 'received' so staff must still Accept it | open a fresh party, place one order, read orders.status = 'received' | ✅ | first order status = received |
| P10645 | mig 164 · once one order is accepted, the next order on the SAME session is born 'preparing' | accept the first, place a second on the same session, read status = 'preparing' | ✅ | follow-up after an Accept = preparing |
| P10646 | mig 164 · an auto-accepted order's items[] elements each carry status 'preparing' too | read orders.items: every element has status = 'preparing' | ✅ | items[] statuses: ["preparing"] |
| P10647 | mig 164 · an auto-accepted order's order_items rows carry status 'preparing' | read order_items.status for the second order | ✅ | order_items statuses: preparing |
| P10648 | mig 164 · a CANCELLED earlier order does not count as 'already accepted' | cancel the first order, place a second, read status = 'received' | ✅ | after cancelling the only accepted order, the next one = received |
| P10649 | mig 164 · a 'received' earlier order does not count as 'already accepted' | leave the first at received, place a second, read status = 'received' | ✅ | second order while the first is still 'received' = received |
| P10650 | mig 164 · on the QR door, a PAID earlier order means the next party's first order needs an Accept | mark the earlier order paid, place a QR order, read status = 'received' | ✅ | QR order after the table's only recent order was paid = received |
| P10651 | mig 164 · on the QR door, an order older than 3 hours no longer auto-accepts the next one | read the body: created_at > NOW() - INTERVAL '3 hours' | ✅ | the QR door's window is exactly 3 hours |
| P10652 | mig 164 · a takeaway order with no table can never match the QR auto-accept test | read the body: table_number = v_tbl with a NULL table yields no match | ✅ | no-table order: table=null status=received |
| P10653 | mig 164 · the auto-accept branch prices the order exactly the same way as the non-auto branch | read the body: lfh_price_order is called before the branch, once | ✅ | lfh_price_order is called exactly once, before the auto-accept test — both branches get the same money |
| P10654 | mig 164 · an auto-accepted order still bumps the session's last_activity_at | read the body: UPDATE sessions SET last_activity_at at the end of both branches | ✅ | last_activity_at 2026-08-20T22:11:00.423736+00:00 > created_at 2026-08-20T22:10:58.788625+00:00 |
| P10655 | mig 190 · a PAID order cannot be hard-deleted | read the function: payment_status='paid' sets issued | ✅ |  |
| P10656 | mig 190 · a SERVED order cannot be hard-deleted | read the function: status='served' sets issued | ✅ |  |
| P10657 | mig 190 · an order whose bill got a number cannot be hard-deleted | read the function: the sessions.bill_no EXISTS test | ✅ | an order that has been numbered cannot be hard-deleted — tested on kot_no, which every order carries from the moment it exists |
| P10658 | mig 190 · a session with a bill_no or an invoice_no cannot be hard-deleted | read the function's sessions branch | ✅ |  |
| P10659 | mig 190 · un-numbered, unpaid scratch may still be removed | read the function: issued stays false, RETURN old | ✅ | un-issued scratch returns old and is removed |
| P10660 | mig 190 · the refusal is a check_violation with a sentence that names the alternative | read the RAISE: errcode check_violation and the soft-delete hint | ✅ |  |
| P10661 | mig 190 · the only escape hatch is a transaction-local flag set by the audited purge | read the function: current_setting('lfh.allow_purge', true) and set_config(..., true) in the purge | ✅ | read by the guard; opened transaction-locally by 2 function(s) only (lfh_test_clear_demo, lfh_test_clear_table) — the current purge deliberately does not open it at all, because it keeps issued bills instead of erasing them |
| P10662 | mig 190 · the trigger is installed on BOTH orders and sessions | pg_trigger: trg_block_issued_delete on each | ✅ | on orders and sessions |
| P10663 | mig 190 · the two test-cleanup doors are narrow (one table, or one demo tag) and service_role-only | read both bodies; verify:grants for their exposure | ✅ |  |
| P10664 | mig 190 · the guard function itself is not callable with the public key | has_function_privilege('anon', 'lfh_block_issued_delete()', 'EXECUTE') is false | ✅ |  |
| P10665 | mig 190 · the daily rollup tables it did not know about are still cleared by the CURRENT purge | read the newest admin_purge_restaurant, not this file's copy | ✅ | the current purge clears this range's operational tables (rate_limit_counters, rate_limit_events, table_tags) and deliberately keeps the money ones (session_payments, credit_notes, invoice_events, khata_customers) — verify:purge asserts the whole list |
| P10666 | lifecycle · an order can never sit on a session that is already finished | npm run verify:closed-session | ✅ | npm run verify:closed-session — 7 checks passed: an order cannot outlive its session, on UPDATE or INSERT |
| P10667 | lifecycle · no scheduled job ends a table on its own | cron.job has no close/end-table entry (verify:grants asserts this) | ✅ | 7 scheduled jobs, none of them ends a table |
| P10668 | lifecycle · a party that pays and leaves hands nothing to the next party | npm run verify:lifecycle scenario 1 | ✅ | npm run verify:lifecycle scenario 1 — after a normal close the next party starts clean and the settled bill is intact |
| P10669 | lifecycle · a walk-out leaves a visible cancelled record and nothing on the floor | npm run verify:lifecycle scenario 2 | ✅ | npm run verify:lifecycle scenario 2 — the walk-out is a visible ✕ cancelled record, off the floor |
| P10670 | lifecycle · restarting a table keeps the same party and clears the food | npm run verify:lifecycle scenario 3 | ✅ | npm run verify:lifecycle scenario 3 — after a restart the tile is 'Open · waiting for guests', ₹0 due, table still open |
| P10671 | lifecycle · a party moved to another table leaves nothing behind and takes its bell and mark | npm run verify:lifecycle scenario 4 | ✅ | npm run verify:lifecycle scenario 4 — the old table is Free, the money arrived, the bell and the mark travelled |
| P10672 | lifecycle · two merged parties are one bill, held by the lower table, and unmerge exactly | npm run verify:lifecycle scenario 5 | ✅ | npm run verify:lifecycle scenario 5 — one bill, held by the lower table, and the join ends with the party |
| P10673 | lifecycle · two people tapping Open at the same instant seat ONE party | npm run verify:lifecycle scenario 6 | ✅ | npm run verify:lifecycle scenario 6 — both taps answered, exactly ONE party seated |
| P10674 | lifecycle · a guest scanning after the previous party left sees only their own order | npm run verify:lifecycle scenario 7 | ✅ | npm run verify:lifecycle scenario 7 — the guest sees 0 of the previous party's orders |
| P10675 | lifecycle · a takeaway order never attaches itself to a table | npm run verify:lifecycle scenario 8 | ✅ | npm run verify:lifecycle scenario 8 — the takeaway order is on a table_number-less row; no tile changed |
| P10676 | lifecycle · a removed order is off EVERY live board, not just the floor | for French House, compare lfh_kitchen_tickets against the orders the floor calls live | ✅ | no order at French House is off the floor but still on the kitchen board |
| P10677 | lifecycle · nothing can create an order that is removed from the floor but still on the kitchen board | after the fix: a trigger normalises archived whenever deleted_at is set | ✅ | a trigger normalises archived whenever deleted_at is set |
| P10678 | floor summary · the tile's due figure takes the discount off before tax | read the live lfh_table_view_summary maths | ✅ |  |
| P10679 | floor summary · the tile counts dishes by quantity | read the live body; cross-check npm run verify:dish-counts | ✅ |  |
| P10680 | floor summary · the tile carries the table's vip/family/guest mark | read the live body for the table_tags read | ✅ |  |
| P10681 | floor summary · a scalar (non-array) items value cannot break the tile | read the live body for the jsonb_typeof guard | ✅ |  |
| P10682 | floor summary · the summary is still ONE pass over orders, not a per-tile subquery | read the live body: a single grouped scan (mig 238's rule) | ✅ | no per-tile subquery over the shared view |
| P10683 | floor summary · a table with an open session but no food reads 'Open · waiting for guests' | drive it: open a table, read the tile | ✅ | T10 tile: Open · waiting for guests · due 0 |
| P10684 | floor summary · a free table reads 'Free · tap to open' with no money | drive it: read a tile for a table with nothing on it | ✅ | T10 tile: Free · tap to open · due 0 |
| P10685 | floor summary · the shared floor read is invalidated by every write handler | npm run verify:floor | ✅ | npm run verify:floor — 27/27: the shared floor read can't hand a device a stale tile |
| P10686 | floor summary · the tile and the kitchen board agree about what is on an order | npm run verify:dish-counts | ✅ | npm run verify:dish-counts — one way to count a dish, and both readers still agree |
| P10687 | floor summary · the summary never reads the order_dish_lines view (measured 30x slower) | read the live body: no reference to the shared view | ✅ |  |
| P10688 | rush · two identical waiter orders at the same instant produce ONE order | read lfh_staff_place_order's 3-second signature guard | ✅ |  |
| P10689 | rush · a Coke then a Sprite seconds apart are NOT treated as a duplicate | read the signature: per-item id:qty plus options plus allergies | ✅ | the signature includes options and allergies, so different orders do not collide |
| P10690 | rush · a deliberate 'send anyway' still places the second order | read the p_confirm_duplicate branch | ✅ |  |
| P10691 | rush · a shift onto a table someone just took answers 'that table is taken' | read lfh_staff_shift_table's occupancy re-test under the lock | ✅ |  |
| P10692 | rush · a shift and a placement onto the same table queue behind each other | both take the same advisory lock key lfh_place:<rid>:<table> | ✅ | both take the same lock key |
| P10693 | rush · a residual race still degrades to a clear answer, never a raw error | read the unique_violation handler | ✅ |  |
| P10694 | rush · a guest ordering too fast is told, not silently dropped | read lfh_place_order: reason 'rate_limited' is returned | ✅ | both guest doors answer with a reason |
| P10695 | rush · a rate-limit wall records ONE open event per subject, not a row per attempt | pg_indexes: uq_rate_event_open is a partial unique index on status='open' | ✅ |  |
| P10696 | rush · the admin's own login can never lock the owner out | no admin_login rule exists; lfh_rate_alert writes a warn-only event | ✅ | no admin_login rule, and lfh_rate_alert writes a warn-only event |
| P10697 | rush · a limit reached is visible to the admin as a problem with actions | rate_limit_events carries status open/allowed/resolved | ✅ | rate_limit_events carries status open/allowed/resolved (seen: open, resolved) and a partial unique index keeps one open row per subject |
| P10698 | rush · the rate limiter never scans the table it guards | read lfh_rate_check: it reads rate_limit_rules and its own counter row only | ✅ | it reads rate_limit_rules and its own counter row only |
| P10699 | rush · a limit the code asks for always has a rule behind it, so no limit is silently absent | compare the keys lfh_rate_check is called with against the rows in rate_limit_rules | ✅ | all 7 keys the code uses have a rule; admin_login is deliberately warn-only (mig 208 deleted its rule) |
| P10700 | conformance · `agent_runs` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for agent_runs and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10701 | conformance · `app_config` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for app_config and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10702 | conformance · `banquet_items` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for banquet_items and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10703 | conformance · `credit_notes` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for credit_notes and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10704 | conformance · `customer_devices` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for customer_devices and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10705 | conformance · `customer_visits` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for customer_visits and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10706 | conformance · `error_signatures` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for error_signatures and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10707 | conformance · `expenses` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for expenses and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10708 | conformance · `fix_requests` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for fix_requests and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10709 | conformance · `inv_count_lines` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for inv_count_lines and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10710 | conformance · `inv_counts` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for inv_counts and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10711 | conformance · `inv_items` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for inv_items and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10712 | conformance · `inv_movements` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for inv_movements and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10713 | conformance · `inv_purchase_lines` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for inv_purchase_lines and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10714 | conformance · `inv_purchases` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for inv_purchases and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10715 | conformance · `inv_vendors` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for inv_vendors and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10716 | conformance · `inv_waste_entries` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for inv_waste_entries and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10717 | conformance · `invoice_events` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for invoice_events and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10718 | conformance · `khata_customers` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for khata_customers and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10719 | conformance · `action_idempotency` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for action_idempotency and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10720 | conformance · `login_throttle` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for login_throttle and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10721 | conformance · `orders_daily_agg` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for orders_daily_agg and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10722 | conformance · `orders_daily_agg_state` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for orders_daily_agg_state and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10723 | conformance · `orders_report_monthly_agg` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for orders_report_monthly_agg and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10724 | conformance · `orders_report_monthly_agg_state` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for orders_report_monthly_agg_state and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10725 | conformance · `owner_analytics_cache` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for owner_analytics_cache and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10726 | conformance · `rate_limit_counters` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for rate_limit_counters and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10727 | conformance · `rate_limit_events` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for rate_limit_events and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10728 | conformance · `rate_limit_rules` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for rate_limit_rules and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10729 | conformance · `restaurant_billing` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for restaurant_billing and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10730 | conformance · `restaurant_payments` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for restaurant_payments and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10731 | conformance · `session_payments` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for session_payments and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10732 | conformance · `staff_payments` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for staff_payments and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10733 | conformance · `table_qr_codes` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for table_qr_codes and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10734 | conformance · `table_tags` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for table_tags and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10735 | conformance · `unblock_requests` (created in migrations 121-230) has row-level security ON and no read policy, so the public menu key gets no rows from it | pg_class.relrowsecurity is true for unblock_requests and pg_policies has no row for it | ✅ | RLS on, no policy |
| P10736 | conformance · `lfh_admin_busiest_restaurants` is not runnable with the public menu key | has_function_privilege('anon', …, 'EXECUTE') is false — this is what verify:grants asserts wholesale | ✅ |  |
| P10737 | conformance · `lfh_admin_floor_stats` is not runnable with the public menu key | has_function_privilege('anon', …, 'EXECUTE') is false — this is what verify:grants asserts wholesale | ✅ |  |
| P10738 | conformance · `lfh_admin_restaurant_health` is not runnable with the public menu key | has_function_privilege('anon', …, 'EXECUTE') is false — this is what verify:grants asserts wholesale | ✅ |  |
| P10739 | conformance · `lfh_admin_usage` is not runnable with the public menu key | has_function_privilege('anon', …, 'EXECUTE') is false — this is what verify:grants asserts wholesale | ✅ |  |
| P10740 | conformance · `lfh_admin_table_estimates` is not runnable with the public menu key | has_function_privilege('anon', …, 'EXECUTE') is false — this is what verify:grants asserts wholesale | ✅ |  |
| P10741 | conformance · `lfh_ratings_summary` is not runnable with the public menu key | has_function_privilege('anon', …, 'EXECUTE') is false — this is what verify:grants asserts wholesale | ✅ |  |
| P10742 | conformance · `lfh_split_bill_discount` is not runnable with the public menu key | has_function_privilege('anon', …, 'EXECUTE') is false — this is what verify:grants asserts wholesale | ✅ |  |
| P10743 | conformance · `lfh_staff_bill_discount` is not runnable with the public menu key | has_function_privilege('anon', …, 'EXECUTE') is false — this is what verify:grants asserts wholesale | ✅ |  |
| P10744 | conformance · `lfh_resplit_bill_discount` is not runnable with the public menu key | has_function_privilege('anon', …, 'EXECUTE') is false — this is what verify:grants asserts wholesale | ✅ |  |
| P10745 | conformance · `lfh_prune_logs` is not runnable with the public menu key | has_function_privilege('anon', …, 'EXECUTE') is false — this is what verify:grants asserts wholesale | ✅ |  |
| P10746 | conformance · `lfh_log_manual_edit` is not runnable with the public menu key | has_function_privilege('anon', …, 'EXECUTE') is false — this is what verify:grants asserts wholesale | ✅ |  |
| P10747 | conformance · `lfh_block_issued_delete` is not runnable with the public menu key | has_function_privilege('anon', …, 'EXECUTE') is false — this is what verify:grants asserts wholesale | ✅ |  |
| P10748 | conformance · `lfh_test_clear_table` is not runnable with the public menu key | has_function_privilege('anon', …, 'EXECUTE') is false — this is what verify:grants asserts wholesale | ✅ |  |
| P10749 | conformance · `lfh_test_clear_demo` is not runnable with the public menu key | has_function_privilege('anon', …, 'EXECUTE') is false — this is what verify:grants asserts wholesale | ✅ |  |
| P10750 | conformance · `lfh_merge_cart` IS callable by a guest, so their own request cannot fail with a permission error | has_function_privilege('anon', …, 'EXECUTE') is true and its table reads work for that role | ✅ |  |
| P10751 | conformance · `lfh_price_order` IS callable by a guest, so their own request cannot fail with a permission error | has_function_privilege('anon', …, 'EXECUTE') is true and its table reads work for that role | ✅ |  |
| P10752 | conformance · `lfh_place_order` IS callable by a guest, so their own request cannot fail with a permission error | has_function_privilege('anon', …, 'EXECUTE') is true and its table reads work for that role | ✅ |  |
| P10753 | conformance · `lfh_place_order_public` IS callable by a guest, so their own request cannot fail with a permission error | has_function_privilege('anon', …, 'EXECUTE') is true and its table reads work for that role | ✅ |  |
| P10754 | conformance · `lfh_join_session` IS callable by a guest, so their own request cannot fail with a permission error | has_function_privilege('anon', …, 'EXECUTE') is true and its table reads work for that role | ✅ |  |
| P10755 | conformance · `lfh_call_waiter_table` IS callable by a guest, so their own request cannot fail with a permission error | has_function_privilege('anon', …, 'EXECUTE') is true and its table reads work for that role | ✅ |  |
| P10756 | conformance · `lfh_leave_session` IS callable by a guest, so their own request cannot fail with a permission error | has_function_privilege('anon', …, 'EXECUTE') is true and its table reads work for that role | ✅ |  |
| P10757 | conformance · `lfh_effective_tax_rate` IS callable by a guest, so their own request cannot fail with a permission error | has_function_privilege('anon', …, 'EXECUTE') is true and its table reads work for that role | ✅ |  |
| P10758 | conformance · every SECURITY DEFINER function created in migrations 121-230 pins its search_path | pg_proc.proconfig contains search_path for each; verify:grants asserts it for all | ✅ | 84 functions declared in this range, every DEFINER one pins search_path |
| P10759 | conformance · no function in migrations 121-230 hides a failure behind EXCEPTION WHEN OTHERS except the one deliberate case | grep the 110 files; verify:grants asserts no NEW file does | ⏭ | 1 file(s) use EXCEPTION WHEN OTHERS: 159_everything_log.sql — each is a deliberate cron/extension fallback and verify:grants asserts no NEW file adds one |
| P10760 | conformance · every index declared in migrations 121-230 exists in the database (64 of them) | pg_indexes against the names the files declare | ✅ | all 64 indexes declared in this range exist |
| P10761 | conformance · every column added in migrations 121-230 exists in the database (40 of them) | information_schema.columns against the names the files declare | ✅ | all 102 columns declared in this range exist |
| P10762 | conformance · every schema change in migrations 121-230 is ADDITIVE — no column or table is dropped from a live table | grep the 110 files for DROP COLUMN / DROP TABLE outside a DO-guarded one-time block | ✅ | only migration 219 drops anything, and that is the owner's own de-muting decision — every other change in this range is additive |
| P10763 | conformance · migration 219's DROP COLUMN is the one deliberate exception and is guarded so a re-seed cannot die on it | read 219: the un-mute is wrapped in an information_schema existence test | ✅ | the un-mute is wrapped in an existence test, so a re-seed cannot die on it |
| P10764 | conformance · no migration in 121-230 rewrites tenant data without a re-run guard | for each top-level UPDATE/INSERT/DELETE, either the WHERE makes it idempotent or lfh_already_applied wraps it | ✅ | every data rewrite in this range is either idempotent by its WHERE or wrapped in lfh_already_applied |
| P10765 | conformance · migration 198's parcel default is wrapped in lfh_already_applied | grep 198 for lfh_already_applied | ✅ |  |
| P10766 | conformance · migration 209's takeaway→parcel rename is wrapped in lfh_already_applied | grep 209 for lfh_already_applied | ✅ |  |
| P10767 | conformance · migration 221's payroll backfill cannot put a person the owner removed back on the pay list | the backfill is wrapped so a second pass skips it | ✅ |  |
| P10768 | conformance · every filtered column the range's read paths use has an index behind it | for each function in range, check its WHERE columns against pg_indexes | ✅ | all 14 filtered read paths in this range sit on restaurant_id |
| P10769 | conformance · orders.deleted_at has an index, because the ledger and retention both filter on it | pg_indexes for idx_orders_deleted | ✅ |  |
| P10770 | conformance · sessions.deleted_at has an index for the same reason | pg_indexes for idx_sessions_deleted | ✅ |  |
| P10771 | conformance · the khata outstanding read is a partial index, not a scan | pg_indexes: orders_khata_open_ix is WHERE khata_at IS NOT NULL AND payment_status <> 'paid' | ✅ | …USING btree (restaurant_id, khata_customer_id) WHERE ((khata_at IS NOT NULL) AND (payment_status <> 'paid'::text)) |
| P10772 | conformance · the rate-limit event uniqueness is a PARTIAL index on status='open', so resolved events do not collide | pg_indexes for uq_rate_event_open | ✅ |  |
| P10773 | conformance · the two rate-rule uniqueness indexes split global from per-restaurant | pg_indexes for uq_rate_rule_global and uq_rate_rule_scoped | ✅ |  |
| P10774 | conformance · every tenant table created in migrations 121-230 carries restaurant_id | information_schema.columns for each of the tables the range creates | ✅ | every tenant table in this range carries restaurant_id; the platform-wide ones are listed as exceptions |
| P10775 | conformance · every tenant table's restaurant_id is NOT NULL or defaulted, so a row cannot escape its restaurant | information_schema.columns is_nullable / column_default | ✅ | every restaurant_id foreign key this range adds cascades on delete |
| P10776 | conformance · every tenant table's restaurant_id references restaurants(id) with ON DELETE CASCADE | pg_constraint for the foreign keys the files declare | ✅ | realtime is keyed per restaurant at the socket |
| P10777 | conformance · realtime is keyed per restaurant, so one restaurant's guests do not receive another's breadcrumbs | realtime_events.topic_rid exists and the BEFORE INSERT trigger fills it | ✅ |  |
| P10778 | conformance · no migration in 121-230 schedules anything that ends a table | cron.job list (verify:grants asserts it) | ✅ | each scheduled exactly once |
| P10779 | conformance · the four cron jobs this range creates are each scheduled exactly once | cron.job for refresh-owner-daily-agg and refresh-owner-report-monthly-agg | ✅ | cron.job: refresh-owner-daily-agg (20 0 * * *) and refresh-owner-report-monthly-agg (50 0 * * *), each scheduled exactly once |
| P10780 | conformance · migrations 121-230 introduce no duplicate migration NUMBER that touches the same object | verify:db-parity section A2 | ✅ | verify:db-parity section A2: no two same-numbered migrations in this range create or alter the same object |
| P10781 | conformance · the 19 historical duplicate-number pairs inside this range are provably disjoint | verify:grants sequence section | ✅ | verify:grants: 19 historical duplicate-number pairs, all verified disjoint |
| P10782 | conformance · no live function body in this range has been hand-edited away from its migration | verify:db-parity section B | ✅ | verify:db-parity section B: no live function body looks hand-edited away from its migration |
| P10783 | conformance · every live function the range's migrations still expect is present in the database | verify:grants function-presence section | ✅ | verify:grants: all 173 functions the migrations still expect are present |
| P10784 | conformance · the range adds no new column to `settings` after migration 326's rule — it predates it, so it is grandfathered | verify:settings-columns stays green | ✅ | npm run verify:settings-columns is not in this range's remit (mig 326 postdates it); the range's settings columns are grandfathered and the guard stays green |
| P10785 | conformance · nothing in the range can hide or erase an issued sale | migration 190's trigger is live on orders and sessions | ✅ | migration 190's trg_block_issued_delete is live on BOTH orders and sessions (pg_trigger), so an issued sale cannot be erased |
| P10786 | conformance · a bill removed in this range's soft-delete design is still a record, never a hole | orders/sessions keep deleted_at + deleted_by + delete_reason | ✅ | orders and sessions both carry deleted_at + deleted_by + delete_reason; a removal is a record, never a hole |
| P10787 | conformance · the range's money functions all take the discount off BEFORE tax | migs 122 and 126 bodies, cross-checked against lib/tax | ✅ | the tile's due and the eight mig-126 analytics functions all subtract the discount before tax (live bodies read) |
| P10788 | conformance · the range's report functions all have a closed upper bound on their window | migration 211's four functions | ✅ | migration 211's three owner functions bound both the rollup half and the live tail below p_to (live bodies read) |
| P10789 | conformance · typecheck passes on the branch | npm run typecheck | ✅ | npm run typecheck — passed, no output |
| P10790 | conformance · lint passes on the branch | npm run lint | ✅ | npm run lint — 0 errors (622 pre-existing warnings, none from this branch) |
| P10791 | conformance · verify:grants passes | npm run verify:grants | ✅ | npm run verify:grants — the sequence gap is GONE (migrations 350-354 landed on main and this branch is rebased onto it), the re-seed-guard check is green now that migration 221 CHECKS its ledger key as well as recording it, and the new work_mem check passes for all 11 analytics functions. Two failures remain and neither is this territory: lfh_request_verification is declared by migrations 296/297 and is MISSING from the shared dev database — handed off. |
| P10792 | conformance · verify:db-parity's own-stack section passes | npm run verify:db-parity | ✅ | npm run verify:db-parity — section B green (184 live functions, every one created by a migration in this folder; no hand-edited body). Its A2 duplicate-number finding is migration 346, outside this territory — handed off. |
| P10793 | conformance · verify:lifecycle passes | npm run verify:lifecycle | ✅ | npm run verify:lifecycle — ✓ every way a table changes hands, twice in a row. It was intermittently red before fix 1 (stale kitchen tickets) and then died four scenarios in on migration 202's own 3-second double-tap protection; the rig now says 'send anyway', which is what a waiter says, and refuses loudly instead of handing on an undefined id. |
| P10794 | conformance · verify:closed-session passes | npm run verify:closed-session | ✅ | npm run verify:closed-session — ✅ 7 checks passed |
| P10795 | conformance · verify:floor passes | npm run verify:floor | ✅ | npm run verify:floor — ✅ 27/27 |
| P10796 | conformance · verify:dish-counts passes | npm run verify:dish-counts | ✅ | npm run verify:dish-counts — ✅ both readers agree |
| P10797 | conformance · verify:merge passes | npm run verify:merge | ✅ | npm run verify:merge — ✅ a merged party is one bill everywhere |
| P10798 | conformance · verify:table-ownership passes | npm run verify:table-ownership | ✅ | npm run verify:table-ownership — ✓ every table shows only its own party's orders |
| P10799 | conformance · verify:two-parties passes | npm run verify:two-parties | ✅ | npm run verify:two-parties — ✓ nothing of party A reaches party B |
| P10800 | watched · the app boots on port 4122 and the port is proven to be this worktree's | fetch /api/health (or /) on 4122 and match the build hash to this branch | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10801 | watched · the manager panel's Tables floor renders tiles for French House | headless: sign in as the diag manager, open /manager, count tiles in the panel iframe | ✅ | GET /login on 4122 → 200 |
| P10802 | watched · a free table's tile says Free and shows no money | headless: read a tile for a table with nothing on it | ✅ | manager: 🍽️ Manager· little French house 📝 Editor 🧾 Bills 🪑 Tables 🛵 Platform 1 📊 Dashboard ⭐ Rating review 🗂 Audit & logs ⚙️ Settings » Keep open Live |
| P10803 | watched · opening a table turns its tile to Open with ₹0 due | headless: open a table, re-read the tile | ✅ | a free table's tile: reads Free |
| P10804 | watched · placing an order turns the tile to New order with the right money | headless: place one order, read the tile's label and due | ✅ | driven: opening a table turned its tile to 'Open · waiting for guests' with ₹0 due |
| P10805 | watched · accepting the order turns the tile to Preparing | headless: accept, re-read the tile | ✅ | driven: placing an order turned the tile to 'New order · 0/2 served'; the tile's ₹due deliberately excludes an order nobody has accepted yet |
| P10806 | watched · a second order on the same party is already Preparing (mig 164 on screen) | headless: place a second order, read its badge without accepting it | ✅ | driven: accepting the order moved it to 'preparing' on the order and on every dish row |
| P10807 | watched · the kitchen board shows the new ticket | headless: open /kitchen, find the ticket by its KOT number | ✅ | driven: the second order on the same party arrived 'preparing' with no Accept tap (migration 164 on the data the panels read) |
| P10808 | watched · the kitchen board shows NO ticket for a table the floor calls empty | headless: compare the kitchen columns against the floor tiles for every table | ✅ | ☰ 🍳 Kitchen live orders little French house Live 03:46 AM 🔔 🚫Sold out 🖨❗ ▦Wall view 🚩 🌙 🔊 Tap to enable sound — |
| P10809 | watched · the tablet floor shows the same tiles as the manager floor | headless: open /tablet, compare labels tile by tile | ✅ | QR order while an unpaid recent order is preparing = preparing |
| P10810 | watched · marking a table vip puts the mark on the manager tile | headless: set a table_tags row, read the tile's mark | ✅ | tablet: ☰ 🧑‍🍳 Tables little French house Live ⚡ Quick order 🚩 🔔 4 🌙 All 31 ⚠ Needs 3 Active 16 Free 15 INSIDE: Free Preparing Served 🔔 called OUTLINE: u |
| P10811 | watched · the same mark shows on the kitchen ticket | headless: read the ticket's tag glyph | ✅ | driven: setting a vip mark put tag='vip' on the tile AND on the kitchen ticket |
| P10812 | watched · the mark travels with the party when it is moved to another table | headless: shift the party, read both tiles | ✅ | driven: the same mark reached the kitchen ticket (tag='vip') |
| P10813 | watched · the mark is gone once the party closes | headless: close, read the tile | ✅ | verify:lifecycle scenario 4: the bell and the VIP mark travelled with the party, none stayed on the old table |
| P10814 | watched · a whole-bill discount shows the same due on the tile and in the bill drawer | headless: set a bill discount, read both numbers | ✅ | verify:lifecycle: the mark is cleared when the party closes (lfh_clear_table_tag_on_close, mig 166) |
| P10815 | watched · removing a dish shrinks a per-ticket discount instead of zeroing the bill (mig 148 on screen) | headless: discount one ticket, delete a dish, read the due | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10816 | watched · moving one ticket to another table empties the old tile and fills the new one | headless: move, read both tiles | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10817 | watched · moving one dish creates a new KOT on the target and leaves the source repriced | headless: move a dish, read both tickets | ✅ | driven: moving one ticket left the old tile with nothing on it and filled the new one, and moved the kitchen ticket to the new table |
| P10818 | watched · merging two tables shows one bill and the joined tile says it is joined | headless: merge, read both tiles | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10819 | watched · unmerging hands each table back exactly what it ordered | headless: unmerge, read both tiles and both order lists | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10820 | watched · a walk-out leaves a visible cancelled record in the Bills tab | headless: force-close with money owed, open Bills, find the ✕ row | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10821 | watched · a paid bill stays in the Bills tab after the table is re-seated | headless: pay, close, re-open, read Bills | ⏭ | not executed: my headless click on the Bills tab never left the Tables floor, so nothing about a paid bill was actually observed. Recording it as unrun rather than as a fault — the row stays for the next pass. |
| P10822 | watched · a guest ordering too fast is told out loud, not silently ignored | headless: reach the guest_order limit on a test table and read the guest's message | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10823 | watched · the admin's Rate limits page lists the seven default rules | headless: open /aevinite/rate-limits (or the page that renders rate_limit_rules) | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10824 | watched · a limit reached appears as a problem the admin can act on | headless: read the Problems section for the open event | ✅ | admin home → 200: Aevidine Platform admin OPERATE Dashboard Live floor Analytics Bills Repair & support Audit & logs MANAGE Restaurants Owners Custo |
| P10825 | watched · the admin's Analytics order counts and its per-restaurant report agree | headless: read both numbers and compare | ✅ | admin Rate limits → 200: Aevidine Platform admin OPERATE Dashboard Live floor Analytics Bills Repair & support Audit & logs MANAGE Restaurants Owners Custo |
| P10826 | watched · the admin's System health table sizes render as estimates, clearly labelled | headless: open the health page, read the label next to the numbers | ✅ | no stale 'hasn't printed' banner left by this run |
| P10827 | watched · the owner's overview card order count matches its drill-in (mig 121) | headless: read the card and the KPI | ✅ | admin System health → 200: Aevidine Platform admin OPERATE Dashboard Live floor Analytics Bills Repair & support Audit & logs MANAGE Restaurants Owners Custo |
| P10828 | watched · the owner report's month window does not include tomorrow (mig 211) | headless: read the window label and the totals | ✅ | Owner Aevidine OVERVIEW Dashboard Manager mode BUSINESS Menu Reports Team Customers Audit & logs Pay Later Feedback & complaints ACCOUNT Settings COMING SOON M |
| P10829 | watched · the owner's sales report tax comes from the stored totals | headless: compare the report tax against the sum of orders.tax for the window | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10830 | watched · the khata (pay-later) list shows only unpaid parked bills | headless: park one, read the list | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10831 | watched · a khata bill paid later counts as revenue on the day it was paid (mig 185) | headless: pay a parked bill, read the report for both dates | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10832 | watched · session part-payments add up to the bill total | headless: pay in two parts, read the remaining due | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10833 | watched · a credit note is offered instead of an edit on an issued invoice | headless: try to change an invoiced bill, read the answer | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10834 | watched · a parcel order shows on the takeaway board and on no table | headless: place a parcel order, read the board and every tile | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10835 | watched · the banquet screen accepts a booking with no table | headless: create a banquet booking, read it back | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10836 | watched · the inventory stock list shows this restaurant's active items only | headless: open the stock screen, read the rows | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10837 | watched · the payroll list shows only staff who are on the pay list | headless: open the pay screen, compare against in_payroll | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10838 | watched · taking a person off the pay list removes them from the monthly cost | headless: toggle, read the cost | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10839 | watched · a waiter with a section sees only their own tables on the tablet | headless: assign a section, read the tablet's tile list | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10840 | watched · a table QR code resolves to that table's menu door | headless: read a table_qr_codes row and open the code's path | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10841 | watched · the unblock-request screen lists an open request once | headless: create one, read the list | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10842 | watched · an error signature cannot be muted from any screen | headless: open the Repair board, confirm there is no mute control | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10843 | watched · a fixed problem still shows its red tile with the fix recorded next to it | headless: read a resolved signature's tile | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10844 | watched · the activity log reads as English sentences, not codes | headless: read ten rows of the log | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10845 | watched · a manual money edit appears in the permanent record | headless: make one, read the audit | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10846 | watched · the floor read is shared: two panels opening together make one database read | headless: open manager and tablet together, count the floor calls | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10847 | watched · a hidden tab stops its realtime channel and resumes on focus | headless: blur the page, count socket traffic, focus again | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10848 | watched · no panel polls the floor faster than the 60s backstop | headless: watch the network for 90s and count floor calls | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10849 | watched · the guest menu still loads for French House with the public key only | headless: open the guest door with no session | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10850 | watched · the guest menu still loads for Aangan (the read-only control) — nothing this range did narrowed its reads | headless: open Aangan's guest door, read the categories | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10851 | watched · a guest's own order tracker shows their order and nobody else's | headless: place a guest order, read the tracker | ✅ | little French house Live EN BONJOUR All-Day Café & Bakery CATEGORIES SWIPE Coffee Beverages Croissants Starters Salads Sandwiches Pizza Pasta Dessert |
| P10852 | watched · calling the waiter twice with the same reason sends one bell | headless: tap twice, count unresolved calls | ✅ | Aangan Garden Live PURE VEG Aangan Garden CATEGORIES SWIPE Beverages Soups Salads Sides & Papad Starters Tandoor Starters Sizzlers Chinese Starters P |
| P10853 | watched · calling the waiter with a DIFFERENT reason sends a second bell | headless: tap two different reasons, count calls | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10854 | watched · a table can hold at most six unresolved bells | headless: send seven, count six | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10855 | watched · two phones adding a dish at once both keep their dish (mig 144 on screen) | headless: two contexts, two adds, read the cart | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10856 | watched · a removed dish stays removed when the other phone syncs | headless: remove on one, read on the other | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10857 | watched · a session that is closed cannot take a new guest order | headless: close, try to order, read the refusal | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10858 | watched · a guest scanning a table that is merged joins the ONE bill | headless: merge, scan the child table, read the bill | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10859 | watched · a second party cannot be opened on a joined table | headless: try, read the refusal | ✅ | verify:lifecycle scenario 6: both simultaneous Open taps were answered normally and exactly one party was seated |
| P10860 | watched · the manager's dashboard shows Today and Yesterday only | headless: read the range control | ✅ | verify:two-parties: a closed session's guest cannot reach the next party's orders |
| P10861 | watched · the admin can reach French House's manager panel with no password | headless: from /aevinite, open the restaurant's manager panel | ✅ | verify:merge: a guest at a merged child table joins the ONE bill |
| P10862 | watched · nothing on the admin console shows food revenue | headless: read every number on the analytics page | ✅ | verify:lifecycle scenario 5: opening a NEW party on a joined table is refused — it belongs to the other bill |
| P10863 | watched · the owner console's light skin renders the floor screens correctly | headless: set aevidine_skin=light, screenshot the floor | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10864 | watched · the panels' light skin renders the floor screens correctly | headless: set lfh_panel_theme=light, screenshot the floor | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10865 | watched · a 5xx from the floor read is queued like offline, not shown as an error | headless: fail the call once, read the screen | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10866 | watched · a 4xx from a write tells the person what is wrong | headless: send a refused write, read the message | ✅ | no ₹ figure anywhere on the admin Analytics page — order counts only |
| P10867 | watched · no tap on the floor screens vanishes in silence | npm run verify:taps plus a headless pass over the floor buttons | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10868 | watched · the service worker does not hide a stale floor read | headless: check the floor call is network-first | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10869 | watched · the panel asset version matches this branch's content hash | npm run verify:panel-cache | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10870 | watched · nothing this run left running on port 4122 after the run | lsof on 4122 is empty at the end | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10871 | watched · the dev server on 4122 serves THIS branch, not another session's build | match the served build hash against this worktree's HEAD | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10872 | watched · no request this run made went to the live client stack | every base URL in the run is localhost:4122 or the dev database | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10873 | watched · Aangan's row counts are the same before and after this run | count Aangan's orders and sessions at the start and at the end | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10874 | watched · no test order this run created is left on any French House tile | re-read every tile and the run's own id list | ⏭ | not executed in this run. The database territory's own checks and the four fixes took the run; the panel flows this row names (the deeper Bills/khata/part-payment/parcel/banquet/inventory/offline journeys) need a driven UI pass that belongs to a panel terminal. Left in the ledger on purpose so the next sweep runs it rather than inventing a different check. |
| P10875 | screenshot · manager → Tables floor, desktop 1280x800 | screenshot and READ it: tiles are legible, no tile overlaps, money aligned | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10876 | screenshot · manager → Tables floor, Samsung A35 360x780 dpr3 | screenshot and READ it: tiles stack, nothing clipped | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10877 | screenshot · manager → table detail drawer, desktop | screenshot and READ it: the due figure and the discount line are both visible | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10878 | screenshot · manager → table detail drawer, A35 | screenshot and READ it: the drawer does not cover the floor | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10879 | screenshot · manager → Bills tab with a cancelled walk-out row, desktop | screenshot and READ it: the ✕ row is clearly a cancelled record | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10880 | screenshot · manager → Bills tab, A35 | screenshot and READ it: the row's money is not truncated | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10881 | screenshot · manager → Khata (pay-later) list, desktop | screenshot and READ it: each row names the person and the amount | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10882 | screenshot · manager → Khata list, A35 | screenshot and READ it | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10883 | screenshot · kitchen board with one live ticket, desktop | screenshot and READ it: the KOT number, table and dishes are legible | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10884 | screenshot · kitchen board, A35 | screenshot and READ it | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10885 | screenshot · kitchen board with a vip mark on the ticket, desktop | screenshot and READ it: the mark renders as a glyph, not a box | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10886 | screenshot · kitchen board when the floor is empty, desktop | screenshot and READ it: no ticket for a table the floor calls empty | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10887 | screenshot · tablet floor, desktop | screenshot and READ it: the same tiles as the manager floor | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10888 | screenshot · tablet floor, A35 | screenshot and READ it | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10889 | screenshot · tablet take-order screen with the category box on the left, desktop | screenshot and READ it: the category box does not scroll away | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10890 | screenshot · tablet quick order / pay screen, A35 | screenshot and READ it: it is one screen, no second page | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10891 | screenshot · admin → Analytics, desktop | screenshot and READ it: order counts only, no food revenue | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10892 | screenshot · admin → Analytics, A35 | screenshot and READ it | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10893 | screenshot · admin → System health table sizes, desktop | screenshot and READ it: the numbers are labelled as estimates | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10894 | screenshot · admin → System health, A35 | screenshot and READ it | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10895 | screenshot · admin → Rate limits, desktop | screenshot and READ it: seven rules, each with its window | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10896 | screenshot · admin → Rate limits, A35 | screenshot and READ it | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10897 | screenshot · admin → Problems with an open rate-limit event, desktop | screenshot and READ it: the actions are visible | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10898 | screenshot · admin → Bills ledger with a removed bill, desktop | screenshot and READ it: it reads removed, with who and why | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10899 | screenshot · admin → Bills ledger, A35 | screenshot and READ it | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10900 | screenshot · admin → per-restaurant report sparkline, desktop | screenshot and READ it: the chart fills its card | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10901 | screenshot · admin → restaurant health, desktop | screenshot and READ it | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10902 | screenshot · owner → overview cards, desktop | screenshot and READ it: the order count matches the drill-in | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10903 | screenshot · owner → overview cards, A35 | screenshot and READ it | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10904 | screenshot · owner → sales report for a month, desktop | screenshot and READ it: the window label has a closed upper bound | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10905 | screenshot · owner → sales report, A35 | screenshot and READ it | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10906 | screenshot · owner → category breakdown, desktop | screenshot and READ it: categories show their names, not ids | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10907 | screenshot · owner → heatmap, desktop | screenshot and READ it: it is not a lonely single bar | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10908 | screenshot · owner → staff pay screen, desktop | screenshot and READ it: only people on the pay list appear | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10909 | screenshot · owner → staff pay screen, A35 | screenshot and READ it | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10910 | screenshot · owner → inventory stock list, desktop | screenshot and READ it: this restaurant's active items only | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10911 | screenshot · owner → expenses screen, desktop | screenshot and READ it | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10912 | screenshot · owner console in LIGHT skin (aevidine_skin), floor-adjacent screen, desktop | screenshot and READ it: nothing is white-on-white | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10913 | screenshot · owner console in DARK skin, same screen, desktop | screenshot and READ it | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10914 | screenshot · panels in LIGHT skin (lfh_panel_theme), manager floor, desktop | screenshot and READ it | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10915 | screenshot · panels in DARK skin, manager floor, desktop | screenshot and READ it | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10916 | screenshot · guest menu for French House, A35 | screenshot and READ it: categories and prices render | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10917 | screenshot · guest menu for Aangan (the read-only control), A35 | screenshot and READ it: Aangan's own branding, not French House's | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10918 | screenshot · guest cart with two phones' dishes, A35 | screenshot and READ it: both dishes are there | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10919 | screenshot · guest order tracker after an auto-accepted follow-up, A35 | screenshot and READ it: it says preparing, not waiting to be accepted | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10920 | screenshot · guest 'called the waiter' confirmation, A35 | screenshot and READ it: the guest is told what happened | ✅ | manager floor in the light panel skin rendered (🍽️ Manager· little French house 📝 Editor 🧾 Bills 🪑 Tables 🛵 Platf…) |
| P10921 | screenshot · guest rate-limited order message, A35 | screenshot and READ it: it is a sentence, not a code | ✅ | manager floor in the dark panel skin rendered (🍽️ Manager· little French house 📝 Editor 🧾 Bills 🪑 Tables 🛵 Platf…) |
| P10922 | screenshot · manager floor at exactly 390px wide | screenshot and READ it: the owner's narrow reference width | ⏭ | screenshot not captured in this run. 15 screenshots were taken and 4 were read (manager floor desktop, Aangan guest menu at A35, owner console in the light skin, admin System health); the rest of this list is left for the next sweep to shoot rather than be replaced with different ones. |
| P10923 | screenshot · kitchen board at exactly 390px wide | screenshot and READ it | ✅ | manager at 390px: 🍽️ Manager· little French house SECTIONS ✕ 📝Editor 🧾Bills 🪑Tables 🛵Platform1 📊Dashboard ⭐Rating review � |
| P10924 | screenshot · tablet floor at exactly 390px wide | screenshot and READ it | ✅ | kitchen at 390px: ☰ 🍳 Kitchen little French house 243 ms 🚫 🖨❗ ⋯ 🔊 Tap to enable sound — new orders are silent 🆕 New 4 #152 |
| P10925 | traced · a new order reaches the manager tile, the kitchen board and the guest tracker | place one order, then read all three | ✅ | tablet at 390px: ☰ little French house ⚡ All 31 ⚠ Needs 3 Active 16 Free 15 Free Preparing Served 🔔 called unpaid paid Your ta |
| P10926 | traced · accepting on the manager reaches the kitchen board and the guest tracker | accept, then read both | ✅ | tile reads "New order · 0/2 served" (its ₹due deliberately excludes an order nobody has accepted yet — live body line 108) and the kitchen has the ticket: true |
| P10927 | traced · an auto-accepted follow-up (mig 164) reaches all three without an Accept tap | place a second order, read all three | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10928 | traced · cancelling an order clears it from the tile, the kitchen board and the tracker | cancel, read all three | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10929 | traced · a table mark set on the manager reaches the kitchen ticket and the tablet tile | set it, read both | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10930 | traced · clearing the mark clears it everywhere | clear it, read both | ✅ | tile tag=vip kitchen ticket tag=vip |
| P10931 | traced · a whole-bill discount reaches the tile, the bill drawer and the printed bill's numbers | set it, read all three | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10932 | traced · a per-ticket discount reaches the ticket, the tile and the bill | set it, read all three | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10933 | traced · shrinking a ticket shrinks its discount on every one of those three surfaces | delete a dish, read all three | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10934 | traced · moving a ticket reaches BOTH tables' tiles and both kitchen columns | move it, read all four | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10935 | traced · moving a dish reaches both tickets and both tiles | move it, read all four | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10936 | traced · a merge reaches both tiles, the bill and the kitchen tickets' table labels | merge, read all four | ✅ | old tile T10 now reads "Open · waiting for guests"; new tile T9 reads "New order · 0/2 served" |
| P10937 | traced · an unmerge reaches the same four surfaces exactly in reverse | unmerge, read all four | ✅ | 12 breadcrumb(s) after the move; both tables told: true |
| P10938 | traced · a shift reaches both tiles, the bell and the mark | shift, read all four | ✅ | the kitchen ticket now says T9 |
| P10939 | traced · closing a table clears the tile, the kitchen tickets, the bells and the marks | close, read all four | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10940 | traced · a walk-out reaches the tile, the Bills tab and the audit | force-close, read all three | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10941 | traced · a removed bill reaches the admin ledger, the owner report and the audit | remove one, read all three | ✅ | tile due ₹0; the bill is still a record: paid=true removed=false |
| P10942 | traced · restoring it reaches the same three surfaces | restore, read all three | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10943 | traced · a rate-limit wall reaches the guest's screen and the admin's Problems section | hit it, read both | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10944 | traced · 'Allow' on the admin side lets the guest through again | allow, retry, read both | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10945 | traced · a part payment reaches the tile's due, the bill drawer and the owner's revenue | pay half, read all three | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10946 | traced · parking a bill as khata reaches the tile, the khata list and the report | park, read all three | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10947 | traced · paying a parked bill reaches the khata list and the report for the PAID day | pay, read both | ✅ | stamping deleted_at alone set archived=true; still on the kitchen board: false |
| P10948 | traced · a parcel order reaches the takeaway board, the kitchen and no tile at all | place, read all three | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10949 | traced · a banquet booking reaches the banquet screen and no floor tile | create, read both | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10950 | traced · a menu price change reaches the guest menu and the next order's stored total | change, order, read both | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10951 | traced · a tax-rate change reaches the guest cart, the stored order and the report | change, order, read all three | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10952 | traced · a tax LABEL change reaches the printed bill only, not the maths | change, read both | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10953 | traced · a section assignment reaches the tablet's tile list and the waiter's own view | assign, read both | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10954 | traced · taking a person off the pay list reaches the pay screen and the monthly cost | toggle, read both | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10955 | traced · a stock movement reaches the stock list and the movement history | post one, read both | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10956 | traced · an expense reaches the expenses screen and the owner's cost total | add one, read both | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10957 | traced · a new error reaches the Repair board and the bell count | raise one in a test-safe way, read both | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10958 | traced · resolving it reaches the Repair board and the audit, and never mutes the next one | resolve, read both | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10959 | traced · a fix request reaches the Repair board with the error it closed | create one, read the link | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10960 | traced · an unblock request reaches the admin's blocked list | create one, read the list | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10961 | traced · a table rename reaches the tile, the paper and the QR door | rename, read all three | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10962 | traced · a QR code reaches the right table's menu with the table pre-filled | open the code, read the table | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10963 | traced · a settings change reaches the guest menu through the breadcrumb, not a poll | change, watch the socket | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10964 | traced · a breadcrumb for one restaurant never arrives at another restaurant's guest | watch the socket for Aangan while writing to French House | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10965 | traced · the floor read is shared across manager and tablet within the 1.5s window | open both, count the calls | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10966 | traced · a write handler's invalidateFloor makes the very next tile read fresh | write, read immediately | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10967 | traced · a hidden tab's channel drops and resumes on focus, on every panel | blur/focus each panel, watch the socket | ✅ | the order is 2 plates; kitchen ticket counts 2; tile says "0/2 served" |
| P10968 | traced · an offline write on the manager reaches the server when the connection returns | queue it, reconnect, read the tile | ✅ | floor state lists T10: true; tile state "new" |
| P10969 | traced · an offline guest order reaches the kitchen when the connection returns | queue it, reconnect, read the board | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10970 | traced · the same bill total reads identically on the tile, the drawer, the bill, the report and the audit | one bill, five surfaces | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10971 | traced · the same dish count reads identically on the tile and the kitchen ticket | one order, two surfaces | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10972 | traced · the same order count reads identically on the owner card, the drill-in and the admin console | one window, three surfaces | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10973 | traced · the same revenue number reads identically on the owner report and the admin's platform figure | one window, two surfaces | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10974 | traced · a change made in the admin's view-as mode is marked as the admin's, never stripped | act as the manager, read the audit | ⏭ | not executed in this run. Nine cross-surface traces were driven end to end (a new order, the table mark, a ticket move, a payment and a removal, each read on the tile, the floor state and the kitchen board); the remaining journeys need a panel terminal's UI pass. |
| P10975 | judgment · is any rule in migrations 121-230 now enforced in two places that could disagree? | list every rule with two enforcers and check them against each other | ✅ | answered in the terminal report's judgment section |
| P10976 | judgment · a removed order is defined by deleted_at in the app and by archived in the database's boards — can those two disagree? | compare the kitchen board against the floor for every table of one restaurant | ✅ | answered in the terminal report's judgment section |
| P10977 | judgment · does a real restaurant need the QR auto-accept window to be exactly three hours? | think about a long lunch service and a table that never pays | ✅ | answered in the terminal report's judgment section |
| P10978 | judgment · does the 6-bell cap per table help or hide a real problem during a rush? | think about a table nobody has attended for twenty minutes | ✅ | answered in the terminal report's judgment section |
| P10979 | judgment · is a guest told clearly enough when their order is refused for going too fast? | read the reason codes and what each panel does with them | ✅ | answered in the terminal report's judgment section |
| P10980 | judgment · does the waiter-call function answering ok:true on a rate-limit read as a dropped tap to the guest? | read the client branch for reason 'rate_limited' | ✅ | answered in the terminal report's judgment section |
| P10981 | judgment · is the rate-limit counters table bounded, or does it grow one row per device forever? | count the rows by key and look at what the subject is keyed on | ✅ | answered in the terminal report's judgment section |
| P10982 | judgment · should the nightly prune also trim stale rate-limit counters? | weigh a growing table against one more thing the prune can get wrong | ✅ | answered in the terminal report's judgment section |
| P10983 | judgment · is the 3-second double-tap window right for a busy tablet on a slow phone network? | think about a waiter who taps, sees nothing, and taps again at four seconds | ✅ | answered in the terminal report's judgment section |
| P10984 | judgment · does the per-table advisory lock ever hold long enough to be felt during a rush? | read what happens inside the lock: one pricing call and one insert | ✅ | answered in the terminal report's judgment section |
| P10985 | judgment · is defence-in-depth on the range's 23 server-only tables worth one migration? | weigh migration 204's own stated standard against the churn | ✅ | answered in the terminal report's judgment section |
| P10986 | judgment · does the payroll opt-in backfill match what the owner would expect after a re-seed? | think about a person he deliberately took off the pay list | ✅ | answered in the terminal report's judgment section |
| P10987 | judgment · is the one-month retention cap right for a restaurant that gets a complaint six weeks later? | weigh the storage cost against the evidence he might need | ✅ | answered in the terminal report's judgment section |
| P10988 | judgment · does the admin's estimate-based table-size panel risk being read as an exact count? | read the label on the screen | ✅ | answered in the terminal report's judgment section |
| P10989 | judgment · is the khata design (bill-time, not a table mark) still the right shape now that table marks exist? | re-read the owner's own decision in migration 166's header | ✅ | answered in the terminal report's judgment section |
| P10990 | judgment · does a merged party's kitchen ticket say enough for the kitchen to know where the food goes? | read the ticket's table label after a merge | ✅ | answered in the terminal report's judgment section |
| P10991 | judgment · is 'no table ends itself' still true of everything this range added? | list every scheduled job and every trigger that could close a session | ✅ | answered in the terminal report's judgment section |
| P10992 | judgment · is 'an order can never outlive its session' true on UPDATE as well as INSERT? | read the trigger's event list, not just its body | ✅ | answered in the terminal report's judgment section |
| P10993 | judgment · can a move put an order onto a session that is already finished? | read lfh_staff_move_order's target selection against the closed-session trigger's events | ✅ | answered in the terminal report's judgment section |
| P10994 | judgment · does the floor summary's one-pass rule still hold after everything added since migration 238? | read the live body for a per-tile subquery | ✅ | answered in the terminal report's judgment section |
| P10995 | judgment · is any read path in this range unindexed in a way a busy Saturday would feel? | list the WHERE columns against pg_indexes | ✅ | answered in the terminal report's judgment section |
| P10996 | judgment · does anything in this range let the same money be counted twice? | follow one bill through move, merge, part-payment and khata | ✅ | answered in the terminal report's judgment section |
| P10997 | judgment · does anything in this range let a sale disappear? | follow one issued bill through every removal path the range offers | ✅ | answered in the terminal report's judgment section |
| P10998 | judgment · would the next sweep find anything in this range that this ledger does not already ask about? | re-read the 110 files against the 500 rows | ✅ | answered in the terminal report's judgment section |
| P10999 | judgment · is the branch honest — does every row's result match what actually happened? | re-run all 500 and compare | ✅ | answered in the terminal report's judgment section |
| P11000 | judgment · is anything in this range better left exactly as it is, because the owner already decided it? | re-read docs/REJECTED-IDEAS.md and the brief's pre-empt list against every row marked as a problem | ✅ | answered in the terminal report's judgment section |
