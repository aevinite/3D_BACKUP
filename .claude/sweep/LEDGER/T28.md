# SWEEP #6 — TERMINAL 28 LEDGER · THE REPO'S OWN TESTS
#
# Territory: `scripts/**`, `tests/**`, and the `verify:*` entries in `package.json`.
# Branch: `sweep6/t28-the-guards` · worktree `/Users/aevinite/Documents/Projects/sweep6/T28` · port 4128.
# IDs P13501–P14000 are permanent. Never renumber them. The next sweep RE-RUNS these rows first.
#
# THE BUDGET, as set in the prompt (and what each block is)
#
# | block | ids | share | what it is |
# |-------|-----|-------|------------|
# | A · reading the code for correctness | P13501–P13713 | 42.6% (213) | every one of the 213 files I own gets at least one row; the 128 guards get the "is your subject still there?" row |
# | B · conformance to the project's own rules | P13714–P13813 | 20% (100) | one sign-in per run · clean up by id · no real alerts · restore-on-crash · French House only |
# | C · watching it run | P13814–P13875 | 12.4% (62) | actually execute the guard on port 4128 and record the REAL exit code |
# | D · screenshots I actually Read | P13876–P13925 | 10% (50) | desktop 1280×800 and Samsung A35 360×780 dpr3, alternating |
# | E · tracing a change across panels | P13926–P13965 | 8% (40) | the same rule in editor / tablet / kitchen, and fixture collisions between guards |
# | F · my own judgment | P13966–P14000 | 7% (35) | does a real restaurant need it to work this way? |
#
# Result column: ✅ pass · ❌ real problem (fixed in this PR) · ⏭ skipped, with a written reason.
# Two passes were run over every row. This file records the SECOND pass.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P13501 | `verify-3d-viewer.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13502 | `verify-access-live.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13503 | `verify-access-model.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13504 | `verify-access-search.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13505 | `verify-admin-api-a.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13506 | `verify-admin-health-logs.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13507 | `verify-admin-money.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13508 | `verify-admin-refusals.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ❌→✅ item 10: the NOT_YET allowance said 2 database-words sites inside ensureCodes(); yesterday's H3 race fix added a third, legitimately. Red on a correct file. |
| P13509 | `verify-admin-restaurants.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13510 | `verify-allergy-isolation.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ❌→✅ item 19: printed 'Cleaned up test order.' and cleaned up nothing — a hard DELETE the database refuses (mig 190), unread. Left a live ALGTEST table, an open session and a queued ticket on the manager's floor. |
| P13511 | `verify-audit-coverage.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13512 | `verify-avlive-offline-complete.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ⏭ | reads the AV-live folder by design and is red whenever that stack is behind; excluded from CI on purpose. Read-only md5 compare, no keys printed. NOT TOUCHED. |
| P13513 | `verify-bill-reprint-is-silent.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13514 | `verify-billdoc-paper.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13515 | `verify-board-sig.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13516 | `verify-busy-server.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13517 | `verify-cache.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13518 | `verify-cancel-loss.ts` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13519 | `verify-cancel-made.ts` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13520 | `verify-cancelled-tile-parity.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ❌→✅ item 1: order_items inserts had no restaurant_id and the error was unread, so both sides of the comparison ran on ZERO dishes and its main check printed a ✓ over nothing. Teardown hard-DELETEd rows the DB refuses (mig 190), leaving a live '288 · Preparing' phantom tile on the manager's floor and blocking its own next run. |
| P13521 | `verify-clash-coverage.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13522 | `verify-closed-session-orders.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ✅ item 17: its teardown named the table but not the restaurant. The name is random per run so it could only match its own rows, but the shape is the one that reached another restaurant. |
| P13523 | `verify-css-tokens.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13524 | `verify-customer-erase.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13525 | `verify-customers.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ❌→✅ item 8: four checks red on two deliberate fixes — the @page size:80mm that was removed because a forced page size rotates the job, and the real `disabled` attribute dropped so a waiter's tap can't die in silence. |
| P13526 | `verify-db-grants.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ⏭ | RED, and not from this branch: `lfh_request_verification()` is missing from the shared dev DB because T21's unmerged migration 354 drops it. Guard is correct. |
| P13527 | `verify-db-parity.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ⏭ | RED, and not from this branch: duplicate migration 352 on main (T24) + a function on the dev DB in no migration. HANDOFF H4. |
| P13528 | `verify-dead-css.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13529 | `verify-deps.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13530 | `verify-dish-counts.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13531 | `verify-doc-pointers.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ⏭ | RED: docs/GUARD-MAP.md needs 5 rows (2 already missing on clean main, 3 mine). docs/** is T29's. HANDOFF H1. |
| P13532 | `verify-edge-cases.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ❌→✅ item 2: DEAD — ten page.goto calls in DOUBLE quotes handed Chrome the literal `${BASE}/menu`. 0 of 14 checks had run. Also: no restaurant_id on any write, an UNSCOPED teardown that measurably closed another restaurant's table-11 session, un-namespaced guest storage keys, a four-servers-era password POST, and three assertions describing deleted screens. Item 4: its make-head refusal check passed on 'No restaurant scope' — any 400 satisfied it. |
| P13533 | `verify-everything.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13534 | `verify-families.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ❌→✅ item 12: called localhost:4003, retired 2026-06-13 — nothing had run for over two months. Rewritten to the four properties nothing else holds; the rest was duplicated or describes paths the owner closed. |
| P13535 | `verify-fix-survives.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13536 | `verify-floor-offplan.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13537 | `verify-floor-per-row.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13538 | `verify-floor-share.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13539 | `verify-guest-doors.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13540 | `verify-guest-pass-private.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13541 | `verify-guest-read.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13542 | `verify-guest-recovery.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13543 | `verify-guest.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ❌→✅ item 9: P00155 asked for URLSearchParams/qs.append in the menu page after the building moved into queryStringOf() in lib/tenant.ts. Now asserts the behaviour. |
| P13544 | `verify-heatmap-parity.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ⏭ | refuses to run (exit 2) until `lfh_owner_heatmap_old` is staged beside the live function, and nothing creates it. Honest, but it can never assert — listed as 🟡 I5 for him. |
| P13545 | `verify-hidden-dishes.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13546 | `verify-i18n-scope.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13547 | `verify-loadall-preserves-orders.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13548 | `verify-manager-behaviour.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13549 | `verify-manager-gates.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13550 | `verify-manager-hidden.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13551 | `verify-manager-live-rush.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ❌→✅ item 18: once it could run to the end it reported SIXTEEN failures on a correct floor — a tile with dishes prints the served counter as text and carries the state phrase in the line's title. It reads both now. ALSO item 21: its teardown read EVERY non-closed session in each crew's restaurant and closed the lot, while printing 'closed the tables they opened'. On a real restaurant that ends every meal in the house (mig 232 cancels + archives every unpaid live order on a closed session). Now closes only the tables it touched, and dismisses their kitchen tickets. |
| P13552 | `verify-menu-parts-live.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13553 | `verify-menu-parts.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13554 | `verify-merge-keeps-mark.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13555 | `verify-merge-party.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13556 | `verify-merge-records-who.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13557 | `verify-merged-floor.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ✅ item 13: read the SHARED folder's .env.local by absolute path — a lane in its own worktree asserted against whatever stack that copy pointed at. FLAKY once under three concurrent processes; passed on re-run and in pass 2. ALSO item 22: it had NO teardown at all — seven live tables left on the floor after every run, header reading '7/30 OCCUPIED' on an empty restaurant, plus five 'hasn't printed' banners. Now clears on the way out and on a crash. |
| P13558 | `verify-no-ask-before-delete.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13559 | `verify-no-fatal-ui.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13560 | `verify-offline-retry.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13561 | `verify-offline.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | 55/55 against a real production build on port 4129. Against a dev server it refuses with exit 2 (item 14b) — dev serves JS no-cache, so every offline check would fail for an unrelated reason. |
| P13562 | `verify-one-number.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13563 | `verify-open-no-attend-flash.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13564 | `verify-open-request-guard.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13565 | `verify-order-retry.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13566 | `verify-order.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13567 | `verify-outbound-honesty.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13568 | `verify-outbox-drain.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13569 | `verify-owner-clash.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13570 | `verify-owner-home.ts` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13571 | `verify-owner-money-screens.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13572 | `verify-owner-panel.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13573 | `verify-owner-reports.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13574 | `verify-owner-scope-503.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13575 | `verify-owner-screen.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13576 | `verify-owner-territory-live.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ⏭ | 126/127. The one red (P06476, add a dish through the real editor) is T21's migration 353 applied to the shared dev DB without its panel change — POST /api/editor/items answers 500 'no rating column' and the panel says Saved ✓. See findings. |
| P13577 | `verify-owner-territory.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13578 | `verify-panel-api-guards.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13579 | `verify-panel-cache.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13580 | `verify-panel-plumbing-live.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13581 | `verify-panel-plumbing.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13582 | `verify-panel-scope.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13583 | `verify-panel-secrets.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13584 | `verify-panel-twins.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13585 | `verify-parcel-home.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13586 | `verify-pay-history-delete.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13587 | `verify-personal-data.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13588 | `verify-print-format.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13589 | `verify-print-helper.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13590 | `verify-print-queue.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13591 | `verify-purge-classified.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13592 | `verify-read-guards.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13593 | `verify-ready-tile-and-kitchen.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13594 | `verify-realtime.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ❌→✅ item 5: picked a dish and a category with .limit(1) and NO restaurant filter (could edit Aangan), flipped a category's `active` with the restore outside any finally, crashed on a session insert with no restaurant_id, and deleted activity rows by action name instead of id. |
| P13595 | `verify-recycle-bin.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13596 | `verify-recycle-name.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13597 | `verify-rejected-ideas.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13598 | `verify-retention-lock.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13599 | `verify-rota-clash.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13600 | `verify-scrollspy.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13601 | `verify-sections.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13602 | `verify-server-only-imports.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13603 | `verify-session-ux.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ❌→✅ item 3: DEAD — died at its first write (no restaurant_id), 3 literal templates, unscoped teardown on table 9, make-head with no ?rid=, and section 4 reaching the panel as a bare path. 0 of 11 checks had run; 11/11 now. |
| P13604 | `verify-settings-columns.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13605 | `verify-skin-ink.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13606 | `verify-slow-load.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ✅ passed; flaked ONCE on 'the Add button is not usable while the model loads' under three concurrent processes, and passed alone and in pass 2. Timing-sensitive by nature. |
| P13607 | `verify-split-payment.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13608 | `verify-staff-accounts.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13609 | `verify-static.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ⏭ | 31 of its 32 guards green; the one red is verify-doc-pointers (HANDOFF H1). verify:rejected was the other and is fixed (item 11). |
| P13610 | `verify-summary-parity.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ⏭ | refuses to run (exit 2) until a `lfh_table_view_summary_v2` candidate exists. That is how mig 238 was proved, so the refusal is the design. |
| P13611 | `verify-sweep-extras.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ❌→✅ item 23: phase 5465 only had something to click while ANOTHER guard's leftover was on the floor, and 5466-5468 hang off it, so four checks never ran. It seats its own party now. 18 pass, 0 fail. |
| P13612 | `verify-t24-money-rules.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ❌→✅ item 11: said 'REJECTED (owner, …)' without naming docs/REJECTED-IDEAS.md, so verify:rejected AND verify:static were red on clean main. |
| P13613 | `verify-table-lifecycle.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ❌→✅ item 18: tripped the app's own 3-second double-tap protection and died as `invalid input syntax for type uuid: "undefined"` six lines from the cause. Ran it three times in a row after the fix to prove it is no longer flaky. |
| P13614 | `verify-table-ownership.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ✅ passed; flaked ONCE on a click the panel's own nav intercepted mid-repaint (the 'a repainting board makes a real click miss' shape) and passed on re-run. |
| P13615 | `verify-tablet-floor.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13616 | `verify-tablet-parity.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ❌→✅ item 6: crashed before its first check (no restaurant_id on five tables) and had no try/finally, so a throw left a live 'preparing' order on table 9932 — a phantom table on the floor. |
| P13617 | `verify-tablet-taps.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13618 | `verify-tablet-wants-in.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13619 | `verify-tap-guard.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13620 | `verify-tax-mode-e2e.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13621 | `verify-tax-mode.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13622 | `verify-test-safety.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ✅ item 17: gained the check that would have caught five of the dead guards — every test write on a tenant table must name its restaurant. Proved on all three halves by re-introducing each real bug. |
| P13623 | `verify-two-parties.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ❌→✅ item 18: same double-tap trip; now sends p_confirm_duplicate and stops with the refusal's own words. |
| P13624 | `verify-ui-integrity.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13625 | `verify-void-on-joined-party.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ❌→✅ item 7: reported three failures on a cancel that worked. The removal sheet has asked TWO required questions since 2026-08-18; the guard answered one and clicked a disabled Remove. |
| P13626 | `verify-warm-shell.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13627 | `verify-write-paths.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ | ✅ item 13: same absolute-path .env.local read, fixed. Also flaked once under load and passed on re-run. |
| P13628 | `verify-xray-marks.mjs` still asserts a live property — every file/symbol/selector it looks for exists today, so it cannot pass vacuously | read the file; for each string/regex it greps for, grep the subject file for it; a miss = the subject moved | ✅ |  |
| P13629 | `scripts/agent-run-record.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13630 | `scripts/apply-migration-avlive.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13631 | `scripts/apply-migration-dev.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13632 | `scripts/apply-migration-prod.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13633 | `scripts/apply-migration.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13634 | `scripts/avlive-preflight.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13635 | `scripts/build-aangan-menu.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13636 | `scripts/build-vendor.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13637 | `scripts/check-folder-current.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13638 | `scripts/cleanup-stale-orders.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13639 | `scripts/compare-schemas.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13640 | `scripts/copy-demo-to-prod.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13641 | `scripts/create-branding-bucket.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13642 | `scripts/db-maintain.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13643 | `scripts/fetch-fix-requests.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13644 | `scripts/generate-cafe-menu.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13645 | `scripts/load-ramp-orders.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13646 | `scripts/local-db-validate.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13647 | `scripts/measure-load.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13648 | `scripts/mockup-server.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13649 | `scripts/read-timings.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13650 | `scripts/release-avlive.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13651 | `scripts/report-access-config-keys.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13652 | `scripts/reset-demo-history.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13653 | `scripts/reset-diag-password.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13654 | `scripts/reset-prod-owner-pw.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13655 | `scripts/resolve-fix-request.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13656 | `scripts/run-migration.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13657 | `scripts/seed-aangan.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13658 | `scripts/seed-clean-orders.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13659 | `scripts/seed-demo-orders.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13660 | `scripts/seed-demo-restaurants.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13661 | `scripts/seed-history-all-restaurants.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13662 | `scripts/seed-history-french-house.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13663 | `scripts/seed-owner-dev.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13664 | `scripts/seed-owner-inventory-demo.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13665 | `scripts/seed-supabase.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13666 | `scripts/seed-today.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13667 | `scripts/set-access-defaults.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13668 | `scripts/set-glb-cache.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13669 | `scripts/shot-access-help.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13670 | `scripts/shot-qr.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13671 | `scripts/slow-proxy.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13672 | `scripts/stress-max.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13673 | `scripts/stress-realtime.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13674 | `scripts/stress.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13675 | `scripts/sweep-site.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13676 | `scripts/t9-fixture-test.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13677 | `scripts/t9-leftovers-check.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13678 | `scripts/vercel-load.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13679 | `scripts/view-device.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13680 | `scripts/wait-vercel-ready.mjs` — its DB writes name `restaurant_id`, its deletes name the rows it made, and it points at the dev stack only | read the file; grep for `.eq("restaurant_id"`/`restaurant_id=`, for delete/update filters, and for any AV-live path or key | ✅ |  |
| P13681 | `scripts/live-fix-prompt.md` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13682 | `scripts/LOCAL-DB-NOTES.md` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13683 | `scripts/nightly-owner-audit.sh` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13684 | `scripts/nightly-repair.sh` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13685 | `scripts/nightly-tablet-audit.sh` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13686 | `scripts/owner-audit-prompt.md` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13687 | `scripts/repair-agent-prompt.md` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13688 | `scripts/tablet-audit-prompt.md` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13689 | `scripts/panel-stubs/oplog.mjs` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13690 | `scripts/panel-stubs/state.mjs` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13691 | `scripts/panel-stubs/userAuth.mjs` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13692 | `scripts/panel-stubs/sb.mjs` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13693 | `scripts/sweep/appUp.mjs` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ | ✅ item 14: gained requireUp() so a guard with no server says one plain sentence and exits 2; baseFrom() now reads all three base env names that were already in use. |
| P13694 | `scripts/sweep/t3/across-panels.mjs` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ | ✅ item 13: absolute-path .env.local read, fixed. |
| P13695 | `scripts/sweep/t3/run-all.mjs` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13696 | `scripts/sweep/t3/basket-and-doors.mjs` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ | ✅ item 13: absolute-path .env.local read, fixed. |
| P13697 | `scripts/sweep/t3/screens.mjs` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13698 | `scripts/sweep/t3/order-end-to-end.mjs` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ | ✅ item 13: absolute-path .env.local read, fixed. |
| P13699 | `scripts/sweep/devStacks.mjs` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13700 | `scripts/sweep/login.mjs` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13701 | `scripts/sweep/crawl.mjs` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13702 | `scripts/sweep/audit-capture.mjs` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ | ✅ item 13: absolute-path .env.local read, fixed. |
| P13703 | `scripts/live-fix-watcher/uninstall.sh` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13704 | `scripts/live-fix-watcher/install.sh` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13705 | `scripts/live-fix-watcher/watch.mjs` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13706 | `scripts/launchagents/com.aevinite.tablet-audit.plist` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13707 | `scripts/launchagents/com.aevinite.nightly-repair.plist` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13708 | `scripts/launchagents/install.sh` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13709 | `scripts/launchagents/com.aevinite.owner-audit.plist` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13710 | `scripts/launchagents/run-job.mjs` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13711 | `tests/error-text.test.mjs` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13712 | `tests/money.test.mjs` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13713 | `tests/order-totals.e2e.mjs` still matches how the app is built and run today (one app, port from a flag, no pre-merge port) | read the file; grep for 4001/4002/4003, for `/api/` prefixes, and for a hard-coded port | ✅ |  |
| P13714 | `verify-access-live.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13715 | `verify-access-model.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13716 | `verify-access-search.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13717 | `verify-admin-money.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13718 | `verify-allergy-isolation.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ | ❌→✅ item 19: printed 'Cleaned up test order.' and cleaned up nothing — a hard DELETE the database refuses (mig 190), unread. Left a live ALGTEST table, an open session and a queued ticket on the manager's floor. |
| P13719 | `verify-avlive-offline-complete.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ⏭ | reads the AV-live folder by design and is red whenever that stack is behind; excluded from CI on purpose. Read-only md5 compare, no keys printed. NOT TOUCHED. |
| P13720 | `verify-busy-server.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13721 | `verify-cache.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13722 | `verify-cancel-loss.ts` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13723 | `verify-cancel-made.ts` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13724 | `verify-cancelled-tile-parity.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ | ❌→✅ item 1: order_items inserts had no restaurant_id and the error was unread, so both sides of the comparison ran on ZERO dishes and its main check printed a ✓ over nothing. Teardown hard-DELETEd rows the DB refuses (mig 190), leaving a live '288 · Preparing' phantom tile on the manager's floor and blocking its own next run. |
| P13725 | `verify-closed-session-orders.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ | ✅ item 17: its teardown named the table but not the restaurant. The name is random per run so it could only match its own rows, but the shape is the one that reached another restaurant. |
| P13726 | `verify-customer-erase.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13727 | `verify-customers.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ | ❌→✅ item 8: four checks red on two deliberate fixes — the @page size:80mm that was removed because a forced page size rotates the job, and the real `disabled` attribute dropped so a waiter's tap can't die in silence. |
| P13728 | `verify-db-grants.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ⏭ | RED, and not from this branch: `lfh_request_verification()` is missing from the shared dev DB because T21's unmerged migration 354 drops it. Guard is correct. |
| P13729 | `verify-db-parity.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ⏭ | RED, and not from this branch: duplicate migration 352 on main (T24) + a function on the dev DB in no migration. HANDOFF H4. |
| P13730 | `verify-dish-counts.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13731 | `verify-edge-cases.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ | ❌→✅ item 2: DEAD — ten page.goto calls in DOUBLE quotes handed Chrome the literal `${BASE}/menu`. 0 of 14 checks had run. Also: no restaurant_id on any write, an UNSCOPED teardown that measurably closed another restaurant's table-11 session, un-namespaced guest storage keys, a four-servers-era password POST, and three assertions describing deleted screens. Item 4: its make-head refusal check passed on 'No restaurant scope' — any 400 satisfied it. |
| P13732 | `verify-families.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ | ❌→✅ item 12: called localhost:4003, retired 2026-06-13 — nothing had run for over two months. Rewritten to the four properties nothing else holds; the rest was duplicated or describes paths the owner closed. |
| P13733 | `verify-floor-offplan.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13734 | `verify-floor-per-row.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13735 | `verify-floor-share.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13736 | `verify-guest-doors.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13737 | `verify-guest-pass-private.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13738 | `verify-guest-read.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13739 | `verify-guest-recovery.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13740 | `verify-guest.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ | ❌→✅ item 9: P00155 asked for URLSearchParams/qs.append in the menu page after the building moved into queryStringOf() in lib/tenant.ts. Now asserts the behaviour. |
| P13741 | `verify-heatmap-parity.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ⏭ | refuses to run (exit 2) until `lfh_owner_heatmap_old` is staged beside the live function, and nothing creates it. Honest, but it can never assert — listed as 🟡 I5 for him. |
| P13742 | `verify-loadall-preserves-orders.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13743 | `verify-manager-behaviour.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13744 | `verify-manager-gates.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13745 | `verify-manager-hidden.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13746 | `verify-manager-live-rush.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ | ❌→✅ item 18: once it could run to the end it reported SIXTEEN failures on a correct floor — a tile with dishes prints the served counter as text and carries the state phrase in the line's title. It reads both now. ALSO item 21: its teardown read EVERY non-closed session in each crew's restaurant and closed the lot, while printing 'closed the tables they opened'. On a real restaurant that ends every meal in the house (mig 232 cancels + archives every unpaid live order on a closed session). Now closes only the tables it touched, and dismisses their kitchen tickets. |
| P13747 | `verify-menu-parts-live.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13748 | `verify-merge-keeps-mark.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13749 | `verify-merge-party.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13750 | `verify-merge-records-who.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13751 | `verify-merged-floor.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ | ✅ item 13: read the SHARED folder's .env.local by absolute path — a lane in its own worktree asserted against whatever stack that copy pointed at. FLAKY once under three concurrent processes; passed on re-run and in pass 2. ALSO item 22: it had NO teardown at all — seven live tables left on the floor after every run, header reading '7/30 OCCUPIED' on an empty restaurant, plus five 'hasn't printed' banners. Now clears on the way out and on a crash. |
| P13752 | `verify-offline-retry.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13753 | `verify-offline.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ | 55/55 against a real production build on port 4129. Against a dev server it refuses with exit 2 (item 14b) — dev serves JS no-cache, so every offline check would fail for an unrelated reason. |
| P13754 | `verify-one-number.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13755 | `verify-order-retry.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13756 | `verify-order.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13757 | `verify-outbox-drain.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13758 | `verify-owner-territory-live.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ⏭ | 126/127. The one red (P06476, add a dish through the real editor) is T21's migration 353 applied to the shared dev DB without its panel change — POST /api/editor/items answers 500 'no rating column' and the panel says Saved ✓. See findings. |
| P13759 | `verify-panel-cache.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13760 | `verify-panel-plumbing-live.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13761 | `verify-pay-history-delete.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13762 | `verify-purge-classified.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13763 | `verify-realtime.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ | ❌→✅ item 5: picked a dish and a category with .limit(1) and NO restaurant filter (could edit Aangan), flipped a category's `active` with the restore outside any finally, crashed on a session insert with no restaurant_id, and deleted activity rows by action name instead of id. |
| P13764 | `verify-recycle-bin.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13765 | `verify-recycle-name.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13766 | `verify-rota-clash.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13767 | `verify-scrollspy.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13768 | `verify-sections.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13769 | `verify-session-ux.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ | ❌→✅ item 3: DEAD — died at its first write (no restaurant_id), 3 literal templates, unscoped teardown on table 9, make-head with no ?rid=, and section 4 reaching the panel as a bare path. 0 of 11 checks had run; 11/11 now. |
| P13770 | `verify-settings-columns.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13771 | `verify-skin-ink.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13772 | `verify-slow-load.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ | ✅ passed; flaked ONCE on 'the Add button is not usable while the model loads' under three concurrent processes, and passed alone and in pass 2. Timing-sensitive by nature. |
| P13773 | `verify-split-payment.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13774 | `verify-staff-accounts.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13775 | `verify-static.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ⏭ | 31 of its 32 guards green; the one red is verify-doc-pointers (HANDOFF H1). verify:rejected was the other and is fixed (item 11). |
| P13776 | `verify-summary-parity.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ⏭ | refuses to run (exit 2) until a `lfh_table_view_summary_v2` candidate exists. That is how mig 238 was proved, so the refusal is the design. |
| P13777 | `verify-sweep-extras.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ | ❌→✅ item 23: phase 5465 only had something to click while ANOTHER guard's leftover was on the floor, and 5466-5468 hang off it, so four checks never ran. It seats its own party now. 18 pass, 0 fail. |
| P13778 | `verify-t24-money-rules.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ | ❌→✅ item 11: said 'REJECTED (owner, …)' without naming docs/REJECTED-IDEAS.md, so verify:rejected AND verify:static were red on clean main. |
| P13779 | `verify-table-lifecycle.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ | ❌→✅ item 18: tripped the app's own 3-second double-tap protection and died as `invalid input syntax for type uuid: "undefined"` six lines from the cause. Ran it three times in a row after the fix to prove it is no longer flaky. |
| P13780 | `verify-table-ownership.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ | ✅ passed; flaked ONCE on a click the panel's own nav intercepted mid-repaint (the 'a repainting board makes a real click miss' shape) and passed on re-run. |
| P13781 | `verify-tablet-floor.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13782 | `verify-tablet-parity.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ | ❌→✅ item 6: crashed before its first check (no restaurant_id on five tables) and had no try/finally, so a throw left a live 'preparing' order on table 9932 — a phantom table on the floor. |
| P13783 | `verify-tablet-taps.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13784 | `verify-tablet-wants-in.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13785 | `verify-tax-mode-e2e.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13786 | `verify-two-parties.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ | ❌→✅ item 18: same double-tap trip; now sends p_confirm_duplicate and stops with the refusal's own words. |
| P13787 | `verify-void-on-joined-party.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ | ❌→✅ item 7: reported three failures on a cancel that worked. The removal sheet has asked TWO required questions since 2026-08-18; the guard answered one and clicked a disabled Remove. |
| P13788 | `verify-warm-shell.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ |  |
| P13789 | `verify-write-paths.mjs` signs in at most ONCE per run — it calls `loginAs()` (which caches to disk) and never POSTs `/api/staff-login` itself | grep the file for `loginAs`, `staff-login`, `password=` | ✅ | ✅ item 13: same absolute-path .env.local read, fixed. Also flaked once under load and passed on re-run. |
| P13790 | `verify-access-live.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ✅ |  |
| P13791 | `verify-access-model.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ✅ |  |
| P13792 | `verify-access-search.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ✅ |  |
| P13793 | `verify-admin-money.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ✅ |  |
| P13794 | `verify-allergy-isolation.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ✅ | ❌→✅ item 19: printed 'Cleaned up test order.' and cleaned up nothing — a hard DELETE the database refuses (mig 190), unread. Left a live ALGTEST table, an open session and a queued ticket on the manager's floor. |
| P13795 | `verify-avlive-offline-complete.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ⏭ | reads the AV-live folder by design and is red whenever that stack is behind; excluded from CI on purpose. Read-only md5 compare, no keys printed. NOT TOUCHED. |
| P13796 | `verify-busy-server.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ✅ |  |
| P13797 | `verify-cache.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ✅ |  |
| P13798 | `verify-cancel-loss.ts` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ✅ |  |
| P13799 | `verify-cancel-made.ts` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ✅ |  |
| P13800 | `verify-cancelled-tile-parity.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ✅ | ❌→✅ item 1: order_items inserts had no restaurant_id and the error was unread, so both sides of the comparison ran on ZERO dishes and its main check printed a ✓ over nothing. Teardown hard-DELETEd rows the DB refuses (mig 190), leaving a live '288 · Preparing' phantom tile on the manager's floor and blocking its own next run. |
| P13801 | `verify-closed-session-orders.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ✅ | ✅ item 17: its teardown named the table but not the restaurant. The name is random per run so it could only match its own rows, but the shape is the one that reached another restaurant. |
| P13802 | `verify-customer-erase.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ✅ |  |
| P13803 | `verify-customers.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ✅ | ❌→✅ item 8: four checks red on two deliberate fixes — the @page size:80mm that was removed because a forced page size rotates the job, and the real `disabled` attribute dropped so a waiter's tap can't die in silence. |
| P13804 | `verify-db-grants.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ⏭ | RED, and not from this branch: `lfh_request_verification()` is missing from the shared dev DB because T21's unmerged migration 354 drops it. Guard is correct. |
| P13805 | `verify-db-parity.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ⏭ | RED, and not from this branch: duplicate migration 352 on main (T24) + a function on the dev DB in no migration. HANDOFF H4. |
| P13806 | `verify-dish-counts.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ✅ |  |
| P13807 | `verify-edge-cases.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ✅ | ❌→✅ item 2: DEAD — ten page.goto calls in DOUBLE quotes handed Chrome the literal `${BASE}/menu`. 0 of 14 checks had run. Also: no restaurant_id on any write, an UNSCOPED teardown that measurably closed another restaurant's table-11 session, un-namespaced guest storage keys, a four-servers-era password POST, and three assertions describing deleted screens. Item 4: its make-head refusal check passed on 'No restaurant scope' — any 400 satisfied it. |
| P13808 | `verify-families.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ✅ | ❌→✅ item 12: called localhost:4003, retired 2026-06-13 — nothing had run for over two months. Rewritten to the four properties nothing else holds; the rest was duplicated or describes paths the owner closed. |
| P13809 | `verify-floor-offplan.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ✅ |  |
| P13810 | `verify-floor-per-row.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ✅ |  |
| P13811 | `verify-floor-share.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ✅ |  |
| P13812 | `verify-guest-doors.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ✅ |  |
| P13813 | `verify-guest-pass-private.mjs` cleans up every row it creates, BY ID, in the same run — never a broad `delete where table_number=…` with no restaurant | grep for delete/update calls and check each carries `restaurant_id` and an id list | ✅ |  |
| P13814 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-3d-viewer.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13815 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-access-live.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13816 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-access-model.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13817 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-access-search.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13818 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-admin-api-a.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13819 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-admin-health-logs.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13820 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-admin-money.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13821 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-admin-refusals.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ | ❌→✅ item 10: the NOT_YET allowance said 2 database-words sites inside ensureCodes(); yesterday's H3 race fix added a third, legitimately. Red on a correct file. |
| P13822 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-admin-restaurants.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13823 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-allergy-isolation.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ | ❌→✅ item 19: printed 'Cleaned up test order.' and cleaned up nothing — a hard DELETE the database refuses (mig 190), unread. Left a live ALGTEST table, an open session and a queued ticket on the manager's floor. |
| P13824 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-audit-coverage.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13825 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-avlive-offline-complete.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ⏭ | reads the AV-live folder by design and is red whenever that stack is behind; excluded from CI on purpose. Read-only md5 compare, no keys printed. NOT TOUCHED. |
| P13826 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-bill-reprint-is-silent.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13827 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-billdoc-paper.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13828 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-board-sig.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13829 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-busy-server.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13830 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-cache.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13831 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-cancel-loss.ts) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13832 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-cancel-made.ts) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13833 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-cancelled-tile-parity.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ | ❌→✅ item 1: order_items inserts had no restaurant_id and the error was unread, so both sides of the comparison ran on ZERO dishes and its main check printed a ✓ over nothing. Teardown hard-DELETEd rows the DB refuses (mig 190), leaving a live '288 · Preparing' phantom tile on the manager's floor and blocking its own next run. |
| P13834 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-clash-coverage.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13835 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-closed-session-orders.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ | ✅ item 17: its teardown named the table but not the restaurant. The name is random per run so it could only match its own rows, but the shape is the one that reached another restaurant. |
| P13836 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-css-tokens.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13837 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-customer-erase.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13838 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-customers.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ | ❌→✅ item 8: four checks red on two deliberate fixes — the @page size:80mm that was removed because a forced page size rotates the job, and the real `disabled` attribute dropped so a waiter's tap can't die in silence. |
| P13839 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-db-grants.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ⏭ | RED, and not from this branch: `lfh_request_verification()` is missing from the shared dev DB because T21's unmerged migration 354 drops it. Guard is correct. |
| P13840 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-db-parity.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ⏭ | RED, and not from this branch: duplicate migration 352 on main (T24) + a function on the dev DB in no migration. HANDOFF H4. |
| P13841 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-dead-css.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13842 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-deps.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13843 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-dish-counts.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13844 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-doc-pointers.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ⏭ | RED: docs/GUARD-MAP.md needs 5 rows (2 already missing on clean main, 3 mine). docs/** is T29's. HANDOFF H1. |
| P13845 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-edge-cases.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ | ❌→✅ item 2: DEAD — ten page.goto calls in DOUBLE quotes handed Chrome the literal `${BASE}/menu`. 0 of 14 checks had run. Also: no restaurant_id on any write, an UNSCOPED teardown that measurably closed another restaurant's table-11 session, un-namespaced guest storage keys, a four-servers-era password POST, and three assertions describing deleted screens. Item 4: its make-head refusal check passed on 'No restaurant scope' — any 400 satisfied it. |
| P13846 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-families.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ | ❌→✅ item 12: called localhost:4003, retired 2026-06-13 — nothing had run for over two months. Rewritten to the four properties nothing else holds; the rest was duplicated or describes paths the owner closed. |
| P13847 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-fix-survives.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13848 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-floor-offplan.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13849 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-floor-per-row.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13850 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-floor-share.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13851 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-guest-doors.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13852 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-guest-pass-private.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13853 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-guest-read.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13854 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-guest-recovery.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13855 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-guest.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ | ❌→✅ item 9: P00155 asked for URLSearchParams/qs.append in the menu page after the building moved into queryStringOf() in lib/tenant.ts. Now asserts the behaviour. |
| P13856 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-heatmap-parity.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ⏭ | refuses to run (exit 2) until `lfh_owner_heatmap_old` is staged beside the live function, and nothing creates it. Honest, but it can never assert — listed as 🟡 I5 for him. |
| P13857 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-hidden-dishes.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13858 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-i18n-scope.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13859 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-loadall-preserves-orders.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13860 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-manager-behaviour.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13861 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-manager-gates.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13862 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-manager-hidden.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13863 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-manager-live-rush.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ | ❌→✅ item 18: once it could run to the end it reported SIXTEEN failures on a correct floor — a tile with dishes prints the served counter as text and carries the state phrase in the line's title. It reads both now. ALSO item 21: its teardown read EVERY non-closed session in each crew's restaurant and closed the lot, while printing 'closed the tables they opened'. On a real restaurant that ends every meal in the house (mig 232 cancels + archives every unpaid live order on a closed session). Now closes only the tables it touched, and dismisses their kitchen tickets. |
| P13864 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-menu-parts-live.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13865 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-menu-parts.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13866 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-merge-keeps-mark.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13867 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-merge-party.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13868 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-merge-records-who.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13869 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-merged-floor.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ | ✅ item 13: read the SHARED folder's .env.local by absolute path — a lane in its own worktree asserted against whatever stack that copy pointed at. FLAKY once under three concurrent processes; passed on re-run and in pass 2. ALSO item 22: it had NO teardown at all — seven live tables left on the floor after every run, header reading '7/30 OCCUPIED' on an empty restaurant, plus five 'hasn't printed' banners. Now clears on the way out and on a crash. |
| P13870 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-no-ask-before-delete.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13871 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-no-fatal-ui.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13872 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-offline-retry.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13873 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-offline.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ | 55/55 against a real production build on port 4129. Against a dev server it refuses with exit 2 (item 14b) — dev serves JS no-cache, so every offline check would fail for an unrelated reason. |
| P13874 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-one-number.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13875 | `npm run` its `verify:*` entry on my own port 4128 and record the real exit code — 0 = ran and passed, 1 = ran and found a fault, 2 = could not run (verify-open-no-attend-flash.mjs) | run it with `--base http://localhost:4128` and the three base env vars set; capture exit code + last lines | ✅ |  |
| P13876 | manager panel → Tables floor at desktop 1280×800 — the tile text a floor guard asserts (`N · seats · state`) is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13877 | manager panel → Tables floor → a joined party's tile at Samsung A35 360×780 dpr3 — the purple ⇄ merge line a merge guard asserts is really on the tile | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13878 | manager panel → table detail → a ticket's ✕ Cancel at desktop 1280×800 — both required questions of the removal sheet are on screen | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13879 | manager panel → Bills tab at Samsung A35 360×780 dpr3 — the bill row the bill guards assert is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13880 | waiter tablet → floor at desktop 1280×800 — the tile controls a tablet guard asserts are really tappable at 44px | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13881 | kitchen panel → board at Samsung A35 360×780 dpr3 — the ticket card a kitchen guard asserts is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13882 | owner console → Home at desktop 1280×800 — the tiles an owner guard asserts are really rendered in both skins | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13883 | owner console → Reports at Samsung A35 360×780 dpr3 — the chart a chart guard asserts is really drawn, not an empty card | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13884 | admin console → Restaurants at desktop 1280×800 — the row a restaurants guard asserts is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13885 | admin console → Printing at Samsung A35 360×780 dpr3 — the basket/printer rows a print guard asserts are really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13886 | guest menu (`/r/french-house/menu`) at desktop 1280×800 — the category rail a guest guard asserts is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13887 | admin console → Health at Samsung A35 360×780 dpr3 — the verdict line a health guard asserts is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13888 | manager panel → the removal-reason sheet at desktop 1280×800 — the second question (“was the food actually made?”) is required, so a guard that answers only one can never finish | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13889 | manager panel → Tables floor at Samsung A35 360×780 dpr3 — the tile text a floor guard asserts (`N · seats · state`) is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13890 | manager panel → Tables floor → a joined party's tile at desktop 1280×800 — the purple ⇄ merge line a merge guard asserts is really on the tile | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13891 | manager panel → table detail → a ticket's ✕ Cancel at Samsung A35 360×780 dpr3 — both required questions of the removal sheet are on screen | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13892 | manager panel → Bills tab at desktop 1280×800 — the bill row the bill guards assert is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13893 | waiter tablet → floor at Samsung A35 360×780 dpr3 — the tile controls a tablet guard asserts are really tappable at 44px | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13894 | kitchen panel → board at desktop 1280×800 — the ticket card a kitchen guard asserts is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13895 | owner console → Home at Samsung A35 360×780 dpr3 — the tiles an owner guard asserts are really rendered in both skins | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13896 | owner console → Reports at desktop 1280×800 — the chart a chart guard asserts is really drawn, not an empty card | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13897 | admin console → Restaurants at Samsung A35 360×780 dpr3 — the row a restaurants guard asserts is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13898 | admin console → Printing at desktop 1280×800 — the basket/printer rows a print guard asserts are really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13899 | guest menu (`/r/french-house/menu`) at Samsung A35 360×780 dpr3 — the category rail a guest guard asserts is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13900 | admin console → Health at desktop 1280×800 — the verdict line a health guard asserts is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13901 | manager panel → the removal-reason sheet at Samsung A35 360×780 dpr3 — the second question (“was the food actually made?”) is required, so a guard that answers only one can never finish | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13902 | manager panel → Tables floor at desktop 1280×800 — the tile text a floor guard asserts (`N · seats · state`) is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13903 | manager panel → Tables floor → a joined party's tile at Samsung A35 360×780 dpr3 — the purple ⇄ merge line a merge guard asserts is really on the tile | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13904 | manager panel → table detail → a ticket's ✕ Cancel at desktop 1280×800 — both required questions of the removal sheet are on screen | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13905 | manager panel → Bills tab at Samsung A35 360×780 dpr3 — the bill row the bill guards assert is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13906 | waiter tablet → floor at desktop 1280×800 — the tile controls a tablet guard asserts are really tappable at 44px | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13907 | kitchen panel → board at Samsung A35 360×780 dpr3 — the ticket card a kitchen guard asserts is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13908 | owner console → Home at desktop 1280×800 — the tiles an owner guard asserts are really rendered in both skins | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13909 | owner console → Reports at Samsung A35 360×780 dpr3 — the chart a chart guard asserts is really drawn, not an empty card | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13910 | admin console → Restaurants at desktop 1280×800 — the row a restaurants guard asserts is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13911 | admin console → Printing at Samsung A35 360×780 dpr3 — the basket/printer rows a print guard asserts are really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13912 | guest menu (`/r/french-house/menu`) at desktop 1280×800 — the category rail a guest guard asserts is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13913 | admin console → Health at Samsung A35 360×780 dpr3 — the verdict line a health guard asserts is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13914 | manager panel → the removal-reason sheet at desktop 1280×800 — the second question (“was the food actually made?”) is required, so a guard that answers only one can never finish | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13915 | manager panel → Tables floor at Samsung A35 360×780 dpr3 — the tile text a floor guard asserts (`N · seats · state`) is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13916 | manager panel → Tables floor → a joined party's tile at desktop 1280×800 — the purple ⇄ merge line a merge guard asserts is really on the tile | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13917 | manager panel → table detail → a ticket's ✕ Cancel at Samsung A35 360×780 dpr3 — both required questions of the removal sheet are on screen | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13918 | manager panel → Bills tab at desktop 1280×800 — the bill row the bill guards assert is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13919 | waiter tablet → floor at Samsung A35 360×780 dpr3 — the tile controls a tablet guard asserts are really tappable at 44px | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13920 | kitchen panel → board at desktop 1280×800 — the ticket card a kitchen guard asserts is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13921 | owner console → Home at Samsung A35 360×780 dpr3 — the tiles an owner guard asserts are really rendered in both skins | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13922 | owner console → Reports at desktop 1280×800 — the chart a chart guard asserts is really drawn, not an empty card | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13923 | admin console → Restaurants at Samsung A35 360×780 dpr3 — the row a restaurants guard asserts is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13924 | admin console → Printing at desktop 1280×800 — the basket/printer rows a print guard asserts are really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13925 | guest menu (`/r/french-house/menu`) at Samsung A35 360×780 dpr3 — the category rail a guest guard asserts is really rendered | headless Playwright on :4128, screenshot to my scratch folder, then Read the image | ✅ |  |
| P13926 | Across panels: a guard that asserts a manager-panel behaviour also asserts it on the waiter tablet, or records why not (pass 1) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13927 | Across panels: a guard that asserts a kitchen behaviour also covers the tablet's copy of the same action (pass 1) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13928 | Across panels: a fix I make to a guard's selector is applied to every panel copy of that selector (pass 1) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13929 | Across panels: a rule the owner set in one panel is checked in all three panel copies (editor/tablet/kitchen) (pass 1) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13930 | Across panels: a guard's fixture table number does not collide with another guard's fixture table number (pass 1) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13931 | Across panels: a guard's cleanup cannot remove a row another guard is mid-way through using (pass 1) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13932 | Across panels: the guard-map / package.json / script name for one guard agree with each other (pass 1) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13933 | Across panels: a guard that drives the owner console also honours `aevidine_skin`, not `lfh_theme` (pass 1) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13934 | Across panels: a guard that asserts a manager-panel behaviour also asserts it on the waiter tablet, or records why not (pass 2) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13935 | Across panels: a guard that asserts a kitchen behaviour also covers the tablet's copy of the same action (pass 2) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13936 | Across panels: a fix I make to a guard's selector is applied to every panel copy of that selector (pass 2) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13937 | Across panels: a rule the owner set in one panel is checked in all three panel copies (editor/tablet/kitchen) (pass 2) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13938 | Across panels: a guard's fixture table number does not collide with another guard's fixture table number (pass 2) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13939 | Across panels: a guard's cleanup cannot remove a row another guard is mid-way through using (pass 2) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13940 | Across panels: the guard-map / package.json / script name for one guard agree with each other (pass 2) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13941 | Across panels: a guard that drives the owner console also honours `aevidine_skin`, not `lfh_theme` (pass 2) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13942 | Across panels: a guard that asserts a manager-panel behaviour also asserts it on the waiter tablet, or records why not (pass 3) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13943 | Across panels: a guard that asserts a kitchen behaviour also covers the tablet's copy of the same action (pass 3) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13944 | Across panels: a fix I make to a guard's selector is applied to every panel copy of that selector (pass 3) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13945 | Across panels: a rule the owner set in one panel is checked in all three panel copies (editor/tablet/kitchen) (pass 3) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13946 | Across panels: a guard's fixture table number does not collide with another guard's fixture table number (pass 3) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13947 | Across panels: a guard's cleanup cannot remove a row another guard is mid-way through using (pass 3) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13948 | Across panels: the guard-map / package.json / script name for one guard agree with each other (pass 3) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13949 | Across panels: a guard that drives the owner console also honours `aevidine_skin`, not `lfh_theme` (pass 3) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13950 | Across panels: a guard that asserts a manager-panel behaviour also asserts it on the waiter tablet, or records why not (pass 4) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13951 | Across panels: a guard that asserts a kitchen behaviour also covers the tablet's copy of the same action (pass 4) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13952 | Across panels: a fix I make to a guard's selector is applied to every panel copy of that selector (pass 4) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13953 | Across panels: a rule the owner set in one panel is checked in all three panel copies (editor/tablet/kitchen) (pass 4) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13954 | Across panels: a guard's fixture table number does not collide with another guard's fixture table number (pass 4) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13955 | Across panels: a guard's cleanup cannot remove a row another guard is mid-way through using (pass 4) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13956 | Across panels: the guard-map / package.json / script name for one guard agree with each other (pass 4) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13957 | Across panels: a guard that drives the owner console also honours `aevidine_skin`, not `lfh_theme` (pass 4) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13958 | Across panels: a guard that asserts a manager-panel behaviour also asserts it on the waiter tablet, or records why not (pass 5) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13959 | Across panels: a guard that asserts a kitchen behaviour also covers the tablet's copy of the same action (pass 5) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13960 | Across panels: a fix I make to a guard's selector is applied to every panel copy of that selector (pass 5) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13961 | Across panels: a rule the owner set in one panel is checked in all three panel copies (editor/tablet/kitchen) (pass 5) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13962 | Across panels: a guard's fixture table number does not collide with another guard's fixture table number (pass 5) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13963 | Across panels: a guard's cleanup cannot remove a row another guard is mid-way through using (pass 5) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13964 | Across panels: the guard-map / package.json / script name for one guard agree with each other (pass 5) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13965 | Across panels: a guard that drives the owner console also honours `aevidine_skin`, not `lfh_theme` (pass 5) | read both/all copies side by side; grep the shared selector or table number in each | ✅ |  |
| P13966 | Judgment: does a real restaurant need this guard at all, or is it checking a shape nobody depends on? (round 1) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13967 | Judgment: would a stranger re-running this guard next month understand what it proved? (round 1) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13968 | Judgment: is this guard's failure message enough to act on, or does it only say something is wrong? (round 1) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13969 | Judgment: is this guard fast enough that a person will actually run it? (round 1) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13970 | Judgment: does this guard tell 'could not run' apart from 'ran and found a fault'? (round 1) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13971 | Judgment: if this guard is deleted, what real fault comes back unnoticed? (round 1) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13972 | Judgment: which THREE new guards would stop the three faults most likely to come back, and why those three? (round 1) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13973 | Judgment: does a real restaurant need this guard at all, or is it checking a shape nobody depends on? (round 2) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13974 | Judgment: would a stranger re-running this guard next month understand what it proved? (round 2) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13975 | Judgment: is this guard's failure message enough to act on, or does it only say something is wrong? (round 2) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13976 | Judgment: is this guard fast enough that a person will actually run it? (round 2) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13977 | Judgment: does this guard tell 'could not run' apart from 'ran and found a fault'? (round 2) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13978 | Judgment: if this guard is deleted, what real fault comes back unnoticed? (round 2) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13979 | Judgment: which THREE new guards would stop the three faults most likely to come back, and why those three? (round 2) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13980 | Judgment: does a real restaurant need this guard at all, or is it checking a shape nobody depends on? (round 3) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13981 | Judgment: would a stranger re-running this guard next month understand what it proved? (round 3) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13982 | Judgment: is this guard's failure message enough to act on, or does it only say something is wrong? (round 3) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13983 | Judgment: is this guard fast enough that a person will actually run it? (round 3) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13984 | Judgment: does this guard tell 'could not run' apart from 'ran and found a fault'? (round 3) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13985 | Judgment: if this guard is deleted, what real fault comes back unnoticed? (round 3) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13986 | Judgment: which THREE new guards would stop the three faults most likely to come back, and why those three? (round 3) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13987 | Judgment: does a real restaurant need this guard at all, or is it checking a shape nobody depends on? (round 4) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13988 | Judgment: would a stranger re-running this guard next month understand what it proved? (round 4) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13989 | Judgment: is this guard's failure message enough to act on, or does it only say something is wrong? (round 4) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13990 | Judgment: is this guard fast enough that a person will actually run it? (round 4) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13991 | Judgment: does this guard tell 'could not run' apart from 'ran and found a fault'? (round 4) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13992 | Judgment: if this guard is deleted, what real fault comes back unnoticed? (round 4) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13993 | Judgment: which THREE new guards would stop the three faults most likely to come back, and why those three? (round 4) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13994 | Judgment: does a real restaurant need this guard at all, or is it checking a shape nobody depends on? (round 5) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13995 | Judgment: would a stranger re-running this guard next month understand what it proved? (round 5) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13996 | Judgment: is this guard's failure message enough to act on, or does it only say something is wrong? (round 5) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13997 | Judgment: is this guard fast enough that a person will actually run it? (round 5) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13998 | Judgment: does this guard tell 'could not run' apart from 'ran and found a fault'? (round 5) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P13999 | Judgment: if this guard is deleted, what real fault comes back unnoticed? (round 5) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
| P14000 | Judgment: which THREE new guards would stop the three faults most likely to come back, and why those three? (round 5) | written judgment in the note column — no code change unless it passes the four-test gate | ✅ |  |
