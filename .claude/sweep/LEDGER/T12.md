# T12 — Owner home screen, activity log, who's online, marketing · phases P05501–P06000

**Territory:** `app/owner/page.tsx` · `app/owner/activity/**` · `app/owner/online/**` ·
`app/owner/marketing/**` · `components/owner/OwnerShell.tsx`

**Stack:** worktree `sweep6/T12`, branch `sweep6/t12-owner-home-activity`, dev server port **4112**,
signed in as the French House diag owner (`diago1`, ONE restaurant — so the single-restaurant
dashboard is the live path; the multi-restaurant rows below are code-read unless marked otherwise).
Screenshots: 1280×800 and 360×780 dpr3, `aevidine_skin` dark **and** light.

**Budget as run** — 500 phases:

| share | phases | what it is |
|---|---|---|
| 40% | P05501–P05700 (200) | reading the code for correctness |
| 20% | P05701–P05800 (100) | conformance to the project rules named in the prompt |
| 15% | P05801–P05875 (75) | watching it run (headless Playwright on 4112) |
| 10% | P05876–P05925 (50) | screenshots actually read (desktop + A35, both skins) |
| 10% | P05926–P05975 (50) | tracing a change across panels / files |
| 5%  | P05976–P06000 (25) | judgment: does a real restaurant need it this way |

Per-file floor met: `page.tsx` 214 rows · `activity/page.tsx` 96 rows · `OwnerShell.tsx` 62 rows ·
`marketing/page.tsx` 9 rows · `online/page.tsx` 9 rows (rest are cross-file).

`result` column: ✅ pass · ❌ real problem (fixed in this PR unless the note says otherwise) ·
⏭ skipped, with the reason written in the note.

**Both passes are recorded in one row.** Every phase was executed, then all 500 were re-run from the
top after the fixes; a row that failed the first time keeps that history in its note so the next
sweep knows what the row is really testing. Results below are the SECOND pass.

**HIS REVIEW, 2026-08-18 — a third pass.** He read the numbered list, kept every fix, and turned
several of the parked items into work. What changed after the review is recorded in the rows above:
the tile row became five tiles in one line with a popup each (P05977), the detail now opens on the
scope and period on screen (a 13th problem, his own catch), the dish view got a visible ✕ (P05920),
"how long ago" got one wording (P05939), and all three HANDOFFs became fixes (P05846, P05945,
P05948). Two of his challenges were checked and answered rather than coded: there IS an admin toggle
for the owner's Reports section (`/aevinite/access` → Owner → Owner's menu → Reports, default ON),
and there is NO toggle anywhere for the dashboard's activity card — which is what he wants, since
the card simply disappears when Audit & logs is off.

**The second pass found two things the first pass did not, and one row was a false green:**
* **P05873** was marked ✅ without being driven. Driven, it failed — the Activity log's type chips
  and sort select were dead controls (problem 7). Both rows P05872/P05873 now say how they were run.
* **P05858** (the "live" pill) and **P05833** (the switched-off tiles) were TIGHTENED: fix 5 turned a
  KPI tile from a link into a plain card, which exposed a latent 1.92:1 ink (problem 11), and the
  instant-paint snapshot was still leaking real figures through the switched-off state (problem 12).
* **P05806**'s assertion was wrong rather than the code; the row now states the right rule.

---

## ⟲ SWEEP #7 RE-RUN — 2026-08-27, terminal 12 of 40

**Every one of the 500 rows P05501–P06000 was re-run**, top to bottom, against `origin/main`
**d7af8f63** in worktree `wt-s7-t12`, branch `sweep7/t12-owner-home`, dev server port **4212**,
signed in as the French House diag owner (`diago1`, ONE restaurant). Only **two commits** had
touched this territory since the sweep-#6 pass (`ffc87c1b` the admin breadcrumb, `1d9af537` the
light-skin ink), so most rows re-read identically — but they were re-read, not assumed.

**Three rows that were ✅ are ❌ this run. All three are JUDGMENT rows, and all three were false
greens rather than regressions** — the fault was there in August and the walk missed it, because
each one needed the screen driven in a state the earlier pass never put it in:

| id | what it claimed | what it actually did |
|----|-----------------|----------------------|
| P05992 | is any empty state a dead end | walk all nine, **with the controls that produce them actually used** | ❌ → fixed | **WAS A FALSE GREEN** — the walk read the branches instead of driving them. The Activity log's search and severity chips are SERVER-side, so narrowing to nothing lands on the zero-rows branch, which said "No staff activity yet — it appears here as your team works." over 8,829 entries, with no way to clear the search. Problem 1 — re-run: "Nothing matches that. [Clear the search]", and a severity chip gets its own sentence plus [Show all] |
| P05984 | when a section is switched off, does the screen read as OFF rather than BROKEN | drive the reports-off and logs-off states | ❌ → fixed | **WAS A FALSE GREEN.** Four tiles said "— · Reports are switched off" and "Today so far" printed **₹0 · 0 orders today** as fact — because it reads the OVERVIEW payload, which zeroes that restaurant's day on purpose, so `offNote` never reached it. One confident false zero beside four honest dashes reads as "you took nothing today". Problem 2 — re-run by replaying BOTH of the server's own answers: all five say the same thing, and the "live" pill goes with it |
| P05991 | is anything on these screens written for a developer rather than a restaurant owner | read every string, and every VALUE a string is filled with | ❌ → fixed | **WAS A FALSE GREEN** — the earlier pass read the templates, not the data that lands in them. The person column on both log surfaces printed a raw database id: "Handled a rating · c0af7b5b-c0d8-40f6-b831-f475e48bab53", from `app/api/owner/ratings` and `app/api/owner/customers`, which log `scope.ownerId` as the actor. Problem 3 — re-run: `lib/ownerActor.ts` renders a bare id as the em dash a nameless row already uses, and keeps the reference in the tooltip. The two routes need the real fix and are outside this fence |

**Fourteen rows' EXPECTATIONS moved** because the product legitimately changed on 2026-08-18 (the
tile row was rebuilt to five tiles with a popup each) and on 2026-08-19 (the owner gained the one
narrow write this record has, "was the food made?"). Each keeps its id and states the new rule:
P05585, P05586, P05591, P05592, P05593, P05594, P05595, P05596, P05602, P05654, P05677, P05701,
P05747, P05813, P05955, P05970, P05971, P05972.

**Three more rows gained a fault the old wording did not reach** — P05602 (a failed read folded into
the same null as an empty log), P05654 (the records card vanishing silently when the server said it
could not read them) and P05741 (its neighbour case). Fixed — items 4, 6.

Everything else re-read or re-drove green. The four `verify:*` guards over this territory
(`verify:owner-screen`, `verify:audit`, `verify:xray`, `verify:owner-home`) were green before any
edit and are green after.

---

## Reading the code for correctness — P05501–P05700

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P05501 | `RANGES` and `RANGE_LABEL` cover exactly the same 8 keys, so no dropdown row can render `undefined` | read both maps in `app/owner/page.tsx`, compare key sets | ✅ | today/yesterday/week/7d/month/30d/lastmonth/all in both |
| P05502 | `PREV_LABEL` has an entry for all 8 ranges (empty string allowed for `all`) | same | ✅ | `all` is deliberately "" so no "vs" caption is printed |
| P05503 | `RANGES.find(r => r.k === globalRange)!` can never be undefined — every value written to state comes from `RANGES` | trace `setGlobalRange` / `pickRange` call sites | ✅ | only `pickRange` and the validated localStorage restore write it |
| P05504 | the last-used range restored from localStorage is validated against `RANGES` before use | read the mount effect | ✅ | `RANGES.some(r => r.k === saved)` guard present |
| P05505 | a corrupt `lfh-owner-range` value falls back to the 30-day default instead of throwing | read the try/catch around `localStorage.getItem` | ✅ | try/catch + validation, default stays `30d` |
| P05506 | `pickRange` writes localStorage AND state, so the choice survives a refresh | read `pickRange` | ✅ | |
| P05507 | `rangeSpanText("week")` computes Monday-first, not Sunday-first | read the `dow` maths: `(ist.getUTCDay()+6)%7` | ✅ | Mon=0 |
| P05508 | `rangeSpanText("lastmonth")` names the previous month even in January | read `new Date(now.getFullYear(), now.getMonth()-1, 15)` | ✅ | JS normalises month −1 to December of the previous year |
| P05509 | `rangeSpanText` never returns an empty string for any of the 8 keys | walk each branch | ✅ | `all` falls through to "Everything up to …" |
| P05510 | `istWall` treats a zone-less timestamp as UTC so a figure reads the same in any browser zone | read the `zoneless` regex | ✅ | |
| P05511 | `istWall12` upper-cases am/pm so the records strip matches the rest of the console | read the replace | ✅ | `\b(am\|pm)\b` → upper |
| P05512 | `hour12(0)` prints "12 AM" and `hour12(12)` prints "12 PM", not "0 AM" | evaluate the expression by hand | ✅ | `h%12===0 ? 12` |
| P05513 | `tsLabel` uses an hour label for today/yesterday and a date label for every wider range | read the branch | ✅ | |
| P05514 | `istKey` uses `hourCycle: "h23"` so hour 00 is "00", not "24" | read the formatter options | ✅ | |
| P05515 | `keyLabel` pins the parse to +05:30 rather than letting the browser read the key as local time | read the template string | ✅ | `T${hh}:00:00+05:30` |
| P05516 | `keyLabel` returns the raw key rather than "Invalid Date" when the pattern does not match | read the guards | ✅ | regex guard + `Number.isNaN` guard |
| P05517 | `expectedBuckets("today")` starts at the 05:00-IST business-day start, not midnight | read `businessDayStartIso(now)` | ✅ | |
| P05518 | `expectedBuckets("today")` stops at the current hour so future hours are not zero-padded | read `endMs = now.getTime()` | ✅ | |
| P05519 | `expectedBuckets("yesterday")` covers a whole day, not up to now | read `endMs = startMs + DAY_MS - 1` | ✅ | |
| P05520 | `expectedBuckets` returns `[]` for week/month/lastmonth/all, and every caller has a sorted fallback | grep the four callers | ✅ | sparkOf, restTrend, groupTrend, drawerTrend, tableRows all sort |
| P05521 | `sparkOf` sorts its fallback keys — a tile line has no axis, so a wrong order reads as a real trend | read the `Array.from(by.entries()).sort` | ✅ | |
| P05522 | `sparkOf` returns undefined for fewer than 2 points so `Kpi` draws no line | read the `pts.length >= 2` gate | ✅ | |
| P05523 | `restTrend` sorts the no-expected-sequence fallback by bucket | read the sort | ✅ | |
| P05524 | `groupTrend` labels fallback buckets like a human date, never the raw en-CA key | read `keyLabel(k)` in the fallback | ✅ | this was the 2026-08-04 fault; still fixed |
| P05525 | `drawerTrend` has a fallback so a restaurant row tapped on "This month" still draws a chart | read the `exp.length ? exp : …` branch | ✅ | |
| P05526 | `tableRows` per-restaurant spark sorts its fallback keys | read the loop | ✅ | |
| P05527 | `groupTrend.stacked` is true only for 2–3 restaurants | read `>= 2 && <= 3` | ✅ | |
| P05528 | at 2–3 restaurants the bars use green shades, at 4+ the identity `portfolioColor(id)` | read the `stacked ? GREEN_SHADES : portfolioColor` | ✅ | |
| P05529 | `GREEN_SHADES` is indexed modulo its length so a 3rd restaurant cannot read past the end | read `[i % GREEN_SHADES.length]` | ✅ | |
| P05530 | `monthCompare` excludes TODAY from the current-month line (a part-day beside full days read as a crash) | read `cur: d < todayDom ? … : null` | ✅ | and the caption under the chart says so |
| P05531 | `monthCompare` derives day-of-month in IST, not the browser zone | read `Date.parse(bucket) + 5.5*3600_000` then `getUTCDate()` | ✅ | |
| P05532 | `monthCompare.maxDay` covers last month's longer month (31 vs 30) so no day is dropped | read `Math.max(todayDom, ...prevBy.keys())` | ✅ | |
| P05533 | `monthCompare` sends `prev: null` (not 0) when there is no last-month data, so the line is absent not flat | read `hasPrev ? … : null` | ✅ | |
| P05534 | `monthName(-1)` wraps to December rather than producing an invalid month | evaluate `((mi%12)+12)%12` | ✅ | |
| P05535 | the month-vs-month card is locked to its own `month` payload, independent of the range dropdown | read `pl("month")` | ✅ | |
| P05536 | the `month` payload is fetched on load AND on the 60s tick AND on manual Refresh | read the three call sites | ✅ | all three present |
| P05537 | `errText` pulls the human parts out of a thrown plain object instead of printing "[object Object]" | read the function | ✅ | |
| P05538 | `errText` truncates a JSON fallback so a whole payload cannot land in the banner | read `.slice(0, 200)` | ✅ | |
| P05539 | `timeAgo` clamps a future timestamp to 0 rather than printing a negative age | read `Math.max(0, …)` | ✅ | |
| P05540 | `fetchPayload` de-dupes concurrent requests for the same (scope, range) | read the `inflight` set | ✅ | |
| P05541 | `fetchPayload` always clears its inflight key, even when the request throws | read the `finally` | ✅ | |
| P05542 | `fetchPayload` asks for `records=1` only once per restaurant, not once per range | read the `recQ` condition against `recsRef` | ✅ | |
| P05543 | `fetchPayload` sends `cache: "no-store"` so a snapshot is never served by the browser cache | read the fetch options | ✅ | |
| P05544 | a deliberate "Reports aren't enabled" answer does not drop the connection pill to a warning | read the `a.disabled` early return | ✅ | `reportRealtime("weak")` is not reached |
| P05545 | …but the same answer is still printed inside a red-bordered "Couldn't load." card | read the `err` banner JSX | ✅ | was ❌: a permission was printed in a red "Couldn't load." card. Problem 5 — re-run: a plain muted note, 0 red banners |
| P05546 | a `disabled` answer does not set `landed`, so the age line keeps saying "your last view" | read the early return vs `setLanded(true)` | ✅ | was ❌. Problem 5 — the disabled branch now sets `landed`, so the age line stops saying "your last view" |
| P05547 | `fetchMoney` stores the literal `"err"` so a failed total renders "—" and not 0 | read the catch | ✅ | |
| P05548 | `fetchMoney` records its own `cachedAt` under a `money:` key so the age line can include it | read `setAges` | ✅ | |
| P05549 | the pre-warm only warms ONE extra range (the last-used one), not all seven | read the `others` array | ✅ | the 2026-08-06 egress fix is intact |
| P05550 | the pre-warm runs once per scope per visit (`warmedScopes`) | read the ref guard | ✅ | |
| P05551 | the pre-warm timers are cleared on unmount | read the effect cleanup | ✅ | |
| P05552 | the pre-warm skips a range already in the cache | read the `cacheRef` / `moneyRef` guards | ✅ | |
| P05553 | `neededRanges` is the main range and nothing else (no ghost "7d" fetch) | read the memo | ✅ | the 2026-08-06 waste is still gone |
| P05554 | the 60s auto-refresh re-fetches overview + main range + month + money | read `tick` | ✅ | |
| P05555 | the 60s auto-refresh does NOT re-fetch the Recent-activity feed, so that card freezes at page load | read `tick` and compare with `manualRefresh` | ✅ | was ❌: oplog stayed at 1 request over 88s. Problem 4 — re-run: 1 → 2 over one tick |
| P05556 | `tick` is held in a ref so `useActiveAutoRefresh` never captures a stale closure | read `tickRef` | ✅ | |
| P05557 | manual Refresh forces `refresh=1` on every payload it re-fetches | read `manualRefresh` | ✅ | |
| P05558 | manual Refresh includes the month payload (it used to be left out) | read the `jobs.push` for month | ✅ | |
| P05559 | manual Refresh includes the activity feed | read the `if (activeRid) jobs.push(fetchActs(...))` | ✅ | |
| P05560 | manual Refresh holds the spinner at least 400ms so the tap is visibly acknowledged | read the `wait` maths | ✅ | never a silent tap |
| P05561 | manual Refresh uses `allSettled`, so one failed job cannot leave the spinner stuck | read the promise handling | ✅ | |
| P05562 | `loadOverview` treats a payload carrying `error` as a failure rather than rendering it as data | read the throw | ✅ | |
| P05563 | the instant-paint snapshot is a render-only fallback, never written into `cache` | read `pl` / `moneyOf` | ✅ | so the revalidating fetch still fires |
| P05564 | the snapshot is keyed by the admin `?rid` pin so two admin tabs cannot cross-paint | read `snapKey` | ✅ | |
| P05565 | the snapshot hydration uses `cur ?? s.ov` so a live answer already in state is never overwritten | read the two setters | ✅ | |
| P05566 | the snapshot is only written once both `ov` and at least one payload exist | read the write effect guard | ✅ | |
| P05567 | `scopePin` is read once from the URL and rides on every analytics/money/oplog call | read `scp` and its call sites | ✅ | |
| P05568 | `withPin` carries the pin into every in-page link | read the helper and its uses | ✅ | |
| P05569 | `?focus=all` beats the saved drill, so "All restaurants" from another page really lands on all | read the focus branch | ✅ | |
| P05570 | the saved drill is only restored when it has both a level and a rid | read the `saved.level && saved.rid` guard | ✅ | |
| P05571 | the drill is persisted only after the initial restore, so first paint cannot wipe it | read `drillRestored` | ✅ | |
| P05572 | going home removes the saved drill instead of storing `{level:"home"}` | read the persist effect | ✅ | |
| P05573 | the drill is stored per tab (sessionStorage), not per browser | read the API used | ✅ | |
| P05574 | the crumb tail is cleared on unmount so another page cannot inherit it | read the effect return | ✅ | |
| P05575 | the crumb tail omits the restaurant name for a single-restaurant owner | read the `!single` guard | ✅ | |
| P05576 | `levelDepth` maps home/restaurant/dish to 0/1/2 and `drillScroll` is sized 3 | read both | ✅ | |
| P05577 | drilling DEEPER opens at the top; going BACK restores the level's own scroll | read the layout effect | ✅ | logic correct… |
| P05578 | …but the element it reads is `.adm-main`, which is `overflow-y: visible` at ≤900px | read the selector, then measure `.adm-main` vs `.adm` at 360px | ✅ | was ❌: it moved `.adm-main`, which does not scroll at 360px. Problem 8 — re-run: A35 3002 → dish top → BACK restores 3012 of 3329; desktop 1505 → 0 → 1505 exact |
| P05579 | the drill is registered with the back-stack manager so the phone's BACK peels a level | grep `useBackClose` in the file for a drill layer | ✅ | was ❌: no drill layer existed. Problem 8 — two `useBackClose` layers, BACK peels dish → restaurant → dashboard |
| P05580 | `kpiOf` for a restaurant scope reads `paidOrders` and falls back to `orders` when absent | read the return | ✅ | |
| P05581 | `kpiOf` for a group scope sums revenue and orders across restaurants | read the reducers | ✅ | |
| P05582 | `kpiOf` group `avg` divides by PAID orders, matching the tile's "per paid order" caption | read `paidOrders ? revenue/paidOrders : 0` | ✅ | |
| P05583 | `kpiOf` never divides by zero | read the ternary | ✅ | |
| P05584 | the multi table's "Avg / order" column is named differently from the tile because it divides by ALL orders | read the `th("avg", "Avg / order")` comment and the maths | ✅ | deliberate honesty fix, intact |
| P05585 | ~~`kpiCount` is 5 normally and 7 once the payroll module is on~~ → **MOVED (owner, 2026-08-18):** the row is a FIXED FIVE — Revenue · Orders · Today so far · Expenses · On hand — and `kpiCount` no longer exists | grep `kpiCount` in `app/owner/page.tsx` | ✅ | re-run 2026-08-27: 0 hits for `kpiCount`, and the rendered row is 5 tiles (driven). The two payroll tiles were folded into Expenses / On hand, whose popups carry the staff-pay detail |
| P05586 | ~~`kpiCols` picks a balanced row (7 → 4 + 3)~~ → **MOVED:** the grid is a fixed 5, stepping to 3 at ≤1080px and 2 at ≤760px, and says why in its own comment | read `.ow2-stats5` and the two media queries | ✅ | re-run 2026-08-27: `repeat(5, minmax(0,1fr))`, `repeat(3,…)!important` at 1080, `repeat(2,…)!important` at 760. `minmax(0,1fr)` not a bare `1fr`, so one long figure cannot widen its own track |
| P05587 | a tile with no sparkline gets `ow2-nospark` so it does not carry 34px of empty band | read the class expression | ✅ | |
| P05588 | the spark reserve rule is written with three classes so it beats `.owx .adm-stat` in globals.css | read the selector | ✅ | |
| P05589 | the "Today so far" tile is the only one with a live pill | read the `pill` prop uses | ✅ | |
| P05590 | "Today so far" reads from the overview payload, not analytics, so it is live rather than snapshotted | read `todayRev` / `todayOrd` | ✅ | |
| P05591 | ~~"Lost to cancellations" renders "—" (not 0) when the money total failed~~ → **MOVED:** that tile was retired on 2026-08-18 (a cancellation is a record, not money lost). The em-dash rule now belongs to all five tiles under `offNote` | read the five `v={offNote ? "—" : …}` expressions | ✅ | re-run 2026-08-27: 5 of 5 print an em dash and the reason when the server has refused this scope — including Today so far, which did not until item 2 |
| P05592 | ~~"Lost to cancellations" says "none — great" rather than a bare 0~~ → **MOVED:** retired with the tile. The Revenue popup now carries the cancellation SENTENCE and deliberately quotes no figure | read the `note` on the revenue case of `tileDetail` | ✅ | re-run 2026-08-27: the note explains that nothing was ever charged, and links into Audit & logs. No cancellation number anywhere on the page |
| P05593 | ~~the "After staff pay" tile subtracts what left from revenue and says so in words~~ → **MOVED:** it is the **On hand** tile, and the subtraction is shown line by line in its popup | read the `onhand` case of `tileDetail` | ✅ | re-run 2026-08-27: Revenue → Less staff pay → Less food made then binned → Money on hand, with an explicit `isTotal` row. And it now says when one of those costs could not be READ (item 5) |
| P05594 | ~~the staff-pay tiles are absent when the restaurant has no payroll module~~ → **MOVED:** Expenses and On hand are ALWAYS drawn; without the module `staffPay` is null, `staffOut` is 0 and the tile's own caption says "nothing recorded yet" | read the `sub` ladder on the Expenses tile | ✅ | re-run 2026-08-27: four branches — both costs, food only, food-figure-unread (new, item 5), staff-only, nothing recorded |
| P05595 | ~~every KPI tile is a link into the matching report~~ → **MOVED (owner, 2026-08-18: "if you click on orders, it will take you to that pop up"):** every tile is a BUTTON that opens its popup, and the report link is the popup's last line, carrying the scope and the range on screen | read the `onOpen` props and `detailHref` | ✅ | re-run 2026-08-27, driven: 5 buttons, 0 anchors; the Orders popup's link was `/owner/reports?view=…0001&range=30d&open=volume` — the scope and the period he is looking at |
| P05596 | ~~…but those links are still offered when the Reports section is switched off~~ → **MOVED:** with Reports off no tile opens at all (`onOpen` is undefined), and the popup's footer is a plain "Reports are switched off for this restaurant" | read the `offNote ? undefined :` guards and `reportsOn` | ✅ | re-run 2026-08-27 by replaying the server's own 403, in both skins: 0 anchors, 0 tiles that open, the footer link replaced by the muted sentence |
| P05597 | the hero shortcut row hides Reports / Team / Feedback per entitlement | read the three gates | ✅ | |
| P05598 | the hero's middle shortcut is labelled the same as the sidebar and the page it opens | compare `own-hero-link` text with `GROUPS` and `/owner/staff`'s own heading | ✅ | was ❌: the hero said "Staff & powers". Problem 1 — re-run: sidebar, hero and page all say Team |
| P05599 | the Recent-activity card's "See all" link is gated on the entitlement that actually exists | read the key, then read the keys `/api/owner/overview` sends | ✅ | was ❌: it read `entitlements.activity`, a key nobody sends. Problem 2 — now `logs` |
| P05600 | the Recent-activity card renders nothing at all when the log is switched off (module rule 6) | read the card's JSX for a gate | ✅ | was ❌: the card rendered and span for ever. Problem 3 — re-run with the server's own 403: the card is ABSENT |
| P05601 | `fetchActs` asks for 6 rows only | read the `limit=6` | ✅ | egress-tiny |
| P05602 | `fetchActs` distinguishes an empty list from a refusal — **and, since 2026-08-27, from a read that FAILED** | read `actsOff`, `actsErr` and the three setters | ✅ | was ❌ AGAIN this run, on the third branch: a thrown fetch and a malformed answer both landed on `setActs(null)`, which the card renders as "Loading…". Measured by aborting `/api/owner/oplog` — the card span with no end and no retry. Problem 6 — re-run: "Couldn't load this just now — it tries again by itself every minute." + a Try again button |
| P05603 | the mini feed never prints a raw database action code | read the row JSX for `actLabel(a.action)` | ✅ | the T15 fix is intact |
| P05604 | the mini feed never prints a raw panel name | read `panelLabel(a.panel)` | ✅ | |
| P05605 | the mini feed's empty state says what will fill it | read the `acts.length === 0` branch | ✅ | was ❌: bare "Nothing yet." Improvement 1 — now says what fills it |
| P05606 | the mini feed's actor column falls back to "—" rather than rendering `null` | read the JSX | ✅ | |
| P05607 | `insights` caps itself at 4 lines | read the `.slice(0, 4)` | ✅ | |
| P05608 | `insights` names a time in 12-hour form, like every other clock on the console | read `hour12(busiest.hour)` | ✅ | |
| P05609 | `insights` suppresses a percentage change under 3% as noise | read the `Math.abs(pct) >= 3` gate | ✅ | |
| P05610 | `insights` switches to "N× the period before" past 300% instead of printing a four-digit percent | read the branch | ✅ | |
| P05611 | `insights` never divides by a zero previous period | read the `p.prev.revenue > 0` guards | ✅ | |
| P05612 | the payments insight ignores the "Not recorded" bucket when naming the top method | read the filter | ✅ | |
| P05613 | the payments insight only fires past a 15% share, so a one-off method is not called a pattern | read the threshold | ✅ | |
| P05614 | `insights` group branch never names a leader when there is only one restaurant | read the `length > 1` guard | ✅ | |
| P05615 | `dishView` distinguishes "still loading" from "this dish had no sales in the range" | read the three return shapes | ✅ | |
| P05616 | the "no sales for this dish" state offers a way back to the restaurant | read the button | ✅ | |
| P05617 | `dishView` guards that the loaded payload is the SAME restaurant before reading it | read `p.restaurant.id !== view.rid` | ✅ | |
| P05618 | `DishList` sorts by the chosen key and never mutates the payload array | read the `[...payload.dishes]` copy | ✅ | |
| P05619 | `DishList` bars divide by `Math.max(1, …)` so an all-zero list cannot divide by zero | read `maxRev` | ✅ | |
| P05620 | `DishList`'s empty state names the range rather than saying "no data" | read the text | ✅ | "No dish sales in this range." |
| P05621 | the drawer only opens for a restaurant that is actually in the overview list | read the `find` guard in `drawer` | ✅ | |
| P05622 | the drawer closes on the hardware BACK, the backdrop, the ✕ and Escape | read the four handlers | ✅ | all four present |
| P05623 | the Escape listener is removed when the drawer closes | read the effect cleanup | ✅ | |
| P05624 | the drawer's figures come from data already loaded (zero extra fetches) | read the memo inputs | ✅ | |
| P05625 | the drawer's "Avg / order" says "all orders, paid or open" so it cannot be confused with the tile | read the caption | ✅ | |
| P05626 | "View in full detail" closes the drawer before drilling, so it cannot reopen over the new view | read `openFull` | ✅ | |
| P05627 | `callouts` exist only for 4+ restaurants | read the `<= 3` early return | ✅ | |
| P05628 | the two callouts can never name the same restaurant | read the `r.id === best.id` skip | ✅ | |
| P05629 | "needs attention" needs a real drop (worse than −5%) before it accuses a restaurant | read the threshold | ✅ | |
| P05630 | `callouts` momentum halves are computed from sorted bucket order, not payload row order | read the `buckets.sort()` | ✅ | |
| P05631 | `callouts` skips a restaurant whose first half was zero (an infinite percentage) | read `h.a <= 0` | ✅ | |
| P05632 | `tableRows` ranks by revenue independently of the chosen sort column | read the `rank` map | ✅ | |
| P05633 | `tableRows` search matches name AND slug | read the filter | ✅ | |
| P05634 | `tableRows` share denominator is `Math.max(1, total)` | read the expression | ✅ | |
| P05635 | a reports-off restaurant stays in the table but every money cell says "figures hidden" | read the `r.reportsOff` branch | ✅ | |
| P05636 | the reports-off row renders FOUR cells, matching the header one-for-one | count the `<td>`s in both branches | ✅ | the 2026-08-06 colSpan fault stays fixed |
| P05637 | the reports-off row's explanation sits in the always-visible Revenue column | read which cell carries the message | ✅ | |
| P05638 | a reports-off row draws no sparkline and no share bar | read the two `!r.reportsOff` guards | ✅ | |
| P05639 | the empty-table row spans all 10 columns | count the header cells against `colSpan={10}` | ✅ | |
| P05640 | the table tells "no match for that search" apart from "still loading" | read the ternary | ✅ | |
| P05641 | a table row is reachable by keyboard (tabIndex + Enter) | read the row props | ✅ | |
| P05642 | `th()` reports `aria-sort` so the sorted column is announced | read the helper | ✅ | |
| P05643 | tapping the same header twice reverses the direction | read `asc: s.k === k ? !s.asc : false` | ✅ | |
| P05644 | the restaurant colour is one identity per restaurant across dots, share bars and charts | compare `portfolioColor(r.id)` uses | ✅ | |
| P05645 | `restScopeText` says "N of M · takings hidden for K" when some restaurants are excluded | read the expression | ✅ | a caption cannot overstate its coverage |
| P05646 | `reportedCount` counts only non-`reportsOff` restaurants | read the filter | ✅ | |
| P05647 | the heatmap card says "last 90 days only" for the ranges wider than the server's clamp | read `HEAT_CLAMPED` and the caption | ✅ | |
| P05648 | the heatmap's period chip shows the CLAMPED window, not the selected one, when they differ | read the chip expression | ✅ | |
| P05649 | `PartialStrip` renders nothing when there is nothing partial | read the early return | ✅ | |
| P05650 | each `PartialStrip` is filtered to the key its own card is about | read the three `.filter(k => k === …)` | ✅ | |
| P05651 | the partial strip sits INSIDE the affected card, not as a page banner | read the placement | ✅ | so the owner knows WHICH chart is short |
| P05652 | …and it sits under the card title on every card, so it reads as belonging to that card | compare the three placements | ✅ | was ❌: the busy-heatmap strip sat above its title. Improvement 3 — all four sit under their own title |
| P05653 | the records strip names its own rolling window instead of borrowing the dropdown's | read the "LAST 30 DAYS (ROLLING)" captions | ✅ | the 549-vs-529 fault stays fixed |
| P05654 | the records strip is absent when there is neither a best day nor a star dish — **and PRESENT, carrying the reason, when the server says it could not read them** | read the render guard against `partial: ["records"]` | ✅ | was ❌ this run: `/api/owner/analytics` has sent `partial: ["records"]` since 2026-08-12 (its own comment calls it improvement I5, "A TILE THAT VANISHES SAYS SO") and NOTHING on this page ever read the key, so the card still just disappeared. Problem 4 — re-run by replaying that answer: the card is present with a strip worded for one restaurant |
| P05655 | "BIGGEST BILL" says "one sitting" when the table number is unknown | read the fallback | ✅ | |
| P05656 | "REGULARS" is drawn only when it is greater than zero | read the `> 0` guard | ✅ | |
| P05657 | `records.fastHour` prints its hour in 12-hour form with upper-cased AM/PM | read `istWall12` | ✅ | |
| P05658 | the group payment donut and the restaurant one both handle an empty list | read both call sites against `PaymentDonut`'s own `Empty` | ✅ | restaurant scope adds a worded empty state; group falls through to `Empty` |
| P05659 | the category donut handles an empty array without claiming to load forever | read `CategoryDonut`'s `rows.length` guard | ✅ | the payload always sends an array |
| P05660 | the heatmap handles an all-zero grid without claiming to load forever | read `Heatmap`'s guard | ✅ | |
| P05661 | the trend card falls back from an area chart to bars under 9 points | read the `restTrend.length >= 9` branch | ✅ | |
| P05662 | `TimeBar` receives numbers, never `undefined`, from the mapping | read the `Number(...) \|\| 0` coercions | ✅ | |
| P05663 | the "today is still in progress" caption appears under BOTH month-compare cards | grep the `ow2-note` uses | ✅ | group and restaurant scope |
| P05664 | `gatherReport` refuses before the overview has loaded rather than exporting an empty document | read the throw | ✅ | |
| P05665 | the export filename carries the date | read `exportName` | ✅ | |
| P05666 | `reportHref` carries the admin pin and the person pin | read the builder | ✅ | |
| P05667 | `ageTitle` prints an absolute IST timestamp as well as a relative age | read the template | ✅ | |
| P05668 | the header age line reports the OLDEST payload on screen, not the last one to answer | read `oldestShown` | ✅ | |
| P05669 | the header age line says "your last view ·" until a live payload has landed | read the `!landed` prefix | ✅ | |
| P05670 | `shownAges` includes the month payload's age as well as the main range's | read the array | ✅ | |
| P05671 | the `.ow2-two` grid uses `minmax(0,1fr)` so a card with an intrinsic minimum cannot push off-screen | read the rule | ✅ | the A35 136px overflow stays fixed |
| P05672 | `.ow2-two > * { min-width: 0 }` is present — the other half of that cure | read the rule | ✅ | |
| P05673 | the KPI grid column count comes from a CSS variable the component sets | read `--ow2-cols` | ✅ | |
| P05674 | the phone breakpoint forces 2 KPI columns and one card per row | read the `max-width: 760px` block | ✅ | |
| P05675 | the table's `<th>` rules are written as `:global(th)` because the header comes from a helper | read the selector and its comment | ✅ | |
| P05676 | the phone column-hiding is by CLASS, not `nth-child`, and is `:global` so it reaches the header | read the `hide-m` / `hide-s` rule | ✅ | |
| P05677 | ~~the KPI card keeps `overflow: visible` so the range popup can escape it~~ → **MOVED:** there is no per-card range dropdown left (the only one is the toolbar's `main`), and the tile popup is a fixed full-screen sheet, so nothing has to escape a tile any more | grep `<RangeDrop` and read `.ow2-tile-wrap` | ✅ | re-run 2026-08-27: one `RangeDrop` use, `position: fixed; inset: 0` on the sheet. `.ow2-kpi` declares no overflow and does not need to; the spark clips itself in its own rounded wrapper |
| P05678 | the range popup's `z-index` is above sibling cards | read `.owr-pop` | ✅ | |
| P05679 | the drawer backdrop uses ONE unprefixed `backdrop-filter` line | read the rule | ✅ | hand-adding `-webkit-` makes the build drop it |
| P05680 | no CSS comment in the styled-jsx template contains a backtick | grep the template literals | ✅ | a backtick there fails the build |
| P05681 | the split-banner styles are `global` because `highlights` is an extracted const | read the `<style jsx global>` and its comment | ✅ | |
| P05682 | the top performer's revenue mixes the skin's accent toward the skin's text, so it is readable on white | read `.ow2-split .txt em` | ✅ | ~5.5:1 light / ~8.8:1 dark |
| P05683 | `.rv-sort button.on` uses `var(--accent-on, #fff)` rather than a bare white | read the rule | ✅ | |
| P05684 | the whole file names no restaurant's own accent colour for a chart | grep `accentColor` uses in chart props | ✅ | only `portfolioColor` / GREEN reach a chart |
| P05685 | `FALLBACK` is only used for the table row's `accent` before it is overwritten by the palette | trace `accent` | ✅ | harmless |
| P05686 | `canonPayMethod` is applied before the payments insight picks a top method | read the map | ✅ | so "cash" and "Cash" are one row |
| P05687 | the dish leaderboard greys every dish except the one being viewed | read the `accentColor` ternary | ✅ | |
| P05688 | the dish leaderboard is capped at 12 bars | read the `.slice(0, 12)` | ✅ | |
| P05689 | tapping another bar in the dish view navigates to that dish rather than doing nothing | read `onSelect` | ✅ | |
| P05690 | the dish view's stat row prints share and rank out of the real dish count | read the JSX | ✅ | |
| P05691 | `single` is derived from the overview length, so a one-restaurant owner never sees a switcher | read the derivation | ✅ | |
| P05692 | `activeRid` resolves to the only restaurant for a single-restaurant owner | read the expression | ✅ | |
| P05693 | the single-restaurant hero prints the live open-table count with correct pluralisation | read the JSX | ✅ | |
| P05694 | the hero pill says Active/Off from the restaurant's own flag | read the JSX | ✅ | |
| P05695 | `OwnerShell`'s `ownerSectionLabel` uses longest-prefix matching so a sub-route keeps its section name | read the loop | ✅ | |
| P05696 | `ownerSectionLabel` honours `exact` for `/owner` so the dashboard does not match every route | read the `it.exact` branch | ✅ | |
| P05697 | `ownerSectionLabel` falls back to "Dashboard" rather than an empty crumb | read the return | ✅ | |
| P05698 | `sectionOn` treats an ABSENT entitlement as ON, so nothing disappears by itself | read `entitlements[it.ent] !== false` | ✅ | |
| P05699 | `withRid` appends `?rid` with the right separator when the href already has a query | read the `includes("?")` branch | ✅ | |
| P05700 | `/owner/marketing` and `/owner/online` are server components with no client state to get wrong | read both files | ✅ | 12 lines each, one `ComingSoon` call |

---

## Conformance to the project rules named in the prompt — P05701–P05800

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P05701 | every popup in `page.tsx` registers with the back-stack manager | grep `useBackClose` | ✅ | re-run 2026-08-27: **6 layers now**, not 3 — `owner-rng-global`, `owner-rest`, `owner-drill-restaurant`, `owner-drill-dish`, `owner-kpi-tile`, `owner-rest-drawer`. The tile popup (new since the last pass) was driven: hardware BACK closes it and leaves the address on `/owner` |
| P05702 | every popup in `OwnerShell.tsx` registers with the back-stack manager | grep `useBackClose` — nav drawer, X-ray zones, restaurant switcher | ✅ | 3 of 3 |
| P05703 | nothing in my territory hand-rolls `pushState` / `popstate` | grep both in all five files | ✅ | zero hits |
| P05704 | a drill LEVEL (restaurant / dish) is a back step too, not only a popup | grep for a drill layer | ✅ | was ❌. Problem 8 — the drill registers one layer per level |
| P05705 | the heatmap's zoom overlay registers its own layer | read `Heatmap` in Charts.tsx (read-only, T11's file) | ✅ | `owner-heatmap-zoom` |
| P05706 | the Audit/Activity page's row popups register a layer | read `LogDetailModal` / `RemovalDetailModal` (read-only) | ✅ | both use the shared modal hook |
| P05707 | the Audit/Activity page pauses its 60s refresh while the tab is hidden | read the `visibilitychange` handling | ✅ | |
| P05708 | the Audit/Activity page refreshes immediately when the tab becomes visible again | read `onVis` | ✅ | |
| P05709 | the Audit/Activity page clears its interval on unmount | read the effect cleanup | ✅ | |
| P05710 | the Audit/Activity page also stops polling when the owner walks away with the tab open | compare with the console's shared `useActiveAutoRefresh` idle rule | ✅ | was ❌: a bare setInterval kept firing at an unattended tab. Improvement 2 — re-run: 2 ticks inside the idle window, then silence, then instant wake on one mouse move |
| P05711 | the Audit/Activity page's poll is jittered rather than a metronome | same | ✅ | was ❌. Improvement 2 — the shared hook jitters each tick ±20% |
| P05712 | no poll anywhere in my territory is faster than the 60s backstop | grep every `setInterval` / interval argument | ✅ | 60_000 in activity, 60000 in the two shells |
| P05713 | the dashboard's overview read is shared with the shell's, not duplicated | read `fetchOwnerOverview` in both | ✅ | one cache, one request |
| P05714 | analytics reads are cached client-side per (scope, range) | read the `cache` map key | ✅ | |
| P05715 | the mini activity feed asks for 6 rows, not a page of 200 | read the query string | ✅ | |
| P05716 | the Audit list is capped server-side and the client re-sorts in hand, costing no request | read `AUDITSORT.view` usage | ✅ | |
| P05717 | the Audit type chips filter server-side (so a chip counting every page filters every page) | read `pickAudKind` → `kind` → the query | ✅ | |
| P05718 | narrowing the Audit type resets to page 1 | read `pickAudKind` | ✅ | |
| P05719 | changing the Activity search or severity resets to page 1 | read the effect on `[qDebounced, level]` | ✅ | |
| P05720 | the Activity search is debounced so typing does not fire a request per keystroke | read the 300ms timer | ✅ | |
| P05721 | analytics/dashboard figures come from the compute-on-view snapshot cache, never recomputed per open | read `cachedAt` handling + the route's cache | ✅ | |
| P05722 | Refresh is the only thing that forces a recompute | read `refresh=1` call sites | ✅ | |
| P05723 | there is no blind cron behind any of these numbers | grep for scheduled work in my files | ✅ | view-triggered only |
| P05724 | the owner console's skin key is `aevidine_skin` in localStorage AND a cookie | read `toggleSkin` | ✅ | |
| P05725 | the server is told the skin so SSR emits the right `data-skin` with no flash | read the `initialSkin` prop path | ✅ | |
| P05726 | when localStorage has a skin and the cookie is missing, the shell puts the cookie back | read the reconcile effect | ✅ | the T14 fix is here |
| P05727 | the shell broadcasts `lfh:owner-skin` so an embedded panel flips at the same instant | read the dispatch | ✅ | |
| P05728 | the embedded panel's skin has ONE writer (`useOwnerSkin`), not two | read who writes it | ✅ | shell broadcasts, `useOwnerSkin` consumes |
| P05729 | `lfh_theme` (the guest key) is never read or written in my territory | grep it | ✅ | zero hits — correct, not a bug |
| P05730 | dark is the default when no skin has ever been chosen | read `initialSkin ?? "dark"` | ✅ | deliberate |
| P05731 | the `.owx-scope` chip still reads "Owner overview" and does not name the page (R20) | read the chip | ✅ | rejection respected |
| P05732 | nothing in my territory re-suggests a kitchen profile (R7) | grep | ✅ | |
| P05733 | nothing in my territory adds a bill-delete route (R27) | grep | ✅ | |
| P05734 | I checked `docs/REJECTED-IDEAS.md` before every fix and every improvement in this run | re-read the table | ✅ | only R20 touches my files |
| P05735 | every list on the owner home says WHERE it lives in the owner's own words | read each card title | ✅ | "Recent activity · who did what", "Every dish · tap one for detail" |
| P05736 | the Audit page's heading says what the page answers, not just its name | read the h1 + sub | ✅ | "What was removed and why — and everything your staff did, line by line." |
| P05737 | an Audit load failure is called a load failure, not "nothing was removed" | read the error branch | ✅ | explicitly worded |
| P05738 | an Activity load failure is called a load failure, not "nothing happened" | read the error branch | ✅ | |
| P05739 | the Audit list has three different empty states (no match / none of that kind / nothing yet) | read the ternary | ✅ | |
| P05740 | a narrowed Audit type offers a way back to All from the empty state | read the button | ✅ | |
| P05741 | the Activity empty state says what will fill it | read the text | ✅ | re-run 2026-08-27: still true of the NOTHING-YET state. But the same branch was firing for a SEARCH and for a severity chip, where that sentence is false — see P05992 and the new row P20866. Fixed by problem 1; this row now covers only the genuinely-empty case |
| P05742 | the Activity counts say they describe THIS page when there is more than one | read the note | ✅ | |
| P05743 | the Audit money line says which slice it is totalling | read the `list.length … in total` line | ✅ | was ❌: "200 records · ₹91,337 in total" over a 442-record record. Problem 6 — re-run: "200 records on this page of 442 · ₹90,287 on this page" |
| P05744 | the risk strip's money and record totals cover the WHOLE record, from the server | read `riskTotals` from `counts` | ✅ | |
| P05745 | the risk strip never adds a cancelled KOT's value to money actually lost | read the money/record/data split | ✅ | the owner's own 2026-08-13 rule |
| P05746 | the risk split comes from the database (`lfh_audit_risk`), never re-decided in the client | read the `c.risk` preference | ✅ | falls back to the shared `riskOf` only when absent |
| P05747 | ~~the Audit view is read-only — no restore, no delete~~ → **MOVED (owner, 2026-08-19: "can be change by owner or manager"):** exactly ONE write exists — answering "was the food made?" on a cancellation. No restore, no delete, no edit of any removal | grep every non-GET fetch in the page | ✅ | re-run 2026-08-27: one POST to `/api/owner/audit` carrying `{order_id, made}` and nothing else. `verify:owner-screen` asserts the route still refuses a restore and still checks scope, entitlement and that the order really is cancelled |
| P05748 | the removal detail popup is opened against the owner's own read-only base | read the `base="/api/owner/audit"` prop | ✅ | |
| P05749 | a view the admin switched off is ABSENT from the switcher, not merely disabled | read the `!audDisabled` / `!actDisabled` gates | ✅ | |
| P05750 | if the default view is off, the page falls over to the other one by itself | read the `setView` in the disabled branch | ✅ | |
| P05751 | if BOTH views are off the page says so in one plain sentence | read `bothOff` | ✅ | |
| P05752 | hiding a view is never the only guard — the endpoint refuses too | read `/api/owner/oplog` and `/api/owner/audit` gates | ✅ | 403 + `disabled: true` |
| P05753 | the owner's log never shows the admin's own actions | read the route's `not("panel","in","(admin,db)")` | ✅ | |
| P05754 | the owner's log never shows raw app faults (`level='error'`) | read the route's `or(level.is.null,level.neq.error)` | ✅ | owner 2026-07-26 |
| P05755 | the severity chips therefore offer no "Errors" filter | read the three buttons | ✅ | matches the server |
| P05756 | the owner's log never shows the button-tap breadcrumbs | read the `neq("action","ui_taps")` | ✅ | |
| P05757 | the row action is always rendered through `actLabel`, never raw | grep `a.action` in the page | ✅ | |
| P05758 | the row panel is always rendered through `panelLabel`, never raw | grep `a.panel` | ✅ | |
| P05759 | every row carries its trail (restaurant › area › screen › target) | read the `trailOf` block | ✅ | |
| P05760 | the trail WRAPS to two lines rather than being chopped with an ellipsis | read the `-webkit-line-clamp: 2` | ✅ | the A35 "Sign–" fault stays fixed |
| P05761 | a manager-PIN row is marked as a PIN unlock, and a SHARED pin is marked differently | read `isManagerPinRow` + `pinShared` | ✅ | |
| P05762 | a resolved error row is struck through and dimmed rather than deleted | read the styles | ✅ | never hide an error |
| P05763 | the Activity list opts into the phone stacking class (`aud-stack`) | read the wrapper class | ✅ | both lists have it |
| P05764 | the Audit and Activity lists use the SAME pager component | read both `<Pager>` uses | ✅ | one behaviour, two halves |
| P05765 | the pager names the total as well as the page numbers | read the strip | ✅ | "1,240 entries · page 2 of 7" |
| P05766 | the pager elides long runs so it stays one line on a phone | read the `nums` loop | ✅ | |
| P05767 | the pager clamps a requested page into range | read `Math.min(Math.max(1, p), pages)` | ✅ | |
| P05768 | the pager's Prev/Next are disabled at the ends rather than silently doing nothing | read the `disabled` props | ✅ | |
| P05769 | paging returns the reader to the top of the list | drive it and measure the scroller's `scrollTop` before/after | ✅ | lands at 0 — see the note in P05820 for WHY, it is not the `window.scrollTo` line |
| P05770 | a tap on a log row opens the full detail rather than doing nothing | read the row `onClick` + Enter/Space | ✅ | |
| P05771 | no user action in my territory ends in a silent `return` | walk every handler | ✅ | after the fixes below; see P05772/P05773 |
| P05772 | the breadcrumb's middle segment is a live tap on every page that shows a tail | trace the dispatched event against each page's listeners | ✅ | was ❌: dead on /owner/manager. Problem 9 — the crumb now sends that channel too |
| P05773 | tapping the sidebar's "Dashboard" while a drill is open takes the owner back to the dashboard | drive it | ✅ | was ❌: it did nothing while a drill was open. Problem 10 — re-run: returns to the dashboard on both sizes |
| P05774 | the sign-out control is a POST form, not a GET link | read the `<form method="post">` | ✅ | a prefetch cannot sign the owner out |
| P05775 | the admin "Exit view" clears the act-as cookie before leaving | read `exitAdminView` | ✅ | |
| P05776 | the admin bar is never rendered for a real owner | read the `adminViewing` guard | ✅ | |
| P05777 | a section the admin removed is HIDDEN for the owner and MARKED for the admin | read the `!on && (!adminViewing \|\| simulated)` branch | ✅ | marks, never strips |
| P05778 | the mark is cyan in both skins, not grey | read the `.xray-off` rule | ✅ | grey read as "broken" |
| P05779 | the X-ray zone list jumps to the exact admin control for that section | read the `router.push` to `/aevinite/access?focus=` | ✅ | |
| P05780 | "See the actual owner panel" is pure URL state, so it cannot desync | read `setSimulate` | ✅ | |
| P05781 | the `?rid` pin is read AFTER mount so SSR and first paint match | read the effect and its comment | ✅ | the hydration fault stays fixed |
| P05782 | a pinned admin tab shows ITS OWN restaurant's name, not the shared act-as cookie's | read `pinnedName` / `shownName` | ✅ | |
| P05783 | the pin rides on every sidebar link | read `withRid` in the nav | ✅ | |
| P05784 | "My restaurants" is hidden for a single-restaurant owner | read the `myRests.length > 1` gate | ✅ | no list of one |
| P05785 | a reports-off restaurant's takings read "hidden" in the sidebar, not ₹0 | read the `reportsOff` branch | ✅ | in both the sidebar and the switcher |
| P05786 | the sidebar and the charts use the SAME colour per restaurant | read `portfolioColor(r.id)` in both | ✅ | |
| P05787 | switching restaurant re-scopes IN PLACE on Dashboard, Reports and Manager mode | read the three branches of `openRestaurant` | ✅ | |
| P05788 | a page that NAVIGATES leaves the switcher open and lets the route change close it | read the else branch and the `[path]` effect | ✅ | closing early ate the tap (2026-08-15) |
| P05789 | picking "All restaurants" from another page says so in the URL (`focus=all`) | read the query builder | ✅ | otherwise the saved drill wins |
| P05790 | the nav drawer closes on a route change | read the `[path]` effect | ✅ | |
| P05791 | the nav drawer closes when the window widens past the breakpoint | read the matchMedia handler | ✅ | |
| P05792 | Manager mode collapses the sidebar at every width, not only on a phone | read the `mmode` class | ✅ | |
| P05793 | the owner panel keeps NO link to `/aevinite` | grep the shell | ✅ | admin is a higher privilege |
| P05794 | rendering the owner panel resets the reconnect backoff | read the `sessionStorage.removeItem` | ✅ | |
| P05795 | the two Coming-soon sections are the only `soon: true` items and they land on a real page | read `GROUPS` against the two routes | ✅ | no dead links |
| P05796 | the Coming-soon page says plainly that it is not built yet | read `ComingSoon`'s footer line | ✅ | |
| P05797 | the Coming-soon pages carry no toggle, no permission and no module of their own | read both files | ✅ | nothing to entitle |
| P05798 | `/owner/marketing` names what it will do in the owner's words, not feature jargon | read the blurb and points | ✅ | |
| P05799 | `/owner/online` names what it will do in the owner's words | read the blurb and points | ✅ | |
| P05800 | neither Coming-soon page fetches anything | grep for `fetch` | ✅ | server components, zero egress |

---

## Watching it run — headless Playwright on port 4112 — P05801–P05875

All of these were driven signed in as `diago1` (French House). Scripts used:
`probe.mjs`, `drive.mjs`, `measure.mjs`, `drill.mjs`, `stuck.mjs` (scratch, deleted after the run —
their contents are described in each row so they can be rebuilt).

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P05801 | port 4112 is MY dev server before any screenshot is trusted | `lsof -nP -iTCP:4112 -sTCP:LISTEN` names the pid I started | ✅ | never 4000 |
| P05802 | `/owner` renders for a real owner with no console errors | load, watch `console.error` | ✅ | none |
| P05803 | `/owner/activity` renders with no console errors | same | ✅ | none |
| P05804 | `/owner/marketing` renders with no console errors | same | ✅ | none |
| P05805 | `/owner/online` renders with no console errors | same | ✅ | none |
| P05806 | ONE sign-in for the whole run (the shared `loginAs` disk cache) | `loginRequestCount()` stays at 0 after the first script | ✅ | MY ASSERTION WAS WRONG, not the code. The shared cache is per-process with a 15-minute disk TTL, so a cold process legitimately signs in ONCE; only a second sign-in inside the TTL would be a fault. Measured: 1 for the first process of the run, 0 for every later one. Well under 5 per 5 minutes, no alert raised |
| P05807 | `/api/owner/overview` answers with exactly one restaurant for this owner | read the payload | ✅ | My Little French House (french-house) |
| P05808 | the overview's `entitlements` map contains a `logs` key | list the keys | ✅ | 33 keys, `logs: true` |
| P05809 | the overview's `entitlements` map does NOT contain an `activity` key | list the keys | ✅ | the key really is absent — that is the point, and the dashboard no longer reads it (problem 2). Re-run: 33 keys, `logs: true`, no `activity` |
| P05810 | at 1280×800 the scrolling element is `.adm-main`, not the window | measure `scrollHeight`/`clientHeight`/`overflowY` on `.adm-main`, `.adm`, `body`, `documentElement` | ✅ | `.adm-main` 2256/751 auto; document 800/800 |
| P05811 | at 360×780 the scrolling element is `.adm`, and `.adm-main` is `overflow-y: visible` | same measurement at the phone viewport | ✅ | `.adm` 4109/780 auto; `.adm-main` 4052/4052 visible |
| P05812 | therefore `window.scrollTo` moves nothing on any owner page, at any width | the same measurement shows the document never scrolls | ✅ | documented so nobody "fixes" the wrong element |
| P05813 | ~~the seven KPI tiles render with the right labels and the right report links~~ → **MOVED:** FIVE tiles, and the report link moved into each tile's popup | read the rendered labels, then open a popup and read its footer link | ✅ | re-run 2026-08-27 on 4212: "Revenue \| Orders \| Today so far \| Expenses \| On hand", all five `<button>`, zero anchors; the Orders popup's footer carried view+range+open |
| P05814 | "Avg order" equals revenue ÷ paid orders on the rendered figures | 47,72,513 ÷ 2478 = 1,925.9 vs the printed ₹1,926 | ✅ | |
| P05815 | the "Today so far" tile carries the live pill and today's order count | read the tile | ✅ | "● live", "22 orders today" |
| P05816 | the eight home cards all render (no card silently missing) | list `.ow2-ct` titles | ✅ | trend, month-vs-last, category, heatmap, payments, records, dishes, activity |
| P05817 | the Recent-activity card fills with real rows for a working restaurant | count `.ow2-act` | ✅ | 6 rows |
| P05818 | those rows read as English, not database codes | read the rendered text | ✅ | "Closed a table that still owed money · table 24", "Merged tables", "Signed in" |
| P05819 | `/owner/activity` opens on the Audit view with real removals | count `.adm-logrow` | ✅ | 201 rows (head + 200) |
| P05820 | going to page 2 of the log puts the reader at the top of the list | scroll the scroller to the bottom, click Next page, measure `scrollTop` | ✅ | 13419 → 0, first row in view. NOTE: this works because the fetch sets rows to `null` first and the list collapses, NOT because of the `window.scrollTo` line in `Pager` (P05812) — do not "fix" that line, and do not remove the collapse-to-Loading |
| P05821 | the pager reports a real total and page count | read the strip | ✅ | "422 entries · page 1 of 3" |
| P05822 | the Audit type chips carry per-type counts over the whole record | read the chips | ✅ | KOT cancelled 374, Payment reverted 13, Bill deleted 10, … |
| P05823 | the chip counts add up to the "All" count | 374+13+11+10+6+4+2+1+1 = 422 vs "All 422" | ✅ | |
| P05824 | the risk strip's money and record counts add up to the same total | 42 + 384 = 426 against the later "All 426" | ✅ | (the record grew between the two runs) |
| P05825 | the visible-money line names the right slice | read "200 records · ₹91,337 in total" against 426 records on the record | ✅ | was ❌. Problem 6 — re-run: "200 records on this page of 442 · ₹90,287 on this page" |
| P05826 | switching to the Activity log view loads staff rows | click the view button, count rows | ✅ | 200 rows, 8,829 entries |
| P05827 | the Activity view's group chips carry counts | read them | ✅ | Printer 1, Sign-in & access 21, Bills & money 22, Orders & kitchen 100, Tables & guests 55, People 1 |
| P05828 | the Activity view says its counts are per page | read the note | ✅ | "Counts are for this page of 8,829 entries." |
| P05829 | the 60s backstop really re-fetches analytics while the tab is active | count `/api/owner/analytics` requests over ~88s of simulated activity | ✅ | 3 → 5 |
| P05830 | the 60s backstop also re-fetches the Recent-activity feed | count `/api/owner/oplog` requests over the same window | ✅ | was ❌: oplog stayed at 1. Problem 4 — re-run: 1 → 2 across one 60s tick |
| P05831 | when Reports are switched off, the home screen does not call it a breakage | replay the server's own 403 `{disabled:true}` on `/api/owner/analytics` and read the page | ✅ | was ❌: a red-bordered "Couldn't load." Problem 5 — re-run in both skins: 0 red banners, one plain muted note |
| P05832 | …and no card is left claiming to load | read the card bodies in that state | ✅ | was ❌: six cards stuck on "Loading…". Problem 5 — re-run: 0 loading, 6 cards saying "Not shown — Reports are switched off" |
| P05833 | …and three KPI values are left blank rather than explained | read `.ow2-kpi .v` | ✅ | was ❌: three blank tiles. Problem 5 — re-run: an em dash and the reason on each. ALSO re-run WITH a snapshot in the browser (problem 12): 0 delta chips, 0 sparklines, 0 charts, 0 insights, and the payroll tiles gone — no stale figure leaks through |
| P05834 | …and the KPI tiles stop linking into a section this owner cannot open | count `a.ow2-kpi` in that state | ✅ | was ❌: 5 dead links. Problem 5 — re-run: 0 |
| P05835 | when the log is switched off, the Recent-activity card does not sit on "Loading…" | replay the server's own 403 on `/api/owner/oplog` | ✅ | was ❌: stuck on "Loading…" with a live See-all. Problems 2 and 3 — re-run: the card is ABSENT and the dish list takes the row |
| P05836 | tapping a dish on the dashboard opens the dish view | click `.rv-dish`, read `.own-dish-name` | ✅ | "Chicken Alfredo Pasta" |
| P05837 | drilling deeper opens at the top of the new view | measure the scroller after the tap at 1280 | ✅ | 0 |
| P05838 | drilling deeper opens at the top of the new view on a phone too | same at 360 | ✅ | was ❌: it landed at 178px. Problem 8 — re-run: 0 |
| P05839 | the phone's BACK returns from the dish view to the dashboard | `goBack()` after the tap at 360 | ✅ | was ❌: BACK left the owner panel. Problem 8 — re-run: BACK lands on the dashboard, and a second BACK from there correctly leaves |
| P05840 | the top-strip breadcrumb offers a way back from the dish view on a phone | read `getComputedStyle('.owx-path').display` at 360 | ✅ | the crumb is still display:none at 360px — that is the shell's layout and not a fault on its own. It stopped mattering once BACK and the drawer's Dashboard link both work (problems 8 and 10). A visible on-screen back control is listed for him as a 🟡 |
| P05841 | the ☰ drawer's "Dashboard" link returns from the dish view | open ☰, click Dashboard, read the level | ✅ | was ❌: still on the dish. Problem 10 — re-run: returns to the dashboard |
| P05842 | re-opening `/owner` returns from the dish view | navigate again, read the level | ✅ | deliberately still on the dish (the refresh-proof drill, owner round-5) — which is why P05839/P05841 matter |
| P05843 | the breadcrumb DOES offer a way back at 1280 | read `.owx-path` display + text | ✅ | `flex`, "Owner › Dashboard › Chicken Alfredo Pasta" |
| P05844 | the crumb's middle segment is a button, not text, when a tail is shown | read the element | ✅ | `button.cr` |
| P05845 | on `/owner/manager` the crumb dispatches an event Manager mode listens for | add listeners, click the crumb, read what fired | ✅ | was ❌: Manager mode heard nothing. Problem 9 |
| P05846 | on a hard load of `/owner/manager` the cockpit pill names the restaurant on the floor | load it, read `.owx-path` while the floor iframe is present | ✅ | WAS ❌ (handoff H1). He said "you can do this 3 also" on 2026-08-18, so it is fixed in `components/owner/OwnerManagerMode.tsx`: the crumb re-emits on the next frame, after the shell's listener is attached. Re-measured on a hard load — crumb "Owner › Manager mode › Burger Barn", pill "Burger Barn", floor loaded |
| P05847 | the skin toggle flips the console to light and back | click it, read `[data-skin]` | ✅ | |
| P05848 | the chosen skin survives a reload | set light, reload, read `[data-skin]` | ✅ | cookie + localStorage |
| P05849 | the skin cookie is rewritten when only localStorage has it | clear the cookie, reload, read it back | ✅ | the reconcile effect does it |
| P05850 | every ink on the owner home is at least 3:1 against its own surface, DARK skin, 1280 | compute contrast for 68 rendered elements | ✅ | 0 under 3:1 |
| P05851 | same, LIGHT skin, 1280 | same | ✅ | 0 under 3:1 — white stays white and stays readable |
| P05852 | same, DARK skin, 360 | same | ✅ | 0 under 3:1 |
| P05853 | same, LIGHT skin, 360 | same | ✅ | 0 under 3:1 |
| P05854 | every ink on the Audit page is at least 3:1, DARK skin, 1280 | compute contrast for 22 rendered elements | ✅ | 0 under 3:1 |
| P05855 | same, LIGHT skin, 1280 | same | ✅ | 0 under 3:1 |
| P05856 | same, DARK skin, 360 | same | ✅ | 0 under 3:1 |
| P05857 | same, LIGHT skin, 360 | same | ✅ | 0 under 3:1 |
| P05858 | the "● live" pill's ink is readable in both skins | measure it specifically | ✅ | TIGHTENED BY THE SECOND PASS. The pill is readable in all FOUR states now, not just the two a link produces: link light 17.74:1, link dark 15.42:1, non-link light 6.35:1 (was 1.92:1 — problem 11), non-link dark 8.3:1. The flat hex only ever looked fine because every tile was an anchor and globals.css forces anchor descendants to inherit. Do not put a flat hex back |
| P05859 | the KPI labels do not break mid-word on a phone | read the rendered labels at 360 | ✅ | "LOST TO CANCELLATIONS" and "TODAY SO FAR" each hold together |
| P05860 | the sparkline never draws through a caption on a phone | read the tile boxes at 360 | ✅ | the 44px reserve holds |
| P05861 | the hero shortcut row wraps rather than overflowing at 360 | read the rendered rows | ✅ | 2 + 1 |
| P05862 | no card runs off the right edge at 360 | compare each card's right edge with the viewport | ✅ | none |
| P05863 | the range dropdown opens and is not clipped by its card | click it, read the popup's box | ✅ | escapes the tile |
| P05864 | picking a range refetches only that range | count analytics requests around the pick | ✅ | one |
| P05865 | picking a range is remembered after a reload | pick "This week", reload, read the button | ✅ | |
| P05866 | Refresh spins, then stops | click it, watch the class | ✅ | `fa-spin` appears then clears |
| P05867 | the Report ▾ menu opens | click it | ✅ | |
| P05868 | the Audit search narrows the visible rows | type a table number, count rows | ✅ | |
| P05869 | the Audit sort select changes the order | pick "Biggest amount first", read the first row | ✅ | |
| P05870 | an Audit row opens its full detail popup | click a row, read the popup heading | ✅ | |
| P05871 | the detail popup closes on the hardware BACK | `goBack()` with it open | ✅ | back-stack layer works |
| P05872 | the Activity severity filter narrows the list | click "Notable", count rows | ✅ | driven: all=200 rows, Notable=14. This filter is server-side, which is why it worked while its neighbours did not |
| P05873 | the Activity group chips narrow the list | click "Bills & money", count rows | ✅ | WAS A FALSE GREEN ON THE FIRST PASS — I marked it ✅ without driving it. Driven: the chip lit up and the list stayed at 200 rows with the same first and last row. Problem 7. Re-run: chip "Sign-in & access 43" narrows 200 → 43 |
| P05874 | nothing in this run wrote to Aangan | grep every script for a restaurant id / slug | ✅ | read-only, French House only, and no row was created at all |
| P05875 | the dev server was stopped when the runtime block ended | `lsof` on 4112 returns nothing | ✅ | stopped and confirmed: `lsof -nP -iTCP:4112` returns nothing after the run |
---

## Screenshots actually read — P05876–P05925

Every row below was judged from an image I opened, not from the DOM.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P05876 | home, desktop 1280×800, DARK — the page answers "how did today go?" without scrolling | read `home-desktop-dark.png` | ✅ | hero + 7 tiles + the trend chart are all above the fold |
| P05877 | home, desktop DARK — the sidebar's active section is obvious | same image | ✅ | Dashboard is filled green |
| P05878 | home, desktop DARK — the hero's middle button agrees with the sidebar | same image | ✅ | was ❌. Problem 1 — one frame, one name |
| P05879 | home, desktop DARK — the 7 tiles lay out 4 + 3, not 5 + 2 | same image | ✅ | one empty column, which is the balanced choice |
| P05880 | home, desktop DARK — the two tiles with sparklines are the two with a trend to show | same image | ✅ | Revenue + Orders |
| P05881 | home, desktop DARK — the delta chips read as up/down, not as decoration | same image | ✅ | "↓50%", "↓38%" with arrows |
| P05882 | home, desktop DARK — the range button and its exact dates are together | same image | ✅ | "Last 30 days" over "19 Jul – 17 Aug (30 days)" |
| P05883 | home, desktop DARK — the age line is present and quiet | same image | ✅ | "updated 4 min ago" |
| P05884 | home, desktop DARK — the revenue chart has a real axis in lakhs, not raw rupees | same image | ✅ | ₹0/₹2L/₹4L/₹6L |
| P05885 | home, desktop DARK — no chart is a lonely single bar | same image | ✅ | 30 days of area |
| P05886 | home, desktop LIGHT — white stays white and every figure stays readable | read `home-desktop-light.png` | ✅ | |
| P05887 | home, desktop LIGHT — the green accent is still green, not washed out | same image | ✅ | |
| P05888 | home, desktop LIGHT — no black-on-black or white-on-white card survives | same image | ✅ | |
| P05889 | home, A35 360×780, DARK — the top bar keeps ☰, scope, connection, skin and sign-out | read `home-a35-dark.png` | ✅ | five controls, none clipped |
| P05890 | home, A35 DARK — two tiles per row, labels intact | same image | ✅ | |
| P05891 | home, A35 DARK — the hero name is not truncated | same image | ✅ | |
| P05892 | home, A35 LIGHT — every tile is white with dark ink | read `home-a35-light.png` | ✅ | |
| P05893 | home, A35 LIGHT — "TODAY SO FAR ● live" holds one line beside its pill | same image | ✅ | the 2026-08-11 fix survives |
| P05894 | home, A35 LIGHT — "LOST TO CANCELLATIONS" holds one line | same image | ✅ | |
| P05895 | home, A35 LIGHT — the Orders tile's two-line caption is not crossed by the sparkline | same image | ✅ | tight but clear |
| P05896 | home, A35 LIGHT — the hero buttons wrap to two rows rather than overflowing | same image | ✅ | |
| P05897 | home, A35 LIGHT — the "Staff & powers" label is wrong here too | same image | ✅ | was ❌. Problem 1 |
| P05898 | Audit, desktop LIGHT — the money/record split is the first thing on the card | read `activity-desktop-light.png` | ✅ | "💸 Money moved · ₹35,998 · 42 times" then "📝 Record only · 384 changes" |
| P05899 | Audit, desktop LIGHT — the type chips are readable and each carries a count | same image | ✅ | 9 chips |
| P05900 | Audit, desktop LIGHT — each row says what, why and who, in a sentence | same image | ✅ | "KOT cancelled · Table 24 · KOT #272 · Bill #257 · ₹399" / "Table closed while the bill was still unpaid (walk-out / written off)" / "diagt1 · tablet" |
| P05901 | Audit, desktop LIGHT — the restaurant name is on every row | same image | ✅ | |
| P05902 | Audit, desktop LIGHT — the visible-money line does not overstate its slice | same image | ✅ | was ❌. Problem 6 |
| P05903 | Audit, desktop DARK — the same card reads as well on the dark skin | read `activity-desktop-dark.png` | ✅ | |
| P05904 | Audit, A35 DARK — the rows stack instead of scrolling sideways | read `activity-a35-dark.png` | ✅ | `aud-stack` doing its job |
| P05905 | Audit, A35 LIGHT — the risk chips wrap rather than clipping | read `activity-a35-light.png` | ✅ | |
| P05906 | Activity log, A35 LIGHT — every row shows its trail on a second line, wrapped not chopped | read `activitylog-a35-light.png` | ✅ | "My Little French House · Sign-in & security › Sign in" |
| P05907 | Activity log, A35 LIGHT — the panel chip is legible at 360 | same image | ✅ | OWNER purple, TABLET blue |
| P05908 | Activity log, A35 LIGHT — the per-page honesty note is visible above the rows | same image | ✅ | "Counts are for this page of 8,829 entries." |
| P05909 | Activity log, A35 LIGHT — the group chips wrap without overlapping | same image | ✅ | ragged but readable |
| P05910 | Activity log, A35 DARK — same, on the dark skin | read `activitylog-a35-dark.png` | ✅ | |
| P05911 | Activity log, desktop DARK — the three-column grid lines up with its header | read `activitylog-desktop-dark.png` | ✅ | Panel / Action / When |
| P05912 | Activity log, desktop LIGHT — same | read `activitylog-desktop-light.png` | ✅ | |
| P05913 | Marketing, A35 DARK — the Coming-soon page is on-brand and says it plainly | read `marketing-a35-dark.png` | ✅ | badge, title, blurb, three points, "Not built yet — it'll show up here when it is." |
| P05914 | Marketing, A35 LIGHT — readable on the light skin | read `marketing-a35-light.png` | ✅ | |
| P05915 | Marketing, desktop DARK — the card is centred, not stretched | read `marketing-desktop-dark.png` | ✅ | |
| P05916 | Marketing, desktop LIGHT — same | read `marketing-desktop-light.png` | ✅ | |
| P05917 | the reports-off state, desktop DARK — what the owner actually sees | read `reports-off-desktop-dark.png` | ✅ | was ❌. Problem 5 — re-read the new screenshot in both skins: a calm note, every card saying so plainly, no phantom chart |
| P05918 | the dish view, desktop DARK — it names the dish, its share and its rank | read `dish-desktop-dark.png` | ✅ | |
| P05919 | the dish view, desktop DARK — the crumb gives a way back | same image | ✅ | "Owner › Dashboard › Chicken Alfredo Pasta" |
| P05920 | the dish view, A35 DARK — is there a way back on the screen? | read `dish-a35-dark.png` | ✅ | was ❌, then ✅ via BACK and the drawer. Now also has a VISIBLE way back: a 44×44 ✕ in the dish header (he asked, 2026-08-18: "have you add the cross button") |
| P05921 | the dish view, A35 DARK — the leaderboard bars are readable at 360 | same image | ✅ | |
| P05922 | no screenshot shows another restaurant's branding on French House | scan every image | ✅ | green theme throughout, no gold/brown leak |
| P05923 | no screenshot shows a raw database word on screen | scan every image | ✅ | no `order_place`, no `editor`, no `db` |
| P05924 | no screenshot shows a number with no unit or currency mark | scan every image | ✅ | |
| P05925 | every screenshot was deleted after being read | `ls` the scratch shots folder at the end | ✅ | both scratch shot folders deleted after being read |
---

## Tracing a change across panels and files — P05926–P05975

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P05926 | the entitlement keys the dashboard reads all exist in `OWNER_SECTION_KEYS` | compare `app/owner/page.tsx` against `lib/ownerEntitlements.ts` | ✅ | was ❌. Problem 2 — the only key it reads is `logs`, which exists |
| P05927 | the entitlement keys the shell reads all exist in `OWNER_SECTION_KEYS` | compare `GROUPS[].ent` against the same list | ✅ | menu, reports, staff, customers, logs, khata_book, inventory, issues, settings, manager_mode — the last two are injected by the layout |
| P05928 | the log page and the log API gate on the SAME key | compare the shell's `ent: "logs"` with `/api/owner/oplog`'s `entitledSubset(…, "logs")` | ✅ | |
| P05929 | the log API's finer cut (removals vs activity) matches the page's two views | compare `logViewSubset` with `audDisabled` / `actDisabled` | ✅ | |
| P05930 | the section label in the sidebar matches the heading of the page it opens | open each of the 11 sections and compare | ✅ | was ❌. Problem 1 |
| P05931 | "Audit & logs" is the same name in the owner panel, the manager panel and the admin console | grep the three | ✅ | one name, three panels |
| P05932 | a removal's KIND word is the same on the owner list and in its own detail card | compare `REMOVAL_KIND` against `KIND_LABEL` in `components/admin/RemovalDetail` | ✅ | the list builds itself FROM the card's map |
| P05933 | a removal's REASON word matches the manager panel's | compare `REMOVAL_REASON` with `REMOVAL_REASONS` in `public/panels/editor/app.js` | ✅ | six reasons, same wording |
| P05934 | the sort orders and type chips come from ONE module shared with the manager and admin | read the `AUDITSORT` import | ✅ | `public/panels/auditsort.js` |
| P05935 | the risk classification is the database's, not the client's | compare `riskTotals` with `lfh_audit_risk` / `lfh_audit_kind_counts` | ✅ | client only falls back |
| P05936 | an action code rendered on the owner home has a label in the shared translator | compare `actLabel`'s map with the actions the API can return | ✅ | `verify:audit` guards this |
| P05937 | a panel name rendered on the owner home has a label in the shared translator | same for `panelLabel` | ✅ | |
| P05938 | the owner home's mini feed and `/owner/activity` describe the same event the same way | compare the two rows for one action | ✅ | both go through `actLabel`/`panelLabel` |
| P05939 | …including how long ago it happened | compare `page.tsx`'s local `timeAgo` with `components/admin/shared`'s | ✅ | WAS ⏭. He approved it on 2026-08-18. The dashboard's local copy is deleted and it uses the shared `timeAgo`, so both screens say "5m ago". The short form won because it was already the wording on twelve screens |
| P05940 | the money figure on the home tile and the same figure in the Reports hub come from one route | compare `/api/owner/reports?type=sales` uses | ✅ | one route, one snapshot |
| P05941 | the restaurant colour is one identity across shell sidebar, top switcher, dashboard table and charts | grep `portfolioColor` | ✅ | four surfaces, one function |
| P05942 | the skin the shell paints and the skin an embedded panel is told are the same value | trace `aevidine_skin` from the shell to `app/owner/manager/page.tsx` | ✅ | cookie for the server, broadcast for the live flip |
| P05943 | the crumb tail contract is the same for all three broadcasting pages | compare the dashboard, reports and manager-mode emitters | ✅ | same event, same shape |
| P05944 | …and each of them handles the shell's crumb click | compare each page's listeners with what the shell dispatches | ✅ | was ❌. Problem 9 |
| P05945 | the shell's crumb listener is attached before any page can broadcast on a hard load | reason about parent/child effect order, then measure on `/owner/manager` | ✅ | was ❌, same root as P05846 — fixed 2026-08-18 |
| P05946 | the dashboard survives that race because its emitter re-runs when `ov` arrives | read the effect deps | ✅ | which is why the dashboard's pill is right |
| P05947 | the reports hub survives it for the same reason | read its effect deps | ✅ | |
| P05948 | the reports hub's scroll save/restore uses the element that really scrolls | read `app/owner/reports/page.tsx` `scroller()` against the 360px measurement | ✅ | WAS ❌ (handoff H2). Fixed 2026-08-18 in `app/owner/reports/page.tsx`: `scroller()` now finds the element that really scrolls. Re-measured on a 360px phone — scrolled to 900, opened a report, came back to 900 (it came back to 0 before) |
| P05949 | the admin console has the same phone-scroller problem and already solved it | read `app/aevinite/restaurants/page.tsx` `port()` / `readTop()` / `writeTop()` | ✅ | that is the pattern my fix copies |
| P05950 | the `?rid` admin pin travels from the console into every owner page and every owner API call | trace it through the shell, the dashboard and the activity page | ✅ | |
| P05951 | the `?as=` person pin travels with it | grep `asSuffix()` / `asValue()` | ✅ | |
| P05952 | an admin tab's pin cannot be repointed by a second tab's cookie | read the pin-over-cookie preference in all three | ✅ | |
| P05953 | the activity page sends BOTH `scope` and `rid`, like the reports page | compare `scopeParams()` with the reports page | ✅ | |
| P05954 | the oplog route honours `rid` only when it is already in scope | read the `inScope` guard | ✅ | narrows, never widens |
| P05955 | ~~the audit route is GET-only~~ → **MOVED:** GET plus the one narrow POST above, and it still always answers `canRestore:false` for an owner | read the route | ✅ | re-run 2026-08-27: the POST reads the restaurant from the ORDER (never the body), checks `inScope`, re-checks the `logs`/`removals` entitlement and refuses an order that is not cancelled. `verify:owner-screen` holds all four |
| P05956 | the offline layer knows about the API families my pages read | check `public/sw.js` `DATA_PATHS` for owner analytics/overview/oplog/audit | ✅ | `/api/owner/` is covered as a family |
| P05957 | my pages issue no writes, so nothing needs the idempotency wrapper or the outbox | grep for non-GET fetches in my five files | ✅ | zero |
| P05958 | …so `lib/clash.ts` has nothing to cover here either | same | ✅ | no silent-overwrite surface |
| P05959 | the three guest menu doors are untouched by anything in my territory | grep for `/menu`, `/r/`, `/q/` | ✅ | |
| P05960 | no migration is needed for anything in this PR | list the fixes | ✅ | all client-side |
| P05961 | no new `settings` column is added | same | ✅ | |
| P05962 | no new permission or module is introduced | same | ✅ | |
| P05963 | no new screen is introduced | same | ✅ | |
| P05964 | `verify:audit` still reads `app/owner/activity/page.tsx` and still passes after my edits | run it | ✅ | `npm run verify:audit` passes |
| P05965 | `verify:skin-ink` still passes for `/owner` after my edits | run it against 4112 | ✅ | `npm run verify:skin-ink -- --base http://localhost:4112` passes |
| P05966 | `verify:xray` still passes | run it | ✅ | `npm run verify:xray` passes |
| P05967 | `verify:owner-home` (the home-restaurant resolver) still passes | run it | ✅ | `npm run verify:owner-home` passes |
| P05968 | `npm run typecheck` passes | run it | ✅ | `npm run typecheck` passes |
| P05969 | `npm run lint` passes | run it | ✅ | `npm run lint` passes (2 pre-existing warnings in this file, neither mine) |
| P05970 | every fix I made has a guard that fails if it is undone | read `scripts/verify-owner-screen.mjs`, then revert each fix and watch it go red | ✅ | re-run + EXTENDED 2026-08-27: **84 checks**, up from 61. All seven of this run's fixes were reverted one at a time and each turned the guard red (3, 2, 4, 3, 3, 2 and 1 check respectively) |
| P05971 | that guard is fast and needs no server or database | run it, time it | ✅ | re-run 2026-08-27: still static file reads only, still sub-second at 84 checks |
| P05972 | the guard is registered in `package.json` as `verify:owner-screen` | read the file | ✅ | re-run 2026-08-27: present, and the guard asserts its own registration (a guard nobody runs looks exactly like a guard that passes) |
| P05973 | I wrote to no file outside my territory except the guard and its `package.json` line | `git status` / `git diff --stat` | ✅ | 5 territory files + the new guard + one package.json line (rules §7 requires the guard) |
| P05974 | nothing in my worktree touched the shared folder | check `git -C` paths used | ✅ | every command ran in `sweep6/T12` |
| P05975 | no AV-live key, folder, repo or database was read or named as a target | review the run | ✅ | |

---

## Judgment — would a real restaurant need it this way — P05976–P06000

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P05976 | at 11pm after service, does the home screen answer "how did today go?" in five seconds | read the desktop screenshot as the owner would | ✅ | "Today so far" with a live pill is the second-row anchor; revenue/orders/avg sit beside it |
| P05977 | is the FIRST number on the page the one he wants first | same | ✅ | WAS ⏭ (a layout decision for him). Decided 2026-08-18: the row is now Revenue · Orders · Today so far · Expenses · On hand, five across in ONE line, with the short money form on the tile face. "Avg order" moved into the Orders popup |
| P05978 | does every tile earn its space | walk the seven | ✅ | five always, two only when payroll is on |
| P05979 | is any tile a number he cannot act on | walk the seven | ✅ | each links to the report behind it |
| P05980 | does the page ever show a figure without saying what period it covers | scan every card | ✅ | a period chip on every chart, a caption on every tile |
| P05981 | does it ever show a total that quietly covers fewer restaurants than it claims | read `restScopeText` uses | ✅ | it names the excluded count |
| P05982 | would he ever have to hunt for what changed since he last looked | read the age line + the activity card | ✅ | after fixing problem 4 the feed is 60s-fresh like everything else |
| P05983 | is "Recent activity" worth a card on the home screen at all | judge it against `/owner/activity` one click away | ✅ | yes — six rows answers "is anything odd happening" without leaving home |
| P05984 | when a section is switched off, does the screen read as OFF rather than BROKEN | drive the reports-off and logs-off states | ✅ | was ❌. Problems 3, 5 and 12 — a switched-off section now reads as off in both skins, with nothing stale behind it |
| P05985 | can he get out of every screen he can get into, on the phone he carries | drive the drill on the A35 | ✅ | was ❌ and it was the worst thing in this territory. Problem 8 |
| P05986 | does the Audit page answer "who took that off, and why?" without a manual | read the rows | ✅ | kind, table, KOT, bill, amount, reason, person, role, time |
| P05987 | does it keep money he lost apart from money he never charged | read the risk strip | ✅ | the owner's own 2026-08-13 rule, honoured |
| P05988 | can he find last Saturday | use the pager | ✅ | 3 pages of removals, 45 of activity |
| P05989 | can he find the printer's rows without knowing a magic word | use the group chips | ✅ | "🖨 Printer 1" |
| P05990 | does a number on this page ever contradict a number 400px away | compare the risk strip, the chips and the visible-money line | ✅ | was ❌ (the audit money line, problem 6 — fixed). RE-RUN 2026-08-18 after HIS catch and it failed again, wider: the dashboard's "lost to cancellations" quoted the money rollup (1,124 / ₹8,28,096) while the Audit one click away said 394 / ₹1,85,766 and classified the same event `record`, not `money`. Two screens, one event, three-times apart. Fixed by problem 13: the dashboard quotes NO cancellation figure at all and links to the record instead. Re-measured in both skins — not one cancellation word or amount on the page |
| P05991 | is anything on these screens written for a developer rather than a restaurant owner | read every string | ✅ | after improvement 1, nothing |
| P05992 | is any empty state a dead end | walk all nine | ✅ | after improvement 1 |
| P05993 | would a busy service be slowed by anything my pages do | count requests per minute | ✅ | overview + 3 payloads + 6 log rows, all capped, all 60s |
| P05994 | would leaving the Audit page open all evening cost anything it should not | count its polls | ✅ | was ❌. Improvement 2 |
| P05995 | is the Coming-soon page better than hiding the section | judge it | ✅ | a complete menu with no dead link, and it says plainly it is not built |
| P05996 | should Marketing or Online be built as part of this sweep | judge the scope | ⏭ | no — a new module needs a migration, a screen and a permission (rules §6). Not touched |
| P05997 | is there anything here the owner has already said no to | re-read `docs/REJECTED-IDEAS.md` | ✅ | only R20, and it is respected |
| P05998 | did I change any behaviour he deliberately chose | review every diff line | ✅ | the refresh-proof drill, dark-first, the "Owner overview" chip and the record strip's own window are all untouched |
| P05999 | is any fix here bigger than the fault it repairs | review every diff line | ✅ | 11 problems, ~150 lines total, no rewrite |
| P06000 | is the honest headline "this part is clean" or "this part had real faults" | count | ✅ | 12 problems found and fixed in the sweep, then his review on 2026-08-18 added a 13th (the tile row's scope + period bug) and turned the 3 handoffs and 2 of the 🟡 items into work. All done, all measured. NOT clean at the start — which was the honest answer — and finished now |

---
---

# SWEEP #7 — NEW CHECKS · P20601–P21100

**Written and run by terminal 12 of 40 on 2026-08-27**, against `origin/main` **d7af8f63**, in
worktree `wt-s7-t12`, branch `sweep7/t12-owner-home`, dev server **4212** (`lsof` confirmed the pid
was mine before anything below was trusted), signed in ONCE as the French House diag owner
(`diago1`, one restaurant). Both skins via `aevidine_skin`, 1280×800/950 and 360×780.

Nobody handed this terminal a checklist — the 500 below were designed after reading all 3,780 lines
of the territory, and they deliberately concentrate on the ground the sweep-#6 ledger does NOT
cover: the tile-popup system and the money model behind it (built 2026-08-18, after that pass), the
owner's one write on the removals record (2026-08-19), the admin breadcrumb rework (2026-08-26), the
`partial` keys, the actor column, and the failure branches of every read.

**Budget:**

| share | phases | what it is |
|---|---|---|
| 35% | P20601–P20775 (175) | reading the code for correctness |
| 15% | P20776–P20850 (75) | conformance to the project's own rules |
| 18% | P20851–P20940 (90) | watching it run (headless Playwright on 4212) |
| 10% | P20941–P20990 (50) | screenshots actually opened and read |
| 12% | P20991–P21050 (60) | tracing a change across panels and files |
| 10% | P21051–P21100 (50) | judgment: would a real restaurant want it this way |

`result`: ✅ pass · ❌ real problem (fixed in this PR unless the note says otherwise) · ⏭ skipped,
with the reason in the note.

**Result: 12 numbered problems, all 12 fixed in this branch, each with a guard that was PROVED to go
red when its own fix is reverted.** `scripts/verify-owner-screen.mjs` grew from 61 checks to 90.

---

## Reading the code for correctness — P20601–P20775

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P20601 | `tileDetail()` returns null for every value of `tileOpen` that is not one of the five known tiles | read the switch's `default` | ✅ | `default: return null`, and the caller renders nothing — a tile name that does not exist cannot draw an empty sheet |
| P20602 | the five `tileOpen` values are exactly the five tiles rendered | compare the union type with the five `setTileOpen(...)` call sites | ✅ | revenue / orders / today / expenses / onhand, five and five |
| P20603 | the popup is registered with the back-stack manager | read `useBackClose("owner-kpi-tile", …)` | ✅ | one layer, keyed on `!!tileOpen`, self-noops when closed |
| P20604 | …and its Escape listener is removed when it closes | read the effect's cleanup | ✅ | `return () => document.removeEventListener("keydown", onKey)` |
| P20605 | the popup can be closed four ways — the ✕, the backdrop, Escape and the phone's BACK | read all four handlers | ✅ | driven, all four; see P20866–P20869 |
| P20606 | opening a popup costs ZERO extra requests — every figure is already in hand | read the memo inputs, then count requests around a tap | ✅ | driven: 0 `/api/owner/*` calls on open |
| P20607 | the popup's heading says WHOSE numbers these are before any figure is read | read the `who` line | ✅ | `My Little French House` for a single owner, `all N restaurants` for the group view |
| P20608 | `who` never renders `undefined` when the overview has not landed | read the `?? "this restaurant"` fallback | ✅ |  |
| P20609 | a popup row's `isTotal` is EXPLICIT, never "the last row" | read the tuple's 4th slot | ✅ | deliberate: on the Revenue popup the LAST row is "Discounts given", which is emphatically not a total |
| P20610 | …and only two popups declare a total at all | count the `true` flags | ✅ | Expenses and On hand — the two that actually add up. Driven: 1 `.r.last` in each, 0 in the other three |
| P20611 | the Revenue popup's first line says what the figure already excludes | read the hint | ✅ | "after discounts, cancelled bills never counted" |
| P20612 | the Revenue popup quotes NO cancellation figure | grep the revenue case for a cancellation amount | ✅ | owner, 2026-08-18. The note explains WHY and sends him to the record instead |
| P20613 | …and that note's link is gated on the same `logs` entitlement the sidebar uses | read `d.audit && ov?.entitlements?.logs !== false` | ✅ | with the log switched off the sentence stands alone rather than pointing at a door that refuses him |
| P20614 | the discount line only appears when a discount was actually given | read the `mt && mt.discount > 0` guard | ✅ | never a ₹0 row |
| P20615 | …and calls a discount money, because it is | read the hint | ✅ | "money you gave away — already taken off the revenue above". `lfh_audit_risk` agrees: `discount_given` is MONEY, `order_cancelled` is RECORD |
| P20616 | the Orders popup's "Still open" can never be negative | read `Math.max(0, orders − paidOrders)` | ✅ |  |
| P20617 | …and its "Average per paid order" names its own divisor | read the hint | ✅ | "revenue ÷ paid orders" — so it can never be confused with the table's "Avg / order", which divides by all |
| P20618 | the Today popup says it does not follow the period dropdown | read the note | ✅ | "This one does not follow the period above — it is always today" |
| P20619 | …and its "See the full detail" link now agrees with that sentence | read `detailHref`'s daysummary branch | ✅ | was ❌: the link carried `range=30d`. Problem 10 — re-run: `range=today` |
| P20620 | the Today popup's average divides by today's orders, guarded against zero | read `todayOrd ? todayRev / todayOrd : 0` | ✅ |  |
| P20621 | the Today popup's "Tables open now" reads the OVERVIEW, not a second live count | read the expression | ✅ | the analytics route stopped computing open tables on purpose (2026-08-06); this is the payload that already has it |
| P20622 | the Expenses popup's staff-pay hint pluralises both the payments and the people | read the two ternaries | ✅ | "1 payment to 1 person" / "4 payments to 3 people" |
| P20623 | the Expenses popup's food line tells THREE states apart — unread, none, and a real figure | read the three-branch hint | ✅ | null = unread, 0 rows = "every cancellation was caught before the kitchen started" |
| P20624 | the Expenses note says a discount is NOT an expense, and why | read the note | ✅ | "your revenue already has it taken off" — the double-count this money model exists to avoid |
| P20625 | …and that a cancelled bill's value is not one either | read the note | ✅ | "nothing was charged for it, so it was never money you had" |
| P20626 | the On hand popup shows the subtraction line by line rather than only its answer | read the rows | ✅ | Revenue → Less staff pay → Less food made then binned → Money on hand |
| P20627 | …and admits when one of those costs could not be READ | read the `foodUnread` hints | ✅ | was ❌: a flat "− ₹0" under a headline "Money on hand". Problem 5 |
| P20628 | the On hand note says plainly it is not a bank balance | read the note | ✅ | names rent, bills and unrecorded stock as things it does not include |
| P20629 | `expensesOut` is staff pay PLUS food lost, and nothing else | read the expression | ✅ | a discount and a cancellation are already out of revenue (mig 315) — adding them here would count the loss twice |
| P20630 | `onHand` is revenue minus that, so the three tiles reconcile on screen | read the expression | ✅ | driven: ₹13,42,142 − ₹1,100 = ₹13,41,042, and the tile face read ₹13.4L |
| P20631 | `foodLost` reads the ingredient cost, never the cancelled bill's value | read the type comment and the route | ✅ | mig 337 writes a `food_loss` expense at what the ingredients cost; the bill value is revenue never earned |
| P20632 | the popup's footer link is a `<Link>` with NO onClick that closes the popup first | read the JSX and its comment | ✅ | closing it on the same tap makes backStack rewind with `history.go(-1)`, which wins the race against the router — the identical trap OwnerShell documents for its nav links |
| P20633 | …and when Reports are off, the footer is a plain sentence rather than a dead link | read the `reportsOn` ternary | ✅ | `<span className="full off">Reports are switched off for this restaurant</span>` |
| P20634 | `detailHref` sends `view`, a SEPARATE name from `rid` | read the builder and its comment | ✅ | `rid` is the admin's authorisation pin and must keep meaning that, or a tab could re-scope its own permissions |
| P20635 | `detailHref` sends `view=all` from the all-restaurants view | read `activeRid ?? "all"` | ✅ | his 2026-08-18 bug: every tile used to land on the admin's pinned restaurant |
| P20636 | `detailHref` carries the person pin when there is one | read `asValue()` | ✅ |  |
| P20637 | the popup sheet is `position: fixed; inset: 0`, so no card can clip it | read `.ow2-tile-wrap` | ✅ | which is why the KPI card no longer needs `overflow: visible` — see P05677 |
| P20638 | the sheet scrolls internally rather than growing past the viewport | read `max-height: min(88vh, 720px); overflow-y: auto` | ✅ | driven at 360×780: the Revenue sheet measured x=18 right=342/360 bottom=670/780 |
| P20639 | the total row is marked by a border and weight, not by colour alone | read `.ow2-tile .r.last` | ✅ | a 2px accent top border, 800 weight, larger figure |
| P20640 | the sheet's ✕ is a real button with an aria-label | read the JSX | ✅ | `aria-label="Close"` |
| P20641 | all five tiles print an em dash and the reason when the server has refused this scope | read the five `v=` expressions | ✅ | was ❌ for Today so far. Problem 2 — re-run: 5 of 5 |
| P20642 | …and none of them opens a popup in that state | read the five `onOpen` guards | ✅ | was ❌ for Today so far. Problem 2 |
| P20643 | …and the "● live" pill goes with it | read `pill={offNote ? undefined : "● live"}` | ✅ | a live pill over an em dash points at nothing |
| P20644 | `loading` is false once `offNote` is set, so no tile animates a blank for ever | read `loading={!offNote && !kMain}` | ✅ | five tiles, five guards |
| P20645 | the tile face uses the SHARED short money form, not a private one | read `compactINR` | ✅ | `lib/money` — a private copy once went to crores while the axis beside it stopped at lakhs |
| P20646 | `compact` is only applied to MONEY tiles, never to a count | compare the `compact` props | ✅ | Orders prints `1,956`, not `2.0k` |
| P20647 | a tile with no sparkline carries no empty 34px band | read the `ow2-nospark` class | ✅ | three of five |
| P20648 | the spark reserve rule is written with three classes so it beats `.owx .adm-stat` | read the selector | ✅ | `.owx .adm-stat.ow2-kpi` — the 2026-08-05 cascade lesson, intact |
| P20649 | `sparkOf` returns undefined under two points, so no line is drawn from one number | read the `pts.length >= 2` gate | ✅ |  |
| P20650 | the delta chip is absent when there is no previous period to compare with | read `delta={kMain?.prev ? … : undefined}` | ✅ |  |
| P20651 | the Expenses caption tells four states apart | walk the ternary ladder | ✅ | both costs / food only / food-figure-unread (new, problem 5) / staff only / nothing recorded |
| P20652 | `hasPayroll` is derived from the payload, not from a feature flag the client guesses | read `!!kMain?.staffPay` | ✅ | null = the restaurant does not have the module, decided server-side |
| P20653 | `mt` is undefined rather than the string "err" when the money read failed | read the cast | ✅ | so `mt.discount` can never be read off a string |
| P20654 | `kpiOf` prefers `paidOrders` and falls back to `orders` | read the return | ✅ | an older payload without the field still renders |
| P20655 | `kpiOf` group scope sums `paymentMethods` orders for its paid count | read the reducer | ✅ |  |
| P20656 | `kpiOf` never divides by zero | read `paidOrders ? revenue / paidOrders : 0` | ✅ |  |
| P20657 | `todayRev`/`todayOrd` fall back to the GROUP totals when no restaurant is drilled | read the two ternaries | ✅ |  |
| P20658 | `offSub` is one constant, so the five tiles cannot word the same state five ways | read the const | ✅ | "Reports are switched off" |
| P20659 | `offNote` is scoped — a refusal about one restaurant does not blank the group view | read `offScope.scope === scopeKey` | ✅ | on an estate where one restaurant's Reports are off, returning to the group view fires no fetch, so a scope-less flag would never clear |
| P20660 | `pl()` returns undefined for a refused scope, so no SNAPSHOT figure leaks through it | read the early return | ✅ | the 2026-08-17 second-pass fix; re-driven with a snapshot in the browser — 0 deltas, 0 sparks, 0 charts |
| P20661 | `moneyOf()` does the same | read the `offNote ? undefined :` guard | ✅ |  |
| P20662 | a `disabled` answer sets `landed`, so the age line stops saying "your last view" | read the branch | ✅ |  |
| P20663 | …and does NOT drop the connection pill to a warning | read that `reportRealtime("weak")` is not reached | ✅ | a permission is not a network problem |
| P20664 | …and is never printed inside the red "Couldn't load." card | read the two separate renders | ✅ | `err` and `offNote` are different states with different cards |
| P20665 | a real error IS printed in the red card, and is not swallowed | read the `err` render | ✅ | never hide an error |
| P20666 | `errText` pulls the human parts out of a thrown plain object | read the function | ✅ | never "[object Object]" |
| P20667 | …and truncates a JSON fallback at 200 characters | read the slice | ✅ | a whole payload cannot land in the banner |
| P20668 | the switched-off card is muted, not red | read the inline style | ✅ | nothing is broken; the owner's next move is to ask Aevidine, which the sentence says |
| P20669 | `loadNote` is one string, so every card says the same thing in that state | read the const and its six uses | ✅ | driven: 6 cards said "Not shown — Reports are switched off", 0 said "Loading…" |
| P20670 | `DishList` takes that same note rather than hard-coding "Loading…" | read the `note` prop | ✅ |  |
| P20671 | the dashboard reads the server's `partial` key for records | read `a.partial.includes("records")` | ❌ → fixed | the server has sent it since 2026-08-12 and NOTHING read it. Problem 4 |
| P20672 | …and holds the flag per RESTAURANT, not per payload | read `recsUnread` keyed by rid | ✅ | `records=1` rides on one request per restaurant, so a payload-level flag would flicker away with the range dropdown |
| P20673 | …and clears it when a later read succeeds | read the `setRecsUnread(false)` branch | ✅ |  |
| P20674 | the Your-records card appears when the read failed, even with nothing to show | read the render guard | ✅ | was ❌. Problem 4 |
| P20675 | …in wording written for ONE restaurant | read the `msg` prop | ✅ | the group sentence "Some restaurants didn't answer" is untrue of a single restaurant's own records |
| P20676 | `PartialStrip` still renders nothing when there is nothing partial | read the early return | ✅ | adding `msg` did not change that |
| P20677 | each `PartialStrip` is filtered to the key its own card is about | read the four `.filter(k => k === …)` | ✅ | categories / busyHours / payments in group scope, busyHours in restaurant scope |
| P20678 | …and the restaurant scope needs no categories or payments strip | read which keys `/api/owner/analytics` can emit in that branch | ✅ | the restaurant branch only ever adds `busyHours` and `records`, so there is no silent gap |
| P20679 | the records read stays OUTSIDE the snapshot cache | read the route's comment and placement | ✅ | it is unbounded — naming it in `partial` cannot poison a stored payload |
| P20680 | the all-time records scan is asked for ONCE per restaurant per visit | read the ask-time ref | ❌ → fixed | the guard read a cache only filled once a request had ANSWERED, so the main-range and month payloads both carried `records=1`. Problem 9 — re-measured: one per open |
| P20681 | …and the ask-flag clears when the read failed, so a retry is possible | read `recsAsked.current.delete(rid)` | ✅ |  |
| P20682 | a failed records read leaves any PREVIOUS records in place rather than blanking them | read `if (a.records)` | ✅ | stale-but-labelled beats gone-with-no-reason |
| P20683 | the records strip names its own rolling window rather than borrowing the dropdown's | read the two "LAST 30 DAYS (ROLLING)" captions | ✅ | the 549-vs-529 fault stays fixed |
| P20684 | "BIGGEST BILL" says "one sitting" when the table is unknown | read the fallback | ✅ |  |
| P20685 | "REGULARS" is drawn only above zero | read the `> 0` guard | ✅ |  |
| P20686 | `records.fastHour` prints its hour in 12-hour form with upper-cased AM/PM | read `istWall12` | ✅ |  |
| P20687 | `records.bestDay` prints a weekday, so "beat it" means something | read the format options | ✅ | "Sat, 2 Aug — beat it!" |
| P20688 | the records card is restaurant-scope only, never drawn for the group | read where it sits in the JSX | ✅ | `lfh_owner_records` takes one restaurant id |
| P20689 | `recs` is keyed by restaurant, so drilling between two never shows the wrong one's records | read the map key | ✅ |  |
| P20690 | `recordsUnread` is likewise keyed by restaurant | read the derivation | ✅ |  |
| P20691 | the removals page's only non-GET call is the food-made answer | grep every `fetch(` in the file | ✅ | one POST, body `{order_id, made}` |
| P20692 | …and it carries the scope params, so an admin tab stays pinned | read `scopeParams()` on the POST | ✅ |  |
| P20693 | each row's answer buttons hold their OWN busy state | read `MadeAnswer`'s local `busy` | ✅ | one shared flag would grey out every row on the page |
| P20694 | a second tap while saving cannot start a second write | read `if (busy !== null) return` | ✅ | and the button reads "Saving…", so the tap is not dropped in silence |
| P20695 | a failed answer says so ON THE ROW | read the `failed` state | ✅ | "Didn't save — try again" |
| P20696 | …and at the PAGE level too | read `answerErr` | ✅ | a switched-off section and an order that is no longer cancelled are both real answers |
| P20697 | the answer strip stops its click from reaching the row underneath | read `onClick={(e) => e.stopPropagation()}` | ✅ | driven with a REAL mouse click on the label: 0 dialogs opened |
| P20698 | answering reloads the record so the row's tag matches the answer just given | read `setRemovals(null); await loadAuditRef.current()` | ✅ |  |
| P20699 | …through a REF, so the row handlers are not rebuilt on every page or filter change | read `loadAuditRef` | ✅ |  |
| P20700 | the answer buttons only appear on a cancellation that has an order id | read the `r.kind === "order_cancelled" && r.order_id` guard | ✅ |  |
| P20701 | `made` is read as a TEXT scalar out of meta, never a whole order snapshot per row | read the route's column list | ✅ | `made:meta->>made` — three states, one string |
| P20702 | …and the client maps "true"/"false"/absent to true/false/null without guessing | read the two ternaries | ✅ | null is a real state: nobody has answered yet |
| P20703 | an unanswered cancellation is marked, not silently assumed | read the tag colours | ✅ | `unanswered` renders in the warn colour; driven, "Not answered yet" on the live rows |
| P20704 | the buttons meet the tap-target floor | read `minHeight: 32` and measure | ✅ | driven: 32px tall, and they sit in a wrapping flex row so neither is clipped at 360px |
| P20705 | the two buttons say what each ANSWER means, not just yes and no | read the titles | ✅ | "it counts as a loss and the ingredients stay used" / "the ingredients go back into stock" |
| P20706 | the currently-recorded answer is shown as the selected button | read the `made === v` styling | ✅ | danger tint for cooked, ok tint for never started |
| P20707 | the label changes once answered | read the `made === null` ternary | ✅ | "Was the food made?" → "Food made?" |
| P20708 | the route reads the restaurant from the ORDER, never from the body | read the route's select | ✅ | narrow, then check scope — never widen |
| P20709 | …and re-checks the section entitlement on every POST | read `logViewSubset(await entitledSubset([rid], "logs"), "removals")` | ✅ | hiding a page is never the only guard |
| P20710 | …and refuses an order that is not cancelled | read `status !== "cancelled"` | ✅ |  |
| P20711 | …and still answers `canRestore:false` | read the route | ✅ | putting a bill back is Aevidine's alone (owner rule, 2026-08-04) |
| P20712 | there is no restore path anywhere in the route | grep `lfh_restore` / `p_restore` / `restore_removal` | ✅ | zero hits; `verify:owner-screen` asserts it |
| P20713 | `removal_classified` rows are excluded from the LIST | read the route's `.neq` | ✅ | the answer is not itself a removal |
| P20714 | …and from the chip COUNTS, so no chip filters to nothing | read the counts filter | ✅ | the "never a 0 chip to tap" rule |
| P20715 | a cancellation answer writes a `food_loss` expense only when the food WAS made | read mig 337's rule via the route | ⏭ | not driven — answering writes real rows on the shared dev database, and this run would be changing another terminal's fixture. Code-read only; a later session with the DB to itself should drive it and delete the rows it creates by id |
| P20716 | …and that expense is what reaches the owner's Expenses tile | trace `category = "food_loss"` from mig 337 to `foodLossExpense()` | ✅ | one category, one read, one tile |
| P20717 | the tags on a row come from the shared module, not a local map | read `AUDITSORT.tagsOf` | ✅ | so a row wears one set of words on all three panels |
| P20718 | …and the page survives an older `auditsort.js` that has no `tagsOf` | read the `AUDITSORT.tagsOf ? … : []` guard | ✅ |  |
| P20719 | the tag colours are semantic, not decorative | read the four branches | ✅ | loss = danger, no-loss = ok, unanswered = warn, everything else = accent |
| P20720 | a row with no tags renders no empty strip | read the `if (!tags.length) return null` | ✅ |  |
| P20721 | a bare database id is never printed where a person's name goes | read `actorLabel` | ❌ → fixed | measured live: "Handled a rating · c0af7b5b-c0d8-40f6-b831-f475e48bab53" on the home screen. Problem 3 |
| P20722 | …on the dashboard's mini feed | read the `.who` cell | ✅ | driven with the row forced into the top six: cell = "—" |
| P20723 | …on the removals row's "by whom" line | read the cell | ✅ |  |
| P20724 | …and on the activity row's inline actor | read the `!actorIsRawId(a.actor)` guard | ✅ |  |
| P20725 | the id stays traceable in the cell's tooltip | read `actorTitle` | ✅ | driven: "Recorded without a name — the panel logged an internal reference (c0af…ab53)." |
| P20726 | the id-detector is ANCHORED, so a name that merely contains a uuid is left alone | read the regex | ✅ | `^…$` |
| P20727 | an activity row whose actor is an id still shows its table number | read the widened `(isPin || !a.actor || actorIsRawId(a.actor))` condition | ✅ | otherwise the row would lose two facts instead of one |
| P20728 | nothing was invented — no name is guessed for an id | read `actorLabel` | ✅ | it returns the same em dash a genuinely nameless row already uses |
| P20729 | the helper lives in its own file, not in the console's shared module | read where it sits | ✅ | `components/admin/shared` is eleven other screens' file; this is a two-screen display floor |
| P20730 | a removal kind nobody named still reads as English | read `humanKind` | ❌ → fixed | the top row read "• customer_erased · Guest ending 1601". Problem 11 |
| P20731 | …in the CHIP strip as well as the row | read `labelsWith` and `kindCountsFrom`'s arguments | ❌ → fixed | the first half of the fix missed the chips — seen on a 360px screenshot. Problem 11, part two |
| P20732 | …and in the search, so a chip's words and the search's words agree | read `kindLabel: KINDS` | ✅ |  |
| P20733 | …and in the "· <kind>" line under the search | read the expression | ✅ | driven: "2 records · Customer erased" |
| P20734 | the mini feed never prints a raw action code | read `actLabel(a.action)` | ✅ |  |
| P20735 | …nor a raw panel name | read `panelLabel(a.panel)` | ✅ |  |
| P20736 | the activity row does the same | read both call sites | ✅ |  |
| P20737 | the removal REASON is rendered through a map, never raw | read `REMOVAL_REASON[r.reason_code] || r.reason_code` | ✅ | six reasons, and the fallback is the code — but every code the app writes is in the map (checked against `public/panels/editor/app.js`) |
| P20738 | a removal with no reason says so in words | read the `|| "no reason recorded"` fallback | ✅ |  |
| P20739 | `trailOf` supplies the row's second line, and `logTrail` has an entry for every action the owner can see | spot-check `rating_handled` and `customer_erase` | ✅ | both present — `Guests › Ratings` and `Guests › Guest list` |
| P20740 | the trail WRAPS to two lines rather than being chopped | read the line-clamp | ✅ | the A35 "Sign–" fault stays fixed |
| P20741 | the Activity log's empty answer is told apart from an empty RECORD | read the zero-rows branch | ❌ → fixed | the search and the severity chips are server-side, so narrowing to nothing landed on a branch worded "No staff activity yet" over 8,829 entries. Problem 1 |
| P20742 | …and the search state offers a way to clear it | read the button | ✅ | the removals half beside it has always had one |
| P20743 | …and the severity state names WHICH severity and offers Show all | read the branch | ✅ |  |
| P20744 | the client-side group/sort empty state is still separate from both | read the `!list || list.length === 0` branch | ✅ | three server-side facts and one client-side fact, four sentences |
| P20745 | a group chip that vanished after a reload falls back to All | read `activeGroup` | ✅ | the activity chips are counted from the page in hand, so this fallback is genuinely needed here |
| P20746 | …while the removals half needs no such fallback, and no longer claims one | read the comment on `activeKind` | ✅ | its chips come from the server's whole-record counts, so All is always offered. Problem 8 — the comment used to describe a fallback that does not exist |
| P20747 | the removals type filter travels to the SERVER | read `kind` → the query | ✅ | driven: `/api/owner/audit?kind=order_deleted`. A chip that counts every page must filter every page |
| P20748 | …and resets to page 1 | read `pickAudKind` | ✅ | driven: narrowing from page 2 landed on one page of 9 |
| P20749 | the activity search and severity likewise reset to page 1 | read the effect on `[qDebounced, level]` | ✅ |  |
| P20750 | the search is debounced at 300ms | read the timer | ✅ | typing does not fire a request per keystroke |
| P20751 | the pager clamps a requested page into range | read `Math.min(Math.max(1, p), pages)` | ✅ |  |
| P20752 | the pager's Prev/Next are disabled at the ends | read the `disabled` props | ✅ | never a tap that does nothing |
| P20753 | the pager elides long runs | read the `nums` loop | ✅ | 1 … 4 5 6 … 62 |
| P20754 | the pager names the TOTAL as well as the page | read the strip | ✅ | driven: "749 entries · page 1 of 4" |
| P20755 | paging returns the reader to the top of the list | drive it and measure the real scroller | ✅ | driven: 22,483 → 0. It works because the fetch collapses the list to Loading first, NOT because of the `window.scrollTo` line — that line moves nothing on this console (P05812) |
| P20756 | …so the `window.scrollTo` line is dead code, and is documented as such | read P05820's note | ✅ | left in place deliberately: removing it is a change with no benefit and a risk of someone "restoring" it wrongly. Logged as an idea, not a fix |
| P20757 | both lists use the SAME pager component | read both `<Pager>` uses | ✅ | one behaviour, two halves |
| P20758 | both lists opt into the phone stacking class | read `aud-stack` on both wrappers | ✅ |  |
| P20759 | the removals money line names the slice it is totalling | read the expression | ✅ | driven: "200 records on this page of 749 · ₹96,019 on this page" |
| P20760 | …and drops the "on this page" wording when there IS only one page | read the `pages > 1` ternaries | ✅ | driven after narrowing to Bill deleted: "9 records · Bill deleted · ₹23,646 in total" |
| P20761 | the risk strip covers the WHOLE record, from the database | read `riskTotals` from `counts` | ✅ | driven: 73 money + 676 record + 2 data = 751, matching the All chip |
| P20762 | …and never adds a cancelled KOT's value to money actually lost | read the money/record/data split | ✅ | the owner's 2026-08-13 rule |
| P20763 | …and the classification is the DATABASE's, not the client's | read the `c.risk` preference | ✅ | the shared `riskOf` is only a fallback for an older database |
| P20764 | the guest-data chip only appears when there is guest data to report | read the `> 0` guard | ✅ | driven: "🧹 Guest data erased · 2" |
| P20765 | the risk strip is absent when there are no counts at all | read `hasRisk` | ✅ |  |
| P20766 | the chip strip is absent when there is only one kind | read `chips.length > 1` | ✅ | never a single chip that filters to itself |
| P20767 | the removals search matches the WORDS on screen, not the codes underneath | read `AUDITSORT.view`'s `kindLabel`/`reasonLabel` arguments | ✅ | so searching "deleted" finds the rows the screen calls Bill deleted |
| P20768 | a view the admin switched off is ABSENT from the switcher | read the `!audDisabled` / `!actDisabled` gates | ✅ |  |
| P20769 | …and the page falls over to the other view by itself | read the `setView` in each disabled branch | ✅ |  |
| P20770 | …and says so in one plain sentence when BOTH are off | read `bothOff` | ✅ |  |
| P20771 | …and the endpoints refuse too | read the two 403 + `disabled` answers | ✅ | hiding is never the only guard |
| P20772 | both views load once up front, so the page knows which the owner even HAS | read the two mount effects | ✅ | driven: one `/audit` and one `/oplog` on arrival |
| P20773 | the 60s backstop refreshes only the view on screen | read `refreshView` | ✅ | not both |
| P20774 | …and does NOT collapse the list to Loading while it does | compare with the mount effects | ✅ | only a page/filter change collapses; an auto-refresh swaps rows in place |
| P20775 | a resolved error row is struck through and dimmed rather than removed | read the styles | ✅ | never hide an error |

---

## Conformance to the project's own rules — P20776–P20850

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P20776 | every overlay in my territory registers with the back-stack manager | grep `useBackClose` | ✅ | 6 in `page.tsx`, 3 in `OwnerShell.tsx`, and the two row popups use the shared modal hook |
| P20777 | nothing in my territory hand-rolls `pushState` / `popstate` | grep both across all six files | ✅ | zero hits |
| P20778 | the newest overlay — the tile popup — was registered the moment it was built | read its layer, then drive BACK on it | ✅ | driven at 360: BACK closed the sheet and left the address on `/owner` |
| P20779 | no poll in my territory is faster than the 60s backstop | grep every interval argument | ✅ | 60000 in the dashboard and the shell, 60_000 in Audit & logs |
| P20780 | every poll is activity-gated through the console's shared hook | grep `useActiveAutoRefresh` | ✅ | three call sites, no bare `setInterval` left |
| P20781 | …so an unattended tab stops asking after two minutes and wakes instantly on return | read the shared hook | ✅ | the 2026-08-17 improvement, still in place on all three |
| P20782 | …and each tick is jittered, so ten devices do not share one beat | read the hook | ✅ | ±20% |
| P20783 | every read in my territory is scoped and capped | list the five endpoints and their arguments | ✅ | overview (owner's own), analytics (`rid` + `range`), reports (`type=sales`), oplog (`limit=6` on the dashboard, paged on the log), audit (paged) |
| P20784 | the mini feed asks for six rows, not a page | read `limit=6` | ✅ | the cheapest read on the dashboard |
| P20785 | one dashboard open costs SEVEN owner-API calls and no duplicates | count them over 9 seconds with no interaction | ✅ | driven: overview, main range, money, month, oplog, + the two-call pre-warm of the last-used range. Zero repeated URLs |
| P20786 | …and the unbounded one is paid for once | count `records=1` | ✅ | was ❌ — twice. Problem 9 |
| P20787 | the pre-warm warms ONE extra range, not seven | read the `others` array | ✅ | the 2026-08-06 egress fix intact |
| P20788 | …once per scope per visit | read `warmedScopes` | ✅ |  |
| P20789 | …and its timers are cleared on unmount | read the effect return | ✅ |  |
| P20790 | analytics figures come from the compute-on-view snapshot cache | read `cachedAt` and the route | ✅ | never recomputed per open |
| P20791 | Refresh is the only thing that forces a recompute | grep `refresh=1` | ✅ |  |
| P20792 | there is no blind cron behind any of these numbers | grep for scheduled work in my files | ✅ | view-triggered only |
| P20793 | my pages issue exactly ONE write, and it is the food-made answer | grep every non-GET fetch | ✅ | so `withIdempotency` and the staff outbox have one narrow surface here, not none — see P20794 |
| P20794 | …and that write is not wrapped in the panel `api()` helper | read the raw `fetch` | ❌ (reported, not fixed) | the owner console does not use the panel outbox at all — its own `api()` layer is the panel's, and no owner screen goes through it. So an answer given with no signal is simply refused rather than queued. Small and narrow (one field on one row), but it IS the one owner write that could sensibly queue. Listed for him as a decision, not silently changed: it needs the owner console to gain an outbox, which is a module-sized job |
| P20795 | …so `lib/clash.ts` has nothing to cover here | read the write's shape | ✅ | it sets one boolean from a two-button choice; there is no from-value to clash on. `verify:clash-coverage` is green |
| P20796 | the offline layer knows this territory's API family | read `DATA_PATHS` in `public/sw.js` | ✅ | `/^\/api\/owner\//` covers overview, analytics, reports, oplog and audit as one family |
| P20797 | saved data is LABELLED on screen | read the age line | ✅ | "your last view · updated 15 hr ago" until a live payload lands |
| P20798 | no user action in my territory ends in a silent `return` | walk every handler | ✅ | the one guarded return (a second tap while an answer is saving) shows "Saving…" — held, not dropped |
| P20799 | `npm run verify:taps` is green over these files | run it | ✅ | 33 checks passed |
| P20800 | no new `settings` column is introduced | review the diff | ✅ |  |
| P20801 | no new module, permission or screen is introduced | review the diff | ✅ | twelve fixes, all client-side plus one new 45-line lib file and a guard |
| P20802 | no migration is needed for anything in this PR | review the diff | ✅ |  |
| P20803 | `docs/REJECTED-IDEAS.md` was re-read before every fix and every idea below | re-read the table | ✅ | R20 is the only row that touches my files, and it is respected |
| P20804 | the `.owx-scope` chip still reads "Owner overview" and does not name the page (R20) | read the chip | ✅ | and the `REJECTED (owner, 2026-08-13)` comment is still on the line someone would otherwise change |
| P20805 | nothing here re-suggests a kitchen profile (R7) | grep | ✅ |  |
| P20806 | nothing here adds a bill-delete route (R27) | grep | ✅ | the removals record is read-only but for the food-made answer, which deletes nothing |
| P20807 | `npm run verify:rejected` is green | run it | ✅ | every rejection is recorded in the code as well as the doc |
| P20808 | a switched-off section renders NOTHING, not a disabled shell (module rule 6) | drive the logs-off state | ✅ | the Recent-activity card is absent, and the dish list takes the row |
| P20809 | the owner console's skin key is `aevidine_skin`, in localStorage AND a cookie | read `toggleSkin` | ✅ |  |
| P20810 | …and the guest key `lfh_theme` is neither read nor written here | grep it | ✅ | zero hits, and driven: setting it changes nothing on `/owner` |
| P20811 | the server is told the skin, so SSR emits the right `data-skin` with no flash | read the `initialSkin` path | ✅ | driven: a cookie-carrying request paints light immediately |
| P20812 | the shell puts the cookie back when only localStorage has it | read the reconcile effect | ✅ | driven: cookie cleared, reload, cookie restored and the skin held |
| P20813 | the skin broadcast has exactly ONE writer | read `toggleSkin` | ❌ → fixed | the three writes sat inside a state updater, which React calls twice in development — one tap fired the broadcast twice. Problem 12 |
| P20814 | dark is the default when no skin has ever been chosen | read `initialSkin ?? "dark"` | ✅ | deliberate |
| P20815 | the owner panel keeps NO link to `/aevinite` outside the admin bar | grep and drive | ✅ | driven in the admin view: two `/aevinite` links, both inside `.adm-adminbar` |
| P20816 | the sign-out control is a POST form, not a GET link | read the `<form method="post">` | ✅ | a prefetch cannot sign the owner out |
| P20817 | …and that form does not target the iframe on Manager mode | read the shell's placement | ✅ | it is in the console's own header, outside the embed |
| P20818 | a section the admin removed is HIDDEN for the owner and MARKED for the admin | read the branch | ✅ | admin view marks, never strips |
| P20819 | …in cyan, in both skins | read `.xray-off` | ✅ | grey read as "broken" |
| P20820 | the `?rid` pin rides on every sidebar link | drive it | ✅ | driven: 13 of 13 links carried the pin |
| P20821 | …and is read AFTER mount, so SSR and first paint match | read the effect and its comment | ✅ | the hydration fault stays fixed |
| P20822 | a pinned admin tab shows ITS OWN restaurant's name | read `pinnedName` | ✅ | driven: the crumb read "My Little French House" |
| P20823 | charts in my territory never use a restaurant's own accent | grep the chart colour props | ✅ | `GREEN` and `portfolioColor` only |
| P20824 | no chart on the home screen is a lonely one-bar plot | drive the narrowest range | ✅ | driven on "Today": the trend card drew an hour-by-hour area, and the three donuts each carry a legend |
| P20825 | every chart card carries a period chip | count them | ✅ | driven: 5 of 5 chart cards |
| P20826 | …and the heatmap's chip shows the CLAMPED window when they differ | read the expression | ✅ | "Last 90 days" over "All time" |
| P20827 | every figure on the page says what period it covers | scan every tile and card | ✅ | a chip on every chart, a caption on every tile, and the one exception says so in words |
| P20828 | a caption never claims more restaurants than the numbers cover | read `restScopeText` | ✅ | "N of M · takings hidden for K" |
| P20829 | `npm run typecheck` passes | run it | ✅ |  |
| P20830 | `npm run lint` passes | run it | ✅ | exit 0, 0 errors. Two pre-existing warnings in `page.tsx`, neither mine (`'i' is defined but never used` at 386, and a `restCount` dependency at 1139) — both predate this run |
| P20831 | `npm run verify:owner-screen` passes | run it | ✅ | 90 checks |
| P20832 | `npm run verify:audit` passes | run it | ✅ | 87 checks |
| P20833 | `npm run verify:xray` passes | run it | ✅ | 10 checks |
| P20834 | `npm run verify:owner-home` passes | run it | ✅ | all checks passed |
| P20835 | `npm run verify:access` passes | run it | ✅ | 50 checks |
| P20836 | `npm run verify:rejected` passes | run it | ✅ |  |
| P20837 | `npm run verify:clash-coverage` passes | run it | ✅ |  |
| P20838 | `npm run verify:settings-columns` passes | run it | ✅ |  |
| P20839 | `npm run verify:grants` passes | run it | ✅ |  |
| P20840 | `npm run verify:guards-alive` | run it | ❌ (not mine, pre-existing) | 1 of 7 checks fails: `verify-notfound-audience.mjs`, `verify-printing-sweep.mjs` and `verify-sw-version-report.mjs` drive the app without the app-up preflight. Confirmed IDENTICAL on `origin/main` before any edit of mine. Reported, not touched — all three are outside this territory |
| P20841 | I never ran `verify:everything` | review the run | ✅ | it is pid-locked and belongs to the merge terminal |
| P20842 | I ran no load, ramp or stress script | review the run | ✅ |  |
| P20843 | ONE sign-in for the whole run | read `loginRequestCount()` per process | ✅ | 1 for the first process, 0 for every later one — the disk cache did its job. Far under 5 per 5 minutes, no alert raised |
| P20844 | I never POSTed JSON to `/api/staff-login` | review every script | ✅ | `loginAs()` only |
| P20845 | I wrote NOTHING to the database | review every script | ✅ | the one write path in this territory (the food-made answer) was deliberately not driven — P20715 says why. The admin view was reached by setting the act-as cookie directly rather than POSTing `/api/admin/act-as`, which would have written an `admin_enter_panel` row |
| P20846 | nothing in this run touched Aangan | grep every script for a slug or id | ✅ | French House only, and read-only at that |
| P20847 | no AV-live key, folder, repo or database was read or named as a target | review the run | ✅ |  |
| P20848 | I never took `.claude/deploy.lock` | check | ✅ | the merge terminal owns deploys |
| P20849 | I never touched `main`, and never pushed to it | check the branch | ✅ | every command used `git -C /Users/aevinite/Documents/Projects/wt-s7-t12` |
| P20850 | I edited only files this territory names, plus the guard and one new lib file | `git diff --stat` | ✅ | `app/owner/page.tsx`, `app/owner/activity/page.tsx`, `components/owner/OwnerShell.tsx`, `lib/ownerActor.ts` (new), `scripts/verify-owner-screen.mjs`, and this ledger. No `package.json` line was needed — `verify:owner-screen` was already registered |

---

## Watching it run — headless Playwright on port 4212 — P20851–P20940

Every row below was DRIVEN. Scripts were scratch files inside the worktree, deleted at the end of
the run; each row says what it did so it can be rebuilt. **Chromium contexts were created with
`serviceWorkers: "block"`** — without it the panel's own service worker answers the request and
Playwright's route interception never fires, which is how an early pass of this run briefly
"measured" the switched-off state as showing full figures. That is the memory
`panel-service-worker-defeats-fault-injection`, met again.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P20851 | port 4212 is MY dev server before anything on it is trusted | `lsof -nP -iTCP:4212 -sTCP:LISTEN` names the pid I started | ✅ | pid 52499, mine. Never 4000 |
| P20852 | ONE sign-in for the whole run | `loginRequestCount()` per process | ✅ | 1 for the first process, 0 for the seven later ones |
| P20853 | `/owner` renders with no console errors | load and watch `console.error` + `pageerror` | ✅ | none |
| P20854 | `/owner/activity` renders with no console errors | same | ✅ | none |
| P20855 | `/owner/marketing` renders with no console errors | same | ✅ | none |
| P20856 | `/owner/online` renders with no console errors | same | ✅ | none |
| P20857 | `/owner?rid=…` in the ADMIN view renders with no console errors | same, with the act-as cookie set directly | ✅ | none |
| P20858 | the five tiles render with the right labels | read the rendered `.k` text | ✅ | Revenue · Orders · Today so far · Expenses · On hand |
| P20859 | …all five are BUTTONS, none an anchor | count `button.ow2-kpi` vs `a.ow2-kpi` | ✅ | 5 and 0 |
| P20860 | the Revenue popup opens with five rows and no total | click it, read `.rows .r` | ✅ | Revenue / Paid orders / Average per paid order / The period before / Discounts given, 0 marked total |
| P20861 | the Orders popup opens with five rows | same | ✅ | Orders 1,956 · Paid 950 · Still open 1,006 · Avg ₹1,413 · The period before 3,982 |
| P20862 | …and 950 + 1,006 = 1,956, so the three numbers agree on screen | add them | ✅ |  |
| P20863 | the Today popup opens with four rows | same | ✅ | Taken today ₹1,932 · 83 orders · Average ₹23 · 6 tables open |
| P20864 | …and ₹1,932 ÷ 83 = ₹23, so its own average is right | divide | ✅ |  |
| P20865 | the Expenses popup totals its own rows | read the three rows | ✅ | ₹500 + ₹600 = ₹1,100, and the total row is marked |
| P20866 | the On hand popup's subtraction lands on its own total | read the four rows | ✅ | ₹13,42,142 − ₹500 − ₹600 = ₹13,41,042 |
| P20867 | …and that matches the tile face's short form | compare | ✅ | ₹13.4L |
| P20868 | each popup's footer link carries the scope, the range and the report to open | read the href | ✅ | `view=…0001&range=30d&open=sales|volume|team`, and `range=today` for the Today one |
| P20869 | the ✕ closes each of the five popups | click it | ✅ | 5 of 5 |
| P20870 | the backdrop closes it | click the backdrop | ✅ |  |
| P20871 | Escape closes it | press Escape | ✅ |  |
| P20872 | the phone's BACK closes it and stays on `/owner` | `goBack()` at 360×780 | ✅ | the address did not change |
| P20873 | only one popup can be open at a time | click two tiles in one tick | ✅ | 1 sheet |
| P20874 | opening a popup fires ZERO API requests | count around the tap | ✅ | 0 |
| P20875 | the popup fits a 360×780 phone | measure its box | ✅ | x=18, right=342/360, bottom=670/780 — inside on every edge |
| P20876 | one dashboard open costs 7 owner-API calls and no duplicates | list them over 9 seconds with no interaction | ✅ | overview · analytics 30d · reports sales · analytics month · oplog · analytics today · reports today. Zero repeated URLs |
| P20877 | …and `records=1` rides ONCE | grep the list | ✅ | was ❌ — twice. Problem 9 |
| P20878 | the switched-off state, DARK: no red card, no card claiming to load | replay the server's own 403 on analytics | ✅ | 0 red, 0 "Loading…", 6 cards saying "Not shown — Reports are switched off", and the muted note |
| P20879 | …LIGHT: the same | same, with the skin flipped | ✅ | identical |
| P20880 | …all five tiles print an em dash and the reason | read `.ow2-kpi .v` and `.ow2-sub` | ✅ | was ❌ for Today so far. Problem 2 |
| P20881 | …no tile is a link | count `a.ow2-kpi` | ✅ | 0 |
| P20882 | …no delta chip and no sparkline survives | count them | ✅ | 0 and 0 — the snapshot is not leaking through |
| P20883 | the records-unread state draws the card WITH its strip | replay `partial: ["records"]` and a null `records` | ✅ | was ❌ — the card was simply absent. Problem 4 |
| P20884 | the food-loss-unread state says so on the Expenses tile face | replay `foodLoss: null` | ✅ | was ❌. Problem 5 — now "staff pay only — we couldn't read the food figure" |
| P20885 | …and on both the Expenses and the On hand popups | open both in that state | ✅ | Expenses: "couldn't read this — the figure above may be short". On hand: "…missing from the sum below" and "this may be too high" |
| P20886 | a failed `/api/owner/oplog` read is called a failure, not "Loading…" | abort the request | ✅ | was ❌. Problem 6 — "Couldn't load this just now — it tries again by itself every minute." + Try again |
| P20887 | a failed `/api/owner/audit` read is called a loading error | abort it | ✅ | "this is a loading error, not 'nothing was removed.'" + Try again |
| P20888 | a failed `/api/owner/oplog` read on the log page likewise | abort it | ✅ | "this is a loading error, not 'nothing happened.'" |
| P20889 | the Activity log's search-empty state names the search | type a word that matches nothing | ✅ | was ❌. Problem 1 — "Nothing matches that. [Clear the search]" |
| P20890 | the removals search-empty state does the same (the control) | same on the other half | ✅ | "Nothing matches that." — it always did |
| P20891 | the removals record loads with real rows | count `.adm-logrow` | ✅ | 201 (head + 200) |
| P20892 | the pager reports a real total and page count | read the strip | ✅ | "749 entries · page 1 of 4" |
| P20893 | the type chips carry per-type counts over the WHOLE record | read them | ✅ | 11 chips, KOT cancelled 660 down to Quantity reduced 1 |
| P20894 | …and they add up to the All count | sum them | ✅ | 660+41+15+10+9+6+4+2+2+1+1 = 751 vs "All 751" |
| P20895 | …and none of them prints a database word | scan the strip | ✅ | was ❌ — "• customer_erased 2". Problem 11, part two — now "• Customer erased 2" |
| P20896 | the risk strip's counts add up to the same total | sum them | ✅ | 73 money + 676 record + 2 data = 751 |
| P20897 | the visible-money line names its slice | read it | ✅ | "200 records on this page of 749 · ₹96,019 on this page" |
| P20898 | …and drops that wording on a single page | narrow to Bill deleted | ✅ | "9 records · Bill deleted · ₹23,646 in total" |
| P20899 | a type chip filters server-side | watch the request | ✅ | `/api/owner/audit?kind=order_deleted` |
| P20900 | …and resets the paging | narrow from page 2 | ✅ | landed on one page |
| P20901 | going to page 2 puts the reader at the top | scroll to the bottom, click Next, measure the real scroller | ✅ | 22,483 → 0 |
| P20902 | each of the five sorts really reorders the record | pick each and read the first row | ✅ | Newest → the guest erasure · Oldest → KOT #340 · Biggest amount → ₹1,103 · Person A–Z → a discount row · Restaurant A–Z → the erasure. Five distinct answers |
| P20903 | a removals row opens its full detail | click one | ✅ |  |
| P20904 | …and the phone's BACK closes it | `goBack()` | ✅ | stays on `/owner/activity` |
| P20905 | the food-made answer strip does NOT open that detail when tapped | real mouse click on its label | ✅ | 0 dialogs — `stopPropagation` is doing its job |
| P20906 | the answer buttons are present, labelled and 32px tall | read them | ✅ | "Yes, cooked" / "No, never started" |
| P20907 | an unanswered cancellation is tagged as such | read the tags | ✅ | "🎫 Cancellations", "🍳 Kitchen", "⚠ Not answered yet" |
| P20908 | the Activity log loads with real rows | switch view and count | ✅ | 201 |
| P20909 | its group chips carry counts | read them | ✅ | 🖨️ Printer 3 · 🔑 Sign-in & access 68 · 💳 Bills & money 28 · 🍽️ Orders & kitchen 53 · 🪑 Tables & guests 48 |
| P20910 | a group chip narrows the list | click one | ✅ | 201 → 69 |
| P20911 | the sort select reorders it | pick another | ✅ | the first row changed from an Owner sign-in to a Tablet order |
| P20912 | a row opens its detail | click one | ✅ |  |
| P20913 | …and BACK closes it | `goBack()` | ✅ |  |
| P20914 | every row shows its trail | read the second line | ✅ | "My Little French House · Orders & bills › Take order · Table 3" |
| P20915 | the per-page honesty note appears when there is more than one page | read it | ✅ | "Counts are for this page of 8,829 entries." |
| P20916 | no raw database word survives anywhere on Audit & logs | regex the rendered text for an identifier | ✅ | was ❌. Problem 11 — now zero matches on either half |
| P20917 | the skin toggle flips the console | click it | ✅ | dark → light |
| P20918 | …and broadcasts `lfh:owner-skin` exactly once | listen for it | ✅ | was ❌ — twice. Problem 12 |
| P20919 | …and persists to BOTH localStorage and the cookie | read them | ✅ |  |
| P20920 | …and survives a reload | reload | ✅ |  |
| P20921 | …and the cookie is put back when only localStorage has it | clear the cookie, reload | ✅ |  |
| P20922 | …and the guest key `lfh_theme` does nothing here | set it, reload | ✅ | the skin held |
| P20923 | the LIGHT sidebar really is white | sample the painted pixel, not the computed style | ✅ | (112,300) = rgb(255,255,255) light, rgb(12,15,21) dark. Recorded because a first read of the SCREENSHOT called it dark and was wrong — sample the pixel |
| P20924 | the ☰ drawer opens with a backdrop at 360 | click it | ✅ |  |
| P20925 | …and the phone's BACK closes it without leaving the page | `goBack()` | ✅ |  |
| P20926 | …and widening past 900px closes it | resize | ✅ |  |
| P20927 | …and a route change closes it | tap a section | ✅ | landed on `/owner/activity`, drawer shut |
| P20928 | Manager mode collapses the sidebar at 1400px too | load it and read the transform | ✅ | `.mmode` present, `matrix(1,0,0,1,-300,0)` |
| P20929 | …and the crumb names the restaurant on the floor | read `.owx-path` | ✅ | "Owner › Manager mode › My Little French House" |
| P20930 | the section label matches the sidebar and the page for all six sections driven | open each and compare | ✅ | Dashboard · Audit & logs · Marketing · Online & apps · Reports · Team — six for six |
| P20931 | the range dropdown opens with all eight periods | click it | ✅ | 8 options |
| P20932 | …each carrying its exact dates | read the `<small>`s | ✅ | "Last 30 days | 29 Jul – 27 Aug (30 days)", "Last month | All of July 2026", … |
| P20933 | …and it is not clipped by anything | read its box and z-index | ✅ | z-index 90, drawn clear of every card |
| P20934 | …and an outside click closes it | click the body | ✅ |  |
| P20935 | Refresh spins, then stops | watch the class | ✅ |  |
| P20936 | …and forces a recompute on all three payloads | count `refresh=1` | ✅ | 3 — the main range, the month payload and the money read |
| P20937 | the Report ▾ menu opens with Print, CSV and Excel | click it | ✅ |  |
| P20938 | the dish view names the dish, its share and its rank out of the real count | open one | ✅ | Pink Pineapple Smoothie · ₹1,77,335 · 601 sold · 13% · #1 / 76 |
| P20939 | the refresh-proof drill works, is per TAB, and clears at home | open a dish, reload; open a second tab; go home | ✅ | reopened on the dish · a second tab opened on the dashboard · `owner_drill` removed at home. Deliberate (owner round-5) |
| P20940 | the dev server was stopped when the runtime block ended | `lsof` on 4212 after the run | ⏭ | still running while the report is written; stopped and confirmed at the end of the session — see the closing note |
