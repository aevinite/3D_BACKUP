# SWEEP #9 — TERMINAL 28 · THE OWNER SERVER ROUTES AND EVERY REMAINING ROUTE · phases P104301–P104350

**Territory (write access):** all 13 `app/api/owner/**/route.ts` · `app/api/log/client-error` ·
`app/api/health` · `app/api/maintenance` · `app/api/blocked` ·
`app/api/aggregators/webhook/[source]` · `lib/aggregators.ts` · `lib/ownerScope.ts`.
Twenty files, **6,685 lines** — re-derived with `wc -l` rather than inherited from the prompt, which
said 5,352 (that is the thirteen owner routes alone). `app/api/print-agent` is NOT here: S9-T11
names it, so it is theirs.

**Branch:** `sweep9/t28-owner-api-and-the-rest` · **worktree:** `../wt-s9-t28` · **port:** 4428
**ID block:** `P104301`–`P104400`, pre-allocated. Fifty used; `P104351`–`P104400` stay free.

## Where these fifty point, and why — MEASURED before one was written (rule 2b)

Rows whose **subject** is one of my twenty files, counted across all 44 ledgers on disk:

| rows | per kloc | file | lines |
|---:|---:|---|---:|
| **0** | 0.0 | `lib/aggregators.ts` | 317 |
| 1 | 12.2 | `app/api/aggregators/webhook/[source]/route.ts` | 82 |
| 2 | 10.4 | `app/api/log/client-error/route.ts` | 192 |
| 2 | 6.1 | `lib/ownerScope.ts` | 327 |
| 4 | 40.0 | `app/api/maintenance/route.ts` | 100 |
| 5 | 42.4 | `app/api/blocked/route.ts` | 118 |
| 10 | 227.3 | `app/api/health/route.ts` | 44 |
| 14 | 63.6 | `app/api/owner/printing/route.ts` | 220 |
| 21–33 | | the other twelve owner routes | |
| **202** | 173.7 | `app/api/owner/reports/route.ts` | 1163 |

**503 rows already cover this territory, and 1,180 of its 6,685 lines carried 24 of them** — the
whole platform-order intake seam, the public crash sink, the blocked-device door, and the GATE all
thirteen owner routes stand on. Not because they are unimportant: because no sweep prompt ever
happened to name them. All fifty go there, and none to `owner/reports`, which has 202.

## How they run

```sh
npm run verify:t28-new50 -- --base http://localhost:4428
```

Two halves, deliberately. The **pure** half calls the SHIPPED exported functions — compiled with
esbuild at run time, never re-typed, because a check that re-implements the logic only proves the
re-implementation — and reads the real answer. The **driven** half posts to a running dev server as
a real owner, as the admin, and with no cookies at all, and reads the rendered answer: the status,
the body, and the database row a write left behind. Every row it writes is deleted by its own id in
the same run. Nothing answering at the base prints "nothing was checked" and exits 2, never a stack
trace that reads like the app is broken.

The ids are **auto-numbered in declaration order** from the block's first id, and the run refuses to
start unless there are exactly fifty. Typed ids are how a generated band ends up with two of the
same number — this registry records six such collisions.

**Judged by breaking it, not by the green.** Four shipped behaviours were reverted one at a time —
the item-name fallback, the finite-number test on an outlet id, the no-secret refusal in the webhook
lock, and `ownerActorName`'s name-before-uuid order — and the matching rows went red every time
(P104301, P104302, P104310, P104311, P104331). Restoring them turned the suite green again.


## Why every row below names its subject FILE (added 2026-09-16, round 2)

These fifty originally named their FUNCTION — *"normalizeIncoming: an item with no name…"* — and
never the file. Rule 2b's measurement counts rows **by subject**, so it read `lib/aggregators.ts` as
having **zero** rows when it had twenty-one, and the next terminal to measure this territory would
have re-aimed fifty checks at ground already covered. That is precisely the failure rule 2b exists to
prevent, produced by the run that was following it.

Nothing about any check changed — only the claim column now opens with the file it is about. **A
ledger row that does not name its subject is invisible to the only measurement this sweep converges
on.**

**Result key:** ✅ pass · ❌ fail · ⏭ skipped, with a written reason.

| id | check | how to verify | result | note |
|---|---|---|---|---|
| P104301 | `lib/aggregators.ts` — normalizeIncoming: an item with no name at all still reaches the cook as a readable line | call the shipped function with { items: [{}] } | ✅ | run 2026-09-15 on port 4428 |
| P104302 | `lib/aggregators.ts` — …and a name of nothing but spaces does not become a blank line | { items: [{ name: '   ' }] } | ✅ | run 2026-09-15 on port 4428 |
| P104303 | `lib/aggregators.ts` — a quantity or a price that is not a number never reaches the slip as NaN | { items: [{ name:'Tea', qty:'two', price:'free' }] } and a qty of 0 | ✅ | run 2026-09-15 on port 4428 |
| P104304 | `lib/aggregators.ts` — the order total falls back to the lines when the platform sends none | { items: [{ name:'A', qty:2, price:30 }, { name:'B', price:15 }] } and no total | ✅ | run 2026-09-15 on port 4428 |
| P104305 | `lib/aggregators.ts` — items that are not a list are treated as no items, not a crash | { items: 'nope' } and { items: null } | ✅ | run 2026-09-15 on port 4428 |
| P104306 | `lib/aggregators.ts` — …and the platform's own id is used verbatim when it sends one, in any of its three spellings | order_id / id / orderId | ✅ | run 2026-09-15 on port 4428 |
| P104307 | `lib/aggregators.ts` — the outlet id is found in every field name a platform might use for it | all eight flat spellings | ✅ | run 2026-09-15 on port 4428 |
| P104308 | `lib/aggregators.ts` — …and nested under the outlet/store/restaurant/merchant object, where some platforms put it | { outlet: { id } } and { store: { code } } | ✅ | run 2026-09-15 on port 4428 |
| P104309 | `lib/aggregators.ts` — an outlet id of blank space is NOT an outlet id — it must fall through to 'unknown' | { outlet_id: '   ' } → "" | ✅ | run 2026-09-15 on port 4428 |
| P104310 | `lib/aggregators.ts` — an outlet id that is a NaN number is refused rather than becoming the text "NaN" | { outlet_id: NaN } | ✅ | run 2026-09-15 on port 4428 |
| P104311 | `lib/aggregators.ts` — the webhook lock refuses when no secret is configured — dormant means CLOSED, not open | verifyWebhook with the env var absent | ✅ | run 2026-09-15 on port 4428 |
| P104312 | `lib/aggregators.ts` — …and refuses when the caller presents no secret, even if one IS configured | verifyWebhook(source, null) | ✅ | run 2026-09-15 on port 4428 |
| P104313 | `lib/aggregators.ts` — …accepts the right secret, and refuses one that is merely a prefix of it | the configured value vs a truncation of it | ✅ | run 2026-09-15 on port 4428 |
| P104314 | `lib/aggregators.ts` — each platform's lock is its own — Swiggy's secret does not open Zomato's door | configure only Zomato, present it as Swiggy | ✅ | run 2026-09-15 on port 4428 |
| P104315 | `lib/aggregators.ts` — the compare is constant-time and hashed — the shipped code uses the login door's own helper | read: safeEqual over sha256hex of BOTH sides, never a plain === | ✅ | run 2026-09-15 on port 4428 |
| P104316 | `lib/aggregators.ts` — the platform-wide "is intake on anywhere" question is a rows-free COUNT, not a read of every restaurant's settings | read: head + count, with the filter pushed into Postgres | ✅ | run 2026-09-15 on port 4428 |
| P104317 | `lib/aggregators.ts` — …and a FAILED read is never cached, so a blip cannot switch the integration off for the TTL | read: the cache is written only after the error test | ✅ | run 2026-09-15 on port 4428 |
| P104318 | `lib/aggregators.ts` — an unknown restaurant answers "intake off" rather than throwing — it fails CLOSED | aggregatorsEnabled(<a uuid nothing owns>) | ✅ | run 2026-09-15 on port 4428 |
| P104319 | `lib/aggregators.ts` — the status push back to the platform has a deadline, so it can never hold an instance open | read: a bounded NOTIFY_TIMEOUT_MS, guarded because reading AbortSignal.timeout can throw | ✅ | run 2026-09-15 on port 4428 |
| P104320 | `lib/aggregators.ts` — …and a status nobody maps, or a source with no upstream, is simply not pushed | notifyAggregator('takeaway', …) and an unmapped status both return without a fetch | ✅ | run 2026-09-15 on port 4428 |
| P104321 | `lib/aggregators.ts` — "we could not ask" and "no such outlet" are different answers, not one null | the shipped type has an `unread` flag, and the read-failure branch sets it | ✅ | run 2026-09-15 on port 4428 |
| P104322 | `lib/aggregators.ts` — an outlet nothing is mapped to resolves to NOBODY — it never falls back to restaurant #1 | resolveWebhookRestaurant with an outlet id no restaurant claims | ✅ | run 2026-09-15 on port 4428 |
| P104323 | `lib/aggregators.ts` — the channel-mapping read names its columns, carries a ceiling, and asks Postgres to filter | read: select(restaurant_id, platform_channels) + .eq(on) + .limit() | ✅ | run 2026-09-15 on port 4428 |
| P104324 | `lib/aggregators.ts` — ingesting an order still re-checks the target restaurant's own Platform module and channel | read: platformLadder(rid).effective and the per-channel on flag, both before the insert | ✅ | run 2026-09-15 on port 4428 |
| P104325 | `lib/aggregators.ts` — a platform we have never heard of is refused at the door, before any switch is read | POST http://localhost:4428/api/aggregators/webhook/dunzo → 404 | ✅ | run 2026-09-15 on port 4428 |
| P104326 | `app/api/aggregators/webhook/[source]/route.ts` — an oversized order is REFUSED, never silently trimmed — a trimmed order is food somebody doesn't get | POST a 70 KB body → 413, and the body cap is read before the JSON is parsed | ✅ | run 2026-09-15 on port 4428 |
| P104327 | `app/api/aggregators/webhook/[source]/route.ts` — the gates are read in the safe order: the source, then the flag, then the lock, then the tenant | read: each refusal precedes the next read in the handler | ✅ | run 2026-09-15 on port 4428 |
| P104328 | `app/api/aggregators/webhook/[source]/route.ts` — the lock is AWAITED — an un-awaited async check is always truthy and its refusal unreachable | read: `await verifyWebhook(`, the fault /api/issue-media shipped with | ✅ | run 2026-09-15 on port 4428 |
| P104329 | `app/api/aggregators/webhook/[source]/route.ts` — an unreadable mapping asks the platform to send the order again; an unmapped one asks a person to fix it | read: 503 + transient for the unread case, the 404 kept for a real answer | ✅ | run 2026-09-15 on port 4428 |
| P104330 | `app/api/aggregators/webhook/[source]/route.ts` — an ADMIN action performed from the owner's screens is recorded against the ADMIN's log, in every admin shape | ownerLogPanel over all-view, act-as, and a real owner | ✅ | run 2026-09-15 on port 4428 |
| P104331 | `app/api/aggregators/webhook/[source]/route.ts` — the person written into a log row is a NAME for a real owner and "admin" for Aevidine — never a uuid | ownerActorName over the same three shapes | ✅ | run 2026-09-15 on port 4428 |
| P104332 | `app/api/aggregators/webhook/[source]/route.ts` — "is this restaurant mine?" answers yes for the admin and only for their own list for an owner | inScope over both shapes | ✅ | run 2026-09-15 on port 4428 |
| P104333 | `lib/ownerScope.ts` — a database failure never reaches an owner in the database's own words, and says whether to retry | dbFail with a PostgREST-shaped object | ✅ | run 2026-09-15 on port 4428 |
| P104334 | `lib/ownerScope.ts` — …and a statement timeout keeps its own advice and its own status, because it is the one thing an owner can act on | dbFail with 57014 | ✅ | run 2026-09-15 on port 4428 |
| P104335 | `lib/ownerScope.ts` — a PARTIAL restaurant list is never returned as a list — it is a retryable refusal instead | the two declared failure types, and the shared 503 the routes answer with | ✅ | run 2026-09-15 on port 4428 |
| P104336 | `lib/ownerScope.ts` — …and the admin's all-restaurants list is PAGED, so it cannot stop at PostgREST's row cap | read: a page loop with .range(), not a flat .limit() | ✅ | run 2026-09-15 on port 4428 |
| P104337 | `app/api/log/client-error/route.ts` — a crash report from a panel nobody has heard of is not written at all | POST with panel:"nonsense" → ok:true, skipped:"bad_panel", and NO row lands | ✅ | run 2026-09-15 on port 4428 |
| P104338 | `app/api/log/client-error/route.ts` — …and a body too large to be a real crash report is dropped before it is parsed | POST a 3 KB body → skipped:"too_large", no row | ✅ | run 2026-09-15 on port 4428 |
| P104339 | `app/api/log/client-error/route.ts` — …and broken JSON is answered calmly, never as an error from the error-logger itself | POST `{not json` → 200 skipped:"bad_json" | ✅ | run 2026-09-15 on port 4428 |
| P104340 | `app/api/log/client-error/route.ts` — a real crash on a guest menu is filed against the restaurant whose address it happened at | POST with where=/r/french-house/menu and NO rid → the row carries French House | ✅ | run 2026-09-15 on port 4428 |
| P104341 | `app/api/log/client-error/route.ts` — …and the row says WHICH browser it came from, because that is the most useful fact about a crash | the same POST with a Safari-on-iPhone user agent → the detail carries a short tag | ✅ | run 2026-09-15 on port 4428 |
| P104342 | `app/api/log/client-error/route.ts` — a tap breadcrumb is stored at info level and never reaches the owner's Activity list | POST kind:taps → a level:'info' ui_taps row, which /api/owner/oplog excludes by action | ✅ | run 2026-09-15 on port 4428 |
| P104343 | `app/api/blocked/route.ts` — a device that is not blocked is told so plainly, with no login and no cookie | GET http://localhost:4428/api/blocked → 200, blocked:false | ✅ | run 2026-09-15 on port 4428 |
| P104344 | `app/api/blocked/route.ts` — …and asking to be unblocked when you are not blocked writes nothing and says why | POST http://localhost:4428/api/blocked → ok:false, reason:"not_blocked", and no unblock_requests row | ✅ | run 2026-09-15 on port 4428 |
| P104345 | `app/api/blocked/route.ts` — the counter that enforces the three-a-day cap tells "couldn't count" apart from "none used" | read: usedToday returns number \| null, never 0 on failure | ✅ | run 2026-09-15 on port 4428 |
| P104346 | `app/api/blocked/route.ts` — …and the two callers use it in OPPOSITE directions: the page renders on doubt, the write refuses on doubt | read: the GET coalesces to 0 for display; the POST answers 503 when the count is null | ✅ | run 2026-09-15 on port 4428 |
| P104347 | `app/api/blocked/route.ts` — …and the device is identified from proxy headers on our side, never from the body it sent | read: clientIp(req) only, and `ip` is never taken off the request body | ✅ | run 2026-09-15 on port 4428 |
| P104348 | `app/api/maintenance/route.ts` — reading whether the guest menu is down needs a login, and reading it is all a kitchen or tablet may do | GET http://localhost:4428/api/maintenance with no cookie → 401, and with the admin's → the real state | ✅ | run 2026-09-15 on port 4428 |
| P104349 | `app/api/maintenance/route.ts` — …and a malformed restaurant id gets a sentence a person can read, never the database's own words | GET http://localhost:4428/api/maintenance?rid=not-a-uuid → a plain refusal, no "invalid input syntax" | ✅ | run 2026-09-15 on port 4428 |
| P104350 | `app/api/maintenance/route.ts` — …and the admin's own flip of that switch is recorded against the ADMIN's log, not the owner's feed | read: the panel is decided from whether there is a staff user, not hard-coded | ✅ | run 2026-09-15 on port 4428 |
