# T20 — ADMIN SERVER ROUTES (PART B) + THE OWNER ROUTES · phases P09501–P10000

**Territory (write access):** the last 24 `app/api/admin/**/route.ts` files
(`find app/api/admin -name route.ts | sort | tail -24`) · all 12 `app/api/owner/**/route.ts` files.
36 files, 8,076 lines. Everything else in the repo was read only.

**Branch:** `sweep6/t20-admin-api-b` · **worktree:** `~/Documents/Projects/sweep6/T20` · **port:** 4120

## The budget these 500 phases were written to

| block | ids | share | what it is |
|-------|-----|-------|------------|
| A | P09501–P09700 | 40% | reading the code for correctness |
| B | P09701–P09800 | 20% | conformance to the project rules named in the prompt |
| C | P09801–P09875 | 15% | watching it run (headless Playwright, port 4120) |
| D | P09876–P09925 | 10% | screenshots actually opened and read (1280×800 + A35 360×780 dpr3) |
| E | P09926–P09975 | 10% | tracing a change across panels |
| F | P09976–P10000 | 5% | judgment — does a real restaurant need it to work this way |

**Per-file floor.** All 36 files are named in the scope, so each gets **ten or more**: five
correctness reads (block A), a sign-in gate row and an egress row (block B), and at least three
more from the live / screenshot / trace / judgment blocks. The two biggest —
`app/api/owner/reports/route.ts` (1,118 lines) and `app/api/owner/staff/route.ts` (1,142) — carry
the most, and `app/api/admin/restaurants/access-tree/route.ts` and
`app/api/admin/restaurants/settings/route.ts` each carry their own named findings.

**Result key:** ✅ pass · ❌ fail · ⏭ skipped, with a written reason.

**The result column is the SECOND full pass**, re-run from the top after every fix landed. Six rows
read ❌ on the first pass; each says so in its note, names exactly what was wrong, and is green here
because the fix is in this PR.

**Restoration rule.** Every write phase below runs against **French House** and restores what it
changed in a `finally` block. **Aangan is read-only** and was never written to; block E's last trace
re-reads its whole settings row before and after a French House write and diffs it. Rows this run
created were deleted by id in the same run.

**The restore proves itself.** On the FIRST pass two restores were wrong and left French House
changed: the delivery-channel restore sent `key: null` when that route only clears on an empty
STRING, so a throwaway key survived; and the banquet counter's "before" was captured AFTER the write,
so it restored the new value over the old. Both were found by re-reading the row rather than trusting
the restore, both are fixed in the harness, and the harness now FAILS its own run if French House is
not back. Final state verified field by field: 13/13 as found. The one deliberate exception is table
1's QR code — rotating it is what that button is for, and the old code is meant to stop working.

**Three of my own assertions were wrong before any code was.** Each is recorded on its row rather
than quietly corrected: the bill/parcel preview documents have no literal `<html>` tag (valid HTML5,
the element is implied); French House genuinely HAS table-types handed to its owner, so a 200 there is
correct; and `/owner/logs` does not exist — the route is `/owner/activity`. A test that asserts the
wrong thing is worse than no test.

**One gate is RED for a reason that is not mine.** `npm run verify:read-guards` fails on a clean
`origin/main`: `cancel_classified` and `cancel_classify_failed` (migration 337) are in
`components/admin/shared.tsx`'s `ACT_LABEL` and missing from `lib/logTrail.ts`, so those two rows read
"System › Other". Both files are outside my territory — see the 🔗 HANDOFF in
`.claude/sweep/T20-findings.md`. My own three checks in that file pass.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P09501 | `app/api/admin/repair/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09502 | `app/api/admin/repair/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09503 | `app/api/admin/repair/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09504 | `app/api/admin/repair/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09505 | `app/api/admin/repair/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09506 | `app/api/admin/resolve-error/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09507 | `app/api/admin/resolve-error/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09508 | `app/api/admin/resolve-error/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09509 | `app/api/admin/resolve-error/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09510 | `app/api/admin/resolve-error/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09511 | `app/api/admin/restaurants/access-tree/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09512 | `app/api/admin/restaurants/access-tree/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09513 | `app/api/admin/restaurants/access-tree/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09514 | `app/api/admin/restaurants/access-tree/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09515 | `app/api/admin/restaurants/access-tree/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09516 | `app/api/admin/restaurants/bill-preview/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09517 | `app/api/admin/restaurants/bill-preview/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09518 | `app/api/admin/restaurants/bill-preview/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09519 | `app/api/admin/restaurants/bill-preview/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09520 | `app/api/admin/restaurants/bill-preview/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09521 | `app/api/admin/restaurants/branding/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09522 | `app/api/admin/restaurants/branding/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09523 | `app/api/admin/restaurants/branding/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09524 | `app/api/admin/restaurants/branding/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09525 | `app/api/admin/restaurants/branding/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09526 | `app/api/admin/restaurants/create-defaults/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09527 | `app/api/admin/restaurants/create-defaults/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09528 | `app/api/admin/restaurants/create-defaults/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09529 | `app/api/admin/restaurants/create-defaults/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09530 | `app/api/admin/restaurants/create-defaults/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09531 | `app/api/admin/restaurants/credentials/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09532 | `app/api/admin/restaurants/credentials/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09533 | `app/api/admin/restaurants/credentials/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09534 | `app/api/admin/restaurants/credentials/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09535 | `app/api/admin/restaurants/credentials/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09536 | `app/api/admin/restaurants/export/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09537 | `app/api/admin/restaurants/export/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09538 | `app/api/admin/restaurants/export/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09539 | `app/api/admin/restaurants/export/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09540 | `app/api/admin/restaurants/export/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09541 | `app/api/admin/restaurants/google-review/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09542 | `app/api/admin/restaurants/google-review/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09543 | `app/api/admin/restaurants/google-review/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09544 | `app/api/admin/restaurants/google-review/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09545 | `app/api/admin/restaurants/google-review/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09546 | `app/api/admin/restaurants/health/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09547 | `app/api/admin/restaurants/health/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09548 | `app/api/admin/restaurants/health/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09549 | `app/api/admin/restaurants/health/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09550 | `app/api/admin/restaurants/health/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09551 | `app/api/admin/restaurants/logo/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09552 | `app/api/admin/restaurants/logo/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09553 | `app/api/admin/restaurants/logo/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09554 | `app/api/admin/restaurants/logo/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09555 | `app/api/admin/restaurants/logo/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09556 | `app/api/admin/restaurants/panels/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09557 | `app/api/admin/restaurants/panels/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09558 | `app/api/admin/restaurants/panels/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09559 | `app/api/admin/restaurants/panels/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09560 | `app/api/admin/restaurants/panels/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09561 | `app/api/admin/restaurants/platform-channels/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09562 | `app/api/admin/restaurants/platform-channels/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09563 | `app/api/admin/restaurants/platform-channels/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09564 | `app/api/admin/restaurants/platform-channels/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09565 | `app/api/admin/restaurants/platform-channels/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09566 | `app/api/admin/restaurants/quick-features/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09567 | `app/api/admin/restaurants/quick-features/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09568 | `app/api/admin/restaurants/quick-features/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09569 | `app/api/admin/restaurants/quick-features/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09570 | `app/api/admin/restaurants/quick-features/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09571 | `app/api/admin/restaurants/report/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09572 | `app/api/admin/restaurants/report/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09573 | `app/api/admin/restaurants/report/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09574 | `app/api/admin/restaurants/report/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09575 | `app/api/admin/restaurants/report/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09576 | `app/api/admin/restaurants/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09577 | `app/api/admin/restaurants/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09578 | `app/api/admin/restaurants/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09579 | `app/api/admin/restaurants/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09580 | `app/api/admin/restaurants/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09581 | `app/api/admin/restaurants/settings/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09582 | `app/api/admin/restaurants/settings/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09583 | `app/api/admin/restaurants/settings/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09584 | `app/api/admin/restaurants/settings/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09585 | `app/api/admin/restaurants/settings/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09586 | `app/api/admin/restaurants/staff-features/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09587 | `app/api/admin/restaurants/staff-features/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09588 | `app/api/admin/restaurants/staff-features/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09589 | `app/api/admin/restaurants/staff-features/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09590 | `app/api/admin/restaurants/staff-features/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09591 | `app/api/admin/revenue/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09592 | `app/api/admin/revenue/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09593 | `app/api/admin/revenue/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09594 | `app/api/admin/revenue/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09595 | `app/api/admin/revenue/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09596 | `app/api/admin/settings/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09597 | `app/api/admin/settings/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09598 | `app/api/admin/settings/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09599 | `app/api/admin/settings/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09600 | `app/api/admin/settings/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09601 | `app/api/admin/staff-online/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09602 | `app/api/admin/staff-online/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09603 | `app/api/admin/staff-online/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09604 | `app/api/admin/staff-online/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09605 | `app/api/admin/staff-online/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09606 | `app/api/admin/usage/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09607 | `app/api/admin/usage/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09608 | `app/api/admin/usage/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09609 | `app/api/admin/usage/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09610 | `app/api/admin/usage/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09611 | `app/api/admin/users/photo/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09612 | `app/api/admin/users/photo/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09613 | `app/api/admin/users/photo/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09614 | `app/api/admin/users/photo/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09615 | `app/api/admin/users/photo/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09616 | `app/api/admin/users/route.ts` — every exported handler returns a Response on every branch (no fall-through, no unresolved promise) | read each exported GET/POST/PATCH/DELETE top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09617 | `app/api/admin/users/route.ts` — every Supabase read/write in it has its `.error` inspected, or a written reason why not | grep each `await sb.` result in the file and follow the variable to an error test | ✅ |  |
| P09618 | `app/api/admin/users/route.ts` — no query in it uses `select("*")` on a table holding a secret (password_hash, pin_hash, platform_channels key) | grep `select("*")` in the file and check the table it reads | ✅ |  |
| P09619 | `app/api/admin/users/route.ts` — a write that changes 0 rows is never answered as success | for each update/delete, check there is an existence read or a `.select()` whose emptiness is tested | ✅ |  |
| P09620 | `app/api/admin/users/route.ts` — no request-body value reaches a query without a type/shape check first | read each `body?.x` use and find its String()/isUuid()/typeof guard | ✅ |  |
| P09621 | `app/api/owner/analytics/route.ts` — every exported handler returns a Response on every branch | read each exported handler top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09622 | `app/api/owner/analytics/route.ts` — every read's `.error` is inspected, or the row says which figure degrades and how it is named | follow each `await sb.` result to an error test or an `rd()`/ReadSet wrapper | ✅ |  |
| P09623 | `app/api/owner/analytics/route.ts` — a failed read is never rendered as a confident ₹0 / empty list | for each numeric or list output, check the failure path yields null/partial, not 0/[] | ✅ |  |
| P09624 | `app/api/owner/analytics/route.ts` — the scope is applied BEFORE the query, never filtered afterwards | check each read carries `.eq/.in("restaurant_id", …)` or `p_ids`, not a post-filter on the result | ✅ |  |
| P09625 | `app/api/owner/analytics/route.ts` — a `?rid=`/`?restaurant_id=` parameter can only NARROW the scope, never widen it | check every pin is tested with inScope()/scope.ids.includes() before it is used | ✅ |  |
| P09626 | `app/api/owner/audit/route.ts` — every exported handler returns a Response on every branch | read each exported handler top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09627 | `app/api/owner/audit/route.ts` — every read's `.error` is inspected, or the row says which figure degrades and how it is named | follow each `await sb.` result to an error test or an `rd()`/ReadSet wrapper | ✅ |  |
| P09628 | `app/api/owner/audit/route.ts` — a failed read is never rendered as a confident ₹0 / empty list | for each numeric or list output, check the failure path yields null/partial, not 0/[] | ✅ |  |
| P09629 | `app/api/owner/audit/route.ts` — the scope is applied BEFORE the query, never filtered afterwards | check each read carries `.eq/.in("restaurant_id", …)` or `p_ids`, not a post-filter on the result | ✅ |  |
| P09630 | `app/api/owner/audit/route.ts` — a `?rid=`/`?restaurant_id=` parameter can only NARROW the scope, never widen it | check every pin is tested with inScope()/scope.ids.includes() before it is used | ✅ |  |
| P09631 | `app/api/owner/customers/route.ts` — every exported handler returns a Response on every branch | read each exported handler top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09632 | `app/api/owner/customers/route.ts` — every read's `.error` is inspected, or the row says which figure degrades and how it is named | follow each `await sb.` result to an error test or an `rd()`/ReadSet wrapper | ✅ |  |
| P09633 | `app/api/owner/customers/route.ts` — a failed read is never rendered as a confident ₹0 / empty list | for each numeric or list output, check the failure path yields null/partial, not 0/[] | ✅ |  |
| P09634 | `app/api/owner/customers/route.ts` — the scope is applied BEFORE the query, never filtered afterwards | check each read carries `.eq/.in("restaurant_id", …)` or `p_ids`, not a post-filter on the result | ✅ |  |
| P09635 | `app/api/owner/customers/route.ts` — a `?rid=`/`?restaurant_id=` parameter can only NARROW the scope, never widen it | check every pin is tested with inScope()/scope.ids.includes() before it is used | ✅ |  |
| P09636 | `app/api/owner/inventory/route.ts` — every exported handler returns a Response on every branch | read each exported handler top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09637 | `app/api/owner/inventory/route.ts` — every read's `.error` is inspected, or the row says which figure degrades and how it is named | follow each `await sb.` result to an error test or an `rd()`/ReadSet wrapper | ✅ |  |
| P09638 | `app/api/owner/inventory/route.ts` — a failed read is never rendered as a confident ₹0 / empty list | for each numeric or list output, check the failure path yields null/partial, not 0/[] | ✅ |  |
| P09639 | `app/api/owner/inventory/route.ts` — the scope is applied BEFORE the query, never filtered afterwards | check each read carries `.eq/.in("restaurant_id", …)` or `p_ids`, not a post-filter on the result | ✅ |  |
| P09640 | `app/api/owner/inventory/route.ts` — a `?rid=`/`?restaurant_id=` parameter can only NARROW the scope, never widen it | check every pin is tested with inScope()/scope.ids.includes() before it is used | ✅ |  |
| P09641 | `app/api/owner/issues/route.ts` — every exported handler returns a Response on every branch | read each exported handler top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09642 | `app/api/owner/issues/route.ts` — every read's `.error` is inspected, or the row says which figure degrades and how it is named | follow each `await sb.` result to an error test or an `rd()`/ReadSet wrapper | ✅ |  |
| P09643 | `app/api/owner/issues/route.ts` — a failed read is never rendered as a confident ₹0 / empty list | for each numeric or list output, check the failure path yields null/partial, not 0/[] | ✅ |  |
| P09644 | `app/api/owner/issues/route.ts` — the scope is applied BEFORE the query, never filtered afterwards | check each read carries `.eq/.in("restaurant_id", …)` or `p_ids`, not a post-filter on the result | ✅ |  |
| P09645 | `app/api/owner/issues/route.ts` — a `?rid=`/`?restaurant_id=` parameter can only NARROW the scope, never widen it | check every pin is tested with inScope()/scope.ids.includes() before it is used | ✅ |  |
| P09646 | `app/api/owner/khata/route.ts` — every exported handler returns a Response on every branch | read each exported handler top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09647 | `app/api/owner/khata/route.ts` — every read's `.error` is inspected, or the row says which figure degrades and how it is named | follow each `await sb.` result to an error test or an `rd()`/ReadSet wrapper | ✅ |  |
| P09648 | `app/api/owner/khata/route.ts` — a failed read is never rendered as a confident ₹0 / empty list | for each numeric or list output, check the failure path yields null/partial, not 0/[] | ✅ |  |
| P09649 | `app/api/owner/khata/route.ts` — the scope is applied BEFORE the query, never filtered afterwards | check each read carries `.eq/.in("restaurant_id", …)` or `p_ids`, not a post-filter on the result | ✅ |  |
| P09650 | `app/api/owner/khata/route.ts` — a `?rid=`/`?restaurant_id=` parameter can only NARROW the scope, never widen it | check every pin is tested with inScope()/scope.ids.includes() before it is used | ✅ |  |
| P09651 | `app/api/owner/oplog/route.ts` — every exported handler returns a Response on every branch | read each exported handler top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09652 | `app/api/owner/oplog/route.ts` — every read's `.error` is inspected, or the row says which figure degrades and how it is named | follow each `await sb.` result to an error test or an `rd()`/ReadSet wrapper | ✅ |  |
| P09653 | `app/api/owner/oplog/route.ts` — a failed read is never rendered as a confident ₹0 / empty list | for each numeric or list output, check the failure path yields null/partial, not 0/[] | ✅ |  |
| P09654 | `app/api/owner/oplog/route.ts` — the scope is applied BEFORE the query, never filtered afterwards | check each read carries `.eq/.in("restaurant_id", …)` or `p_ids`, not a post-filter on the result | ✅ |  |
| P09655 | `app/api/owner/oplog/route.ts` — a `?rid=`/`?restaurant_id=` parameter can only NARROW the scope, never widen it | check every pin is tested with inScope()/scope.ids.includes() before it is used | ✅ |  |
| P09656 | `app/api/owner/overview/route.ts` — every exported handler returns a Response on every branch | read each exported handler top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09657 | `app/api/owner/overview/route.ts` — every read's `.error` is inspected, or the row says which figure degrades and how it is named | follow each `await sb.` result to an error test or an `rd()`/ReadSet wrapper | ✅ |  |
| P09658 | `app/api/owner/overview/route.ts` — a failed read is never rendered as a confident ₹0 / empty list | for each numeric or list output, check the failure path yields null/partial, not 0/[] | ✅ |  |
| P09659 | `app/api/owner/overview/route.ts` — the scope is applied BEFORE the query, never filtered afterwards | check each read carries `.eq/.in("restaurant_id", …)` or `p_ids`, not a post-filter on the result | ✅ |  |
| P09660 | `app/api/owner/overview/route.ts` — a `?rid=`/`?restaurant_id=` parameter can only NARROW the scope, never widen it | check every pin is tested with inScope()/scope.ids.includes() before it is used | ✅ |  |
| P09661 | `app/api/owner/ratings/route.ts` — every exported handler returns a Response on every branch | read each exported handler top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09662 | `app/api/owner/ratings/route.ts` — every read's `.error` is inspected, or the row says which figure degrades and how it is named | follow each `await sb.` result to an error test or an `rd()`/ReadSet wrapper | ✅ |  |
| P09663 | `app/api/owner/ratings/route.ts` — a failed read is never rendered as a confident ₹0 / empty list | for each numeric or list output, check the failure path yields null/partial, not 0/[] | ✅ |  |
| P09664 | `app/api/owner/ratings/route.ts` — the scope is applied BEFORE the query, never filtered afterwards | check each read carries `.eq/.in("restaurant_id", …)` or `p_ids`, not a post-filter on the result | ✅ |  |
| P09665 | `app/api/owner/ratings/route.ts` — a `?rid=`/`?restaurant_id=` parameter can only NARROW the scope, never widen it | check every pin is tested with inScope()/scope.ids.includes() before it is used | ✅ |  |
| P09666 | `app/api/owner/reports/route.ts` — every exported handler returns a Response on every branch | read each exported handler top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09667 | `app/api/owner/reports/route.ts` — every read's `.error` is inspected, or the row says which figure degrades and how it is named | follow each `await sb.` result to an error test or an `rd()`/ReadSet wrapper | ✅ |  |
| P09668 | `app/api/owner/reports/route.ts` — a failed read is never rendered as a confident ₹0 / empty list | for each numeric or list output, check the failure path yields null/partial, not 0/[] | ✅ |  |
| P09669 | `app/api/owner/reports/route.ts` — the scope is applied BEFORE the query, never filtered afterwards | check each read carries `.eq/.in("restaurant_id", …)` or `p_ids`, not a post-filter on the result | ✅ |  |
| P09670 | `app/api/owner/reports/route.ts` — a `?rid=`/`?restaurant_id=` parameter can only NARROW the scope, never widen it | check every pin is tested with inScope()/scope.ids.includes() before it is used | ✅ |  |
| P09671 | `app/api/owner/settings/route.ts` — every exported handler returns a Response on every branch | read each exported handler top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09672 | `app/api/owner/settings/route.ts` — every read's `.error` is inspected, or the row says which figure degrades and how it is named | follow each `await sb.` result to an error test or an `rd()`/ReadSet wrapper | ✅ |  |
| P09673 | `app/api/owner/settings/route.ts` — a failed read is never rendered as a confident ₹0 / empty list | for each numeric or list output, check the failure path yields null/partial, not 0/[] | ✅ |  |
| P09674 | `app/api/owner/settings/route.ts` — the scope is applied BEFORE the query, never filtered afterwards | check each read carries `.eq/.in("restaurant_id", …)` or `p_ids`, not a post-filter on the result | ✅ |  |
| P09675 | `app/api/owner/settings/route.ts` — a `?rid=`/`?restaurant_id=` parameter can only NARROW the scope, never widen it | check every pin is tested with inScope()/scope.ids.includes() before it is used | ✅ |  |
| P09676 | `app/api/owner/staff/route.ts` — every exported handler returns a Response on every branch | read each exported handler top to bottom; every branch ends in NextResponse/Response | ✅ |  |
| P09677 | `app/api/owner/staff/route.ts` — every read's `.error` is inspected, or the row says which figure degrades and how it is named | follow each `await sb.` result to an error test or an `rd()`/ReadSet wrapper | ✅ |  |
| P09678 | `app/api/owner/staff/route.ts` — a failed read is never rendered as a confident ₹0 / empty list | for each numeric or list output, check the failure path yields null/partial, not 0/[] | ✅ |  |
| P09679 | `app/api/owner/staff/route.ts` — the scope is applied BEFORE the query, never filtered afterwards | check each read carries `.eq/.in("restaurant_id", …)` or `p_ids`, not a post-filter on the result | ✅ |  |
| P09680 | `app/api/owner/staff/route.ts` — a `?rid=`/`?restaurant_id=` parameter can only NARROW the scope, never widen it | check every pin is tested with inScope()/scope.ids.includes() before it is used | ✅ |  |
| P09681 | `app/api/owner/customers/route.ts` DELETE — the pay-later balance that blocks an erase is EXACT, not the top-N debtors | read the balance check; confirm it does not depend on lfh_khata_outstanding's p_limit ranking | ✅ | FIRST PASS ❌ — it filtered `lfh_khata_outstanding(p_limit:500)`, which returns only the 500 biggest-debt PEOPLE, so a guest ranked 501+ read as owing ₹0 and their khata record was anonymised while the debt stood. Fixed (item 2): a direct per-person open-bill read on the indexed (restaurant_id, khata_customer_id). |
| P09682 | `app/api/owner/customers/route.ts` DELETE — the `lfh_khata_outstanding_summary` probe's data is actually used, or the probe is gone | read the call; confirm its result is read and not only its `.error` | ✅ | FIRST PASS ❌ — `owing.data` was never read; the call existed only as a can-we-read probe and cost a round trip. Removed with item 2; the exact read's own `.error` does the same job. |
| P09683 | `app/api/admin/restaurants/settings/route.ts` — the banquet starting-number lock cannot be opened by a failed count | read the `banquet_bill_next` branch; confirm `issued.error` refuses rather than falling through to already=0 | ✅ | FIRST PASS ❌ — `issued.error` was unchecked, so `Number(issued.count)\|\|0` = 0 and the counter could be moved after bills existed. Fixed (item 4). |
| P09684 | `app/api/admin/restaurants/access-tree/route.ts` — a patch whose feature keys are ALL unknown does not answer "Saved" | read the `patch.features` branch; confirm a survivor count gates the column write, like `grants`/`sections` | ✅ | FIRST PASS ❌ — `setPatch.features` was assigned regardless, so the column was rewritten with its own value, the handler concluded something landed, and it also busted the guest menu cache. Fixed (item 5). |
| P09685 | `app/api/admin/restaurants/access-tree/route.ts` — the same for `channels`/`creds` | read the merged channels/creds branch; confirm the allow-list survivor count gates `setPatch.platform_channels` | ✅ | FIRST PASS ❌ — same shape as the features branch. Fixed (item 5). An allow-listed cred sent as "" still counts (it is a deliberate no-op the channel form sends on every save), so an ordinary save is unaffected. |
| P09686 | `app/api/owner/settings/route.ts` GET — the REAL owner's restaurant list read checks its error | read the `else` branch of the scope.all test and follow `r.error` | ✅ | FIRST PASS ❌ — unchecked, so a blip emptied the page: no restaurants, no feature switches, no printing rows, nothing saying why. This is F16's fault in the branch F16 did not cover. Fixed (item 3). |
| P09687 | `lib/ownerScope.ts` `ownerScopeOr503` has at least one caller | grep the whole repo for `ownerScopeOr503` | ✅ | FIRST PASS ❌ — zero callers. `ownerScope()` throws OwnerScopeUnavailable and nothing caught it, so the retryable 503 the T9 F22 fix was written to give became an unhandled 500 on every owner route. Fixed (item 1) by wiring all 12 routes to the helper. |
| P09688 | `app/api/owner/staff/route.ts` — `mgrStaffOpt("create")` and `("delete")` have call sites | grep `mgrStaffOpt(` in the file | ✅ | Only `reset_pw` is called. `create` and `delete` are dead, and no node in lib/accessTree writes `access_config.manage_staff.manager_opts` at all, so all three default open. NOT a fault today (default open = the behaviour before the switches were imagined) — reported as 🟡 item 12 with a 🔗 HANDOFF, because the fix is in another terminal's file. |
| P09689 | `app/api/admin/users/route.ts` — its payroll-effective formula matches lib/tableTags moduleLadder | compare line 85 against `allowed && (!ownerControl \|\| enabled !== false)` | ✅ | The expressions DIFFER when owner_control is true and enabled is NULL — but mig 220 declares `payroll_enabled BOOLEAN NOT NULL DEFAULT true`, so NULL is unreachable and the two always agree. Deliberately NOT changed: no person is worse off and touching a working money-gate formula to satisfy a shape argument is the wrong trade. |
| P09690 | `app/api/owner/audit/route.ts` POST — the classify write is safe to replay without withIdempotency | read mig 337's `lfh_cancel_classify` and confirm it states, and is, idempotent | ✅ | The RPC's own header says "Everything it does is idempotent, so an offline replay or a double-tap is safe" and the body re-reads the previous answer before acting. The manager twin is wrapped only because its whole dispatcher is. No finding. |
| P09691 | `app/api/admin/restaurants/branding/route.ts` + `logo/route.ts` — a brand change does not need the guest menu cache tag busting | read lib/menuDataServer.ts MenuBundle: it caches dishes + categories only, never accent_color or logo_url | ✅ | Confirmed: accent/logo are read outside that cache (AppShell/getSettings), so no revalidateTag is owed here. Would have been a wrong fix. |
| P09692 | `app/api/admin/restaurants/settings/route.ts` — writing table_count/table_names does not owe invalidateFloor(rid) | read lib/floorSummary.ts WINDOW_MS and scripts/verify-floor-share.mjs FLOOR_TABLES | ✅ | The shared window is 1500ms and `settings` is not a floor table. A 1.5s staleness on a floor nobody is watching while the admin edits settings is not a person-visible fault. Deliberately not "fixed". |
| P09693 | `app/api/admin/restaurants/route.ts` — `restaurants.name` is never JSONB, so raw-name reads cannot print "[object Object]" | read mig 078 (`name text NOT NULL`) and probe the dev DB's 17 rows | ✅ | All 17 are plain strings and the column is `text`. lib/restaurantNames' object branch is defensive only. So the two hand-rolled name reads in owner/reports are NOT a fault — reported as nothing. |
| P09694 | `app/api/admin/restaurants/route.ts` create_restaurant — the slug-uniqueness while-loop cannot spin forever | read the loop: a failed read yields `(null\|\|[]).length === 0` and exits | ✅ |  |
| P09695 | `app/api/admin/repair/route.ts` — `dropFloorAfter` keys its rid on the REQUEST, not a module variable | read the WeakMap<NextRequest,string> and confirm no module-level `let rid` | ✅ | Two admins repairing two restaurants at once cannot drop each other's snapshot. |
| P09696 | `app/api/admin/restaurants/credentials/route.ts` — a password never reaches a log line | read both logAction calls and openPassword's contract | ✅ |  |
| P09697 | `app/api/admin/restaurants/platform-channels/route.ts` — a channel's key value never reaches the response | read `present()`: it returns `{on, hasKey}` only | ✅ |  |
| P09698 | `app/api/admin/restaurants/export/route.ts` — the backup carries no password hash and no channel key | read the staff_users column list and the panelSafeSettings call | ✅ |  |
| P09699 | `app/api/admin/users/route.ts` PATCH — no branch selects `password_hash` into route memory | read the named column list at the top of PATCH | ✅ |  |
| P09700 | `app/api/owner/staff/route.ts` — the roster strips pay for a caller without canSeePay, in the JSON not just the UI | read `withoutPay()` and both of its call sites (roster + detail) | ✅ |  |
| P09701 | `app/api/owner/reports/route.ts` — `restaurantNames` is imported but never called; the two name reads are hand-rolled | grep restaurantNames in the file and read both `sb.from("restaurants").select("id, name")` sites | ✅ | Both hand-rolled reads are correct for THIS database: `restaurants.name` is `text NOT NULL` (mig 078) and all 17 dev rows are plain strings, so the "[object Object]" case the helper guards is unreachable. The dishes/categories read checks its error and limits to okIds.length; the merged-inventory one does neither, but it only labels a per-restaurant row that already degrades to "Restaurant". Left alone deliberately — the unused import is a lint warning, not a fault, and removing it is churn. |
| P09702 | `app/api/admin/repair/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09703 | `app/api/admin/resolve-error/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09704 | `app/api/admin/restaurants/access-tree/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09705 | `app/api/admin/restaurants/bill-preview/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09706 | `app/api/admin/restaurants/branding/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09707 | `app/api/admin/restaurants/create-defaults/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09708 | `app/api/admin/restaurants/credentials/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09709 | `app/api/admin/restaurants/export/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09710 | `app/api/admin/restaurants/google-review/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09711 | `app/api/admin/restaurants/health/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09712 | `app/api/admin/restaurants/logo/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09713 | `app/api/admin/restaurants/panels/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09714 | `app/api/admin/restaurants/platform-channels/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09715 | `app/api/admin/restaurants/quick-features/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09716 | `app/api/admin/restaurants/report/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09717 | `app/api/admin/restaurants/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09718 | `app/api/admin/restaurants/settings/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09719 | `app/api/admin/restaurants/staff-features/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09720 | `app/api/admin/revenue/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09721 | `app/api/admin/settings/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09722 | `app/api/admin/staff-online/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09723 | `app/api/admin/usage/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09724 | `app/api/admin/users/photo/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09725 | `app/api/admin/users/route.ts` — EVERY exported handler checks `tokenIsValid` before its first database call | read each handler's first statements; the cookie test must precede any `sb.` call | ✅ |  |
| P09726 | `app/api/owner/analytics/route.ts` — EVERY exported handler resolves the caller's scope before its first database call | read each handler; `ownerScopeOr503`/`scope()` must precede any `sb.` call | ✅ |  |
| P09727 | `app/api/owner/audit/route.ts` — EVERY exported handler resolves the caller's scope before its first database call | read each handler; `ownerScopeOr503`/`scope()` must precede any `sb.` call | ✅ |  |
| P09728 | `app/api/owner/customers/route.ts` — EVERY exported handler resolves the caller's scope before its first database call | read each handler; `ownerScopeOr503`/`scope()` must precede any `sb.` call | ✅ |  |
| P09729 | `app/api/owner/inventory/route.ts` — EVERY exported handler resolves the caller's scope before its first database call | read each handler; `ownerScopeOr503`/`scope()` must precede any `sb.` call | ✅ |  |
| P09730 | `app/api/owner/issues/route.ts` — EVERY exported handler resolves the caller's scope before its first database call | read each handler; `ownerScopeOr503`/`scope()` must precede any `sb.` call | ✅ |  |
| P09731 | `app/api/owner/khata/route.ts` — EVERY exported handler resolves the caller's scope before its first database call | read each handler; `ownerScopeOr503`/`scope()` must precede any `sb.` call | ✅ |  |
| P09732 | `app/api/owner/oplog/route.ts` — EVERY exported handler resolves the caller's scope before its first database call | read each handler; `ownerScopeOr503`/`scope()` must precede any `sb.` call | ✅ |  |
| P09733 | `app/api/owner/overview/route.ts` — EVERY exported handler resolves the caller's scope before its first database call | read each handler; `ownerScopeOr503`/`scope()` must precede any `sb.` call | ✅ |  |
| P09734 | `app/api/owner/ratings/route.ts` — EVERY exported handler resolves the caller's scope before its first database call | read each handler; `ownerScopeOr503`/`scope()` must precede any `sb.` call | ✅ |  |
| P09735 | `app/api/owner/reports/route.ts` — EVERY exported handler resolves the caller's scope before its first database call | read each handler; `ownerScopeOr503`/`scope()` must precede any `sb.` call | ✅ |  |
| P09736 | `app/api/owner/settings/route.ts` — EVERY exported handler resolves the caller's scope before its first database call | read each handler; `ownerScopeOr503`/`scope()` must precede any `sb.` call | ✅ |  |
| P09737 | `app/api/owner/staff/route.ts` — EVERY exported handler resolves the caller's scope before its first database call | read each handler; `ownerScopeOr503`/`scope()` must precede any `sb.` call | ✅ |  |
| P09738 | `find app/api/admin -name route.ts \| wc -l` equals the number that grep `tokenIsValid` | run both counts and compare (CLAUDE.md's own stated invariant) | ✅ |  |
| P09739 | no route under app/api/owner reaches the database without a scope, and none is on the deliberately-public list | walk app/api/owner and confirm every route.ts imports ownerScope/ownerScopeOr503 | ✅ |  |
| P09740 | `app/api/admin/repair/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09741 | `app/api/admin/resolve-error/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09742 | `app/api/admin/restaurants/access-tree/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09743 | `app/api/admin/restaurants/bill-preview/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09744 | `app/api/admin/restaurants/branding/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09745 | `app/api/admin/restaurants/create-defaults/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09746 | `app/api/admin/restaurants/credentials/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09747 | `app/api/admin/restaurants/export/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09748 | `app/api/admin/restaurants/google-review/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09749 | `app/api/admin/restaurants/health/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09750 | `app/api/admin/restaurants/logo/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09751 | `app/api/admin/restaurants/panels/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09752 | `app/api/admin/restaurants/platform-channels/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09753 | `app/api/admin/restaurants/quick-features/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09754 | `app/api/admin/restaurants/report/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09755 | `app/api/admin/restaurants/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09756 | `app/api/admin/restaurants/settings/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09757 | `app/api/admin/restaurants/staff-features/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09758 | `app/api/admin/revenue/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09759 | `app/api/admin/settings/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09760 | `app/api/admin/staff-online/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09761 | `app/api/admin/usage/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09762 | `app/api/admin/users/photo/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09763 | `app/api/admin/users/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09764 | `app/api/owner/analytics/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09765 | `app/api/owner/audit/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09766 | `app/api/owner/customers/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09767 | `app/api/owner/inventory/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09768 | `app/api/owner/issues/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09769 | `app/api/owner/khata/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09770 | `app/api/owner/oplog/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09771 | `app/api/owner/overview/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09772 | `app/api/owner/ratings/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09773 | `app/api/owner/reports/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09774 | `app/api/owner/settings/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09775 | `app/api/owner/staff/route.ts` — every read names its columns and every unbounded list read has a `.limit()` or a written reason | grep each `select(` in the file for a column list, and each non-head read for `.limit(`/`.range(`/`maybeSingle()` | ✅ |  |
| P09776 | every write in my territory that a guest or another panel reads busts the cache tag it owns | grep revalidateTag across my files and match it against what each write changes | ✅ |  |
| P09777 | `app/api/admin/restaurants/access-tree/route.ts` busts `menuTag(rid)` with `{expire:0}`, not "max" | read the revalidateTag call (T13's measured reason: "max" serves one more stale read) | ✅ |  |
| P09778 | no route in my territory adds a column to `settings` | grep for ALTER/new column names; there is no migration in this PR | ✅ |  |
| P09779 | no route in my territory introduces a new migration | `git diff --name-only` contains no supabase/migrations file | ✅ |  |
| P09780 | no route in my territory introduces a new screen or module | `git diff --name-only` contains no page.tsx / accessModel change | ✅ |  |
| P09781 | no route in my territory grants a permission that lib/staffCaps does not enforce | read the two set_permissions handlers: both allow-list from capsForRole() | ✅ |  |
| P09782 | `app/api/admin/users/route.ts` set_permissions refuses an unknown key rather than storing it | read the capByKey/cap lookup and its `return bad` | ✅ |  |
| P09783 | `app/api/owner/staff/route.ts` set_permissions refuses an unknown key rather than storing it | read the capByKey lookup and its `return bad` | ✅ |  |
| P09784 | a manager can never GRANT a power through `/api/owner/staff` — only reduce or reset it | read the `s.actor === "manager" && (v === "on" \|\| v === "pin")` refusal | ✅ |  |
| P09785 | a manager can never delete a login through `/api/owner/staff` | read the first guard in deleteImpl | ✅ |  |
| P09786 | a manager never sees another manager's or an owner's account through `/api/owner/staff` | read assignableFor() and its three call sites (list, detail, target) | ✅ |  |
| P09787 | `/api/admin/users` refuses to manage an OWNER account except for the three profile actions | read the OWNER_OK set and its test | ✅ |  |
| P09788 | kitchen has NO profile, in both staff routes | read hasProfile()/PROFILE_ROLES and the refusals in `target()` and admin `set_job` | ✅ |  |
| P09789 | a pay setup cannot be stored for a role no payment can be recorded against | read admin `set_job`'s in_payroll guard | ✅ |  |
| P09790 | a person with pay history is never deleted, from either door | read payHistoryBlocksDelete() in both DELETE handlers | ✅ |  |
| P09791 | a payment can only be recorded for someone the owner put on the pay list | read postImpl's `t.u.in_payroll` refusal | ✅ |  |
| P09792 | a pay-ledger entry is never edited or deleted — only voided WITH a reason | read void_payment: the row stays, reason ≥3 chars required | ✅ |  |
| P09793 | no route in my territory can erase or hide an issued sale | read admin/repair delete_order (soft delete + recordRemoval) and confirm no hard DELETE on orders/sessions | ✅ |  |
| P09794 | a bill removed through the Repair Kit reaches the Removals audit, not only the activity log | read both recordRemoval calls in admin/repair | ✅ |  |
| P09795 | every repair op requires a reason and writes a warn-level line | read the `if (!reason)` refusal and logRepair | ✅ |  |
| P09796 | `X-LFH-Expect` (no silent overwrites) is honoured on every free-text write in my territory | grep expectClash in my files and check each free-text branch is behind it | ✅ |  |
| P09797 | `npm run verify:owner-clash` passes | run it | ✅ |  |
| P09798 | `npm run verify:read-guards` passes | run it | ✅ |  |
| P09799 | `npm run verify:write-paths` passes | run it | ✅ |  |
| P09800 | `npm run verify:server-only` passes | run it | ✅ |  |
| P09801 | `npm run verify:test-safety` passes | run it | ✅ |  |
| P09802 | GET /api/admin/usage answers as it should | headless on :4120 with the cached admin cookie — 200 with rows+totals; every row has a name and a slug | ✅ |  |
| P09803 | GET /api/admin/revenue answers as it should | headless on :4120 with the cached admin cookie — 200; mrr/arr/byStatus present; Trial count matches /api/admin/billing's | ✅ |  |
| P09804 | GET /api/admin/staff-online answers as it should | headless on :4120 with the cached admin cookie — 200; every staff row carries restaurantName or null, never undefined | ✅ |  |
| P09805 | GET /api/admin/restaurants/health answers as it should | headless on :4120 with the cached admin cookie — 200 with one health row per restaurant, no money field anywhere | ✅ |  |
| P09806 | GET /api/admin/restaurants answers as it should | headless on :4120 with the cached admin cookie — 200; restaurants[] + owners[]; no binned restaurant in the live list | ✅ |  |
| P09807 | GET /api/admin/restaurants?deleted=1 answers as it should | headless on :4120 with the cached admin cookie — 200; the bin lists no purged restaurant | ✅ |  |
| P09808 | GET /api/admin/restaurants/create-defaults answers as it should | headless on :4120 with the cached admin cookie — 200 with defaults null or an object | ✅ |  |
| P09809 | GET /api/admin/restaurants/report?restaurant_id=<French House>&range=today answers as it should | headless on :4120 with the cached admin cookie — 200; usage counts only, no revenue key | ✅ |  |
| P09810 | GET /api/admin/restaurants/settings?restaurant_id=<French House> answers as it should | headless on :4120 with the cached admin cookie — 200; settings + slug + codes for tables 1..table_count | ✅ |  |
| P09811 | GET /api/admin/restaurants/panels?restaurant_id=<French House> answers as it should | headless on :4120 with the cached admin cookie — 200; all four panels true | ✅ |  |
| P09812 | POST /api/admin/restaurants/panels answers as it should | headless on :4120 with the cached admin cookie — 410 with the words "was removed and has no effect" | ✅ |  |
| P09813 | GET /api/admin/restaurants/quick-features?restaurant_id=<French House> answers as it should | headless on :4120 with the cached admin cookie — 200; banquet/auto_print_kot/payroll booleans | ✅ |  |
| P09814 | GET /api/admin/restaurants/staff-features?restaurant_id=<French House> answers as it should | headless on :4120 with the cached admin cookie — 200; flags + hasSettings | ✅ |  |
| P09815 | GET /api/admin/restaurants/google-review?restaurant_id=<French House> answers as it should | headless on :4120 with the cached admin cookie — 200; url + a mode from the four | ✅ |  |
| P09816 | GET /api/admin/restaurants/platform-channels?restaurant_id=<French House> answers as it should | headless on :4120 with the cached admin cookie — 200; three channels, each {on,hasKey}, NO key value | ✅ |  |
| P09817 | GET /api/admin/restaurants/branding?restaurant_id=<French House> answers as it should | headless on :4120 with the cached admin cookie — 200; accent_color/theme/hero_title/tagline/logo | ✅ |  |
| P09818 | GET /api/admin/restaurants/credentials?restaurant_id=<French House> answers as it should | headless on :4120 with the cached admin cookie — 200; logins sorted owner→manager→kitchen→tablet; vaultReady present | ✅ |  |
| P09819 | GET /api/admin/restaurants/bill-preview?rid=<French House>&mode=bill answers as it should | headless on :4120 with the cached admin cookie — 200 text/html with French House's own header, not #1's | ✅ |  |
| P09820 | GET /api/admin/restaurants/bill-preview?rid=<French House>&mode=kot answers as it should | headless on :4120 with the cached admin cookie — 200 text/html, a kitchen ticket | ✅ |  |
| P09821 | GET /api/admin/restaurants/access-tree?restaurant_id=<French House> answers as it should | headless on :4120 with the cached admin cookie — 200 with `state` only — no 25KB `sections` blob | ✅ |  |
| P09822 | GET /api/admin/restaurants/export?rid=<French House> answers as it should | headless on :4120 with the cached admin cookie — 200 JSON attachment; no password_hash, no channel key | ✅ |  |
| P09823 | GET /api/admin/settings answers as it should | headless on :4120 with the cached admin cookie — 200; retention days + audit years + auditYearOptions + environment | ✅ |  |
| P09824 | GET /api/admin/users answers as it should | headless on :4120 with the cached admin cookie — 200; no owner in the list; every row has hasPin and no pin_hash | ✅ |  |
| P09825 | GET /api/admin/users?id=<a French House manager> answers as it should | headless on :4120 with the cached admin cookie — 200; person + restaurant + payrollOn + activity | ✅ |  |
| P09826 | GET /api/admin/repair?restaurant_id=<French House> answers as it should | headless on :4120 with the cached admin cookie — 200; sessions + orders, no total/amount field on a session | ✅ |  |
| P09827 | GET /api/owner/overview?scope=all answers as it should | headless on :4120 with the cached admin cookie — 200; restaurants[] + totals + entitlements + modules | ✅ |  |
| P09828 | GET /api/owner/overview?scope=<French House> answers as it should | headless on :4120 with the cached admin cookie — 200; exactly the restaurants that owner owns | ✅ |  |
| P09829 | GET /api/owner/analytics?scope=all&range=today answers as it should | headless on :4120 with the cached admin cookie — 200; scope:'group', range echoed as 'today' | ✅ |  |
| P09830 | GET /api/owner/analytics?scope=all&range=NOT_A_RANGE answers as it should | headless on :4120 with the cached admin cookie — 200 and `range` comes back as 'today', never the junk | ✅ |  |
| P09831 | GET /api/owner/analytics?scope=<French House>&rid=<French House>&range=7d answers as it should | headless on :4120 with the cached admin cookie — 200; scope:'restaurant', kpis, paidOrders present | ✅ |  |
| P09832 | GET /api/owner/reports?scope=all&type=sales&range=30d answers as it should | headless on :4120 with the cached admin cookie — 200; rows + totals; tax null for an all-restaurants scope | ✅ |  |
| P09833 | GET /api/owner/reports?scope=<French House>&rid=<French House>&type=sales&range=30d answers as it should | headless on :4120 with the cached admin cookie — 200; tax split components sum EXACTLY to totals.tax | ✅ |  |
| P09834 | GET /api/owner/reports?...&type=daysummary&range=day&date=<today IST> answers as it should | headless on :4120 with the cached admin cookie — 200; the business day, not the calendar day | ✅ |  |
| P09835 | GET /api/owner/reports?...&type=bogus answers as it should | headless on :4120 with the cached admin cookie — 400 'unknown report type' | ✅ |  |
| P09836 | GET /api/owner/reports?...&type=payments&range=7d answers as it should | headless on :4120 with the cached admin cookie — 200; rows sorted by revenue desc | ✅ |  |
| P09837 | GET /api/owner/reports?...&type=byrestaurant&range=30d answers as it should | headless on :4120 with the cached admin cookie — 200; one row per in-scope restaurant | ✅ |  |
| P09838 | GET /api/owner/oplog?scope=<French House>&limit=200&page=1 answers as it should | headless on :4120 with the cached admin cookie — 200; no row with panel 'admin' or 'db'; no ui_taps; pages ≥ 1 | ✅ |  |
| P09839 | GET /api/owner/oplog?...&page=2 answers as it should | headless on :4120 with the cached admin cookie — 200; a different page of the same total | ✅ |  |
| P09840 | GET /api/owner/audit?scope=<French House> answers as it should | headless on :4120 with the cached admin cookie — 200; removals + kindCounts; no 'removal_classified' chip | ✅ |  |
| P09841 | GET /api/owner/audit?scope=<French House>&detail=<a real id> answers as it should | headless on :4120 with the cached admin cookie — 200 with canRestore:false | ✅ |  |
| P09842 | GET /api/owner/audit?scope=<French House>&detail=999999999 answers as it should | headless on :4120 with the cached admin cookie — 404 'not found', not a 500 | ✅ |  |
| P09843 | GET /api/owner/issues?scope=<French House> answers as it should | headless on :4120 with the cached admin cookie — 200; issues + openCount; openCount is a DB head-count | ✅ |  |
| P09844 | GET /api/owner/ratings?scope=<French House> answers as it should | headless on :4120 with the cached admin cookie — 200; summary.dist has five entries | ✅ |  |
| P09845 | GET /api/owner/khata?scope=<French House> answers as it should | headless on :4120 with the cached admin cookie — 200; summary from the aggregate, never summed from `customers` | ✅ |  |
| P09846 | GET /api/owner/customers?scope=<French House> answers as it should | headless on :4120 with the cached admin cookie — 200; summary tiles + customers + restaurants picker | ✅ |  |
| P09847 | GET /api/owner/customers?scope=<French House>&phone=<a real one> answers as it should | headless on :4120 with the cached admin cookie — 200 with `detail` only — no tiles recomputed | ✅ |  |
| P09848 | GET /api/owner/inventory?scope=<French House>&rid=<French House> answers as it should | headless on :4120 with the cached admin cookie — 200 or a 403 that says inventory isn't enabled — never a page of zeroes | ✅ |  |
| P09849 | GET /api/owner/inventory?scope=all&estate=1 answers as it should | headless on :4120 with the cached admin cookie — 200 estate[] + totals + offCount, or the incomplete-list 503 | ✅ |  |
| P09850 | GET /api/owner/settings?scope=all answers as it should | headless on :4120 with the cached admin cookie — 200; the ADMIN view lists every restaurant (F16 stays fixed) | ✅ |  |
| P09851 | GET /api/owner/settings?scope=<French House> answers as it should | headless on :4120 with the cached admin cookie — 200; restaurants[] is not empty for a restaurant that exists | ✅ |  |
| P09852 | GET /api/owner/staff?scope=<French House> answers as it should | headless on :4120 with the cached admin cookie — 200; actor, restaurants[].payAccess, staff[] with hasPin | ✅ |  |
| P09853 | GET /api/owner/staff?scope=<French House>&view=real answers as it should | headless on :4120 with the cached admin cookie — 200 and `actor` comes back 'manager', not 'admin' | ✅ |  |
| P09854 | GET /api/owner/staff?scope=<French House>&staff=<a manager> answers as it should | headless on :4120 with the cached admin cookie — 200; person + tree with creds:{} — never a masked channel key | ✅ |  |
| P09855 | GET /api/owner/staff?scope=<French House>&staff=<a kitchen login> answers as it should | headless on :4120 with the cached admin cookie — 200 notEligible:true — kitchen has no profile | ✅ |  |
| P09856 | GET /api/owner/staff?scope=<French House>&staff=<a bogus uuid> answers as it should | headless on :4120 with the cached admin cookie — 404 'That person isn't on your staff.' | ✅ |  |
| P09857 | POST /api/admin/restaurants/quick-features flips banquet ON then back to its original value | headless on :4120; read the effective value before, flip, re-read, restore in a finally block | ✅ |  |
| P09858 | POST /api/admin/restaurants/staff-features flips auto_print_kot_allowed and restores it | same shape — capture the original, flip, verify, restore in a finally block | ✅ |  |
| P09859 | POST /api/admin/restaurants/google-review refuses a Google mode with no link | post mode:'google' with url:'' — expect 400 with the 'Add the Google review link' sentence | ✅ |  |
| P09860 | POST /api/admin/restaurants/google-review round-trips a link and then restores the original | capture, set a test URL, re-read, restore in a finally block | ✅ |  |
| P09861 | POST /api/admin/restaurants/platform-channels stores a key and never hands it back | set a throwaway key, GET, assert hasKey:true and no key string anywhere in the body, then clear it | ✅ |  |
| P09862 | POST /api/admin/restaurants/branding refuses a non-hex accent colour | post accent_color:'notacolour' — expect 400 naming hex | ✅ |  |
| P09863 | POST /api/admin/restaurants/branding refuses an unknown restaurant id | post a valid-shaped uuid that does not exist — expect 404 'Restaurant not found.' | ✅ |  |
| P09864 | POST /api/admin/restaurants/logo refuses an SVG | post multipart with image/svg+xml — expect 400 'PNG, JPG or WEBP' | ✅ |  |
| P09865 | POST /api/admin/restaurants/logo refuses a file over 1MB | post a 1.1MB PNG — expect 400 '1 MB or smaller' | ✅ |  |
| P09866 | POST /api/admin/users/photo refuses an SVG and a file over 2MB | two posts — expect the two 400 sentences | ✅ |  |
| P09867 | POST /api/admin/settings clamps a retention window to 30 days and snaps audit years to the offered set | post 9999 days / 4 years, read back 30 and 5, then restore the originals | ✅ |  |
| P09868 | POST /api/admin/restaurants/settings refuses a banquet starting number once bills exist | on a restaurant with banquet bills, post banquet_bill_next — expect the 409 naming how many bills | ✅ |  |
| P09869 | POST /api/admin/restaurants/settings action:regen_code mints a new code for one table and logs it | capture the old code, regen, assert it changed, assert a table_qr_regen line exists | ✅ |  |
| P09870 | POST /api/admin/restaurants/access-tree saves one manager grant and the Activity line reads as English | flip one grant, read /api/admin/oplog for an access_change line naming the ROW, then restore it | ✅ |  |
| P09871 | POST /api/admin/restaurants/access-tree answers 400 when nothing in the patch could be saved | post `{grants:{not_a_flag:true}}` — expect 'Nothing in that change could be saved' | ✅ |  |
| P09872 | POST /api/admin/restaurants/access-tree answers 400 for a features patch of only unknown keys | post `{features:{not_a_feature:true}}` — expect the same refusal, NOT a green 'Saved' | ✅ |  |
| P09873 | PATCH /api/owner/issues resolves and reopens one complaint and both land in the owner's log | patch resolved then open on a test issue I created, then delete that issue row by id | ✅ |  |
| P09874 | PATCH /api/owner/ratings writes a note, refuses a stale one, and clears it | patch with X-LFH-Expect matching, then with a stale expectation (expect the clash 409), then clear | ✅ |  |
| P09875 | PATCH /api/owner/settings refuses a module the admin has not transferred | patch a module whose *_owner_control is false — expect 403 'hasn't handed you this switch' | ✅ |  |
| P09876 | /aevinite/restaurants at 1280×800 — the Restaurants list — every row shows a name, an owner or '—', and its panel chips | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09877 | /aevinite/restaurants at 360×780 dpr3 (Samsung A35) — the Restaurants list — every row shows a name, an owner or '—', and its panel chips, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09878 | /aevinite/restaurants at 1280×800 — the recycle bin tab — a purged restaurant is absent, a binned one shows its countdown | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09879 | /aevinite/restaurants at 360×780 dpr3 (Samsung A35) — the recycle bin tab — a purged restaurant is absent, a binned one shows its countdown, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09880 | /aevinite/restaurants (one restaurant open) at 1280×800 — the Settings tab — every field populated from that restaurant, none from #1 | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09881 | /aevinite/restaurants (one restaurant open) at 360×780 dpr3 (Samsung A35) — the Settings tab — every field populated from that restaurant, none from #1, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09882 | /aevinite/restaurants (one restaurant open) at 1280×800 — the Quick features card — each toggle shows the EFFECTIVE state | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09883 | /aevinite/restaurants (one restaurant open) at 360×780 dpr3 (Samsung A35) — the Quick features card — each toggle shows the EFFECTIVE state, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09884 | /aevinite/restaurants (one restaurant open) at 1280×800 — the Platform channels card — 'Key saved' but never the key | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09885 | /aevinite/restaurants (one restaurant open) at 360×780 dpr3 (Samsung A35) — the Platform channels card — 'Key saved' but never the key, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09886 | /aevinite/restaurants (one restaurant open) at 1280×800 — the Credentials / handover sheet — one row per login, ★ on the primary owner | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09887 | /aevinite/restaurants (one restaurant open) at 360×780 dpr3 (Samsung A35) — the Credentials / handover sheet — one row per login, ★ on the primary owner, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09888 | /aevinite/restaurants (one restaurant open) at 1280×800 — the Branding card — the colour swatches match the stored theme | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09889 | /aevinite/restaurants (one restaurant open) at 360×780 dpr3 (Samsung A35) — the Branding card — the colour swatches match the stored theme, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09890 | /aevinite/access at 1280×800 — the Access & permissions tree loads and its 'Recent changes here' strip reads as English | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09891 | /aevinite/access at 360×780 dpr3 (Samsung A35) — the Access & permissions tree loads and its 'Recent changes here' strip reads as English, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09892 | /aevinite/access at 1280×800 — the Format-of-bill preview opens a real page, not a placeholder | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09893 | /aevinite/access at 360×780 dpr3 (Samsung A35) — the Format-of-bill preview opens a real page, not a placeholder, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09894 | /aevinite/users at 1280×800 — the staff list — no owner row, every row shows its restaurant | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09895 | /aevinite/users at 360×780 dpr3 (Samsung A35) — the staff list — no owner row, every row shows its restaurant, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09896 | /aevinite/users (one person open) at 1280×800 — the profile panel — the pay block is present or absent, never half-drawn | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09897 | /aevinite/users (one person open) at 360×780 dpr3 (Samsung A35) — the profile panel — the pay block is present or absent, never half-drawn, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09898 | /aevinite/revenue at 1280×800 — MRR/ARR/collected tiles and the 12-month bar chart fill their cards | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09899 | /aevinite/revenue at 360×780 dpr3 (Samsung A35) — MRR/ARR/collected tiles and the 12-month bar chart fill their cards, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09900 | /aevinite/usage at 1280×800 — the usage table and its totals row | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09901 | /aevinite/usage at 360×780 dpr3 (Samsung A35) — the usage table and its totals row, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09902 | /aevinite/settings at 1280×800 — the Environment row names the BACKUP stack, not a hard-coded 'Production' | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09903 | /aevinite/settings at 360×780 dpr3 (Samsung A35) — the Environment row names the BACKUP stack, not a hard-coded 'Production', nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09904 | /aevinite/repair at 1280×800 — the Repair Kit lists this restaurant's open tables and recent orders, with no money column | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09905 | /aevinite/repair at 360×780 dpr3 (Samsung A35) — the Repair Kit lists this restaurant's open tables and recent orders, with no money column, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09906 | /aevinite/staff-online at 1280×800 — who is online, or a calm empty state | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09907 | /aevinite/staff-online at 360×780 dpr3 (Samsung A35) — who is online, or a calm empty state, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09908 | /owner (via the act-as door) at 1280×800 — the cockpit — restaurant cards, totals, and the hero shortcuts | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09909 | /owner (via the act-as door) at 360×780 dpr3 (Samsung A35) — the cockpit — restaurant cards, totals, and the hero shortcuts, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09910 | /owner/reports at 1280×800 — the Sales report — chart + GST-style table, chart total equals the KPI | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09911 | /owner/reports at 360×780 dpr3 (Samsung A35) — the Sales report — chart + GST-style table, chart total equals the KPI, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09912 | /owner/reports at 1280×800 — the Day summary sheet — settlement block present or absent, never an empty table beside real revenue | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09913 | /owner/reports at 360×780 dpr3 (Samsung A35) — the Day summary sheet — settlement block present or absent, never an empty table beside real revenue, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09914 | /owner/reports at 1280×800 — Team & pay — no ₹0 where a figure could not be read | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09915 | /owner/reports at 360×780 dpr3 (Samsung A35) — Team & pay — no ₹0 where a figure could not be read, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09916 | /owner/khata at 1280×800 — Pay Later — total outstanding, collected today/this month, and the people list | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09917 | /owner/khata at 360×780 dpr3 (Samsung A35) — Pay Later — total outstanding, collected today/this month, and the people list, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09918 | /owner/customers at 1280×800 — the four tiles + the guest list + the restaurant picker | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09919 | /owner/customers at 360×780 dpr3 (Samsung A35) — the four tiles + the guest list + the restaurant picker, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09920 | /owner/staff at 1280×800 — the team roster — completeness rings, pay column present only when allowed | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09921 | /owner/staff at 360×780 dpr3 (Samsung A35) — the team roster — completeness rings, pay column present only when allowed, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09922 | /owner/logs at 1280×800 — Activity — paged footer says 'page 1 of N', no admin rows | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09923 | /owner/logs at 360×780 dpr3 (Samsung A35) — Activity — paged footer says 'page 1 of N', no admin rows, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09924 | /owner/activity (Removals tab) at 1280×800 — Removals record — chips carry whole-record counts, not this-page counts | headless on :4120 (an /owner screen needs the admin act-as door — a bare admin cookie is deliberately sent to the sign-in), screenshot, then Read the image | ✅ |  |
| P09925 | /owner/activity (Removals tab) at 360×780 dpr3 (Samsung A35) — Removals record — chips carry whole-record counts, not this-page counts, nothing clipped or overlapping | headless A35 emulation on :4120 (act-as door for /owner), screenshot, then Read the image; horizontal overflow measured, not eyeballed | ✅ |  |
| P09926 | Trace: an admin flips Quick features → banquet ON → the manager panel's banquet controls appear and the tablet cap becomes grantable | follow the write to the column, then to every reader; quick-features writes the same settings columns Access reads and moduleLadder enforces | ✅ |  |
| P09927 | Trace: an admin flips Quick features → banquet ON → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09928 | Trace: an admin flips Quick features → auto-print KOT ON → the kitchen starts queueing tickets (mig 335) and the owner's Settings page shows a printing row | follow the write to the column, then to every reader; auto_print_kot_allowed AND auto_print_kot are both required by the kitchen route | ✅ |  |
| P09929 | Trace: an admin flips Quick features → auto-print KOT ON → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09930 | Trace: an admin flips staff-features → banquet_allowed OFF → the banquet screens disappear from manager AND the server refuses a banquet write | follow the write to the column, then to every reader; one column, two readers | ✅ |  |
| P09931 | Trace: an admin flips staff-features → banquet_allowed OFF → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09932 | Trace: an admin sets a Google review mode → the guest menu shows the right invite in all THREE doors (/menu, /r/<slug>/menu, /q/<code>) | follow the write to the column, then to every reader; the mode lives on settings and the guest reads it per door | ✅ |  |
| P09933 | Trace: an admin sets a Google review mode → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09934 | Trace: an admin saves a guest-facing feature on the Access tree → the guest menu changes on the next load, not in 24h | follow the write to the column, then to every reader; revalidateTag(menuTag(rid), {expire:0}) | ✅ |  |
| P09935 | Trace: an admin saves a guest-facing feature on the Access tree → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09936 | Trace: an admin changes tax_components → the owner's tax report split and the printed bill agree to the paisa | follow the write to the column, then to every reader; one settings source; the report only re-proportions | ✅ |  |
| P09937 | Trace: an admin changes tax_components → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09938 | Trace: an admin changes price_tax_mode to composition → the printed bill shows no tax line AND the owner's tax report says so instead of a 50/50 table of zeroes | follow the write to the column, then to every reader; the `composition` flag travels to the report | ✅ |  |
| P09939 | Trace: an admin changes price_tax_mode to composition → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09940 | Trace: an admin renames a table (table_names) → the manager floor, the tablet floor and the PRINTED bill all use the new name | follow the write to the column, then to every reader; table_names is read by all three | ✅ |  |
| P09941 | Trace: an admin renames a table (table_names) → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09942 | Trace: an admin regenerates one table's QR → the old printed code stops working and /q/<old> lands on 'invalid code', not a neighbouring table | follow the write to the column, then to every reader; table_qr_codes is globally unique | ✅ |  |
| P09943 | Trace: an admin regenerates one table's QR → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09944 | Trace: an admin uploads a restaurant logo → the guest menu, the bill preview and the printed bill all show THAT restaurant's logo, never #1's | follow the write to the column, then to every reader; logo_url on the restaurants row | ✅ |  |
| P09945 | Trace: an admin uploads a restaurant logo → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09946 | Trace: an admin changes branding accent → the guest menu accent changes and the dish pages (which render OUTSIDE AppShell) change with it | follow the write to the column, then to every reader; accent must be repeated on the dish route | ✅ |  |
| P09947 | Trace: an admin changes branding accent → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09948 | Trace: an admin assigns a new owner → the OLD owner immediately stops seeing that restaurant in /owner | follow the write to the column, then to every reader; the restaurant_owners delete is the scope revoke | ✅ |  |
| P09949 | Trace: an admin assigns a new owner → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09950 | Trace: an admin clears a restaurant's owner → the previous owner's membership row is gone, not just owner_user_id | follow the write to the column, then to every reader; PATCH deletes the join row and surfaces its error | ✅ |  |
| P09951 | Trace: an admin clears a restaurant's owner → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09952 | Trace: an admin bins a restaurant → the guest menu 404s, staff logins are refused, and it leaves every admin list | follow the write to the column, then to every reader; deleted_at drives the resolver and the login gate | ✅ |  |
| P09953 | Trace: an admin bins a restaurant → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09954 | Trace: an admin restores a binned restaurant whose name was taken → the RETURNING restaurant is renamed, never the one serving guests | follow the write to the column, then to every reader; the restore endpoint renames the returner | ✅ |  |
| P09955 | Trace: an admin restores a binned restaurant whose name was taken → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09956 | Trace: an admin purges a binned restaurant → its bills stay readable in the Bills ledger and it disappears from the bin | follow the write to the column, then to every reader; mig 309 keeps the row, marked purged_at | ✅ |  |
| P09957 | Trace: an admin purges a binned restaurant → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09958 | Trace: an admin sets a retention window → EVERY restaurant's settings row moves, and the change is itself audited | follow the write to the column, then to every reader; one platform-wide update + retention_change | ✅ |  |
| P09959 | Trace: an admin sets a retention window → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09960 | Trace: an admin resets a login from the handover sheet → that person is signed out everywhere and the sheet shows the new password | follow the write to the column, then to every reader; token_version bump | ✅ |  |
| P09961 | Trace: an admin resets a login from the handover sheet → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09962 | Trace: an admin adds a waiter with sections on → the tablet only shows that waiter's tables and the manager's who-serves-what agrees | follow the write to the column, then to every reader; assigned_tables via newWaiterTables | ✅ |  |
| P09963 | Trace: an admin adds a waiter with sections on → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09964 | Trace: an admin sets a per-person permission → the manager/tablet panel refuses the action server-side, not just hides it | follow the write to the column, then to every reader; staff_users.permissions is read by the enforcers | ✅ |  |
| P09965 | Trace: an admin sets a per-person permission → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09966 | Trace: the owner records a salary → the dashboard's 'after staff pay' figure moves on the next open, not whenever an order happens | follow the write to the column, then to every reader; fpWithStaffPay folds staff_payments into the fingerprint | ✅ |  |
| P09967 | Trace: the owner records a salary → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09968 | Trace: the owner voids a pay entry → the roster's 'paid this month', the Team & pay report and the day sheet all drop it | follow the write to the column, then to every reader; voided_at is excluded by every reader | ✅ |  |
| P09969 | Trace: the owner voids a pay entry → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09970 | Trace: the owner adds someone to the pay list → a payment becomes possible for them and they start counting as an expense | follow the write to the column, then to every reader; in_payroll gates both | ✅ |  |
| P09971 | Trace: the owner adds someone to the pay list → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09972 | Trace: the owner erases a guest → the guest is gone from the Customers list AND from the manager's 'Collect later' person picker | follow the write to the column, then to every reader; khata_customers is anonymised in the same run | ✅ |  |
| P09973 | Trace: the owner erases a guest → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09974 | Trace: the owner answers 'was the food made?' on a cancellation → stock moves, a food_loss expense appears, and the dashboard's Expenses tile rises | follow the write to the column, then to every reader; lfh_cancel_classify + the analytics foodLoss read | ✅ |  |
| P09975 | Trace: the owner answers 'was the food made?' on a cancellation → nothing ELSE changes for another restaurant | re-read Aangan's equivalent screen/API before and after and diff it | ✅ |  |
| P09976 | Judgment: If the owner's connection drops mid-save on /owner/staff, what does he see and is it the truth? | reasoned against the code and the owner's recorded decisions | ✅ | every write here answers with a sentence or nothing; a lost reply is re-sent through withIdempotency and runs at most once, so a retried save cannot double-pay anyone. A real restaurant needs exactly this. |
| P09977 | Judgment: If the owner's connection drops mid-save on the admin Access tree, is a half-applied save possible? | reasoned against the code and the owner's recorded decisions | ✅ | no: every allow-list runs and can `return bad` BEFORE the first write, and the two writes go out together at the end. That ordering is load-bearing and is now a ledger row. |
| P09978 | Judgment: Does a real restaurant need the Repair Kit's edit_time? | reasoned against the code and the owner's recorded decisions | ✅ | yes — a KOT punched at 04:50 on the wrong side of the 05:00 business day is a real mistake, and it is audited with before→after. Keep it. |
| P09979 | Judgment: Should the admin see a restaurant's food revenue anywhere in my routes? | reasoned against the code and the owner's recorded decisions | ✅ | no, and none of them returns it. /api/admin/restaurants/export is the one deliberate exception and it says so in its own _meta. Correct as built. |
| P09980 | Judgment: Is `ensureCodes` writing rows on a GET acceptable? | reasoned against the code and the owner's recorded decisions | ✅ | yes — a table with no QR code is unusable and minting one is idempotent per (restaurant, table). A separate 'mint' button would be a step a person forgets. |
| P09981 | Judgment: Does a restaurant need the four backend-only flags to stay invisible? | reasoned against the code and the owner's recorded decisions | ✅ | yes, and they are. Not re-reported. |
| P09982 | Judgment: Is refusing a banquet starting-number change after the first bill the right call, or too strict? | reasoned against the code and the owner's recorded decisions | ✅ | right. An accountant's series that can move backwards is the thing an audit checks. The prefix and style stay editable, which is the pressure valve. |
| P09983 | Judgment: Would a busy restaurant notice the owner routes' snapshot cache being stale? | reasoned against the code and the owner's recorded decisions | ✅ | no — each key carries its resolved window and each fingerprint watches the table that actually moves. Verified per route as its own phase. |
| P09984 | Judgment: Should /api/owner/audit let the OWNER answer 'was the food made?' | reasoned against the code and the owner's recorded decisions | ✅ | yes — he asked for it directly on 2026-08-19 and only a person who was there can know. It is append-only and names who answered. |
| P09985 | Judgment: Is it right that a manager can disable a login but never delete one? | reasoned against the code and the owner's recorded decisions | ✅ | yes (owner, 2026-08-02) and both doors enforce it. |
| P09986 | Judgment: Is the 500-people cap on the Pay Later list the right shape? | reasoned against the code and the owner's recorded decisions | ✅ | yes for the LIST (bounded by person, so each shown person is complete) — but it was wrong as the basis for an erase decision. That is item 2. |
| P09987 | Judgment: Does the admin console need the database's own words when something breaks? | reasoned against the code and the owner's recorded decisions | ✅ | yes, and adminFail already keeps both halves — a plain sentence plus `detail`. Four of my files were still sending the raw sentence alone; that is improvement 8. |
| P09988 | Judgment: Would a real operator want the export file to say which tables it could not read? | reasoned against the code and the owner's recorded decisions | ✅ | yes — it is offered as the thing you rebuild a restaurant from. Improvement 10. |
| P09989 | Judgment: Is the 1.5s shared floor window a problem for admin settings writes? | reasoned against the code and the owner's recorded decisions | ✅ | no. Judged and deliberately not changed. |
| P09990 | Judgment: Should /api/admin/revenue page past PostgREST's row cap today? | reasoned against the code and the owner's recorded decisions | ✅ | not urgent at 17 restaurants, and doing it properly is a SQL aggregate (a migration). 🟡 item 13 — his call. |
| P09991 | Judgment: Is the payroll-formula difference in /api/admin/users worth changing? | reasoned against the code and the owner's recorded decisions | ✅ | no. Unreachable by the column's NOT NULL DEFAULT, and editing a working money gate to satisfy a shape argument is a worse trade than leaving it. |
| P09992 | Judgment: Does the owner need to be told when a restaurant NAME could not be read? | reasoned against the code and the owner's recorded decisions | ✅ | on a multi-restaurant estate, yes — five routes say so and khata did not. Improvement 9. On a single-restaurant owner it is invisible either way. |
| P09993 | Judgment: Is the three-way manager staff switch worth finishing? | reasoned against the code and the owner's recorded decisions | ✅ | it is a product decision, not a bug: today all three default open, which is the behaviour before they were imagined. 🟡 item 12. |
| P09994 | Judgment: Should I have fixed the `id: slug` settings key even though nothing hits it today? | reasoned against the code and the owner's recorded decisions | ✅ | yes as an improvement, not as a bug — a uuid cannot collide, four of the five siblings already do it, and the symptom (a raw settings_pkey error on his screen) is invisible until it happens. |
| P09995 | Judgment: Does any route in my territory let Aevidine secretly hide a sale? | reasoned against the code and the owner's recorded decisions | ✅ | no. The only delete is a soft delete that also writes the Removals audit, and the export keeps the money trail. Checked against docs/COMPLIANCE-GUARDRAILS.md §3. |
| P09996 | Judgment: Is the admin's act-as pin the right way to reach an owner's cockpit? | reasoned against the code and the owner's recorded decisions | ✅ | yes — per-tab, never a cookie, and it can only narrow. Two admin tabs on two restaurants cannot repaint each other. |
| P09997 | Judgment: Does an owner ever learn that Aevidine was in their panel? | reasoned against the code and the owner's recorded decisions | ✅ | no. ownerLogPanel() files an admin action under the admin panel and auditForReader withholds the identity. R36 holds in my routes. |
| P09998 | Judgment: Would a restaurant with 300 tables notice /api/admin/restaurants/settings? | reasoned against the code and the owner's recorded decisions | ✅ | no — ensureCodes is capped at 500 and the payload is one small map. Measured shape, not a guess. |
| P09999 | Judgment: Is answering a bogus `?detail=` with 404 rather than 400 right on the owner Audit? | reasoned against the code and the owner's recorded decisions | ✅ | the shape is validated first (`/^\d+$/` → 400) and only a real-but-absent id gets 404. Correct. |
| P10000 | Judgment: After all 500: is there anything in my territory I would still not put in front of a paying client? | reasoned against the code and the owner's recorded decisions | ✅ | no. The five problems are fixed and guarded; the four 🟡 items are decisions, not defects. |

---

## CLOSED — 2026-08-20 · every one of the 15 items is resolved

T20 reported 5 problems, built 5 improvements and left 5 (numbered 11–15 in its chat report) for
the owner. He answered on 2026-08-20 and asked for all five to be built, alongside a separate
recycle-bin rework. All fifteen are now merged, so `T20-findings.md` and `T20-improvements.md` were
deleted as spent paperwork — this section is what survives them.

| # | what it was | what happened |
|---|---|---|
| 1 | `ownerScopeOr503` had zero callers → blank 500 on every owner screen | FIXED, PR #1057 |
| 2 | erasing a guest could destroy the record of a SMALL unpaid pay-later debt (the ranked read only saw the 500 biggest debtors) | FIXED, PR #1057 — the HIGH one |
| 3 | owner Settings could come up empty on a blip | FIXED, PR #1057 |
| 4 | a banquet bill series could be renumbered because a count hiccuped | FIXED, PR #1057 |
| 5 | Access & permissions said "Saved" for a change it had thrown away | FIXED, PR #1057 |
| 6 | four settings-row writes keyed by slug could collide on a name the bin freed | BUILT, PR #1057 |
| 7 | 26 admin sites still showed Postgres prose in the red toast | BUILT, PR #1057 |
| 8 | Pay Later dropped the shared lookup's `partial` | BUILT, PR #1057. He was asked whether Pay Later should also list the pending bills and confirmed it already does: *"if it is listing, there is no more to change."* |
| 9 | a recovery backup that is incomplete now says so | BUILT, PR #1057 |
| 10 | the owner dashboard read the whole restaurant list twice | BUILT, PR #1057 |
| 11 | three finer manager staff switches were half-built | **BUILT 2026-08-20** — three nodes in `lib/accessTree.ts` (`MANAGER_USER_POWERS`) + gates for all three in `app/api/owner/staff`. The stub's fourth key, `delete`, was DELETED rather than finished: a manager can never delete a login (owner, 2026-08-02). |
| 12 | two routes read `""` as opposite instructions on the same column | **BUILT 2026-08-20** — `lib/channelKey.ts` holds the one convention. The access-tree reading won: `""` leaves a write-only key alone, `null` clears it. |
| 13 | platform revenue under-reported past PostgREST's caps | **BUILT 2026-08-20** — `lib/pageAll` for the two per-restaurant tables, **migration 343** (a SQL aggregate) for the payments ledger. |
| 14 | the Full report showed three figures as absent on a read blip | **BUILT 2026-08-20** — he asked for the hiccup fixed *and* the improvement, so both: `lib/readRetry` on every read, and `partial` naming what still failed. |
| 15 | assigning an owner to a restaurant that isn't there answered "Saved" | **BUILT 2026-08-20** — 404 instead, with a failed read told apart from a missing row. |
| H1 | two Activity-log rows read "System › Other" (the handoff T20 could not fix in its own territory) | **FIXED 2026-08-20**, along with eight more from the print helper that had the same gap. This was making `verify:read-guards` RED on a clean `main`. |
| H2 | which convention wins for clearing a channel key | = item 12 above. |

**New guard from this round:** `npm run verify:recycle-bin` (23 live checks, self-cleaning), proven
to go red when the removed 90-day lock is put back.
