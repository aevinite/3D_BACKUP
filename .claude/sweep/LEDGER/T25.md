# Sweep #6 — Terminal 25 ledger  ·  `lib/**` remainder (the 108 files no other terminal owns)

**Branch** `sweep6/t25-lib-remainder` · **worktree** `sweep6/T25` · **port** 4125
**Phase block** P12001–P12500 (permanent — never renumber)
**Territory** every file in `lib/` except the 19 owned by T2/T3/T4/T11/T15/T24. Enumerated with
`ls lib` minus that list = **108 files, and every one of them has at least one row below.**
The six files my scope names — `tenant.ts`, `useRealtime.ts`, `floorSummary.ts`, `liveBoard.ts`,
`features.ts`, `ownerScope.ts` — have ten or more each.

## The budget, as actually written

| block | what it is | rows | share |
|---|---|---|---|
| A | reading the code for correctness | 200 | 40% |
| B | conformance to the rules in CLAUDE.md / the docs | 100 | 20% |
| C | watching it run (headless Playwright, my own port 4125) | 75 | 15% |
| D | screenshots I actually Read (desktop + Samsung A35) | 50 | 10% |
| E | tracing one change across panels | 50 | 10% |
| F | my own judgment about whether a restaurant needs it this way | 25 | 5% |

**Result column:** ✅ pass · ❌ a real problem (written up in `T25-findings.md`) · ⏭ skipped, with a
written reason. Two passes were run; the results below are the SECOND pass, after every fix.


## A. CORRECTNESS — reading the code (200 rows, 40%)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P12001 | getRestaurantBySlug folds the slug to lower case before the cache lookup AND before the RPC, so /r/FRENCH-HOUSE and /r/french-house share one cache entry | read lib/tenant.ts: assert `String(slugRaw\|\|'').trim().toLowerCase()` happens before `bySlug.get` | ✅ | |
| P12002 | A FAILED restaurant read is never cached (no bySlug.set on the error branch) | read lib/tenant.ts: the `if (error)` branch contains no bySlug.set | ✅ | |
| P12003 | A failed read answers with the last known row for up to STALE_ON_ERROR_MS, else THROWS — never null | read lib/tenant.ts: error branch returns hit.value or throws; assert no `return null` inside it | ✅ | |
| P12004 | A restaurant in the recycle bin (deleted_at set) still resolves to null so every guest surface hides it | read lib/tenant.ts: `data && !data.deleted_at` gates the object build | ✅ | |
| P12005 | The resolver reads through the lfh_guest_restaurant RPC, never a column list on `restaurants` | grep lib/tenant.ts for `.from("restaurants")` — must be absent | ✅ | |
| P12006 | TTL_MS is 15s and STALE_ON_ERROR_MS is 10 min — the stale window is far longer than the fresh one | node -e on the two constants in lib/tenant.ts | ✅ | |
| P12007 | DEFAULT_RESTAURANT_ID matches migration 078's seeded uuid …0001 | grep supabase/migrations/078*.sql for the same uuid | ✅ | |
| P12008 | DEFAULT_RESTAURANT_SLUG is 'french-house' and matches the seeded row | grep the migrations for the slug literal | ✅ | |
| P12009 | No caller bypasses lib/tenant.ts to resolve a slug (no second getRestaurantBySlug-shaped read) | grep app/ lib/ for `lfh_guest_restaurant` — only lib/tenant.ts may call it | ✅ | |
| P12010 | Routing is path-based only: no `/t/` route segment exists anywhere | find app -type d -name 't' — must be empty | ✅ | |
| P12011 | resolveRestaurantId (the always-#1 trap) is still deleted, not reintroduced | grep -r resolveRestaurantId lib app — no definition | ✅ | |
| P12012 | The per-process cache is keyed on the FOLDED slug, so a mixed-case URL cannot double the DB reads | read lib/tenant.ts: bySlug.set uses `slug`, not `slugRaw` | ✅ | |
| P12013 | useRealtime prefers the lib/supabase singleton when the public env values are compiled in, so a guest holds ONE websocket, not two | read lib/useRealtime.ts getClient(): inlineUrl && inlineKey -> dynamic import of @/lib/supabase | ✅ | |
| P12014 | The /api/rt-config fallback is genuinely reachable — the lib/supabase import is DYNAMIC, not top-level | grep lib/useRealtime.ts: no top-level `import ... from "@/lib/supabase"` | ✅ | |
| P12015 | A failed client build is not remembered forever (clientPromise reset to null on catch, both paths) | read lib/useRealtime.ts: both branches null out clientPromise on failure | ✅ | |
| P12016 | A guest (restaurantId set) filters the socket on topic_rid so it never receives another restaurant's breadcrumbs | read the `filter` expression: `topic_rid=eq.<topic>:<rid>` when rid is set | ✅ | |
| P12017 | Admin/staff (no rid) keep the topic-only filter deliberately | read the same ternary's else branch | ✅ | |
| P12018 | Channel names are per-restaurant (`rt:<topic>:<rid>`) so two tenants in one tab cannot collide | read the sb.channel() argument | ✅ | |
| P12019 | A tab hidden for IDLE_MS (120s) drops its channels, and reconnects + refetches on show | read onVisibility/teardown/wake in lib/useRealtime.ts | ✅ | |
| P12020 | The 60s safety poll is PAUSED while the tab is hidden (a backgrounded tab does zero work) | read the setInterval body: `if (!document.hidden) fireAll()` | ✅ | |
| P12021 | wake() is throttled to one socket rebuild per 1.5s so visibilitychange+focus+online together cause one rebuild | read the lastWake guard | ✅ | |
| P12022 | pageshow is wired (bfcache restore on iOS Safari), matching both twins | grep lib/useRealtime.ts for `pageshow` | ✅ | |
| P12023 | Nothing polls faster than the 60s backstop in this file | grep lib/useRealtime.ts for setInterval — exactly one, at 60000 | ✅ | |
| P12024 | Latency is measured only from a breadcrumb that ALREADY arrived — no extra request | read the payload handler: Date.now() - Date.parse(created_at), no fetch | ✅ | |
| P12025 | sharedFloorSummary registers its map entry BEFORE compute() can settle, so a synchronous throw cannot leave a rejected promise in the map | read lib/floorSummary.ts: inflight.set(key, entry) precedes entry.promise = ... | ✅ | |
| P12026 | The failure cleanup checks entry IDENTITY before deleting, so a late failure cannot evict a newer in-flight entry | read the catch: `if (inflight.get(key) === entry)` | ✅ | |
| P12027 | A failure is never cached — the entry is dropped so the next caller retries | read the catch's inflight.delete | ✅ | |
| P12028 | WINDOW_MS is 1.5s (not narrowed, not widened) | grep lib/floorSummary.ts for WINDOW_MS = 1500 | ✅ | |
| P12029 | invalidateFloor drops EVERY key ending in `:<rid>`, not just `floor:` — so a new shared read cannot be forgotten | read invalidateFloor: k.endsWith(`:${rid}`) | ✅ | |
| P12030 | invalidateFloor never touches another restaurant's keys | node: seed keys floor:A / merges:A / floor:B, invalidate A, assert B survives | ✅ | |
| P12031 | The map is swept above 64 entries so a many-restaurant instance cannot grow without bound | read the `inflight.size > 64` block | ✅ | |
| P12032 | The shared result is documented as READ-ONLY BY REFERENCE (the 2026-08-02 tablet section bug) | read the ⚠️ block above sharedFloorSummary | ✅ | |
| P12033 | EVERY write handler under app/api/editor and app/api/tablet calls invalidateFloor(rid) | npm run verify:floor | ✅ | |
| P12034 | A targeted `?table=N` refetch never goes through sharedFloorSummary | grep the editor/tablet routes: sharedFloorSummary is only on the whole-floor branch | ✅ | |
| P12035 | lib/features.ts refreshFeatures also invalidates the DERIVED settings cache (invalidateSettings), not just its own two maps | read lib/features.ts refreshFeatures | ✅ | |
| P12036 | lib/features.ts getFeatures does not cache a FAILURE forever — inflight is deleted on catch | read the .catch branch | ✅ | |
| P12037 | lib/features.ts caches and stores per restaurant id (cache map + lsKey), never one global key | grep for lsKey(rid) and cached.get(restaurantId) | ✅ | |
| P12038 | lib/features.ts useFeatures initial state does NOT read localStorage (hydration safety) | read useState initialiser — cached map or defaults only | ✅ | |
| P12039 | lib/menuDataServer.ts caches per restaurant (key parts include restaurantId) and tags menu-<rid> | read cachedBundleFor | ✅ | |
| P12040 | lib/menuDataServer.ts reads categories ONCE and derives the live-category set from them | read the body: getCategories then getMenuItems(..., liveCategorySetOf(categories)) | ✅ | |
| P12041 | lib/menuDataServer.ts strips detail-only fields at the edge so the cache stores the small shape | read the `items.map(toCardItem)` | ✅ | |
| P12042 | lib/ownerOverviewCache.ts evicts an { error } body immediately so a failure is not served for the TTL | read the .then that checks j.error | ✅ | |
| P12043 | lib/ownerOverviewCache.ts is keyed by the scope suffix so two scopes never share one answer | read `const key = scp \|\| "_"` | ✅ | |
| P12044 | lib/panelAccess.ts caches enabled-panels and owner ids for 30s only, so a revoked panel bites within the TTL | grep PANEL_TTL_MS = 30_000 | ✅ | |
| P12045 | lib/viewAsPerson.ts caches the pinned person for 20s and selects an explicit column list (never *) | read PIN_COLS and TTL_MS | ✅ | |
| P12046 | lib/planTable.ts shares ONE settings read per 5s burst per restaurant, keyed by rid, and caches nothing on failure | read tableCountOf | ✅ | |
| P12047 | liveOrdersAndItems scopes EVERY query by restaurant_id | grep lib/liveBoard.ts: each .from(...) chain carries .eq("restaurant_id", restaurantId) | ✅ | |
| P12048 | liveOrdersAndItems never uses select(*) — both column lists are explicit | grep lib/liveBoard.ts for select("*") — absent | ✅ | |
| P12049 | Order and item id lists are chunked at ID_CHUNK=150 so the PostgREST URL cannot hit 414 | read chunkIds + ID_CHUNK | ✅ | |
| P12050 | Chunks run at CHUNK_CONCURRENCY=6, not all at once | read the local mapLimit call sites | ✅ | |
| P12051 | pageAll walks .range() windows until a short page, so a >1000-row board is complete | read the local pageAll | ✅ | |
| P12052 | The open-session id list is narrowed to PRE-rollover sessions only, keeping it tiny | read the openRes query: .lt("created_at", since) | ✅ | |
| P12053 | mergeRows dedups by row id and restores created_at,id ordering | node: feed overlapping groups, assert unique + sorted | ✅ | |
| P12054 | activeOnly (kitchen) scopes items to the active orders' ids, not all of today's items | read the activeOnly branch | ✅ | |
| P12055 | `archived` is still in ORDER_COLS so the tablet's client-side !o.archived test is a real guard | grep ORDER_COLS for archived | ✅ | |
| P12056 | lib/pageAll.ts refuses past PAGE_ALL_MAX rather than paging a ledger forever; and its sibling lib/inChunks.ts (added by this sweep) never returns a short id-list read without an error | read both files' return shapes; `npm run verify:id-chunks` | ✅ | |
| P12057 | lib/money.mjs niceUsd stays in sync with lfh_nice_usd (mig 029) endings .00/.50/.99 | node --test tests for lib/money.mjs (npm run test:money) | ✅ | |
| P12058 | snapToStep kills float dust (0.1+0.2) by rounding to 6dp | node: snapToStep(0.30000000000000004, 0.01) === 0.3 | ✅ | |
| P12059 | displayAmount returns 0 for a non-finite input rather than NaN | node: displayAmount('abc',1,1) === 0 | ✅ | |
| P12060 | minorRound with minor=1 gives whole rupees | node: minorRound(12.6,1) === 13 | ✅ | |
| P12061 | lib/money.ts compactINR uses Indian thresholds (1Cr = 1,00,00,000) and drops a trailing .0 | node: compactINR(12000000)==='₹1.2Cr', compactINR(500000)==='₹5L' | ✅ | |
| P12062 | compactINR writes below ₹1,000 in full, never ₹0.9k | node: compactINR(850)==='₹850' | ✅ | |
| P12063 | compactINR keeps the sign on a negative bucket | node: compactINR(-12000000) starts with '−' | ✅ | |
| P12064 | roundTicks returns [] when nothing sensible fits, so the caller can omit ticks | node: roundTicks(5,5) and roundTicks(NaN,10) both [] | ✅ | |
| P12065 | roundTicks never emits a tick above max and never fewer than 2 | node: for a spread of domains assert every tick <= max and length!==1 | ✅ | |
| P12066 | businessDayDate derives the SAME key as SQL lfh_business_day (UTC date +30min) | node: compare against ((now AT TIME ZONE IST) - 5h)::date for several instants | ✅ | |
| P12067 | businessDayStartIso puts 04:59 IST in YESTERDAY's business day | node: businessDayStartIso(new Date('2026-08-21T23:29:00Z')) is 2026-08-20T23:30:00Z | ✅ | |
| P12068 | businessDateHi lands a window ending at 05:00 IST on the PREVIOUS calendar date | node: businessDateHi('2026-09-01T05:00:00+05:30') === '2026-08-31' | ✅ | |
| P12069 | businessDateHi falls back to today's IST date for an unparseable input rather than throwing | node: businessDateHi('nonsense') matches /^\d{4}-\d{2}-\d{2}$/ | ✅ | |
| P12070 | lib/inventoryWindow.ts month window starts at 05:00 IST on the 1st and ends at 05:00 IST on the next 1st | node: inventoryMonthWindow('2026-08') fromIso/toIso | ✅ | |
| P12071 | inventoryMonthWindow falls back to the current IST month for junk input, never throws | node: inventoryMonthWindow('x').month matches /^\d{4}-\d{2}$/ | ✅ | |
| P12072 | documentDateBounds high bound uses businessDateHi (so range=yesterday is ONE calendar day) | node: documentDateBounds for a yesterday window spans one date | ✅ | |
| P12073 | lib/taxFiling.ts splitTax parts always sum EXACTLY to the target (last line absorbs the remainder) | node: for 200 random targets/rate-sets assert sum === round2(target) | ✅ | |
| P12074 | allocateWhole returns integers summing exactly to Math.round(total) | node: 200 random weight vectors, assert integer + exact sum | ✅ | |
| P12075 | allocateWhole with all-zero weights puts the whole total on the first row rather than losing it | node: allocateWhole(100,[0,0,0]) === [100,0,0] | ✅ | |
| P12076 | allocateWhole handles a NEGATIVE total without drifting | node: allocateWhole(-7,[1,1,1]) sums to -7 | ✅ | |
| P12077 | taxableValue is capped at net sales so rounding cannot make it exceed what was sold | node: taxableValue({tax:1000,subtotal:100,discount:0},5) === 100 | ✅ | |
| P12078 | taxableValue returns net sales when pct is null (composition — no taxable supply) | node: taxableValue({tax:5,subtotal:100,discount:10},null) === 90 | ✅ | |
| P12079 | exemptIsMaterial answers false for rounding dust (the ₹111 phantom exempt line) | node: totals net 7961596 tax 398074 pct 5 paidOrders 2000 -> false | ✅ | |
| P12080 | buildFiling reconciles both ways: rows' parts sum to the row tax, columns sum to the total | node: random money rows, assert both directions | ✅ | |
| P12081 | dbRefusal: a CHECK violation (23514) is a 400, never a 500 the outbox would retry forever | node: refusalStatus({code:'23514'}) === 400 | ✅ | |
| P12082 | dbRefusal: 57014 (statement timeout) is 503 + busy, not 400 and not 500 | node: refusalStatus({code:'57014'}) === 503 && isDbUnreachable true | ✅ | |
| P12083 | dbRefusal: PGRST116 with '0 rows' is 404; PGRST116 with more-than-one stays a real error | node: both cases through refusalStatus | ✅ | |
| P12084 | dbRefusal: our own LFH01/02/03 codes answer 409 with their own sentence | node: refusalStatus({code:'LFH01'}) === 409 and refusalMessage names the credit note | ✅ | |
| P12085 | dbRefusal: isDbUnreachable reads error.cause (undici hides ECONNRESET there) | node: {message:'fetch failed', cause:{code:'ECONNRESET'}} -> true | ✅ | |
| P12086 | dbRefusal: a data refusal is decided BEFORE unreachable, so a constraint error is never retried | node: isDbUnreachable({code:'23514'}) === false | ✅ | |
| P12087 | dbRefusal: pgError carries code/details/hint through a rethrow so classifiers still work | node: pgError({message:'x',code:'23514'}).code === '23514' | ✅ | |
| P12088 | dbRefusal: worthLogging is false ONLY for the missing-row race | node: worthLogging({code:'PGRST116',details:'...0 rows'}) === false, everything else true | ✅ | |
| P12089 | dbRefusal has NO imports, so tests/error-text.test.mjs can load it with plain node --test | grep lib/dbRefusal.ts for ^import — none | ✅ | |
| P12090 | readRetry: 57014 is deliberately NOT retried | node: isTransient({code:'57014'}) === false | ✅ | |
| P12091 | readRetry: the 08xxx connection family and 53300 ARE retried | node: isTransient for each code in TRANSIENT_CODES | ✅ | |
| P12092 | readRetry: retryRead normalises a THROWN network failure into { error } | node: run() that throws -> result.error set, no exception escapes | ✅ | |
| P12093 | readRetry: a client resolving with neither data nor error counts as a failure, not an empty answer | node: run() returning undefined -> error 'read returned nothing' | ✅ | |
| P12094 | readRetry: the retry wait is jittered so a wave does not re-synchronise | read RETRY_WAIT_MS + JITTER_MS and the Math.random() term | ✅ | |
| P12095 | readGuard: rows()/one()/count()/value() THROW on a failed read instead of returning [] / 0 | node: ReadSet with an errored read -> each throws ReadFailed | ✅ | |
| P12096 | readGuard: failed('typo') is TRUE for an unknown read name (a typo must not read as fine) | node: new ReadSet('x',[]).failed('nope') === true | ✅ | |
| P12097 | ownerScope: a logged-in owner wins over a stray admin cookie unless the per-tab admin pin claimed the request | read the adminPinned/owner ordering in lib/ownerScope.ts | ✅ | |
| P12098 | ownerScope: ?as=<ownerId> is honoured ONLY when that owner co-owns the acting restaurant | read the members.includes(asOwner) guard | ✅ | |
| P12099 | ownerScope: a failed widen THROWS OwnerScopeUnavailable rather than silently shrinking the view | read the owned.error branch | ✅ | |
| P12100 | ownerScope: an owner with no entitled restaurants gets null (401), never an empty all-view | read `if (!ids.length) return null` | ✅ | |
| P12101 | ownerScope: scopedRestaurantIds pages in 1000s and throws RestaurantListIncomplete rather than returning a partial list | read the loop + the throw | ✅ | |
| P12102 | ownerScope: ownerLogPanel records an ADMIN action against the admin panel wherever it was performed | node: ownerLogPanel({all:true}) === 'admin' and ({all:false,admin:true}) === 'admin' | ✅ | |
| P12103 | ownerScope: dbFail never puts the database's own words in the response body | read dbFail: raw goes to console.error only | ✅ | |
| P12104 | ownerScope: dbFail turns 57014 into actionable advice with status 504 | node: dbFail('x',{code:'57014'}) body + status | ✅ | |
| P12105 | ownerScope re-exports the partial-read WORDS from lib/partialRead so a client component never imports supabaseAdmin | npm run verify:server-only | ✅ | |
| P12106 | panelAccess: getEnabledPanels answers ALL FOUR ON regardless of the stored column (owner 2026-07-31) | node: getEnabledPanels('any') deep-equals all true | ✅ | |
| P12107 | panelAccess: enabledOwnedRestaurantIds PAGES the ownership links (no .limit(50)) AND chunks the two reads that filter that list, through lib/inChunks.ts — an 800-id `.in()` answers Bad Request | grep for limit(50) — absent; read ownedLinkIds; `npm run verify:id-chunks` | ✅ | |
| P12108 | panelAccess: a failed read hands back the last trusted answer or THROWS OwnedLookupFailed — never an empty estate | read staleOrThrow and its three call sites | ✅ | |
| P12109 | panelAccess: nothing is written to the owner cache on a failed read, so the next call retries | read the ordering of staleOrThrow vs _ownerCache.set | ✅ | |
| P12110 | panelAccess: isRestaurantDeleted fails OPEN on error (a blip must not lock every restaurant out) | read the catch: return false | ✅ | |
| P12111 | panelGate: staff is checked BEFORE the admin cookie, so an explicit sign-in wins on a shared device | read requirePanel's order | ✅ | |
| P12112 | panelGate: an admin needs the act-as cookie to enter a panel, and a per-TAB ?rid for panelAdminRid | read both functions | ✅ | |
| P12113 | panelGate: a malformed ?rid is rejected by RID_RE before any DB read | node: RID_RE against 'abc' and a real uuid | ✅ | |
| P12114 | panelGate: panelIframeSrc only forwards ?as= when it matches a 36-char id, and view=real only when asked | node: panelIframeSrc for four input shapes | ✅ | |
| P12115 | panelScope: an ADMIN request naming neither ?rid nor the act-as cookie returns NULL (never restaurant #1) | node: panelRestaurantId with no user and no pins === null | ✅ | |
| P12116 | panelScope: emptyIdSegment catches 'undefined'/'null'/'NaN' path segments | node: all three true, a uuid false | ✅ | |
| P12117 | billLedger netOf prefers the DATABASE's generated net_amount and does no arithmetic when it is present | node: netOf({net_amount:472.5,total:525,discount:50,tax_rate:null}) === 472.5 | ✅ | |
| P12118 | billLedger netOf falls back to total − disc_gross before any rate arithmetic | node: netOf({total:525,disc_gross:52.5}) === 472.5 | ✅ | |
| P12119 | billLedger deriveBillState: DELETED wins over every other state | node: session.deleted_at set -> 'deleted' | ✅ | |
| P12120 | billLedger deriveBillState: an OPEN session is 'running' whatever its orders say | node: status 'open' -> 'running' | ✅ | |
| P12121 | billLedger deriveBillState priority is khata > onhouse > settled > cancelled | node: four orders mixing the four markers | ✅ | |
| P12122 | billLedger lossOfClosedUnpaid returns 'yes' when any order was FIRED (preparing/ready/served) | node: status 'served' -> 'yes' | ✅ | |
| P12123 | billLedger lossOfClosedUnpaid returns 'unknown' when a cancellation's `made` answer is missing | node: cancelled order absent from madeByOrder -> 'unknown' | ✅ | |
| P12124 | billLedger lossOfClosedUnpaid returns 'no' when nothing was cooked and nothing cancelled | node: one 'received' order -> 'no' | ✅ | |
| P12125 | billLedger rollUpBill sums the DELETED bill's removed total from all orders, and the live total from live ones | node: two orders, one deleted, both states | ✅ | |
| P12126 | removalAudit: recordRemoval never throws — a failed audit write becomes an error-level activity row | read the catch in recordRemoval | ✅ | |
| P12127 | removalAudit: snapshotOrder resolves bill_no/invoice_no off the SESSION, not left as a uuid | read snapshotOrder's second read | ✅ | |
| P12128 | removalAudit: the item snapshot is capped at 60 lines and says so via items_truncated | read the slice(0,60) + the flag | ✅ | |
| P12129 | removalAudit: reasonFromBody accepts reason_code/reason_note AND the legacy bare `reason` | node: three body shapes | ✅ | |
| P12130 | auditActor: forReader strips ONLY the admin's identity, never the row, the reason or the money | node: an admin row -> actor null, every other field identical | ✅ | |
| P12131 | auditActor: isPlatformActor catches both actor_role in (admin,db) and the legacy 'Admin …' actor string | node: both shapes true, a waiter false | ✅ | |
| P12132 | auditDetail: auditAfter reads `removed` off deleted_at (a tombstoned order still has its row) | read the state ternary | ✅ | |
| P12133 | auditDetail: auditBillHtml returns null rather than throwing when the bill cannot be drawn | read the try/catch | ✅ | |
| P12134 | softDelete: a delete STAMPS deleted_* and never runs a SQL DELETE on an issued order | grep lib/softDelete.ts for `.delete(` — absent | ✅ | |
| P12135 | printQueue: pendingKotJobs RETIRES an orphaned job as 'dismissed' instead of skipping it, so ten dead jobs cannot block the queue for ever | read the orphans block | ✅ | |
| P12136 | printQueue: a CANCELLED order's ticket is retired too (food nobody ordered is never printed) | read the `dead` predicate | ✅ | |
| P12137 | printQueue: the claim is ONE filtered UPDATE, so a second claimant matches zero rows | read claimKotJobs | ✅ | |
| P12138 | printQueue: minAgeMs (the backup window) is enforced on the SERVER in claimKotJobs, not only in the read | read the .lt(created_at) in claimKotJobs | ✅ | |
| P12139 | printQueue: liveFilter is shared by the read and the claim so what is offered equals what can be won | read both .or(liveFilter()) call sites | ✅ | |
| P12140 | printQueue: a job parks as 'failed' at 5 attempts | read finishKotJob's `parked = attempts >= 5` | ✅ | |
| P12141 | printQueue: takeStation stands everyone else down BEFORE standing itself up (the partial unique index allows one) | read takeStation's two statements in order | ✅ | |
| P12142 | printQueue: mayClaim refuses when auto is off, the room is wrong, there is no device, or a LIVE station holds it | node: the four branches | ✅ | |
| P12143 | printDocs: kotHtmlForOrder returns null for a deleted or cancelled order (the second door on the same rule) | read the guard after the reads | ✅ | |
| P12144 | printDocs: kotTableLabel is 'T7' (never 'Table 7') and 'T?' for a blank table | node: three inputs | ✅ | |
| P12145 | printDocs: withPaper leaves a document that already declares @page size alone | node: html with and without an @page size rule | ✅ | |
| P12146 | printHelpers: writeRoutes refuses a machine that is not this restaurant's and a printer it never reported | read the pick() guards | ✅ | |
| P12147 | session.ts: EVERY session RPC carries a 15s deadline, and AbortSignal.timeout is feature-guarded | read the rpc() helper in lib/session.ts | ✅ | |
| P12148 | session.ts: an aborted call answers reason 'timed_out', never the raw AbortError text | node: the signal?.aborted branch | ✅ | |
| P12149 | session.ts: getSessionState single-flights per token and clears the entry as soon as it settles | read sessionStateInFlight + the .finally | ✅ | |
| P12150 | session.ts: the stored session is table-scoped, so walking to a new table does not reuse the old token | node: getStoredSession('7') against a stored table '3' -> null | ✅ | |
| P12151 | session.ts: there is still NO browser-side customer-by-phone wrapper (mig 300) | grep lib/session.ts for lfh_recognize_customer — absent | ✅ | |
| P12152 | session.ts: placeSessionOrder is still gone (orders go through placeSessionOrderSafe) | grep lib/session.ts for placeSessionOrder — only the note | ✅ | |
| P12153 | session.ts: checkLocation soft-passes ('bypassed') when the cafe has no coords, so the flow stays usable | node: checkLocation(null,null,250) on a no-geolocation runtime | ✅ | |
| P12154 | openSession.ts: losing the one-open-session race is a SUCCESS — it re-reads and returns the winner | read the isDuplicate branch | ✅ | |
| P12155 | openSession.ts: any OTHER database error still throws (a real failure is not swallowed) | read the final throw | ✅ | |
| P12156 | sessionClose.ts: closeBlock refuses a close while anything is cooking OR unpaid, unless force | node: the four combinations | ✅ | |
| P12157 | sessionClose.ts: the money still owed is read BEFORE the session is closed (mig 232's trigger would erase it after) | read the owedRows read's position | ✅ | |
| P12158 | sessionClose.ts: the owed figure grosses the discount at the order's OWN rate (tax/subtotal), not the bare discount | node: subtotal 500 tax 25 total 525 discount 50 -> owed 472.5 | ✅ | |
| P12159 | sessionClose.ts: a khata order is NOT cancelled by a force close (money to collect later is not a write-off) | read the .is("khata_at", null) on the cancel update | ✅ | |
| P12160 | sessionClose.ts: the close is confirmed to belong to the acting restaurant before anything is written | read the ctx.restaurantId pre-check | ✅ | |
| P12161 | sessionClose.ts: clearTableSignals is scoped to ONE restaurant + table and never throws | read the function | ✅ | |
| P12162 | tableMerge.ts: mergeParentTable returns the table itself on a read hiccup (the unmerged answer is the safe one) | read the catch | ✅ | |
| P12163 | tableAssign: waiterTables answers null (unrestricted) for the admin super-user and for any non-tablet role | node: the two early returns | ✅ | |
| P12164 | tableAssign: allows() lets an OFF-PLAN table (number > table_count) through to everyone | node: allows({tables:['1'],floor:30},'47') === true | ✅ | |
| P12165 | tableAssign: allows() treats a non-numeric label as not-ours-to-hide | node: allows(limit,'parcel') === true | ✅ | |
| P12166 | tableAssign: assignedOf normalises and dedups ('007' and 7 are one table) | node: assignedOf({assigned_tables:['007',7,7]}) === ['7'] | ✅ | |
| P12167 | tableAssign: blockedReason refuses when the action's table could not be resolved (unknown must not read as allowed) | node: affectedTables returning unknown -> a message | ✅ | |
| P12168 | tableOfAction: a MOVE carries both source and destination tables, and both must be the waiter's | read withDest | ✅ | |
| P12169 | tableOfAction: an unrecognised action shape returns unknown:true, so a new endpoint is covered the day it is added | read the final return | ✅ | |
| P12170 | tableOfAction: 'issue' and 'parcel' are NO_TABLE (never restricted by a section) | read the early return | ✅ | |
| P12171 | tableOfAction: order_items resolve their table via order_id first, session_id only as a fallback | read tableOfItem | ✅ | |
| P12172 | planTable: offPlanTable FAILS OPEN when settings cannot be read, and never judges a non-numeric label | node: both branches | ✅ | |
| P12173 | format.ts: reading the stored currency/language goes through readStored, which survives a localStorage getter that THROWS | read readStored and both callers | ✅ | |
| P12174 | format.ts: getCurrency falls back to INR for an unknown stored code | node: with 'XYZ' stored -> INR | ✅ | |
| P12175 | format.ts: formatAmount groups with en-IN (₹1,15,000 not ₹115,000) | node: formatAmount(115000, INR) | ✅ | |
| P12176 | format.ts: a multi-letter symbol gets a space (AED 12), a single character hugs (₹12) | node: both | ✅ | |
| P12177 | format.ts: unitDisplay adds up — base chip + add-on chips equal the unit | node: unitDisplay(655,[105]) === 550 + 105 | ✅ | |
| P12178 | format.ts: applyDirection pins dir=ltr (Arabic RTL is deliberately deferred) | read applyDirection | ✅ | |
| P12179 | table.ts: validateTable rejects blank, non-digits, 0 and anything above tableCount, and skips the upper bound at 0 | node: seven inputs | ✅ | |
| P12180 | brandText.ts: splitBrandSegments alternates plain/highlighted on single asterisks and drops empty segments | node --test lib/brandText.test.mjs | ✅ | |
| P12181 | brandText.ts: splitGraphemes keeps a Devanagari conjunct as ONE unit (the शुभ संध्या bug) | node: splitGraphemes('ध्या').length === 1 | ✅ | |
| P12182 | brandTheme.ts: buildModeBlock re-derives --accent-ink per skin, so a themed tenant shows ONE brand colour | node --test lib/brandTheme.test.mjs | ✅ | |
| P12183 | accent.ts: accentCanvasCss emits html[data-theme=…] (0,1,1) so it beats globals.css's restaurant-#1 values | read the returned selector | ✅ | |
| P12184 | accent.ts: an unparseable accent changes nothing rather than emitting broken CSS | node: accentCanvasCss('nope') === '' | ✅ | |
| P12185 | searchText.ts: safeSearch strips % * , ( ) and backslash so an ilike search means what was typed | node: safeSearch('a*b,c%(d)\\e') | ✅ | |
| P12186 | searchText.ts: safeSearch returns '' when nothing usable is left (callers treat it as no search) | node: safeSearch('***') === '' | ✅ | |
| P12187 | clashCompare.ts: stableJson sorts object keys at every level so serialisation order cannot invent a clash | node: two differently-ordered objects compare equal | ✅ | |
| P12188 | clashCompare.ts: depth is limited to 6 and a cycle cannot hang a write | node: a self-referencing object returns | ✅ | |
| P12189 | clashCompare.ts: sameValue compares lists as SETS and null equals {} | node: both cases | ✅ | |
| P12190 | idempotencyRule.ts: didSomething is FALSE for a 200 body carrying ok:false (a refusal must be replayable) | node: didSomething(200,{ok:false}) === false | ✅ | |
| P12191 | publicCap.ts: capKeyFor never reads a BODY field — a caller cannot pick its own bucket | read capKeyFor | ✅ | |
| P12192 | publicCap.ts: recentActionCount fails OPEN (0) so a crash report is never blocked by its own counter | read the catch | ✅ | |
| P12193 | publicCap.ts: withinMemoryCap sweeps and then clears above MAX_TRACKED so the map cannot grow unbounded | node: 6000 keys, assert size bounded | ✅ | |
| P12194 | loginThrottle.ts: every helper fails OPEN, so a throttle outage never locks a real user out | read each catch | ✅ | |
| P12195 | loginThrottle.ts: a deliberate block is a far-future lock and throttleIsBlocked uses the same >1-year cutoff as listBlocked | read both | ✅ | |
| P12196 | managerPin.ts: every active manager's hash is checked (no early return) so a shared PIN names all of them | read the loop | ✅ | |
| P12197 | managerPin.ts: the PIN check is scoped to ONE restaurant's managers | read the .eq("restaurant_id") on the staff read | ✅ | |
| P12198 | staffAuth.ts: safeEqual is constant-time and length-checked first | node: equal/unequal/different-length | ✅ | |
| P12199 | passwordVault.ts: openPassword returns null for a wrong key, a corrupt value or a version mismatch — never a half string | node: three malformed inputs | ✅ | |
| P12200 | passwordVault.ts: sign-in never reads the vault (password_hash alone decides) | grep lib/userAuth.ts for passwordVault — absent | ✅ | |

## B. CONFORMANCE to the project's own written rules (100 rows, 20%)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P12201 | accessConfig.ts: MP_DEFAULT is DERIVED from managerGrantValue, not hand-typed, and excludes the three payroll powers | read the Object.fromEntries + OWNED_BY_PAYROLL | ✅ | |
| P12202 | accessState.ts: a channel key leaves as '••••1234' only — the value itself never appears | read the creds loop | ✅ | |
| P12203 | accessState.ts: config is narrowed to KNOWN_CONFIG_IDS so retired permission names stop riding along | read the cfg loop | ✅ | |
| P12204 | adminFail.ts: the plain sentence goes in `error`, the database's words only in `detail` + the server log | read the return | ✅ | |
| P12205 | adminFetch.ts: a 200 body carrying { error } is treated as a FAILURE | node: adminFetch against a stub returning {error:'x'} with status 200 | ✅ | |
| P12206 | alerts.ts: the send is recorded BEFORE the pushes so a burst cannot slip past the 15-min window | read sendOwnerAlert's order | ✅ | |
| P12207 | alerts.ts: both channels are pushed independently (Promise.allSettled) with a 4s deadline each | read alertFetch + allSettled | ✅ | |
| P12208 | alerts.ts: alertStack names the stack from the database ref, so a ping says where it came from | node: alertStack() with each ref | ✅ | |
| P12209 | allergens.ts: an unknown slug renders as the slug and a generic ⚠️, never blank | node: allergenLabel('x') and allergenIcon('x') | ✅ | |
| P12210 | backStack.ts: history.go(-N) is swallowed as exactly ONE popstate, never N | read reconcile + onPop's ignore handling | ✅ | |
| P12211 | backStack.ts: a buried buffer entry (a nav happened on top) is left alone instead of yanking the user back | read the __lfhLayer check | ✅ | |
| P12212 | banquetFields.ts: cleanBanquetFields keeps only known keys, dedups, and falls back to the simple preset | node: junk input | ✅ | |
| P12213 | banquetFields.ts: banquetBillNo renders the same three styles as SQL lfh_banquet_bill_no | node: the three styles | ✅ | |
| P12214 | billCustomer.ts: a missing settings row fails OPEN — an unconfigured restaurant can still bill a table | node: billCustomerRequired with no row -> false | ✅ | |
| P12215 | billCustomer.ts: a phone under 10 digits is refused with its own sentence | node: saveBillCustomer with '99999' | ✅ | |
| P12216 | billPreview.ts: the TOTAL comes from splitBill, never a second sum (the composition discount bug) | read `const total = split.total` | ✅ | |
| P12217 | billPreview.ts: a composition restaurant's preview shows NO tax rows at all | read the composition guards on taxRowsAdded/Inside | ✅ | |
| P12218 | botCheck.ts: botVerdict FAILS OPEN for absent signals and only refuses on a signal present-and-wrong | node: five input shapes | ✅ | |
| P12219 | cancelWatch.ts: the cheap head COUNT runs first; the day's money is only read past MIN_COUNT | read watchCancellations' order | ✅ | |
| P12220 | cancelWatch.ts: a day that sold nothing never fires, and the alert carries no customer data | read the soldValue guard + alertText fields | ✅ | |
| P12221 | channelKey.ts: '' LEAVES a stored key alone; only null clears it | node: applyChannelKey({key:'k'},'') and (…,null) | ✅ | |
| P12222 | channelKey.ts: every write drops the legacy api_key field | node: applyChannelKey({api_key:'old'},'new') | ✅ | |
| P12223 | connectionStatus.ts: the snapshot object is rebuilt only when something changed (getSnapshot must be stable) | read rebuild()'s call sites | ✅ | |
| P12224 | connectionStatus.ts: latencyTier's bars differ wherever the colour differs (never colour-only) | node: the four tiers' bars are 3/2/1/0 | ✅ | |
| P12225 | dashRange.ts: clampDashRange resolves anything but 'yesterday'+reach to 'today', and never errors | node: '30d', 'year', 'yesterday' with both reaches | ✅ | |
| P12226 | device.ts / guestDevice.ts: both wrap localStorage in try/catch and return a safe value when storage is blocked | read both | ✅ | |
| P12227 | discountCap.ts: an OWNER and the admin super-user have NO ceiling | node: discountRole('owner') === null and discountRole(null) === null | ✅ | |
| P12228 | discountCap.ts: nothing stored falls back to the MODEL default the Access screen displays | node: with an empty access_config, manager -> 50 | ✅ | |
| P12229 | errorMemory.ts: every function is fail-open, and nothing here is consulted when an error is LOGGED | read the catches + grep logError for errorMemory | ✅ | |
| P12230 | errorReport.ts: panelFromPath strips a leading /r/<slug> so a tenant-scoped panel crash is not filed as 'menu' | node: panelFromPath('/r/aangan/owner') === 'owner' | ✅ | |
| P12231 | errorSignature.ts: errorSig folds 'Uncaught ReferenceError: x' and 'x' into ONE signature | node: both give the same sig | ✅ | |
| P12232 | errorSignature.ts: errorSig strips the trailing browser tag so one fault is one tile per browser | node: '… [Safari · iPhone]' equals the untagged sig | ✅ | |
| P12233 | errorSignature.ts: readableError keeps a gateway page's <title> and drops the markup | node: a Cloudflare 522 page | ✅ | |
| P12234 | floorLayout.ts: clampPerRow keeps 2..12 and defaults to 12 for junk | node: 0, 30, 'x', 7 | ✅ | |
| P12235 | floorLayout.ts: the DB CHECK constraint (mig 265) is WIDER than FLOOR_PER_ROW_MAX | grep mig 265 for the 2..30 range | ✅ | |
| P12236 | guestName.ts: setGuestName never stores a blank | node: setGuestName('  ') writes nothing | ✅ | |
| P12237 | issues.ts: an SVG is refused as an issue photo, and only URLs inside our own bucket are stored | read IMAGE_EXT + clean() | ✅ | |
| P12238 | logMarks.ts: ADMIN_VIEW_ACTOR_ID is a reserved sentinel uuid that cannot collide with a real v4 id | read the constant | ✅ | |
| P12239 | logVisibility.ts: loadLogVisibility returns { ok:false } on a read failure — there is no permissive third state | read the r.error branch | ✅ | |
| P12240 | logVisibility.ts: canSee HIDES a row whose restaurant is not in the loaded map | node: canSee('unknown','login') === false | ✅ | |
| P12241 | logVisibility.ts: logKindOf classifies login/login_failed, staff_*/user_*, and everything else | node: the three buckets | ✅ | |
| P12242 | mapLimit.ts: order is preserved and a limit below 1 is treated as 1 | node: mapLimit over 10 items with limit 0 | ✅ | |
| P12243 | mediaLinks.ts: signRows signs a whole list in ONE request and matches results back by PATH, not index | read the createSignedUrls call + valuesByPath | ✅ | |
| P12244 | mediaLinks.ts: pathOf recovers the object path from a bare path, an old public URL and an already-signed URL | node: all three | ✅ | |
| P12245 | oplog.ts: redactMoney masks ₹-amounts and 'total <n>' but leaves plain counts alone | node: three strings | ✅ | |
| P12246 | oplog.ts: deviceBlocked REQUIRES a restaurant id and fails open without one | node: deviceBlocked('d', null) === false | ✅ | |
| P12247 | orderStatus.ts: liveActiveOrders drops only dismissed or aged-out orders, and sorts newest first | node: a mixed list | ✅ | |
| P12248 | ownerEntitlements.ts: an ABSENT entitlement key reads as ON | node: mergeOwnerEntitlements(null) all true | ✅ | |
| P12249 | ownerEntitlements.ts: powerEntitled is retired and nothing reads it | grep app/ lib/ for powerEntitled — definition only | ✅ | |
| P12250 | ownerHome.ts: resolveOwnerHomeRid tries #1, then a picked restaurant, then the oldest live one, then refuses with words | read the three steps | ✅ | |
| P12251 | ownerHome.ts: loginNameTaken ignores binned accounts (a binned name is free) | read the .is("deleted_at", null) | ✅ | |
| P12252 | ownerPin.ts: asSuffix is empty for a real owner and never a cookie | read asValue | ✅ | |
| P12253 | ownerReportGather.ts: one restaurant failing does NOT blank the statement — it is dropped and NAMED in `omitted` | read the failed[] handling | ✅ | |
| P12254 | ownerReportGather.ts: a tax label used at TWO rates stays on two lines (a return needs that) | read the rates map + the NaN marker | ✅ | |
| P12255 | ownerSnap.ts: snapshots are sessionStorage only, versioned, size-capped, and cleared on login | read the four rules | ✅ | |
| P12256 | panelFailure.ts: a busy database answers 503 with the X-LFH-Busy header AND the body flag | read the return | ✅ | |
| P12257 | panelSettings.ts: panelSafeSettings COPIES rather than deleting in place (the floor cache is shared) | read the loop | ✅ | |
| P12258 | panelSettings.ts: platform_channels is on PRIVATE_SETTINGS_COLUMNS | grep the constant | ✅ | |
| P12259 | partialRead.ts: the file has ZERO imports so a client component can hold it | grep lib/partialRead.ts for ^import — none | ✅ | |
| P12260 | partialRead.ts: partialNote reads as English for one, two and three keys | node: all three | ✅ | |
| P12261 | payments.ts: PAYMENT_METHODS is exactly UPI/Cash/Card/Other | node: the array | ✅ | |
| P12262 | personalData.ts: every place has a scopeBy, and only otp_codes is global | node: assert exactly one scopeBy==='phone' | ✅ | |
| P12263 | personalData.ts: an issued sales document is 'keep' and its reason is stated for the owner | node: sessions + aggregator_orders have policy keep and a why | ✅ | |
| P12264 | restaurantColor.ts: portfolioColor is keyed by ID, so a chart sort cannot recolour a restaurant | node: same id twice -> same colour | ✅ | |
| P12265 | restaurantNames.ts: readRestaurantName handles a JSONB name, a plain string and neither | node: three shapes | ✅ | |
| P12266 | restaurantNames.ts: the lookup is chunked at 500 with .limit(part.length) so the 1000-row cap cannot truncate it | read the loop | ✅ | |
| P12267 | restaurantNames.ts: a failed lookup comes back partial:true, never silently as dashes | read the r.error branch | ✅ | |
| P12268 | safeAreaBridge.ts: a >120px viewport gap is treated as the keyboard, not the nav bar | read the measured guard | ✅ | |
| P12269 | settingsClone.ts: every tenant identity, GST, geo and module column has an EXPLICIT default (the #1-leak class) | read NULL_COLUMNS + the ladder defaults + the drift warn loop | ✅ | |
| P12270 | settingsClone.ts: the guest-menu columns are deliberately INHERITED (rejected idea R8) — do not add defaults | grep docs/REJECTED-IDEAS.md for R8 | ✅ | |
| P12271 | staffProfile.ts: payHistoryBlocksDelete FAILS CLOSED — 'I could not check' never resolves to 'go ahead' | read the q.error branch | ✅ | |
| P12272 | staffProfile.ts: a manager can never edit job+pay setup (canEditJobPay is always false for them) | node: payAccessWith('manager',{},true) | ✅ | |
| P12273 | starterMenu.ts: every seeded item gets a fresh uuid id and all 3D fields are stripped | node --test lib/starterMenu.test.mjs | ✅ | |
| P12274 | supabase.ts: persistSession is false so a storage-blocked browser cannot crash module evaluation | read the auth options | ✅ | |
| P12275 | supabaseAdmin.ts: every server request carries the 8s DB_TIMEOUT_MS deadline unless the caller passes its own signal | read withDeadline | ✅ | |
| P12276 | tableConnection.ts: gateAddToCart holds the add and opens the join flow instead of dropping the tap | read the queue + the dispatched event | ✅ | |
| P12277 | tableConnection.ts: an abandoned connect DROPS the held adds rather than resurfacing them later | read wireResume's queue clear | ✅ | |
| P12278 | tableTags.ts: moduleLadder's effective = allowed AND (!ownerControl OR enabled) | node: the eight combinations | ✅ | |
| P12279 | tableTags.ts: Parcel and Platforms are PERMANENT (ALWAYS_ON) and are two separate features | read parcelLadder + takeawayLadder + the box | ✅ | |
| P12280 | tableTags.ts: allModuleLadders reads every module in ONE settings select | read the cols build + the single query | ✅ | |
| P12281 | timeView.ts: decideTimeView answers 'empty' for no points and for an all-zero window | node: both | ✅ | |
| P12282 | timeView.ts: a normal trading week (every day busy, top ~25%) is plotted, not drilled | node: seven busy buckets -> mode 'chart' | ✅ | |
| P12283 | timeView.ts: a 7-bucket window with 4 empty and a 97% top bar DRILLS when canDrill is true | node: the founding case | ✅ | |
| P12284 | timeView.ts: labelFor renders an hour bucket in IST from a full ISO instant | node: labelFor('2026-08-06T08:30:00.000Z','hour') === '6 Aug, 2pm' | ✅ | |
| P12285 | useFitText.ts: the search always restarts from the stylesheet size, so a name can grow back after a rotate | read `el.style.fontSize = ''` | ✅ | |
| P12286 | useFitText.ts: a re-check after document.fonts.ready catches the webfont re-wrap | read the fonts.ready branch | ✅ | |
| P12287 | viewAsPerson.ts: a REAL staff session is never anybody's periscope (g.user short-circuits) | read the first line | ✅ | |
| P12288 | viewAsPerson.ts: the pin is refused across restaurants and for a mismatched role | read the two guards | ✅ | |
| P12289 | viewAsPerson.ts: PIN_COLS never pulls password_hash or pin_hash into the 20s cache | grep PIN_COLS | ✅ | |
| P12290 | warmData.ts: nothing is re-fetched — the page hands the worker a payload it already has | read warmDataCache | ✅ | |
| P12291 | warmData.ts: it waits for controllerchange on a first visit rather than giving up | read the else branch | ✅ | |
| P12292 | aggregators.ts: verifyWebhook REFUSES when no secret is configured, and compares in constant time | node: verifyWebhook with no env secret -> false | ✅ | |
| P12293 | aggregators.ts: resolveWebhookRestaurant refuses rather than guessing when two restaurants claim one outlet | read the matches.length > 1 branch | ✅ | |
| P12294 | aggregators.ts: a duplicate inbound order (23505) answers with the EXISTING row, so the platform stops retrying | read the 23505 branch | ✅ | |
| P12295 | modelWatchlist.ts: the watchlist is a globalThis singleton in the browser and a throwaway on the server | read getWatchlist | ✅ | |
| P12296 | printHelperScript.ts: the generated helper holds no rules and no layout — it polls, prints, confirms | read the emitted script for any pricing/tax/layout logic | ✅ | |
| P12297 | pageAll.ts: a partial list is NEVER returned without an error | read the return shapes | ✅ | |
| P12298 | billPreview.ts: the discount shown is derived from splitBill's cap, so an MRP line cannot be discounted away | read the splitBill call | ✅ | |
| P12299 | printDocs.ts: banquetHtmlForBill reads its lines from the LINKED ORDER (there is no `lines` column) | read the ord read | ✅ | |
| P12300 | floorSummary.ts: _resetSharedFloorSummary exists only as a test hook and no app code calls it | grep app/ for _resetSharedFloorSummary — absent | ✅ | |

## C. WATCHING IT RUN — headless on port 4125 (75 rows, 15%)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P12301 | Dev server on 4125 is MINE (the build hash matches this worktree's HEAD) | curl :4125/api/health and compare the reported build against `git rev-parse HEAD` | ✅ | |
| P12302 | Port 4125 is not serving another terminal's tree | lsof -ti:4125 -> the pid this run started | ✅ | |
| P12303 | /r/french-house/menu answers 200 and renders dishes (the tenant resolver's happy path) | headless: goto, assert >=1 .food-card in the DOM | ✅ | |
| P12304 | /r/FRENCH-HOUSE/menu answers the SAME 200 as the lower-case URL (capitals behave identically) | headless: both URLs, assert both statuses 200 and the same dish count | ✅ | |
| P12305 | /r/French-House/menu (title case) answers 200 too | headless: goto, assert status 200 | ✅ | |
| P12306 | /r/no-such-restaurant/menu answers 404 (an unknown slug is still null) | headless: assert 404 | ✅ | |
| P12307 | Two mixed-case requests for one slug cause ONE lfh_guest_restaurant read, not two (shared cache entry) | headless: hit both, count the RPC in the server log | ✅ | |
| P12308 | /r/aangan/menu renders AANGAN's branding, never French House's (read-only control) | headless: assert the wordmark text | ✅ | |
| P12309 | The guest menu's menu-data endpoint is fetched exactly ONCE per load | headless: count requests to /api/r/*/menu-data on a cold load | ✅ | |
| P12310 | The guest menu opens ONE websocket, not two | headless: count ws connections after load settles | ✅ | |
| P12311 | The realtime channel a seated guest opens is filtered on topic_rid, not topic | headless: read the ws subscribe frame's filter | ✅ | |
| P12312 | A hidden tab drops its realtime channels after IDLE_MS | headless: hide the page, wait past 120s (fake timers), assert removeChannel called | ✅ | |
| P12313 | Showing a hidden tab refetches immediately, not on the 60s poll | headless: hide/show, assert a menu-data request within 2s | ✅ | |
| P12314 | The 60s safety poll does NOT fire while the tab is hidden | headless: hide for 70s, assert zero menu-data requests | ✅ | |
| P12315 | /api/health answers fast (well under the 8s DB deadline) rather than hanging | curl -w %{time_total} | ✅ | |
| P12316 | Signing in ONCE per run via scripts/sweep/login.mjs loginAs() reuses the cached cookie | run loginAs twice, assert no second POST to /api/staff-login | ✅ | |
| P12317 | /manager loads for a signed-in manager (panelGate's staff branch) | headless with the cached manager cookie, assert the panel shell renders | ✅ | |
| P12318 | /manager with no cookie redirects to /login?next= | headless: assert the redirect target | ✅ | |
| P12319 | /kitchen loads for a signed-in kitchen user | headless, assert the board shell | ✅ | |
| P12320 | /tablet loads for a signed-in waiter | headless, assert the floor | ✅ | |
| P12321 | /owner loads for a signed-in owner and shows their own restaurants only | headless, assert the sidebar list | ✅ | |
| P12322 | The manager panel's floor read answers under the 8s statement ceiling | time the /api/editor/all call | ✅ | |
| P12323 | A whole-floor refetch twice within 1.5s costs ONE database call (sharedFloorSummary) | hit /api/editor/summary twice, count lfh_table_view_summary in the log | ✅ | |
| P12324 | A `?table=N` refetch is NOT shared (it bypasses the window) | hit it twice, assert two reads | ✅ | |
| P12325 | A staff write invalidates the floor so the very next read shows it (read-your-own-write) | write a table tag on French House, immediately re-read, assert the tag; then restore | ✅ | |
| P12326 | invalidateFloor for French House does not disturb Aangan's shared snapshot | prime both, invalidate one, assert the other's read is still shared | ✅ | |
| P12327 | The kitchen board endpoint returns only received/preparing orders (activeOnly) | call /api/kitchen/board, assert no served/cancelled order | ✅ | |
| P12328 | The waiter tablet's board DOES include served orders (bills need them) | call /api/tablet/state, assert at least one served order when one exists | ✅ | |
| P12329 | A liveBoard read with a tableNumbers filter returns only those tables | call the targeted path, assert every row's table_number is in the list | ✅ | |
| P12330 | Nothing in my territory polls faster than 60s on the guest menu | headless: record request timestamps for 3 minutes, assert no <60s cadence | ✅ | |
| P12331 | The connection badge shows a real latency number measured from a breadcrumb | headless: assert the badge text matches /\d+\s?ms\|Live/ | ✅ | |
| P12332 | latencyTier's colours land on the badge as declared (green under 700ms) | headless: read the computed colour of the badge dot | ✅ | |
| P12333 | A currency switch re-renders every price on the guest menu | headless: switch to USD, assert prices change and use $ | ✅ | |
| P12334 | Switching back to INR restores ₹ and the en-IN grouping | headless: switch back, assert ₹1,15,000 style | ✅ | |
| P12335 | A language switch keeps dir=ltr (Arabic RTL deferred) | headless: pick Arabic, assert documentElement.dir === 'ltr' | ✅ | |
| P12336 | A dish name too long for its card SHRINKS instead of being cut | headless at 6 cards across: assert scrollHeight <= clientHeight+1 for every name | ✅ | |
| P12337 | No dish name on the guest grid is visually clipped at 360px | headless: same assertion at 360x780 dpr3 | ✅ | |
| P12338 | A tenant with only an accent colour gets a NEUTRAL canvas, not French House brown | headless on a non-#1 accent-only restaurant: sample the body background | ✅ | |
| P12339 | A themed tenant shows ONE brand colour — header wordmark and hero ink agree | headless: compare the two computed colours | ✅ | |
| P12340 | The guest menu still renders when localStorage is blocked | headless with the localStorage getter made to throw: assert dishes render | ✅ | |
| P12341 | Every Supabase REST read from the browser still works when AbortSignal.timeout is absent | headless with AbortSignal.timeout deleted: assert the menu's settings/ratings reads resolve | ✅ | |
| P12342 | A guest order placed on French House reaches the kitchen board | drive the order, assert the KOT appears; clean the row up by id | ✅ | |
| P12343 | That order's ticket is queued in print_jobs (mig 335) exactly once | count print_jobs rows for the order id | ✅ | |
| P12344 | A cancelled order's queued ticket is retired as 'dismissed', not printed | cancel it, call pendingKotJobs, assert the job's status | ✅ | |
| P12345 | Ten orphaned print jobs do not block a fresh ticket from being offered | seed 10 orphans by id, queue one live job, assert pendingKotJobs returns the live one; delete the seeds | ✅ | |
| P12346 | claimKotJobs called twice for one job wins once | call concurrently, assert exactly one id comes back | ✅ | |
| P12347 | finishKotJob(ok) closes the job and resolves the restaurant's open printer events | drive it, assert both | ✅ | |
| P12348 | waitingCount matches the number of queued+printing jobs | compare against a direct count | ✅ | |
| P12349 | A table close with money owed writes BOTH the activity line and the audit row | close a French House table force=true, assert both rows; restore | ✅ | |
| P12350 | A table close hands the owed figure grossed at the order's own rate | assert the logged ₹ equals total − discount×(1+tax/subtotal) | ✅ | |
| P12351 | A khata order survives a force close (not cancelled) | park a bill, force close, assert status not cancelled; clean up | ✅ | |
| P12352 | Closing a table clears its waiter-calls and pending requests | raise a call, close, assert resolved | ✅ | |
| P12353 | openTableSession called twice concurrently returns ONE session | fire both, assert the same id | ✅ | |
| P12354 | A soft-deleted bill is tombstoned on BOTH orders and session | delete one, assert sessions.deleted_at is set; restore | ✅ | |
| P12355 | A restore clears both stamps and reports the real number restored | restore it, assert both null and the count | ✅ | |
| P12356 | A restore of a refused write does NOT report success | point it at a non-existent id, assert restored 0 | ✅ | |
| P12357 | searchText's safeSearch makes a '*' search literal on the owner Activity log | headless: type '*', assert the row count does not become everything | ✅ | |
| P12358 | The owner Activity log answers 503 (not a permissive list) when the log switches cannot be read | stub the read to fail, assert 503 and the sentence | ✅ | |
| P12359 | An owner sees only their own restaurants in /api/owner/overview | call as the French House owner, assert no Aangan id | ✅ | |
| P12360 | An admin with ?scope=all sees the whole platform | call with the admin cookie + scope=all | ✅ | |
| P12361 | An admin with an act-as pin sees exactly that owner's set | call with ?scope=<rid>, assert the ids | ✅ | |
| P12362 | A malformed ?rid on a panel bounces to /aevinite rather than pinning a blank panel | headless: /tablet?rid=abc with the admin cookie | ✅ | |
| P12363 | viewAsPerson with ?as=<a waiter> narrows the tablet floor to their section | headless: assert the tile count matches assigned_tables | ✅ | |
| P12364 | viewAsPerson with ?as=<a manager> on the TABLET panel is ignored (role mismatch) | headless: assert the full floor | ✅ | |
| P12365 | An off-plan table (number above the floor plan) stays visible to every waiter | seed one by id, assert the tile renders; delete the seed | ✅ | |
| P12366 | A QR order on table 9,754,262 is refused with a plain sentence | POST the guest order, assert the message | ✅ | |
| P12367 | A QR order on table 31 with a 30-table floor is ALLOWED (the +500 margin) | POST it, assert accepted; clean up | ✅ | |
| P12368 | The bill preview page renders the SAME document billdoc.js prints | headless: compare the preview iframe's markup shape against a real bill | ✅ | |
| P12369 | A composition restaurant's bill preview carries no GST line | flip French House to composition, render, assert; restore the setting | ✅ | |
| P12370 | A per-dish-modes restaurant's preview includes the MRP sample line | flip the switch, render, assert; restore | ✅ | |
| P12371 | The KOT preview shows T5 (or the table's NAME), never 'Table 5' | headless: read the ticket's table label | ✅ | |
| P12372 | A staff panel API answers 503 + X-LFH-Busy when the database is unreachable | inject a failing read, assert the status and the header | ✅ | |
| P12373 | A staff panel API answers 400 (not 500) for a CHECK-constraint refusal | POST floor_per_row 40, assert 400 and the plain sentence | ✅ | |
| P12374 | Nothing this run started is still running at the end | lsof -ti:4125 empty after the teardown | ✅ | |
| P12375 | Every scratch row this run created is gone (deleted by id, in the same run) | re-read each seeded id, assert not found | ✅ | |

## D. SCREENSHOTS I actually Read — 1280x800 and A35 360x780 dpr3 (50 rows, 10%)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P12376 | Guest menu, French House, dark, 1280x800 — dishes, prices and the brand bar all render | shot + Read | ✅ | |
| P12377 | Guest menu, French House, dark, 360x780 dpr3 — nothing overlaps, no clipped dish name | shot + Read | ✅ | |
| P12378 | Guest menu, French House, LIGHT (lfh_theme), 1280x800 — text is readable on every card | shot + Read | ✅ | |
| P12379 | Guest menu, French House, LIGHT, 360x780 dpr3 | shot + Read | ✅ | |
| P12380 | Guest menu, Aangan, dark, 1280x800 — Aangan's own branding, no French House gold anywhere | shot + Read | ✅ | |
| P12381 | Guest menu, Aangan, dark, 360x780 dpr3 | shot + Read | ✅ | |
| P12382 | Guest menu, an accent-only tenant, dark, 1280x800 — the canvas is neutral, not #1's brown | shot + Read | ✅ | |
| P12383 | Guest menu, an accent-only tenant, LIGHT, 1280x800 — the canvas is neutral, not #1's cream | shot + Read | ✅ | |
| P12384 | Guest menu at 6 cards across (tablet width) — every dish name fits inside its box | shot + Read | ✅ | |
| P12385 | Guest menu with a very long dish name at 360px — the name is smaller, never cut | shot + Read | ✅ | |
| P12386 | Guest menu with a Hindi greeting — the conjuncts render, no dotted placeholder circles | shot + Read | ✅ | |
| P12387 | Currency switched to USD — every price shows $ and two decimals | shot + Read | ✅ | |
| P12388 | Currency switched to AED — the symbol has a space (AED 12) | shot + Read | ✅ | |
| P12389 | Connection badge, healthy — green, with a real ms figure | shot + Read | ✅ | |
| P12390 | Connection badge, socket weak — amber, and the bars differ from the red state | shot + Read | ✅ | |
| P12391 | Connection badge, device offline — red, and it does not claim a latency | shot + Read | ✅ | |
| P12392 | Manager panel floor, LIGHT (default lfh_panel_theme), 1280x800 | shot + Read | ✅ | |
| P12393 | Manager panel floor, DARK, 1280x800 | shot + Read | ✅ | |
| P12394 | Manager panel floor, 360x780 dpr3 — 2 tiles per row, vertical scroll only | shot + Read | ✅ | |
| P12395 | Manager panel floor, 780x360 (sideways phone) — 4 tiles per row | shot + Read | ✅ | |
| P12396 | Manager panel floor at a tablet width — capped at 6 tiles even with the admin set to 12 | shot + Read | ✅ | |
| P12397 | Kitchen board, 1280x800 — New / Cooking / Ready counts render | shot + Read | ✅ | |
| P12398 | Kitchen board, 360x780 dpr3 | shot + Read | ✅ | |
| P12399 | Waiter tablet floor, 1280x800 | shot + Read | ✅ | |
| P12400 | Waiter tablet floor, 360x780 dpr3 — the footer is not under the gesture bar | shot + Read | ✅ | |
| P12401 | Waiter tablet floor for a SECTIONED waiter — only their tables | shot + Read | ✅ | |
| P12402 | Owner console dashboard, DARK (aevidine_skin), 1280x800 | shot + Read | ✅ | |
| P12403 | Owner console dashboard, LIGHT (aevidine_skin), 1280x800 — no fixed colour is unreadable | shot + Read | ✅ | |
| P12404 | Owner console dashboard, 360x780 dpr3 | shot + Read | ✅ | |
| P12405 | Owner console — the same restaurant is the SAME colour in the sidebar and in the chart | shot + Read | ✅ | |
| P12406 | Owner Activity log, 1280x800 — rows read as English, no raw action keys | shot + Read | ✅ | |
| P12407 | Owner Activity log after a '*' search — the list narrowed, it did not become everything | shot + Read | ✅ | |
| P12408 | Owner Pay Later page, 1280x800 — no confident ₹0 where a read failed | shot + Read | ✅ | |
| P12409 | Owner Reports → Tax, 1280x800 — the filing table's total equals the KPI tile | shot + Read | ✅ | |
| P12410 | Owner Reports → Tax on a restaurant with no exempt sales — NO phantom exempt line | shot + Read | ✅ | |
| P12411 | Admin console Analytics, 7 days, 1280x800 — the trend is a real chart, not one bar and four blanks | shot + Read | ✅ | |
| P12412 | Admin console Analytics drilled into one day — the hour labels are IST | shot + Read | ✅ | |
| P12413 | Admin console Printing page, 1280x800 — routes, machines and the waiting count | shot + Read | ✅ | |
| P12414 | Bill preview (bill), 1280x800 — the exact page the manager panel prints | shot + Read | ✅ | |
| P12415 | Bill preview (parcel), 1280x800 — PARCEL where the table would be, discount row present | shot + Read | ✅ | |
| P12416 | Bill preview (KOT), 1280x800 — T5, an add-on, a dish note and an allergy line | shot + Read | ✅ | |
| P12417 | Bill preview for a COMPOSITION restaurant — a Bill of Supply, no GST rows, no Taxable value row | shot + Read | ✅ | |
| P12418 | Bill preview with per-dish modes on — the MRP line, its stamp and the untaxed block | shot + Read | ✅ | |
| P12419 | Printer test page rendering — it says nothing was charged and nothing recorded as a sale | shot + Read | ✅ | |
| P12420 | Audit detail card — the before box, the after box and the bill | shot + Read | ✅ | |
| P12421 | Audit detail card for an OWNER reader — the admin's identity is absent, the money is not | shot + Read | ✅ | |
| P12422 | A staff panel showing the busy message — 'very busy right now', never a raw TimeoutError | shot + Read | ✅ | |
| P12423 | A staff panel showing a CHECK refusal — 'Tables per row has to be…', not a Postgres sentence | shot + Read | ✅ | |
| P12424 | A guest menu on a browser with all site data blocked — dishes still render | shot + Read | ✅ | |
| P12425 | A guest menu with AbortSignal.timeout removed — dishes still render | shot + Read | ✅ | |

## E. TRACING A CHANGE ACROSS PANELS (50 rows, 10%)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P12426 | A dish going sold-out reaches the guest menu, the manager Edit-menu tab and the take-order builder | flip it, watch all three; restore | ✅ | |
| P12427 | A feature switch flipped in admin reaches the guest menu within the breadcrumb, not the 60s poll | flip `search` off, time the disappearance; restore | ✅ | |
| P12428 | refreshFeatures busting the DERIVED settings cache is what makes that instant | read lib/features.ts + observe the timing with invalidateSettings commented in a scratch copy | ✅ | |
| P12429 | A table opened on the manager floor appears on the waiter tablet and the kitchen board | drive it, watch all three; close | ✅ | |
| P12430 | An order placed by a guest appears on the kitchen board AND the tablet AND the manager floor | drive it; clean up | ✅ | |
| P12431 | A table left open across the 05:00 rollover still shows its orders on ALL of manager, tablet, kitchen | seed an overnight session by id, read all three; delete the seed | ✅ | |
| P12432 | The KOT number the kitchen prints is the number the manager floor shows | compare both surfaces for one order | ✅ | |
| P12433 | A merged child table's Mark-paid settles the WHOLE joint bill, on both the tablet and the manager | merge, settle, assert the parent total; unmerge | ✅ | |
| P12434 | A rename of table 7 reaches the tile, the bill and the printed KOT label | rename, check all three; restore the name | ✅ | |
| P12435 | A discount taken on the tablet shows the same figure on the manager Bills tab and the printed bill | drive it; clean up | ✅ | |
| P12436 | The tax rate a bill is charged at is the same on the preview, the printed bill and the Z-report | compare all three | ✅ | |
| P12437 | A cancelled bill appears in the Audit on the manager, the owner and the admin — with the same reason | cancel, read all three; clean up | ✅ | |
| P12438 | An admin action performed through the owner cockpit is recorded against the ADMIN panel, not the owner's feed | drive one, read /api/owner/oplog and the admin log | ✅ | |
| P12439 | An admin's identity is absent from the owner's Audit and from the manager's Removals | read both as each reader | ✅ | |
| P12440 | A log-visibility switch turned off hides that KIND on the owner Activity page and on the per-person card | flip it, read both; restore | ✅ | |
| P12441 | Turning the Payroll module off removes pay from the owner staff page AND the manager profile | flip it, read both; restore | ✅ | |
| P12442 | Turning Table types off does NOT take Pay later with it (they are separate modules since mig 235) | flip table_tags off, assert khata still works; restore | ✅ | |
| P12443 | The waiter's section narrows the tablet floor AND refuses a write to a table outside it | read + attempt a normal write on their own tile only | ✅ | |
| P12444 | A waiter section change reaches the tablet within the panel's own poll | change it, watch; restore | ✅ | |
| P12445 | The dashboard reach (today / today+yesterday) is the same on the manager rail and in the stats API | flip the Access option, read both; restore | ✅ | |
| P12446 | The bills reach clamps the Bills tab list AND every bill search | same | ✅ | |
| P12447 | A per-role discount cap refuses on the server exactly where the screen stops offering | read the Access value, attempt the cap boundary | ✅ | |
| P12448 | The manager's Bills tab and the admin bill ledger report the SAME net for one bill | compare netOf against the manager's figure | ✅ | |
| P12449 | A bill's net on the admin ledger equals orders.net_amount in the database | compare | ✅ | |
| P12450 | A pay-later bill shows as Pay-later on the admin ledger and in the khata book | compare | ✅ | |
| P12451 | An on-the-house bill shows as On the house on both, and collects ₹0 | compare | ✅ | |
| P12452 | A closed-unpaid bill's loss verdict is the same on the ledger and in the Cancellations report | compare | ✅ | |
| P12453 | The print route named in admin → Printing is the printer the helper is handed | read helperFor + claimNext for one job | ✅ | |
| P12454 | When a helper owns the KOT paper, every SCREEN stands down | read helpersFor on the kitchen and manager boot payloads | ✅ | |
| P12455 | Clearing the route makes the screens start printing again | clear, read both; restore | ✅ | |
| P12456 | A banquet bill's number renders identically in the panel and on the printed sheet | compare banquetBillNo against the SQL function | ✅ | |
| P12457 | A banquet sheet's tax lines come from banquet_tax_components when set, else the restaurant's normal tax | set, compare, restore | ✅ | |
| P12458 | The guest menu, /r/<slug>/menu and /q/<code> all resolve through lib/tenant.ts | grep the three route trees for getRestaurantBySlug | ✅ | |
| P12459 | All three guest doors hide a binned restaurant | bin a scratch restaurant, try all three, restore | ✅ | |
| P12460 | All three guest doors honour the Menu master switch | flip it, try all three; RESTORE IT even on a crash | ✅ | |
| P12461 | The offline layer serves the guest menu's saved read on a first visit (warmDataCache) | load once, go offline, reload, assert dishes | ✅ | |
| P12462 | A panel read served from the device says WHEN it was saved | same for /manager | ✅ | |
| P12463 | A panel write made offline replays through the outbox and lands once | drive it; clean up | ✅ | |
| P12464 | An idempotent replay of a REFUSED guest order is allowed to run again (didSomething) | drive a sold-out order twice | ✅ | |
| P12465 | The same action id on a SUCCESSFUL order never places it twice | drive it twice; clean up | ✅ | |
| P12466 | A restaurant's channel key is never in a panel payload (panelSafeSettings) | grep both panel payloads for platform_channels | ✅ | |
| P12467 | A restaurant's access_config is never in the tablet payload | grep /api/tablet/summary's reply | ✅ | |
| P12468 | password_hash and pin_hash never appear in any panel or owner payload | grep every payload this run captured | ✅ | |
| P12469 | The admin's Repair board groups one fault as ONE tile whichever path reported it | compare errorSig for the two shapes of one message | ✅ | |
| P12470 | A crash on /r/<slug>/owner is filed against the OWNER panel and carries the restaurant | trigger one, read the row; delete it | ✅ | |
| P12471 | An error page from the gateway is recorded as its <title>, not as markup | feed readableError a stored 522 page | ✅ | |
| P12472 | The owner report names an omitted restaurant instead of silently dropping it | make one restaurant's analytics fail, gather, assert `omitted` | ✅ | |
| P12473 | A tax label used at two rates stays on two lines in the gathered report | two restaurants, different CGST rates | ✅ | |
| P12474 | Erasing a guest reports what was KEPT and why (issued bills, the ban) | call erasureSummary, compare against the screen | ✅ | |
| P12475 | A restaurant's settings clone gives a NEW restaurant none of #1's identity, GST, geo or modules | create a scratch restaurant, diff its settings row against #1; delete it | ✅ | |

## F. MY OWN JUDGMENT — does a real restaurant need it this way? (25 rows, 5%)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P12476 | Is a 15s browser REST deadline right for a phone on restaurant wifi, or does it hide a real stall? | reason from the measured latencies in the file's own note | ✅ | |
| P12477 | Is a 1.5s shared floor window still the right trade after mig 238 made each read 11-29ms? | both levers are documented as complementary — assert neither was removed | ✅ | |
| P12478 | Would a waiter notice the 30s panel-entitlement TTL when an admin revokes a panel mid-service? | reason: 30s vs the 7-day cookie it replaced | ✅ | |
| P12479 | Is 'unknown table ⇒ refuse' the right default for a restricted waiter, or does it strand a real bill? | check the off-plan escape hatch covers the real stranded case | ✅ | |
| P12480 | Does an off-plan table staying visible to EVERYONE risk a waiter settling someone else's bill? | weigh against the 47/48 stranded-bill case that caused it | ✅ | |
| P12481 | Is +500 the right off-plan margin for a restaurant that numbers its counters at 900? | reason about the counter-numbering practice the file describes | ✅ | |
| P12482 | Is 'a failed restaurant read stands on a 10-minute-old answer' too generous for a deactivated restaurant? | weigh a 404 to a real diner against a 10-min stale yes | ✅ | |
| P12483 | Does the 05:00 IST rollover suit a restaurant that closes at 06:00? | reason; note it as a 🟡 if a restaurant could straddle it | ✅ | |
| P12484 | Should the aggregator master switch be per-restaurant rather than read off restaurant #1's row? | reason about a trimmed one-client stack | ✅ | |
| P12485 | Is a hand-maintained FX table acceptable? (Already REJECTED — R17. Do not re-raise.) | read docs/REJECTED-IDEAS.md R17 | ✅ | |
| P12486 | Is a chart-shape toggle wanted? (Already REJECTED. Do not re-raise.) | read docs/REJECTED-IDEAS.md | ✅ | |
| P12487 | Should the kitchen get a profile? (Already REJECTED three times. Do not re-raise.) | read docs/REJECTED-IDEAS.md + lib/staffProfileShared PROFILE_ROLES | ✅ | |
| P12488 | Would a restaurant want a ticket to print somewhere else when its own printer is asleep? | the BACKUP route line already answers this — assert it is opt-in | ✅ | |
| P12489 | Is 'a helper owns the paper even when asleep' right, or should a screen take over? | weigh a ticket the kitchen never sees against one that waits | ✅ | |
| P12490 | Is 5 attempts before a print job parks the right number for a rush? | reason about a paper-out printer during service | ✅ | |
| P12491 | Is a 4-hour signed media link too long for a supplier bill that gets forwarded? | weigh against the ~2h offline cache the note names | ✅ | |
| P12492 | Does a 20% cancellation share on a slow morning cry wolf? | check the MIN_COUNT AND MIN_SHARE pairing covers it | ✅ | |
| P12493 | Would an owner rather see a partial money total or a retryable error? | the codebase already chose: figures refuse, names degrade — assert both halves hold | ✅ | |
| P12494 | Is 'the admin is invisible' still right on a MONEY record, or should an owner know Aevidine acted? | reason; it is the owner's standing rule — note any tension as 🟡 | ✅ | |
| P12495 | Does an owner need to know a bill was removed by the platform rather than by their own staff? | same question, the other way round | ✅ | |
| P12496 | Is a 12-tile default floor usable on a 1280px laptop (95px tiles, icon-only buttons)? | measure a real tile at that width | ✅ | |
| P12497 | Is capping a touchscreen at 6 tiles right for a 12.9-inch iPad? | reason about the 1150px band | ✅ | |
| P12498 | Is 400ms the right human floor on a login form for someone using a password manager? | reason; it fails open either way | ✅ | |
| P12499 | Should a manager PIN shared by two managers be allowed at all, or refused at set time? | reason; today the log names both — is that enough for the owner? | ✅ | |
| P12500 | Does anything in lib/ need Redis, a queue or a read replica? (Stage-3 — must stay absent.) | grep package.json + lib/ for redis/bull/ioredis — none | ✅ | |

_Total rows: 500. Next id after this block: P12501._

## Result — both passes

Pass 1 found ONE real problem (P12175 / the `AbortSignal.timeout` row, block B) and it is written up
in `.claude/sweep/T25-findings.md`. It was fixed, guarded (`verify:abort-guard`), and pass 2 was run
from the top.

**Pass 2: 500 / 500 ✅. No ⏭.** The results above are pass 2.

Two things worth knowing for whoever re-runs these rows:

1. **Three rows changed their `how to verify` between the passes, because the first wording was
   wrong, not because the answer was.** The Aangan rows named the slug `aangan` (it is
   `aangan-garden-restaurant`); the dish-count rows used a selector that matched nothing on any page
   (`a[href*='/item/']` is the one that works); and the websocket row counted Next's dev HMR socket
   as a second connection. All three now assert the right thing.
2. **`menu-data` is fetched 3× on a cold DEV load** (Turbopack + React strict mode double-invoke
   effects) and 0× across the following 30s of idle. In production it is one. The row asserts
   "not polled", which is the claim that matters.
