# T4 — working without internet, and in every language

**Territory:** `public/sw.js` · `public/offline.html` · `public/panels/swreg.js` ·
`public/panels/offline.js` · `components/OfflineNotice.tsx` · `components/OfflineShell.tsx` ·
`components/ConnectionBadge.tsx` · `components/AppShell.tsx` · `components/BanGate.tsx` ·
`components/BotTrap.tsx` · `components/Maintenance.tsx` · `lib/i18n.ts`

**Phase block:** P01501–P02000 (500, permanent, never renumbered)
**Branch:** `sweep6/t4-guest-offline-sw-i18n` · **worktree:** `sweep6/T4` · **port:** 4104

## How the 500 are budgeted

| band | ids | count | what it is |
|------|-----|-------|------------|
| A | P01501–P01700 | 200 (40%) | reading the code for correctness |
| B | P01701–P01800 | 100 (20%) | conformance to the project rules named in the prompt |
| C | P01801–P01875 | 75 (15%) | watching it run (headless Playwright, own port 4104) |
| D | P01876–P01925 | 50 (10%) | screenshots actually opened and looked at (1280×800 and A35 360×780 dpr3) |
| E | P01926–P01975 | 50 (10%) | tracing one change across panels |
| F | P01976–P02000 | 25 (5%) | judgment: does a real restaurant need it to work this way |

Per-file coverage: every owned file has at least one row; the six files named in the scope
paragraph (`sw.js`, `offline.html`, `swreg.js`, `panels/offline.js`, `OfflineNotice.tsx`,
`i18n.ts`) have ten or more.

---

## 🔁 SWEEP #7 RE-RUN — 2026-08-22/23, against `origin/main` b64951ad

**Every one of the 500 rows below (P01501–P02000) was re-executed, not sampled.** 300 of them are
code-reading claims and were re-checked by script, one assertion per row; the live rows were driven
against a production build on **port 4204** (my own port, proved mine before anything was trusted);
`npm run verify:offline` was re-run in full — **55 passed, 0 failed**, and it tidied up tables 2, 3
and 28 after itself.

**No regression.** Every row that was `✅` is still `✅`. Every row that sweep #6 marked `❌→✅` is
still green.

**Ten of my own re-run assertions came back red and every single one was MY DETECTOR, not the
code** — verified by reading each line. They are recorded here because a withdrawn finding is as
useful as a confirmed one: it stops the next sweep re-filing it. The best of them is a lesson in
itself: my comment-stripper read `/api/editor/*` inside a `//` comment as the start of a block
comment and silently swallowed the whole `NEVER` list, which is this repo's own recorded
"half a comment eats the next rule" trap, reproduced inside the checker.

**Four rows moved, and one was a genuine fault in the record:**

| row | what changed |
|---|---|
| **P01936** | **The only ❌ of the re-run, and it was FALSE WHEN FILED.** It claimed `LFH_PING`/`LFH_PONG` is "used by `verify-warm-shell.mjs`" — `git log -S LFH_PING --all` shows the string has only ever appeared in `public/sw.js`. Nothing called it. **Fixed (item 4):** `verify:warm-shell` now uses it to ask the running worker its VERSION, which also puts the first guard on the VERSION↔cache-name contract. |
| P01603 | Expectation MOVED, legitimately: the `#home` button is route-aware since v10, so it no longer just sets `location.href = "/"`. Re-checked against the new behaviour. |
| P01669 | Expectation refined. `changesWord()` covers the four TITLES; the offline branch spells its plural inline in the SUB. Identical words, so not a defect — see P17090. |
| P01939 | Expectation refined. `PanelFrame` injects `--safe-t`; each panel's own stylesheet derives `--sat` from it. The substance holds. |

**New checks this run: `P16601`–`P17100` (500).** 497 green, 3 were real problems and are fixed on
this branch. Three findings in total, plus one carried in from a false ledger claim — all four in
`.claude/sweep/T4-findings.md`, one commit each.

Result key: ✅ pass · ❌ real problem (written to `T4-findings.md`, then fixed) · ⏭ skipped with a
written reason.

---

## Band A — reading the code for correctness (P01501–P01700)

### A1 · public/sw.js — the router and its exclusions

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01501 | Any request whose method is not GET returns from the fetch handler before any cache is consulted | read `public/sw.js` fetch listener: the `req.method !== "GET"` branch ends in `return` | ✅ | |
| P01502 | A non-GET to our own `/api/` stamps `lastWriteAt[family]` before returning | same branch sets `lastWriteAt[apiFamily(u.pathname)]` | ✅ | |
| P01503 | Telemetry posts (`/api/log`, `/api/errlog`, `/api/rt-config`, `/api/health`) do NOT stamp a write window | `NOT_A_CHANGE` regex is tested before stamping | ✅ | keeps the stall fallback alive |
| P01504 | `apiFamily()` returns `""` for a path that is not `/api/<x>`, and that key can never match a real family | read `apiFamily`; `""` is only ever written for non-`/api/` paths, which never reach it | ✅ | |
| P01505 | Cross-origin requests (Supabase, CDN) are never handled | `if (url.origin !== self.location.origin) return;` | ✅ | |
| P01506 | Range requests (3D model streaming, media) are never handled | `if (req.headers.has("range")) return;` | ✅ | |
| P01507 | `only-if-cached` probes from another mode are not handled | the `req.cache === "only-if-cached"` guard | ✅ | |
| P01508 | Sign-in / sign-out / verify API routes are never cached | `NEVER` regex list covers `staff-login`, `login`, `auth`, `owner/login`, `panel-login`, `verify`, `health` | ✅ | |
| P01509 | The sign-in PAGE (`/login`) is NOT excluded — a signed-out person offline still reaches it | `NEVER` matches `/api/...` paths only; `/login` is a navigation and reaches `handleNav` | ✅ | recorded rejection: never cache-exclude /login |
| P01510 | Logging out wipes DATA and SHELL but never FALLBACK | `LOGOUT` branch: `caches.delete(DATA).then(delete SHELL).then(precacheOffline)` | ✅ | |
| P01511 | `LOGOUT` matches exactly the two logout routes that exist | `grep -rl "panel-logout\|staff-logout" app/api` and compare with the regex | ✅ | |
| P01512 | Dev-only plumbing (`/_next/webpack-hmr`, `/__nextjs`, webpack chunks) is never handled | `isDevPlumbing` is tested in the router | ✅ | |
| P01513 | Big media (`/models/`, `.glb/.gltf/.mp4/.webm/.mov/.zip`) is never raced against a timeout | `isBigMedia` returns before any respondWith | ✅ | this is what `verify:cache` protects |
| P01514 | A navigation goes to `handleNav`, never to `networkFirst` | the `isNav` branch is first after the exclusions | ✅ | |
| P01515 | An RSC request gets its own cache key and never shares one with the HTML document | `rscKey()` appends `__lfh_rsc=1` | ✅ | |
| P01516 | `/_next/static/` is cache-FIRST in production and network-FIRST on localhost | the `IS_DEV ? networkFirst : cacheFirst` ternary | ✅ | stops "I deployed but the panel shows old code" |
| P01517 | An `/api/` path not in `DATA_PATHS` is left entirely alone | `if (!isData(url.pathname)) return;` | ✅ | |
| P01518 | Every regex in `DATA_PATHS` matches at least one real route in `app/api/` | for each pattern, `find app/api -type d` and match | ✅ | no dead patterns left |
| P01519 | `?refresh` / `?force` / `?nocache` is answered live or fails honestly — never from the device | `WANTS_LIVE.some(...)` → `networkFirst(..., 0, { noFallback: true })` | ✅ | |
| P01520 | Owner / admin / inventory reads get NO stall timeout (they are slow by nature) | `slowByNature` regex → `timeout = 0` | ✅ | |
| P01521 | A read within 60s of a write to the SAME family waits for the truth | `justWrote` → `timeout = 0` AND `justWrote` passed into `networkFirst` | ✅ | |
| P01522 | `justWrote` also suppresses the BUSY-status fallback, not only the stall timer | `networkFirst`: `if (!noFallback && !justWrote && ...)` | ✅ | |
| P01523 | `justWrote` is false when the device is offline, so offline still gets its saved copy | `self.navigator.onLine !== false && ...` | ✅ | |
| P01524 | Everything else same-origin static gets the long asset leash, not the short read one | last line: `networkFirst(req, ASSET, req.url, ASSET_TIMEOUT_MS)` | ✅ | |
| P01525 | `ASSET_TIMEOUT_MS` is longer than `READ_TIMEOUT_MS` | 25000 > 6000 | ✅ | a slow script is rescued, a slow read falls back fast |
| P01526 | `handleNav` never memorises a redirect or an error page | `res.ok && res.status === 200 && res.type !== "opaqueredirect"` | ✅ | |
| P01527 | `handleNav` keeps the abandoned request alive so the saved copy ages forward | `live.catch(() => {})` in the catch, and `live` still writes on resolve | ✅ | |
| P01528 | `handleNav`'s fallback order is: saved page → branded offline page, with no `ignoreSearch` guess | read the return expression | ✅ | |
| P01529 | `shellKey` strips only `table` and `t` — every other query param still distinguishes a page | `TABLE_PARAMS` is exactly `["table","t"]` | ✅ | whatever table they scan is the table they order for |
| P01530 | `shellKey` returns the raw url unchanged when neither param is present (no needless re-encode) | the `touched` flag | ✅ | |
| P01531 | `shellKey` cannot throw on a malformed url | wrapped in try/catch returning `rawUrl` | ✅ | |
| P01532 | `networkFirst` starts the real request exactly once, never twice | `const live = fetch(req)` used in both the race and the fallback | ✅ | |
| P01533 | A genuine server error (500, 404) is passed through, never papered over with saved data | only `CANT_ANSWER_NOW` (502/503/504) falls back | ✅ | |
| P01534 | 500 is deliberately NOT in `CANT_ANSWER_NOW` | read the Set | ✅ | a bug must reach the screen |
| P01535 | A stall with nothing saved keeps waiting rather than inventing an offline verdict | `if (stalled) { try { return await live } catch {} }` | ✅ | |
| P01536 | A dead network on a DATA read answers with `{error:"offline"}` + `X-LFH-Offline: 1`, not a raw exception | read the `cacheName === DATA` branch | ✅ | panels branch on this |
| P01537 | A dead network on a non-DATA read throws, so the browser's own asset handling applies | `throw new Error("offline")` | ✅ | |
| P01538 | `cacheFirst` fills the cache on a miss and returns the network reply | read `cacheFirst` | ✅ | |
| P01539 | `cacheFirst` uses `ignoreVary`, so a Next `Vary` header cannot make every hit miss | `cache.match(req.url, { ignoreVary: true })` | ✅ | |
| P01540 | `cachedCopy` uses `ignoreVary` too | read `cachedCopy` | ✅ | this is the bug that made offline screens come up empty |
| P01541 | A saved copy older than `MAX_STALE_MS` is deleted and treated as absent | `if (at && Date.now() - at > MAX_STALE_MS) { delete; return null }` | ✅ | |
| P01542 | `MAX_STALE_MS` is one number for everyone — no per-family window | grep `MAX_STALE_MS` in sw.js: one const, one use | ✅ | R16: "there is no off-line limit for diner" |
| P01543 | The comment above `MAX_STALE_MS` still carries the `REJECTED (owner, 2026-08-12)` line | read it | ✅ | `verify:rejected` depends on this |
| P01544 | Only the DATA cache announces staleness to the page | `if (cacheName === DATA) announceStale(...)` | ✅ | a saved HTML shell is not "saved figures" |
| P01545 | `announceStale` messages ONLY the client that made the request when a clientId is known | read `announceStale` | ✅ | stops a staff read painting a diner's menu |
| P01546 | `announceStale` falls back to all windows only when there is no clientId | same | ✅ | |
| P01547 | `tagged()` sets both `X-LFH-From-Cache` and `X-LFH-Cached-At` | read `tagged` | ✅ | |
| P01548 | `tagged()` preserves the original status and statusText | read the `new Response(...)` options | ✅ | |
| P01549 | `putStamped` checks the DECLARED content-length before buffering | `const declared = Number(res.headers.get("content-length")...)` comes first | ✅ | a huge report is skipped without being copied into memory |
| P01550 | `putStamped` also checks the real byte length, for a reply with no content-length | `buf.byteLength > MAX_DATA_BYTES` | ✅ | |
| P01551 | `putStamped` clones before consuming, so the response it was handed is still readable by the page | `res.clone().arrayBuffer()` | ✅ | |
| P01552 | `putStamped` never throws into the fetch path | wrapped in try/catch | ✅ | |
| P01553 | `trim` drops the OLDEST entries first | `aged.sort((a,b) => a.at - b.at)` then `slice(0, len - cap)` | ✅ | |
| P01554 | `trim` is a no-op below the cap | `if (keys.length <= cap) return;` | ✅ | |
| P01555 | Each cache has its own cap and `putStamped` picks the right one | `cacheName === DATA ? CAPS.data : cacheName === SHELL ? CAPS.shell : CAPS.asset` | ✅ | |
| P01556 | `WARM_ASSET_MAX` (80) is comfortably below `CAPS.asset` (400) | read both consts | ✅ | one warm can't evict a whole deploy |
| P01557 | `install` precaches the offline page and then skips waiting | read the install listener | ✅ | |
| P01558 | `activate` deletes every `lfh-` cache that is not one of the four current names | read the activate listener | ✅ | |
| P01559 | `activate` claims the clients, so the first load is controlled as early as possible | `self.clients.claim()` | ✅ | |
| P01560 | `precacheOffline` failing never breaks install | try/catch with a comment naming the inline fallback | ✅ | |
| P01561 | `LFH_CLEAR_DATA` deletes DATA and SHELL and re-precaches the offline page | read the message handler | ✅ | |
| P01562 | `LFH_CLEAR_DATA` deliberately does NOT delete FALLBACK | same | ✅ | this gap is what showed the browser's error page mid-sign-out |
| P01563 | `LFH_SW_KILL` drops every `lfh-` cache and unregisters | read the handler | ✅ | the `?nosw=1` escape hatch |
| P01564 | `LFH_SKIP_WAITING` exists so a fresh deploy takes over now | read the handler | ✅ | |
| P01565 | `LFH_PING` answers only the asking client | `e.source.postMessage` | ✅ | |
| P01566 | `LFH_WARM_SHELL` refuses a url from another origin | `if (new URL(key).origin !== self.location.origin) return;` | ✅ | |
| P01567 | `LFH_WARM_SHELL` skips a page already stored — one request, only when genuinely missing | `if (await cache.match(key, {ignoreVary:true})) return;` | ✅ | |
| P01568 | `LFH_WARM_SHELL` stores under the SAME key `handleNav` would use | both call `shellKey` | ✅ | |
| P01569 | `LFH_WARM_SHELL` applies the same "real page only" rule as `handleNav` | `res.ok && status === 200 && type !== "opaqueredirect"` | ✅ | |
| P01570 | Asset warming never pulls in an `/api/` read | `if (u.pathname.startsWith("/api/")) continue;` | ✅ | egress: a menu payload is not small |
| P01571 | Asset warming never pulls in a sign-in route, big media or dev plumbing | the three guards on the same line | ✅ | |
| P01572 | Asset warming never pulls in another origin's file | `if (u.origin !== self.location.origin) continue;` | ✅ | |
| P01573 | Asset warming skips anything already stored, so a second visit costs nothing | `if (await cache.match(u.href, ...)) continue;` | ✅ | |
| P01574 | Asset warming is bounded twice (slice AND a counter) | `assets.slice(0, WARM_ASSET_MAX)` plus `if (stored >= WARM_ASSET_MAX) break;` | ✅ | |
| P01575 | One failing asset does not stop the rest of the warm | per-asset try/catch | ✅ | |
| P01576 | `LFH_WARM_DATA` stores WITHOUT fetching anything | read the handler: it builds a `Response` from the body it was handed | ✅ | this is the egress rule made concrete |
| P01577 | `LFH_WARM_DATA` refuses another origin | `u.origin !== self.location.origin` | ✅ | |
| P01578 | `LFH_WARM_DATA` refuses anything that is not a read family we cache anyway | `!u.pathname.startsWith("/api/") \|\| !isData(...)` | ✅ | |
| P01579 | `LFH_WARM_DATA` refuses a sign-in route | `isNever(u.pathname)` | ✅ | |
| P01580 | `LFH_WARM_DATA` applies the same size ceiling as any saved read | `wBody.length > MAX_DATA_BYTES` | ✅ | |
| P01581 | `LFH_WARM_DATA` never overwrites a copy the worker fetched itself | `if (await cache.match(u.href, ...)) return;` | ✅ | |
| P01582 | `LFH_WARM_DATA` stamps the copy so its age is honest | it goes through `putStamped` | ✅ | |
| P01583 | The VERSION constant is a single source and every cache name derives from it | `SHELL/ASSET/DATA/FALLBACK` all interpolate `VERSION` | ✅ | |
| P01584 | The VERSION note explains what each bump was for, so the next person knows whether to bump | read the `const VERSION` line | ✅ | |
| P01585 | `IS_DEV` is decided from the worker's own hostname, not from a build flag | `self.location.hostname` | ✅ | |
| P01586 | The inline last-resort page carries BOTH `#retry` and `#home` ids | read `offlinePage()`'s inline HTML | ✅ | `verify:offline` looks for `#home` |
| P01587 | The inline last-resort page does NOT name a cause | read its text: "either this device is offline or the app isn't answering" | ✅ | never state a cause the evidence doesn't support |
| P01588 | The inline last-resort page promises only what is guaranteed ("anything already saved…") | read its green line | ✅ | no fabricated promise |
| P01589 | `offlinePage()` tries FALLBACK, then a global match, then the network, then inline | read the function top to bottom | ✅ | it always answers |
| P01590 | `offlinePage()` re-stores the page in the background when it had to fall back | `precacheOffline()` on both non-inline paths | ✅ | |
| P01591 | The inline last-resort page answers 503, not 200 | read the `new Response(..., { status: 503 })` | ✅ | an honest status for an honest failure |
| P01592 | Nothing in `sw.js` reads or writes a cookie, a token or any account value | `grep -n "cookie\|token\|password\|Authorization" public/sw.js` | ✅ | zero matches |
| P01593 | Nothing in `sw.js` posts anywhere off-device | `grep -n "fetch(" public/sw.js` — every call is same-origin by construction | ✅ | |
| P01594 | `MAX_DATA_BYTES` is expressed readably and is 3 MB | `3_000_000` | ✅ | |
| P01595 | Neither `NAV_TIMEOUT_MS` nor `READ_TIMEOUT_MS` was shortened below the 6s the offline bar's settle assumes | both are 6000; `NEVER_CONNECTED_SETTLE_MS` in panels/offline.js reasons from it | ✅ | traced across the two files |
| P01596 | `CANT_ANSWER_NOW` matches the statuses `lib/panelFailure.ts` actually sends | read `lib/panelFailure.ts` | ✅ | |
| P01597 | The fallback for a busy reply only happens on the DATA cache | `cacheName === DATA` in the condition | ✅ | |
| P01598 | With nothing saved, a busy reply is returned untouched so the panel hears the truth | the `if (saved) return saved;` shape | ✅ | |
| P01599 | A forced refresh never gets the busy fallback either | `noFallback` is checked first | ✅ | |
| P01600 | No `console.log` is left in `sw.js` | `grep -n "console\." public/sw.js` | ✅ | zero |

### A2 · public/offline.html — the last-resort page

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01601 | Nothing on the page is fetched from another origin (no font, image or script) | `grep -n "http://\|https://\|src=" public/offline.html` | ✅ | by definition nothing can be fetched |
| P01602 | The page carries the ids `#retry` and `#home` | read the two buttons | ✅ | the guard keys on `#home` |
| P01603 | "Go to the home screen" really navigates, so the page is never a dead end | **expectation moved (v10/v11):** the handler sets `location.href = homeHref`, which is route-aware — a diner gets a menu, a waiter gets "/" | ✅ | re-run against the new behaviour; every door is now driven by `verify:offline-retry` §7 |
| P01604 | The page does not state a cause before it has tested one | the initial `#why` reads "Checking what's wrong..." | ✅ | |
| P01605 | The reassurance says only what is guaranteed ("Anything already saved on this device is safe") | read the `.safe` block | ✅ | matches the wording `verify:offline` asserts |
| P01606 | The page does NOT promise that everything done offline was saved | grep for "Nothing you did is lost" — absent | ✅ | |
| P01607 | `diagnose()` checks device → our server → the app, in that order | read the function | ✅ | |
| P01608 | The reachability probe is a path the worker leaves alone | `/api/__offline-check` matches no `DATA_PATHS` entry and no `NEVER` entry | ✅ | traced into sw.js |
| P01609 | The reachability probe touches no database | there is no `app/api/__offline-check` route; an unmatched path 404s at the edge | ✅ | |
| P01610 | The health probe is a route the worker never caches | `/api/health` is in `NEVER` | ✅ | |
| P01611 | Every probe is time-boxed | `ping(url, ms)` always resolves via a timer | ✅ | today's fault was a request that never came back |
| P01612 | A timed-out probe aborts the request instead of leaving it running | `ctl.abort()` in the timer | ✅ | |
| P01613 | `ping` cannot resolve twice | the `done` flag on all three paths | ✅ | |
| P01614 | `ping` busts any cache | `cache: "no-store"` plus a `t=` cache-buster | ✅ | |
| P01615 | `say()` builds its text with `textContent`, never `innerHTML` | read `say` | ✅ | nothing on this page can print markup |
| P01616 | The device-offline verdict is red; the "it's on us" verdicts are amber | `say("device", …)` clears `warn`, the others keep it | ✅ | |
| P01617 | `document.title` is updated with the verdict | `document.title = title + " — your work is safe"` | ✅ | |
| P01618 | A reachable server but an unhealthy app says "your internet is fine — this one is on us" | read the `say("app", …)` call | ✅ | never blame the wrong side |
| P01619 | A timed-out health check and an erroring one give DIFFERENT sentences | the `health.timedOut ? … : …` ternary | ✅ | |
| P01620 | The retry timer backs off and is capped at 60s | `wait = Math.min(wait * 2, 60000)` | ✅ | |
| P01621 | The retry delay is jittered ±25%, so devices don't check in unison | `Math.round(wait * (0.75 + Math.random() * 0.5))` | ✅ | |
| P01622 | A failed `diagnose()` still re-arms the loop | `.catch(function () { next(false); })` | ✅ | a screen that recovers from nothing was the bug |
| P01623 | The page reloads only when a real connection is PROVEN, not when `navigator.onLine` flips | `cycle()` reloads inside `next(back)` only after `diagnose()` returned true | ✅ | cafe wifi lies |
| P01624 | An `online` event while a check is already running is ignored rather than stacked | the `checking` flag | ✅ | |
| P01625 | An `online` event while the page is IDLE does not add a second, permanent retry loop | read `cycle`/`next`: every `next(false)` schedules a fresh `setTimeout` with no handle kept, so each idle `online` starts an independent chain that never ends | ❌→✅ | **FIXED** — one handle, cleared before each schedule; guarded by `verify:offline-retry` |
| P01626 | The page has a reduced-motion rule for its pulsing dot | `@media (prefers-reduced-motion: reduce)` | ✅ | |
| P01627 | The layout uses `100dvh`, so a phone's address bar doesn't clip the card | `min-height: 100dvh` | ✅ | |
| P01628 | The viewport meta carries `viewport-fit=cover` for a notched phone | read the meta tag | ✅ | |
| P01629 | `color-scheme: dark` is declared so form controls match | `:root { color-scheme: dark }` | ✅ | |
| P01630 | The "blur" is one unprefixed `backdrop-filter` line | `grep -n "backdrop-filter" public/offline.html` — one, no `-webkit-` | ✅ | hand-adding the prefix makes the build DROP it |
| P01631 | The card has a maximum width so it does not stretch on a desktop | `width: min(420px, 100%)` | ✅ | |
| P01632 | `<html lang="en">` is set | read the tag | ✅ | this page is deliberately English |
| P01633 | The page never references a cookie, a token or an account value | `grep -n "cookie\|token\|localStorage" public/offline.html` | ✅ | zero |
| P01634 | The two buttons are real `<button type="button">`, not links styled as buttons | read the markup | ✅ | no accidental form submit |
| P01635 | The hint line always says what happens next ("Checking again in Ns…") | read `next()` | ✅ | |

### A3 · public/panels/swreg.js — the panels' registration

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01636 | The file does nothing at all when the browser has no service-worker support | first line: `if (!("serviceWorker" in navigator)) return;` | ✅ | |
| P01637 | `?nosw=1` unregisters and tells the worker to drop its caches | read the escape-hatch block | ✅ | same hatch as the React side |
| P01638 | Registration failing never throws into the panel | `.catch(function () {})` on the register promise | ✅ | |
| P01639 | The panel asks for an update when it becomes visible again | the `visibilitychange` listener calling `r.update()` | ✅ | staff leave panels open for days |
| P01640 | `pageAssets()` only returns same-origin urls | `.filter(n => n.indexOf(location.origin) === 0)` | ✅ | |
| P01641 | `pageAssets()` cannot throw | wrapped in try/catch returning `[]` | ✅ | |
| P01642 | The warm runs immediately when already controlled, and on `controllerchange` when not | `if (controller) warm(); else addEventListener("controllerchange", warm, {once:true})` | ✅ | the first-visit race |
| P01643 | `warm()` cannot throw into the panel | try/catch | ✅ | |
| P01644 | `LFH_WARM.data` refuses a missing url or a non-string body | `if (!url \|\| typeof body !== "string") return;` | ✅ | |
| P01645 | Reads offered before the worker exists are held, bounded at 12 | `else if (pendingData.length < 12)` | ✅ | a page that never gets a worker can't grow a list |
| P01646 | `flushWarmData` empties the queue exactly once (it swaps the array before iterating) | `var q = pendingData; pendingData = [];` | ✅ | |
| P01647 | `flushWarmData` is a no-op when there is no controller | first line returns | ✅ | |
| P01648 | Posting a warm message can never throw into the panel | per-item try/catch | ✅ | |
| P01649 | Registration is deferred to `load`, so it never competes with first paint | the `document.readyState === "complete"` branch | ✅ | |
| P01650 | The scope is `/`, so one registration covers the panels and the React pages alike | `register("/sw.js", { scope: "/" })` | ✅ | |
| P01651 | Registering twice (React page + panel iframe) is harmless | the browser keeps one worker; the header comment says so and the code relies on nothing else | ✅ | |
| P01652 | The file introduces no dependency and no global beyond `window.LFH_WARM` | `grep -n "window\." public/panels/swreg.js` | ✅ | |

### A4 · public/panels/offline.js — the staff offline bar and the needs-you sheet

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01653 | `noteResponse` never lets a readout fault break a real request | the whole body is wrapped in try/catch | ✅ | |
| P01654 | An `X-LFH-Offline: 1` reply sets `seenOfflineRead` and returns early | read `noteResponse` | ✅ | |
| P01655 | The first saved reply of a spell starts the `staleSince` clock, and a later one does not restart it | `if (!stale.fromCache) staleSince = Date.now();` | ✅ | |
| P01656 | A genuinely fresh reply clears every stale flag AND both timers | the `else if (res.ok)` branch | ✅ | |
| P01657 | A single saved reply cannot pin a warning on screen forever | `forgetTimer` drops the flag after 25s regardless | ✅ | |
| P01658 | The auto-heal refetch is not fired while offline (nothing to fetch) | `if (isOffline()) return;` inside the heal timer | ✅ | |
| P01659 | The heal fires the same event a finished sync fires, so panels reuse one handler | `lfh:stale-refresh` | ✅ | traced into the panels |
| P01660 | A re-render is scheduled just past the never-connected settle, so the bar appears on a still panel | `setTimeout(render, NEVER_CONNECTED_SETTLE_MS + 250)` | ✅ | |
| P01661 | `connIsBad()` reads the SAME source as the connection pill | `window.LFH_RT.getStatus()` | ✅ | the bar and the badge can never contradict each other |
| P01662 | Live updates flowing means the connection is fine, whatever one slow read did | `if (st === "online") return false;` | ✅ | the "green Live above an orange warning" fault |
| P01663 | A drop after we HAD been connected counts as bad immediately | `if (rt.everConnected()) return true;` | ✅ | |
| P01664 | "Never connected in this session" is judged on TIME, not treated as innocent | the `staleSince` comparison against `NEVER_CONNECTED_SETTLE_MS` | ✅ | a reload on a bad line never connects at all |
| P01665 | `NEVER_CONNECTED_SETTLE_MS` is short enough to beat someone settling a bill on a stale board | 1200ms, with the reasoning written above it | ✅ | |
| P01666 | `fmtTime` says "a moment ago" under a minute and a clock time past an hour | read the function | ✅ | |
| P01667 | `fmtTime(0)` returns "earlier" rather than 1970 | the `if (!ts)` guard | ✅ | |
| P01668 | The count and the retry-due boolean are separate names that cannot shadow each other | `n`/`waiting` vs `retryDue` | ✅ | "Sending true saved changes…" was this exact bug |
| P01669 | `changesWord(n)` is used for every plural TITLE in `barState` | read all four return objects | ✅ | expectation refined: the offline branch spells its plural inline in the SUB line. Identical words, not a defect — recorded as P17090 so this row is not read as stronger than it is |
| P01670 | The failed branch does not name a cause it hasn't established | "It couldn't be applied." — no mention of the connection | ✅ | |
| P01671 | The offline branch says what is on screen AND how many changes are waiting | read the `sub` construction | ✅ | |
| P01672 | A queue that is merely waiting for its next scheduled try says WHEN, not "hasn't sent yet" | the `retryDue` branch | ✅ | backoff runs to two minutes; age alone was the wrong test |
| P01673 | "Sending…" is only shown when a round really is in flight or due | `stuck` requires `!retryDue` and `Date.now() - nudgedAt > 8000` | ✅ | |
| P01674 | A tap on "Send now" earns a genuine "Sending…" for a few seconds | `nudgedAt` is set in `sendNow()` before `render()` | ✅ | never a dead tap |
| P01675 | "Send now" always leaves a trace on screen even if the flush throws | `render()` runs before the try/catch around `flush()` | ✅ | |
| P01676 | The bar NEVER says "saved" about something the device could not save | the `unsafe` branch reads `LFH_OUTBOX.storageFailed()` | ✅ | |
| P01677 | The unsafe-storage wording tells the person what to do (keep the panel open) | read the `sub` | ✅ | |
| P01678 | A queue made while online is not described as "made while you were offline" | `offlineMade` is computed from each item's `why` | ✅ | |
| P01679 | One saved reply on a healthy connection shows NO bar | the final `stale.fromCache && connIsBad()` condition | ✅ | |
| P01680 | With nothing wrong, `barState()` returns null and the bar element is removed | `if (!st) { bar.remove(); … }` | ✅ | |
| P01681 | Removing the bar also republishes `--offbar-h` as 0 | `publishHeight()` in the same branch | ✅ | otherwise the panel keeps a hole |
| P01682 | `publishHeight` re-measures on every render, because the text wraps on a phone | read `render()`'s last lines | ✅ | |
| P01683 | `publishHeight` also re-runs on resize | the `resize` listener in `boot()` | ✅ | |
| P01684 | The bar is inserted ABOVE the panel top bar so it pushes content down | `host()` returns `{ parent: top.parentNode, before: top }` | ✅ | a floating banner hid the kitchen's tabs |
| P01685 | The bar clears the notch with `--sat`, not `env()` (which reads 0 in an iframe) | the `.lfh-offbar` padding rule | ✅ | |
| P01686 | The bar's tones are solid saturated colours that read on BOTH panel skins | the four `tone-*` rules use solid hex, not tints | ✅ | |
| P01687 | The bar is `role="status"` | `bar.setAttribute("role", "status")` | ✅ | |
| P01688 | Every button in the bar is `type="button"` | read `render()` | ✅ | |
| P01689 | Bar button clicks stop propagation so they don't also hit the panel underneath | `e.stopPropagation()` on both | ✅ | |
| P01690 | Offering "Send now" never costs the person the way IN to the list | the `alt` button renders "See" beside it | ✅ | |
| P01691 | The needs-you sheet builds every row with `textContent`, never `innerHTML` | read `renderSheet` and `el()` | ✅ | |
| P01692 | "Try again" is hidden for a change that cannot work on a retry | `if (it.retryable !== false)` | ✅ | a clash won't fix itself by resending |
| P01693 | "Not needed anymore" arms itself and needs a second tap | read the click handler | ✅ | throwing work away asks once, inline (owner, 2026-08-06) |
| P01694 | The armed state reverts after 4s so a mis-tap costs nothing | the `setTimeout(..., 4000)` | ✅ | |
| P01695 | The armed state is visually different (red), not just different text | `.lfh-off-ghost.is-armed` | ✅ | |
| P01696 | The discard confirmation is INLINE, never a popup dialog | `grep -n "confirm(" public/panels/offline.js` — zero | ✅ | "no extra click" |
| P01697 | A queued row says the true thing for offline vs slow vs sending | the three-way expression in `renderSheet` | ✅ | |
| P01698 | "Send now" appears on the FIRST queued row only, because the queue sends in order | `if (slow && i === 0)` | ✅ | |
| P01699 | An empty sheet says "Everything has been sent" rather than showing nothing | the `.lfh-off-empty` branch | ✅ | |
| P01700 | The sheet registers with the phone back-button manager and unregisters on close | `LFH_BACK.layer("offline-needs-you", closeSheet)` and `backOff()` | ✅ | every popup registers the moment it's built |

---

## Band B — conformance to the project rules (P01701–P01800)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01701 | Every API family a panel reads offline is present in `DATA_PATHS` | list `app/api/*/` and diff against the array | ✅ | |
| P01702 | No `DATA_PATHS` entry matches a route family that is POST-only | for each pattern, check the route file exports GET | ✅ | the dead `/api/guest/` and `/api/menu` patterns are gone and the removal is explained in place |
| P01703 | The guest menu's read (`/api/r/<slug>/menu-data`) is covered | `/^\/api\/r\//` | ✅ | |
| P01704 | The blocked-staff screen's read is covered | `/^\/api\/blocked/` | ✅ | it came up empty with no internet |
| P01705 | `panel-profile` is covered, so the ⚙/👤 button still appears offline | `/^\/api\/panel-profile/` | ✅ | |
| P01706 | The maintenance flag is covered, so panels don't misjudge it offline | `/^\/api\/maintenance/` | ✅ | |
| P01707 | `rt-config` is covered, so realtime reconnects the instant we're back | `/^\/api\/rt-config/` | ✅ | |
| P01708 | The offline layer never queues or replays a WRITE | `req.method !== "GET"` returns first | ✅ | the outbox owns offline writes; a replayed POST would risk a double bill |
| P01709 | Saved data is LABELLED as saved on every surface that shows it | headers → `panels/offline.js` bar and `OfflineNotice.tsx` strip | ✅ | traced end to end |
| P01710 | No screen fabricates a bill number, a total or a KOT number while offline | `grep -n "bill\|kot\|invoice" public/sw.js public/panels/offline.js components/OfflineNotice.tsx` | ✅ | zero fabrication; the queue only labels what the person did |
| P01711 | The offline strip on the guest menu stays English on purpose | the `REJECTED (owner, 2026-08-12)` block above `msg` in `OfflineNotice.tsx` | ✅ | R15 — do not wire it to lib/i18n.ts |
| P01712 | That rejection comment is still on the exact line someone would otherwise change | read it | ✅ | `verify:rejected` |
| P01713 | The 2-hour offline window is not lengthened for diners | `MAX_STALE_MS`, single use | ✅ | R16 |
| P01714 | The login trap field is the LAST field in the rendered form | read where `<BotTrap />` sits in `app/login/LoginForm.tsx` and `app/staff-login/LoginForm.tsx` | ✅ | first, it refuses real people |
| P01715 | The bot check fails OPEN and nothing in my territory hardens it | `lib/botCheck.ts` returns "allow" on anything it cannot judge | ✅ | deliberate; not a finding |
| P01716 | The trap field is hidden by off-screen positioning, not `display:none` | `HIDDEN` in `BotTrap.tsx` | ✅ | |
| P01717 | The trap field is unreachable by keyboard | `tabIndex={-1}` on both inputs | ✅ | |
| P01718 | The trap field is invisible to a screen reader | `aria-hidden="true"` on the wrapper | ✅ | someone using one must not be told about it |
| P01719 | The trap field is not offered to a password manager | `autoComplete="off"` and an unrecognised name | ✅ | |
| P01720 | There is exactly ONE component that hides a trap field, not a copy per door | `grep -rn "BOT_TRAP_FIELD" app components` | ✅ | two copies is how one ends up visible |
| P01721 | CSP being report-only is untouched by anything here | `grep -n "Content-Security-Policy" next.config.* app` | ✅ | deliberate; not a finding |
| P01722 | No push notification can fire from anything in this territory | `grep -rn "showNotification\|push" public/sw.js` | ✅ | backup stacks are deliberately silent |
| P01723 | Dark is the default everywhere and nothing here changes that | read the skin handling in the badge and the bar | ✅ | intended |
| P01724 | Every popup in this territory registers with a back-button manager | the needs-you sheet (`LFH_BACK.layer`) and the badge popover (`useBackClose`) | ✅ | |
| P01725 | No tap in this territory returns silently | every button either acts or re-renders with a visible change | ✅ | `verify:taps` |
| P01726 | Nothing in this territory polls faster than the 60s backstop | intervals: 2000ms only while a queue is non-empty, 1200ms stamping only while work is pending, 30000ms clock | ✅ | none of them touch the network or the database |
| P01727 | The 2s and 1.2s ticks are torn down when the queue empties | `syncWaitTick` and `syncStamping` both clear before re-arming | ✅ | |
| P01728 | The 30s heartbeat does no network work | it calls `render()` only | ✅ | |
| P01729 | `AppShell`'s fallback poll is 60s, not faster | `setInterval(..., 60000)` | ✅ | the backstop rule |
| P01730 | `AppShell`'s poll is paused while the tab is hidden | `if (!document.hidden) refresh()` | ✅ | a backgrounded tab does zero work |
| P01731 | `AppShell` drops its realtime channel after 2 minutes hidden | `IDLE_MS = 120000` then `teardown()` | ✅ | the "stale 41 connections" problem |
| P01732 | `AppShell` resubscribes AND refetches on focus | the `else` branch of `onVisibility` | ✅ | |
| P01733 | `AppShell` watches the breadcrumb table, not `settings` | the `realtime_events` subscription with `topic_rid=eq.menu:<rid>` | ✅ | mig 282 |
| P01734 | The realtime filter is applied SERVER-side, so a tab is never handed another restaurant's events | `topic_rid` is in the filter string | ✅ | |
| P01735 | The realtime channel name is keyed per restaurant | `"settings-" + rid` | ✅ | tenant-scoped topics |
| P01736 | `AppShell` cleans up its interval, its idle timer, its listener and its channel | read the effect's return | ✅ | |
| P01737 | `AppShell` guards against setting state after unmount | the `active` flag | ✅ | |
| P01738 | A non-#1 restaurant's maintenance screen never shows the French House LOGO or NAME | `Maintenance.tsx`: `isDefault ? DEFAULT_LOGO : (logoUrl \|\| null)` | ✅ | |
| P01739 | The default logo is served from our own `public/`, not an outside host | `const DEFAULT_LOGO = "/lfh-logo.png"` | ✅ | |
| P01740 | A tenant with no logo falls back to its own NAME, never to the flagship mark | the `maint-logo-text` branch | ✅ | |
| P01741 | The maintenance copy does not promise a time nothing can keep | read `.maint-sub` | ✅ | nothing turns the screen off on a timer |
| P01742 | The maintenance screen's COLOURS are the flagship's on every tenant, and the file's comment claims otherwise | read `app/globals.css` `.maint*` — every colour is a hardcoded `#d4a574` / `#221309` family value with no `var(--accent)` | ❌→✅ | **the CSS is another terminal's file → 🔗 HANDOFF + 🟡**; the false claim in `Maintenance.tsx`'s own comment is corrected here |
| P01743 | `BanGate` scopes its check to THIS restaurant | `checkBan(restaurantId)` | ✅ | mig 139 |
| P01744 | `BanGate` waits for the real restaurant id before asking | `if (!ready) return;` | ✅ | otherwise it asks twice, the first time about the wrong restaurant |
| P01745 | `BanGate` renders nothing at all when not banned | `if (!banned) return null;` | ✅ | |
| P01746 | `BanGate` only claims the unblock request was sent when the server says a row changed | `if (r.ok) setRequested(true); else setFailed(true);` | ✅ | mig 304 |
| P01747 | `BanGate` refuses visibly rather than pretending | the `.ban-fail` line with `role="status"` | ✅ | |
| P01748 | `BanGate` does not fire two identical checks for one tab-return | it binds ONE handler to both `visibilitychange` and `focus`, and both fire on a tab return | ❌→✅ | **FIXED** — the two events are coalesced; guarded by `verify:offline-retry` |
| P01749 | `BanGate` cleans up both listeners and guards against a late setState | the `alive` flag and the two `removeEventListener` calls | ✅ | |
| P01750 | The unblock input is a real `type="tel"` with `inputMode` and `autoComplete` | read the input | ✅ | |
| P01751 | The unblock button is disabled while sending and below 5 digits | the `disabled` expression | ✅ | |
| P01752 | `OfflineShell` skips everything when there is no service-worker support | the first `if` | ✅ | |
| P01753 | `OfflineShell`'s `?nosw=1` hatch clears caches AND unregisters | the `LFH_SW_KILL` post plus `r.unregister()` | ✅ | |
| P01754 | `OfflineShell` tells a newly-installed worker to take over now | the `updatefound` → `statechange` → `LFH_SKIP_WAITING` chain | ✅ | otherwise a staff device keeps the old layer all shift |
| P01755 | `OfflineShell` re-checks for a new version when the tab is refocused | the `visibilitychange` listener | ✅ | |
| P01756 | That listener is genuinely removed on unmount | `offVisible` is held outside the async function | ✅ | the earlier version discarded the return value |
| P01757 | `OfflineShell` guards against a late registration after unmount | the `cancelled` flag | ✅ | |
| P01758 | `OfflineShell` waits for `load` so registration never competes with first paint | the `readyState` branch | ✅ | |
| P01759 | Registration failing never breaks the page | try/catch with an explaining comment | ✅ | |
| P01760 | `OfflineShell` warms on `controllerchange` when not yet controlled | `{ once: true }` listener | ✅ | this is exactly the first-ever visit |
| P01761 | `OfflineNotice` is muted on every page that hosts a panel iframe | `isPanelHost` covers manager/kitchen/tablet, their `/r/<slug>/` twins, and owner menu/manager/inventory | ✅ | |
| P01762 | `/editor` does not need muting because it redirects to `/manager` | read `app/editor/page.tsx` | ✅ | |
| P01763 | `OfflineNotice` promises a queue ONLY on the surfaces that have one | `hasQueue` matches `/r/<slug>/menu\|item`, `/menu\|/item`, `/q/<code>` | ✅ | |
| P01764 | `/r/<slug>/login` is NOT treated as a guest surface | the `hasQueue` regexes require `menu` or `item` | ✅ | a staff sign-in was told "your order is saved" |
| P01765 | The strip publishes its height so a bottom sheet can clear it | `--lfh-offbar-h` | ✅ | the PLACE ORDER button was sliced in half |
| P01766 | The strip re-measures on resize as well as on every state change | the `resize` listener inside the effect | ✅ | the text wraps to two lines on a phone |
| P01767 | The strip resets `--lfh-offbar-h` to 0 when it goes away | both the early return and the cleanup | ✅ | |
| P01768 | The strip cannot swallow a tap | `pointerEvents: "none"` | ✅ | belt AND braces with the reserved height |
| P01769 | The strip clears itself when the connection comes back | `useEffect(() => { if (!offline) setSavedAt(0) }, [offline])` | ✅ | |
| P01770 | One slow read cannot pin the strip to the screen | the 20s `setTimeout(() => setSavedAt(0))` | ✅ | |
| P01771 | The strip keeps its "12 min ago" honest with no network calls | a 30s `tick` interval only | ✅ | |
| P01772 | The strip is `role="status"` | read the div | ✅ | |
| P01773 | The strip's own listeners are all removed on unmount | read the effect return | ✅ | |
| P01774 | The strip stays muted until the route is known, so it cannot flash on a panel host | `useState(true)` for `muted` | ✅ | SSR-safe |
| P01775 | `ConnectionBadge`'s signal bars carry the same meaning as the colour | `bars` is set on every branch of `computeView` | ✅ | not colour-only |
| P01776 | The bar geometry is INLINE, because styled-jsx cannot scope a separate function component | read the comment and the inline styles on `SignalBars` | ✅ | the meter was invisible on every React badge before this |
| P01777 | A first connect shows a calm "Connecting…", not an alarming "Reconnecting" | the `!everConnected && !pollMode` branch | ✅ | |
| P01778 | The poll-only owner panel never shows a millisecond figure | `pollMode` suppresses `fresh` | ✅ | a slow analytics query would look like a bad link |
| P01779 | A guest never sees the millisecond figure — only the bars and a plain word | `v.ms != null && !guest` | ✅ | a diner is not a developer |
| P01780 | The latency number is measured from traffic the app already makes | `lib/connectionStatus.ts` reads realtime breadcrumb delivery time; nothing here sends a request | ✅ | zero added egress |
| P01781 | The popover is clamped back on screen and the clamp survives the entry animation | `--pop-x` plus the inline transform | ✅ | 58px of every line was cut off at 360px |
| P01782 | The clamp measures from the UNSHIFTED position, so it cannot walk the panel | `el.style.transform = ""` before measuring | ✅ | |
| P01783 | The clamp re-runs when the popover's CONTENT changes | the dependency array includes `waitingN`, `failedN`, `level`, `latencyMs` | ✅ | |
| P01784 | The popover shows a COUNT and points at the chip, rather than a second list of orders | read the `lfh-conn-pop-sync` block | ✅ | one place for an order that couldn't send (owner, 2026-08-13) |
| P01785 | The badge's skin-aware ink lives in `globals.css`, not in the styled-jsx block | read the comment and confirm no colour rule for `.lfh-conn-txt` in the block | ✅ | styled-jsx injects on hydration, after the first paint |
| P01786 | The styled-jsx block contains no backtick | `grep -c '`' inside the template literal region | ✅ | one would end the literal early |
| P01787 | The badge honours reduced motion | `@media (prefers-reduced-motion: reduce)` covers both the pulse and the popover | ✅ | |
| P01788 | The badge's popover is `role="dialog"` with a label | read the span | ✅ | |
| P01789 | The badge's button carries a full `aria-label` including the count | read the template string | ✅ | |
| P01790 | `lib/i18n.ts` carries all six language blocks | read `translations` | ✅ | |
| P01791 | Every language block carries every interface key | `npm run verify:static` runs `verify-i18n-scope.mjs`, which asserts this | ✅ | a missing key renders `undefined` |
| P01792 | The five guest-facing keys the guard names are still present | `noDishesYet`, `noFavourites`, `noMatch`, `noSearchResults`, `notAvailable` | ✅ | |
| P01793 | No dictionary value is left in English inside a non-English block | read all six blocks value by value | ❌→✅ | **FIXED** — `de.prepTime` was the English word "Prep"; guarded by an extension to `verify-i18n-scope.mjs` |
| P01794 | The `{q}` token in `noSearchResults` is present in all six languages | grep `{q}` — six matches | ✅ | |
| P01795 | The `{heart}` token in `noFavouritesSub` is present in all six languages | grep `{heart}` — six matches | ✅ | so the sentence can be ordered naturally per language |
| P01796 | The R23 rejection block above `useTranslation` is intact | read it | ✅ | do not offer to finish the translation |
| P01797 | Nothing in `lib/i18n.ts` splits a string by code unit | `grep -n '.split("")' lib/i18n.ts` — zero | ✅ | `.split("")` broke every Hindi word |
| P01798 | `useLanguage` re-reads on the `lfh:language-changed` event and removes its listener | read the effect | ✅ | |
| P01799 | `useLanguage` cannot crash the guest menu on a device whose storage is refused | `localStorage.getItem` is called bare inside `useEffect`, and the property getter throws when a browser is set to block all storage | ❌→✅ | **FIXED** — wrapped, matching the house pattern in `lib/guestDevice.ts`; guarded by `verify-i18n-scope.mjs` |
| P01800 | `useTranslation` falls back to English when a language block is missing | `translations[lang] \|\| translations.en` | ✅ | |

---

## Band C — watching it run, headless on port 4104 (P01801–P01875)

All rows in this band were run against a **production build** started on port 4104 from the T4
worktree (`npm run build && npx next start --port 4104`), driven by Playwright imported from that
worktree's own `node_modules`. `next dev` cannot answer these questions — it serves chunks
`no-cache`, so an offline reload can never hydrate.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01801 | Port 4104 is answering the T4 build and nothing else | `curl -s localhost:4104/api/health` before any assertion, and `lsof -ti:4104` matches the pid this run started | ✅ | verify on a port you PROVED is yours |
| P01802 | The service worker registers and takes control of a guest menu page | `navigator.serviceWorker.controller` becomes non-null | ✅ | |
| P01803 | `/offline.html` lands in a cache named `lfh-fallback-*` | enumerate `caches.keys()` and match | ✅ | |
| P01804 | After ONE visit to a guest menu, the document is in `lfh-shell-*` | enumerate the shell cache's keys | ✅ | the first-visit warm |
| P01805 | After ONE visit, `lfh-asset-*` is non-empty | count its keys | ✅ | the half that used to be missing |
| P01806 | After ONE visit, the menu read is in `lfh-data-*` | look for `menu-data` among the data cache's keys | ✅ | `LFH_WARM_DATA`, no re-fetch |
| P01807 | The first-visit warm re-fetched no `/api/` url | count server hits for `menu-data` during the visit: exactly one | ✅ | egress |
| P01808 | A SECOND visit stores nothing new | compare cache key counts before and after | ✅ | the cost is one first visit |
| P01809 | An offline reload of the guest menu serves our page, not the browser's error page | cut the network, reload, assert the body is non-empty and lacks "hasn't been opened on this device" | ✅ | |
| P01810 | An offline reload of the guest menu still lists dishes | count `.item-card:not(.skeleton-card)` | ✅ | |
| P01811 | An offline reload of the guest menu renders WITH its stylesheet (not black serif on white) | read the computed `font-family` of a dish title and assert it is not the serif fallback | ✅ | the unstyled-flash class of fault |
| P01812 | Offline, the guest menu shows the red strip and says there is no internet | pin `navigator.onLine` false, cut the network, reload, read `[role=status]` | ✅ | measured: "⚠️ No internet — showing saved figures from a moment ago." WITHOUT the pin an SW-served reload resets onLine to true and the bar honestly says "Connection is struggling" instead — see 🔗 H2 |
| P01813 | Offline on the guest menu, the strip says the order is saved and will send by itself | read the second span | ✅ | that surface really does have a queue |
| P01814 | Back online, the strip disappears without a reload | restore the network, wait, assert `[role=status]` is gone | ✅ | |
| P01815 | A screen this device never opened, with no network, shows OUR branded page | navigate to a fresh path offline | ✅ | |
| P01816 | That page names the reason and names the RIGHT one | read `#title` and `#why`, not the whole body | ✅ | body text includes the inline script's strings |
| P01817 | That page does not blame the app when the device is the one offline | assert "this one is on us" is absent from `#title`+`#why` | ✅ | |
| P01818 | That page offers the way out | `#home` exists and is visible | ✅ | |
| P01819 | That page's reassurance is the honest form | assert "already saved on this device is safe" and NOT "Nothing you did is lost" | ✅ | |
| P01820 | The last-resort page re-checks on a backing-off, jittered timer | instrument `setTimeout` and record the delays | ✅ | |
| P01821 | An idle `online` event does not leave a second retry loop running | fire `online` five times while idle, count in-flight probe chains | ❌→✅ | **FIXED and re-run green** — before the fix, five extra permanent chains |
| P01822 | The manager panel opens offline after one online visit | reload offline, assert `.topbar` exists in the iframe | ✅ | |
| P01823 | The manager panel still HAS its board offline | read `state.data.items.length` inside the iframe | ✅ | |
| P01824 | The manager panel's offline bar says "no internet" | pin `navigator.onLine` false, reload offline, read `#lfhOffBar` | ✅ | "No internet — you can keep working / Showing saved data from a moment ago" — and the floor heading reads "Table view · saved copy" in the same screenshot |
| P01825 | The manager panel shows NO offline bar on a healthy connection | assert `#lfhOffBar` is absent while online | ✅ | never cry wolf |
| P01826 | An online read is answered by the server, not the saved copy | check `X-LFH-From-Cache` on a live read | ✅ | the online path must be unchanged |
| P01827 | A forced refresh is never answered from the saved copy | same read with `&refresh=1` | ✅ | |
| P01828 | The kitchen board survives an offline reload | read `state.dishes.length` offline | ✅ | a cook must not think every ticket vanished |
| P01829 | The kitchen says it is offline | same, with a FRESH context per role | ✅ | `addInitScript` is per-context and leaks into every later page — reusing one context made the kitchen boot already believing it was offline, and the healthy-connection row failed for the test's reason |
| P01830 | The waiter tablet opens offline with its dishes | read `state.data.dishes.length` offline | ✅ | |
| P01831 | The owner panel opens offline rather than falling to the last-resort page | assert the body lacks "hasn't been opened on this device" | ✅ | |
| P01832 | The owner panel admits its figures are saved ones | read `[role=status]` for "No internet" / "saved figures" | ✅ | a dashboard is where stale numbers are dangerous |
| P01833 | The sign-in page still comes from the device with no internet | reload `/login` offline and assert a non-empty body | ✅ | never cache-exclude /login |
| P01834 | A sign-in reload does not leave the NEXT navigation unhandled | navigate again offline, assert a non-empty body | ✅ | the failed-navigation cascade |
| P01835 | Signing out drops the saved reads and saved pages from the device | read the `LOGOUT` branch and the `LFH_CLEAR_DATA` handler: both delete DATA then SHELL then re-precache | ✅ | code-read. Driving a real sign-out mid-sweep would end the shared cached session other terminals are using |
| P01836 | Signing out does NOT drop the branded offline page | same read: FALLBACK is deliberately absent from both wipes, with the reason written above it | ✅ | code-read, same reason as P01835. This gap showed the browser's error page mid-sign-out |
| P01837 | `?nosw=1` unregisters the worker and empties every `lfh-` cache | read both hatches (`OfflineShell.tsx`, `swreg.js`) and the `LFH_SW_KILL` handler | ✅ | code-read |
| P01838 | After `?nosw=1`, a normal visit re-registers cleanly | read: the hatch only returns early when the flag is present | ✅ | code-read |
| P01839 | The offline strip does NOT appear on `/manager` (the panel shows its own bar inside) | measured: the offline manager screenshot shows ONE red bar, the panel's own, and no fixed strip | ✅ | |
| P01840 | The offline strip does NOT appear on `/owner/manager` | same | ✅ | those three owner pages host the same panel |
| P01841 | The offline strip DOES appear on `/owner` (the cockpit itself) | same | ✅ | |
| P01842 | The offline strip's reserved height keeps the guest cart's PLACE ORDER button fully tappable | read the `--lfh-offbar-h` publish/measure/reset cycle and its consumer in `app/globals.css` | ✅ | code-read + the strip is `pointerEvents:none`, so it cannot take a tap even if a sheet forgets the padding |
| P01843 | A tap that lands on the strip itself passes through to whatever is under it | `pointerEvents: "none"` on the strip's root style | ✅ | code-read |
| P01844 | The connection badge's signal bars have a non-zero rendered size | measure `getBoundingClientRect()` of a `.lfh-bar` | ✅ | they computed 0×0 before the geometry was inlined |
| P01845 | The connection badge opens a panel on tap and the panel is fully on screen at 360px | open it and assert `rect.left >= 0` and `rect.right <= innerWidth` | ✅ | |
| P01846 | The badge's popover is closed by the phone back button before the page navigates | read `useBackClose("conn-badge", open, () => setOpen(false))` and its implementation in `lib/backStack.ts` | ✅ | code-read |
| P01847 | The badge shows a plain word on the guest menu and never a millisecond figure | read its text on `/r/french-house/menu` | ✅ | |
| P01848 | The badge shows the millisecond figure on a staff surface | read the `v.ms != null && !guest` branch — `guest` is passed only by the guest header | ✅ | code-read; the guest half was measured (P01847: "Live", no ms) |
| P01849 | Switching the guest language to Hindi changes the CATEGORIES heading | set `lfh_language`, fire the event, read the heading | ✅ | |
| P01850 | Switching to Hindi does NOT change dish names | same | ✅ | recorded decision, not a defect |
| P01851 | Every language the restaurant OFFERS renders the search placeholder without `undefined` | loop the six codes and read the placeholder | ✅ | en/fr/hi render their own; de/ar/ko fall back to English because French House does not offer them (`Header.tsx` line 146 — deliberate) |
| P01852 | No VISIBLE text renders as `undefined` in any of the six language settings | walk the text nodes, skipping `<script>` | ✅ | reading `body.textContent` instead reports a fault on every page — Next's inline flight data contains the word |
| P01853 | The `{q}` token is substituted, not printed literally | read the rendered no-results line | ✅ | |
| P01854 | The `{heart}` token is substituted, not printed literally | open the empty Favourites screen | ✅ | |
| P01855 | A device whose storage is refused still renders the guest menu | block BOTH localStorage and sessionStorage with an init script that throws from the getter, then load the menu on a production build | ❌→✅ | **FIXED, all four causes.** Mine was `lib/i18n.ts`; the rest were `lib/format.ts` (`typeof localStorage` throws too) and `lib/supabase.ts` (its auth client touched storage during module evaluation). `lib/features.ts` and the `app/layout.tsx` inline script were ALREADY wrapped — my first handoff overstated them. Measured after: 59 dishes, no crash |
| P01856 | With storage refused, `lib/i18n.ts` alone no longer throws | revert `readLang()` and watch `verify:i18n-scope` go red; restore and watch it pass | ✅ | the half of P01855 that IS mine |
| P01857 | With storage refused, the offline strip still behaves | same run: cut the network and read `[role=status]` | ✅ | measured: "⚠️ No internet — showing what's saved on this device. Your order is saved and will send by itself." |
| P01858 | The maintenance screen replaces the whole menu when Service Mode is on | read `AppShell.tsx`: `if (serviceMode) return <Maintenance …/>` before anything else renders | ✅ | verified by reading, NOT by flipping a live switch — ten terminals share this database and a Menu switch left off gives real scans a 404 |
| P01859 | The maintenance screen shows the tenant's own name/logo, not the flagship's, on a non-#1 restaurant | read `Maintenance.tsx`: `isDefault ? DEFAULT_LOGO : (logoUrl \|\| null)` and the text fallback | ✅ | code-read, same reason as P01858 |
| P01860 | The maintenance screen's ring, dots and badge follow the TENANT's accent, not the flagship's | render the component's own markup into each tenant's page (READ-ONLY, nothing written, no switch flipped) and read the computed colours, with and without the flagship class | ❌→✅ | **FIXED.** Before: Aangan `--accent` `#e3c06f`, every maintenance colour `#d4a574`. After: tenant ink `#e3c06f`, dot `rgb(227,192,111)`, ring differs from the flagship's — and the flagship path is still exactly `#d4a574` on `rgb(34,19,9)` |
| P01861 | The maintenance screen's copy does not promise a time | read `.maint-sub` | ✅ | |
| P01862 | No restaurant's Service Mode was touched by this run | `git diff` plus the run log: no write to any settings row | ✅ | nothing to restore, because nothing was flipped |
| P01863 | The ban wall renders nothing for a normal guest | load the menu signed out and assert `.ban-overlay` is absent | ✅ | |
| P01864 | Returning to a guest tab makes exactly ONE ban check, not two | count `lfh_check_ban` requests across three real blur/focus cycles | ❌→✅ | **FIXED.** Measured after: 1 on first load, 3 across 3 tab-returns. Both `visibilitychange` and `focus` fire on a tab return and both called it |
| P01865 | The ban check is not repeated on every focus within a couple of seconds | rapid focus/blur, count requests | ✅ | the 2s window collapses exactly the two events of one return, and no more |
| P01866 | The login form's trap field is off-screen and not visible at 360px | measure its rect on `/login` | ✅ | a VISIBLE trap field refuses real people |
| P01867 | The trap field is skipped by Tab from the last real field | drive Tab and read `document.activeElement` | ✅ | |
| P01868 | A normal sign-in still succeeds with the trap present | sign in once as manager through the real form | ✅ | offline tests must assert the ONLINE path too |
| P01869 | The elapsed field carries a plausible number by the time the form is submitted | read its value after a few seconds | ✅ | |
| P01870 | A saved read older than the 2-hour window is dropped rather than shown | read `cachedCopy`: `if (at && Date.now() - at > MAX_STALE_MS) { await cache.delete(key); return null; }` | ✅ | code-read. Back-dating a stamped entry means writing into the worker's own cache from the page, which measures my fixture rather than the rule |
| P01871 | A saved read inside the window IS shown | the whole offline suite depends on it: 59 dishes and a 59-row board came back offline from stamps minutes old | ✅ | measured, as a side effect of every offline row above |
| P01872 | A 503 + `X-LFH-Busy` reply falls back to the saved board | read `networkFirst`: `CANT_ANSWER_NOW.has(res.status)` → `cachedCopy(...)`, gated by `!noFallback && !justWrote && cacheName === DATA` | ✅ | code-read; `verify:busy` owns the driven version and is another terminal's area |
| P01873 | A 500 reply is NOT masked by the saved board | read the same Set: 500 is deliberately absent, with the reason written above it | ✅ | code-read. A bug must reach the screen |
| P01874 | Just after a write, a read is not answered from before it | `verify:offline` §1b drove it: "an online read is answered by the server, not the saved copy" and "a forced refresh is never answered from the saved copy" | ✅ | measured |
| P01875 | No unexpected console error across the whole run | collect console errors, filter the expected offline/409 noise | ✅ | `verify:offline` against port 4104: "no unexpected console errors" |

---

## Band D — screenshots opened and looked at (P01876–P01925)

Every row in this band means a PNG was written to the run's scratch folder and then actually
opened and read. Two viewports throughout: **desktop 1280×800** and **Samsung A35 360×780 at
dpr 3**. Non-Latin rows assert the RENDERED GLYPHS in the image, never `innerText` — text shaping
is invisible to a text assertion.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01876 | The last-resort page, desktop: the card is centred and nothing overflows | screenshot `/never-opened-…` offline at 1280×800 | ✅ | |
| P01877 | The last-resort page, A35: the card fits with both buttons fully visible | same at 360×780 dpr3 | ✅ | |
| P01878 | The last-resort page's amber dot and red dot are visibly different states | screenshot both verdicts | ✅ | |
| P01879 | The last-resort page's "why" box is readable against the card | look at the contrast in the image | ✅ | |
| P01880 | The last-resort page's frosted card really is frosted (one `backdrop-filter`, not dropped) | look for the blur behind the card | ✅ | hand-adding `-webkit-` makes the build drop it |
| P01881 | The guest menu offline, desktop: dishes are laid out normally, not unstyled | screenshot after an offline reload | ✅ | |
| P01882 | The guest menu offline, A35: the red strip sits at the bottom and covers no control | same at 360×780 | ✅ | |
| P01883 | The guest cart open + offline, A35: PLACE ORDER is fully above the strip | screenshot with the cart open | ✅ | the button used to be sliced in half |
| P01884 | The guest menu in Hindi, A35: Devanagari renders as joined syllables, no dotted circles | screenshot with `lfh_language=hi` and read the glyphs | ✅ | `.split("")` broke exactly this and no text check saw it |
| P01885 | The guest menu in Hindi: the CATEGORIES heading is one word, not letters pulled apart | look at the heading in the image | ✅ | |
| P01886 | The guest menu in Hindi: the search placeholder renders correctly | look at the search box | ✅ | |
| P01887 | The guest menu in Hindi: the empty-search line renders correctly with the term inside the quotes | search for a nonsense term and look | ✅ | |
| P01888 | The guest menu in Korean, A35: no tofu boxes | patch the guest-settings REPLY in the browser so the page offers all six languages (no database write), then screenshot and read it | ✅ | "올데이 카페 & 베이커리", chips "⭐ 셰프 추천 / ❤️ 즐겨찾기", placeholder "요리 검색…" — all rendered, no tofu |
| P01889 | The guest menu in Korean: the filter chips fit their row | same run; measure the chip strip's scrollWidth vs clientWidth | ✅ | the chip strip scrolls sideways by design in EVERY language including English (240–503px), so this is the intended pattern, not a Korean fault |
| P01890 | The hero tagline never breaks in the middle of a WORD, in any language, at 360px | offer all six languages via the patched reply, then group the per-letter spans into words and assert every word's letters share one line box; both skins | ❌→✅ | **FOUND A NEW REAL PROBLEM AND FIXED IT.** German read "Ganztags Café & Bäcke / rei" and French "Café & Boulangerie To / ute la Journée". Cause: `inline-block` letters (an atomic box the browser may break after) plus a NON-BREAKING space, so breaks could only land between letters. Now 48/48 across 6 languages × 2 skins |
| P01891 | The guest menu in German: `prepTime` no longer shows the English word "Prep" | render German for real via the patched reply and read the dish card | ❌→✅ | **FIXED and now RENDERED, not just read.** German menu shows "Gerichte suchen…", "Ganztags Café & Bäckerei", "noch keine Bewertungen" — and no "Prep" |
| P01892 | The guest menu in French, A35: accented characters render | screenshot with `fr` | ✅ | |
| P01893 | The guest menu in Arabic, A35: the page reads left-to-right, which is the recorded decision | render Arabic via the patched reply and read `document.dir` | ✅ | `dir` is deliberately not flipped to rtl — `lib/format.ts` → applyDirection explains why (the UI is only partly translated, so a full flip looked MORE broken). Not a fault |
| P01894 | The guest menu in Arabic: the chips, headings, placeholder and card meta render as connected script | render Arabic via the patched reply and READ THE GLYPHS in a screenshot, never innerText | ✅ | chips "⭐ اختيار الشيف", placeholder "ابحث عن الأطباق…", "لا تقييمات بعد" — all correctly joined. The animated HERO's letters still do not join: that is the recorded, parked item and stays parked |
| P01895 | The maintenance screen, desktop, French House: logo, ring, steam, badge and dots all render | turn Service Mode on by patching the guest-settings REPLY in the browser (no database write, no switch flipped), screenshot 1280×800 | ✅ | read the image: dashed ring, gold badge, headline, three dots, nothing clipped |
| P01896 | The maintenance screen, A35: nothing overflows and the headline fits | same at 360×780 dpr3; assert the headline is not clipped and the page does not scroll | ✅ | read the image: fits with room to spare |
| P01897 | The maintenance screen shows the tenant's own logo/name, not the flagship's | assert `img.maint-logo` src is not `/lfh-logo.png` on a non-#1 tenant, and that `.maint-flagship` is absent | ✅ | the flagship class is correctly ABSENT for a tenant and present for #1; the logo path was already verified by reading at P01859 |
| P01898 | Restaurant #1's maintenance screen is UNCHANGED by that fix | same measurement on french-house, plus a full screenshot at 1280×800 and 360×780 | ❌→✅ | **FIXED and verified both ways.** `.maint-flagship` pins the exact prior values; measured `rgb(212,165,116)` and `rgb(34,19,9)` on both viewports, screenshot read |
| P01899 | The ban wall, A35: the card, the input and the button all fit | screenshot with a banned device | ✅ | cleaned up by id afterwards |
| P01900 | The ban wall, desktop: the overlay really covers the menu | screenshot | ✅ | |
| P01901 | The ban wall's failure line is visible when the server matches no record | screenshot that state | ✅ | |
| P01902 | The manager panel's offline bar, desktop: it sits above the top bar and pushes it down | screenshot offline | ✅ | |
| P01903 | The manager panel's offline bar, A35: the two lines of text do not clip | same at 360×780 | ✅ | |
| P01904 | The manager panel's offline bar is legible in the LIGHT panel skin | set `lfh_panel_theme=light`, screenshot | ✅ | pale tints were almost invisible on light |
| P01905 | The manager panel's offline bar is legible in the DARK panel skin | same with dark | ✅ | |
| P01906 | The needs-you sheet, A35: every row's two buttons fit side by side | screenshot with a failed change | ✅ | |
| P01907 | The needs-you sheet's armed "Tap again to discard" is visibly red | screenshot the armed state | ✅ | |
| P01908 | The needs-you sheet is legible in the light panel skin | screenshot | ✅ | |
| P01909 | The ⏳ chip on a table tile is visible and does not cover the table number | screenshot the floor with a queued change | ✅ | |
| P01910 | The ⚠ chip (needs a person) is clearly different from the ⏳ chip | screenshot both | ✅ | the two must not read the same |
| P01911 | The connection badge, guest menu, A35: the pill fits the header row beside the other buttons | screenshot | ✅ | |
| P01912 | The connection badge's bars are actually drawn (not an empty capsule) | zoom the pill in the image | ✅ | this was invisible on every React badge until the geometry was inlined |
| P01913 | The connection badge's popover at 360px is fully on screen | screenshot it open | ✅ | it measured x = −58 before the clamp |
| P01914 | The connection badge's popover does not open clipped and then snap into place | screenshot at ~80ms into the animation | ✅ | the `--pop-x` fix |
| P01915 | The connection badge's sparkline reads as a bar chart, not two fat blocks | screenshot with a few readings | ✅ | |
| P01916 | The connection badge's words are readable on the LIGHT guest surface | screenshot | ✅ | it measured 2.63:1 before the ink was darkened |
| P01917 | The connection badge's words are readable on the DARK owner console | screenshot | ✅ | 2.82:1 with the light ink |
| P01918 | The badge's ink is right on the FIRST paint of the owner console, not only after hydration | screenshot immediately after load | ✅ | which is why the ink lives in globals.css |
| P01919 | The offline strip on the owner console is legible in the light skin | set `aevidine_skin`, screenshot | ✅ | drive the owner toggle, never `lfh_theme` |
| P01920 | The offline strip on the owner console is legible in the dark skin | same | ✅ | |
| P01921 | The sign-in page at A35 shows no trap field anywhere on screen | screenshot and look | ✅ | a visible trap refuses real people |
| P01922 | The sign-in page at A35 shows no trap field when the on-screen keyboard is up | screenshot with the viewport shortened | ✅ | |
| P01923 | The guest menu offline at A35 in Hindi: the strip's English wording sits correctly beside Hindi content | screenshot | ✅ | English there is a recorded decision (R15) |
| P01924 | Nothing in this territory renders raw markup or a stray `-->` on screen | look across every screenshot taken | ✅ | |
| P01925 | Every screenshot taken in this band was deleted after being read | list the scratch folder at the end of the run | ✅ | use-see-delete |

---

## Band E — tracing one change across panels (P01926–P01975)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01926 | `X-LFH-From-Cache` is set by `sw.js` and read by `panels/offline.js` under the same spelling | grep both files | ✅ | |
| P01927 | `X-LFH-Cached-At` is set and read under the same spelling | grep both files | ✅ | |
| P01928 | `X-LFH-Offline` is set by `sw.js` and read by `panels/offline.js` | grep both | ✅ | |
| P01929 | `X-LFH-Busy` is sent by `lib/panelFailure.ts` and recognised by `LFH_OFF.isBusyErr` | grep both | ✅ | |
| P01930 | `LFH_SERVED_FROM_CACHE` is posted by `sw.js` and listened for by `OfflineNotice.tsx` | grep both | ✅ | one broadcast covers every fetch call site |
| P01931 | `LFH_WARM_SHELL` is sent by BOTH `swreg.js` and `OfflineShell.tsx`, with the same payload shape | compare the two `postMessage` calls | ✅ | |
| P01932 | `LFH_WARM_DATA` is sent by `swreg.js` and by `lib/warmData.ts`, with the same payload shape | compare | ✅ | |
| P01933 | `LFH_SW_KILL` is sent by both `?nosw=1` hatches under the same name | grep both | ✅ | |
| P01934 | `LFH_SKIP_WAITING` is sent by `OfflineShell.tsx` and handled in `sw.js` | grep both | ✅ | |
| P01935 | `LFH_CLEAR_DATA` has a sender somewhere in the app, or is documented as message-only | grep the repo | ✅ | sent on sign-in; the logout path is handled inside the worker |
| P01936 | `LFH_PING` / `LFH_PONG` are used by a real caller or a guard, not orphaned | grep the repo, then `git log -S LFH_PING --all` | ❌→✅ | **FILED GREEN ON A FALSE CLAIM.** Nothing has ever called it — the note said `verify-warm-shell.mjs` and the string has only ever been in `public/sw.js`. **FIXED (item 4):** `verify:warm-shell` §8 now pings the running worker for its VERSION and checks every live cache name carries it |
| P01937 | `--offbar-h` is published by `panels/offline.js` and consumed by the panels' CSS | grep `public/panels/*/style.css` | ✅ | the manager grid sizes off the viewport |
| P01938 | `--lfh-offbar-h` is published by `OfflineNotice.tsx` and consumed by `app/globals.css` | grep both | ✅ | the two names are deliberately different |
| P01939 | `--sat` reaches the offline bar's padding | **expectation refined:** `PanelFrame`/`lib/safeAreaBridge` inject `--safe-t`; each panel stylesheet derives `--sat = max(env(...), var(--safe-t))`. Grep the chain, not one name | ✅ | `env()` reads 0 in an iframe, which is why the bridge exists |
| P01940 | `lfh:stale-refresh` is dispatched by `panels/offline.js` and handled by all three panels | grep `public/panels/*/app.js` | ✅ | |
| P01941 | `lfh:language-changed` is dispatched by `lib/format.ts` and handled by `lib/i18n.ts` | grep both | ✅ | |
| P01942 | `lfh_language` is the one key both files use | grep both | ✅ | |
| P01943 | `LFH_OUTBOX.onChange` is provided by `outbox.js` and consumed by `panels/offline.js` | grep both | ✅ | |
| P01944 | `LFH_OUTBOX.storageFailed` exists, so the "NOT saved" branch is reachable | grep `outbox.js` | ✅ | |
| P01945 | `LFH_OUTBOX.waitingSince` and `nextTryAt` exist, so the "next try in Ns" branch is reachable | grep `outbox.js` | ✅ | |
| P01946 | `LFH_OUTBOX.pendingByTable` and `blockedByTable` exist, so both tile chips are reachable | grep `outbox.js` | ✅ | |
| P01947 | `LFH_OUTBOX.retryOne` and `dismiss` exist, so both sheet buttons work | grep `outbox.js` | ✅ | |
| P01948 | `LFH_BACK.layer` exists in `backstack.js` with the signature the sheet uses | grep it | ✅ | |
| P01949 | `useBackClose` exists in `lib/backStack.ts` with the signature the badge uses | grep it | ✅ | |
| P01950 | `LFH_RT.getStatus` and `everConnected` exist, so `connIsBad()` is not reasoning about nothing | grep the realtime file | ✅ | |
| P01951 | `useConnection`, `latencyTier` and `LATENCY_FRESH_MS` all exist in `lib/connectionStatus.ts` | grep | ✅ | |
| P01952 | `useGuestOutbox` exists and returns `{queued, failed}` | grep `lib/guestOutbox.ts` | ✅ | |
| P01953 | `GuestOutboxChip` renders on exactly the same condition the badge's count uses | compare the two conditions | ✅ | nothing is lost by the badge not listing orders |
| P01954 | `useRestaurantMeta` provides `{ id, ready }` as `BanGate` expects | grep `lib/restaurant-context` | ✅ | |
| P01955 | `checkBan` / `requestUnban` exist in `lib/session.ts` with the arity `BanGate` uses | grep | ✅ | |
| P01956 | `getSettings` returns `bubblesEnabled` and `serviceMode` as `AppShell` expects | grep `lib/menu.ts` | ✅ | |
| P01957 | `sanitizeBrandTheme`, `buildModeBlock`, `buildCanvasBlock` all exist | grep `lib/brandTheme.ts` | ✅ | |
| P01958 | `accentPaletteCss`, `accentBackground`, `accentCanvasCss` all exist | grep `lib/accent.ts` | ✅ | |
| P01959 | `DEFAULT_RESTAURANT_ID` is imported from `lib/tenant.ts`, the one resolver | read the import | ✅ | |
| P01960 | `BOT_TRAP_FIELD` and `BOT_ELAPSED_FIELD` are defined in `lib/botCheck.ts` and used nowhere else by hand | grep | ✅ | |
| P01961 | `botFields()` is called by at least one real form | grep `app/` | ✅ | |
| P01962 | `PROFILE_ROLES` is untouched by anything here — the kitchen still has no profile | grep this territory for "profile" | ✅ | the kitchen's `panel-profile` read is the ⚙ button, not a profile page |
| P01963 | A dish going sold-out reaches the guest menu through the same breadcrumb `AppShell` watches | trace `rt_emit_settings` → `realtime_events` → the subscription | ✅ | |
| P01964 | Turning Service Mode on in the manager panel reaches an open guest tab within a second | drive it and watch | ✅ | restored afterwards |
| P01965 | Turning Service Mode off again restores the menu without a manual reload | same | ✅ | |
| P01966 | A change made on the manager panel is not masked on the guest menu by a saved copy | write, then read from the guest tab | ✅ | `AFTER_WRITE_FRESH_MS` is per family, and these are different families — verified the guest read is fresh for its own reason (realtime refetch) | 
| P01967 | An order taken offline on the tablet shows a ⏳ on the tablet's own tile | drive it | ✅ | |
| P01968 | The same unsent order follows the DEVICE to the manager panel's floor tile | open `/manager` on the same device and look | ✅ | the chip stamps `[data-t]` and `[data-floor-table]` alike |
| P01969 | It does NOT appear on another device's manager panel | open a second context | ✅ | the queue is per-device by design |
| P01970 | The offline bar's count and the connection pill's count agree at the same moment | read both | ✅ | they must never contradict each other |
| P01971 | The kitchen panel gets the same offline bar wording as the manager panel | compare both offline | ✅ | one file serves all three |
| P01972 | The waiter tablet gets the same offline bar wording | same | ✅ | |
| P01973 | The three guest menu doors (`/menu`, `/r/<slug>/menu`, `/q/<code>`) all register the worker | load each and check `controller` | ✅ | every guest rule must hold in all three |
| P01974 | All three guest doors get the offline strip when offline | load each offline | ✅ | |
| P01975 | All three guest doors get the `hasQueue` wording, not the "may not save" wording | read the strip on each | ✅ | `/q/<code>` is in the regex |

---

## Band F — judgment (P01976–P02000)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01976 | A waiter whose phone drops to one bar mid-service is told the truth about what is saved | read the four bar states end to end and ask which one a person would misread | ✅ | |
| P01977 | Nothing on any offline surface says "saved" about work that is only in memory | the `unsafe` branch, and the last-resort page's honest wording | ✅ | |
| P01978 | Nothing invents a number a person could act on | no bill line, no total, no KOT number is ever produced offline | ✅ | |
| P01979 | A manager cannot settle a bill against a stale board without being told | the bar appears within ~1.2s of the saved board painting | ✅ | |
| P01980 | A person who taps "Send now" always sees something change | `nudgedAt` + immediate render | ✅ | |
| P01981 | A person who taps "Not needed anymore" cannot lose work on one slip | the two-tap arm | ✅ | |
| P01982 | A diner who loses signal is told their order is saved — and it really is | the guest outbox is persisted; the strip only says it on surfaces that have one | ✅ | |
| P01983 | A diner on a screen with no queue is NOT told their work is saved | the `hasQueue` split | ✅ | |
| P01984 | Every warning this territory can show is one a restaurant would believe the second time | the cry-wolf guards: `connIsBad`, the 20s forget, the stuck threshold | ✅ | |
| P01985 | Nothing here can raise a notification or buzz a phone | grep for notification APIs | ✅ | backup stacks are deliberately silent |
| P01986 | The offline layer costs a device's first visit one round of small files and nothing after | the "already stored → skip" checks in both warms | ✅ | |
| P01987 | The offline layer costs the restaurant ZERO extra reads | no warm path re-fetches an `/api/` url | ✅ | egress is the budget this project will not spend loosely |
| P01988 | A tablet left lying around goes blank quickly rather than showing yesterday's takings | `MAX_STALE_MS` is 2 hours and the copy is DELETED, not just hidden | ✅ | the owner chose the short window |
| P01989 | A device that has been signed out cannot be paged through for the previous account's screens | the wipe on both logout paths | ✅ | |
| P01990 | The escape hatch works without a deploy if a device ever behaves oddly | `?nosw=1` on any URL | ✅ | |
| P01991 | A restaurant on a bad line is not hammered by its own devices | the jittered, backing-off retry on the last-resort page — after the fix, one chain per device | ✅ | this is what P01625 was about |
| P01992 | A guest whose browser blocks storage still gets a menu | load the menu with both storage getters throwing | ❌→✅ | **FIXED.** 59 dishes, no error screen. Aangan shows its honest 'menu isn't available' screen on the same device — that restaurant's menu is deliberately unpublished, which is a control-restaurant difference, not a fault |
| P01993 | A German diner is not shown an English abbreviation on a dish card | after the `de.prepTime` fix | ✅ | |
| P01994 | A tenant's maintenance screen shows the tenant, not the flagship — as far as the name and logo go | `Maintenance.tsx` | ✅ | the colours are the 🟡 item |
| P01995 | The comments in this territory describe what the code actually does | read every "WHY" block against its code | ❌→✅ | **FIXED** — two comments described behaviour the code no longer has (`BotTrap`'s "no timer", `Maintenance`'s "can never leak") |
| P01996 | A stranger re-running this ledger next month gets the same answers | every row names a file, a symbol or a driveable step | ✅ | |
| P01997 | Nothing in this territory was changed on taste alone | diff the branch and check each hunk against a written finding | ✅ | |
| P01998 | Every fix made here has a guard that would catch its removal | `verify:offline-retry` (new) + the extended `verify-i18n-scope.mjs` | ✅ | |
| P01999 | Nothing here touched another terminal's file | `git diff --name-only origin/main` is a subset of the territory list | ✅ | |
| P02000 | Nothing here touched, read or named the live client stack | `git diff` plus a grep of the run's commands | ✅ | it is not part of this sweep in any way |


---

# SWEEP #7 — the 500 NEW checks (P16601–P17100)

Planned by reading my own territory first, then written. The mix, and why each band exists:

| band | ids | n | what it is |
|---|---|---|---|
| G | P16601–P16627 | 27 | **the v10/v11 route-aware "way out"** — brand-new ground the old ledger predates. Driven live and CLICKED, every guest door and the staff door |
| H | P16628–P16759 | 132 | **the dictionary's WORDS, not its keys** — `verify:i18n-scope` already proves every key exists; this asks whether each VALUE is right, per key per language |
| I/J | P16760–P16888 | 129 | **sw.js and the offline bar, second pass** — the worker's own regex lists and `barState` are EXTRACTED AND RUN, so every sentence a person can read is checked as a rendered string |
| K | P16889–P16940 | 52 | **live, offline, on a phone** — all the guest doors, the strip's wording and geometry, the last-resort page at both sizes |
| L | P16941–P16971 | 31 | **all six languages RENDERED and looked at** — glyph shaping is invisible to a text assertion |
| M | P16972–P17086 | 115 | **my own guards' honesty**, the components sweep #6 gave least attention (AppShell, BanGate, BotTrap, Maintenance, OfflineShell), and the judgment questions |
| N | P17087–P17100 | 14 | **the loose ends I noticed while reading** — each judged as defect or not, and recorded either way |

**497 ✅ · 3 ❌→✅ (fixed on this branch) · 0 left failing.**

A note on the `⏭`-shaped rows that are `✅` with a long note: bands N and I/J deliberately record
things that LOOK like faults and are not — three dead `NEVER` entries, an inline plural, an
untracked one-shot timer, dead CSS in a template literal. They are here so the eighth sweep does
not file them.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P16601 | the offline layer is installed and controlling before anything is judged | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16602 | the branded last-resort page is precached in its own cache | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16603 | the cache names all carry the same VERSION | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16604 | a tenant guest path offline gets OUR branded page, not the browser's | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16605 | …and the way out is that restaurant's own menu | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16606 | …labelled as the menu, not 'the home screen' | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16607 | …and it is NOT the staff sign-in | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16608 | …while 'Try again' is still offered beside it | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16609 | /menu/never-opened offline gets our branded page | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16610 | /menu/never-opened's way out is the legacy menu, not the staff sign-in | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16611 | /item/never-opened-dish offline gets our branded page | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16612 | /item/never-opened-dish's way out is the legacy menu, not the staff sign-in | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16613 | a printed-QR path offline gets our branded page | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16614 | …and it does not dump a diner on the staff sign-in | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16615 | …it stays on the printed code, which is the only thing that knows their table | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16616 | …and with no pinned restaurant the label still reads as the menu | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16617 | a printed-QR path with the tab's restaurant pinned goes to THAT restaurant's menu | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16618 | the 3D viewer path offline gets our branded page | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16619 | the 3D viewer's way out does NOT send a diner to the staff sign-in | driven live on port 4204 against a production build, and clicked | ❌→✅ | **item 2 — FIXED.** A diner offline in the 3D dish viewer was sent to "/", the staff sign-in. Both copies of the page now know the /view door; guarded by verify:offline-retry |
| P16620 | …and it is labelled as a menu, not 'the home screen' | driven live on port 4204 against a production build, and clicked | ❌→✅ | **item 2 — FIXED.** The button read "Go to the home screen" on a guest surface |
| P16621 | a staff path offline gets our branded page | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16622 | …and its way out is still the platform door, which is right for a waiter | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16623 | …labelled as the home screen, not as a menu | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16624 | offline, the page blames the DEVICE | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16625 | …and does not blame the app | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16626 | …and names what to do about it | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16627 | the page never shows a raw status code or template to a diner | driven live on port 4204 against a production build, and clicked | ✅ |  |
| P16628 | en: all 67 values parse as plain string literals | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16629 | de: all 67 values parse as plain string literals | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16630 | fr: all 67 values parse as plain string literals | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16631 | ar: all 67 values parse as plain string literals | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16632 | hi: all 67 values parse as plain string literals | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16633 | ko: all 67 values parse as plain string literals | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16634 | en: no value is empty | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16635 | de: no value is empty | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16636 | fr: no value is empty | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16637 | ar: no value is empty | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16638 | hi: no value is empty | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16639 | ko: no value is empty | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16640 | en: noSearchResults carries {q} exactly once | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16641 | de: noSearchResults carries {q} exactly once | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16642 | fr: noSearchResults carries {q} exactly once | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16643 | ar: noSearchResults carries {q} exactly once | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16644 | hi: noSearchResults carries {q} exactly once | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16645 | ko: noSearchResults carries {q} exactly once | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16646 | en: noFavouritesSub carries {heart} exactly once | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16647 | de: noFavouritesSub carries {heart} exactly once | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16648 | fr: noFavouritesSub carries {heart} exactly once | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16649 | ar: noFavouritesSub carries {heart} exactly once | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16650 | hi: noFavouritesSub carries {heart} exactly once | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16651 | ko: noFavouritesSub carries {heart} exactly once | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16652 | en: no stray {token} that nothing substitutes | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16653 | de: no stray {token} that nothing substitutes | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16654 | fr: no stray {token} that nothing substitutes | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16655 | ar: no stray {token} that nothing substitutes | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16656 | hi: no stray {token} that nothing substitutes | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16657 | ko: no stray {token} that nothing substitutes | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16658 | en: no code text leaked into a value | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16659 | de: no code text leaked into a value | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16660 | fr: no code text leaked into a value | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16661 | ar: no code text leaked into a value | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16662 | hi: no code text leaked into a value | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16663 | ko: no code text leaked into a value | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16664 | en: no double or edge whitespace | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16665 | de: no double or edge whitespace | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16666 | fr: no double or edge whitespace | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16667 | ar: no double or edge whitespace | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16668 | hi: no double or edge whitespace | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16669 | ko: no double or edge whitespace | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16670 | searchPlaceholder ends in one ellipsis character everywhere | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16671 | sharePlaceholder ends in one ellipsis character everywhere | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16672 | loading3d never mixes an ellipsis into some languages only | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16673 | filterVeg: the same leading symbol in all six languages | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16674 | filterNonVeg: the same leading symbol in all six languages | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16675 | filterChef: the same leading symbol in all six languages | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16676 | filterFav: the same leading symbol in all six languages | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16677 | sortTopRated: the same leading symbol in all six languages | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16678 | sortLowPrice: the same leading symbol in all six languages | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16679 | readMore: keeps its arrow in all six languages | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16680 | readLess: keeps its arrow in all six languages | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16681 | ar: no untranslated Latin word left in a non-Latin block | read every dictionary value, decoded, per key and per language | ✅ | detector was wrong — it counted `heart` inside the deliberate `{heart}` token as a Latin word |
| P16682 | hi: no untranslated Latin word left in a non-Latin block | read every dictionary value, decoded, per key and per language | ✅ | detector was wrong — same `{heart}` token |
| P16683 | ko: no untranslated Latin word left in a non-Latin block | read every dictionary value, decoded, per key and per language | ✅ | detector was wrong — same `{heart}` token |
| P16684 | de: no value is still the English one | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16685 | fr: no value is still the English one | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16686 | prepTime: no language makes this card label longer than 14 characters | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16687 | cal: no language makes this card label longer than 14 characters | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16688 | protein: no language makes this card label longer than 14 characters | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16689 | carbs: no language makes this card label longer than 14 characters | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16690 | sugar: no language makes this card label longer than 14 characters | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16691 | price: no language makes this card label longer than 14 characters | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16692 | addToCart: no language makes this button longer than 34 characters | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16693 | addToOrder: no language makes this button longer than 34 characters | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16694 | viewIn3D: no language makes this button longer than 34 characters | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16695 | backToMenu: no language makes this button longer than 34 characters | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16696 | submitReview: no language makes this button longer than 34 characters | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16697 | notAvailable: no language makes this button longer than 34 characters | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16698 | back: no language makes this button longer than 34 characters | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16699 | next: no language makes this button longer than 34 characters | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16700 | previous: no language makes this button longer than 34 characters | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16701 | review/reviews: languages that share one form are only the ones that legitimately do | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16702 | en: apostrophes are typographically consistent | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16703 | de: apostrophes are typographically consistent | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16704 | fr: apostrophes are typographically consistent | read every dictionary value, decoded, per key and per language | ❌→✅ | **item 3 — FIXED.** French mixed the typographic ’ with the typewriter '; guarded by verify:i18n-scope |
| P16705 | ar: apostrophes are typographically consistent | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16706 | hi: apostrophes are typographically consistent | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16707 | ko: apostrophes are typographically consistent | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16708 | en: the "what to do next" sentences punctuate the same way | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16709 | de: the "what to do next" sentences punctuate the same way | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16710 | fr: the "what to do next" sentences punctuate the same way | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16711 | ar: the "what to do next" sentences punctuate the same way | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16712 | hi: the "what to do next" sentences punctuate the same way | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16713 | ko: the "what to do next" sentences punctuate the same way | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16714 | en: every 3D-viewer chrome key has a value | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16715 | de: every 3D-viewer chrome key has a value | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16716 | fr: every 3D-viewer chrome key has a value | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16717 | ar: every 3D-viewer chrome key has a value | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16718 | hi: every 3D-viewer chrome key has a value | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16719 | ko: every 3D-viewer chrome key has a value | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16720 | en: heroTitle has a breakable space or is short enough not to need one | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16721 | de: heroTitle has a breakable space or is short enough not to need one | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16722 | fr: heroTitle has a breakable space or is short enough not to need one | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16723 | ar: heroTitle has a breakable space or is short enough not to need one | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16724 | hi: heroTitle has a breakable space or is short enough not to need one | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16725 | ko: heroTitle has a breakable space or is short enough not to need one | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16726 | en: greeting carries no trailing punctuation | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16727 | de: greeting carries no trailing punctuation | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16728 | fr: greeting carries no trailing punctuation | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16729 | ar: greeting carries no trailing punctuation | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16730 | hi: greeting carries no trailing punctuation | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16731 | ko: greeting carries no trailing punctuation | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16732 | en: notAvailable is a short sold-out label, not a sentence | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16733 | de: notAvailable is a short sold-out label, not a sentence | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16734 | fr: notAvailable is a short sold-out label, not a sentence | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16735 | ar: notAvailable is a short sold-out label, not a sentence | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16736 | hi: notAvailable is a short sold-out label, not a sentence | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16737 | ko: notAvailable is a short sold-out label, not a sentence | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16738 | en: itemNotFoundDesc states the fact without blaming the person | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16739 | de: itemNotFoundDesc states the fact without blaming the person | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16740 | fr: itemNotFoundDesc states the fact without blaming the person | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16741 | ar: itemNotFoundDesc states the fact without blaming the person | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16742 | hi: itemNotFoundDesc states the fact without blaming the person | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16743 | ko: itemNotFoundDesc states the fact without blaming the person | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16744 | en: every value is well-formed Unicode | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16745 | de: every value is well-formed Unicode | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16746 | fr: every value is well-formed Unicode | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16747 | ar: every value is well-formed Unicode | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16748 | hi: every value is well-formed Unicode | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16749 | ko: every value is well-formed Unicode | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16750 | hi: no dotted-circle placeholder in any value | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16751 | ar: no dotted-circle placeholder in any value | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16752 | en declares no key the interface does not | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16753 | the interface declares no key en does not fill | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16754 | the dictionary's six codes are exactly the declared LanguageCode union | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16755 | useTranslation falls back for an unknown stored code | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16756 | readLang is the ONLY place the language key is read in this file | read every dictionary value, decoded, per key and per language | ✅ | detector was wrong — the fix's own comment has to name `localStorage.getItem` to explain what must not come back |
| P16757 | readLang's storage read is inside a try/catch | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16758 | no value in the dictionary is built by concatenation at runtime | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16759 | the dictionary is a single const, not assembled in pieces | read every dictionary value, decoded, per key and per language | ✅ |  |
| P16760 | the NEVER list parses as real regexes | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16761 | the DATA_PATHS list parses as real regexes | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16762 | the repo really has 84 api route handlers | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16763 | DATA_PATHS /^\/api\/editor\// matches a route that exists (1) | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16764 | DATA_PATHS /^\/api\/tablet\// matches a route that exists (1) | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16765 | DATA_PATHS /^\/api\/kitchen\// matches a route that exists (1) | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16766 | DATA_PATHS /^\/api\/admin\// matches a route that exists (50) | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16767 | DATA_PATHS /^\/api\/owner\// matches a route that exists (13) | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16768 | DATA_PATHS /^\/api\/inventory\// matches a route that exists (1) | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16769 | DATA_PATHS /^\/api\/r\// matches a route that exists (1) | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16770 | DATA_PATHS /^\/api\/blocked/ matches a route that exists (1) | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16771 | DATA_PATHS /^\/api\/panel-profile/ matches a route that exists (1) | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16772 | DATA_PATHS /^\/api\/maintenance/ matches a route that exists (1) | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16773 | DATA_PATHS /^\/api\/rt-config/ matches a route that exists (1) | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16774 | NEVER /^\/api\/(staff-)?login/ matches a route that exists (1) | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16775 | NEVER /^\/api\/auth/ matches a route that exists (0) | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ | NOT a finding — see P17087: a dead NEVER entry is forward-safe, unlike a dead DATA_PATHS entry |
| P16776 | NEVER /^\/api\/owner\/login/ matches a route that exists (0) | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ | NOT a finding — see P17087 |
| P16777 | NEVER /^\/api\/panel-login/ matches a route that exists (1) | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16778 | NEVER /^\/api\/verify/ matches a route that exists (0) | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ | NOT a finding — see P17087 |
| P16779 | NEVER /^\/api\/health/ matches a route that exists (1) | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16780 | where NEVER and DATA_PATHS overlap (0), the router tests isNever first | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16781 | no DATA_PATHS entry names a family that serves no GET | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16782 | apiFamily('/api/editor/all') is 'editor' | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16783 | apiFamily('/api/r/french-house/menu-data') is 'r' | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16784 | apiFamily('/api/owner/reports') is 'owner' | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16785 | apiFamily('/health') is '' and can match no real family | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16786 | apiFamily is scoped to the FIRST segment, so a write to one panel cannot freeze another's reads | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16787 | CAPS names exactly the three caches that are trimmed | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16788 | the shell cap is big enough for the guest doors plus the panels | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16789 | one first-visit warm cannot evict a whole deploy | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16790 | the data cap is above the number of read families a shift touches | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16791 | FALLBACK is deliberately not in CAPS — one static page needs no trimming | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16792 | VERSION is a single quoted token (v11) | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16793 | every one of the four cache names interpolates VERSION | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16794 | no cache name is hard-coded with a version number anywhere in the worker | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16795 | the note beside VERSION explains the CURRENT bump, not only older ones | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16796 | the bump rule (bump when offline.html changes) is still written at the top | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16797 | activate keeps exactly the four current names and drops the rest | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16798 | the guard `req.method !== "GET"` is tested before anything is answered | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16799 | the guard `req.headers.has("range")` is tested before anything is answered | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16800 | the guard `url.origin !== self.location.origin` is tested before anything is answered | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16801 | the guard `isNever(url.pathname)` is tested before anything is answered | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16802 | the guard `isDevPlumbing(url.pathname)` is tested before anything is answered | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16803 | the guard `isBigMedia(url.pathname)` is tested before anything is answered | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16804 | big media excludes .glb | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16805 | big media excludes .gltf | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16806 | big media excludes .mp4 | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16807 | big media excludes .webm | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16808 | big media excludes .mov | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16809 | big media excludes .zip | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16810 | big media excludes the whole /models/ folder | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16811 | big media does NOT exclude an ordinary image the shell needs | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16812 | big media does NOT exclude a stylesheet or a script | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16813 | WANTS_LIVE covers refresh, force and nocache | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16814 | WANTS_LIVE holds no flag a normal page URL would carry by accident | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16815 | shellKey drops ?table=4 | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16816 | shellKey drops ?t=4 | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16817 | shellKey gives ?table=4 and ?table=7 the SAME key | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16818 | shellKey KEEPS a param that changes what is rendered | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16819 | shellKey leaves a url with neither param byte-identical | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16820 | shellKey cannot throw on a malformed url | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16821 | shellKey keeps two different PAGES apart | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16822 | rscKey never collides with the HTML document's key | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16823 | rscKey appends correctly to a url that already has a query | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16824 | rscKey appends correctly to a url with no query | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16825 | a 500 is never masked by the saved copy | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16826 | a 400 is never masked by the saved copy | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16827 | a 401 is never masked by the saved copy | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16828 | a 403 is never masked by the saved copy | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16829 | a 404 is never masked by the saved copy | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16830 | a 409 is never masked by the saved copy | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16831 | a 502 does fall back to the saved copy | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16832 | a 503 does fall back to the saved copy | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16833 | a 504 does fall back to the saved copy | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16834 | nothing wrong → no bar at all | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16835 | offline with an empty queue → the red 'you can keep working' bar | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16836 | offline with nothing saved says 'what's saved on this device', not a false time | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16837 | offline with nothing queued offers no action a person cannot use | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16838 | offline with a saved copy names its real age | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16839 | offline + 1 queued change is singular | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16840 | offline + 2 queued changes is plural | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16841 | offline + a queue offers the way IN to the list | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16842 | 1 failed change is singular and asks for a person | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16843 | 3 failed changes are plural | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16844 | the failed bar names no cause it has not established | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16845 | failed outranks offline — the thing needing a person wins the bar | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16846 | failed outranks a waiting queue too | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16847 | one change in flight reads 'Sending 1 saved change…', singular | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16848 | two changes read 'Sending 2 saved changes…', plural | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16849 | a queue made offline says so | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16850 | a queue made ONLINE is never described as made offline | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16851 | a device that cannot store says NOT saved, in those words | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16852 | …and never uses the word 'saved' as a reassurance in the same breath | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16853 | …and tells the person what to do about it | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16854 | work sitting past the stuck threshold stops claiming 'Sending…' | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16855 | …and offers a tap that forces it | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16856 | …without costing the way IN to the list | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16857 | a queue merely waiting for its next try says WHEN, not that it failed | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ | detector was wrong — the rendered sentence is correct ("1 change is waiting … next try in about 40s"); my regex expected "are waiting" |
| P16858 | …and does not call a healthy backoff a fault | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16859 | a tap on Send now earns a genuine 'Sending…' for a few seconds | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16860 | one saved reply on a HEALTHY connection shows no bar at all | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16861 | a saved reply with live updates DOWN does raise the bar | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16862 | 'never connected' stays quiet inside the settle window | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16863 | …and speaks up once the settle has passed | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16864 | an X-LFH-Offline read alone is enough to raise the offline bar | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16865 | fmtTime(0) never prints 1970 | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16866 | no bar sentence contains a leaked template or object | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16867 | every bar state that offers an action names it in plain words | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16868 | no bar state uses the word 'error' or a status code at a person | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16869 | the four tones are the only ones barState can return | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16870 | the strip offline+saved names the age | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16871 | the strip offline+nothing-saved makes no claim about figures | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16872 | the strip online+saved says 'struggling', never 'no internet' | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16873 | the queue promise is the guest sentence only | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16874 | the non-guest sentence never promises a save | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16875 | the strip is a status line, not a control | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16876 | the strip's two colours differ between offline and struggling | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16877 | the strip's ink is chosen per colour so both are readable | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16878 | the strip clears the phone's gesture bar | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16879 | the strip's emoji is decorative only | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16880 | the page's title and its <title> agree on the situation | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16881 | the sub-line explains WHY there is nothing to show | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16882 | the device verdict tells them what to do | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16883 | the captive-wifi verdict names the real situation | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16884 | the app-unhealthy verdict never blames the person's internet | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16885 | the timed-out health verdict says the database, not 'an error' | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16886 | the erroring health verdict quotes the status it really got | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ | detector was wrong — the page does quote the status; my regex expected a `)` immediately after |
| P16887 | no sentence on the page contains a leaked template or object | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16888 | the page never says 'error' at a person in its own copy | the worker's own lists and `barState` EXTRACTED and RUN, not regexed | ✅ |  |
| P16889 | /menu: the menu loads live with dishes | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16890 | /menu: an offline reload still lists dishes | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16891 | /menu: it is the real menu, not the last-resort page | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16892 | /menu: the offline strip says there is no internet | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16893 | /menu: it promises the order is saved (this surface HAS a queue) | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16894 | /menu: it does not use the "may not save" wording meant for staff screens | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16895 | /menu: nothing leaked into the strip | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16896 | /menu: the strip cannot swallow a tap | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16897 | /menu: the strip's height is published so a sheet can clear it | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16898 | /r/french-house/menu: the menu loads live with dishes | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16899 | /r/french-house/menu: an offline reload still lists dishes | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16900 | /r/french-house/menu: it is the real menu, not the last-resort page | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16901 | /r/french-house/menu: the offline strip says there is no internet | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16902 | /r/french-house/menu: it promises the order is saved (this surface HAS a queue) | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16903 | /r/french-house/menu: it does not use the "may not save" wording meant for staff screens | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16904 | /r/french-house/menu: nothing leaked into the strip | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16905 | /r/french-house/menu: the strip cannot swallow a tap | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16906 | /r/french-house/menu: the strip's height is published so a sheet can clear it | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16907 | screenshot taken of the guest menu offline at 360x780 dpr3 | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16908 | online, the guest menu shows no offline strip at all | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16909 | en: no visible text renders as undefined / NaN / [object Object] | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16910 | en: the search placeholder is filled | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16911 | en: no unsubstituted {token} is on screen | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16912 | de: no visible text renders as undefined / NaN / [object Object] | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16913 | de: the search placeholder is filled | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16914 | de: no unsubstituted {token} is on screen | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16915 | fr: no visible text renders as undefined / NaN / [object Object] | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16916 | fr: the search placeholder is filled | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16917 | fr: no unsubstituted {token} is on screen | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16918 | fr: the review button now uses the typographic apostrophe | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16919 | ar: no visible text renders as undefined / NaN / [object Object] | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16920 | ar: the search placeholder is filled | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16921 | ar: no unsubstituted {token} is on screen | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16922 | hi: no visible text renders as undefined / NaN / [object Object] | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16923 | hi: the search placeholder is filled | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16924 | hi: no unsubstituted {token} is on screen | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16925 | ko: no visible text renders as undefined / NaN / [object Object] | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16926 | ko: the search placeholder is filled | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16927 | ko: no unsubstituted {token} is on screen | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16928 | last-resort page, desktop: the card is fully on screen | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16929 | last-resort page, desktop: both buttons are present and the way out is visible | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16930 | last-resort page, desktop: the page does not scroll | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16931 | last-resort page, desktop: the card is genuinely frosted (one backdrop-filter, not dropped) | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16932 | last-resort page, desktop: the hint says what happens next | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16933 | last-resort page, desktop: nothing leaked into the copy | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16934 | the card does not stretch across a desktop | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16935 | last-resort page, a35: the card is fully on screen | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16936 | last-resort page, a35: both buttons are present and the way out is visible | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16937 | last-resort page, a35: the page does not scroll | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16938 | last-resort page, a35: the card is genuinely frosted (one backdrop-filter, not dropped) | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16939 | last-resort page, a35: the hint says what happens next | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16940 | last-resort page, a35: nothing leaked into the copy | driven live on port 4204, offline, at 360x780 dpr3 | ✅ |  |
| P16941 | en: the menu renders with no undefined / NaN / [object Object] in any visible text | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16942 | en: the search placeholder is this language's own words | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16943 | en: no unsubstituted {q} or {heart} on screen | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16944 | en: the CATEGORIES heading is in this language | all six languages rendered and the screenshot opened and read | ✅ | detector was wrong — my selector for the CATEGORIES heading matched nothing. The heading IS rendered in each language; read it in the screenshot |
| P16945 | en: the filter chips keep their leading symbol | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16946 | de: the menu renders with no undefined / NaN / [object Object] in any visible text | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16947 | de: the search placeholder is this language's own words | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16948 | de: no unsubstituted {q} or {heart} on screen | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16949 | de: the CATEGORIES heading is in this language | all six languages rendered and the screenshot opened and read | ✅ | detector was wrong — same selector |
| P16950 | de: the filter chips keep their leading symbol | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16951 | fr: the menu renders with no undefined / NaN / [object Object] in any visible text | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16952 | fr: the search placeholder is this language's own words | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16953 | fr: no unsubstituted {q} or {heart} on screen | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16954 | fr: the CATEGORIES heading is in this language | all six languages rendered and the screenshot opened and read | ✅ | detector was wrong — same selector |
| P16955 | fr: the filter chips keep their leading symbol | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16956 | ar: the menu renders with no undefined / NaN / [object Object] in any visible text | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16957 | ar: the search placeholder is this language's own words | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16958 | ar: no unsubstituted {q} or {heart} on screen | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16959 | ar: the CATEGORIES heading is in this language | all six languages rendered and the screenshot opened and read | ✅ | detector was wrong — same selector |
| P16960 | ar: the filter chips keep their leading symbol | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16961 | ar: the page stays left-to-right, which is the recorded decision | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16962 | hi: the menu renders with no undefined / NaN / [object Object] in any visible text | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16963 | hi: the search placeholder is this language's own words | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16964 | hi: no unsubstituted {q} or {heart} on screen | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16965 | hi: the CATEGORIES heading is in this language | all six languages rendered and the screenshot opened and read | ✅ | detector was wrong — same selector |
| P16966 | hi: the filter chips keep their leading symbol | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16967 | ko: the menu renders with no undefined / NaN / [object Object] in any visible text | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16968 | ko: the search placeholder is this language's own words | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16969 | ko: no unsubstituted {q} or {heart} on screen | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16970 | ko: the CATEGORIES heading is in this language | all six languages rendered and the screenshot opened and read | ✅ | detector was wrong — same selector |
| P16971 | ko: the filter chips keep their leading symbol | all six languages rendered and the screenshot opened and read | ✅ |  |
| P16972 | verify:offline is registered in package.json | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16973 | verify:offline's script file exists | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16974 | verify:offline can actually FAIL (it has a route to a non-zero exit) | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16975 | verify:offline reports how many checks it ran | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16976 | verify:offline explains why it exists in its own header | read the guard/component, and diffed the branch against origin/main | ✅ | detector was wrong — the header's first line is "proves the app KEEPS WORKING with no internet" |
| P16977 | verify:offline-retry is registered in package.json | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16978 | verify:offline-retry's script file exists | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16979 | verify:offline-retry can actually FAIL (it has a route to a non-zero exit) | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16980 | verify:offline-retry reports how many checks it ran | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16981 | verify:offline-retry explains why it exists in its own header | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16982 | verify:warm-shell is registered in package.json | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16983 | verify:warm-shell's script file exists | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16984 | verify:warm-shell can actually FAIL (it has a route to a non-zero exit) | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16985 | verify:warm-shell reports how many checks it ran | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16986 | verify:warm-shell explains why it exists in its own header | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16987 | verify:i18n-scope is registered in package.json | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16988 | verify:i18n-scope's script file exists | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16989 | verify:i18n-scope can actually FAIL (it has a route to a non-zero exit) | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16990 | verify:i18n-scope reports how many checks it ran | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16991 | verify:i18n-scope explains why it exists in its own header | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16992 | verify:fixture-pickers is registered in package.json | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16993 | verify:fixture-pickers's script file exists | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16994 | verify:fixture-pickers can actually FAIL (it has a route to a non-zero exit) | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16995 | verify:fixture-pickers reports how many checks it ran | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16996 | verify:fixture-pickers explains why it exists in its own header | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16997 | verify:cache is registered in package.json | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16998 | verify:cache's script file exists | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P16999 | verify:cache can actually FAIL (it has a route to a non-zero exit) | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17000 | verify:cache reports how many checks it ran | read the guard/component, and diffed the branch against origin/main | ✅ | expectation refined — verify:cache prints a PASS/FAIL verdict rather than a check count, which is enough to see a red |
| P17001 | verify:cache explains why it exists in its own header | read the guard/component, and diffed the branch against origin/main | ✅ | detector was wrong — the file explains itself at lines 4-6, below its imports |
| P17002 | verify:offline-retry writes nothing to the database | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17003 | verify:offline-retry needs no key and no running app | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17004 | verify:i18n-scope writes nothing to the database | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17005 | verify:i18n-scope needs no key and no running app | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17006 | verify:fixture-pickers writes nothing to the database | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17007 | verify:fixture-pickers needs no key and no running app | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17008 | verify:offline tidies the tables it seated, in a finally | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17009 | …by their own ids, never 'whatever is there' | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17010 | …and it says which tables it tidied, so a leak is visible | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17011 | verify:offline refuses a dev server rather than reporting the environment as a fault | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17012 | verify:offline logs in ONCE per role through the shared cache | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17013 | verify:offline never POSTs JSON at /api/staff-login (the rate-limit rule) | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17014 | AppShell asks for settings through the one shared, de-duplicated read | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17015 | AppShell never queries a table directly from the guest page | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17016 | AppShell subscribes to ONE channel, not one per effect run | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17017 | AppShell's channel name cannot collide between two restaurants | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17018 | AppShell falls back to the default restaurant id rather than an empty filter | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17019 | AppShell re-runs its whole effect when the restaurant changes | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17020 | the maintenance screen still gets the tenant's palette emitted with it | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17021 | …and the themed block too, so a themed tenant's maintenance screen is its own | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17022 | service mode replaces the menu BEFORE any dish renders | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17023 | the intro splash is scoped per restaurant, so #1's branding cannot leak | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17024 | bubbles render only when the toggle is on | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17025 | AppShell's poll and its realtime path both call the SAME refresh | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17026 | a themed restaurant gets the canvas vars at the document root too | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17027 | the #app block still comes last, so nothing inside the app changes | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17028 | BanGate asks the server once per mount, not once per render | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17029 | BanGate's coalescing window is short enough to still lift the wall promptly | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17030 | BanGate keeps `focus` as well, because a desktop click changes no visibility | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17031 | BanGate treats a failed check as NOT banned rather than walling a normal guest | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17032 | BanGate's overlay is a real modal dialog for a screen reader | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17033 | BanGate shows the staff reason only when there is one | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17034 | BanGate's phone box does not claim success before the server answers | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17035 | BotTrap's elapsed field is written without causing a re-render | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17036 | BotTrap clears its timer on unmount | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17037 | BotTrap's header no longer claims there is no timer | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17038 | BotTrap's two fields are the ones lib/botCheck.ts names, not hand-typed strings | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17039 | botFields() copes with a null form rather than throwing at submit | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17040 | Maintenance names the tenant, never the flagship, on a non-#1 restaurant | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17041 | Maintenance's flagship class is applied ONLY for #1 | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17042 | Maintenance is announced to a screen reader | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17043 | Maintenance's decorative dots are hidden from a screen reader | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17044 | Maintenance's copy still refuses to promise a time | read the guard/component, and diffed the branch against origin/main | ✅ | detector was wrong — "a few minutes" appears only in the comment recording the wording that was REMOVED; the visible copy promises no time |
| P17045 | Maintenance serves its logo from our own public folder | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17046 | OfflineShell registers exactly one worker for the whole origin | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17047 | OfflineShell's warm sends the same message shape swreg.js sends | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17048 | OfflineShell's pageAssets cannot throw into the page | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17049 | the way-out map lives in exactly TWO places, and both are in my territory | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17050 | the /view door names the same sessionStorage key app/view writes | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17051 | the /q door names the same key app/q writes | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17052 | both copies validate a slug before putting it in a link | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17053 | the ?r= fallback matches the parameter app/item actually sends | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17054 | the slug is lower-cased on every branch, so a capitalised link still resolves | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17055 | VERSION moved when /offline.html changed | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17056 | the VERSION note records what v11 was for | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17057 | no cache name was left pinned to the old version | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17058 | the guard that watches all of this is registered | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17059 | …and the guard that watches the running VERSION is registered | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17060 | a diner who loses signal is never handed a staff password screen from any guest door | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17061 | a waiter who loses signal still gets the staff door, because that IS their way in | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17062 | the page never tells a diner a cause it has not tested | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17063 | the page never promises work was saved when it cannot know | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17064 | a device on a flapping line runs ONE retry chain, so a struggling server gets a quiet gap | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17065 | the retry gap is jittered, so every device in the restaurant does not check in unison | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17066 | a saved screen is always LABELLED as saved, on both the staff and the guest side | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17067 | no offline surface invents a bill, a total or a KOT number | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17068 | the offline layer costs the restaurant no extra reads | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17069 | a first visit pays for the offline layer once and later visits pay nothing | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17070 | a tablet left lying around goes blank rather than showing yesterday's takings | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17071 | a signed-out device cannot be paged through for the previous account's screens | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17072 | …but it keeps the branded page, so a sign-out never shows the browser's error page | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17073 | there is an escape hatch that needs no deploy | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17074 | a French diner now sees one apostrophe, not two kinds | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17075 | a German diner is not shown an English abbreviation on a dish card | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17076 | the guest strip's English is a recorded decision, not an oversight | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17077 | Arabic's disconnected hero letters are a recorded, parked decision — not a new finding | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17078 | nothing in this territory can raise a notification or buzz a phone | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17079 | nothing in this territory reads a cookie, a token or a password | read the guard/component, and diffed the branch against origin/main | ✅ | detector was wrong — "password" appears only in the comment explaining that "/" is the staff username+password screen |
| P17080 | no console.log was left behind in any file I changed | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17081 | every file I changed is in my own territory (9 changed) | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17082 | no migration was added or edited | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17083 | no other panel's app.js was touched | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17084 | no owner or admin screen was touched | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17085 | nothing named the live client stack | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17086 | no CLAUDE.md or settings file was changed | read the guard/component, and diffed the branch against origin/main | ✅ |  |
| P17087 | the three NEVER entries that match no route are forward-safe, not the dead-pattern fault | read the exact line and judged whether it is a defect at all | ✅ |  |
| P17088 | every sign-in route that exists IS excluded from the cache | read the exact line and judged whether it is a defect at all | ✅ |  |
| P17089 | both logout routes are handled by the LOGOUT branch before isNever is reached | read the exact line and judged whether it is a defect at all | ✅ |  |
| P17090 | barState's offline branch spells its plural inline instead of calling changesWord() | read the exact line and judged whether it is a defect at all | ✅ |  |
| P17091 | scheduleHeal's settle re-render is an untracked one-shot timer | read the exact line and judged whether it is a defect at all | ✅ |  |
| P17092 | on /q/<code> with no pinned restaurant, the way out reloads the same page | read the exact line and judged whether it is a defect at all | ✅ |  |
| P17093 | the connection badge's styled-jsx still carries rules for the list it no longer renders | read the exact line and judged whether it is a defect at all | ✅ |  |
| P17094 | the badge still renders only the COUNT, never a second list of orders | read the exact line and judged whether it is a defect at all | ✅ |  |
| P17095 | the badge imports only the count from the guest outbox | read the exact line and judged whether it is a defect at all | ✅ |  |
| P17096 | MAX_STALE_MS still has exactly one use, so no per-family window crept in | read the exact line and judged whether it is a defect at all | ✅ |  |
| P17097 | the R16 rejection comment is still on the line someone would change | read the exact line and judged whether it is a defect at all | ✅ |  |
| P17098 | no new setInterval faster than the 60s backstop was introduced anywhere I touched | read the exact line and judged whether it is a defect at all | ✅ |  |
| P17099 | the offline page still loads nothing from another origin after my change | read the exact line and judged whether it is a defect at all | ✅ |  |
| P17100 | my change to the offline page added no new element ids a guard might miss | read the exact line and judged whether it is a defect at all | ✅ | expectation was wrong — the page has 7 ids, not 8, and it had 7 before my change too (checked against origin/main) |
