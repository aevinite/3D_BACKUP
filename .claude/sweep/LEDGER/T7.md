# LEDGER T7 — the waiter tablet (sweep #6)

**Territory:** `app/tablet/**` · `public/panels/tablet/app.js` · `public/panels/tablet/index.html` ·
`public/panels/tablet/style.css`
**Phase IDs:** P03001–P03500 (permanent — never renumber)
**Dev server for the live phases:** `http://localhost:4107` (this terminal's own port, proved by
`lsof -nP -iTCP:4107 -sTCP:LISTEN` before any screenshot was trusted)

## The budget these 500 phases were written to

| block | ids | count | share | what it is |
|-------|-----|-------|-------|-----------|
| A · reading the code for correctness | P03001–P03200 | 200 | 40% | does the source actually do what it says |
| B · conformance to the project's own rules | P03201–P03300 | 100 | 20% | CLAUDE.md, REJECTED-IDEAS, the standing owner rules |
| C · watching it run (headless Playwright, port 4107) | P03301–P03375 | 75 | 15% | the panel driven the way a waiter drives it |
| D · screenshots actually READ | P03376–P03425 | 50 | 10% | desktop 1280×800 **and** Samsung A35 360×780 dpr3 |
| E · tracing a change across panels | P03426–P03475 | 50 | 10% | tablet ↔ manager ↔ kitchen ↔ server ↔ SQL |
| F · my own judgment about a real restaurant | P03476–P03500 | 25 | 5% | one hand, a full tray, a busy room |

**Per-file coverage (every file I own gets at least one phase; the scope files get ten or more):**

| file | phases |
|------|--------|
| `public/panels/tablet/app.js` | 300+ (blocks A, B, C, E, F throughout) |
| `public/panels/tablet/style.css` | 60+ (P03056–P03060, P03086–P03095, P03216–P03225, P03301–P03320, P03376–P03425) |
| `public/panels/tablet/index.html` | 14 (P03186–P03192, P03226–P03230, P03296, P03301) |
| `app/tablet/page.tsx` | 6 (P03283–P03287, P03426) |
| `app/tablet/layout.tsx` | 4 (P03288–P03290, P03427) |

**Result key:** ✅ pass · ❌ fail (fixed unless the note says otherwise) · ⏭ skipped, with a written
reason.

**PASS 4 — SWEEP #7, 2026-08-22/23, against `origin/main` b64951ad.** Every one of the 500 rows
below was re-run and its result confirmed in place. **No row regressed: 480 ✅ stayed ✅, the 16
`❌→✅` fixes all still hold, and the 4 `⏭` are still skipped for the same written reasons.** Eight
rows had their EXPECTATION move because the product legitimately changed — each says so in its own
note and keeps its id (P03242, P03268, P03270, P03274, P03275, P03327, P03399, P03406).

Three things were found and fixed this pass, and they are the new rows' work, not these:
  · **item 1** — the split-payment panel reported a 40-paise shortfall as "₹0 of the bill is still
    uncovered", so Take payment refused, named nothing, and refused again. `inr` → `inrExact` on
    the three figures a waiter has to MATCH. (P18293, P18303, P18304)
  · **item 2** — the `--scrim` token added on 2026-08-22 so "every overlay dims the page by the same
    amount" had reached 4 of 15 overlays; the other eleven still carried their own value, so one
    action could dim the floor three different ways. (P18302)
  · **item 3** — `npm run verify:tablet` was dying at check 81 of 103 on a correctly-disabled
    button, taking the whole order→serve→pay→close loop, the touch pass and its own cleanup with
    it. A guard fault, not a product one. (P18405 reserved for its re-check)

**HOW THIS PASS WAS RUN.** Port 4207, proved mine with `lsof` before anything was trusted. One
login for the whole run through `loginAs(ctx, "tablet", …)`. Its own fixture party on tables
**17, 18, 19** (seated through the waiter's own RPC, retired by id at the end); it merged 18 into 17
through the panel's own KOT ▾ and unmerged it again, and it settled nothing that it did not have to.
Five devices — desktop 1280×800 (mouse), iPad 1194×834 and 834×1194 (touch), Samsung A35 360×780 and
780×360 — and both skins.

**FIVE red rows this pass were MY OWN CHECK, not the product**, and they are written up in
`T7-findings.md` so the next sweep does not re-file them: a dish tap that "added nothing" (it was the
one dish on the menu WITH size options, so it correctly opened the options popup), a typed allergy
that "vanished" (my selector matched the CART's ＋ Other, not the popup's), a free tile that "did not
return to the floor" (it held an open session, so keeping its detail is right), a dark-skin contrast
failure of 1.11:1 (my own maths fell back to white because a tile's background is a gradient), and a
missing admin ribbon (`?rid=` is deliberately ignored without the console's act-as cookie — the
manager panel does the same). **Judge a red row against the file it accuses before filing it.**

**THREE passes are recorded.** Pass 1 found the faults; pass 2 re-ran all 500 after fixing them; pass
3 (2026-08-19) re-ran everything again against `origin/main` — where this work is now merged — after
the owner asked for the merge/un-merge follow-ups. Pass 3 was run on the code that is actually live,
including another terminal's reprint change inside this territory, and it found **three more things**
(P03054b, P03313b and the confirm's line breaks). All three are fixed and guarded; the live half was
then green three runs in a row. Notes carry the latest run's evidence.

---

## A · READING THE CODE FOR CORRECTNESS (P03001–P03200)

### A1 · helpers, money and tax (P03001–P03030)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03001 | `inr()` formats with `en-IN` grouping (₹1,07,880), not `en-US` | read `public/panels/tablet/app.js` — the `inr` const must pass `"en-IN"` to `toLocaleString` | ✅ | line 44; matches billdoc.js and the manager |
| P03002 | `INR_RATE` is 1 — prices are stored in rupees since mig 043, so nothing is converted | grep `INR_RATE` in tablet app.js; must be `= 1` and used only inside `inr()` | ✅ | |
| P03003 | `esc()` escapes all five of `& < > " '` before anything reaches innerHTML | read the `esc` const's replace map | ✅ | all five present |
| P03004 | `effRate()` sums NAMED `tax_components` first, falls back to `tax_rate`, then 5% | read `effRate`; must filter components on `label && rate > 0` | ✅ | mirrors lib/tax.ts |
| P03005 | `banquetRate()` uses `banquet_tax_components` when set, else exactly `effRate()` | read `banquetRate`; the fallback must be `effRate()`, not a literal | ✅ | mig 239 |
| P03006 | `preTax(gross)` divides by `(1 + effRate())` — never subtracts the rate | read `preTax` | ✅ | |
| P03007 | `discPct()` delegates to `LFH_BILLDOC.discPct` and returns `""` when billdoc is absent | read `discPct`; must guard `typeof LFH_BILLDOC !== "undefined"` | ✅ | one definition of "how much %" |
| P03008 | `orderTaxSplit()` counts `nontax` from the LINES, never as `total − base×(1+rate)` | read the loop; the comment forbids the subtraction and the code must match | ✅ | avoids a phantom ₹0.01 MRP |
| P03009 | A pre-mig-270 ticket (no line carries `tax_mode`) falls back to the whole gross worked back through the rate | read the `if (!lines.length \|\| !typed.length)` branch | ✅ | |
| P03010 | `resolveTaxMode()` returns `exempt` for a composition-scheme restaurant before any per-dish answer | read `resolveTaxMode`; composition must be the FIRST test | ✅ | mirrors lib/tax.ts case for case |
| P03011 | With `item_tax_modes_allowed` off, a dish's own `tax_mode` is ignored | read the `if (!itemTaxModesOn()) return restaurant;` line | ✅ | |
| P03012 | `mrp` mode resolves via `settings.mrp_tax_treatment` (`inclusive` → incl, else exempt) | read the `m === "mrp"` branch | ✅ | |
| P03013 | `dishIsMrp()` is presentational only — no money path reads it | grep `dishIsMrp` usages: `dishBtnHtml` only | ✅ | |
| P03014 | `taxableBaseOf(o)` is exactly `orderTaxSplit(o).base` (one definition, not two sums) | read both | ✅ | |
| P03015 | `tableAgg`'s due is `Σtotal − Σdiscount×(1+rate)` clamped ≥ 0 (discount BEFORE tax) | read the live branch of `tableAgg` | ✅ | matches billMath |
| P03016 | `tableAgg` counts due only for ACCEPTED unpaid orders — a `received` order adds nothing | read the `o.status !== "received"` test inside the due accumulator | ✅ | |
| P03017 | `patchTileFromSlice` computes due with the SAME formula as `tableAgg` (no second rule) | diff the two accumulator blocks by eye | ✅ | identical, deliberately duplicated |
| P03018 | The tile's pay ring is three-way (unpaid / paid / nothing), never a two-way ternary | read `payCls` in `tileHtml` | ✅ | a brand-new order rings nothing |
| P03019 | `unpaid`/`paid` in `tableAgg` are computed off `accepted`, not off all orders | read the `accepted` const and the two `some()` calls | ✅ | |
| P03020 | `openDiscountModal` caps the discount at the TAXABLE base, never the gross total | read `maxDisc = base` and `base` derivation | ✅ | mig 270 |
| P03021 | "They pay" solves `d = base − (P − lockedAmt)/(1+rate)` so an MRP line is paid whatever happens | read `payInput.oninput` | ✅ | |
| P03022 | A blank / negative / non-numeric discount box leaves the discount untouched and Apply refuses OUT LOUD | read `setBlank` + the Apply handler's `if (blankNow)` toast | ✅ | button stays enabled and dims |
| P03023 | Over-cap is REFUSED with a sentence and every box snaps to the real maximum — never trimmed silently | read `flagCap` + the Apply handler's `if (overCap)` branch | ✅ | |
| P03024 | The cap note names the MRP portion when there is one, and says "food value before tax" when there isn't | read the two `capNote.textContent` strings | ✅ | |
| P03025 | `tabletBillDiscount` measures against the WHOLE party bill (paid + unpaid), matching what the server subtracts | read `billable = partyOrders(t).filter(o => o.status !== "cancelled")` | ✅ | audit 2026-07-09 |
| P03026 | A PIN-mode discount skips the optimistic write, so a cancelled PIN can't leave a phantom discount | read the `tperm("tablet_discount") === "pin"` branch of `save()` | ✅ | |
| P03027 | A non-PIN discount sends `expect: { discount: wasDiscount }` captured BEFORE the optimistic flip | read `const wasDiscount` position relative to `order.discount = amount` | ✅ | no silent overwrite |
| P03028 | The whole-bill discount label quotes a percentage measured on the party's TAXABLE base | read `billDiscLbl` in `renderPanel` | ✅ | not the gross subtotal |
| P03029 | `renderSplitSettle`'s due uses discount-before-tax, the same as every other money view | read the `due` reduce in `renderSplitSettle` | ✅ | |
| P03030 | The split legs must sum to the due within ₹0.011 or the tap is refused with a sentence | read the `.ss-go` handler | ✅ | |

### A2 · the merged-party model (P03031–P03055)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03031 | `mergeList()` reads `state.summary.merges` and returns `[]` when absent | read `mergeList` | ✅ | |
| P03032 | `mergeParentOf(t)` compares as STRINGS both sides | read the `String(x.child_table) === String(t)` test | ✅ | |
| P03033 | `mergeChildrenOf(t)` sorts numerically, not lexically ("T10" after "T9") | read the `.sort((a,b) => Number(a) - Number(b))` | ✅ | |
| P03034 | `partyTablesOf(t)` puts the PARENT first and always includes `t` | read `partyTablesOf` | ✅ | |
| P03035 | `partyOrders(t)` dedupes by order id across the party's tables | read the `seen` Set | ✅ | |
| P03036 | `sessionOf(t)` falls back to the PARENT's open session for a merged child | read `rawSessionOf` / `sessionOf` | ✅ | ownership is the session |
| P03037 | `ordersOf(t)` admits a party-less order only when it is NOT older than the session (60s slack) | read the `since` guard | ✅ | stops a 9-day-old order landing on tonight's bill |
| P03038 | `ordersOf(t)` excludes `cancelled` and `archived` rows | read the filter | ✅ | |
| P03039 | `tileState` reads the state for the PARTY head, so a joined table never reads "Free" | read the first line of `tileState` | ✅ | mig 249 |
| P03040 | `tileIsOpen(i)` resolves through `mergeParentOf` before asking the summary | read `tileIsOpen` | ✅ | one answer, not three local workarounds |
| P03041 | `canHostAParty(i)` refuses a merged CHILD as a destination | read `canHostAParty` | ✅ | |
| P03042 | `tileHtml` draws the ⇄ chip in the TOP row (the seat chip's place), never its own row | read the `mergeChip` placement inside `.t-top` | ✅ | an extra row overflowed the square |
| P03043 | Badges on a merged tile stay the table's OWN (a bell at T7 belongs on T7) | read `ownTile = summaryTile(i)` vs `partyHead` | ✅ | |
| P03044 | `needsAttention` takes calls/requests from the own tile and food flags from the party | read `needsAttention` | ✅ | |
| P03045 | `patchTileFromSlice` patches the PARENT's tile key for a merged member | read `const tk = mergeParentOf(...) \|\| String(t)` | ✅ | |
| P03046 | `optimisticPay` flips every party member table's orders, matching what the server settles | read the `party` Set in `optimisticPay` | ✅ | |
| P03047 | `ensurePartySlices` forces a fresh read of EVERY party table's slice | read `ensurePartySlices` | ❌→✅ | **Pass 3.** `ensurePartySlices` ran its members through `Promise.all(map(ensureTableSlice))`. On a party of THREE opened from the table HOLDING the bill, ⇹ Unmerge announced "gets nothing back — nothing was ordered at it" about a table holding a ₹483 ticket — and `partyOrders()` (the BILL) reads the same rows. Now fetched together and merged in a LOOP, which is what `loadImpl` and `loadTables` already do. The mechanism was never pinned down; the symptom and its cure both were, and the guard says so honestly rather than inventing one |
| P03048 | `loadTables` expands a breadcrumb's table list to the whole party before fetching | read the `flatMap(partyTablesOf)` line | ✅ | |
| P03049 | `loadImpl` re-pulls party mates' slices after the wipe, not just the selected table | read the `mates` block in `loadImpl` | ✅ | a child's detail said "closed · no orders" without it |
| P03050 | `loadTables` also refreshes mates' slices when the selected table is in the party | read the second `mates` block | ✅ | half-fresh/half-stale bill |
| P03051 | `printTableBill` names every table of a joined party ("T6 + T7") | read `tableDisp` / `mergeGroupLabel` | ✅ | |
| P03052 | `renderKotMenu`'s "Change table" row is disabled for a merged party (unmerge first) | read the `shift` row's `on` argument | ✅ | mig 264 |
| P03053 | `renderSplitSettle` splits the whole PARTY's bill, and the KOT row is gated on the same rows | compare `splittable` in `renderKotMenu` with `payable` in `renderSplitSettle` | ✅ | same filter, deliberately duplicated |
| P03054 | `renderMoveOrderTarget` never offers a party mate or another party's merged child | read the `mates` Set + `mergeParentOf(i)` skip | ✅ | |
| P03055 | `renderMoveItemTarget` never offers a party mate as a destination | read the loop's skip conditions | ❌→✅ | **F3**: it skipped only `t` itself and other parties' children; a merged party's own mate was offered. Fixed to use `partyTablesOf(t)` like its sibling; guarded by `verify:tablet-taps` |

### A3 · the floor: tiles, patching, filters, sections (P03056–P03095)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03056 | `summaryTile` re-presents `waiting` as `free` at ONE place, so tiles/counts/`tileIsOpen` agree | read `summaryTile` | ✅ | owner 2026-07-31 |
| P03057 | `summaryTile` blanks the meta of a free tile ("tap to open" is dead wording) | read the `tile.state === "free"` branch | ✅ | |
| P03058 | A missing tile synthesises a complete `free` shape (every field the callers read) | read the `if (!tile) return {...}` object against every `.counts`/`.pay`/`.due` read | ✅ | |
| P03059 | `summaryCallsOf` trims whitespace off `table_number` before comparing | read the `.trim()` | ✅ | |
| P03060 | `floorTableList()` draws 1…`table_count` plus only OCCUPIED off-plan tables | read `floorTableList` | ✅ | a free off-plan tile is a phantom |
| P03061 | An off-plan table that is a merge member is kept even when its own row reads free | read the `\|\| !!mergeParentOf(key)` clause | ✅ | |
| P03062 | `tableCount()` floors at 1 and defaults to 12 | read `tableCount` | ✅ | |
| P03063 | `floorPerRow()` clamps to 2…12 and defaults to 12 — the same numbers as `lib/floorLayout.ts` | read the three constants, then `lib/floorLayout.ts` | ✅ | 2 / 12 / 12 both sides |
| P03064 | A touch device caps the per-row count at 6; a mouse (`pointer: fine`) is untouched | read `isTouchDevice()` + `floorPerRow()` | ✅ | owner 2026-08-05 |
| P03065 | `renderFloor` writes `--per-row-pc` (never `--per-row`), so the CSS screen bands still win | read the `setProperty` call and the `.tiles` rule in style.css | ✅ | |
| P03066 | `passesFilter` puts the SECTION test first, before the chip filter | read `passesFilter` | ✅ | full render and patch can't disagree |
| P03067 | `patchTabletTiles` full-renders whenever a tile's filter membership flips | read the `if (visible !== !!el)` line | ✅ | |
| P03068 | `patchTabletTiles` full-renders when the grid is absent | read `if (!grid) { renderFloor(); return; }` | ✅ | |
| P03069 | Both render paths build a tile through the SAME `tileHtml` (no path-divergent markup) | grep `tileHtml(` — exactly the two call sites | ✅ | |
| P03070 | `bindFloorDelegation` binds once (boolean guard), on the stable `#tiles` / `#floorNav` | read `floorDelegationBound` | ✅ | patched nodes need no re-binding |
| P03071 | The quick-action branches are matched BEFORE the tile-select branch | read the handler's order | ✅ | replicates the old stopPropagation |
| P03072 | Tapping a different tile mid-order with items in the cart confirms before discarding | read the `state.ordering && state.cart.length` guard in both the tile and `.t-take` branches | ✅ | audit 2026-07-09 |
| P03073 | A FREE tile goes straight into the order builder; a MERGED tile never does | read the `inParty` / `partyState` test | ✅ | |
| P03074 | A free tile with no take-orders permission opens the detail instead of the builder | read the `&& tshow("tablet_take_orders")` clause | ✅ | |
| P03075 | The tile's ✓ Accept forces a fresh party slice before accepting | read the `.tacc` branch's `ensurePartySlices(qt, true)` | ✅ | |
| P03076 | The tile's ✓ Accept SAYS something when there is nothing left to accept | read `optimisticAccept`'s empty-list branch | ❌→✅ | **F1**: `if (!orderIds.length) return;` — the green ✓ did nothing and said nothing. Now toasts and refreshes |
| P03077 | "✓ Accept all" on the detail says something when the list comes back empty | same function, second call site | ❌→✅ | **F1** (same fix) |
| P03078 | "🍽️ Serve all" says something when the list comes back empty | read `optimisticServeAll`'s empty-list branch | ❌→✅ | **F1** (same fix) |
| P03079 | The tile's 💳 pay forces a fresh party slice so `due`/`billNo` are the party's real current bill | read the `.tpay` branch | ✅ | |
| P03080 | The tile's ⏻ close routes through the ONE shared `closeTableAndFree` | read the `.tclose` branch | ✅ | one place for the reason-code ladder |
| P03081 | `stepTables()` walks only tables the floor is DRAWING, through the same section rule | read `stepTables` | ✅ | |
| P03082 | `stepTables()` steps only into tables with something on them (never an empty popup) | read the `busy` filter | ✅ | caught in review 2026-08-04 |
| P03083 | `mySection()` returns `null` (not `[]`) for an unrestricted waiter | read `mySection` | ✅ | `[]` would hide the whole floor |
| P03084 | `inMySection` lets an off-plan table through (its bill must stay reachable) | read the `Number(i) > tableCount()` clause | ✅ | mirrors the server's `allows()` |
| P03085 | `renderMySection` hides itself with an INLINE display, never the `hidden` attribute | read `el.style.display = "none"` | ✅ | `hidden` loses to an author display rule |
| P03086 | `rangeText` collapses "1 2 3 4 5 6" to "1–6" and leaves gaps as separate parts | read `rangeText`, trace `[1,2,4,9,10]` | ✅ | → "1–2, 4, 9–10" |
| P03087 | An unassigned waiter gets the kind full-grid empty state, not a bare "no tables" line | read the `emptyMsg` ternary in `renderFloor` | ✅ | |
| P03088 | `floorFilterCounts` counts MY tables, not the restaurant's | read the `inMySection` continue | ✅ | |
| P03089 | The seat count falls back table → floor default → 4, in that order | read the `seatMap` chain | ✅ | same order as the manager |
| P03090 | The progress strip's four widths are percentages of the dish TOTAL and sum to 100% | read the `strip` template | ✅ | |
| P03091 | The tile's title attribute still carries the state word + KOT/meta the tile no longer prints | read `kotTip` / `statusRow` titles | ✅ | |
| P03092 | A table NAME longer than 3 characters gets the smaller number class | read `numCls` | ✅ | |
| P03093 | `ttagOf` returns "" when the tags module is off for a real waiter | read `ttagOf` + `tabletTagsOn` | ✅ | |
| P03094 | `floorLegendHtml` lists purple only while something IS merged | read the `if (mergeList().length)` push | ✅ | |
| P03095 | The 🔔 legend entry needs BOTH dining sessions AND the guest bell feature | read the `bell` const | ✅ | never explains a colour that can't appear |

### A4 · the table detail popup (P03096–P03130)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03096 | `renderPanel` clears `p.onclick` on every rebuild so a picker's handler can't leak onto the detail | read the `p.onclick = null` line | ✅ | #10 |
| P03097 | An open picker's back layer is torn down before a forced `renderPanel` | read the `state.pickerOpen && activePickerDrop` line | ✅ | #U1 |
| P03098 | The popup's scroll survives a live-update rebuild only when it is the SAME table | read `prevScrollTop` / `restoreScroll` | ✅ | #U2 |
| P03099 | Leaving order mode always drops `om-mode`, `om-open` and the order back layer | read the `if (!state.ordering)` block | ✅ | |
| P03100 | `selectTable` clears `addToOrderId`, `viewOrder` and `editOrders` for the table being left | read `selectTable` | ✅ | audit 2026-07-09 — dishes landed on the old bill |
| P03101 | `selectTable` drops the view-order back layer before switching | read the `if (voBackOff)` line | ✅ | |
| P03102 | `selectTable` bails out of the post-fetch render if the waiter already moved on | read `if (String(state.table) !== String(t)) return;` | ✅ | |
| P03103 | `selectTable` adopts `boardSig` as the baseline so the next poll doesn't re-flicker the detail | read `lastSig = boardSig(state)` | ✅ | |
| P03104 | The instant (stale-while-revalidate) detail only paints for an OCCUPIED, not-yet-loaded table | read `if (!sliceLoaded(t) && tileIsOpen(t))` | ✅ | |
| P03105 | The loading detail is wrapped in the SAME `.detail-pop` popup as the real one | read the loading branch's markup | ✅ | no side-panel flash |
| P03106 | The loading detail's ✕ and backdrop both close it | read the two handlers in the loading branch | ✅ | |
| P03107 | `sliceLoaded` treats a session row OR a live order as "the slice landed" | read `sliceLoaded` | ✅ | |
| P03108 | The "waiting to send" block is its OWN box and is never mixed into the bill total | read `unsentBox` and the bill footer | ✅ | a bill shows only what the kitchen has |
| P03109 | A FAILED queued write is shown in red as "needs you", not as "sending…" | read the `it.status === "failed"` branch | ✅ | |
| P03110 | The bill line says "bill not numbered yet" in words — never a bare em dash | read `billInner` | ✅ | T14 sweep |
| P03111 | The bill number is printed once per card, not twice | read `billInner` and the `pmeta` header line | ✅ | |
| P03112 | `reqsOf` hides an `open` request once the table is open (it is already fulfilled) | read `reqsOf` | ✅ | owner 2026-06-18 |
| P03113 | Un-accepted orders stay SEPARATE cards; accepted ones fold into one list with a dashed rule | read `newCards` / `mergedCard` | ✅ | matches the manager |
| P03114 | "Accept all" only shows when there is more than one un-accepted order | read the `> 1` test | ✅ | |
| P03115 | "Serve all" only shows when some accepted order still has an unserved DB dish | read the `data-serve-all` condition | ✅ | |
| P03116 | A `served` dish's status pill is NOT tappable (the reverse-tap un-serve is gone) | read `statusBadge` | ✅ | #3 |
| P03117 | The only un-serve is "↩ Send back to kitchen" inside the dish edit modal | read `unserveBtn` | ✅ | |
| P03118 | A `served` dish still reaches the ✎ Edit modal (its only recovery path) | read `editCtl`'s served branch | ✅ | audit 2026-07-08 |
| P03119 | A `ready` dish is hands-off: no qty steppers, no edit | read `editCtl`'s final branch | ✅ | |
| P03120 | The dish edit modal locks allergens + note on a served dish and says why on screen | read the served banner + the disabled Save's sibling text | ✅ | visible refusal, not a silent one |
| P03121 | Turning OFF an order-wide avoid confirms first (it clears from EVERY dish) | read the `orderWide.has(s)` confirm | ✅ | #4 |
| P03122 | The dish edit modal sends `expect` on all three writes (note, removed, order allergies) | read the three `api(...)` calls | ✅ | no silent overwrite |
| P03123 | A clash closes the modal, refreshes, and shows the server's plain sentence for 9s | read the catch block | ✅ | |
| P03124 | An order-wide allergy chip toasts and refreshes when the order has vanished from state | read the `if (!o)` branch | ✅ | T4 sweep fix, still in place |
| P03125 | A typed allergy redraws the panel; a standard one only toggles the class (no flicker) | read the `ALG_STD.includes(slug)` branch | ✅ | |
| P03126 | `bumpItemQty` reads the LIVE qty from state, never the button's `data-qty` attribute | read `bumpItemQty` | ✅ | fast taps accumulate |
| P03127 | `bumpItemQty` refuses below 1 with "Use 🗑 to remove the dish", not silently | read the `next < 1` branch | ✅ | |
| P03128 | `bumpItemQty` sends `expect: { qty: cur }` and debounces the reconcile to one call | read the api call + `qtyReconcileTimer` | ✅ | |
| P03129 | Deleting a dish and deleting a whole order each confirm first | read the two `confirmDialog` calls | ✅ | |
| P03130 | Entering per-order EDIT mode is gated on the kitchen-confirm question | read the `data-edit-order` handler | ✅ | owner 2026-06-17 |

### A5 · the pickers: KOT ▾, shift, merge, move, split (P03131–P03155)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03131 | `renderPickerShell` registers its own back layer and records the teardown for a forced repaint | read `renderPickerShell` | ✅ | #U1 |
| P03132 | `state.pickerOpen` makes `load`, `loadTables` and the outbox event skip `renderPanel` | grep `state.pickerOpen` — four guard sites | ✅ | a picker is never wiped mid-selection |
| P03133 | Every picker's ✕ and its dim backdrop both go BACK, not out | read the `.picker-back` + `p.onclick` wiring | ✅ | |
| P03134 | Each picker step drops its OWN back layer before advancing to the next step | read `dropLayer()` at the start of each `data-*` handler | ✅ | audit 2026-07-08 |
| P03135 | `kotOpsOn()` needs the module AND the tri-state for a real waiter; the admin always sees it | read `kotOpsOn` | ✅ | mig 172 |
| P03136 | With KOT ops OFF the two classic buttons render instead — no capability is lost | read `headOps` | ✅ | zero regression |
| P03137 | "Move a KOT" is enabled only when this table has a movable (unpaid, non-cancelled) ticket | read `movable` + the row | ✅ | |
| P03138 | "Move a single dish" is enabled only when a movable ticket has a real DB dish row | read the `dishRowsOf(o).some(r => r.fromDb)` test | ✅ | |
| P03139 | "Split the bill" is hidden entirely when `split_bill_enabled` is off, and sits LAST | read the `split_bill_enabled` ternary | ✅ | mig 248, matches the manager's order |
| P03140 | "Merge tables" is enabled only when the merge PICKER will actually have a destination | compare the `occupiedOthers` loop with `renderMergePicker`'s loop | ❌→✅ | **F2**: the count omitted `inMySection`, so a sectioned waiter got an enabled row that opened "No other open tables to merge with." Fixed to the picker's exact predicate |
| P03141 | The ⇄ Change table row is disabled for a MERGED party (unmerge first), and when no free table exists its PICKER explains itself in words | read the `shift` row's condition, then open the picker on a full floor | ✅ | Deliberately NOT gated on whether a destination exists, unlike the merge row beside it: the shift picker already says "No free tables to shift to.", and a greyed row with no explanation would tell the waiter LESS than the screen it opens. Recorded under "Considered and deliberately NOT done" in `T7-improvements.md` so the next sweep does not silently change it |
| P03142 | `renderShiftPicker` offers only free tables inside this waiter's section | read the loop | ✅ | |
| P03143 | `renderMergePicker` confirms before merging and names both tables | read the confirm string | ✅ | |
| P03144 | A successful merge re-points `state.table` at the destination and repaints | read the `onSuccess` | ✅ | |
| P03145 | `renderMoveOrderPicker` shows each order's NET due, not its gross total | read `netDue` | ✅ | audit 2026-07-08 |
| P03146 | Paid and cancelled orders are excluded from every move picker | read the three `payment_status !== "paid"` filters | ✅ | settled revenue can't be re-homed |
| P03147 | `renderMoveItemTarget` filters by section (the server checks both ends of a move) | read the `inMySection` continue | ✅ | |
| P03148 | Every destination picker uses `tableCount()`, never `floorTableList()` | grep the four picker loops | ✅ | you may take a party OFF an off-plan table, never send one TO one |
| P03149 | `runOptimistic` snaps `state.table` back to the source when a shift is refused | read `prevTable` | ✅ | audit 2026-07-08 round2 |
| P03150 | `runOptimistic` clears `lastSig` on failure so `load()` really repaints server truth | read the catch | ✅ | audit 2026-07-09 |
| P03151 | `runOptimistic` runs `onSuccess` only on a real ONLINE success (not queued, not failed) | read the `done` flag | ✅ | |
| P03152 | A queued (offline) write returns early with a friendly "saved" note and skips the post-write GET | read `isQueued` at each of its 6 call sites | ✅ | #2 |
| P03153 | `savedMsg` says which of the four situations it was — offline / busy / slow / behind | read `savedMsg` | ✅ | T15 P3 |
| P03154 | The split-settle "Collect in parts" button stays ENABLED and refuses out loud | read the `.ss-go` handler | ✅ | |
| P03155 | Split-settle goes through `actGated`, so a PIN restaurant is challenged | read the `actGated` call | ✅ | |

### A6 · order-taking: browse, cart, options, send (P03156–P03185)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03156 | `foldAccents` strips combining marks on BOTH sides, so "caffe" finds "Caffè" | read `foldAccents` + `orderSections` | ✅ | #11 |
| P03157 | An empty dish list mid-load shows "Loading the menu…" and kicks exactly ONE refresh | read `menuReloadInFlight` | ✅ | audit 2026-07-09 |
| P03158 | Dishes with no matching category fall into an "Other" section rather than vanishing | read the `other` bucket | ✅ | |
| P03159 | Inactive categories are dropped from the rail | read `c.active !== false` | ✅ | |
| P03160 | A category chip whose section is missing toasts instead of returning silently | read the `if (!s)` branch in `wireOrderNav` | ✅ | T4 sweep |
| P03161 | The category chip uses `CSS.escape` for the selector when available | read the `CSS.escape` ternary | ✅ | |
| P03162 | The end spacer lets even the LAST category reach the top of the scroller | read the `om-endpad` block | ✅ | |
| P03163 | The scroll-spy is muted for 200ms after a jump so the chip doesn't fight the scroll | read `state._omMute` | ✅ | |
| P03164 | The browse scroll position is remembered only while NOT searching | read the `if (!state.dishSearch.trim())` in `onscroll` | ✅ | |
| P03165 | `updateDishBadges` patches badges in place — the grid is never rebuilt on an add | read `updateDishBadges` | ✅ | browse scroll survives |
| P03166 | `updateDishAvailability` mirrors `dishBtnHtml` exactly, including the open-price label | read both `.dprice` writes | ✅ | T4 sweep — a ₹0 lie |
| P03167 | `updateDishAvailability` also mirrors the OFF MENU mark | read the `offmenu` toggle | ✅ | mig 306 |
| P03168 | A dish tile whose dish has left the menu toasts and rebuilds — both on the tile and on the ✎ | read both `if (!d)` branches | ✅ | T4 sweep |
| P03169 | A cart line whose dish has left the menu toasts and is deliberately LEFT in the cart | read the `[data-edit]` handler's `if (!d)` | ✅ | removing it is the waiter's decision |
| P03170 | An open-price dish asks for the price through ONE function, whatever the entry point | grep `addOpenPriceDish` — tile tap, ✎, and the cart's ✎ | ✅ | |
| P03171 | The price prompt refuses ≤0 and >100000 with a sentence in its own error line | read `pricePrompt` | ✅ | |
| P03172 | Each open-price tap is its OWN cart line (two cans can differ) | read `state.cart.push` in `addOpenPriceDish` | ✅ | |
| P03173 | A quick tap only stacks onto a PLAIN line — a customised line is left alone | read the `line` find in the `[data-dish]` handler | ✅ | owner 2026-06-16 |
| P03174 | `addDishToOrder` coalesces repeat taps for the same dish+price into ONE add | read `addingDishKeys` / `pendingAddQty` | ✅ | no fast tap is lost |
| P03175 | The coalescing key includes the typed price for open-price dishes | read the key template | ✅ | |
| P03176 | A per-dish allergy travels as that LINE's note; the order-level list stays whole-order only | read `buildBody` | ✅ | 2026-06-17 leak fix |
| P03177 | The ✎ note travels VERBATIM; the allergy chips are worded "⚠ no X" | read the `avoidTxt` / `note` join | ✅ | "less ice" never becomes "NO LESS ICE" |
| P03178 | `sendOrder` claims its re-entry guard BEFORE the confirm and releases it on cancel | read the `sendingOrder` flow | ✅ | sweep C2 |
| P03179 | A duplicate-warning refusal offers "Send anyway" instead of hard-refusing | read the `duplicateWarning` branch | ✅ | #15 |
| P03180 | A queued send says "saved ✓ — it'll go the moment you're back online" and resets cleanly | read the `isQueued(r)` branch | ✅ | never "#undefined" |
| P03181 | `sold_out` / `unknown_item` get their own friendly sentence and the cart is KEPT | read the reason ternary | ✅ | audit 2026-07-08 |
| P03182 | `exitOrderMode` clears the cart, the allergy list and any quick order — nothing leaks to the next table | read `exitOrderMode` | ✅ | |
| P03183 | Backing out of an order for a still-free table returns to the FLOOR, not an empty popup | read the `!sessionOf && !ordersOf` branch | ✅ | 2026-08-03 walkthrough |
| P03184 | `openQuickOrder` refuses OUT LOUD while a cart is already being built | read the `state.ordering && state.cart.length` toast | ✅ | |
| P03185 | `openQuickDest` closes a previous picker through its OWN teardown, never a bare `.remove()` | read `qdestClose` | ✅ | else the back layer leaks |

### A7 · load, poll, realtime, offline (P03186–P03200)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03186 | `boardSig` hashes the WHOLE summary plus the selected table's slice rows | read `boardSig` | ✅ | the grid draws from the summary |
| P03187 | `RT_VOLATILE` excludes only heartbeat columns; the sig is NOT a hand-picked field list | read `RT_VOLATILE` + `stableRow` | ✅ | bug fixed 2026-06-17 |
| P03188 | `fullSeq` is bumped only by a whole-floor `load()`; each table has its own `tileSeq` ticket | read `loadImpl` / `loadTables` | ✅ | T13 sweep — two targeted polls no longer cancel each other |
| P03189 | `load()` coalesces concurrent callers onto one in-flight fetch plus at most one trailing refresh | read `loadInFlight` / `loadQueued` | ✅ | audit 2026-07-09 |
| P03190 | A slim (`?nomenu=1`) refresh never wipes the cached dish list | read the `Array.isArray(dishes)` branch | ✅ | perf 2026-07-20 |
| P03191 | The 60s backstop does nothing while the tab is HIDDEN | read the `if (document.hidden) return;` in the interval | ✅ | T4 sweep |
| P03192 | The 10-minute menu self-heal ASKS `/menu-sig` before downloading the dish list | read `healMenu` / `rebaseMenuSig` | ✅ | T4 improvement 7 |
| P03193 | `LFH_RT.catchUp` is used when the socket is down — never a fixed fast poll | read the `catchUp` call | ✅ | CLAUDE.md rush rule 4 |
| P03194 | The no-realtime fallback backs off 2s→60s, jitters ±20% and stops while hidden | read the `_fbTick` block | ✅ | |
| P03195 | An `ops` breadcrumb naming tables goes TARGETED; anything unscopable goes full | read the `ops` handler | ✅ | egress |
| P03196 | A `menu` breadcrumb re-reads `/whoami` (throttled 5s) so an Access change reaches an open tablet | read `refreshWhoami` | ✅ | T13 sweep |
| P03197 | `loadTables` skips the GET entirely when offline with nothing saved on the device | read the first line | ✅ | #2/#19 |
| P03198 | A first-visit GET is handed to `LFH_WARM` so the floor opens offline on the same shift | read the `uncontrolled` branch of `api()` | ✅ | |
| P03199 | The boot error handler tells "no internet", "very busy" and everything else apart | read the `load().catch(...)` at boot | ✅ | T15 finding P1 |
| P03200 | A section taken away mid-shift drops the open detail back to the floor | read the `!inMySection(state.table)` branch in `loadImpl` | ✅ | mig 222 |

---

## B · CONFORMANCE TO THE PROJECT'S OWN RULES (P03201–P03300)

### B1 · "a tap must never vanish in silence" (P03201–P03225)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03201 | `npm run verify:taps` passes for the tablet | run it | ✅ | 27/27 |
| P03202 | `confirmDialog` refuses re-entry and ANSWERS the second caller (never orphans the first promise) | read `confirmOpen` | ✅ | …and a multi-line confirm now keeps its lines: the box sets `textContent` (deliberately — nothing typed can become markup), which collapsed the split confirm's three lines into one run-on grey block. `white-space: pre-line` on `.confirm-box p`; single-sentence confirms have no newlines, so nothing else changed. **Pass 3** |
| P03203 | A refused second question SHAKES the one on screen (`cf-nudge` in app.js AND style.css) | grep both files | ✅ | |
| P03204 | `openPaymentMethodModal` guards double-tap with `payModalOpen` and resolves null | read the guard | ✅ | #18 |
| P03205 | `payModalOpen` is cleared in `close()`, so a second settle is never refused forever | read `close` | ✅ | |
| P03206 | `allergyPrompt` says WHY on an empty value and on a duplicate | read the two error lines | ✅ | |
| P03207 | Every self-built modal's Cancel resolves its promise (`done(null)`) — six modals | grep `done(null)` in pinPrompt / reasonPrompt / pricePrompt / allergyPrompt / payment / khata | ✅ | |
| P03208 | `bumpItemQty` toasts + refreshes when the dish row has vanished from state | read the `if (!it)` branch | ✅ | T4 fix holds |
| P03209 | The order-wide allergy chip toasts + refreshes when the order has vanished | read the `if (!o)` branch | ✅ | T4 fix holds |
| P03210 | Both ✎ pencils (tile and cart line) toast when the dish has left the menu | read both handlers | ✅ | T4 fix holds |
| P03211 | `tabletDiscount` toasts when the order is no longer on the board | read `tabletDiscount` | ✅ | |
| P03212 | `tabletBillDiscount` toasts for "open the table first" and "no unpaid orders" | read both branches | ✅ | |
| P03213 | `printTableBill` toasts when billdoc is missing, when there is nothing to print, and when pop-ups are blocked | read the three toasts | ✅ | |
| P03214 | `closeTableAndFree` tells "already free" and "couldn't check" APART | read the `readOk && sliceLoaded(t)` ternary | ✅ | …and `closeTableAndFree` now READS `ensurePartySlices`' answer instead of catching a throw it no longer makes — otherwise every failed read would have looked like a good one and "already free" would be the lie this guard exists to stop. **Pass 3** |
| P03215 | The close refusal branches on the server's reason CODE, with the prose only as a fallback | read `const why = e.data.reason` | ✅ | |
| P03216 | The tile's ✓ Accept never returns in silence | read `optimisticAccept` | ❌→✅ | **F1** fixed — see P03076 |
| P03217 | "Serve all" never returns in silence | read `optimisticServeAll` | ❌→✅ | **F1** fixed |
| P03218 | "Attend all" refuses out loud when there are no calls left | read the `if (!ids.length) return;` in the attend-all handler | ❌→✅ | **F1** — same shape, same fix |
| P03219 | "💳 Mark bill paid" explains itself on a TOUCH device when an order is still un-accepted | read the `disabled title=` on `#payBill` | ❌→✅ | **I1**: a `disabled` button + a hover-only `title` is a swallowed tap on a tablet. Now enabled and refuses out loud, exactly like the split-payment button 40 lines away |
| P03220 | The ⚡ quick-order table picker explains itself when the waiter's section is empty | read `openQuickDest`'s `tiles` array | ❌→✅ | **I2**: the grid rendered empty with no words. Now says so and points at the manager |
| P03221 | The split-payment "Take payment" button stays enabled and names the shortfall | read the `goBtn` handler | ✅ | the pattern the two fixes above copy |
| P03222 | `updateDishAvailability`'s silent `return` carries the written `tap-guard: silent-ok` reason | read the line | ✅ | it is a repaint loop, not a tap |
| P03223 | No NEW silent `state.…find()` return was introduced by this PR | re-run `verify:taps` after the fixes | ✅ | |
| P03224 | `sendOrder`'s `if (sendingOrder) return;` is covered by a visibly disabled "Sending…" button | read the two together | ✅ | the refusal is on screen |
| P03225 | A refused `#sendOrder` (empty cart) is explained by the cart's own "Tap dishes to add them." | read `orderCartHtml` | ✅ | |

### B2 · the mobile back button (P03226–P03240)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03226 | `backstack.js` is loaded by `index.html` before `app.js` | read the script order | ✅ | |
| P03227 | Every overlay registers `LFH_BACK.layer(...)` — never a hand-rolled pushState/popstate | grep `pushState` in tablet app.js → none | ✅ | |
| P03228 | Every registered layer is unregistered in its own close path | grep the 15 `backOff()` / `off()` calls against the 15 `LFH_BACK.layer` calls | ✅ | 1:1 |
| P03229 | The confirm dialog registers `tablet-confirm` and drops it in `finish()` | read `confirmDialog` | ✅ | |
| P03230 | The order screen's layer (`tablet-order`) is dropped on send, back, ✓ Done and hardware back | read `exitOrderMode` + `finishSent` | ✅ | |
| P03231 | The view-order screen is its OWN layer ON TOP, so back peels to the dish list | read `voBackOff` | ✅ | owner 2026-07-05 |
| P03232 | Every picker step's layer is dropped before the next step registers one | read `dropLayer()` in each handler | ✅ | no extra Back press |
| P03233 | A forced `renderPanel` drops an orphaned picker layer via `activePickerDrop` | read the top of `renderPanel` | ✅ | #U1 |
| P03234 | The ☰ drawer registers and drops `tablet-drawer` | read `openDrawer` / `closeDrawer` | ✅ | |
| P03235 | The Settings sheet, the issue sheet and My-profile each close the drawer FIRST | read the three handlers | ✅ | never two layers for one Back |
| P03236 | The x-ray zones popout registers `xray-zones` and drops it in `closeXrayZones` | read both | ✅ | |
| P03237 | The banquet overlay registers `tablet-banquet` and drops it in `closeBanquet` | read both | ✅ | |
| P03238 | The `tablet-panel` layer follows the panel's open/closed state via a MutationObserver | read the `sync()` IIFE | ✅ | |
| P03239 | The floating ✕ peels ONE layer at a time (options → view order → order → detail → floor) | read `closeBtn.onclick` | ✅ | owner 2026-07-05 |
| P03240 | The quick-destination picker's layer is dropped through `qdestClose`, not a bare remove | read `openQuickDest` | ✅ | |

### B3 · egress and the data-cost rules (P03241–P03255)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03241 | The GRID renders from the slim summary — an unselected table caches no order rows | read `tableAgg`'s two tiers | ✅ | mig 101 |
| P03242 | A table's FULL slice is fetched only on select or a quick action | grep `ensureTableSlice` call sites | ✅ |  ⚠ **EXPECTATION MOVED (pass 4).** `ensureTableSlice` has NO call sites left — `ensurePartySlices` now fetches `/state?table=N` itself and merges through `mergeSelectedSlice` (the P03047 rewrite). The RULE still holds and is what this row checks: a table's full slice is fetched only on select or a quick action, and the grid still renders from the slim summary. The ~35-line dead function and the three comments pointing at it are reported as housekeeping, not a fault |
| P03243 | A breadcrumb naming tables goes targeted (`/summary?table=N`), never a whole-floor read | read the `ops` handler | ✅ | |
| P03244 | The recurring 60s refresh is slim (`?nomenu=1`) — it drops ~50KB of ~77KB | read `needMenu` | ✅ | perf 2026-07-20 |
| P03245 | The dish list is cached on-device (`lfh_tablet_menu`) and hydrated before the first network read | read the boot IIFE | ✅ | |
| P03246 | The 10-minute self-heal costs ~40 bytes (`/menu-sig`) unless the digest actually moved | read `healMenu` | ✅ | |
| P03247 | Nothing polls faster than the 60s backstop while realtime is up | grep every `setInterval` — the clock (1s, DOM-only) and the 60s floor sync | ✅ | |
| P03248 | The clock interval writes nothing while the drawer that holds it is shut | read `tickClock` | ✅ | owner-picked improvement 2026-08-07 |
| P03249 | `refreshWhoami` is throttled to one call per 5s and repaints only when the answer DIFFERS | read `WHOAMI_MIN_GAP_MS` + the `j === whoamiJson` test | ✅ | |
| P03250 | `boardSig` short-circuits the repaint when nothing visible changed | read the `sig === lastSig` returns | ✅ | |
| P03251 | The customer-recognise lookup is debounced to 400ms and needs ≥7 digits | read the `phoneEl` listener | ✅ | |
| P03252 | The khata person search is debounced 250ms and is latest-wins | read `doSearch` / `seq` | ✅ | |
| P03253 | `patchTabletTiles` repaints only the tiles this pass applied | read `applied.length ? applied : tables` | ✅ | |
| P03254 | No query in this panel is unscoped — every read is `/api/tablet/*`, which is `restaurant_id`-scoped server-side | grep `fetch(` in tablet app.js → `api()`, `/api/panel-profile`, `/api/admin/act-as` | ✅ | |
| P03255 | Nothing this panel added scans a whole table client-side (no `.filter` over an unbounded server list) | read every `state.data.*` filter — all are the selected table's slice | ✅ | |

### B4 · offline, the outbox and no-silent-overwrites (P03256–P03270)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03256 | Every non-GET goes through `LFH_OUTBOX.send` when the outbox is present | read `api()` | ✅ | |
| P03257 | `expect` travels as the clash header on the write paths that edit a value | grep `expect:` — qty, note, removed, order allergies, discount ×2 | ✅ | `lib/clash.ts` |
| P03258 | `npm run verify:clash-coverage` still passes with the changes | run it | ✅ | |
| P03259 | An error carries `status`, `data`, `offline` and `busy` so callers can branch on flags | read the `if (!r.ok)` line | ✅ | |
| P03260 | `wantsPin` branches on the `needPin` FLAG, with the prose only as a legacy fallback | read `wantsPin` | ✅ | CLAUDE.md: codes, not prose |
| P03261 | `errText` renders a clash's plain sentence, not the code `clash_changed_elsewhere` | read `errText` | ✅ | |
| P03262 | A clash message is shown for 9s, not the usual 2.6s | grep the `9000` arguments | ✅ | |
| P03263 | The "waiting to send" block appears for the open table the moment anything is queued | read the `lfh:outbox-changed` listener | ✅ | |
| P03264 | That listener does NOT repaint while a picker is open | read its `!state.pickerOpen` guard | ✅ | 2026-08-03 |
| P03265 | `lfh:outbox-flushed`, `lfh:stale-refresh` and `online` each pull true server state | read the three listeners | ✅ | |
| P03266 | `load()` returns a resolved promise (never rejects) when offline with nothing saved | read the first line of `load` | ✅ | else every awaiting caller says "Failed" |
| P03267 | The panel's own service worker family covers `/api/tablet` reads | grep `DATA_PATHS` in `public/sw.js` | ✅ | `/api/tablet` listed |
| P03268 | `npm run verify:offline` still passes | run it | ⏭ |  Pass 2: run against a PRODUCTION build on 4108 — 53 passed, 2 failed, and BOTH failures were other panels (the manager's offline bar and the kitchen's). The WAITER half is green: order saved on the device, not marked failed, "waiting to send" shown, the tile marked. Still ⏭ because the suite as a whole is red for reasons outside this territory · ⚠ **PASS 4:** still `⏭`, and for a NEW reason worth writing down — `verify:offline` was not run this pass at all. It needs a production build on its own port, and this terminal's port was carrying the live walk. The WAITER half was green in pass 2; nothing this pass touched the offline paths. A later session should run it against a production build |
| P03269 | A queued order never invents a KOT number in the toast | read the `isQueued(r)` branch of `sendOrder` | ✅ | no fake bill line |
| P03270 | `/login` is never excluded from the offline cache by anything in this panel | grep `login` in tablet app.js → only the 401 redirect | ✅ |  ⚠ **EXPECTATION MOVED (pass 4).** There are now TWO mentions: the 401 redirect (line 202) and a COMMENT in the Settings sheet explaining that `/api/panel-logout` answers 303 → /login. Neither excludes /login from the offline cache, which is what the row is about. Re-run greps for a `DATA_PATHS`/exclusion change, not for the word |

### B5 · things the owner has already said NO to (P03271–P03282)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03271 | R1 — a FREE tile is still a number, a seat count and the word Free in a big square | read `tileHtml`'s free branch; nothing added, nothing shrunk | ✅ | the blank space is the design |
| P03272 | R1 — the `REJECTED (owner, 2026-08-07)` comment is still on `tileHtml` | read the comment block above `tileHtml` | ✅ | `verify:rejected` |
| P03273 | R2 — the "Your tables" strip still shows when the section covers the whole floor | read `renderMySection`; no "hide when everything" rule | ✅ | |
| P03274 | R4 — the 💳 and ⏻ tile controls are still ~22–25px and were NOT widened by this PR | diff `style.css` `.tacc, .tpay, .tclose` — unchanged | ✅ |  the confirm is the answer to a mis-tap · ⚠ **PASS 4:** the tile controls' WIDTH is untouched — `.tacc, .tpay, .tclose` still measure `clamp(22px, 17cqw, 36px)`, so R4 holds. What DID change (another terminal, 2026-08-22, `ea1bbf67`) is `.panel .btn` min-height 42px→44px, which is the DETAIL popup's buttons, not the tile's. Measured: no popup action row overflows at any of the five sizes |
| P03275 | R6 — `TILE_MIN_PX` is still NOT restated in this panel | grep `TILE_MIN_PX` in tablet app.js → absent | ✅ |  ⚠ **EXPECTATION MOVED (pass 4).** The name now appears ONCE, inside the `REJECTED (owner, 2026-08-07)` comment that tells the next person not to restate it — which is exactly what `verify:rejected` requires of a recorded rejection. The row's rule (the CONSTANT is not restated here) holds: there is no declaration, only the prohibition |
| P03276 | R25 — the floor legend still lists only 3 of the 5 colours | read `const LEG` | ✅ | he said no twice |
| P03277 | R25 — the `REJECTED` comment is still on `floorLegendHtml` | read the comment | ✅ | |
| P03278 | R7 — nothing in this panel adds a profile to the kitchen | grep `kitchen` in tablet app.js → nothing | ✅ | |
| P03279 | R22 — no ✕ close was padded or resized by this PR | diff style.css `.detail-x` / `.opt-x` / `.qdest-x` — unchanged | ✅ | |
| P03280 | R26 — nothing here changes the printed document's width | `printTableBill` builds no document of its own | ✅ | |
| P03281 | R27 — this panel offers no "delete a bill"; cancel/close are the only routes | grep `delete` — per-dish and per-ORDER delete only, both logged and recoverable | ✅ | |
| P03282 | `npm run verify:rejected` passes with the changes | run it | ✅ | |

### B6 · the access model, x-ray and permissions (P03283–P03295)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03283 | `app/tablet/layout.tsx` gates the route through `requirePanel("tablet", "/tablet")` | read the file | ✅ | |
| P03284 | `app/tablet/page.tsx` strips `?rid=` for a non-admin via `panelAdminRid` | read the file + `lib/panelGate` | ✅ | |
| P03285 | `?as=` is only honoured when `?rid=` is present (admin tabs only) | read `const PANEL_AS` | ✅ | |
| P03286 | `ridQ()` echoes rid/view/as on EVERY call so the tab never shifts restaurant | read `ridQ` | ✅ | owner bug 2026-07-03 |
| P03287 | `app/tablet/page.tsx` sets its own tab title ("Waiter tablet — Aevidine") | read `metadata` | ✅ | T15 sweep |
| P03288 | Hiding is never the only guard — every gated action is refused server-side too | read `app/api/tablet/[...path]/route.ts` `tabletPerm` on each | ✅ | |
| P03289 | `tshow`/`txray` replace every raw `!== "off"` check in the templates | grep `tperm(` — only inside `tshow`/`txray`/`kotOpsOn`/`payBill`/`genInvoice`/the ribbon | ✅ | |
| P03290 | An x-rayed FILLED control takes a cyan RING, never cyan TEXT on gold | read the `.qo-top.xray-off, .t-take.xray-off …` rule | ✅ | 1.15:1 was unreadable |
| P03291 | Every key in `XRAY_CAPS` has a row on /aevinite → Access → Waiter | cross-check the 8 keys against `lib/accessTree.ts` | ✅ | `tablet_invoice` correctly absent |
| P03292 | A waiter can never issue an invoice on any restaurant; the greyed control says so on hover | read the `isInvoice` branch of the mouseover handler | ✅ | owner 2026-08-04 |
| P03293 | `kotOpsOn`, `tabletTagsOn` and `tabletKhataOn` each read their OWN module ladder | read the three | ✅ | khata stopped sharing table_tags (mig 235) |
| P03294 | The x-ray ribbon names the person when `?as=` is confirmed by the server, never from the URL | read `TABLET_WHO.asName` | ✅ | |
| P03295 | `npm run verify:access` passes | run it | ✅ | |

### B7 · multi-tenant branding and billing compliance (P03296–P03300)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03296 | The top bar shows the RESTAURANT's own name; the generic word is what drops on a phone | read `index.html`'s `.brand` + the `@media (max-width: 760px)` rule | ✅ | no tenant wears another's branding |
| P03297 | `restDisplayName` never hardcodes a brand and never prints "undefined" | read `restDisplayName` | ✅ | |
| P03298 | `setRestName` strips the `*accent*` wordmark markers | read `setRestName` | ✅ | |
| P03299 | Nothing in this panel can erase, hide or bulk-delete an issued sale | grep every write path — cancel/close/unpay only, all logged | ✅ | CGST §132 |
| P03300 | `↩ Mark unpaid` requires a confirm AND a typed reason, and is logged | read `markBillUnpaid` | ✅ | a refund is a record, not a rub-out |

---

## C · WATCHING IT RUN — headless Playwright on port 4107 (P03301–P03375)

Driven by `scripts/sweep/t7-live.mjs` (throwaway, deleted after the run), signed in ONCE through
`loginAs(ctx, "tablet", "http://localhost:4107")` — the shared disk-cached diag session, so the
app's own login limit is never approached and the owner's phone is never pinged. French House is
the restaurant that is written to; every row it creates is removed by id in the same run.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03301 | `/tablet` loads and the panel iframe (`/panels/tablet`) appears | goto, wait for the frame | ✅ | |
| P03302 | The floor paints at least one `.tile[data-t]` within 30s | waitForSelector | ✅ | |
| P03303 | The page raises NO uncaught error while the floor loads | collect `pageerror` | ✅ | 0 errors. This is the check that caught the one thing I broke while fixing: two backticks inside an HTML comment that sits INSIDE a template literal ended the string and blanked the whole panel. Every static guard passed on the broken file; only the live walk saw it. `verify:tablet-taps` now compiles app.js with `vm.Script` |
| P03304 | The console logs no `[guest bell] not shown` warning | collect `console` | ✅ | the bell mounts |
| P03305 | The restaurant's own name is rendered in `#restName` and is not empty | read textContent | ✅ | |
| P03306 | `#restName` is not truncated to fewer than 6 visible characters at 1194px | measure scrollWidth vs clientWidth | ✅ | |
| P03307 | The four filter chips all render with a count | read `.fnav` texts | ✅ | All / ⚠ Needs / Active / Free |
| P03308 | Tapping "Free" leaves only tiles whose state is free/req | click, then read every tile's class | ✅ | |
| P03309 | Tapping "Active" leaves only tiles that are not free/req | click, then read | ✅ | |
| P03310 | Free + Active counts sum to the All count | read the three `em` values | ✅ | |
| P03311 | Tapping "⚠ Needs" never shows a tile with no call, request, new order or ready dish | click, inspect each tile's badges/strip | ✅ | |
| P03312 | Switching filters never leaves a stale tile from the previous filter on screen | click through all four, count `.tile` each time | ✅ | full render on flip |
| P03313 | Every tile is a SQUARE (width ≈ height within 2px) at 1194×834 | measure every tile's box | ✅ | |
| P03314 | No tile CLIPS its own content (scrollHeight ≤ clientHeight + 1) | measure every tile | ❌→✅ | **Pass 3.** A tile with BOTH a badge row and an action row needs 104px inside a 95px square, and `min-height: fit-content` does not save it (the box has a definite height from its 1/1 aspect-ratio) — so **＋ Take order and ✓ Accept were clipped by 11px** at 12-per-row on a desktop and 5px at 6-per-row on an iPad upright, on exactly the tables that have rung the bell AND placed a new order. Measured identical on a clean checkout, so it long pre-dates this sweep. Fixed with the tile's own "shed detail" ladder — the served WORDS go, the bar, the badges and the buttons stay. Never shrink `.t-act`: its height IS the tap target |
| P03315 | Every tile's number is visible and not ellipsised to fewer than 1 character | read each `.tnum` box + text | ✅ | |
| P03316 | The floor's first tile starts within 200px of the top at 1194×834 | measure `.tiles` bounding box | ✅ | "as big as possible" |
| P03317 | Tapping a FREE tile lands directly in the order builder | click, assert `.om.lite` exists | ✅ | |
| P03318 | `← back` from that builder returns to the FLOOR, not an empty popup | click `#omExit`, assert `.tiles` visible and no `.detail-pop` | ✅ | |
| P03319 | Tapping an OCCUPIED tile opens `.detail-pop` | click, assert | ✅ | |
| P03320 | The detail popup's ✕ is inside the popup's own box and is at least 40×40 | measure `.detail-x` | ✅ | |
| P03321 | The detail popup's action row is fully on screen at 1194×834 (every button's bottom ≤ viewport) | measure every `.dacts .btn` | ✅ | |
| P03322 | The bill bar (`.foot`) is fully on screen at 1194×834 | measure | ✅ | |
| P03323 | The orders list scrolls, and the actions + bill bar do NOT scroll with it | scroll `.detail-body`, re-measure `.dacts` | ✅ | fixed 2026-08-04 |
| P03324 | The backdrop closes the detail popup | click at (5,5) of `#panel`, assert gone | ✅ | |
| P03325 | ‹ › step to another table WITHOUT closing the popup | click `[data-step-table]`, assert `.detail-pop` still present and the heading changed | ✅ | |
| P03326 | ‹ › never step into a table with nothing on it | read every `[data-step-table]` target against the tile states | ✅ | |
| P03327 | 🧾 KOT ▾ opens the operations sheet with five rows in the documented order | click, read the `[data-kotop]` list | ✅ |  shift, merge, movekot, moveitem, split · ⚠ **EXPECTATION MOVED (pass 4).** The sheet lists **FOUR** rows here now, not five: `split_bill_enabled` is currently **false** on little French house, so \"Split the bill\" is hidden entirely — which is the documented behaviour (mig 248) and what P03139 asserts. Order confirmed: shift, merge, movekot, moveitem. Re-run this row against the SETTING, never a fixed count of five |
| P03328 | An ENABLED "Merge tables" row opens a picker with at least one destination | click through | ✅ | after **F2** |
| P03329 | An ENABLED "Change table" row opens a picker with at least one destination | click through | ✅ | after **F2** |
| P03330 | A DISABLED KOT row cannot be clicked into an empty screen | assert `disabled` on the rows whose predicate is false | ✅ | |
| P03331 | Each picker's ✕ returns to the table detail, not to the floor | click, assert `.detail-pop` with the table heading | ✅ | |
| P03332 | Browser Back from a picker returns to the table detail (one press) | `page.goBack()` | ✅ | |
| P03333 | Browser Back from the table detail returns to the floor (one press) | `page.goBack()` | ✅ | |
| P03334 | ⚡ Quick order opens the builder with no table chosen | click `#quickOrderBtn` | ✅ | |
| P03335 | ⚡ Quick order with an empty cart refuses SEND out loud | click `#sendOrder` (disabled) — the cart's own line explains it | ✅ | |
| P03336 | Adding a dish updates the tile badge, the cart pane and the View-order pill together | click a `.dish`, read all three | ✅ | |
| P03337 | The `−` badge removes exactly one and disappears at zero | click twice, read | ✅ | |
| P03338 | Adding a dish does NOT reset the browse scroll | scroll, add, re-read scrollTop | ⏭ | this restaurant's dish list is shorter than its scroller, so there is no browse scroll to lose. The first version of this check set scrollTop on an empty scroller, measured 0 against 3 and reported the app — it now asserts the scroll took before judging |
| P03339 | A category chip jumps to its section and the chip becomes active | click, read scrollTop + `.on` | ✅ | |
| P03340 | Scrolling moves the active chip (scroll-spy) | scroll, read `.on` | ✅ | |
| P03341 | The LAST category can reach the top of the scroller (the end spacer works) | click the last chip, measure | ✅ | |
| P03342 | Searching collapses the sections into one flat result list and hides the rail | type, read | ✅ | |
| P03343 | Clearing the search returns to the remembered browse position | clear, read scrollTop | ✅ | |
| P03344 | An accented dish is found by its unaccented spelling | search a folded term, assert a hit (skipped if this restaurant has no accented dish) | ⏭ | French House's menu has no accented dish title; the fold is covered statically by P03156 |
| P03345 | The ✎ on a dish tile opens the options popup, not the quick-add | click `.dedit`, assert `#optOverlay` and an unchanged cart | ✅ | |
| P03346 | The options popup's quantity stepper clamps to 1…99 | click `−` at 1 and `+` at 99 | ✅ | |
| P03347 | The options popup's ⚠ allergy row ends in ＋ Other | read the last chip | ✅ | |
| P03348 | ＋ Other with nothing typed says why and does not close | click Add, read the error line | ✅ | |
| P03349 | ＋ Other with a duplicate says it is already listed | add the same word twice | ✅ | |
| P03350 | The options popup's backdrop closes it and leaves the cart untouched | click the backdrop, read the cart | ✅ | |
| P03351 | SEND for a table asks a confirm before anything is posted | click, assert `#confirmOverlay` visible and no network POST yet | ✅ | |
| P03352 | Cancelling that confirm keeps the cart intact | click Cancel, read the cart | ✅ | |
| P03353 | A second confirm raised while one is open SHAKES the first instead of vanishing | fire two, assert `.cf-nudge` | ✅ | |
| P03354 | A ⚡ quick order's SEND opens the table picker and does NOT also confirm | click, assert `.qdest-overlay` and no `#confirmOverlay` | ✅ | |
| P03355 | The quick picker labels a busy table "joins its bill" and a merged one "joins T…'s bill" | read the `small` texts | ✅ | |
| P03356 | The quick picker's ✕ closes it and leaves the cart intact | click, read | ✅ | |
| P03357 | Sending a real order returns a KOT number and the toast quotes it | place one order on the sweep's own table | ✅ | KOT issued, then the session was closed to put the table back |
| P03358 | That order appears on the floor tile within 5s without a manual refresh | watch the tile | ✅ | realtime |
| P03359 | The tile's ✓ Accept flips the tile out of "New order" | click, watch | ✅ | |
| P03360 | The tile's ✓ Accept on a table with nothing to accept SAYS so | force the empty case (accept twice) | ❌→✅ | **F1** proved live: with that one slice read failing (in a context with the service worker BLOCKED — a request the service worker answers never reaches the fault injection), the tap now says "Nothing left to accept here — refreshing this table." Before the fix: nothing |
| P03361 | "🍽️ Serve all" flips every dish to served instantly and offers an undo bar | click, read `#lfh-undobar` | ✅ | |
| P03362 | The undo bar's Undo puts the dishes back | click Undo, re-read the pills | ✅ | |
| P03363 | The undo bar does not cover the detail popup's action row at 1194×834 | measure both boxes | ✅ | measured IN THE FRAME (undobar.js appends to the IFRAME's body — a page-level lookup finds nothing and reports "no bar" forever, which is what the first version did): bar 68px at y696–764, bill bar y547–634 on a 780px screen. No overlap |
| P03364 | 💳 Mark bill paid opens the payment sheet (one step, not two) | click, assert `.pay-overlay` and no confirm | ✅ | |
| P03365 | The payment sheet's four method buttons are each ≥ 44px tall | measure | ✅ | 64px |
| P03366 | "Other → Split the payment" refuses a short total out loud and names the shortfall | type 1, click Take payment | ✅ | |
| P03367 | A double-tap on 💳 opens exactly ONE payment sheet | click twice fast, count `.pay-overlay` | ✅ | #18 |
| P03368 | 💳 Mark bill paid on a table with an un-accepted order EXPLAINS itself on tap | click it | ❌→✅ | **I1** — before, a disabled button swallowed the tap with a hover-only title |
| P03369 | ✕ Close table asks a confirm naming the table, and warns when the bill is unpaid | click, read the text | ✅ | |
| P03370 | Closing a table frees its tile instantly (optimistic) and it stays free after the reconcile | click through, watch the tile for 5s | ✅ | |
| P03371 | The ☰ drawer opens, shows the signed-in name, the restaurant, the clock and the build tag | open, read | ✅ | build tag = the app.js content hash |
| P03372 | The drawer's Build tag CHANGES after this PR's edits (it reads the real hash) | compare before/after `verify:panel-cache -- --fix` | ✅ | the drawer read `tablet 6ce540b3` after this PR's edits — the real content hash, not a typed string |
| P03373 | ⚙️ Settings → Sign out is a POST form, not a link | read the element | ✅ | T9 improvement 13 |
| P03374 | The drawer's 🚩 Report an issue row exists at every width | open the drawer at 1194 and at 360 | ✅ | T4 sweep |
| P03375 | Every row this run created was removed by id in the same run | read the runner's cleanup block + re-read the table | ✅ | the session was CLOSED (never deleted — the issued-bill rule) |

---

## D · SCREENSHOTS I ACTUALLY READ (P03376–P03425)

Two devices for every screen: **desktop 1280×800** (mouse, `pointer: fine`) and **Samsung A35
360×780 dpr3** (touch). Both orientations of the waiter tablet — **1194×834** and **834×1194** —
are measured too, with and without each banner that can appear. Shots go to the session scratchpad
and are deleted after they are read.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03376 | Desktop 1280×800: the floor reads as a floor — tiles square, numbers legible, nothing clipped | screenshot + Read | ✅ | |
| P03377 | Desktop 1280×800 LIGHT skin: same, with no white-on-white and no invisible text | set `lfh_panel_theme=light`, screenshot + Read | ✅ | |
| P03378 | A35 360×780: exactly TWO tiles per row (the owner's fixed rule) | screenshot + Read + measure | ✅ | |
| P03379 | A35 360×780 sideways (780×360): exactly FOUR tiles per row | screenshot + Read + measure | ✅ | |
| P03380 | iPad 1194×834: SIX tiles per row (the touch cap), not twelve | screenshot + Read + measure | ✅ | owner 2026-08-05 |
| P03381 | iPad 834×1194 (upright): still six per row, tiles still square | screenshot + Read | ✅ | |
| P03382 | A35: the filter chips are one scrollable line and no word is cut through | screenshot + Read | ✅ | |
| P03383 | A35: the legend WRAPS to two lines and every key shows its word | screenshot + Read | ✅ | owner 2026-08-15 — no horizontal scroll |
| P03384 | A35: the restaurant's name is readable, not "lit…" | screenshot + Read | ✅ | |
| P03385 | A35: the ⚡ button is the bolt only, and ≥44px | screenshot + measure | ✅ | |
| P03386 | A35: the first tile starts within 200px of the top | screenshot + measure | ✅ | |
| P03387 | A35: the last tile row is not under the phone's navigation bar | screenshot + measure against `--sab` | ✅ | |
| P03388 | iPad 1194×834: the tile action row is ≥26px tall and ＋ Take order is not cut mid-word | screenshot + Read | ✅ | T14 fix holds |
| P03389 | iPad 1194×834 with the ✓ present: the label degrades to ＋ rather than "＋ Take orde" | screenshot + Read | ✅ | T12 fix holds |
| P03390 | A merged tile shows "⇄ T…" and its progress bar is NOT sliced | screenshot + Read | ✅ | |
| P03391 | A merged tile's NUMBER is not the thing that gets ellipsised | screenshot + Read | ✅ | T11 fix holds |
| P03392 | A tagged (VIP) tile's number sits at the same height as its untagged neighbours | screenshot + measure | ✅ | T11 fix holds |
| P03393 | A tagged tile shows ONE marker (the ribbon), not a ribbon and a pill | screenshot + Read | ✅ | owner 2026-08-15 |
| P03394 | Desktop: a selected tile's ring/glow is not sliced flat on the far-left/far-right column | screenshot + Read | ✅ | the 20px padding holds |
| P03395 | The table detail popup at 1194×834: header, orders, actions and bill all on screen | screenshot + Read | ✅ | |
| P03396 | The table detail popup at 360×780: the ORDERS are the part you can see | screenshot + Read | ✅ | the 95dvh / three-per-row fix holds |
| P03397 | The detail popup at 360×780: six actions sit three-and-three, none on a third row | screenshot + Read | ✅ | |
| P03398 | The detail popup at 834×1194: same, nothing clipped | screenshot + Read | ✅ | |
| P03399 | The detail popup with the ADMIN ribbon showing: the actions are still fully on screen | admin `?rid=`, screenshot + Read | ✅ |  measured, not reasoned: with the admin ribbon up (38px iPad / 78px A35), every action and the bill bar are still fully inside the viewport in BOTH orientations. The popup is `position: fixed; inset: 0`, so a top band cannot narrow it — and the admin's SIX actions still sit three-and-three at 360px · ⚠ **PASS 4: `⏭`.** Could not re-measure. The admin view needs the console's **act-as cookie** as well as the admin cookie — `requirePanel` says so in its own comment (\"only via the admin console's act-as flow… No scope → back to /aevinite\") and `/manager?rid=` behaves identically, so this is not a tablet fault. Setting that cookie is a browser-wide write this terminal would not make on a shared stack. A later session should enter through /aevinite → Restaurants → the panel link and re-measure |
| P03400 | The detail popup with the OFFLINE bar showing: the actions are still fully on screen | offline the context, screenshot + Read | ✅ | measured with the offline bar really on screen (lfhOffBar, 50px), both orientations and the phone: dacts, foot, ＋ Take order and ✕ Close table all inside |
| P03401 | The detail popup with the UNDO bar showing: the bill bar is not covered | trigger an undo, screenshot + Read | ✅ | see P03363 — measured in the frame, no overlap |
| P03402 | The order builder at 1194×834: SEND TO KITCHEN is fully on screen and ≥44px | screenshot + measure | ✅ | |
| P03403 | The order builder at 834×1194: SEND TO KITCHEN is fully on screen | screenshot + measure | ✅ | |
| P03404 | The order builder at 360×780: the View-order pill is on screen and above the nav bar | screenshot + measure | ✅ | |
| P03405 | The view-order screen at 360×780: SEND scrolls INTO view and is never behind the nav bar | screenshot + measure | ✅ | the 84px reserve holds |
| P03406 | The order builder with the ADMIN ribbon: SEND is still fully on screen | screenshot + measure | ✅ |  SEND fully inside at 1194×834, 834×1194 and 360×780 with the admin ribbon up (bottom 359 of 834 / 359 of 1194 / 470 of 780), ≥50px tall in every case · ⚠ **PASS 4: `⏭`** — same reason as P03399 (the admin view needs the act-as cookie). The ribbon-less geometry WAS re-measured at all three sizes and every action stays on screen |
| P03407 | The order builder with the OFFLINE bar: SEND is still fully on screen | screenshot + measure | ✅ | same three sizes with the offline bar up — SEND inside and ≥50px in all of them |
| P03408 | ⚡ Quick order held SIDEWAYS on a touch device: menu left, order right, no View-order pill | 1194×834 touch, screenshot + Read | ✅ | owner 2026-08-05 |
| P03409 | ⚡ Quick order sideways: the whole screen starts at x≥0 (the `.quick` class-collision lesson) | measure the box | ✅ | |
| P03410 | ⚡ Quick order sideways: every category chip is visible (the rail wraps) | screenshot + Read | ✅ | |
| P03411 | ⚡ Quick order sideways: SEND at the bottom of the right pane is on screen | measure | ✅ | |
| P03412 | ⚡ Quick order UPRIGHT on a phone keeps the pill and the review screen | 360×780, screenshot + Read | ✅ | his choice when asked |
| P03413 | The payment sheet at 360×780: every method button and Cancel are reachable | screenshot + Read | ✅ | |
| P03414 | The discount modal at 360×780: all three boxes and Apply are on screen | screenshot + Read | ✅ | |
| P03415 | The discount modal's red cap note is legible in BOTH skins | screenshot both + Read | ✅ | |
| P03416 | The KOT ▾ sheet at 360×780: five rows, each ≥44px, none clipped | screenshot + measure | ✅ | |
| P03417 | The ☰ drawer at 360×780: every row reachable, Sign out visible | screenshot + Read | ✅ | |
| P03418 | LIGHT skin: the order card, KOT chip and bill box are all light surfaces (no dark islands) | screenshot + Read | ✅ | |
| P03419 | LIGHT skin: the MRP stamp is visible (it borrows theme tokens, not a fixed colour) | screenshot + Read | ✅ | |
| P03420 | LIGHT skin: the merged ⇄ chip is readable (violet on violet was the trap) | screenshot + Read | ✅ | |
| P03421 | LIGHT skin: the admin ribbon's own words are readable on its amber wash | screenshot + Read | ✅ | T11 fix holds |
| P03422 | LIGHT skin: a FREE tile's number and the word Free are readable across a room | screenshot + Read | ✅ | |
| P03423 | DARK skin remains the tablet's non-default (light is the panel default) and both are complete | screenshot both + Read | ✅ | not a finding either way |
| P03424 | The skeleton tiles on first paint look like tiles, not a blank grid | screenshot the first 300ms + Read | ✅ | 6 skeleton tiles on first paint with the summary read held for 4s |
| P03425 | The streaming "opening…" detail looks like the real detail, not a spinner line | screenshot + Read | ✅ | owner 2026-07-03 |

---

## E · TRACING A CHANGE ACROSS PANELS (P03426–P03475)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03426 | `app/tablet/page.tsx` and `app/manager/page.tsx` use the same `panelAdminRid` / `panelIframeSrc` pair | read both | ✅ | |
| P03427 | `app/tablet/layout.tsx` and the kitchen's layout both call `requirePanel` | read both | ✅ | |
| P03428 | The tablet's tile states are the manager's `floorTileHtml` states, name for name | diff the two state maps | ✅ | free/req/new/ready/prep/bill/done/seated/waiting |
| P03429 | Both floors read `settings.floor_per_row` and both cap it at 6 on a touch device | diff the two `floorPerRow` functions | ✅ | 2026-08-16 |
| P03430 | Both floors draw occupied off-plan tables and neither draws free ones | diff `floorTableList` with the manager's | ✅ | |
| P03431 | Both floors treat a merged CHILD as its party, never as free | diff `tileIsOpen` with the manager's | ✅ | mig 249 |
| P03432 | Both panels compute due as discount-before-tax | diff against `billMath` in the manager | ✅ | |
| P03433 | The tablet's `discPct` and the manager's both come from `billdoc.js` | grep both | ✅ | one definition |
| P03434 | The tablet's `combineBillLines` equivalent is billdoc's, not a local copy | `printTableBill` builds no lines of its own | ✅ | |
| P03435 | The tablet's printed bill and the manager's go through the SAME `billData` → `billDocHtml` | diff `printTableBill` with `printBill` | ✅ | one assembler |
| P03436 | Both panels print the live party bill from `partyOrders(t).filter(o => o.status !== "cancelled")` | diff | ✅ | the same rows, so the same paper |
| P03437 | A waiter never issues an invoice; the manager's Print issues-then-prints | read both | ✅ | deliberate difference (owner 2026-08-04) |
| P03438 | Both panels' `NEXT_STATUS` are forward-only and identical | diff with `public/panels/kitchen/app.js` | ✅ | |
| P03439 | `STATUS_WORD` reads the same on the tablet, the manager and the kitchen | diff the three | ✅ | new/cooking/ready/served |
| P03440 | The kitchen's ✓ and the tablet's ✓ Serve hit the same endpoint with the same payload | diff the two `api` calls | ✅ | `/items/:id/status` |
| P03441 | `ALLERGENS` is the same six slugs in the tablet, the manager and `lib/allergens.ts` | diff | ✅ | |
| P03442 | `normAlg` behaves identically in the tablet and the manager (commas → spaces, "no " stripped) | diff | ✅ | |
| P03443 | Every allergy row in BOTH panels ends in ＋ Other | `verify:taps` counts ≥3 per panel | ✅ | |
| P03444 | A per-dish allergy set on the tablet reaches the KOT and NOT the bill | trace `note` → `/order` → `lfh_price_order` → `kotDocHtml` | ✅ | |
| P03445 | A whole-order allergy set on the tablet lands on every line of the ticket | trace `allergies[]` → the server | ✅ | |
| P03446 | The tablet's `tperm` keys all exist in `lib/accessTree.ts` | cross-check the 9 keys | ✅ | |
| P03447 | The tablet's `kotOpsOn` mirrors the server's `tableOpsTabletAllowed` | diff | ✅ | |
| P03448 | `tabletKhataOn` mirrors `khataLadder()` in `lib/tableTags.ts` | diff | ✅ | mig 235 |
| P03449 | `tabletTagsOn` mirrors the manager's table-tag ladder | diff | ✅ | mig 166 |
| P03450 | `resolveTaxMode` matches `lib/tax.ts` AND `lfh_resolve_tax_mode` case for case | diff all three | ✅ | mig 270 |
| P03451 | `effRate` matches `lib/tax.ts` and `lfh_effective_tax_rate` | diff all three | ✅ | |
| P03452 | `banquetRate` matches `lfh_banquet_tax_rate` | diff | ✅ | mig 239 |
| P03453 | `patchTileFromSlice`'s state precedence matches the summary RPC (mig 136) | diff the ladder against the SQL | ✅ | |
| P03454 | A tablet close emits an `ops` breadcrumb the manager and kitchen both act on | read the server handler's `rt_emit` | ✅ | |
| P03455 | A tablet order emits a breadcrumb naming the table, so the manager patches ONE tile | read `/order`'s emit | ✅ | |
| P03456 | Every tablet write handler calls `invalidateFloor(rid)` | grep the tablet route file | ✅ | `verify:floor` |
| P03457 | `npm run verify:floor` passes | run it | ✅ | |
| P03458 | `npm run verify:table-ownership` passes | run it | ✅ | |
| P03459 | `npm run verify:tablet-parity` passes | run it | ✅ | |
| P03460 | `npm run verify:tablet-wants-in` passes | run it | ✅ | |
| P03461 | `npm run verify:merged-floor` passes | run it | ⏭ |  not this territory — `verify:merged-floor` drives the MANAGER's floating tables (`[data-floating-table]`) and needs a manager fixture; it times out from here whatever this panel does · ⚠ **PASS 4:** still `⏭`, unchanged reason — `verify:merged-floor` drives the MANAGER's floating tables and needs a manager fixture. This terminal's own merge walk covered the tablet side instead (P18332–P18373, 50 checks on a real party) |
| P03462 | `npm run verify:board-sig` passes (the redraw fingerprint was not narrowed) | run it | ✅ | |
| P03463 | `npm run verify:twins` passes (the three panel routes did not drift) | run it | ✅ | |
| P03464 | `npm run verify:ui` passes | run it | ✅ | |
| P03465 | `npm run verify:panel-cache` passes AFTER the `?v=` hashes were rewritten | run `-- --fix` then plain | ✅ | `-- --fix` rewrote tablet/index.html → app.js ?v=6ce540b3, then the plain run passed 55/55 |
| P03466 | `npm run verify:busy` passes (no fixed fast poll was introduced) | run it | ✅ | |
| P03467 | `npm run verify:static` passes | run it | ✅ | |
| P03468 | The guest bell (`guestbell.js`) is shared with the manager and reads nothing of its own | read `syncGuestBell` | ✅ | an adapter, not a feature |
| P03469 | The guest bell shows a sectioned waiter only their own tables — the manager's shows all | read the `mine` Set | ✅ | deliberate difference, documented |
| P03470 | The undo bar (`undobar.js`) is the shared one, used by the tablet, manager and kitchen | grep the three | ✅ | |
| P03471 | `billcustomer.js` is shared between the tablet and the manager | grep both index.html files | ✅ | |
| P03472 | The tablet's `?v=` list in `index.html` names every shared script the manager loads for the same job | diff the two script lists | ✅ | |
| P03473 | A tablet-side settings change is picked up by an OPEN manager panel through the same `menu` topic | read both `menu` handlers | ✅ | |
| P03474 | A table renamed in the manager reaches the tablet's tiles, popup heading and printed bill | trace `settings.table_names` → `tname` → `tableLabel` → `tableDisp` | ✅ | renames reach paper |
| P03475 | The three guest menu doors are untouched by anything in this PR | `git diff --name-only` — nothing outside my territory | ✅ | |

---

## F · MY OWN JUDGMENT — a real restaurant, one hand, a full tray (P03476–P03500)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03476 | Nothing on the floor screen needs two hands | walk every control: tiles, chips, ⚡, ☰ — all single-tap | ✅ | |
| P03477 | Nothing in the table popup needs two hands | ‹ ›, ✕, KOT ▾, the six actions — all single-tap | ✅ | |
| P03478 | Taking an order needs no typing unless the waiter chooses to | dish tiles, chips, steppers — typing is only search, price and notes | ✅ | |
| P03479 | Settling a bill is two taps plus the method — never three questions | 💳 → method → done | ✅ | |
| P03480 | Closing a table is exactly two taps, and it warns when money is owed | ⏻/✕ → confirm | ✅ | |
| P03481 | A waiter can hand a guest their bill without finding a manager | 🖨 Print bill exists on the detail | ✅ | T7 F9, already shipped |
| P03482 | A waiter who mis-taps Serve can take it back within seconds | the undo bar | ✅ | |
| P03483 | A waiter who mis-taps Accept can take it back | the undo bar's ✋ | ✅ | |
| P03484 | A waiter who mis-taps Mark-paid can take it back | `offerPayUndo` | ✅ | |
| P03485 | A waiter walking a section is never shown a table they cannot serve | sections applied in the floor, the bell, and every picker | ✅ | after **F2** the KOT rows agree too |
| P03486 | A waiter can tell a merged table from a solo one at a glance, at every tile size | the ⇄ never drops, only its list does | ✅ | |
| P03487 | A waiter can tell a paid table from an owing one across the room | the ring colour, not a word | ✅ | |
| P03488 | A waiter who loses signal keeps working, and is told honestly what is saved vs sent | the outbox + the "waiting to send" block | ✅ | |
| P03489 | A waiter is never told "syncing" — the words are what is SAFE and what they must do | read every offline message | ✅ | T15 P3 |
| P03490 | A waiter is never shown a raw error code | `errText` + the busy/offline branches | ✅ | |
| P03491 | A waiter never sees the word "database" | grep the panel's user-facing strings | ✅ | T15 P1 fix holds |
| P03492 | Nothing on this panel can make a sale disappear | only cancel/close/unpay, all logged | ✅ | |
| P03493 | A brand-new waiter can find Sign out without being shown | ☰ → ⚙️ Settings → Sign out | ✅ | two taps |
| P03494 | A waiter on a phone can still report a spill or a broken card machine | the drawer's 🚩 row | ✅ | T4 sweep |
| P03495 | The busiest control on the screen (＋ Take order) is the biggest one on the tile | read `.t-take { flex: 1 1 auto }` | ✅ | |
| P03496 | The most dangerous control (⏻ Close) sits away from it, on the far side | read `.tclose { order: -1 }` | ✅ | the manager's rule |
| P03497 | A ⚡ quick order cannot be sent to a table the server will refuse | the picker uses the floor plan only | ✅ | |
| P03498 | A waiter is never left staring at an empty screen with no words | the floor, the pickers, the menu and (after **I2**) the quick picker all have an empty state | ❌→✅ | **I2** closed the last one |
| P03499 | A tap on the most repeated money button always produces something on screen | 💳 Mark bill paid, in every state | ❌→✅ | **I1** closed the un-accepted case |
| P03500 | A waiter's tablet left on a table all shift costs the restaurant nothing while nobody looks at it | the hidden-tab guard on the 60s backstop + the socket teardown | ✅ | ~1,440 reads a day saved, T4 sweep |

---

# SWEEP #7 — NEW CHECKS (P18101 onward)

**Block:** `P18101`–`P18600` (500 ids, this terminal's alone). **Used: `P18101`–`P18404`.**
`P18405`–`P18600` are FREE and the ground they should cover is listed at the bottom — I did not pad
to 500, because §8 of the sweep rules says there is no quota and a check invented to fill a block is
worth less than an honest gap.

**What these 304 checks cost to run:** each geometry/skin check ran on every device it can mean
something on, so the 304 ids were EXECUTED about 800 times. Every figure in a note below is measured,
not reasoned — where a note quotes pixels or a ratio, that is what the running panel reported.

**Devices:** desktop 1280×800 (mouse, `pointer: fine`) · iPad 1194×834 and 834×1194 (touch) ·
Samsung A35 360×780 and 780×360 (touch, dpr 3). **Skins:** light (the panel default) and dark.

## G · THE FLOOR, MEASURED (P18101–P18135)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P18101 | The floor loads with NO uncaught page error | collect `pageerror` while the tiles paint, on every device | ✅ | 0 errors on all five sizes, both skins |
| P18102 | No `[guest bell] not shown` warning — the bell really mounts | collect console warnings | ✅ | clean everywhere; this is the warning that once hid a dead bell |
| P18103 | No leaked code text on the floor | match the rendered `innerText` against `-->`, `${`, undefined, NaN, [object Object] | ✅ |  |
| P18104 | The restaurant's OWN name is rendered | read `#restName` | ✅ | "little French house" on all five |
| P18105 | …and it is not a placeholder | assert it is not empty and not the neutral fallback | ✅ |  |
| P18106 | …and it is not truncated at this width | `scrollWidth − clientWidth` on `#restName` | ✅ | 0px overflow at every size, 360px included |
| P18107 | At least one tile paints | count `.tile[data-t]` | ✅ | 30–33 tiles (the extras are occupied off-plan tables) |
| P18108 | EVERY tile is a square | measure every tile, abs(w−h) ≤ 2.5px | ✅ | 176×176 at 6-per-row, 162×162 on the A35, 162.9 on the desktop at 7 |
| P18109 | NO tile clips its own content | `scrollHeight − clientHeight` on every tile | ✅ | 0px on all five sizes — the four-row shed-detail ladder holds (this is the pass 3 fix P03314) |
| P18110 | Every tile shows its number | assert `.tnum` has text AND an `offsetParent` | ✅ |  |
| P18111 | No tile's ACTION ROW hangs below the tile | compare `.t-act` bottom against the tile's bottom | ✅ | act.b=755 vs tile.b=769 on a merged busy tile at 1194px |
| P18112 | ＋ Take order is never cut off the bottom | compare `.t-take` bottom against the tile's | ✅ |  |
| P18113 | A tile's action row is at least 24px tall | measure `.t-act` height | ✅ | the `clamp(26px, 44cqw, 40px)` floor holds; never shrink it (T14) |
| P18114 | No progress bar is sliced | `.tstrip` bottom inside the tile AND width ≥ 8px | ✅ | 146×6px at 1194, 140×6 on the A35 |
| P18115 | Every merged tile keeps its ⇄ mark | find `.t-merged` tiles, assert `.tmerge` contains ⇄ | ✅ | proved on a real party — see P18345 |
| P18116 | A merged tile NEVER reads Free | assert no tile carries both `t-merged` and `t-free` | ✅ | mig 249's whole point |
| P18117 | No tile's number is squeezed under 8px | measure `.tnum`'s box | ✅ |  |
| P18118 | The floor draws the right number per row | read the computed `--per-row` | ✅ | desktop 7 (the restaurant's own setting) · iPad 6 (the touch cap) · A35 2 · A35 sideways 4 |
| P18119 | …and the first ROW really holds that many | group the tiles by rounded `y` and count the first band | ✅ | the variable and the geometry agree — a `--per-row` that the grid ignores would pass P18118 alone |
| P18120 | The tiles start close to the top | measure the gap between the top bar's bottom and the grid's top | ✅ | 154px iPad upright · 137px A35 · **87px A35 sideways** (+57px bar = 144px, exactly what sweep #6's J2 claimed) |
| P18121 | The page NEVER scrolls sideways | `documentElement.scrollWidth` ≤ `innerWidth` | ✅ | equal at all five sizes |
| P18122 | Four filter chips, each with a count | read `.fnav` | ✅ | All / ⚠ Needs / Active / Free |
| P18123 | No filter chip's word is cut through | `scrollWidth − clientWidth` per chip | ✅ | 0 everywhere, 360px included (the T4 fault stays fixed) |
| P18124 | Every filter chip reaches a 44px finger target | measure each chip's height | ✅ | 44px — `ea1bbf67` raised `.floor-nav button` from 42px on 2026-08-22 and nothing wrapped |
| P18125 | Every legend key is inside the screen | each `.lgi`/`.lgcap` right edge ≤ viewport | ✅ | the legend WRAPS on a phone rather than sliding (owner 2026-08-15) |
| P18126 | ⚡ Quick order reaches 44px | measure `#quickOrderBtn` | ✅ | 130.8×44 with the word, 44×44 as the bolt alone |
| P18127 | The ☰ is a 44px target | measure `#hamburger` | ✅ | 44×44 at every width |
| P18128 | Every top-bar icon button is a 44px target | measure every visible `.theme-toggle` | ✅ | 44×44 — 🚩, 🔔 and ☀/🌙 all raised from 38px on 2026-08-22 |
| P18129 | The top bar is still ONE row | measure `.topbar` height after those three were enlarged | ✅ | 57–59px at every width; nothing wrapped, which was R41's worry |
| P18130 | ⚡ shows its word only above 430px | read whether `.qo-word` has an `offsetParent` | ✅ | word at 834/780/1194/1280, bolt only at 360 — the tenant's name wins the bar |
| P18131 | The Free filter shows ONLY free/req tiles | click it, then read every remaining tile's class | ✅ |  |
| P18132 | The Active filter shows NO free tile | click, read | ✅ |  |
| P18133 | Every ⚠ Needs tile really wants something | click, then assert each tile has a badge, a ✓, or a ready/new/req state | ✅ | no tile appears in Needs without a reason a waiter can see |
| P18134 | Active + Free = All | read the three counts | ✅ | 33/6/7/26 and 30/4/4/26 across runs — the board is live, the arithmetic held every time |
| P18135 | The All count matches the tiles actually drawn | count `.tile[data-t]` against the All chip | ✅ | a count that disagrees with the grid is how the off-plan phantom bug showed |

## H · THE TABLE DETAIL POPUP (P18136–P18175)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P18136 | Tapping an occupied tile opens the detail popup | click, wait for `.detail-pop` | ✅ |  |
| P18137 | The popup does not clip its own content | `scrollHeight − clientHeight` | ✅ | 0px at all four sizes |
| P18138 | The popup is fully inside the screen | measure its box against the viewport | ✅ | y=84 b=716 in an 800px desktop; the card is a fixed-height flex column |
| P18139 | The popup ✕ is at least 40×40 | measure `.detail-x` | ✅ | 40×40 |
| P18140 | …and it is inside the popup's own box | compare its left/right against the card's | ✅ | x=863 r=903 inside a card spanning 360–920 |
| P18141 | The heading names the table that was opened | read `.phead h2` | ✅ |  |
| P18142 | No leaked code text in the popup | scan the rendered text | ✅ |  |
| P18143 | EVERY action button is fully on screen | measure each `.dacts .btn` bottom against the viewport | ✅ | at 1194×834, 834×1194, 1280×800 and 360×780 |
| P18144 | Every action button reaches a 44px finger target | measure each button's height | ✅ | 44–48px |
| P18145 | The bill bar is fully on screen | measure `.foot` | ✅ | b=681 of 780 on the A35 |
| P18146 | The action row has buttons at all | read their labels | ✅ | ＋ Take order · − Discount whole bill · 🖨 Print bill · ✕ Close table (a waiter); the admin also gets 🧾 and 💳 |
| P18147 | The ORDERS get at least as much room as the buttons | compare the scroll body's height against the action stack's | ✅ | 294px of orders vs 100px of buttons on the A35 — the T4 rule holds |
| P18148 | The six actions sit on at most TWO rows on a phone | group the buttons by rounded `y` | ✅ | 2 rows; the `flex: 1 1 28%` three-and-three rule holds even with `min-height: 44px` |
| P18149 | The bill line never ends in a bare dash | read `.billbox` | ✅ | "bill #1066 ● new" / "bill not numbered yet" |
| P18150 | The bill number is printed once per card | count it in the card | ✅ | the T14 double-print stays fixed |
| P18151 | 💳 Mark bill paid is NEVER rendered disabled | read the button's `disabled` | ✅ | sweep #6's I1 holds — a disabled button eats the tap on a touch panel |
| P18152 | …and tapping it with an un-accepted order refuses OUT LOUD | tap it, read the toast, assert no sheet opened | ✅ | "Accept the order first — a bill can only be paid once the kitchen has it." |
| P18153 | The ‹ › steppers are 40px targets | measure them | ✅ | 40×40 |
| P18154 | › keeps the popup OPEN | click, assert `.detail-pop` survives | ✅ | no close-then-reopen |
| P18155 | › really moves to another table | compare the heading before and after | ✅ | T17 → T18 |
| P18156 | …and only into a table with something on it | read what it landed on | ✅ | stepping into an empty popup was caught in review 2026-08-04 and stays fixed |
| P18157 | The KOT sheet lists its operations | read `[data-kotop]` | ✅ | see P03327 — FOUR rows today, because Split is switched off for this restaurant |
| P18158 | …in the documented order | read the ids in order | ✅ | shift, merge, movekot, moveitem |
| P18159 | Every KOT row is a 44px target | measure each row | ✅ | 64px desktop, 73.9px A35 |
| P18160 | No KOT row is off the bottom of the screen | measure each row's bottom | ✅ |  |
| P18161 | No KOT row leaks code text | scan the row text | ✅ |  |
| P18162 | Every ENABLED KOT row opens a screen that either OFFERS something or SAYS why not | click each enabled row and read what it drew | ✅ | shift → 26 free tables · merge → 3 destinations · movekot → "#1459 · Order 1 · ₹483" · moveitem → the dish, grouped by KOT. No row opened an empty screen |
| P18163 | …and every one of them gives a way back | assert `.picker-back` exists on each | ✅ |  |
| P18164 | …and ✕ from each returns to the TABLE DETAIL, not the floor | click ✕, read the heading | ✅ | back on T17 from all four |
| P18165 | A disabled KOT row really carries the `disabled` attribute | read it | ✅ | it cannot be walked into an empty screen |
| P18166 | The KOT sheet opens from a table detail | click `#kotMenuBtn` | ✅ |  |
| P18167 | ONE Back returns from the picker to the table detail | `history.back()` in the page, then read the DOM | ✅ | `page.goBack()` is the WRONG tool here — the back-stack uses pushState inside the iframe, so there is no `load` to wait for and it times out; that cost this pass four false reds |
| P18168 | A second Back returns to the floor | back again, count the tiles | ✅ | 30 tiles, popup gone |
| P18169 | Picking an order advances to the destination step | click a `[data-pickorder]` | ✅ | heading became "Move order →" |
| P18170 | ONE Back peels the destination step back to the ORDER LIST | back once | ✅ | "Move an order" — the step-1 layer is not leaked (audit 2026-07-08) |
| P18171 | A second Back reaches the detail, never straight to the floor | back again | ✅ | T17 |
| P18172 | ✕ Close table asks a confirm first | click it | ✅ |  |
| P18173 | …and the confirm names the table | read the text | ✅ | "Close T17 and free it?" |
| P18174 | Back cancels the confirm and leaves the detail up | back once | ✅ | the confirm is its own layer |
| P18175 | A second question never builds a second box | fire the confirm twice, count `#confirmOverlay` | ✅ | 1 box; the first promise is answered, not orphaned |

## I · THE SHEETS: CONFIRM, DISCOUNT, TABLE TYPE, DRAWER, SETTINGS (P18176–P18215)

Every row measured at **iPad 1194×834** and **A35 360×780**.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P18176 | ✕ Close table asks a confirm first | click, wait for the box to be un-hidden | ✅ | asserted on `hidden`, never `offsetParent` — a fixed element's offsetParent is null even when it is on screen |
| P18177 | The confirm names the table | read it | ✅ |  |
| P18178 | The Yes button says what it will DO | read its label | ✅ | "Close table", not "OK" |
| P18179 | A multi-line confirm keeps its LINES | read the computed `white-space` on `.confirm-box p` | ✅ | `pre-line` — the box sets textContent on purpose, so this is what stops a three-part question collapsing into one grey block |
| P18180 | The confirm box is fully on screen | measure it | ✅ |  |
| P18181 | The confirm sits above every other overlay | read its computed z-index | ✅ | 100002 — above the dish-edit modal (99990) and the options popup (100000), which is what keeps "Send back to kitchen" tappable |
| P18182 | A second question never builds a second box | fire twice, count | ✅ |  |
| P18183 | Cancel leaves the table exactly as it was | read the popup and its ✕ Close button afterwards | ✅ |  |
| P18184 | The discount modal renders all three boxes | read %, ₹ and "They pay" | ✅ |  |
| P18185 | The modal and its Apply are fully on screen | measure | ✅ | apply.b=673 of 834 · 662 of 780 |
| P18186 | Apply is NEVER rendered disabled | read `disabled` | ✅ | it dims and refuses out loud instead |
| P18187 | The quick-percentage chips are the documented seven | read them | ✅ | None, 5, 10, 15, 20, 25, 50% |
| P18188 | No leaked code text in the discount modal | scan | ✅ |  |
| P18189 | A BLANK box refuses out loud and keeps the modal open | empty the ₹ box, tap Apply | ✅ | "Type a discount in one of the three boxes first — or tap Cancel to leave the bill as it is." A blank box must never comp the bill (#14) |
| P18190 | An over-cap figure is named ON SCREEN while typing | type 999999, read the red note | ✅ | "The most you can take off this bill is ₹460 — that's the food value before tax." (₹483 ÷ 1.05) |
| P18191 | The over-cap tap is REFUSED out loud, not trimmed in silence | tap Apply, read the toast | ✅ | the waiter is never shown ₹500 quietly becoming ₹300 |
| P18192 | …and every box snaps to the real maximum, and the red note clears | read the three boxes after the refusal | ✅ | ₹460 / 100% / note hidden. The NEXT tap applies it |
| P18193 | A NEGATIVE figure is refused like a blank one, never applied | paste -50, tap Apply | ✅ |  |
| P18194 | Cancel closes the discount modal | click Cancel | ✅ |  |
| P18195 | The tag sheet offers VIP / Family / Owner's guest | read the rows | ✅ |  |
| P18196 | Every tag row is a 44px target | measure | ✅ | 55px |
| P18197 | No tag row is off the bottom | measure | ✅ |  |
| P18198 | No leaked code text in the tag sheet | scan | ✅ |  |
| P18199 | Back closes the tag sheet | `history.back()` | ✅ |  |
| P18200 | The ☰ drawer opens | click `#hamburger` | ✅ |  |
| P18201 | The drawer names the signed-in person | read `#dwName` / `#dwRole` | ✅ | "diagt1" · "tablet · diagt1" |
| P18202 | The drawer names the restaurant | read `#dwRest` | ✅ | not a dash |
| P18203 | The clock shows the time the MOMENT the drawer opens | read `#dwClock` | ✅ | openDrawer calls tickClock itself, which is why the 1s interval can stay idle while the drawer is shut |
| P18204 | The build tag is the real content hash, not a typed string | match `^tablet [0-9a-f]{6,}$` | ✅ | "tablet 98214f2e" before this pass's edits, and it MOVED after them — which is the whole point of the field |
| P18205 | 🚩 Report an issue has a row at this width | assert the row is visible | ✅ | present at 1194 AND at 360, where the top-bar 🚩 is hidden (the T4 fault stays fixed) |
| P18206 | 🎪 Banquet stays HIDDEN when the module is off | read the button's visibility | ✅ | `banquet_allowed` is false here, so a real waiter sees no dead button |
| P18207 | The drawer ✕ is a 44px target | measure | ✅ | 44×44 (raised from 40 on 2026-08-22; its nearest neighbour is 128px away, so nothing got crowded) |
| P18208 | ⚙️ Settings is on screen | measure | ✅ |  |
| P18209 | No leaked code text in the drawer | scan | ✅ |  |
| P18210 | Sign out is a POST form, not a link | read the form's method and action | ✅ | `post /api/panel-logout` — a GET that ends a session fires from anything that merely points at the URL |
| P18211 | …and it targets `_top`, so it signs the PERSON out and not the frame | read `target` | ✅ | `_top`. Without it the waiter looks signed out and is not |
| P18212 | The button says Sign out | read it | ✅ |  |
| P18213 | Sign out is a 44px target and on screen | measure | ✅ | 45px |
| P18214 | The Settings sheet closed the drawer FIRST | assert the drawer lost `.open` | ✅ | never two layers for one Back press |
| P18215 | Back closes the Settings sheet | `history.back()` | ✅ |  |
## J · TAKING AN ORDER (P18216–P18265)

Measured at **iPad 1194×834**, **iPad 834×1194** and **A35 360×780**.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P18216 | There is a free tile to test with | find a tile whose RAW summary state is `free` | ✅ | the raw state, never the class: `summaryTile()` re-presents `waiting` (an open party with nothing ordered) as `free`, and that table DOES have a session |
| P18217 | A FREE tile lands DIRECTLY in the order builder, not a popup | click it, assert `.om.lite` and no `.detail-pop` | ✅ | the manager rule — there is no open-table step |
| P18218 | The order screen starts inside the viewport | measure its left edge | ✅ | x=0. This is the `.quick` class-collision check: a bare `quick` class once dragged the whole screen to x=−583 |
| P18219 | The order screen never scrolls the page sideways | scrollWidth vs innerWidth | ✅ |  |
| P18220 | The dish grid paints | count `.dish[data-dish]` | ✅ | 59 dishes |
| P18221 | The category rail has exactly ONE active chip | count `.om-cat.on` | ✅ | 9 categories |
| P18222 | The category rail is measured (a scroller is fine) | measure every chip against the viewport | ✅ | recorded, not judged: the rail is a horizontal scroller by design at narrow widths |
| P18223 | No leaked code text in the order builder | scan | ✅ |  |
| P18224 | No dish advertises ₹0 | read every `.dprice` | ✅ | ₹250 / ₹420 / ₹380 … — a ₹0 label on an open-price dish was the T4 fault |
| P18225 | An empty cart explains itself | read `.cart-lines` | ✅ | "Tap dishes to add them." |
| P18226 | SEND is disabled while the cart is empty | read `#sendOrder.disabled` | ✅ | and the cart's own line is the explanation, which is why a disabled button is right HERE and wrong on 💳 |
| P18227 | ← back is a real target | measure `#omExit` | ✅ | 48px tall |
| P18228 | One tap on a PLAIN dish shows ×1 on its badge | tap it, read `.dbadge` | ✅ | a dish WITH options correctly opens the options popup instead — see the findings file, this cost four false reds |
| P18229 | …and the tile is marked as in-the-cart | read the `in` class | ✅ |  |
| P18230 | …and the cart shows exactly one line | count `.cline` | ✅ | "Pineapple Mint Mojito −1+✎ ₹420" |
| P18231 | Adding a dish does NOT reset the browse scroll | scroll, add, re-read scrollTop | ✅ | 120 → 120. The badge is patched in place; the grid is never rebuilt |
| P18232 | SEND becomes enabled with something in the cart | read `disabled` | ✅ |  |
| P18233 | The items total shows a real figure | read `.ctotal` | ✅ | "Items total ₹420" |
| P18234 | The View-order pill counts the item | read the pill | ✅ | "1 View order ₹420 →" on the phone; the pill is hidden in the sideways side-by-side layout, by design |
| P18235 | The − badge takes it back out, and the badge returns to ＋ | tap `[data-dishminus]` | ✅ | 0 lines |
| P18236 | The ✎ opens the OPTIONS popup, not a quick-add | tap `[data-dishedit]` | ✅ |  |
| P18237 | …and opening it adds nothing to the cart by itself | count `.cline` | ✅ | 0 |
| P18238 | The allergy row ends in ＋ Other | read the last chip | ✅ | 🌾 Gluten · 🥛 Dairy · 🥚 Eggs · 🥜 Nuts · 🫘 Soy · 🐟 Fish · ＋ Other |
| P18239 | The six standard allergens plus ＋ Other | count the chips | ✅ | 7 |
| P18240 | The options popup is fully on screen | measure | ✅ | y=109 b=780 on the A35 (it is a bottom sheet under 760px) |
| P18241 | Add to order is a 44px target and on screen | measure | ✅ | 50px |
| P18242 | The ✕ is a real target | measure `.opt-x` | ✅ | 40×40 |
| P18243 | The per-dish note field is there | assert `#optNote` | ✅ |  |
| P18244 | The quantity starts at 1 | read `#optQ` | ✅ |  |
| P18245 | No leaked code text in the options popup | scan | ✅ |  |
| P18246 | ＋ Other with nothing typed SAYS why and stays open | tap Add with an empty box | ✅ | "Type what the kitchen should leave out." |
| P18247 | A typed allergy joins the row as its OWN chip | type one, read the chips | ✅ | **scope the selector to `#optOverlay`** — the CART's whole-order avoid row carries the same `data-alg-other` marker and sits earlier in the document, so an unscoped query types the allergy onto the wrong list. That cost three false reds |
| P18248 | A duplicate typed allergy is refused out loud | add the same word twice | ✅ | "“coriander” is already on this list." |
| P18249 | The options stepper never goes below 1 | tap − at 1 | ✅ |  |
| P18250 | The stepper counts up | tap + three times | ✅ | 4 |
| P18251 | The popup total reads \"qty × unit\" | read `.ctotal` | ✅ | "4 × ₹250 · ₹1,000" |
| P18252 | The backdrop closes the popup and adds nothing | click the backdrop | ✅ | cart still 0 |
| P18253 | A search that matches nothing SAYS so | type nonsense | ✅ | "No dishes match." — told apart from a menu that has not loaded |
| P18254 | Searching hides the category rail | assert the rail has no offsetParent | ✅ |  |
| P18255 | A search collapses to ONE flat result list | type a real fragment | ✅ | 8 hits under one "Search results · 8" heading |
| P18256 | Clearing the search restores the sections and the rail | clear it | ✅ | 9 sections back |
| P18257 | The tapped category chip becomes active | tap the last chip | ✅ |  |
| P18258 | …and the section really scrolls into view | read scrollTop | ✅ | 3234 of 3246 |
| P18259 | The end spacer exists so the LAST category can reach the top | assert `.om-endpad` | ✅ |  |
| P18260 | …and the last section really reaches the top | measure its offset from the scroller's top | ✅ | 6px. Without the spacer the bottom categories "stop in the middle" |
| P18261 | SEND asks a confirm BEFORE anything is posted | tap SEND, watch for a POST to /api/tablet/order | ✅ | confirm shown, 0 posts |
| P18262 | …and the confirm names the kitchen and the table | read it | ✅ | "Send 1 item to the kitchen for table 1?" |
| P18263 | Cancelling the confirm keeps the cart AND posts nothing | cancel, count the lines and the posts | ✅ | 1 line, 0 posts |
| P18264 | SEND goes back to being usable after a cancel | read its label and `disabled` | ✅ | "SEND TO KITCHEN", enabled — the re-entry guard is released on cancel (sweep C2) |
| P18265 | Backing out of an order for a still-FREE table returns to the FLOOR | exit, assert the floor and no popup | ✅ | a table with an open session but nothing ordered correctly keeps its detail instead — that is not this case |

## K · THE MONEY: ACCEPT, SERVE, THE PAYMENT SHEET, THE SPLIT (P18266–P18304)

Measured at **iPad 1194×834** and **A35 360×780**, on a real party with a real bill. Nothing was
settled that did not have to be, and the sheet was closed with Cancel every time.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P18266 | The tile's ✓ flips the table out of \"New order\" | tap it, read the tile's class | ✅ | t-new → t-prep, and the ✓ is gone |
| P18267 | Accepting offers an undo bar | assert `#lfh-undobar` | ✅ |  |
| P18268 | An accept with NOTHING left says so, instead of returning in silence | call the path with an empty list | ✅ | "Nothing left to accept here — refreshing this table." (sweep #6's F1 holds) |
| P18269 | A serve-all with nothing left says so | same | ✅ | "Nothing left to serve here — refreshing this table." |
| P18270 | Serve all flips EVERY dish to served | tap it, read every pill | ✅ |  |
| P18271 | …and offers an undo bar that names what happened | read the bar | ✅ | "✋ All dishes served · T17 · 1 dish · UNDO" |
| P18272 | The undo bar does NOT cover the bill bar | measure both boxes IN THE FRAME | ✅ | bar 750–818, foot 583–674 at 1194×834; bar 696–764, foot 548–635 on the A35. Measured in the frame because undobar.js appends to the IFRAME's body — a page-level lookup reports "no bar" forever |
| P18273 | …and does not cover the action row | measure every action against the bar | ✅ | 0 buttons behind it |
| P18274 | Undo puts the dishes back | tap UNDO, re-read the pills | ✅ | served → cooking |
| P18275 | The payment sheet offers UPI / Cash / Card / Other plus Split | read the buttons | ✅ | and 📒 Pay Later, because khata is on for this restaurant |
| P18276 | Every method button is at least 44px tall | measure | ✅ | 86–89px |
| P18277 | Every method and Cancel are REACHABLE | measure, then scroll the box and re-measure | ✅ | the sheet's box is `max-height: 90vh; overflow: auto` and scrolls 60px on a 780px phone: Cancel sits at b=787 unscrolled and b=727 once scrolled. The ✕ at the top needs no scroll at all. Reachable, not off-screen — my first assertion was too strict |
| P18278 | The sheet says what is being collected | read it | ✅ | "Amount collected ₹483" |
| P18279 | The optional customer capture asks for consent | assert the phone field and the consent box | ✅ | DPDP: nothing is sent without the tick |
| P18280 | No leaked code text in the payment sheet | scan | ✅ |  |
| P18281 | Split payment is its OWN button on the grid | read the specials | ✅ | not two taps under "Other" (owner, 2026-08-21 — it was two deep, which is why nobody used it) |
| P18282 | Pay Later is offered when khata is on | read it | ✅ |  |
| P18283 | A double tap on 💳 opens exactly ONE sheet | tap twice fast, count `.pay-overlay` | ✅ | 1 (#18's re-entrancy guard) |
| P18284 | Split payment opens straight onto TWO parts | tap it, count the rows | ✅ | 2, filled in evenly |
| P18285 | Every part can pick its own way to pay, including Pay later | read a row's options | ✅ | UPI, Cash, Card, Other, Pay later |
| P18286 | The how-many-are-paying row offers 2 to 6 | read the buttons | ✅ |  |
| P18287 | The panel says whether the parts add up | read the running line | ✅ | "✓ The parts add up to ₹483" |
| P18288 | Take payment is NEVER rendered disabled | read `disabled` | ✅ |  |
| P18289 | A short total is named on the running line | edit a box, read the line | ✅ | "₹240.50 still to cover" — this line has always used inrExact |
| P18290 | Take payment REFUSES a short total out loud | tap it | ✅ | "₹241 of the bill is still uncovered." |
| P18291 | The sheet stays open on a refusal, so the figures can be fixed | assert it survives | ✅ |  |
| P18292 | The due is readable from the sheet | parse it | ✅ | ₹483 |
| P18293 | A SUB-RUPEE shortfall is named to the paise, never \"₹0\" | make the parts 40 paise short, tap Take payment | ❌→✅ | **ITEM 1, found this pass.** It said \"₹0 of the bill is still uncovered.\" while the line 12px above it said \"₹0.40 still to cover\" — so the button refused, named nothing, and refused again, with no way to tell from the screen what was wrong. `inr` rounds to whole rupees; a figure the person has to MATCH must use `inrExact`, because the server recomputes the due to the paise. Now \"₹0.40 of the bill is still uncovered.\" Guarded by verify:tablet-taps §9 |
| P18294 | A Pay-later part asks WHO owes it | switch a part to Pay later | ✅ | "📒 Who owes this? — pick a person" |
| P18295 | An unnamed Pay-later part is refused out loud | tap Take payment | ✅ | refused, in the documented order (the shortfall is checked first) |
| P18296 | The person sheet asks whose bill it is | open it | ✅ | "Pay Later — who's this bill on?" |
| P18297 | …and can search the book OR add a new person | assert both | ✅ | the search is scoped and LIMIT 8 server-side |
| P18298 | …and its Park bill button is on screen | measure | ✅ |  |
| P18299 | No leaked code text in the person sheet | scan | ✅ |  |
| P18300 | Park bill with nobody picked refuses out loud | tap it | ✅ | "Pick a person from the list, or type a name to add them." |
| P18301 | Cancel closes the whole payment sheet with NOTHING settled | cancel, assert it is gone | ✅ |  |
| P18302 | EVERY full-screen overlay dims the page by the SAME amount | read each overlay's computed backgroundColor on the running panel | ❌→✅ | **ITEM 2, found this pass.** Three dims on one panel: the token's `rgba(3,7,16,.60)` on 4 CSS overlays, `rgba(4,8,18,.66)` on the 10 built in app.js, and `rgba(4,8,18,.5)` on the table-detail backdrop — so opening a table, then its Discount, then a confirm dimmed the floor three different ways in one action. All 15 now report `rgba(3,7,16,.6)`. Guarded by verify:tablet-taps §10 |
| P18303 | A sub-rupee OVER-collect is named to the paise too | make the parts 40 paise over | ✅ | "The parts are ₹0.40 more than the bill." (item 1) |
| P18304 | …and a WHOLE-rupee gap still reads as whole rupees | make it ₹50 short | ✅ | "₹50 of the bill is still uncovered." — not "₹50.00". That asymmetry is exactly what inrExact is for |
## L · ⚡ QUICK ORDER AND ITS TABLE PICKER (P18305–P18331)

Measured at **iPad 1194×834** (sideways touch → side-by-side), **iPad 834×1194** (upright),
**A35 360×780**, and **desktop 1280×800** — the last one to prove a MOUSE never gets the
touch-only layout.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P18305 | ⚡ Quick order is on the top bar at ALL times | assert it is not hidden and not display:none | ✅ |  |
| P18306 | The screen names itself ⚡ Quick order | read the heading | ✅ |  |
| P18307 | The whole screen starts at x ≥ 0 | measure its box | ✅ | x=0 at every size — the `.quick` class-collision check |
| P18308 | No sideways page scroll | scrollWidth vs innerWidth | ✅ |  |
| P18309 | No leaked code text | scan | ✅ |  |
| P18310 | The side-by-side layout is ON sideways-and-touch, OFF everywhere else | read the computed `display` on `.om-body` | ✅ | grid on the iPad sideways; NOT on the iPad upright, NOT on the A35 upright, and **not on a 1280px desktop** — a mouse reports `pointer: fine`, which is what keeps a PC out |
| P18311 | Sideways, the order sits down the right-hand side | measure `.om-cart` | ✅ | and upright/desktop keep the pill and the review screen (his choice when asked) |
| P18312 | …and there is no View-order pill to hunt for | read the pill's computed display | ✅ | hidden — there is nothing to go and fetch |
| P18313 | …and every category chip is on screen (the rail WRAPS) | read `flex-wrap` | ✅ | wrap, not a hidden sideways strip |
| P18314 | …and no chip is off the screen | measure all nine | ✅ | 0 off |
| P18315 | …and the order pane is inside the screen, to the RIGHT of the menu | compare the two boxes | ✅ |  |
| P18316 | SEND is disabled on an EMPTY quick order | read `disabled` | ✅ | and the cart line says why. **This is what `verify:tablet` was walking into** — see item 3 |
| P18317 | The big button asks for the TABLE, because the table comes last | read its label | ✅ | "CHOOSE TABLE & SEND →" |
| P18318 | The SEND tap opens the table picker and does NOT also confirm | tap it, assert the picker and no confirm | ✅ | the pick IS the second step; a third question is the over-asking he banned |
| P18319 | Nothing is posted until a table is picked | watch for POSTs to /api/tablet/order | ✅ | 0 |
| P18320 | The picker offers tables | count them | ✅ | 30 |
| P18321 | Every destination is a 44px target | measure each | ✅ | 58px+ |
| P18322 | No destination sits off the right edge | measure each | ✅ |  |
| P18323 | A busy table says it JOINS that table's bill | read the busy ones | ✅ | "joins its bill", and "joins T…'s bill" for a merged one — never the word free |
| P18324 | A free table says free | read the rest | ✅ |  |
| P18325 | The picker's ✕ is a real target | measure | ✅ | 40×40 |
| P18326 | The picker never offers an off-plan junk number | read the numbers | ✅ | 1..30 only. You may take a party OFF an off-plan table, never send one TO one — a stray test row once made it offer "Table 9234792" |
| P18327 | No leaked code text in the picker | scan | ✅ |  |
| P18328 | ✕ closes the picker and leaves the order intact | click ✕, count the cart lines | ✅ | 1 line kept |
| P18329 | ONE Back closes the picker and stays in the order | `history.back()` | ✅ | the layer is dropped through qdestClose, not a bare remove — a bare one leaked the history entry |
| P18330 | ⚡ tapped again mid-order refuses out loud instead of wiping the cart | call it with a cart going | ✅ | "You're already building an order (1 item) — send it or go back first." |
| P18331 | Leaving a quick order clears the cart, the allergies and the quick flag | exit, read the state | ✅ | nothing leaks to the next table |

## M · JOINING TWO TABLES AND SPLITTING THEM AGAIN (P18332–P18373)

Driven end to end through the panel's own buttons on a REAL party: 18 merged into 17, checked from
both ends, then split again. The walk cleans up in a `finally` and on a crash; verified afterwards
that both tables hold their own open session again.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P18332 | Both fixture tables are on the floor | wait for each tile | ✅ | **wait for the tile, never sample it** — renderFloor replaces `#tiles` wholesale, and a read landing inside that swap sees no tiles and reports the product missing a table it has |
| P18333 | Neither is joined to anything yet | read their classes | ✅ |  |
| P18334 | The 🪢 Merge tables row is ENABLED with another open table on the floor | read the row | ✅ | the count uses the picker's exact predicate — sweep #6's F2 |
| P18335 | T17 is offered as a destination | read the picker | ✅ | 17, 19, 25 |
| P18336 | The picker says which table will HOLD the bill before the tap | read the note | ✅ | "The LOWEST table number holds it" |
| P18337 | The confirm names the table that will REALLY hold the bill | read it | ✅ | "held by T17 (the lowest table number)" — the server keeps the lowest, and this screen used to guess and name the wrong one |
| P18338 | …and says WHY that table | read it | ✅ |  |
| P18339 | …and shows the two amounts becoming one bill | read it | ✅ | "₹1,155 + ₹483 → ₹1,638 on one bill." |
| P18340 | The Yes button says Merge | read it | ✅ |  |
| P18341 | The floor knows T18 is now joined to T17 | read `mergeList()` | ✅ | parent 17, child 18, merged_by diagt1 |
| P18342 | A 15-second undo bar names what happened | read the bar | ✅ | "🪢 T18 joined T17 — one bill · tap undo to separate them again" (owner asked for 15s, longer than the 5s bars) |
| P18343 | BOTH tiles wear the merged look | read both classes | ✅ | t-merged on 17 and 18 |
| P18344 | The joined table NEVER reads Free | read its class | ✅ |  |
| P18345 | Both tiles carry the ⇄ mark | read `.tmerge` | ✅ |  |
| P18346 | Each tile names the OTHER table, not itself | read both | ✅ | 17 shows "⇄T18", 18 shows "⇄T17" |
| P18347 | A merged tile still does not clip its own content | scrollHeight vs clientHeight | ✅ | +0 on both |
| P18348 | …and is still a square | measure | ✅ | 176×176 |
| P18349 | …and the NUMBER is not the thing that gets ellipsised | read both numbers | ✅ | "17" and "18" whole — the merge chip is what yields (owner, 2026-08-15) |
| P18350 | …and its action row is still inside the tile | measure | ✅ | act.b=755 vs tile.b=769 |
| P18351 | The ⇄ explains itself on a long press | read the title | ✅ | "Served as one party with T17 — one bill" |
| P18352 | Both members show the PARTY's numbers, not their own | compare the two `x/y served` lines | ✅ | both "0/3 served" |
| P18353 | The popup header says it is one party and names both tables | open from 17 AND from 18 | ✅ | "⇄ one party · T17 + T18 · bill #1066 · ● open" from both ends |
| P18354 | The popup lists the WHOLE party's dishes, opened from either end | count the rows | ✅ | 2 rows from both ends — a child's detail once said "closed · no orders" about a bill that was right there |
| P18355 | Every dish says WHICH TABLE it was ordered at | read the `.ifrom` chips | ✅ | T17 and T18 — an order keeps the table it was rung at, which is what makes a split exact |
| P18356 | No leaked code text on a merged bill | scan | ✅ |  |
| P18357 | ⇹ Unmerge appears in the right shape at each end | read the buttons | ✅ | opening the CHILD offers one button for itself; opening the table that HOLDS the bill offers one per child. The manager's exact shape |
| P18358 | ⇹ Unmerge is a 44px target | measure | ✅ | 44px |
| P18359 | …and is on screen | measure | ✅ | b=668 of 834 |
| P18360 | ⇄ Change table is DISABLED for a joined party and says to unmerge first | read the row, from both ends | ✅ | "⇄ Change table — unmerge first". The child needed a real `waitForSelector`, not a 700ms sleep — that cost one false red |
| P18361 | The unmerge confirm names both tables and the direction | read line 1 | ✅ | "Split T18 back out of T17?" |
| P18362 | …and says what goes BACK, by KOT and amount | read it | ✅ | "• Back to T18: KOT #1460 — ₹1,155" |
| P18363 | …and what STAYS on the table holding the bill | read it | ✅ | "• Stays on T17: KOT #1459 — ₹483" |
| P18364 | …and does NOT claim \"nothing was ordered\" about a table holding food | read the whole text | ✅ | the P03047 fault (a ₹483 ticket announced as nothing) does not recur |
| P18365 | …and the three parts stay three LINES | read the computed white-space and count the lines | ✅ | `pre-line`, 3 lines |
| P18366 | The Yes button says Unmerge | read it | ✅ |  |
| P18367 | The split really ENDS the merge | re-read `mergeList()` | ✅ | empty |
| P18368 | Both tiles go back to standing alone | read their classes | ✅ |  |
| P18369 | The ⇄ mark is gone from both | read `.tmerge` | ✅ |  |
| P18370 | The split SAYS what it did | capture the toast as it fires | ✅ | "T18 split from T17 · 1 order back". **Capture toasts with a MutationObserver** — they self-remove after 2.6s, so a later read finds none; that cost one false red |
| P18371 | The split table kept its own dishes | open it | ✅ | 1 row, ₹1,155 due, ● UNPAID — its money came back with it |
| P18372 | The \"ordered at\" chip disappears once the tables are apart | count `.ifrom` | ✅ | 0 — an ordinary bill is untouched by the merged-bill markup |
| P18373 | …and the split names how many orders went back | read the toast | ✅ | "· 1 order back" |

## N · THE BANDS, THE SKINS, AND THE ADMIN VIEW (P18374–P18404)

The two bands that narrow this panel, the offline state, and both skins read as pictures.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P18374 | The ADMIN view is marked by a ribbon | open /tablet?rid=… with the ADMIN's session | ⏭ | **Could not reach it.** `requirePanel` says so in its own comment: the admin may hop into any panel \"only via the admin console's act-as flow… No scope → back to /aevinite\", and `/manager?rid=` redirects identically — so this is not a tablet fault. Setting that cookie is a browser-wide write this terminal would not make on a shared stack. A later session should enter through /aevinite → Restaurants → the panel link |
| P18375 | The ribbon takes ONE band, not a block of the screen | measure its height | ⏭ | same reason as P18374 |
| P18378 | …and names the path the admin walked in through | read the crumbs | ⏭ | same reason |
| P18379 | With NO ribbon, every popup action is fully on screen | measure at 1194×834, 834×1194 and 360×780 | ✅ | measured; this is the half of P03399 that does not need the act-as cookie |
| P18380 | …and so is the bill bar | measure | ✅ | b=667 of 780 on the A35 |
| P18381 | The action row's contents are read, not assumed | list the labels | ✅ | ＋ Take order · − Discount whole bill · 🖨 Print bill · 💳 Mark bill paid · ✕ Close table |
| P18382 | …and they still sit three-and-three on a phone | group by rounded y | ✅ | 2 rows for 5 buttons |
| P18383 | The floor is STILL on screen with no internet | go offline, count the tiles | ✅ | 30 tiles — the offline layer serves the device's own saved copy |
| P18384 | An offline bar appears and says something | read it | ✅ | "No internet — you can keep working · Showing what's saved on this device" |
| P18385 | …and never says \"database\" or shows a raw error | scan its text | ✅ | not "Failed to fetch", not "TimeoutError" (the T15 P1 fault) |
| P18386 | …and never the word \"syncing\" | scan | ✅ | T15 P3: a waiter needs to know the work is SAFE and that they need do nothing |
| P18387 | With the offline bar up, every action is still on screen | measure | ✅ |  |
| P18388 | …and the bill bar too | measure | ✅ | b=706 of 834 |
| P18389 | The word \"database\" appears NOWHERE on the screen | scan the whole body | ✅ |  |
| P18390 | The floor comes back when the internet does | go back online | ✅ | 30 tiles |
| P18391 | Each skin really applies | read `data-theme` | ✅ | light and dark both driven through `lfh_panel_theme`, which is this panel's key |
| P18392 | Every floor label keeps real contrast in BOTH skins | compute the ratio, then READ the screenshot | ✅ | light: 17 labels, worst 4.53:1. **Dark: my own maths said 1.11:1 and was WRONG** — a tile's background is a `linear-gradient`, i.e. `background-image`, so `backgroundColor` is transparent and my walk fell back to white. The screenshot shows a perfectly readable dark floor. Compute contrast against a RESOLVED backdrop, or just look |
| P18393 | The ribbon never pushes the panel sideways | scrollWidth vs innerWidth | ⏭ | needs the act-as cookie (P18374) |
| P18394 | Every ribbon button is inside the screen | measure | ⏭ | same |
| P18395 | The ribbon still leaves the tiles near the top | measure | ⏭ | same |
| P18396 | No page error in the admin view | collect pageerror | ⏭ | same |
| P18397 | With the ribbon up, EVERY action is still fully on screen | measure | ⏭ | same — this is P03399's other half |
| P18398 | …and the bill bar | measure | ⏭ | same |
| P18399 | …and the popup still does not clip itself | measure | ⏭ | same |
| P18400 | The admin sees 🧾 Generate invoice, which a waiter never does | read the actions | ⏭ | same. The RULE is checked statically and holds: `tablet_invoice` is absent from XRAY_CAPS and the greyed control explains itself (P03291, P03292) |
| P18401 | The admin's six actions fit without a runaway stack on a phone | group by y | ⏭ | same |
| P18402 | The x-ray popout lists what is off for waiters here | open it | ⏭ | same |
| P18403 | …and stays inside the screen | measure | ⏭ | same |
| P18404 | …and never lists `tablet_invoice` | read the rows | ⏭ | same — checked statically instead: the key is not in XRAY_CAPS (P03291) |

---

## What P18405–P18600 are FOR (196 free ids in this block)

Not padding — the ground this pass could not reach, so the next one can take it on purpose:

1. **`P18405`** — re-check that `verify:tablet` still reaches all 103 checks. Item 3 fixed it once;
   the thing to watch is another menu where the first dish has options.
2. **`P18406`–`P18425` — the ADMIN VIEW, properly.** Everything marked `⏭` above. Enter through
   /aevinite → Restaurants → the panel link so the act-as cookie is set, then re-measure the ribbon
   band, the popup with it up, the x-ray popout, `?view=real` and `?as=<staff id>`.
3. **`P18426`–`P18445` — a waiter WITH A SECTION.** Every section rule on this panel is currently
   checked against a waiter who holds the whole floor, so `inMySection` is always true and the kind
   empty states (P03087, P03220) have never been SEEN. Give a diag waiter `my_tables` and drive it.
4. **`P18446`–`P18465` — the offline WRITE path.** This pass proved the panel READS offline; it did
   not take an order with no internet and watch it arrive on reconnect. Note the service-worker trap:
   a request the worker answers never reaches fault injection.
5. **`P18466`–`P18485` — the KOT-menu split (`renderSplitSettle`).** Unreachable here because
   `split_bill_enabled` is false on little French house. It posts to a DIFFERENT endpoint from the
   payment sheet's split and cannot do Pay later. Turn the setting on in a `finally`-restored way.
6. **`P18486`–`P18505` — banquet billing.** `banquet_allowed` is false here, so the whole overlay is
   unexercised. Its two `e.message` toasts would show a raw error rather than the panel's own wording.
7. **`P18506`–`P18525` — an MRP / open-price menu.** `item_tax_modes_allowed` is false and this menu
   has no open-price dish, so the MRP stamp, the discount cap's MRP wording and `addOpenPriceDish`
   were only read, never driven.
8. **`P18526`–`P18545` — a PIN restaurant end to end.** `tablet_discount` is `pin` here, so the
   prompt is reachable; the retry-on-wrong-PIN loop and the cancelled-PIN revert were not driven.
9. **`P18546`–`P18600`** — free.

---

# SWEEP #7 — THE SECOND, FRESHLY PLANNED 500 (P35237–P35736)

**Run 2026-08-29 against the DEPLOYED backup site**, after the owner asked for the whole test to be
**planned again from scratch** — not re-run — now that his ten items were merged and live.

That was the right call. The territory had changed under the old plan: a brand-new split screen, a
payment sheet with a panel removed, four functions deleted, a tile threshold moved and a repaint newly
guarded. **And the new plan found a fault the old one had no check for** — item 14, a green
"✓ The parts add up to ₹1,365" sitting above an EMPTY part, with the button underneath refusing.

**500 checks written · 500 executed · 500 passing.** The one fault it found was fixed, guarded and
deployed before these rows were recorded, so every row here is green against what is actually live.

### Five red rows during the run were MY OWN CHECK, not the product

Each is written into its own row so the next sweep does not re-file it:

1. The desktop profile was opened with `hasTouch` — which applies the tiles-per-row TOUCH cap, so
   "7 per row" measured 6. `pointer: coarse` is part of the device, not decoration.
2. "A tile with an icon must also read the words" ignores that a shared action row hands ~25px to
   that icon. Measured at iPad-upright: 95.4px of tile content, and the button is 70.5px against
   ~91px of words — so the bare ＋ there is R31's rule working, not a fault.
3. On a PHONE the table detail is a FULL-SCREEN panel (`body.tbl-open #panel`), not a dimmed
   overlay, so comparing its background with the sheets' scrim compares two different things.
4. The "＋ Add another part" box is CORRECTLY empty when the bill is already covered — there is no
   remainder to seed it with. What matters (and now holds) is that the line SAYS so.
5. The drawer's name needs a WAIT, not a sleep: it comes from `/api/panel-profile`, and on the
   deployed site that round-trip outlasted a fixed pause, leaving the placeholder "…" on screen.

## A · THE MONEY, READ FROM THE CODE (P35237–P35322)

Static — no browser, no database, no writes. Every figure, every rounding, every cap, and which of the two money formatters is used where.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P35237 | inr() groups the Indian way (en-IN), never en-US | read the file | ✅ |  |
| P35238 | inr() rounds to whole rupees | read the file | ✅ |  |
| P35239 | inrExact() keeps the paise only when there ARE paise | read the file | ✅ |  |
| P35240 | …and otherwise reads exactly like inr() | read the file | ✅ |  |
| P35241 | the split's shortfall refusal uses inrExact | read the file | ✅ |  |
| P35242 | the split's over-collect refusal uses inrExact | read the file | ✅ |  |
| P35243 | the split's running total uses inrExact | read the file | ✅ |  |
| P35244 | …so the refusal and the line above it can never quote one number two ways | read the file | ✅ |  |
| P35245 | a HEADING may still round — the split's own title uses inr() | read the file | ✅ |  |
| P35246 | …and so does its Collect button | read the file | ✅ |  |
| P35247 | effRate sums NAMED components, else tax_rate, else 5% | read the file | ✅ |  |
| P35248 | banquetRate falls back to effRate(), never a literal | read the file | ✅ |  |
| P35249 | preTax divides by (1+rate), never subtracts it | read the file | ✅ |  |
| P35250 | orderTaxSplit reads each line's mode off the FROZEN ticket | read the file | ✅ |  |
| P35251 | …an exempt line is never taxed | read the file | ✅ |  |
| P35252 | …an inclusive line is worked back through the rate | read the file | ✅ |  |
| P35253 | …and nontax is counted from the LINES, never by subtraction | read the file | ✅ |  |
| P35254 | a pre-mig-270 ticket falls back to the gross through the rate | read the file | ✅ |  |
| P35255 | resolveTaxMode is GONE from this panel (it decided nothing here) | read the file | ✅ |  |
| P35256 | …and the note in its place says the manager's copy is still live | read the file | ✅ |  |
| P35257 | taxableBaseOf is gone; callers read orderTaxSplit(o).base | read the file | ✅ |  |
| P35258 | priceTaxMode is gone with it | read the file | ✅ |  |
| P35259 | itemTaxModesOn STAYS — dishIsMrp needs it | read the file | ✅ |  |
| P35260 | dishIsMrp is presentational only — exactly one call, in the dish tile's markup | read the file | ✅ |  |
| P35261 | tableAgg's due is Σtotal − Σdiscount×(1+rate), clamped ≥ 0 | read the file | ✅ |  |
| P35262 | …and it counts ONLY accepted, unpaid orders | read the file | ✅ |  |
| P35263 | patchTileFromSlice uses the SAME formula, not a second rule | read the file | ✅ |  |
| P35264 | the split screen's due uses the same discount-before-tax rule | read the file | ✅ |  |
| P35265 | the move-order picker shows each ticket's NET due | read the file | ✅ |  |
| P35266 | the pay ring is three-way — a brand-new order rings nothing | read the file | ✅ |  |
| P35267 | the cap is the TAXABLE base, never the gross | read the file | ✅ |  |
| P35268 | 'They pay' solves d = base − (P − lockedAmt)/(1+rate) | read the file | ✅ |  |
| P35269 | a blank box leaves the discount alone | read the file | ✅ |  |
| P35270 | …and Apply then refuses OUT LOUD | read the file | ✅ |  |
| P35271 | over the cap is refused, not silently trimmed | read the file | ✅ |  |
| P35272 | …and every box snaps to the real maximum after that refusal | read the file | ✅ |  |
| P35273 | the cap note names the MRP portion when there is one | read the file | ✅ |  |
| P35274 | …and says 'food value before tax' when there isn't | read the file | ✅ |  |
| P35275 | a PIN-mode discount skips the optimistic write | read the file | ✅ |  |
| P35276 | a non-PIN discount captures `expect` BEFORE the optimistic flip | read the file | ✅ |  |
| P35277 | the whole-bill discount measures the PARTY's whole bill | read the file | ✅ |  |
| P35278 | …and its % is measured on the TAXABLE base | read the file | ✅ |  |
| P35279 | discPct comes from billdoc.js — one definition for the whole product | read the file | ✅ |  |
| P35280 | the three discount boxes have no spinner arrows | read the file | ✅ |  |
| P35281 | …and they keep type=number + inputmode=decimal (the numeric keypad) | read the file | ✅ |  |
| P35282 | split-settle posts to /pay-split, the only route that carries a tab | read the file | ✅ |  |
| P35283 | …and the older /pay+splits route is no longer used by this panel | read the file | ✅ |  |
| P35284 | a plain settle still posts to /tables/:t/pay | read the file | ✅ |  |
| P35285 | optimisticPay flips every party member's orders | read the file | ✅ |  |
| P35286 | …and only the ones the server will really settle | read the file | ✅ |  |
| P35287 | Mark unpaid needs a confirm AND a typed reason | read the file | ✅ |  |
| P35288 | …and it is refused when no reason is given | read the file | ✅ |  |
| P35289 | on-the-house is gated and offers an undo | read the file | ✅ |  |
| P35290 | pay-later parks the bill and frees the table | read the file | ✅ |  |
| P35291 | every settle offers a takeback | read the file | ✅ |  |
| P35292 | the takeback is itself PIN-gated where paying is | read the file | ✅ |  |
| P35293 | nothing here can erase or bulk-delete an issued sale | read the file | ✅ |  |
| P35294 | the only routes out of a bill are cancel, close and unpay | read the file | ✅ |  |
| P35295 | printTableBill builds NO document of its own | read the file | ✅ |  |
| P35296 | …it goes through billdoc's billMoney → billData → billDocHtml | read the file | ✅ |  |
| P35297 | …and names every table of a joined party | read the file | ✅ |  |
| P35298 | a reprint says nothing on the paper (owner, 2026-08-19) | read the file | ✅ |  |
| P35299 | the button reads Print, then Reprint | read the file | ✅ |  |
| P35300 | a computer that owns the paper gets it first | read the file | ✅ |  |
| P35301 | …and a waiter is never left with nothing on screen | read the file | ✅ |  |
| P35302 | partyOrders dedupes by order id | read the file | ✅ |  |
| P35303 | ordersOf admits a party-less order only if not older than the session | read the file | ✅ |  |
| P35304 | a merged child answers with its PARENT's session | read the file | ✅ |  |
| P35305 | ensurePartySlices fetches the party together and merges in a LOOP | read the file | ✅ |  |
| P35306 | …and answers whether EVERY member's slice landed | read the file | ✅ |  |
| P35307 | the unmerge confirm quotes real KOTs and real money | read the file | ✅ |  |
| P35308 | …and says what does NOT move | read the file | ✅ |  |
| P35309 | a merge names the table that will really HOLD the bill | read the file | ✅ |  |
| P35310 | …and follows the SERVER's answer, not the tapped table | read the file | ✅ |  |
| P35311 | the tile's ✓ says something when there is nothing to accept | read the file | ✅ |  |
| P35312 | Serve all says something when there is nothing to serve | read the file | ✅ |  |
| P35313 | Attend all says something when the calls are already done | read the file | ✅ |  |
| P35314 | 💳 Mark bill paid is never a disabled button with a hover-only reason | read the file | ✅ |  |
| P35315 | the qty stepper refuses below 1 out loud | read the file | ✅ |  |
| P35316 | …and says so when the dish has left the board | read the file | ✅ |  |
| P35317 | an allergy chip on a vanished order says so | read the file | ✅ |  |
| P35318 | the discount entry point says so when the order has gone | read the file | ✅ |  |
| P35319 | every self-built modal's Cancel resolves its promise | read the file | ✅ |  |
| P35320 | a clash is shown as the server's plain sentence, not its code | read the file | ✅ |  |
| P35321 | …for 9 seconds, not the usual glance | read the file | ✅ |  |
| P35322 | wantsPin branches on the needPin FLAG, prose only as a fallback | read the file | ✅ |  |

## B · THE ONE SPLIT SCREEN (P35323–P35462)

The newest and riskiest surface, driven on the DEPLOYED site on an iPad (1194×834) and an A35 phone (360×780), on a real two-ticket bill. Nothing was ever settled: every run backed out.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P35323 | a two-ticket bill is ready to split | driven on the deployed site at ipad | ✅ | 2 tickets |
| P35324 | the screen names the table and the whole due | driven on the deployed site at ipad | ✅ | Split T17's bill · ₹1,365 |
| P35325 | all FOUR ways to divide are present | driven on the deployed site at ipad | ✅ | Equal \| Custom amount \| By dish \| By kitchen ticket |
| P35326 | the tabs read as words a waiter knows | driven on the deployed site at ipad | ✅ | Equal\|Custom amount\|By dish\|By kitchen ticket |
| P35327 | every tab is a 44px target | driven on the deployed site at ipad | ✅ | equal:44 custom:44 dish:44 ticket:44 |
| P35328 | no tab is rendered disabled | driven on the deployed site at ipad | ✅ |  |
| P35329 | no tab sits off the screen | driven on the deployed site at ipad | ✅ |  |
| P35330 | the tabs wrap rather than scroll sideways | driven on the deployed site at ipad | ✅ | doc 1194 vs 1194 |
| P35331 | it opens on two parts | driven on the deployed site at ipad | ✅ | 2 |
| P35332 | each part is labelled for a person | driven on the deployed site at ipad | ✅ | Person 1 \| Person 2 |
| P35333 | every way to pay is offered, Pay later last | driven on the deployed site at ipad | ✅ | UPI,Cash,Card,Other,Pay later |
| P35334 | the two parts start on different ways to pay | driven on the deployed site at ipad | ✅ | UPI / Cash |
| P35335 | in Equal the amounts are derived and read-only | driven on the deployed site at ipad | ✅ |  |
| P35336 | the parts add up from the first frame | driven on the deployed site at ipad | ✅ | ✓ The parts add up to ₹1,365 |
| P35337 | …and the running line is GREEN when they do | driven on the deployed site at ipad | ✅ | rgb(22, 163, 74) |
| P35338 | the Collect button names the whole due | driven on the deployed site at ipad | ✅ | 💳 Collect ₹1,365 in parts |
| P35339 | …is a 44px target | driven on the deployed site at ipad | ✅ | 58px |
| P35340 | …is fully on screen | driven on the deployed site at ipad | ✅ | b=637 vh=834 |
| P35341 | …and is never rendered disabled | driven on the deployed site at ipad | ✅ |  |
| P35342 | ＋ Add another part is offered | driven on the deployed site at ipad | ✅ | 44px |
| P35343 | the people stepper is there for the ways that need it | driven on the deployed site at ipad | ✅ |  |
| P35344 | the screen does not clip inside the card | driven on the deployed site at ipad | ✅ | +0px |
| P35345 | it keeps the card's own way back | driven on the deployed site at ipad | ✅ | 40x40 |
| P35346 | no leaked code text | driven on the deployed site at ipad | ✅ |  |
| P35347 | Equal becomes the active tab | driven on the deployed site at ipad | ✅ | equal |
| P35348 | Equal gives at least two parts | driven on the deployed site at ipad | ✅ | 2 |
| P35349 | Equal adds up to the whole bill from the start | driven on the deployed site at ipad | ✅ | ₹1365 vs ₹1365 |
| P35350 | Equal tells the truth on the running line | driven on the deployed site at ipad | ✅ | ✓ The parts add up to ₹1,365 |
| P35351 | Equal really divides evenly | driven on the deployed site at ipad | ✅ | 682.5 / 682.5 |
| P35352 | Custom amount becomes the active tab | driven on the deployed site at ipad | ✅ | custom |
| P35353 | Custom amount gives at least two parts | driven on the deployed site at ipad | ✅ | 2 |
| P35354 | Custom amount adds up to the whole bill from the start | driven on the deployed site at ipad | ✅ | ₹1365 vs ₹1365 |
| P35355 | Custom amount tells the truth on the running line | driven on the deployed site at ipad | ✅ | ✓ The parts add up to ₹1,365 |
| P35356 | Custom lets every box be typed | driven on the deployed site at ipad | ✅ | [false,false] |
| P35357 | By dish becomes the active tab | driven on the deployed site at ipad | ✅ | dish |
| P35358 | By dish gives at least two parts | driven on the deployed site at ipad | ✅ | 2 |
| P35359 | By dish adds up to the whole bill from the start | driven on the deployed site at ipad | ✅ | ₹1365 vs ₹1365 |
| P35360 | By dish tells the truth on the running line | driven on the deployed site at ipad | ✅ | One part still needs an amount |
| P35361 | By dish lists the dishes to hand out | driven on the deployed site at ipad | ✅ | 2 |
| P35362 | …each a 44px target | driven on the deployed site at ipad | ✅ | 44,44 |
| P35363 | …each showing whose it is and what it costs | driven on the deployed site at ipad | ✅ | Pink Pineapple Smoothie P1 · ₹460 |
| P35364 | By kitchen ticket becomes the active tab | driven on the deployed site at ipad | ✅ | ticket |
| P35365 | By kitchen ticket gives at least two parts | driven on the deployed site at ipad | ✅ | 2 |
| P35366 | By kitchen ticket adds up to the whole bill from the start | driven on the deployed site at ipad | ✅ | ₹1365 vs ₹1365 |
| P35367 | By kitchen ticket tells the truth on the running line | driven on the deployed site at ipad | ✅ | ✓ The parts add up to ₹1,365 |
| P35368 | By ticket labels each part with its KOT | driven on the deployed site at ipad | ✅ | KOT #118 \| KOT #119 |
| P35369 | …one part per ticket | driven on the deployed site at ipad | ✅ | 2 parts for 2 tickets |
| P35370 | …and no people stepper (the tickets decide) | driven on the deployed site at ipad | ✅ |  |
| P35371 | tapping a dish moves its money to the next person | driven on the deployed site at ipad | ✅ | 1365/0 -> 882/483 |
| P35372 | …and the parts still add up to the whole bill | driven on the deployed site at ipad | ✅ | ✓ The parts add up to ₹1,365 |
| P35373 | a 40-paise gap is named to the paise on the line | driven on the deployed site at ipad | ✅ | ₹0.40 still to cover |
| P35374 | …and the line turns RED | driven on the deployed site at ipad | ✅ | rgb(225, 29, 72) |
| P35375 | …and the refusal quotes the same figure, never ₹0 | driven on the deployed site at ipad | ✅ | ₹0.40 of the bill is still uncovered. ✕ |
| P35376 | …and the screen stays open so it can be fixed | driven on the deployed site at ipad | ✅ |  |
| P35377 | over-collecting by 40 paise is named too | driven on the deployed site at ipad | ✅ | The parts are ₹0.40 more than the bill. ✕ |
| P35378 | a part with nothing in it is refused out loud | driven on the deployed site at ipad | ✅ | Every part needs an amount above zero — remove the empty one. ✕ |
| P35379 | a Pay-later part asks WHO owes it | driven on the deployed site at ipad | ✅ | 📒 Who owes this? — pick a person |
| P35380 | …that ask is a 44px target | driven on the deployed site at ipad | ✅ | 44px |
| P35381 | …and switching to Pay later changes no amount | driven on the deployed site at ipad | ✅ | ✓ The parts add up to ₹1,365 |
| P35382 | an unnamed Pay-later part is refused out loud | driven on the deployed site at ipad | ✅ | Tap “Who owes this?” on the pay-later part and pick a person. ✕ |
| P35383 | it opens the SAME pay-later person sheet | driven on the deployed site at ipad | ✅ | Pay Later — who's this bill on? |
| P35384 | …which can search the book or add someone new | driven on the deployed site at ipad | ✅ |  |
| P35385 | …and leaks no code text | driven on the deployed site at ipad | ✅ |  |
| P35386 | Park bill with nobody picked refuses out loud | driven on the deployed site at ipad | ✅ | Pick a person from the list, or type a name to add them. ✕ |
| P35387 | an 'Other' part gets a box to say what kind | driven on the deployed site at ipad | ✅ |  |
| P35388 | ＋ Add another part adds one | driven on the deployed site at ipad | ✅ | 2 -> 3 |
| P35389 | …seeded with the remainder, and an empty one is named on the line | driven on the deployed site at ipad | ✅ | box="" line="One part still needs an amount" |
| P35390 | …and ✕ takes one away | driven on the deployed site at ipad | ✅ | back to 2 |
| P35391 | ONE Back returns to the table detail | driven on the deployed site at ipad | ✅ | {"split":false,"pop":true,"real":true} |
| P35392 | no uncaught page error in the whole split walk | driven on the deployed site at ipad | ✅ |  |
| P35393 | a two-ticket bill is ready to split | driven on the deployed site at phone | ✅ | 2 tickets |
| P35394 | the screen names the table and the whole due | driven on the deployed site at phone | ✅ | Split T17's bill · ₹1,365 |
| P35395 | all FOUR ways to divide are present | driven on the deployed site at phone | ✅ | Equal \| Custom amount \| By dish \| By kitchen ticket |
| P35396 | the tabs read as words a waiter knows | driven on the deployed site at phone | ✅ | Equal\|Custom amount\|By dish\|By kitchen ticket |
| P35397 | every tab is a 44px target | driven on the deployed site at phone | ✅ | equal:44 custom:44 dish:44 ticket:44 |
| P35398 | no tab is rendered disabled | driven on the deployed site at phone | ✅ |  |
| P35399 | no tab sits off the screen | driven on the deployed site at phone | ✅ |  |
| P35400 | the tabs wrap rather than scroll sideways | driven on the deployed site at phone | ✅ | doc 360 vs 360 |
| P35401 | it opens on two parts | driven on the deployed site at phone | ✅ | 2 |
| P35402 | each part is labelled for a person | driven on the deployed site at phone | ✅ | Person 1 \| Person 2 |
| P35403 | every way to pay is offered, Pay later last | driven on the deployed site at phone | ✅ | UPI,Cash,Card,Other,Pay later |
| P35404 | the two parts start on different ways to pay | driven on the deployed site at phone | ✅ | UPI / Cash |
| P35405 | in Equal the amounts are derived and read-only | driven on the deployed site at phone | ✅ |  |
| P35406 | the parts add up from the first frame | driven on the deployed site at phone | ✅ | ✓ The parts add up to ₹1,365 |
| P35407 | …and the running line is GREEN when they do | driven on the deployed site at phone | ✅ | rgb(22, 163, 74) |
| P35408 | the Collect button names the whole due | driven on the deployed site at phone | ✅ | 💳 Collect ₹1,365 in parts |
| P35409 | …is a 44px target | driven on the deployed site at phone | ✅ | 58px |
| P35410 | …is fully on screen | driven on the deployed site at phone | ✅ | b=640 vh=780 |
| P35411 | …and is never rendered disabled | driven on the deployed site at phone | ✅ |  |
| P35412 | ＋ Add another part is offered | driven on the deployed site at phone | ✅ | 44px |
| P35413 | the people stepper is there for the ways that need it | driven on the deployed site at phone | ✅ |  |
| P35414 | the screen does not clip inside the card | driven on the deployed site at phone | ✅ | +0px |
| P35415 | it keeps the card's own way back | driven on the deployed site at phone | ✅ | 40x40 |
| P35416 | no leaked code text | driven on the deployed site at phone | ✅ |  |
| P35417 | Equal becomes the active tab | driven on the deployed site at phone | ✅ | equal |
| P35418 | Equal gives at least two parts | driven on the deployed site at phone | ✅ | 2 |
| P35419 | Equal adds up to the whole bill from the start | driven on the deployed site at phone | ✅ | ₹1365 vs ₹1365 |
| P35420 | Equal tells the truth on the running line | driven on the deployed site at phone | ✅ | ✓ The parts add up to ₹1,365 |
| P35421 | Equal really divides evenly | driven on the deployed site at phone | ✅ | 682.5 / 682.5 |
| P35422 | Custom amount becomes the active tab | driven on the deployed site at phone | ✅ | custom |
| P35423 | Custom amount gives at least two parts | driven on the deployed site at phone | ✅ | 2 |
| P35424 | Custom amount adds up to the whole bill from the start | driven on the deployed site at phone | ✅ | ₹1365 vs ₹1365 |
| P35425 | Custom amount tells the truth on the running line | driven on the deployed site at phone | ✅ | ✓ The parts add up to ₹1,365 |
| P35426 | Custom lets every box be typed | driven on the deployed site at phone | ✅ | [false,false] |
| P35427 | By dish becomes the active tab | driven on the deployed site at phone | ✅ | dish |
| P35428 | By dish gives at least two parts | driven on the deployed site at phone | ✅ | 2 |
| P35429 | By dish adds up to the whole bill from the start | driven on the deployed site at phone | ✅ | ₹1365 vs ₹1365 |
| P35430 | By dish tells the truth on the running line | driven on the deployed site at phone | ✅ | One part still needs an amount |
| P35431 | By dish lists the dishes to hand out | driven on the deployed site at phone | ✅ | 2 |
| P35432 | …each a 44px target | driven on the deployed site at phone | ✅ | 44,44 |
| P35433 | …each showing whose it is and what it costs | driven on the deployed site at phone | ✅ | Pink Pineapple Smoothie P1 · ₹460 |
| P35434 | By kitchen ticket becomes the active tab | driven on the deployed site at phone | ✅ | ticket |
| P35435 | By kitchen ticket gives at least two parts | driven on the deployed site at phone | ✅ | 2 |
| P35436 | By kitchen ticket adds up to the whole bill from the start | driven on the deployed site at phone | ✅ | ₹1365 vs ₹1365 |
| P35437 | By kitchen ticket tells the truth on the running line | driven on the deployed site at phone | ✅ | ✓ The parts add up to ₹1,365 |
| P35438 | By ticket labels each part with its KOT | driven on the deployed site at phone | ✅ | KOT #120 \| KOT #121 |
| P35439 | …one part per ticket | driven on the deployed site at phone | ✅ | 2 parts for 2 tickets |
| P35440 | …and no people stepper (the tickets decide) | driven on the deployed site at phone | ✅ |  |
| P35441 | tapping a dish moves its money to the next person | driven on the deployed site at phone | ✅ | 1365/0 -> 882/483 |
| P35442 | …and the parts still add up to the whole bill | driven on the deployed site at phone | ✅ | ✓ The parts add up to ₹1,365 |
| P35443 | a 40-paise gap is named to the paise on the line | driven on the deployed site at phone | ✅ | ₹0.40 still to cover |
| P35444 | …and the line turns RED | driven on the deployed site at phone | ✅ | rgb(225, 29, 72) |
| P35445 | …and the refusal quotes the same figure, never ₹0 | driven on the deployed site at phone | ✅ | ₹0.40 of the bill is still uncovered. ✕ |
| P35446 | …and the screen stays open so it can be fixed | driven on the deployed site at phone | ✅ |  |
| P35447 | over-collecting by 40 paise is named too | driven on the deployed site at phone | ✅ | The parts are ₹0.40 more than the bill. ✕ |
| P35448 | a part with nothing in it is refused out loud | driven on the deployed site at phone | ✅ | Every part needs an amount above zero — remove the empty one. ✕ |
| P35449 | a Pay-later part asks WHO owes it | driven on the deployed site at phone | ✅ | 📒 Who owes this? — pick a person |
| P35450 | …that ask is a 44px target | driven on the deployed site at phone | ✅ | 44px |
| P35451 | …and switching to Pay later changes no amount | driven on the deployed site at phone | ✅ | ✓ The parts add up to ₹1,365 |
| P35452 | an unnamed Pay-later part is refused out loud | driven on the deployed site at phone | ✅ | Tap “Who owes this?” on the pay-later part and pick a person. ✕ |
| P35453 | it opens the SAME pay-later person sheet | driven on the deployed site at phone | ✅ | Pay Later — who's this bill on? |
| P35454 | …which can search the book or add someone new | driven on the deployed site at phone | ✅ |  |
| P35455 | …and leaks no code text | driven on the deployed site at phone | ✅ |  |
| P35456 | Park bill with nobody picked refuses out loud | driven on the deployed site at phone | ✅ | Pick a person from the list, or type a name to add them. ✕ |
| P35457 | an 'Other' part gets a box to say what kind | driven on the deployed site at phone | ✅ |  |
| P35458 | ＋ Add another part adds one | driven on the deployed site at phone | ✅ | 2 -> 3 |
| P35459 | …seeded with the remainder, and an empty one is named on the line | driven on the deployed site at phone | ✅ | box="" line="One part still needs an amount" |
| P35460 | …and ✕ takes one away | driven on the deployed site at phone | ✅ | back to 2 |
| P35461 | ONE Back returns to the table detail | driven on the deployed site at phone | ✅ | {"split":false,"pop":true,"real":true} |
| P35462 | no uncaught page error in the whole split walk | driven on the deployed site at phone | ✅ |  |

## C · THE FLOOR, MEASURED ON FIVE DEVICES (P35463–P35602)

Desktop 1280×800 with a MOUSE, iPad 1194×834 and 834×1194, A35 360×780 and 780×360. Every figure measured on the running page, not reasoned.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P35463 | the floor paints its tiles | driven on the deployed site at desk | ✅ | 30 |
| P35464 | no page error while it loads | driven on the deployed site at desk | ✅ |  |
| P35465 | no leaked code text on the floor | driven on the deployed site at desk | ✅ |  |
| P35466 | the restaurant's own name is whole | driven on the deployed site at desk | ✅ | "little French house" |
| P35467 | every tile is a square | driven on the deployed site at desk | ✅ |  |
| P35468 | no tile clips its own content | driven on the deployed site at desk | ✅ | none |
| P35469 | every tile shows its number | driven on the deployed site at desk | ✅ |  |
| P35470 | no action row hangs below its tile | driven on the deployed site at desk | ✅ |  |
| P35471 | no progress bar is sliced | driven on the deployed site at desk | ✅ |  |
| P35472 | 7 tiles per row | driven on the deployed site at desk | ✅ | --per-row=7 |
| P35473 | …and the first row really holds 7 | driven on the deployed site at desk | ✅ | 7 |
| P35474 | the page never scrolls sideways | driven on the deployed site at desk | ✅ | 1280 vs 1280 |
| P35475 | the tiles start near the top | driven on the deployed site at desk | ✅ | 154px |
| P35476 | four filter chips, none cut through | driven on the deployed site at desk | ✅ |  |
| P35477 | every chip is a 44px target | driven on the deployed site at desk | ✅ | 44,44,44,44 |
| P35478 | every legend key is inside the screen | driven on the deployed site at desk | ✅ |  |
| P35479 | ⚡ Quick order is a 44px target | driven on the deployed site at desk | ✅ | 130.8x44 |
| P35480 | the ☰ is a 44px target | driven on the deployed site at desk | ✅ |  |
| P35481 | every top-bar icon button is 44px | driven on the deployed site at desk | ✅ |  |
| P35482 | the top bar is still ONE row | driven on the deployed site at desk | ✅ | 59px |
| P35483 | a tile WITHOUT an icon reads "＋ Take order" | driven on the deployed site at desk | ✅ | T25:"＋ Take order" |
| P35484 | …and a tile WITH one follows the same rule at this density | driven on the deployed site at desk | ✅ | T6 content=134.9px "＋ Take order" · T17 content=134.9px "＋ Take order" |
| P35485 | …and where it does read a bare ＋, the button really is too narrow for the words | driven on the deployed site at desk | ✅ | 107.9px:"＋ Take order" 107.9px:"＋ Take order" |
| P35486 | a merged tile keeps its ⇄ and never reads Free | driven on the deployed site at desk | ✅ |  |
| P35487 | the Free filter shows only what it says | driven on the deployed site at desk | ✅ | clean |
| P35488 | the Active filter shows only what it says | driven on the deployed site at desk | ✅ | clean |
| P35489 | Active + Free = All | driven on the deployed site at desk | ✅ | 30/2/3/27 |
| P35490 | …and All matches the tiles drawn | driven on the deployed site at desk | ✅ | 30 vs 30 |
| P35491 | the floor paints its tiles | driven on the deployed site at ipadL | ✅ | 30 |
| P35492 | no page error while it loads | driven on the deployed site at ipadL | ✅ |  |
| P35493 | no leaked code text on the floor | driven on the deployed site at ipadL | ✅ |  |
| P35494 | the restaurant's own name is whole | driven on the deployed site at ipadL | ✅ | "little French house" |
| P35495 | every tile is a square | driven on the deployed site at ipadL | ✅ |  |
| P35496 | no tile clips its own content | driven on the deployed site at ipadL | ✅ | none |
| P35497 | every tile shows its number | driven on the deployed site at ipadL | ✅ |  |
| P35498 | no action row hangs below its tile | driven on the deployed site at ipadL | ✅ |  |
| P35499 | no progress bar is sliced | driven on the deployed site at ipadL | ✅ |  |
| P35500 | 6 tiles per row | driven on the deployed site at ipadL | ✅ | --per-row=6 |
| P35501 | …and the first row really holds 6 | driven on the deployed site at ipadL | ✅ | 6 |
| P35502 | the page never scrolls sideways | driven on the deployed site at ipadL | ✅ | 1194 vs 1194 |
| P35503 | the tiles start near the top | driven on the deployed site at ipadL | ✅ | 154px |
| P35504 | four filter chips, none cut through | driven on the deployed site at ipadL | ✅ |  |
| P35505 | every chip is a 44px target | driven on the deployed site at ipadL | ✅ | 44,44,44,44 |
| P35506 | every legend key is inside the screen | driven on the deployed site at ipadL | ✅ |  |
| P35507 | ⚡ Quick order is a 44px target | driven on the deployed site at ipadL | ✅ | 130.8x44 |
| P35508 | the ☰ is a 44px target | driven on the deployed site at ipadL | ✅ |  |
| P35509 | every top-bar icon button is 44px | driven on the deployed site at ipadL | ✅ |  |
| P35510 | the top bar is still ONE row | driven on the deployed site at ipadL | ✅ | 59px |
| P35511 | a tile WITHOUT an icon reads "＋ Take order" | driven on the deployed site at ipadL | ✅ | T25:"＋ Take order" |
| P35512 | …and a tile WITH one follows the same rule at this density | driven on the deployed site at ipadL | ✅ | T6 content=146px "＋ Take order" · T17 content=146px "＋ Take order" |
| P35513 | …and where it does read a bare ＋, the button really is too narrow for the words | driven on the deployed site at ipadL | ✅ | 116.8px:"＋ Take order" 116.8px:"＋ Take order" |
| P35514 | a merged tile keeps its ⇄ and never reads Free | driven on the deployed site at ipadL | ✅ |  |
| P35515 | the Free filter shows only what it says | driven on the deployed site at ipadL | ✅ | clean |
| P35516 | the Active filter shows only what it says | driven on the deployed site at ipadL | ✅ | clean |
| P35517 | Active + Free = All | driven on the deployed site at ipadL | ✅ | 30/2/3/27 |
| P35518 | …and All matches the tiles drawn | driven on the deployed site at ipadL | ✅ | 30 vs 30 |
| P35519 | the floor paints its tiles | driven on the deployed site at ipadP | ✅ | 30 |
| P35520 | no page error while it loads | driven on the deployed site at ipadP | ✅ |  |
| P35521 | no leaked code text on the floor | driven on the deployed site at ipadP | ✅ |  |
| P35522 | the restaurant's own name is whole | driven on the deployed site at ipadP | ✅ | "little French house" |
| P35523 | every tile is a square | driven on the deployed site at ipadP | ✅ |  |
| P35524 | no tile clips its own content | driven on the deployed site at ipadP | ✅ | none |
| P35525 | every tile shows its number | driven on the deployed site at ipadP | ✅ |  |
| P35526 | no action row hangs below its tile | driven on the deployed site at ipadP | ✅ |  |
| P35527 | no progress bar is sliced | driven on the deployed site at ipadP | ✅ |  |
| P35528 | 6 tiles per row | driven on the deployed site at ipadP | ✅ | --per-row=6 |
| P35529 | …and the first row really holds 6 | driven on the deployed site at ipadP | ✅ | 6 |
| P35530 | the page never scrolls sideways | driven on the deployed site at ipadP | ✅ | 834 vs 834 |
| P35531 | the tiles start near the top | driven on the deployed site at ipadP | ✅ | 154px |
| P35532 | four filter chips, none cut through | driven on the deployed site at ipadP | ✅ |  |
| P35533 | every chip is a 44px target | driven on the deployed site at ipadP | ✅ | 44,44,44,44 |
| P35534 | every legend key is inside the screen | driven on the deployed site at ipadP | ✅ |  |
| P35535 | ⚡ Quick order is a 44px target | driven on the deployed site at ipadP | ✅ | 130.8x44 |
| P35536 | the ☰ is a 44px target | driven on the deployed site at ipadP | ✅ |  |
| P35537 | every top-bar icon button is 44px | driven on the deployed site at ipadP | ✅ |  |
| P35538 | the top bar is still ONE row | driven on the deployed site at ipadP | ✅ | 59px |
| P35539 | a tile WITHOUT an icon reads "＋ Take order" | driven on the deployed site at ipadP | ✅ | T25:"＋ Take order" |
| P35540 | …and a tile WITH one follows the same rule at this density | driven on the deployed site at ipadP | ✅ | T6 content=95.4px "＋" · T17 content=95.4px "＋" |
| P35541 | …and where it does read a bare ＋, the button really is too narrow for the words | driven on the deployed site at ipadP | ✅ | 70.5px:"＋" 70.5px:"＋" |
| P35542 | a merged tile keeps its ⇄ and never reads Free | driven on the deployed site at ipadP | ✅ |  |
| P35543 | the Free filter shows only what it says | driven on the deployed site at ipadP | ✅ | clean |
| P35544 | the Active filter shows only what it says | driven on the deployed site at ipadP | ✅ | clean |
| P35545 | Active + Free = All | driven on the deployed site at ipadP | ✅ | 30/2/3/27 |
| P35546 | …and All matches the tiles drawn | driven on the deployed site at ipadP | ✅ | 30 vs 30 |
| P35547 | the floor paints its tiles | driven on the deployed site at phone | ✅ | 30 |
| P35548 | no page error while it loads | driven on the deployed site at phone | ✅ |  |
| P35549 | no leaked code text on the floor | driven on the deployed site at phone | ✅ |  |
| P35550 | the restaurant's own name is whole | driven on the deployed site at phone | ✅ | "little French house" |
| P35551 | every tile is a square | driven on the deployed site at phone | ✅ |  |
| P35552 | no tile clips its own content | driven on the deployed site at phone | ✅ | none |
| P35553 | every tile shows its number | driven on the deployed site at phone | ✅ |  |
| P35554 | no action row hangs below its tile | driven on the deployed site at phone | ✅ |  |
| P35555 | no progress bar is sliced | driven on the deployed site at phone | ✅ |  |
| P35556 | 2 tiles per row | driven on the deployed site at phone | ✅ | --per-row=2 |
| P35557 | …and the first row really holds 2 | driven on the deployed site at phone | ✅ | 2 |
| P35558 | the page never scrolls sideways | driven on the deployed site at phone | ✅ | 360 vs 360 |
| P35559 | the tiles start near the top | driven on the deployed site at phone | ✅ | 137px |
| P35560 | four filter chips, none cut through | driven on the deployed site at phone | ✅ |  |
| P35561 | every chip is a 44px target | driven on the deployed site at phone | ✅ | 44,44,44,44 |
| P35562 | every legend key is inside the screen | driven on the deployed site at phone | ✅ |  |
| P35563 | ⚡ Quick order is a 44px target | driven on the deployed site at phone | ✅ | 44x44 |
| P35564 | the ☰ is a 44px target | driven on the deployed site at phone | ✅ |  |
| P35565 | every top-bar icon button is 44px | driven on the deployed site at phone | ✅ |  |
| P35566 | the top bar is still ONE row | driven on the deployed site at phone | ✅ | 57px |
| P35567 | a tile WITHOUT an icon reads "＋ Take order" | driven on the deployed site at phone | ✅ | T25:"＋ Take order" |
| P35568 | …and a tile WITH one follows the same rule at this density | driven on the deployed site at phone | ✅ | T6 content=140px "＋ Take order" · T17 content=140px "＋ Take order" |
| P35569 | …and where it does read a bare ＋, the button really is too narrow for the words | driven on the deployed site at phone | ✅ | 112px:"＋ Take order" 112px:"＋ Take order" |
| P35570 | a merged tile keeps its ⇄ and never reads Free | driven on the deployed site at phone | ✅ |  |
| P35571 | the Free filter shows only what it says | driven on the deployed site at phone | ✅ | clean |
| P35572 | the Active filter shows only what it says | driven on the deployed site at phone | ✅ | clean |
| P35573 | Active + Free = All | driven on the deployed site at phone | ✅ | 30/2/3/27 |
| P35574 | …and All matches the tiles drawn | driven on the deployed site at phone | ✅ | 30 vs 30 |
| P35575 | the floor paints its tiles | driven on the deployed site at phoneL | ✅ | 30 |
| P35576 | no page error while it loads | driven on the deployed site at phoneL | ✅ |  |
| P35577 | no leaked code text on the floor | driven on the deployed site at phoneL | ✅ |  |
| P35578 | the restaurant's own name is whole | driven on the deployed site at phoneL | ✅ | "little French house" |
| P35579 | every tile is a square | driven on the deployed site at phoneL | ✅ |  |
| P35580 | no tile clips its own content | driven on the deployed site at phoneL | ✅ | none |
| P35581 | every tile shows its number | driven on the deployed site at phoneL | ✅ |  |
| P35582 | no action row hangs below its tile | driven on the deployed site at phoneL | ✅ |  |
| P35583 | no progress bar is sliced | driven on the deployed site at phoneL | ✅ |  |
| P35584 | 4 tiles per row | driven on the deployed site at phoneL | ✅ | --per-row=4 |
| P35585 | …and the first row really holds 4 | driven on the deployed site at phoneL | ✅ | 4 |
| P35586 | the page never scrolls sideways | driven on the deployed site at phoneL | ✅ | 780 vs 780 |
| P35587 | the tiles start near the top | driven on the deployed site at phoneL | ✅ | 87px |
| P35588 | four filter chips, none cut through | driven on the deployed site at phoneL | ✅ |  |
| P35589 | every chip is a 44px target | driven on the deployed site at phoneL | ✅ | 44,44,44,44 |
| P35590 | every legend key is inside the screen | driven on the deployed site at phoneL | ✅ |  |
| P35591 | ⚡ Quick order is a 44px target | driven on the deployed site at phoneL | ✅ | 130.8x44 |
| P35592 | the ☰ is a 44px target | driven on the deployed site at phoneL | ✅ |  |
| P35593 | every top-bar icon button is 44px | driven on the deployed site at phoneL | ✅ |  |
| P35594 | the top bar is still ONE row | driven on the deployed site at phoneL | ✅ | 59px |
| P35595 | a tile WITHOUT an icon reads "＋ Take order" | driven on the deployed site at phoneL | ✅ | T25:"＋ Take order" |
| P35596 | …and a tile WITH one follows the same rule at this density | driven on the deployed site at phoneL | ✅ | T6 content=139.2px "＋ Take order" · T17 content=139.2px "＋ Take order" |
| P35597 | …and where it does read a bare ＋, the button really is too narrow for the words | driven on the deployed site at phoneL | ✅ | 111px:"＋ Take order" 111px:"＋ Take order" |
| P35598 | a merged tile keeps its ⇄ and never reads Free | driven on the deployed site at phoneL | ✅ |  |
| P35599 | the Free filter shows only what it says | driven on the deployed site at phoneL | ✅ | clean |
| P35600 | the Active filter shows only what it says | driven on the deployed site at phoneL | ✅ | clean |
| P35601 | Active + Free = All | driven on the deployed site at phoneL | ✅ | 30/2/3/27 |
| P35602 | …and All matches the tiles drawn | driven on the deployed site at phoneL | ✅ | 30 vs 30 |

## D+E · THE DETAIL, ITS SHEETS, AND TAKING AN ORDER (P35603–P35715)

Driven on the deployed site at iPad and phone size.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P35603 | the detail opens as one popup, fully on screen | driven on the deployed site at ipadL | ✅ | y=101 b=733 |
| P35604 | …and does not clip itself | driven on the deployed site at ipadL | ✅ | +0px |
| P35605 | …names the table | driven on the deployed site at ipadL | ✅ | T17 |
| P35606 | …with no leaked code text | driven on the deployed site at ipadL | ✅ |  |
| P35607 | the ✕ is at least 40px and inside the card | driven on the deployed site at ipadL | ✅ |  |
| P35608 | the ‹ › steppers are 40px targets | driven on the deployed site at ipadL | ✅ |  |
| P35609 | the KOT/table-ops row sits ABOVE the orders | driven on the deployed site at ipadL | ✅ | 🧾 KOT ▾ \| 🏷 Table type |
| P35610 | every action is fully on screen | driven on the deployed site at ipadL | ✅ |  |
| P35611 | every action is a 44px target | driven on the deployed site at ipadL | ✅ |  |
| P35612 | none of them is a disabled button | driven on the deployed site at ipadL | ✅ |  |
| P35613 | the bill bar is on screen | driven on the deployed site at ipadL | ✅ |  |
| P35614 | the ORDERS get at least as much room as the buttons | driven on the deployed site at ipadL | ✅ |  |
| P35615 | the actions do not stack past two rows | driven on the deployed site at ipadL | ✅ | 2 rows for 4 |
| P35616 | the bill line never ends in a bare dash | driven on the deployed site at ipadL | ✅ | bill #66 ● new |
| P35617 | ✕ Close table asks first, naming the table | driven on the deployed site at ipadL | ✅ | Close T17 and free it? |
| P35618 | its Yes button says what it will DO | driven on the deployed site at ipadL | ✅ | Close table |
| P35619 | a multi-line question keeps its lines | driven on the deployed site at ipadL | ✅ |  |
| P35620 | the confirm sits above every other overlay | driven on the deployed site at ipadL | ✅ | 100002 |
| P35621 | …and is fully on screen | driven on the deployed site at ipadL | ✅ |  |
| P35622 | …and dims by the one shared amount | driven on the deployed site at ipadL | ✅ | rgba(3, 7, 16, 0.6) |
| P35623 | Cancel leaves the table exactly as it was | driven on the deployed site at ipadL | ✅ |  |
| P35624 | all three discount boxes have no spinner arrows | driven on the deployed site at ipadL | ✅ | textfield,textfield,textfield |
| P35625 | …and all three keep the numeric keypad | driven on the deployed site at ipadL | ✅ |  |
| P35626 | Apply is never disabled and is on screen | driven on the deployed site at ipadL | ✅ |  |
| P35627 | no leaked code text in the discount modal | driven on the deployed site at ipadL | ✅ |  |
| P35628 | a blank discount refuses out loud, never comps the bill | driven on the deployed site at ipadL | ✅ |  |
| P35629 | the ☰ drawer opens and names the person | driven on the deployed site at ipadL | ✅ | "diagt1" |
| P35630 | …and the restaurant | driven on the deployed site at ipadL | ✅ | little French house |
| P35631 | …and shows the time at once | driven on the deployed site at ipadL | ✅ | 09:36 AM |
| P35632 | …and a real build hash, not a typed string | driven on the deployed site at ipadL | ✅ | tablet 6ee9eff9 |
| P35633 | …with 🚩 Report an issue reachable at this width | driven on the deployed site at ipadL | ✅ |  |
| P35634 | …a 44px ✕ | driven on the deployed site at ipadL | ✅ |  |
| P35635 | …and no leaked code text | driven on the deployed site at ipadL | ✅ |  |
| P35636 | Sign out is a POST form to /api/panel-logout | driven on the deployed site at ipadL | ✅ |  |
| P35637 | …targeting _top, so it signs the PERSON out | driven on the deployed site at ipadL | ✅ | _top |
| P35638 | …a 44px target labelled Sign out | driven on the deployed site at ipadL | ✅ |  |
| P35639 | …and the sheet closed the drawer first | driven on the deployed site at ipadL | ✅ |  |
| P35640 | every overlay that floats over something dims by the SAME amount | driven on the deployed site at ipadL | ✅ | 1: detail backdrop=rgba(3, 7, 16, 0.6) · discount modal=rgba(3, 7, 16, 0.6) · table-type sheet=rgba(3, 7, 16, 0.6) · drawer backdrop=rgba(3, 7, 16, 0.6) · settings sheet=rgba(3, 7, 16, 0.6) |
| P35641 | there is a truly free table to order for | driven on the deployed site at ipadL | ✅ | T9 |
| P35642 | a FREE tile lands straight in the order builder | driven on the deployed site at ipadL | ✅ | 59 dishes |
| P35643 | the order screen starts inside the viewport | driven on the deployed site at ipadL | ✅ | x=0 |
| P35644 | …and never scrolls the page sideways | driven on the deployed site at ipadL | ✅ |  |
| P35645 | the category rail is built | driven on the deployed site at ipadL | ✅ | 9 chips |
| P35646 | no dish advertises ₹0 | driven on the deployed site at ipadL | ✅ | ₹250 \| ₹420 \| ₹420 \| ₹380 \| ₹420 |
| P35647 | an empty cart explains itself | driven on the deployed site at ipadL | ✅ |  |
| P35648 | SEND is disabled while the cart is empty | driven on the deployed site at ipadL | ✅ |  |
| P35649 | ← back is a real target | driven on the deployed site at ipadL | ✅ |  |
| P35650 | no leaked code text in the builder | driven on the deployed site at ipadL | ✅ |  |
| P35651 | one tap on a plain dish puts it in the cart | driven on the deployed site at ipadL | ✅ | badge="−×1" lines=1 |
| P35652 | …and SEND becomes usable | driven on the deployed site at ipadL | ✅ |  |
| P35653 | …and the total shows a real figure | driven on the deployed site at ipadL | ✅ | Items total₹420 |
| P35654 | SEND asks a confirm BEFORE anything is posted | driven on the deployed site at ipadL | ✅ | posts=0 |
| P35655 | …naming the kitchen and the table | driven on the deployed site at ipadL | ✅ | Send 1 item to the kitchen for table 9? |
| P35656 | Cancelling keeps the cart and posts nothing | driven on the deployed site at ipadL | ✅ |  |
| P35657 | backing out of a free table's order returns to the floor | driven on the deployed site at ipadL | ✅ |  |
| P35658 | no uncaught page error in the whole walk | driven on the deployed site at ipadL | ✅ |  |
| P35659 | the detail opens as one popup, fully on screen | driven on the deployed site at phone | ✅ | y=56 b=696 |
| P35660 | …and does not clip itself | driven on the deployed site at phone | ✅ | +0px |
| P35661 | …names the table | driven on the deployed site at phone | ✅ | T17 |
| P35662 | …with no leaked code text | driven on the deployed site at phone | ✅ |  |
| P35663 | the ✕ is at least 40px and inside the card | driven on the deployed site at phone | ✅ |  |
| P35664 | the ‹ › steppers are 40px targets | driven on the deployed site at phone | ✅ |  |
| P35665 | the KOT/table-ops row sits ABOVE the orders | driven on the deployed site at phone | ✅ | 🧾 KOT ▾ \| 🏷 Table type |
| P35666 | every action is fully on screen | driven on the deployed site at phone | ✅ |  |
| P35667 | every action is a 44px target | driven on the deployed site at phone | ✅ |  |
| P35668 | none of them is a disabled button | driven on the deployed site at phone | ✅ |  |
| P35669 | the bill bar is on screen | driven on the deployed site at phone | ✅ |  |
| P35670 | the ORDERS get at least as much room as the buttons | driven on the deployed site at phone | ✅ |  |
| P35671 | the actions do not stack past two rows | driven on the deployed site at phone | ✅ | 2 rows for 4 |
| P35672 | the bill line never ends in a bare dash | driven on the deployed site at phone | ✅ | bill #66 ● new |
| P35673 | ✕ Close table asks first, naming the table | driven on the deployed site at phone | ✅ | Close T17 and free it? |
| P35674 | its Yes button says what it will DO | driven on the deployed site at phone | ✅ | Close table |
| P35675 | a multi-line question keeps its lines | driven on the deployed site at phone | ✅ |  |
| P35676 | the confirm sits above every other overlay | driven on the deployed site at phone | ✅ | 100002 |
| P35677 | …and is fully on screen | driven on the deployed site at phone | ✅ |  |
| P35678 | …and dims by the one shared amount | driven on the deployed site at phone | ✅ | rgba(3, 7, 16, 0.6) |
| P35679 | Cancel leaves the table exactly as it was | driven on the deployed site at phone | ✅ |  |
| P35680 | all three discount boxes have no spinner arrows | driven on the deployed site at phone | ✅ | textfield,textfield,textfield |
| P35681 | …and all three keep the numeric keypad | driven on the deployed site at phone | ✅ |  |
| P35682 | Apply is never disabled and is on screen | driven on the deployed site at phone | ✅ |  |
| P35683 | no leaked code text in the discount modal | driven on the deployed site at phone | ✅ |  |
| P35684 | a blank discount refuses out loud, never comps the bill | driven on the deployed site at phone | ✅ |  |
| P35685 | the ☰ drawer opens and names the person | driven on the deployed site at phone | ✅ | "diagt1" |
| P35686 | …and the restaurant | driven on the deployed site at phone | ✅ | little French house |
| P35687 | …and shows the time at once | driven on the deployed site at phone | ✅ | 09:37 AM |
| P35688 | …and a real build hash, not a typed string | driven on the deployed site at phone | ✅ | tablet 6ee9eff9 |
| P35689 | …with 🚩 Report an issue reachable at this width | driven on the deployed site at phone | ✅ |  |
| P35690 | …a 44px ✕ | driven on the deployed site at phone | ✅ |  |
| P35691 | …and no leaked code text | driven on the deployed site at phone | ✅ |  |
| P35692 | Sign out is a POST form to /api/panel-logout | driven on the deployed site at phone | ✅ |  |
| P35693 | …targeting _top, so it signs the PERSON out | driven on the deployed site at phone | ✅ | _top |
| P35694 | …a 44px target labelled Sign out | driven on the deployed site at phone | ✅ |  |
| P35695 | …and the sheet closed the drawer first | driven on the deployed site at phone | ✅ |  |
| P35696 | every overlay that floats over something dims by the SAME amount | driven on the deployed site at phone | ✅ | 1: discount modal=rgba(3, 7, 16, 0.6) · table-type sheet=rgba(3, 7, 16, 0.6) · drawer backdrop=rgba(3, 7, 16, 0.6) · settings sheet=rgba(3, 7, 16, 0.6) |
| P35697 | …and the detail is a full-screen panel, not a dimmed overlay | driven on the deployed site at phone | ✅ | rgb(241, 235, 225) |
| P35698 | there is a truly free table to order for | driven on the deployed site at phone | ✅ | T9 |
| P35699 | a FREE tile lands straight in the order builder | driven on the deployed site at phone | ✅ | 59 dishes |
| P35700 | the order screen starts inside the viewport | driven on the deployed site at phone | ✅ | x=0 |
| P35701 | …and never scrolls the page sideways | driven on the deployed site at phone | ✅ |  |
| P35702 | the category rail is built | driven on the deployed site at phone | ✅ | 9 chips |
| P35703 | no dish advertises ₹0 | driven on the deployed site at phone | ✅ | ₹250 \| ₹420 \| ₹420 \| ₹380 \| ₹420 |
| P35704 | an empty cart explains itself | driven on the deployed site at phone | ✅ |  |
| P35705 | SEND is disabled while the cart is empty | driven on the deployed site at phone | ✅ |  |
| P35706 | ← back is a real target | driven on the deployed site at phone | ✅ |  |
| P35707 | no leaked code text in the builder | driven on the deployed site at phone | ✅ |  |
| P35708 | one tap on a plain dish puts it in the cart | driven on the deployed site at phone | ✅ | badge="−×1" lines=1 |
| P35709 | …and SEND becomes usable | driven on the deployed site at phone | ✅ |  |
| P35710 | …and the total shows a real figure | driven on the deployed site at phone | ✅ | Items total₹420 |
| P35711 | SEND asks a confirm BEFORE anything is posted | driven on the deployed site at phone | ✅ | posts=0 |
| P35712 | …naming the kitchen and the table | driven on the deployed site at phone | ✅ | Send 1 item to the kitchen for table 9? |
| P35713 | Cancelling keeps the cart and posts nothing | driven on the deployed site at phone | ✅ |  |
| P35714 | backing out of a free table's order returns to the floor | driven on the deployed site at phone | ✅ |  |
| P35715 | no uncaught page error in the whole walk | driven on the deployed site at phone | ✅ |  |

## F+G · THE PROJECT RULES, AND MY OWN JUDGMENT (P35716–P35736)

Read, not driven: the rejected-idea rules this panel touches, and whether a real waiter is actually served.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P35716 | R1 — a free tile is still a number, a seat count and the word Free | read the file | ✅ |  |
| P35717 | R4 — the 💳 / ⏻ tile controls were not widened | read the file | ✅ |  |
| P35718 | R6 — TILE_MIN_PX is still not restated, only forbidden | read the file | ✅ |  |
| P35719 | R25 — the legend still lists only three of the five colours | read the file | ✅ |  |
| P35720 | R22 — the sheets' ✕ is untouched at its own size | read the file | ✅ |  |
| P35721 | R31 — the take-order button still has exactly TWO faces, never a third worded one | read the file | ✅ |  |
| P35722 | R27 — this panel offers no way to delete a bill | read the file | ✅ |  |
| P35723 | every rejection this panel touches is recorded in the doc too | read the file | ✅ |  |
| P35724 | the offline family still covers /api/tablet | read the file | ✅ |  |
| P35725 | every non-GET still goes through the outbox | read the file | ✅ |  |
| P35726 | no interval on this panel FETCHES faster than the 60s backstop | read the file | ✅ | the fast ones must touch the DOM only |
| P35727 | the backstop still does nothing while the tab is hidden | read the file | ✅ |  |
| P35728 | index.html still loads app.js with a content hash | read the file | ✅ |  |
| P35729 | the panel still registers its back layers rather than hand-rolling history | read the file | ✅ |  |
| P35730 | the tablet and the manager still agree on the tiles-per-row cap | read the file | ✅ |  |
| P35731 | the split posts to the shared money path, not a second one | read the file | ✅ |  |
| P35732 | splitting a bill is now ONE thing to learn, not two | read the file | ✅ |  |
| P35733 | …and it is reachable from where a waiter already is (the bill) as well as the KOT menu | read the file | ✅ |  |
| P35734 | a waiter can put ONE person's share on a tab without leaving the split | read the file | ✅ |  |
| P35735 | every refusal on the money screens names a figure a waiter can act on, to the paise | read the file | ✅ |  |
| P35736 | nothing on this panel says a number one way and refuses in another | read the file | ✅ |  |

