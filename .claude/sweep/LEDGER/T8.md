# T8 — PRINTING, THE BILL DOCUMENT & THE NUMBERS ON IT

Sweep #6, terminal 8 of 30. Phases **P03501–P04000** (500). Branch `sweep6/t8-print-bills-numbering`,
worktree `sweep6/T8`, dev port **4108**.

**Territory (write access):** `public/panels/billdoc.js` · `public/panels/billdoc.d.ts` ·
`public/panels/billcustomer.js` · `docs/NUMBERING.md`

## The budget these 500 phases were written to

| slice | share | phases | block |
|---|---|---|---|
| reading the code for correctness | 40% | 200 | P03501–P03700 |
| conformance to the project's own written rules | 20% | 100 | P03701–P03800 |
| watching it run (headless Chromium, port 4108) | 15% | 75 | P03801–P03875 |
| looking at screenshots actually read (1280×800 desktop **and** A35 360×780 dpr3) | 10% | 50 | P03876–P03925 |
| tracing a change across panels | 10% | 50 | P03926–P03975 |
| judgment — does a real restaurant need it this way | 5% | 25 | P03976–P04000 |

Per file: `billdoc.js` 300+ · `billcustomer.js` 55 · `docs/NUMBERING.md` 30 · `billdoc.d.ts` 25.

**Not a finding here** (pre-empted by the prompt, re-checked against `docs/REJECTED-IDEAS.md`):
gaps in the bill/invoice series (deliberate) · the absence of a delete-a-bill permission (R27) ·
the KOT's fixed 280px body overflowing a 58mm roll (R26) · no printer fires on a backup stack.

**Result key:** ✅ pass · ❌ real problem (fixed this run) · ⏭ skipped, with a written reason.
Results below are the **second pass**, re-run from the top after every fix landed.

---

## A · READING THE CODE FOR CORRECTNESS — the BILL document (`billDocHtml`, `billRows`) — 55

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03501 | `billDocHtml` escapes every caller string it interpolates (name, addr, phone, gstin, footer, item titles, option labels, tableDisp, dateStr, cust, custPhone, discLabel, mrpLabel, mrpNote, note) | node: render with each field set to `<script>x</script> & "q"`, assert the raw tag never appears in the body and `&lt;` does | ✅ | every one goes through `esc()` |
| P03502 | The `<title>` is escaped too — a restaurant named with markup cannot break the tab title | node: `billDocHtml({name:'Bob & "Co" <b>'})`, assert `<title>` holds `&amp;`/`&lt;` | ✅ | |
| P03503 | `esc()` covers all five of `& < > " '` | read `esc` at the top of billdoc.js | ✅ | |
| P03504 | The logo `src` is escaped and fails soft (`onerror` hides it) rather than printing a broken-image box | read the `d.logo` branch | ✅ | |
| P03505 | An absent logo prints no `<img>` at all | node: render with no `logo`, assert no `class="logo"` | ✅ | |
| P03506 | `inr()` prints whole rupees with Indian grouping (1,07,880) and never a decimal | node: `inr(107880.4)` === "₹1,07,880" | ✅ | |
| P03507 | `inr()` of a non-number is ₹0, never "₹NaN" | node: `inr(undefined)`, `inr("abc")` | ✅ | `parseFloat||0` |
| P03508 | `pn()` (the qty/rate/amt column formatter) never emits NaN | node: `pn(undefined)` === "0" | ✅ | |
| P03509 | An item row's base unit price is the unit price MINUS its priced add-ons, so the rows sum to the subtotal | node: qty 2 × ₹250 with a ₹50 add-on → rows 200/400 then 50/100 | ✅ | probe 3 §8 |
| P03510 | Add-on sub-lines only render for options that actually carry a price | read the `opts` filter (`Number(x.price)`) | ✅ | |
| P03511 | A zero-priced option is dropped from the paper rather than printed as "+ Extra ₹0" | node: option `{label:"No ice",price:0}` → no `class="ex"` row | ✅ | |
| P03512 | The money columns are sized from THIS bill's widest figure, never a fixed guess | node: a ₹1,07,880 line → `<col style="width:calc(8ch + 11px)">` | ✅ | probe 2 §H |
| P03513 | The Qty/Rate/Amt columns are never narrower than their own headings (floor 3/4/3) | read `widest` initialiser | ✅ | |
| P03514 | A 60-character dish name does not push the money columns off the roll (`table-layout:fixed` + `word-break`) | node + headless render at 80mm; see P03812 | ✅ | |
| P03515 | An MRP line wears its `MRP` stamp beside the dish name, not in a separate column | node: `is_mrp:true` → `<span class="mrpt">MRP</span>` inside `td.n` | ✅ | |
| P03516 | A non-MRP line carries no stamp | node: assert no `mrpt` when `is_mrp` absent | ✅ | |
| P03517 | `billRows()` clamps a discount larger than the row it comes off, so no negative "Taxable value" reaches paper | node: `billRows({subtotal:100,discount:150})` → discount 100, taxable 0 | ✅ | probe 1 |
| P03518 | The printed discount PERCENTAGE agrees with the rupees printed beside it when the clamp bites | node: `billDocHtml({subtotal:100,discount:150,discLabel:discPct(100,150)})` — label vs amount | ❌→✅ | **F2 fixed**: printed "Discount (150%) − ₹100"; the document now re-words the label from what it actually deducted |
| P03519 | An ordinary (unclamped) discount keeps the caller's own label verbatim | node: subtotal 1000 disc 100 label "10%" → still "10%" | ✅ | the fix only bites when the clamp does |
| P03520 | "Taxable value" is a RESTATEMENT (subtotal − discount), never a caller-supplied figure | grep: `d.taxable` is never read by `billDocHtml` | ✅ | see P03742 |
| P03521 | The "Taxable value" row is suppressed on a tax-inside bill (the row above is gross, so the name would be wrong) | node: `taxIncluded:true` + discount → no `Taxable value` | ✅ | |
| P03522 | The "Taxable value" row is suppressed on a composition Bill of Supply | node: `composition:true` + discount → no `Taxable value` | ✅ | T7 F9, still holds |
| P03523 | The "Taxable value" row is suppressed when explicit `inclRows` are supplied | read `restate = !inclusive && !inclOnly && !composition` | ✅ | |
| P03524 | With no discount there is no Discount row and no restatement | node: discount 0 → neither row | ✅ | |
| P03525 | A "Round off" row appears only when the whole-rupee rows cannot reach the TOTAL | node: sweep 200–900 × 6 discounts, assert the row appears iff `roundOff !== 0` | ✅ | |
| P03526 | The Round off row shows its sign (− / +) and an absolute amount, never "−₹-2" | read the `roundBlock` branch | ✅ | |
| P03527 | The rows above the TOTAL always foot to the TOTAL on a discounted 5% bill | node: 200–900 × {5,10,15,20,25,50}% — base + tax + roundOff === total | ✅ | 4,206 cases |
| P03528 | …and on an undiscounted bill | node: 100–900, same identity | ✅ | 801 cases |
| P03529 | …and on a paise-level 18% bill | node: ₹100.00–₹239.93 step 7p at 18% | ✅ | 2,000 cases |
| P03530 | …and on a bill carrying MRP/untaxed lines priced as EXEMPT | node: food 400 + MRP 40, no discount → 400+10+10+40 = 460 | ✅ | this shape was always right; the tax-INCLUSIVE MRP shape was not — see P03884 |
| P03531 | …and on an MRP bill WITH a discount | node: food 400 − 100 + 8 + 7 + 40 = 355 | ✅ | probe 3 §D |
| P03532 | …and on a tax-inside bill, discounted and not | node: the T7 §1 replay through `billData` | ✅ | |
| P03533 | …and on a composition bill with a discount | node: 800 − 150 = 650, no tax row | ✅ | |
| P03534 | …and on a mixed-rate bill (5% food beside an 18% banquet) | node: T7 §2 replay | ✅ | |
| P03535 | The TOTAL is passed straight through — no fix may ever move what is charged | node: ₹201 less 15% still totals ₹179 | ✅ | |
| P03536 | The first money row is named "Food subtotal" only when there really are untaxed lines | node: nontax 40 → "Food subtotal"; nontax 0 → "Subtotal" | ✅ | |
| P03537 | The MRP row is added AFTER the tax rows, so the column reads in the order a person adds it | read the render order: subtotal → disc → tax → MRP → round → TOTAL | ✅ | |
| P03538 | Tax rows are suppressed entirely on a tax-inclusive bill (reported below, never added) | node: `taxIncluded:true` → no tax row above the TOTAL | ✅ | |
| P03539 | "Price includes" renders under the TOTAL when `inclRows` is non-empty | node: T7 §1 | ✅ | |
| P03540 | A bill can carry BOTH added tax and inside tax and still foot | node: one `incl` line + one `excl` line on one order | ✅ | |
| P03541 | `taxRows: []` prints no tax line at all (a composition restaurant) | node | ✅ | |
| P03542 | The composition declaration prints on a Bill of Supply | node: `composition:true` → "Composition taxable person — not eligible to collect tax on supplies." | ✅ | |
| P03543 | …and never on a cancelled sheet, which charges nothing | node: `composition:true, cancelled:true` → no declaration | ✅ | probe 3 §B |
| P03544 | The document is headed "Bill of Supply" for a composition restaurant, in BOTH the `<title>` and the `.kind` row | node | ✅ | |
| P03545 | A cancelled bill is headed "Cancelled Bill", never "Tax Invoice" | node: `cancelled:true` | ✅ | probe 2 §A |
| P03546 | A cancelled bill carries the double-bordered "Cancelled — no charge" band above the kind row | node: assert `class="vband"` precedes `class="kind"` | ✅ | |
| P03547 | A cancelled bill still LISTS what was ordered — a void record nobody can read is no record | node: item rows present, summing to the Ordered value | ✅ | |
| P03548 | A cancelled bill's money block is Ordered value / Cancelled — not charged / TOTAL ₹0, and nothing else | node: no Subtotal, Discount, tax, MRP or Round off row | ✅ | probe 3 §3 |
| P03549 | The Ordered value is added from the SAME item rows printed above it | read `orderedValue` — it reduces `d.lines` | ✅ | |
| P03550 | A cancelled bill keeps its customer block (the record of who it was for) | node: `bill_cust_name` survives the cancelled branch | ✅ | probe 3 §1 |
| P03551 | A cancelled PARCEL bill still says "Parcel" and shows no Table row | node | ✅ | probe 3 §2 |
| P03552 | The customer block hides each of its two lines independently when blank | node: name only; phone only; neither | ✅ | |
| P03553 | A `Bill no` of 0 still prints its row (0 is a number, not "missing") | node: `billNo:0` → `#0` | ✅ | `!== "" && != null` |
| P03554 | An absent Invoice number prints no Invoice row rather than an empty one | node: `invNo:""` | ✅ | |
| P03555 | A parcel bill prints "Parcel" with nothing where a table number would go, and no Table row | node | ✅ | owner, 2026-08-02 |

## B · READING THE CODE FOR CORRECTNESS — the bill's MONEY — 60

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03556 | `billMoney` drops a CANCELLED order from the subtotal, the tax and the TOTAL | node: one live ₹100 beside a cancelled ₹500 → total 105 | ✅ | |
| P03557 | `billMoney` drops a SOFT-DELETED order the same way | node: `deleted_at` set → total 105 | ✅ | T7 F7 |
| P03558 | `billData`'s printed item rows drop the SAME orders the money does | node: titles exclude the tombstoned dish, rows sum to the subtotal | ✅ | the 2026-08-06 fix |
| P03559 | `mrpTaxInside` uses that same predicate (status + deleted_at) | read the filter | ✅ | |
| P03560 | An all-cancelled bill is detected by "every order cancelled", not by "no live orders" | read `voidedAll` — a tombstoned bill is a different thing | ✅ | |
| P03561 | An all-cancelled bill re-shows the cancelled orders as its item rows | read `if (voidedAll) live = orders` | ✅ | |
| P03562 | A bill with SOME orders cancelled prints only the live ones and is NOT marked cancelled | node: one live + one cancelled → `cancelled:false`, one item row | ✅ | |
| P03563 | `orderTaxRate` honours a positive stamped rate (mig 284) | node: `{tax_rate:0.18}` → 0.18 | ✅ | |
| P03564 | …honours a stamped ZERO from an order that carries money | node: `{tax_rate:0, subtotal:100}` → 0 | ✅ | probe 1 |
| P03565 | …falls back to the settings rate for a ₹0 line, so it cannot drag a taxed bill to nothing | node: `{tax_rate:0, subtotal:0}` → 0.05 | ✅ | |
| P03566 | …falls back for a never-stamped order | node: `{}` → 0.05 | ✅ | |
| P03567 | …survives a null order | node: `orderTaxRate(null, 0.05)` → 0.05 | ✅ | |
| P03568 | `lib/paySplit.ts` uses this ONE definition rather than its own copy | grep `BILLDOC.orderTaxRate` in paySplit.ts | ✅ | 2026-08-06 |
| P03569 | Tax is charged per RATE BUCKET, each rounded once — never per order (that drifts ±½ paise an order) | read the `buckets` loop | ✅ | |
| P03570 | A mixed-rate bill taxes 5% food and an 18% banquet each at its own rate (50 + 180 = 230) | node | ✅ | |
| P03571 | Each bucket caps its own discount at its own base | read `bDisc = Math.min(max(0,bk.disc), bBase)` | ✅ | |
| P03572 | The headline rate is the biggest TAXED slice, not whichever order came first | read the `rateList.sort` by base | ✅ | |
| P03573 | `mixedRates` is true only when more than one distinct rate is present | node: two orders at 0.05 → `mixedRates:false` | ✅ | |
| P03574 | The discount base is the TAXABLE base when the bill carries tax | read `discountBase` | ✅ | mig 272 |
| P03575 | …and everything except the locked MRP when it does not | read the `anyTax` branch | ✅ | |
| P03576 | The bill's discount is clamped to its own base, so a runaway discount cannot make a negative total | node: discount 5000 on a ₹400 bill → total ≥ 0 | ✅ | |
| P03577 | `taxable` is floored at 0 | read `Math.max(0, ...)` | ✅ | |
| P03578 | `taxInside + taxAdded === tax`, always | node: 500 random incl/excl mixes | ✅ | |
| P03579 | On an ordinary bill `taxInside` is 0 and `taxAdded` is the whole tax | node | ✅ | |
| P03580 | `grossTaxed` equals `taxableBase` on an ordinary bill and is larger on a tax-inside one | node | ✅ | |
| P03581 | An `exempt` line contributes to neither `grossTaxed` nor `netIncl` | read the `mode === "exempt"` early return | ✅ | |
| P03582 | `taxComponents` are carried through ONLY when their sum really is this bill's rate | node: 18% bill with CGST 2.5 + SGST 2.5 configured → `taxComponents: []` | ✅ | `compsMatch` |
| P03583 | …and never on a mixed-rate bill | read `!mixedRates &&` in `compsMatch` | ✅ | |
| P03584 | `taxModel` sums named components to the rate | node: 2.5+2.5 → rate 0.05, pct 5 | ✅ | |
| P03585 | …drops a component with no label or a non-positive rate | node | ✅ | |
| P03586 | …falls back to `settings.tax_rate`, then to 5% | node: `{}` → 0.05 | ✅ | |
| P03587 | …returns a genuine ZERO for a composition restaurant, not "5% we then hide" | node: `price_tax_mode:"composition"` → rate 0 | ✅ | mig 272 |
| P03588 | `splitTax` gives the LAST component the remainder, so the lines foot exactly | node: 19 at 2.5+2.5 → 10 + 9 | ✅ | |
| P03589 | `splitTax` refuses to hand rupees to a 0% component | node: `splitTax(19,[{rate:0}])` → `[]` | ✅ | 2026-08-06 |
| P03590 | `splitTax` with no components returns no rows rather than dividing by zero | node: `splitTax(19,[])` → `[]` | ✅ | |
| P03591 | `splitTax(0, comps)` prints zero-amount rows, and `billData` never asks it to | read `addWhole <= 0 ? []` | ✅ | a ₹0 tax line would read as a mistake |
| P03592 | `discPct` keeps one decimal and drops a trailing ".0" | node: 10% / 12.5% | ✅ | |
| P03593 | `discPct` returns "" when there is nothing to say (0 subtotal, 0 discount, sub-0.05% rounding) | node | ✅ | |
| P03594 | `discPct` is the ONE definition — the paper, the manager screen and the tablet all call it | grep in editor/app.js, tablet/app.js, SessionTableBill.tsx, billPreview.ts | ✅ | |
| P03595 | `combineBillLines` groups by everything a guest can see differ (title, price, options, removals, note) | node: "Special 1"@50 and "Special"@150 never merge | ✅ | |
| P03596 | …and its separator is a visible `` escape, not a raw invisible byte | read the SEP comment | ✅ | |
| P03597 | …and a qty of 0 or garbage becomes 1, never 0 or NaN | node | ✅ | |
| P03598 | `mrpTaxInside` only counts lines that are BOTH `is_mrp` and `tax_mode:"incl"` | read the guard | ✅ | |
| P03599 | `mrpPart` returns 0 on a composition restaurant (splitting "food" from "MRP" says nothing there) | node | ✅ | |
| P03600 | `billData` prefers the pair captured at invoice time over the guest's own name | read the `row` / `named` order | ✅ | mig 227 |
| P03601 | `bill_customer_print:false` saves the pair but prints neither line | node: `printCust` false → cust "" and phone "" | ✅ | |
| P03602 | A 10-digit printed phone is grouped 5+5 | node: "9825012345" → "98250 12345" | ✅ | |
| P03603 | A 12-digit number beginning 91 is peeled to the national number, then grouped | node: "919825012345" → "98250 12345" | ✅ | 2026-08-06 |
| P03604 | An 11-digit number beginning 0 is peeled the same way | node: "09825012345" | ✅ | |
| P03605 | Anything else prints whole rather than being wrongly chopped | node: a 13-digit value | ✅ | deliberate |
| P03606 | The bill's date comes from an explicit `now`, then `invoice_at`, then `closed_at`, then really now | read `stampAt` | ✅ | |
| P03607 | A reprint of last June's invoice stamps June, not today | node: `invoice_at:"2025-06-10"` → dateStr holds 2025 | ✅ | |
| P03608 | An invalid stored timestamp falls back to now rather than printing "Invalid Date" | node: `invoice_at:"nonsense"` | ✅ | |
| P03609 | The bill's date is pinned to en-IN + Asia/Kolkata, never the printing device's locale | node under TZ=America/New_York — same string | ✅ | T15, 2026-08-05 |
| P03610 | The bill's TIME is pinned the same way | node under four time zones — "09:31 pm" every time | ✅ | probe TZ |
| P03611 | `billData` sets `autoPrint` true unless the caller says otherwise | node | ✅ | |
| P03612 | The mixed-rate tax split uses the restaurant's OWN component shape, so a third component (a cess) survives | node: CGST+SGST+CESS at 6% and 18% → two CESS rows | ✅ | T7 F1 |
| P03613 | …and each rate's rows still foot to that rate's tax | node: rows sum === round(tax) | ✅ | |
| P03614 | A restaurant with NO components configured falls back to CGST/SGST halves on both branches | node | ✅ | |
| P03615 | The `mrpNote` claims tax is inside the MRP price ONLY when the restaurant treats MRP as inclusive | node: `mrp_tax_treatment:"none"` → note "" | ✅ | probe 3 §5 |

## C · READING THE CODE FOR CORRECTNESS — the KITCHEN TICKET — 25

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03616 | `kotWhen` prints the TIME in one fixed format on every device in the building | node under TZ=Asia/Kolkata, America/New_York, Europe/London, Australia/Sydney — identical string | ❌→✅ | **F1 fixed**: the same order printed "YESTERDAY 09:31 pm", "12:01 pm", "YESTERDAY 05:01 pm" and "02:01 am"; now pinned to en-IN + Asia/Kolkata like the bill and the banquet sheet |
| P03617 | `kotWhen`'s TODAY / YESTERDAY / date decision is made in India time, not the device's | node: an order at 2026-08-16T20:00Z under TZ=Europe/London — the day must match the bill's date row | ❌→✅ | part of F1 |
| P03618 | …and it uses the 05:00 IST BUSINESS day, so a ticket rung at 23:50 does not read "YESTERDAY" at 00:10 of the same service | node: ticket 2026-08-16T18:20Z (23:50 IST) read at 2026-08-16T18:40Z (00:10 IST) → plain time | ❌→✅ | part of F1; mig 044 / `lib/businessDay.ts` / `docs/NUMBERING.md` |
| P03619 | The month name is built explicitly, never from the system locale's own ordering ("6 AUG", never "AUG 6") | read `kotWhen`; node under two locales | ✅ | |
| P03620 | …and the day number is India's day, not the device's | node: 2026-08-16T20:00Z under TZ=UTC → 17 AUG | ❌→✅ | part of F1 — `d.getDate()` was device-local |
| P03621 | `kotWhen` never prints "Invalid Date" | node: `kotWhen("garbage")`, `kotWhen(null)`, `kotWhen(0)` → "" | ✅ | |
| P03622 | `kotWhen` lives in ONE place — four callers used to build this string themselves | grep: kitchen/app.js and editor/app.js both call `LFH_BILLDOC.kotWhen` | ✅ | |
| P03623 | The ticket carries NO money — a KOT is for the kitchen, not a bill | node: no ₹, no Subtotal, no TOTAL in `kotDocHtml` output | ✅ | |
| P03624 | A reprinted ticket carries the big bordered "*** Reprint · Duplicate ***" banner | node: `reprint:true` | ✅ | owner, 2026-08-04 |
| P03625 | …at the very TOP, above the header | node: index of `class="rp"` < index of `class="h"` | ✅ | |
| P03626 | …at ≥16px and uppercase | node: the `.rp` rule | ✅ | |
| P03627 | A FIRST print never carries it — a fresh ticket branded DUPLICATE is a lie on paper | node: `reprint` absent → no `class="rp"` | ✅ | |
| P03628 | No panel draws its own duplicate banner | grep `class="rp"` across the print surfaces | ✅ | |
| P03629 | The ticket prints in ONE ink — no grey, no italics (a thermal head fakes grey with sparse dots) | node: no hex outside #000/#fff in the ticket CSS, no `font-style:italic` | ✅ | 2026-08-05 |
| P03630 | Nothing on the ticket is below 12px, including the per-line note in its bare `<small>` | node: `.kl small{font-size:12px}` present | ✅ | 2026-08-06 |
| P03631 | An empty ticket says "(no items)" rather than printing a blank sheet | node: `lines: []` | ✅ | |
| P03632 | Every ticket string is escaped — dish, options, removals, note, table label, KOT number, restaurant name | node: markup in each field prints literally | ✅ | |
| P03633 | `kotLineHtml` renders options as "(a, b)", removals as "— no x", and the note on its own line | node | ✅ | |
| P03634 | …and drops empty entries rather than printing "(, )" | node: `options:["", null, "x"]` → "(x)" | ✅ | `.filter(Boolean)` |
| P03635 | `kotLineHtml` accepts both string options and `{label}` objects | node: both shapes | ✅ | |
| P03636 | A qty of 0/absent prints "1×", never "0×" | node | ✅ | |
| P03637 | The allergy box renders only when there are allergies, and is never covered by the toolbar | read `allergHtml` + the bar-above-ticket comment | ✅ | |
| P03638 | `@page{margin:0}` and `break-inside:avoid` keep a ticket on ONE piece of paper | node | ✅ | |
| P03639 | A ticket printed from a panel goes through a hidden iframe with NO toolbar and NO script | node: `note` absent → no `class="bar"`, no `<script>` | ✅ | |
| P03640 | A ticket opened as its own page (the admin preview) gets a Print button, a ✕ Close and Esc | node: `note` set → bar + `pageScriptKot` | ✅ | |

## D · READING THE CODE FOR CORRECTNESS — the BANQUET sheet — 25

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03641 | A saved bill's FROZEN `tax_lines` (mig 239) are used when present, so a reprint years later cannot be re-split | read the `b.tax_lines` branch | ✅ | |
| P03642 | With empty `tax_lines`, components are used ONLY when their sum is the rate this bill was charged | read `cMatch` | ✅ | 2026-08-05 |
| P03643 | …else the CGST/SGST halves if THEY match | read `halvesMatch` | ✅ | |
| P03644 | …else one line naming the bill's own effective rate | read the final fallback | ✅ | |
| P03645 | Per-line tax cells are allocated last-takes-the-remainder, so each column foots to its TOTAL row exactly | node: three multi-line sheets, paisa-exact | ✅ | T7 F10 |
| P03646 | The per-line taxable column foots to the BILL's taxable, not just to itself | read the `drift` absorption on the last line | ✅ | T7 I8 |
| P03647 | …and it never divides by zero when every line is ₹0 | node: all lines price 0 → `grossAll || 1` | ✅ | |
| P03648 | A sheet with NO lines still renders its TOTAL row and its money box | node: `lines: []` | ✅ | |
| P03649 | The date and time are pinned to Asia/Kolkata, not the printing device | node under four time zones — "16-08-2026 09:31PM" every time | ✅ | 2026-08-06 |
| P03650 | An advance's date is pinned to IST too (a bare "2026-08-01" is midnight UTC) | read the `advances` loop | ✅ | |
| P03651 | The function date is pinned to IST | read the `fn_date` line | ✅ | |
| P03652 | `bqWords` names the same amount as the figure beside it, paise included | node: 1234.56 → "…and Fifty-Six Paise Only" | ✅ | |
| P03653 | …names a currency on the common whole-rupee case | node: 1234 → "…Thirty-Four Rupees Only" | ✅ | T7 F2 |
| P03654 | …gets the singular right ("One Rupee", "One Paisa") | node | ✅ | |
| P03655 | …says "Minus" on a negative rather than printing positive words | node: −1234.56 | ✅ | |
| P03656 | …handles 0, sub-rupee, Lakh and Crore | node: 0, 0.5, 1e5, 1e7, 999999999 | ✅ | probe 1 |
| P03657 | A renamed table reaches the banquet sheet, not the bare digit | node: `table_names:{5:"Terrace 2"}` → "Terrace 2" | ✅ | T3, 2026-08-06 |
| P03658 | …resolved HERE, so the manager print and the admin preview cannot drift | read the `bqTableDisp` comment | ✅ | |
| P03659 | A banquet with no table prints no Table row rather than an empty one | node: `table_number:null` | ✅ | |
| P03660 | `bqPaper` clamps every margin into its own range and defaults A5 | node: out-of-range values | ✅ | |
| P03661 | `bqTaxModel` uses the banquet's own components (mig 239), falling back to the dine-in model | node: with and without `banquet_tax_components` | ✅ | |
| P03662 | The A4/A5 `@page size` IS explicit here (unlike the thermal bill, where forcing a size makes CUPS rotate) | read the CSS comment + the `@page` line | ✅ | |
| P03663 | A round-off row appears when the stored total does not equal taxable + tax | node: total 1244 vs 1243.84 | ✅ | |
| P03664 | Received / Balance rows appear only when money has been received, and Balance never prints negative | read the `recv > 0` branch + `Math.max(0, bal)` | ✅ | |
| P03665 | The `<colgroup>` is built by concatenation, not by joining half-open tags (a stray "<" once printed) | read the `colg` comment | ✅ | |

## E · READING THE CODE FOR CORRECTNESS — the NUMBERS — 10

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03666 | `financialYear` rolls on 1 April, not 1 January | node: 2026-03-31 → "2025-26"; 2026-04-01 → "2026-27" | ✅ | |
| P03667 | `invFmt` is `<prefix>/<FY>/<6-digit>` | node: `INV/2026-27/000041` | ✅ | |
| P03668 | The FY is the INVOICE'S OWN date, so a March invoice reprinted in April keeps its year | node: `invFmt(41,"2026-03-31")` in August → 2025-26 | ✅ | |
| P03669 | `invFmt(null)` returns "" so the Invoice row disappears rather than printing "INV//" | node | ✅ | |
| P03670 | A 7-digit invoice number is never truncated by the 6-digit pad | node: `invFmt(1234567)` → "1234567" | ✅ | pad, not slice |
| P03671 | The prefix falls back to "INV" when the restaurant has set none | node: `invFmt(1,"…",undefined)` | ✅ | |
| P03672 | A cancelled sheet NAMES the invoice number it retired, marked "— voided" | node: `session.invoice_no` set + all orders cancelled | ✅ | mig 073, COMPLIANCE §2 |
| P03673 | A bill cancelled before any invoice existed prints NO invoice row at all | node: `invoice_no: null` + cancelled → no Invoice row | ✅ | mig 331 |
| P03674 | The bill number is printed from the SESSION, never re-derived on the paper | read `billNo: sess.bill_no` | ✅ | |
| P03675 | The document never invents, renumbers or reformats a number it was handed | grep: no counter arithmetic anywhere in billdoc.js | ✅ | |

## F · READING THE CODE FOR CORRECTNESS — `billcustomer.js` — 25

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03676 | `norm()` folds "+91 98250 12345", "098250 12345", "091…" and "9825012345" to one 10-digit key | node port of `norm` | ✅ | mirrors `lfh_phone10`, mig 227 |
| P03677 | …and hands back anything else unchanged rather than guessing | node: an 8-digit and a 15-digit value | ✅ | |
| P03678 | `pretty()` groups a 10-digit number 5+5 and leaves others alone | node | ✅ | |
| P03679 | Every interpolated value in the sheet's markup is escaped | read: title, name, phone, the suggestion rows | ✅ | |
| P03680 | The suggestion buttons escape both `data-p` and `data-n` inside double-quoted attributes | read `showMatches` | ✅ | |
| P03681 | Layer 1 (the on-device map) answers a known 10-digit number with NO request at all | read `lookup` — the `known.has(p)` early return | ✅ | |
| P03682 | Layer 2 (the per-prefix cache) means backspacing and retyping never repeats a request | read the `prefixCache` branch | ✅ | |
| P03683 | Layer 3 asks only at ≥4 digits, debounced, and at most once per prefix | read `MIN_LOOKUP`, `DEBOUNCE_MS`, the `prefixCache.set` | ✅ | |
| P03684 | A late answer for older digits is dropped by sequence number | read `mine !== seq` | ✅ | |
| P03685 | …and an answer that lands after the sheet closed is dropped too | read `|| done` | ✅ | |
| P03686 | A failed lookup (offline, slow) leaves the sheet usable rather than throwing | read the empty `catch` | ✅ | |
| P03687 | Once the waiter edits the name themselves, a looked-up name never overwrites it | read `nameTouched` | ✅ | |
| P03688 | The Generate button is disabled until the rule is met, and the rule differs for required / optional | read `sync()` | ✅ | |
| P03689 | A tap on a not-ready Generate never dies in silence — it says which box is missing and focuses it | read `goBtn.onclick` AND check the button can actually receive the click | ❌→✅ | **F4** — the handler existed and read correctly; the `disabled` attribute made it unreachable. Reading the code alone passed this row; only tapping it found the fault. |
| P03690 | The red "missing" message clears once the missing box is filled | node/headless: type the 10th digit after the error | ✅ | `showNew`/`showKnown` repaint |
| P03691 | Hardware BACK closes just this sheet, via `LFH_BACK.layer` | read the `offBack` wiring | ✅ | the panel back rule |
| P03692 | …and the unregister runs exactly once, whichever way the sheet is dismissed | read `finish()`'s `done` latch | ✅ | |
| P03693 | Every dismissal path resolves the promise — ✕, Cancel, backdrop, BACK — so no caller hangs | read all four wirings | ✅ | never leave a promise unresolved |
| P03694 | A second `ask()` removes the first sheet rather than stacking two | read `document.querySelector(".bcust-overlay")?.remove()` | ✅ | |
| P03695 | The digit counter turns green at exactly 10 | read `paintCount` | ✅ | |
| P03696 | Typing is capped at 13 digits so a mistyped run cannot grow forever | read the input handler's `.slice(0,13)` | ✅ | |
| P03697 | Enter moves phone → name, and Enter on the name submits only when the button is live | read the two keydown handlers | ✅ | |
| P03698 | A prefilled bill opens with the customer filled in and the cursor on the NAME | read the `pre` block + the focus line | ✅ | owner, 2026-07-30 |
| P03699 | …and re-looks-up immediately so it can say "Returning customer · N visits" | read `lookup(true)` | ✅ | |
| P03700 | The caret does not jump to the end when the waiter corrects a digit in the middle of the number | headless: place the caret after digit 3, type, read `selectionStart` | ❌→✅ | **F3 fixed**: reformatting reassigned `.value` and dropped the caret to the end |

---

## G · CONFORMANCE TO THE PROJECT'S OWN WRITTEN RULES — `docs/NUMBERING.md` — 25

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03701 | The doc names exactly THREE numbers and says where each one lives | read the table against `orders.kot_no`, `sessions.bill_no`, `sessions.invoice_no` | ✅ | |
| P03702 | `orders.kot_no` really exists and is what the kitchen ticket prints | grep the migrations + `kotDocHtml({kot})` | ✅ | |
| P03703 | `sessions.bill_no` really exists and is what the bill's "Bill no" row prints | grep + `billData`'s `billNo` | ✅ | |
| P03704 | `sessions.invoice_no` really exists and is what `invFmt` formats | grep + `billData`'s `invNo` | ✅ | |
| P03705 | "Resets every business day (05:00 IST)" matches migration 044 and `lib/businessDay.ts` | read both | ✅ | |
| P03706 | "Invoice number never resets" matches `seq_counters` (mig 037) having no day in its key | read the migration | ✅ | |
| P03707 | "Bill number given out on the table's FIRST ORDER, not on open" matches migration 040 | read the migration | ✅ | |
| P03708 | The counters are per restaurant (mig 080) | read the migration | ✅ | |
| P03709 | The two stated causes of an honest bill-number gap are both still true (a cancelled first order; the atomic row lock in mig 051) | read mig 051 | ✅ | |
| P03710 | "A missing bill number never means a sale was removed" still holds — nothing hard-deletes a sale | cross-read `docs/COMPLIANCE-GUARDRAILS.md` §3.0 | ✅ | |
| P03711 | The doc sends people to the Audit (`deletion_audit`, mig 251) and the admin bill ledger, and both still exist | grep both | ✅ | |
| P03712 | …and it still WARNS that the Activity log is the wrong place (pruned at 30 days by `lfh_prune_logs`, mig 158) | read the ⚠ block + the migration | ✅ | corrected 2026-08-11 |
| P03713 | "A voided invoice number stays on the record and is never reused" matches migration 073 | read the migration | ✅ | |
| P03714 | "A bill CANCELLED before any invoice existed never takes a number" matches migration 331 | read the migration | ✅ | owner, 2026-08-16 |
| P03715 | …and the printed document agrees: no Invoice row on such a bill | node, see P03673 | ✅ | |
| P03716 | "Dine-in, parcel, banquet and delivery all draw the SAME daily KOT counter" matches migration 261 | read the migration | ✅ | |
| P03717 | …and the printed parcel receipt really carries both numbers | grep `printParcelReceipt` in editor/app.js | ✅ | |
| P03718 | "A writer bringing its own number pushes the counter past itself" matches migration 296 | read the migration | ✅ | |
| P03719 | The "Where each rule actually lives" table names a REAL file for every row | `ls supabase/migrations/` for each of the seven | ✅ | all seven exist |
| P03720 | …and it does not omit a rule the doc's own prose relies on | compare the prose's citations (036/040/044/051/073/080/261/296/331) against the table | ❌→✅ | **F8 fixed**: the table listed seven and silently omitted **261** (one KOT series, all channels) and **331** (a cancelled sale takes no number) — the two newest rules, and the two the prose leans on hardest |
| P03721 | The doc mentions that an issued bill is signed and chained (mig 332), since that is now the strongest answer to "where did that number go" | read the doc | ❌→✅ | **I1 built**: a short section added, pointing at mig 332 rather than restating it |
| P03722 | The doc never claims a number can be reused or a gap closed | read it end to end | ✅ | |
| P03723 | The doc never tells anyone to delete a bill (there is no such permission — R27) | read it | ✅ | |
| P03724 | Every migration number the doc cites is found by CONTENT, not assumed by filename | the fixes above cite the file names as they stand today and say so | ✅ | 18 numbers are already duplicated on main |
| P03725 | The doc still reads as one page a stranger can answer "which number is this?" from | read it end to end after the edits | ✅ | |

## H · CONFORMANCE — `public/panels/billdoc.d.ts` is the ONE description — 20

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03726 | Every function on the runtime `API` object is declared in the `.d.ts` | node: compare `Object.keys(API)` against the `export function` list | ✅ | 23 / 23 |
| P03727 | …and nothing is declared that does not exist at runtime | the same comparison, the other way | ✅ | |
| P03728 | `BillDocData` declares every field `billDocHtml` actually reads | grep every `d.<field>` in the render, compare with the interface | ❌→✅ | **F6 fixed**: `cancelled` — the flag the whole document's identity turns on (Tax Invoice vs Cancelled Bill) — was undeclared, so a TypeScript caller could not render a cancelled bill at all |
| P03729 | `KotDocData` declares every field `kotDocHtml` reads | same method | ✅ | 13 / 13 |
| P03730 | The banquet entry point's argument type matches what `banquetDocHtml` reads | same method | ✅ | |
| P03731 | `BillDocData.noBar` is declared and its consequence (no toolbar AND no script) is written down | read the comment | ✅ | |
| P03732 | `BillDocData.autoPrint` is declared and says it opens the dialog by itself | read | ✅ | |
| P03733 | `BillDocData.composition` says the heading, the `<title>` and the declaration all change | read | ✅ | |
| P03734 | `BillDocData.taxRows` says an EMPTY array means no tax line at all | read | ✅ | |
| P03735 | `BillDocData.inclRows` says it is reported under the total and never added | read | ✅ | |
| P03736 | `BillDocData.nontax` / `mrpLabel` / `mrpNote` describe the MRP block truthfully | read against the render | ✅ | |
| P03737 | `BillDocLine.price` says the unit price INCLUDES its priced add-ons | read against the `baseUnit` arithmetic | ✅ | |
| P03738 | `KotDocData.reprint` says a reprint carries the DUPLICATE banner | read | ✅ | |
| P03739 | `BillMoney` declares all sixteen fields `billMoney` returns | node: compare keys | ✅ | |
| P03740 | `TaxModel` matches what `taxModel` returns | node | ✅ | |
| P03741 | `orderTaxRate`'s three documented cases are the three the code takes | read the doc comment against the function | ✅ | |
| P03742 | A field the document IGNORES is not documented as if it were rendered | grep `d.taxable` — never read; four callers pass it | ❌→✅ | **F7 fixed**: `taxable?: number` read as an input to the "Taxable value" row; it is derived and the passed value is discarded. Now says so. |
| P03743 | `invFmt`'s doc comment states the FY is the invoice's own | read | ✅ | |
| P03744 | `billRows`'s doc comment states it never changes what is charged | read | ✅ | |
| P03745 | `npm run typecheck` still passes with the `.d.ts` edits | run it | ✅ | |

## I · CONFORMANCE — the project's rules, inside `billdoc.js` — 25

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03746 | ONE bill, ONE KOT, ONE file — no second copy of either document anywhere | `npm run verify:print-format` §2 | ✅ | |
| P03747 | …and no second copy of the banquet sheet | same, the third fingerprint | ✅ | |
| P03748 | The file stays plain JavaScript with NO imports, so all three worlds can load it | grep `^import|require(` in billdoc.js | ✅ | panels, Next server, React |
| P03749 | It exports to `module.exports` AND `globalThis.LFH_BILLDOC` | read the tail | ✅ | |
| P03750 | It computes no money that the caller did not hand it, except the derivations it documents | read `billDocHtml` — only `billRows` and `orderedValue` | ✅ | |
| P03751 | Money is `en-IN`, everywhere, on every document | grep every `toLocaleString`/`toLocaleDateString`/`toLocaleTimeString` | ❌→✅ | part of **F1** — `kotWhen`'s time was the one left on the device's locale |
| P03752 | One time zone everywhere: Asia/Kolkata on the bill, the banquet sheet AND the ticket | the same grep | ❌→✅ | part of **F1** |
| P03753 | A tap never dies in silence — every button in every document does something visible | read the three toolbars + `goBtn` | ✅ | |
| P03754 | No window closes itself on `afterprint` (Print and Cancel are the same event) | `verify:print-format` | ✅ | PR #716 |
| P03755 | Esc dismisses every document window | grep the three `keydown` handlers | ✅ | |
| P03756 | The screen-only toolbar never reaches the paper, on any of the three documents | grep `@media print{.bar{display:none !important}}` ×3 | ✅ | |
| P03757 | …and is excluded from the page-length measurement, so no blank roll is fed | read `measure()` | ✅ | |
| P03758 | The thermal documents force NO `@page size` (CUPS rotates a forced-smaller page) | read the CSS comment + the absence | ✅ | |
| P03759 | …except the measured one the script installs at print time, which matches the roll | read `measure()`'s `size:80mm <h>mm` | ✅ | |
| P03760 | The A4/A5 banquet sheet DOES force its size, deliberately | read | ✅ | |
| P03761 | ONE INK: the bill's CSS holds no grey and no italics | node: scan the bill stylesheet for non-#000/#fff hex | ✅ | the toolbar's greys are screen-only |
| P03762 | …and the ticket's does not either | `verify:print-format` §8 | ✅ | |
| P03763 | Nothing printed is below 10.5px on the bill or 12px on the ticket | read every `font-size` in both print stylesheets | ✅ | |
| P03764 | "Blur" is not used anywhere in these documents (a thermal head cannot render it) | grep `backdrop-filter` | ✅ | |
| P03765 | No new `settings` column is required by anything in this file | grep: every setting read is an existing one | ✅ | the 110-column rule |
| P03766 | Nothing here can hide, erase or edit an issued sale | read: the document renders, it never writes | ✅ | COMPLIANCE §3.0 |
| P03767 | A cancelled sale is still fully visible on paper, with its number | node, see P03547/P03672 | ✅ | |
| P03768 | The KOT's fixed 280px body is NOT made responsive (R26) | read the REJECTED comment above `kotDocHtml` — still present and still obeyed | ✅ | owner, 2026-08-16 |
| P03769 | No rejected idea in `docs/REJECTED-IDEAS.md` is re-introduced by anything this run changed | re-read R11, R12, R15, R21, R23, R26, R27 against the diff | ✅ | |
| P03770 | Every fix this run made left a `verify:*` check behind | the guard is written and PROVEN (it fails on the pre-fix code, passes on the fixed code) | ⏭ | **🔗 HANDOFF to T28** — `scripts/**` and the `verify:*` entries in `package.json` are T28's territory, so I may not land it. The complete drop-in source is in `T8-findings.md`; every row's "how to verify" here is precise enough to re-run by hand until it lands. |

## J · CONFORMANCE — the print surfaces and `billcustomer.js` — 30

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03771 | The manager panel prints the shared bill | `verify:print-format` §3 | ✅ | |
| P03772 | The manager panel prints the shared ticket | same | ✅ | |
| P03773 | The kitchen board prints the shared ticket | same | ✅ | |
| P03774 | The waiter tablet prints the shared bill through the shared assembler | same §9 | ✅ | |
| P03775 | The Access "Format of…" preview renders the shared bill | same | ✅ | |
| P03776 | …and the shared ticket | same | ✅ | |
| P03777 | The settings form's bill preview reaches the shared document | same | ✅ | |
| P03778 | The admin's banquet preview renders the REAL sheet | same | ✅ | |
| P03779 | The Audit's evidence card renders the real bill, with no dead buttons on it | read `lib/auditDetail.ts` — `noBar:true, autoPrint:false` | ✅ | 2026-08-12 |
| P03780 | Both panels load `/panels/billdoc.js` BEFORE `app.js` | `verify:print-format` §4 | ✅ | |
| P03781 | The tablet loads it too (it prints bills) | grep the script tag in `public/panels/tablet/index.html` | ✅ | |
| P03782 | `billcustomer.js` is loaded by both panels that call it | grep the script tags in editor + tablet `index.html` | ✅ | |
| P03783 | The panel's own `api()` helper is what does the lookup (auth + action id ride along) | read `ask({api})` and both call sites | ✅ | |
| P03784 | The sheet never POSTs to `/api/staff-login` or any other auth route | grep | ✅ | the rate-limit rule |
| P03785 | The lookup is a scoped, column-named, row-capped read | read `app/api/editor/[...path]/route.ts` → `customer-search` | ✅ | at most 6 rows |
| P03786 | …anchored on the `(restaurant_id, phone)` index, so it is not a full scan | read the route + the index | ✅ | the egress rule |
| P03787 | …and it is never fired faster than one debounced request per new prefix | read `lookup` | ✅ | |
| P03788 | The server enforces the same rule the sheet asks for, so a stale page cannot slip past | read `lib/billCustomer.ts` | ✅ | |
| P03789 | An unconfigured restaurant fails OPEN — a missing settings row never blocks billing | read `billCustomerRequired` | ✅ | |
| P03790 | Capturing the customer and PRINTING them are separate decisions | read `bill_customer_print` in `billData` and the sheet's hint | ✅ | |
| P03791 | The sheet says, in plain words, why the number is being asked for | read the `bcust-why` line | ✅ | |
| P03792 | The sheet registers with `LFH_BACK`, not a hand-rolled pushState | read | ✅ | the mobile back rule |
| P03793 | The sheet renders nothing until it is asked for, and removes itself when done | read `ask()` | ✅ | |
| P03794 | A renamed table reaches the printed BILL | trace `tableDisp` from `tableName`/`mergeGroupLabel` in editor + tablet | ✅ | |
| P03795 | A renamed table reaches the printed KOT | trace `tablePrintLabel` in editor/app.js | ✅ | |
| P03796 | A renamed table reaches the printed BANQUET sheet | node: `table_names` resolved inside `banquetDocHtml` | ✅ | |
| P03797 | A merged party prints ONE bill under the group's label | read `mergeGroupLabel` feeding `tableDisp` in both panels | ✅ | |
| P03798 | The Audit's evidence bill shows the table as it was NAMED, not the bare number | read `lib/auditDetail.ts` → `tableDisp: String(was.table_number)` | ⏭ | **🔗 HANDOFF** — the fix is in `lib/auditDetail.ts`, another terminal's file. Written up in the findings and the chat report. |
| P03799 | Nothing in my territory writes to a database, fires an alert, or can raise a notification | grep: no fetch, no rpc, no toast in billdoc.js | ✅ | |
| P03800 | Nothing in my territory reads or names the AV-live stack | grep | ✅ | |

---

## K · WATCHING IT RUN — headless Chromium, my own port 4108 — 75

Every row here renders the REAL document HTML in a real browser (`chromium.launch()` from this
worktree's own `node_modules`), and asserts what is on screen or on the emitted PDF — never the
source string. The dev server on **4108** is proved mine before any screenshot is trusted, and it is
killed the moment this block ends.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03801 | Port 4108 is serving MY worktree, not another terminal's | fetch `/panels/billdoc.js` off 4108, compare the byte length with the file on disk | ✅ | |
| P03802 | `/panels/billdoc.js` is served at all (a 404 = a blank print) | HTTP 200 + `LFH_BILLDOC` in the body | ✅ | |
| P03803 | …and evaluates in a browser without throwing | `page.addScriptTag` then `typeof LFH_BILLDOC` | ✅ | |
| P03804 | …and defines all 23 entry points on `window` | evaluate `Object.keys(LFH_BILLDOC).length` | ✅ | |
| P03805 | `/panels/billcustomer.js` is served and defines `window.LFH_BILLCUST` | same method | ✅ | |
| P03806 | A rendered bill parses as valid HTML with no leftover template markers | `document.body.innerHTML` holds no `${`, no `undefined`, no `NaN` | ✅ | |
| P03807 | …on a cancelled bill too | same | ✅ | |
| P03808 | …on a composition Bill of Supply too | same | ✅ | |
| P03809 | …on an MRP bill too | same | ✅ | |
| P03810 | …on a tax-inside bill too | same | ✅ | |
| P03811 | …on a mixed-rate bill too | same | ✅ | |
| P03812 | The rendered bill body fits inside 66mm of an 80mm roll (content ≤ ~249px at 96dpi) | measure `document.body.scrollWidth` with the page at 80mm | ✅ | the POS-80 target |
| P03813 | …with a 60-character dish name | same, long title | ✅ | |
| P03814 | …with a ₹1,07,880 amount column | same | ✅ | |
| P03815 | …with a three-component tax block (CGST + SGST + CESS) | same | ✅ | |
| P03816 | …with a customer name and mobile block | same | ✅ | |
| P03817 | No item row overflows its column at 80mm (no horizontal scroll) | `scrollWidth <= clientWidth` on `<table>` | ✅ | |
| P03818 | The TOTAL row's amount is not clipped by its own rule | measure the `.g` right edge against the body box | ✅ | |
| P03819 | The toolbar is genuinely absent under print media | `matchMedia("print")` emulation, `.bar` computed `display === "none"` | ✅ | |
| P03820 | …on the KOT too | same | ✅ | |
| P03821 | …on the banquet sheet too | same | ✅ | |
| P03822 | Browser-print (⌘P) produces a COMPLETE bill, not one clipped page | `page.pdf()` and count the text of every item row in the output | ✅ | the sweep-#5 lesson, re-checked on my own documents |
| P03823 | …the printed PDF carries no toolbar | `page.pdf()` text holds no "Print this"/"Close" | ✅ | |
| P03824 | …the printed PDF carries the TOTAL | text search | ✅ | |
| P03825 | …the printed PDF of a 40-line bill still contains the last line | `page.pdf()` on a long bill | ✅ | |
| P03826 | …and its item header appears ONCE, not on every page | count "Rate"+"Amt" header occurrences in the PDF text | ✅ | `thead{display:table-row-group}` |
| P03827 | The KOT's browser-print PDF is one page for an ordinary ticket | `page.pdf()` page count | ✅ | |
| P03828 | The banquet A5 sheet's PDF is A5-sized | `page.pdf()` with `preferCSSPageSize` | ✅ | |
| P03829 | …and A4 when the restaurant is set to A4 | same | ✅ | |
| P03830 | `measure()` runs on load and installs a `@page` matching the bill's own length | evaluate `document.getElementById("pagesize").textContent` | ✅ | |
| P03831 | …and that length EXCLUDES the toolbar, so no blank roll is fed | render with and without a toolbar, compare the installed height | ✅ | |
| P03832 | …and it re-measures before every reprint | click "Print again" with `window.print` stubbed, assert `measure` ran | ✅ | |
| P03833 | "🖨 Print this / again" is wired and calls `print()` | stub `window.print`, click, assert called | ✅ | |
| P03834 | "✕ Close" is wired and calls `close()` | stub, click, assert | ✅ | |
| P03835 | Esc closes the bill window | `page.keyboard.press("Escape")` with `close` stubbed | ✅ | |
| P03836 | `afterprint` focuses the ✕ Close button and closes nothing | dispatch the event, assert `document.activeElement` and no close | ✅ | |
| P03837 | A REAL bill fires the print dialog by itself | stub `print`, wait 400ms, assert called once | ✅ | |
| P03838 | A PREVIEW measures but never fires the dialog | same, assert NOT called | ✅ | |
| P03839 | The same is true of the banquet sheet, both ways | same | ✅ | |
| P03840 | A `noBar` bill loads with no toolbar, no script and no dialog | render in a sandboxed frame, assert | ✅ | the Audit card's shape |
| P03841 | The MRP stamp renders as a bordered box beside the dish name, not a shaded pill | computed `border-style` and `background` | ✅ | no grey on a thermal head |
| P03842 | The cancelled band renders as a 3px double border across the sheet | computed style of `.vband` | ✅ | |
| P03843 | The reprint banner renders the same way on the ticket | computed style of `.rp` | ✅ | |
| P03844 | Every printed colour resolves to pure black or pure white | walk the print-media computed styles of every element | ✅ | one ink |
| P03845 | Nothing printed computes below 10.5px | walk computed `font-size` under print media | ✅ | |
| P03846 | …and nothing on the ticket computes below 12px | same on the KOT | ✅ | |
| P03847 | A Hindi dish name renders as joined script, not broken glyphs | render "पनीर टिक्का", screenshot, read the image | ✅ | the `.split("")` lesson |
| P03848 | …and a Gujarati one does | render "પનીર", same | ✅ | |
| P03849 | …and an emoji footer does not blow up the line height | render the default 🥐 footer, measure | ✅ | |
| P03850 | A right-to-left customer name does not reverse the money column | render an Arabic name, measure the `.kv` box | ✅ | |
| P03851 | `kotWhen` returns the same string in the browser as in node | evaluate both, compare | ✅ | |
| P03852 | …and the same string with the browser's own time zone overridden to New York | `context({timezoneId})`, compare | ❌→✅ | **F1** — this is the check that catches it in a browser |
| P03853 | …to London | same | ❌→✅ | part of F1 |
| P03854 | …to Sydney | same | ❌→✅ | part of F1 |
| P03855 | …and with the browser's locale set to `en-US` and to `de-DE` | `context({locale})`, compare | ❌→✅ | part of F1 — 24-hour clocks and a swapped date order |
| P03856 | The bill's date row is already immune to all four | same method | ✅ | fixed 2026-08-05 |
| P03857 | The banquet sheet's date and time are already immune | same | ✅ | fixed 2026-08-06 |
| P03858 | The customer sheet opens, and its two boxes take input | `LFH_BILLCUST.ask()` in a page, type into both | ✅ | |
| P03859 | The digit counter tracks typing and turns green at 10 | type 9 then 10 digits, read the counter's class | ✅ | |
| P03860 | The number formats itself to "98250 12345" as it is typed | type 10 digits, read `.value` | ✅ | |
| P03861 | The caret stays where the waiter put it when they correct a middle digit | set `selectionStart`, type, read it back | ❌→✅ | **F3** |
| P03862 | …and typing at the END still behaves exactly as before | type straight through, read the value and the caret | ✅ | |
| P03863 | …and deleting a digit in the middle keeps the caret there | Backspace mid-number, read `selectionStart` | ❌→✅ | part of F3 |
| P03864 | Generate LOOKS not-ready until both boxes are satisfied — and stays tappable, so a tap can refuse visibly | headless: read `aria-disabled`, opacity, cursor and the glow at each step | ✅ | the check moved off the `disabled` attribute with the F4 fix; measured .45/not-allowed/no-glow → 1/pointer/glow |
| P03865 | Tapping a not-ready Generate says which box is missing and focuses it | headless: click it with 5 digits typed, read the status text and `document.activeElement` | ❌→✅ | **F4 fixed**: the button carried the real `disabled` attribute, which emits no click at all — so the handler written to say which box was missing could never run, and the tap did nothing whatsoever |
| P03866 | The red message clears once the missing digit is typed | continue typing, read the status | ✅ | |
| P03867 | Cancel resolves the promise with null | await `ask()`, click Cancel | ✅ | |
| P03868 | The ✕ resolves with null | same | ✅ | |
| P03869 | A backdrop tap resolves with null | click the overlay | ✅ | |
| P03870 | Enter on the phone box moves to the name box | press Enter, read `activeElement` | ✅ | |
| P03871 | Enter on the name box submits when the button is live | press Enter, await the promise | ✅ | |
| P03872 | A prefilled sheet opens filled in, with the cursor on the NAME | `ask({prefill})`, read both values and `activeElement` | ✅ | |
| P03873 | Opening `ask()` twice leaves exactly one sheet in the DOM | call twice, count `.bcust-overlay` | ✅ | |
| P03874 | The sheet's own markup cannot be broken by a customer named with markup | seed a match named `<b>x`, read the rendered suggestion | ✅ | |
| P03875 | The dev server on 4108 is stopped at the end of this block | `lsof -i :4108` returns nothing | ✅ | whatever you start, you end |

## L · LOOKING AT SCREENSHOTS I ACTUALLY READ — desktop 1280×800 and A35 360×780 dpr3 — 50

Every row below was looked at as an image, not asserted from the DOM.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03876 | An ordinary tax invoice reads as a bill a guest would trust — desktop | screenshot at 1280×800, read it | ✅ | |
| P03877 | …and on the A35 (360×780, dpr 3) | screenshot, read it | ✅ | |
| P03878 | The restaurant name is the boldest thing on the sheet, and the TOTAL the second | read both images | ✅ | bold is spent on exactly two things |
| P03879 | The item column, the qty, the rate and the amount line up as four columns | read | ✅ | |
| P03880 | An add-on sub-line is visibly subordinate to its dish | read | ✅ | |
| P03881 | The money block reads top-to-bottom as an addition a person can follow | read | ✅ | |
| P03882 | A discounted bill's Discount, Taxable value and tax rows read in the right order | read | ✅ | |
| P03883 | A Round off row does not look like an error — and is never more than a rupee or two | read a bill that has one, and check the size of every round-off this document can produce | ✅ | a ₹40 one was the tell that found F5; after the fix the MRP shapes produce ₹0 |
| P03884 | An MRP bill's "Food subtotal" and "MRP items" rows read as one column that adds up | read the rendered image and add the column up by hand, for MRP priced as EXEMPT **and** as TAX-INCLUSIVE | ❌→✅ | **F5 fixed** — the worst find of the run. An MRP line priced tax-inclusive was counted twice (once as a taxed item row, once as the MRP row) and a **"Round off − ₹40" on a ₹462 bill** silently absorbed it. Reading the image is what caught it; every code-read row above had passed. |
| P03885 | The MRP stamp is legible beside a long dish name | read | ✅ | |
| P03886 | A tax-inside bill's "Price includes" block reads as a statement, not another charge | read | ✅ | |
| P03887 | A composition Bill of Supply reads as a bill of supply, with its declaration | read | ✅ | |
| P03888 | …and shows no tax anywhere | read | ✅ | |
| P03889 | A CANCELLED bill is unmistakable at a glance | read | ✅ | |
| P03890 | …and its two money rows do not read as an arithmetic error | read | ✅ | |
| P03891 | …and it still names the invoice number it retired, marked voided | read | ✅ | |
| P03892 | A parcel bill's top block reads correctly with no table number | read | ✅ | |
| P03893 | The customer block reads as "who this is for", with the number grouped 5+5 | read | ✅ | |
| P03894 | A mixed-rate bill's two tax lines are readable as two different rates | read | ✅ | |
| P03895 | A three-component (CGST/SGST/CESS) block is readable | read | ✅ | |
| P03896 | The footer sign-off reads as a sign-off, not as another row of the bill | read | ✅ | |
| P03897 | An unconfigured restaurant's header does not leave a hole where the address was | read | ✅ | empty prints no line |
| P03898 | The toolbar sits above the bill and does not cover the first line | read the desktop image | ✅ | |
| P03899 | …and on the A35 it does not cover the first line either | read the phone image | ✅ | |
| P03900 | …and its Print/Close buttons are ≥40px tall on the phone | measure on the image | ✅ | |
| P03901 | A KOT reads as a kitchen ticket at arm's length — desktop | read | ✅ | |
| P03902 | …and on the A35 | read | ✅ | |
| P03903 | The KOT number and the table label are the two biggest things after the header | read | ✅ | |
| P03904 | An option and a removal on the same line are both legible | read | ✅ | |
| P03905 | A per-line note is legible, not the smallest text on the paper | read | ✅ | the 2026-08-06 fix |
| P03906 | The ⚠ AVOID allergy box is impossible to miss | read | ✅ | |
| P03907 | …and is not covered by the toolbar on a short ticket | read | ✅ | |
| P03908 | The REPRINT · DUPLICATE banner is unmistakable at a glance | read | ✅ | |
| P03909 | The ticket's time line reads as a time, and says the day when it is not today | read a today ticket and a five-day-old one | ✅ | |
| P03910 | …and reads identically with the browser set to New York | read both images side by side | ❌→✅ | **F1** — the images differed by nine and a half hours |
| P03911 | The banquet A5 sheet reads as a proper tax invoice | read | ✅ | |
| P03912 | …its per-line tax columns line up under their headings | read | ✅ | |
| P03913 | …its amount-in-words box reads as a sentence | read | ✅ | |
| P03914 | …its money box adds up as printed | read | ✅ | |
| P03915 | …the A4 version is not just a stretched A5 | read both | ✅ | |
| P03916 | …the pad mode leaves the pre-printed letterhead space empty | read | ✅ | |
| P03917 | The customer sheet reads as one question, not two bare boxes — desktop | read | ✅ | owner, 2026-08-01 |
| P03918 | …and on the A35 | read | ✅ | |
| P03919 | The "+91" prefix makes the box read as a phone box | read | ✅ | |
| P03920 | The digit counter is visible without covering the typed number | read | ✅ | |
| P03921 | "Returning customer · N visits" appears without the layout jumping | read before and after | ✅ | room is reserved |
| P03922 | The suggestion list is tappable on the A35 (rows ≥44px) | headless at 360×780 dpr3: measure the row box | ❌→✅ | **I2 built**: measured 29px — under a thumb, and a mis-tap bills the wrong person. Now 44px. |
| P03923 | The red "missing" message is legible against the sheet | read | ✅ | |
| P03924 | The sheet's buttons are reachable with one thumb on the A35 | read | ✅ | |
| P03925 | Every screenshot taken for this block was deleted afterwards | `ls` the scratch folder | ✅ | use, see, delete |

---

## M · TRACING A CHANGE ACROSS PANELS — 50

The rule this block exists for: a change for one channel must not silently change another. Each row
follows ONE thing from where it is set to every place it can come out of a printer.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03926 | A dish's price reaches the manager bill, the waiter bill and the Access preview as the same number | drive all three off one order set, compare the rendered Amt column | ✅ | |
| P03927 | A discount set on the floor reaches all three the same way | same | ✅ | |
| P03928 | …and its PERCENTAGE label is the same string on all three plus the on-screen bill card | compare `discPct` output | ✅ | |
| P03929 | The tax rate stamped on an order (mig 284) reaches all three identically | same | ✅ | |
| P03930 | Changing `tax_components` in Settings changes the preview and the printer together | render the preview and the bill off one settings object | ✅ | one file, one document |
| P03931 | Switching a restaurant to `price_tax_mode:"incl"` changes both | same | ✅ | |
| P03932 | Switching to composition changes the HEADING on both | same | ✅ | |
| P03933 | …and removes the tax line from both | same | ✅ | |
| P03934 | …and adds the declaration to both | same | ✅ | |
| P03935 | Setting `gstin` puts it on the manager bill, the waiter bill, the preview and the banquet sheet | render all four | ✅ | |
| P03936 | Leaving `gstin` empty prints no GSTIN line on any of the four | same | ✅ | never invent a tax number |
| P03937 | Leaving the address empty prints no address line on any of them | same | ✅ | 2026-08-04 |
| P03938 | `bill_footer` reaches all four | same | ✅ | |
| P03939 | `invoice_prefix` reaches the bill and the Audit's evidence card | render both | ✅ | |
| P03940 | `tax_label` reaches the mixed-rate fallback line and the MRP note | render both | ✅ | |
| P03941 | `bill_customer_print:false` hides the customer on the manager bill AND the waiter bill | render both | ✅ | |
| P03942 | …and still SAVES the pair (the server path is untouched by the print switch) | read `lib/billCustomer.ts` | ✅ | |
| P03943 | `mrp_tax_treatment:"inclusive"` adds the MRP note to the printed bill only when there are MRP lines | render both cases | ✅ | |
| P03944 | Renaming table 5 to "Terrace 2" changes the manager BILL | drive `tableName` → `tableDisp` | ✅ | |
| P03945 | …the waiter BILL | drive `tname` → `tableDisp` | ✅ | |
| P03946 | …the manager KOT | drive `tablePrintLabel` | ✅ | |
| P03947 | …the kitchen board's KOT | drive the kitchen's `kotDocHtml` call | ✅ | |
| P03948 | …and the BANQUET sheet | node: `table_names` inside `banquetDocHtml` | ✅ | |
| P03949 | …but NOT the Audit's evidence bill, which still prints the bare digit | read `lib/auditDetail.ts` | ⏭ | **🔗 HANDOFF**, see P03798 |
| P03950 | Merging two tables prints ONE bill under the group's label on both panels | read `mergeGroupLabel` in both | ✅ | |
| P03951 | A parcel order prints the SAME document as a table bill, with only the top line different | node: diff the two renderings | ✅ | owner, 2026-08-02 |
| P03952 | A parcel receipt carries the bill number and the invoice number off the order row | `verify:print-format` §6 | ✅ | mig 261 |
| P03953 | A platform (aggregator) order draws its numbers from the SAME two counters | read the migration's trigger | ✅ | |
| P03954 | …and an order that already has an invoice number is never renumbered by that trigger | same | ✅ | |
| P03955 | A banquet order draws its KOT from the same daily counter | read mig 261 | ✅ | |
| P03956 | Changing the KOT ticket's markup changes the kitchen board and the manager panel together | grep: both call `kotDocHtml` | ✅ | |
| P03957 | …and the admin's KOT preview with them | grep `RestaurantSettings.tsx` | ✅ | |
| P03958 | Changing `kotWhen` changes the kitchen board's auto-print, its queued reprints and both manager tickets | grep all four call sites | ✅ | this is why F1 was one fix, not four |
| P03959 | The reprint banner appears on a kitchen reprint and a manager reprint identically | read both call sites' `reprint` flag | ✅ | |
| P03960 | A first print from either panel carries no banner | same | ✅ | |
| P03961 | `billMoney` is the one money rule behind the manager's screen card and the printed bill | read `billMath` in editor/app.js | ✅ | |
| P03962 | …and behind the waiter panel's bill | read tablet/app.js | ✅ | |
| P03963 | …and `billRows` is what the manager's SCREEN rows are drawn from, so screen and paper quote the same rupees | grep `LFH_BILLDOC.billRows` | ✅ | |
| P03964 | …and `orderTaxRate` is what pay-in-parts uses, so a split can always equal the printed bill | read `lib/paySplit.ts` | ✅ | 2026-08-06 |
| P03965 | The admin bill ledger and the printed bill drop the same orders (cancelled + soft-deleted) | compare `lib/billLedger.ts`'s predicate with `billMoney`'s | ✅ | |
| P03966 | The Z-report's day and the bill's day are the same 05:00 IST business day | read `lib/businessDay.ts` against mig 044 | ✅ | |
| P03967 | …and after F1 the KITCHEN TICKET's day is that same business day | node: the three agree | ❌→✅ | part of F1 |
| P03968 | The customer sheet's `norm()` and the printed bill's phone grouping agree on a 10-digit number | node: both | ✅ | |
| P03969 | …and on a 12-digit number beginning 91 | node | ✅ | |
| P03970 | …and on an 11-digit number beginning 0 | node | ✅ | |
| P03971 | …and the database's `lfh_phone10` (mig 227) agrees with both | read the migration | ✅ | |
| P03972 | The customer sheet is opened from the manager panel and the waiter tablet with the same options shape | read both call sites | ✅ | |
| P03973 | A change to `billdoc.js` cannot reach a panel without the cache-busting `?v=` hash | read the script tags + `verify:panel-cache` | ✅ | staff can run a weeks-old panel |
| P03974 | The three guards this territory answers to all pass on the finished branch | run `verify:print-format`, `verify:one-number`, `verify:audit` | ✅ | |
| P03975 | Nothing I changed touched a file outside my four | `git diff --stat` | ✅ | |

## N · JUDGMENT — would a real restaurant want it this way? — 25

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P03976 | Hand this bill to a customer: is every line something they would understand? | read a rendered bill line by line, as a diner | ✅ | |
| P03977 | …and can they add it up themselves and reach the TOTAL? | do the addition by hand off the image | ✅ | that is what `billRows` is for |
| P03978 | Would a GST officer accept the heading, the GSTIN line and the tax split? | read a rendered tax invoice | ✅ | |
| P03979 | …and the Bill of Supply a composition restaurant hands over? | read | ✅ | |
| P03980 | Does a cancelled sheet read as a record rather than as a mistake? | read | ✅ | |
| P03981 | Would a cook, mid-rush, read this ticket correctly at arm's length? | read a KOT image | ✅ | |
| P03982 | …including the removal line ("— no onion") and the guest's own note? | read | ✅ | one ink, 12px floor |
| P03983 | …and would they know at a glance that a reprint is a reprint? | read | ✅ | |
| P03984 | Does the ticket's time tell a cook something they can act on? | read a today ticket and an old one | ✅ | |
| P03985 | Would a restaurant with tablets bought abroad get the right time on its tickets? | the F1 case | ❌→✅ | this is why F1 is worth fixing rather than filing |
| P03986 | Is a waiter at the till asked for the minimum, in the right order? | drive the customer sheet as a waiter | ✅ | mobile first, name auto-fills |
| P03987 | …and can they correct a mistyped digit without retyping the number? | the F3 case | ❌→✅ | |
| P03988 | …and is it obvious why the number is being asked for, if a guest asks? | read the sheet | ✅ | the `bcust-why` line |
| P03989 | Does anything on these documents ask the restaurant to trust software it cannot check? | read all three end to end | ✅ | every figure is on the paper |
| P03990 | Could a restaurant use any of this to make a sale disappear? | read: the document renders, it never writes | ✅ | CGST §132 |
| P03991 | Does the paper tell an inspector where a missing bill number went? | read `docs/NUMBERING.md` after the edits | ✅ | the Audit, the ledger, and now the chain |
| P03992 | Is a gap in the invoice series explainable from what is printed? | read a voided sheet | ✅ | it names the number it retired |
| P03993 | Would the owner recognise this bill as the one he approved in the preview? | render both off one settings object and compare | ✅ | the whole reason this file exists |
| P03994 | Is there anything on the paper a restaurant would want removed? | read as an owner | ✅ | no |
| P03995 | Is there anything MISSING a restaurant would ask for? | read as an owner, then render it | ✅ | the mig 332 chain reference was missing from the paper. Owner said build it (2026-08-17): `billDocHtml` now prints `Verification <seq> · <hash12>` when the session carries it. Renders nothing until the API sends the columns — handoff 4. |
| P03996 | Does the 80mm roll waste paper on any of these documents? | measure the emitted page heights | ✅ | `measure()` sizes the page to the bill |
| P03997 | Would a second copy of a bill be mistaken for the original? | render a bill with `reprint:true` and read the image | ✅ | it would have been — the KOT was branded DUPLICATE and the bill was not. Owner said build it (2026-08-17): the bill now carries the same band. A first print stays byte-identical; the panels need `sessions.bill_printed_at` to answer it across devices — handoffs 3–4. |
| P03998 | Is anything here slower than a rush can afford? | time 1,000 `billData`+`billDocHtml` renders | ✅ | 1,000 bills in under a second, no network |
| P03999 | Does the customer lookup cost the restaurant egress it does not need? | read the three layers | ✅ | most keystrokes cost nothing |
| P04000 | If nobody touched this territory again for a year, what would rot first? | judgment, written up | ✅ | the device-locale class of fault — now guarded by `verify:billdoc-paper` so it cannot come back silently |
