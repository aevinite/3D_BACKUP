# Sweep #9 · terminal 31 — THE DATABASE, migrations at positions 161–240
#
# Territory: the 80 files at positions 161–240 of `ls supabase/migrations | sort` —
# `155_analytics_tax_rate_once.sql` → `229_floor_summary_scalar_items_guard.sql`, 8,112 lines.
# Branch: sweep9/t31-migrations-161-240 · worktree: ../wt-s9-t31 · port 4431 (unused: no page in this territory)
# Ids P104601–P104650, pre-allocated in this terminal's prompt. Nothing was claimed from the registry.
#
# ── WHAT THIS RUN RE-RAN, AND WHAT IT ADDED ─────────────────────────────────────────────────────
# Re-ran 238 existing rows whose SUBJECT is one of these 80 files: 191 in T22.md, 41 in T23.md,
# and 6 singletons in T2/T5/T8/T24/T28. All 238 green; NO regressions. Results updated in place
# in the file each row lives in. Rows about files this terminal does not own were left alone.
#
# ── THE MEASUREMENT THAT AIMED THE 50 NEW CHECKS (rule 2b) ──────────────────────────────────────
# Rows per file across all 48 ledgers, by subject, before planning. This territory is unusually
# EVEN: 63 of the 80 files carry exactly 2 rows, 7 carry 3, 5 carry 4, 3 carry 5, and one carries 6
# (223_waiter_sections_full_access_backfill.sql). There is no thin corner to aim at — the thinness
# is in the KIND of question, not the file. Of the 238 rows, 82 ask "is this file safe on a
# re-seed?", 10 "does its header name its own number?", 10 "does it erase a sale?" and 134 "is what
# it declared still present?". NOT ONE asks the question this territory actually turns on:
#
#   these files are not stale, they are SUPERSEDED. Thirty-odd of the functions they define have
#   been recreated since by a later migration — mig 301 replaced the pay-later formula, 352 taught
#   it about split payments, a later file rewrote lfh_admin_floor_all from a per-restaurant loop
#   into one grouped pass, another moved lfh_owner_records onto a stored net_amount column. Every
#   recreate is a chance to silently drop the fix the ORIGINAL file existed to make.
#
# So all 50 new checks ask that, and they ask it of the LIVE function body, never of the file.
#
# ── THE TRAP THIS TERRITORY SETS, PAID FOR IN FULL ──────────────────────────────────────────────
# FIVE of this run's first-pass assertions went red against a perfectly correct database, and every
# one was the assertion's fault: each pinned the MECHANISM a file shipped rather than the GUARANTEE.
#   · lfh_owner_records — asserted a scalar sub-select; a later migration replaced it with a stored
#     net_amount column, which honours mig 155's "never per order row" more strongly, not less.
#   · lfh_admin_floor_all — asserted `->>'tag'`; the rewritten grouped pass emits the mark as `'g'`.
#   · lfh_staff_merge_tables — asserted `v_target`/`v_src`; renamed to keep/drop so the LOWEST
#     table number always survives a merge.
#   · lfh_khata_outstanding — asserted mig 184's discount-from-tax/subtotal formula, which mig 301
#     REPLACED because it is wrong the moment a bill carries an untaxed line.
#   · the manual-edit footprint — asserted from the file; the database had to be asked instead.
# A sixth went GREEN for the wrong reason: `\b\d{3}\b` cannot match `224_inventory_recipes.sql`
# because `_` is a word character, so the header check passed 80 files by finding no number at all.
# THE RULE FOR WHOEVER EXTENDS THIS: assert the guarantee, not the spelling. An assertion pinned to
# a mechanism becomes a false alarm the day someone improves the code, and a guard that cries wolf
# stops being read.
#
# ── THE ONE PROBLEM FOUND AND FIXED ────────────────────────────────────────────────────────────
# P104643 · `192_owner_analytics_workmem.sql` and `193_orders_effective_date_index.sql` each
# declared themselves to be migration 188 in their own first line, while a real and different
# `188_bill_ledger_soft_delete.sql` — the compliance-critical bill ledger — holds that number.
# Both were written for slot 188 and renumbered around parallel PRs without their headers being
# updated. Headers corrected with a renumber note, the convention files 172–176 already use.
#
# Every row below is produced and re-run by ONE command, which re-derives the territory from disk
# rather than trusting this list:  `npm run verify:migrations-161-240`   (`--static` skips the DB).
# READ-ONLY against the database: writes nothing, creates nothing, deletes nothing.
#
# Result column: ✅ pass · ❌ real problem · ⏭ skipped, with a written reason.
#
# ── SUBJECTS (so a by-file grep finds this ledger — T28's lesson, 2026-09-16) ───────────────────
# 155_analytics_tax_rate_once.sql
# 155_google_review_url.sql
# 156_staff_actions_actor_id.sql
# 157_prune_logs_platform_default.sql
# 158_retention_one_month_max.sql
# 159_everything_log.sql
# 160_fix_requests.sql
# 161_agent_runs.sql
# 162_prune_agent_runs.sql
# 163_fix_request_mode.sql
# 164_auto_accept_followup_orders.sql
# 166_table_tags_khata.sql
# 167_banquet_full_ladder.sql
# 172_table_ops_ladder.sql
# 173_staff_move_order.sql
# 174_staff_merge_tables.sql
# 175_staff_move_order_item.sql
# 176_session_payments.sql
# 177_table_ops_canonical_ladder.sql
# 178_tablet_take_orders_cap.sql
# 179_take_orders_module.sql
# 180_access_panel_config.sql
# 181_edit_menu_subopts_default.sql
# 181_resolve_errors.sql
# 182_error_seen.sql
# 183_fix_request_resolve_link.sql
# 184_khata_outstanding.sql
# 185_khata_revenue_by_paid_date.sql
# 186_app_config.sql
# 187_google_review_mode.sql
# 188_bill_ledger_soft_delete.sql
# 189_invoice_events_and_settled_lock.sql
# 190_bill_immutability_lock.sql
# 190_owner_analytics_daily_rollup.sql
# 191_owner_daily_rollup_cron.sql
# 192_owner_analytics_workmem.sql
# 193_orders_effective_date_index.sql
# 194_credit_notes.sql
# 195_category_breakdown_join_title.sql
# 196_owner_analytics_cache.sql
# 196_parcel_paid.sql
# 197_parcel_module.sql
# 198_parcel_default_manager_on.sql
# 199_owner_heatmap.sql
# 200_sales_report_index_friendly.sql
# 201_owner_report_monthly_rollup.sql
# 202_owner_report_month_fingerprint.sql
# 202_place_order_concurrency.sql
# 203_owner_report_group_call.sql
# 203_price_order_options_type_guard.sql
# 204_enable_rls_server_only_tables.sql
# 205_rate_limits.sql
# 206_rate_limit_guest_orders.sql
# 207_rate_limit_waiter_join.sql
# 208_admin_login_alert.sql
# 208_owner_recycle_bin.sql
# 209_platform_module.sql
# 210_table_qr_codes.sql
# 211_report_window_upper_bound.sql
# 212_customer_crm.sql
# 213_fix_owner_fn_drift.sql
# 214_unblock_requests.sql
# 215_open_price_items.sql
# 217_shift_table_concurrency.sql
# 218_error_signatures.sql
# 219_error_signatures_no_muting.sql
# 220_staff_profiles_payroll.sql
# 221_inventory_core.sql
# 221_payroll_optin.sql
# 222_waiter_table_sections.sql
# 223_waiter_sections_full_access_backfill.sql
# 224_inventory_recipes.sql
# 225_waiter_sections_follow_table_count.sql
# 226_floor_tiles_per_row.sql
# 227_bill_customer.sql
# 227_inventory_reports.sql
# 228_customer_views.sql
# 228_open_table_concurrency.sql
# 229_customer_scale.sql
# 229_floor_summary_scalar_items_guard.sql
#
## Sweep #9 · terminal 31 — fifty NEW checks, `P104601`–`P104650` (2026-09-18)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P104601 | every table these files create still exists | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | 32/32 |
| P104602 | every column these files add still exists | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | 89/89 |
| P104603 | every index these files create still exists | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | 64/64 |
| P104604 | every function these files define still exists (bar the one mig 219 retires) | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | 77/77, 1 deliberately retired |
| P104605 | every trigger these files attach still exists | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | 11/11 |
| P104606 | every table these files lock down still has row-level security on | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | 32/32 |
| P104607 | the inventory photo bucket still exists | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | inv-media |
| P104608 | every staff-only function these files define is still closed to the public menu key | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | 56 staff-only function(s) checked |
| P104609 | every guest-called function these files define is still open to the public menu key | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | 5 open as intended |
| P104610 | the customer-recognition read being closed to the public key is a LATER deliberate narrowing, not drift | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | mig 300 closed it to the public key; both remaining call sites are server-side |
| P104611 | the second, older guest place-order signature is a shim that re-prices on the server | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | not a second way to name your own price — the six-argument form ignores the subtotal/tax/total it is handed and delegates to the server-priced three-argument one (mig 029's shim) |
| P104612 | no function these files define carries two live signatures that a named-argument call could not choose between | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | 1 name(s) with two signatures, none defaultable: lfh_place_order |
| P104613 | mig 155 · the portfolio tiles still resolve the tax rate once per query, never once per order row | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries a MATERIALIZED rates join, or no per-order-row rate call at all |
| P104614 | mig 155 · the all-time records read still never asks for the tax rate once per order row | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries a stored net amount or a once-per-query rate |
| P104615 | mig 157 · log pruning still consults the platform-wide default row | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries the id='site' fallback |
| P104616 | mig 158 · log pruning still hard-caps every restaurant's window at one month | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries the 30-day ceiling on both windows |
| P104617 | mig 162 · pruning still trims the session history and SETTLED repair requests, and never an open one | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries both sweeps, with open requests spared |
| P104618 | mig 159 · the manual-edit footprint still cannot break the edit it is recording | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries the swallow-everything handler |
| P104620 | mig 164 · a follow-up guest order still skips the second Accept | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries the auto-accept branch |
| P104621 | mig 206 · the guest session ordering door still passes through the rate limiter | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries the rate-limit guard |
| P104622 | mig 164 · a guest order is still priced by the server for the session's OWN restaurant | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries the tenant-scoped server pricing call |
| P104623 | mig 166 · the realtime breadcrumb still has its own table-marks branch (that table has no id column) | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries the explicit table_tags branch |
| P104624 | mig 166 · a table's mark still clears when that party leaves, unless another party still holds the table | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries the other-session-still-open exemption |
| P104625 | mig 166 · the admin all-restaurants floor still carries each table's mark and still carries NO money | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries the mark, and no amount anywhere |
| P104626 | mig 173 · moving one kitchen ticket still refuses a paid order and a live invoice on either side | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries all three refusals |
| P104627 | mig 174 · merging two tables still adds both discounts up and re-splits them over the one combined bill | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries both halves of the money rule |
| P104628 | mig 174 · a merged table still ends up with exactly one head | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries the head demotion |
| P104629 | mig 175 · moving the last dish off a ticket still cancels it instead of leaving a ₹0 line on the bill | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries the empty-order cancellation |
| P104630 | mig 175 · a dish already served still keeps its status when it moves, so it never returns as a new kitchen task | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries the served-stays-served mapping |
| P104631 | mig 189 · an invoice still cannot be re-issued once the bill is settled | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries the settled lock |
| P104632 | mig 189 · voiding an invoice is still refused once the bill is settled, and still leaves a permanent record | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries the settled lock and the history row |
| P104633 | mig 190 · an issued bill still cannot be permanently erased, and the audited purge is still the only exception | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries the escape hatch and both definitions of issued |
| P104634 | mig 194 · a credit note still cannot exceed the bill it credits, and still needs a reason | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries both refusals |
| P104635 | mig 195 · the category mix still matches a sold line to the menu by title (matching by id put every sale in "Other") | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries the title join, and not the broken id join |
| P104636 | mig 202 · two identical orders fired at one table in the same instant are still serialised and de-duplicated | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries the per-table lock, the warning, and the deliberate send-anyway |
| P104637 | mig 203 · a malformed add-on list on one line still degrades to "no add-ons" instead of failing the whole order | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries all three extraction sites type-guarded |
| P104638 | mig 215 · an open-price dish still takes the price the staff typed, clamped, and still refuses to ring up at ₹0 | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | live body carries the typed price, the clamp, and the ₹0 refusal |
| P104619 | mig 159 · the manual-edit footprint is still on the config tables only, never the hot order tables | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | on menu_items, categories, restaurants, settings; on a hot order table: none |
| P104639 | no top-level data rewrite in these eighty files would change a stored choice on a second run | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | 9 file(s) rewrite data at the top level; unguarded: 0 |
| P104640 | every do-block in these files that rewrites data is behind the applied-once ledger or an existence test | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | 5 such block(s): 198_parcel_default_manager_on.sql DO $reseed_guard$, 209_platform_module.sql DO $reseed_guard$, 219_error_signatures_no_muting.sql DO $unmute_once$, 221_payroll_optin.sql DO $payroll_optin_once$, 225_waiter_sections_follow_table_count.sql DO $reseed_guard$ |
| P104641 | not one of these eighty files carries any of the seven statement-level re-seed hazards | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | all 80 files clean |
| P104642 | no statement in these eighty files erases or zeroes a sale when the file is applied | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | no top-level statement touches a money table destructively |
| P104643 | every one of these files still names its own migration number in its header, or says it was renumbered | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | all 80 headers agree with their filename |
| P104644 | mig 227 · the four ways of writing one Indian mobile still resolve to the same customer, and a foreign number is still not mangled | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | +91 / 0 / bare / 091 all resolve to 9825012345; a US number stays 14155550123; empty stays empty |
| P104645 | mig 212 · the returning-guest greeting still never carries a phone number | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | no linked devices on this database; the function selects only name and visit count |
| P104646 | mig 228 · the admin Customers view still returns head-counts only, never a restaurant's earnings | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | keys returned: restaurant_id, guests, regulars, blocked; money-shaped: none |
| P104647 | mig 184 · a pay-later bill still shows exactly what is still owed, and never a negative debt | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | 1 open pay-later bill(s); 1 agreeing to the paisa with "stored net amount minus what the split already collected"; negative debts: 0 |
| P104648 | mig 227 · a purchase or expense dated today is still inside a "this month" window | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | window end resolves to 2026-09-18, today is 2026-09-18 |
| P104649 | mig 229 · the floor view still survives an order whose items are not a list, and still answers for a real restaurant | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | the guard turns a non-list items value into 0 rows instead of an error; malformed rows today: 0; the live floor call answered with a object |
| P104650 | mig 205 · a key with no configured limit is still allowed through, and still writes no counter and no problem row | `npm run verify:migrations-161-240` — it re-derives the territory and asks the live database | ✅ | allowed=true; counter rows written: 0; problem rows written: 0 (nothing to clean up) |
