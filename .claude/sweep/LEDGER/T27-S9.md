# T27 of SWEEP #9 — the admin server routes, 26 to the end · phases `P104201`–`P104250`

> **This is NOT the same territory as `T27.md`.** That file is terminal 27 of **sweep #6** — "every
> word on every screen". Terminal numbers are per-sweep and the two have nothing to do with each
> other. This file is sweep #9's terminal 27: the last 26 of `app/api/admin/**/route.ts`.

**Territory (write access):** `find app/api/admin -name route.ts | sort | sed -n '26,51p'` — 26 files,
4,847 lines, re-derived on 2026-09-15 rather than taken from the prompt's list (which named 24 and
was missing both `reveal` routes). Everything else in the repo was read only.

**Branch:** `sweep9/t27-admin-api-part-b` · **worktree:** `~/Documents/Projects/wt-s9-t27` · **port:** 4427

**ID block:** `P104201`–`P104300`, pre-allocated to this terminal. **Fifty used, `P104251`–`P104300`
left free** — `INDEX.md`'s *Next free ID* line was never read and never edited, which is the half of
the rule that registry has recorded six collisions over.

**Result key:** ✅ pass · ❌ fail · ⏭ unanswered, with a written reason.

---

## What was re-run BEFORE any of these were written

The re-run is the work; these fifty are the extension. **1,138 existing rows across 13 ledger files
name a file in this territory.** All of them were re-run on 2026-09-15:

| ledger | rows | how it was re-run | result |
|---|---|---|---|
| `T20.md` | 876 | hand-written — every row's note updated in place | 876 ✅ (4 of them `❌→✅` history from sweep #7) |
| `T19.md` | 57 | hand-written — updated in place | 57 ✅ |
| `T30.md` | 26 | hand-written — updated in place | 26 ✅ |
| `T27.md` | 23 | hand-written — updated in place | 23 ✅ |
| `T28.md` | 3 | hand-written — updated in place | 3 ✅ |
| `T18.md` | 1 | hand-written — updated in place | 1 ✅ |
| `T17-R2.md` | 48 | **GENERATED** — re-run by `node scripts/verify-admin-sweep.mjs --base http://localhost:4427` | 525/525 ✅ (whole file) |
| `T20-S8.md` | 20 | **GENERATED** — `node scripts/verify-bin-billing-usage.mjs` | 608 passed, 0 failed (whole file) |
| `T18-S8.md` | 1 | **GENERATED** — `node scripts/verify-repair-health-sweep.mjs` | 612 planned, 608 ✅, 0 ❌, 4 ⏭ "not reachable on this stack" |
| `T13/T15/T16` | 34 | matched on the row's prose, not its subject — **left alone**, they belong to other territories | — |

**The three generated ledgers were NOT hand-edited.** Each one's own header forbids it — *"a row is
never re-typed here by hand: the table drifts from the checks within days"* — so for those files
"update the result in place" means run the generator, and that is what was done.

### ZERO REGRESSIONS. Nothing that was green is red.

986 hand-written rows re-run: **982 ✅ · 3 ⏭ · 4 `❌→✅`** (all four already green before this run,
fixed in sweep #7) · **0 standing ❌**. 1,745 generated rows re-run: **0 ❌**.

### Rows per file, measured before planning (rule 2b, owner 2026-09-14)

Counted by SUBJECT across all 45 ledgers on disk, not from `ROW-COUNTS.json` and not from memory:

```
admin/reveal + reveal/password    0     ← where twenty of these fifty went
admin/staff-online               34     restaurants/bill-preview        34
restaurants/report               35     restaurants/create-defaults     36
admin/resolve-error              40     restaurants/staff-features      40
restaurants/google-review        41     restaurants/health              41
restaurants/platform-channels    41     users/photo                     41
restaurants/credentials          42     restaurants/export              42
restaurants/logo                 42     restaurants/quick-features      45
admin/revenue                    45     admin/settings                  46
admin/usage                      51     restaurants/access-tree         58
restaurants/settings             59     admin/repair                    68
admin/rate-limits               125     admin/users                    127
```

**`app/api/admin/reveal/route.ts` and `app/api/admin/reveal/password/route.ts` carried ZERO rows
across all 45 ledgers.** They landed after sweep #8's territory map was measured, so no ledger row and
no earlier prompt had ever named them — the whole password-cover gate, on the one door in the product
that hands back a client's sign-in password, had never been checked once. That is where the biggest
block below went.

### ⚠️ `verify:ledger-index` is RED on clean `origin/main`, and it is not this terminal's doing

518 problems, none naming an id in `P104201`–`P104300`:

- **500** ids are phase rows in **both `T1.md` and `T3.md`** (`P110001`–`P110500` — the range
  `INDEX.md` records T3 as having claimed);
- **17** more are in both `T1.md` and `T2.md` (`P110501`–`P110517`);
- **1** says the *Next free ID* mark sits below 8,071 rows that already use it or higher.

Measured both ways: 518 on the shared folder's `main`, 517 on this branch's base — the difference is
the base commit, not an edit here. **Not fixed from this terminal, deliberately:** the rule is *"never
renumber anyone else's"*, and deciding which of two terminals' 500 rows moves is not a call this one
gets to make. Carried to the report as a decision.

---

## A · the password-cover gate, driven end to end · `P104201`–`P104220`

The first checks these two files have ever had. Driven on port 4427, signed in once as admin.
`REVEAL_TTL_MS` is 5 minutes and the wrong-guess wall is 5 tries per 5 minutes, so exactly **one**
wrong answer was spent — and the two audit rows the run wrote were deleted by their own ids.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P104201 | `/api/admin/reveal` GET — reports whether a secret is set on this deployment at all | GET signed in; expect `configured:true` | ✅ | `{configured:true, unlocked:false, until:null, ttlMs:300000}` |
| P104202 | …and with nothing typed yet, reports the passwords COVERED | same call; `unlocked===false && until===null` | ✅ | fails closed from a cold start |
| P104203 | …and names the window length, so the screen can count down rather than guess | `ttlMs === 300000` | ✅ | 5 min, matching `REVEAL_TTL_MS` |
| P104204 | the cover state never carries the secret itself | search the whole body for the configured password | ✅ | absent |
| P104205 | while COVERED, asking for one person's password is refused with 423 — not 401, not 403 | POST `/api/admin/reveal/password` `{user_id}` with no uncover cookie | ✅ | 423; the three codes mean three different things to the screens |
| P104206 | …with the ONE sentence every screen reacts to | body matches `REVEAL_LOCKED_MESSAGE` exactly | ✅ | "Passwords are covered. Type the admin password to uncover them." |
| P104207 | …and that refusal carries no password and no hash | grep the body for `password`/`password_shown` | ✅ | only the sentence |
| P104208 | while COVERED, the handover sheet still lists every login | GET `/restaurants/credentials?restaurant_id=<FH>` | ✅ | 200, 13 logins, names and usernames all present |
| P104209 | …with every password held back, and the sheet SAYING it is covered | `unlocked===false` and every `password === null` | ✅ | 13/13 null |
| P104210 | …but still saying WHICH rows would print filled in, which is the question you ask before uncovering | at least one `hasPassword:true` while covered | ✅ | 13 of 13 have a readable copy stored |
| P104211 | the sheet never carries a password hash or a PIN | grep for `password_hash`, `pin_hash`, `password_shown` | ✅ | none — rule 3 of the route's header holds |
| P104212 | while COVERED, the one-press handover reset is refused BEFORE it can burn a password | POST `{action:"reset_all"}` with no uncover cookie | ✅ | 423, nothing written |
| P104213 | a wrong answer is refused, says how many tries are left, and says nothing about how close it was | one wrong POST; read the sentence | ✅ | 403 · "That isn't the admin password. 4 tries left." · no hint, no length, no echo of what was typed |
| P104214 | …and hands out no uncover cookie, leaving the passwords covered | inspect `set-cookie`; re-read GET | ✅ | no cookie; still `unlocked:false` |
| P104215 | an EMPTY box is told to type the password rather than counted as a wrong try | POST `{}` | ✅ | 400 "Type the admin password." — a mis-tap does not spend a try |
| P104216 | the right answer uncovers them, on an HttpOnly cookie that is not the password | POST the real password; inspect the cookie | ✅ | 200 · `HttpOnly; SameSite=lax` · value is `<expiry>.<hmac>`, contains no part of the password |
| P104217 | …the browser drops that cookie at the same moment the server stops honouring it | `Max-Age` vs `until` | ✅ | `Max-Age=300`, `until` 300s out — the two agree |
| P104218 | uncovered, one person's stored password reads back, and the answer says WHO it is about | POST `{user_id}` with both cookies | ✅ | 200 · `stored:true` · name, username and role all named · no hash |
| P104219 | a DAMAGED uncover cookie reads as covered, never as uncovered — all four shapes | no signature · a re-dated expiry · an unknown shape · a non-numeric expiry | ✅ | 4/4 `unlocked:false`, and 4/4 still 423 on the per-person route. "It fails closed" holds |
| P104220 | the uncover cookie ALONE is not enough — the admin sign-in is still required, on all three verbs | send only `lfh_reveal`; then no cookies at all | ✅ | 401 on the per-person route, on GET and on DELETE. Two doors, both locked |

## B · the four faults found this run, as permanent regression rows · `P104221`–`P104228`

Each was ❌ when this run started and is ✅ because the fix is in this branch. `verify:admin-api-a`
**rule 6** now watches all eight.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P104221 | a permission save NEVER seeds its merge from an unreadable settings row | read `access-tree`'s `patch.features` branch: the query is assigned and its `.error` answered before `{ ...curFeat }` | ❌→✅ | was `(await sb…).data?.features` — error unreachable, so a read blip made the base `{}` and one saved switch wiped every other guest feature. Item 1 |
| P104222 | …and the delivery-channel merge does the same, so a stored API key cannot be dropped | same, for `patch.channels \|\| patch.creds` | ❌→✅ | same shape, worse loss: the keys are write-only, so nothing can retype them. Item 1 |
| P104223 | "does this restaurant have a settings row?" is never answered by a FAILED read | `existing` is assigned and `.error` answered before the clone branch is chosen | ❌→✅ | the clone branch upserts `onConflict: restaurant_id` with a full copy of #1's cleaned row. Item 1 |
| P104224 | …and that upsert really would overwrite a live row, which is why P104223 matters | drive the clone-branch upsert against an EXISTING settings row on a throwaway restaurant | ✅ | **measured**: 11 of 11 probed columns reset — `table_count` 77→10, `price_tax_mode` composition→excl, `khata_allowed`/`payroll_allowed` true→false, `tablet_mark_paid` pin→off, table names Patio/Window→`{}`, `platform_channels` (with its key)→the template's. The probe restaurant was binned and purged in the same run |
| P104225 | removing a person's photo when there is none does NOT write "photo removed" into the record | DELETE `/api/admin/users/photo?id=` for someone with no photo; read `staff_actions` | ❌→✅ | was `{ok:true}` + a `user_set_photo` row reading `photo removed for "raj"`. Now `{ok:true, removed:false, message:"There was no photo to remove."}` and **nothing** written. Item 2 |
| P104226 | …and its sibling, the restaurant logo, still behaves the same way (the 2026-09-01 fix, re-run) | DELETE the logo of a restaurant with none | ✅ | `{ok:true, removed:false, message:"There was no logo to remove."}`, no record. The two now answer alike |
| P104227 | the Repair Kit never says "that isn't for this restaurant" because a read failed | all five ownership reads go through `readOne`, which separates absent from unreadable | ❌→✅ | five `(await sb…).data` reads each decided a 404 ACCUSING the admin of the wrong restaurant, on the screen used when service is already going wrong. Item 3 |
| P104228 | no refusal in the territory is decided from a read whose error is unreachable | `node scripts/verify-admin-api-a.mjs` → rule 6 | ❌→✅ | 9 hits across 7 files at the start (branding · logo POST · restaurants PATCH+create ×2 · users POST+PATCH · rate-limits approve · resolve-error · repair ×5 · access-tree ×3); 0 now. Items 1–5 |

## C · the bill preview, rendered and looked at · `P104229`–`P104234`

34 rows before today, none of which opened the page. Screenshotted at 1280×900 and 360×780 (A35) and
read like a picky human.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P104229 | all three modes return a real HTML document, not JSON or an error page | GET `?mode=bill\|parcel\|kot` | ✅ | `text/html; charset=utf-8`, `no-store`, 12,155 / 12,124 / 2,991 bytes |
| P104230 | no leaked code text reaches the printed page in any mode | read the rendered `innerText` for `[object Object]`, `undefined`, `NaN`, `${`, `-->` | ✅ | none, in any of the three |
| P104231 | the sample bill FOOTS — the page the admin approves is arithmetically real | add the rendered lines up by hand | ✅ | 640+80+280+60+240 = **₹1,300** subtotal · −₹50 (3.8%) = **₹1,250** taxable · CGST ₹32 + SGST ₹31 (₹31.25 each, whole rupees, last line takes the remainder) · **TOTAL ₹1,313**. Every figure agrees |
| P104232 | the KOT mode shows no money at all, and says it is a sample | read the rendered ticket | ✅ | "KITCHEN TICKET · SAMPLE", KOT #SAMPLE, allergy line boxed, quantities and add-ons — not one price |
| P104233 | the preview fits the owner's phone without sideways scrolling | A35 360×780 dpr3; compare `scrollWidth` to `clientWidth` | ✅ | no horizontal overflow; paper renders 310px (bill/parcel) and 280px (KOT) |
| P104234 | **NOT A FINDING — deliberate.** the parcel bill prints the word "PARCEL" with nothing beside it | compare against `public/panels/billdoc.js` line 805 | ✅ deliberate | the label sits where "TABLE T5" would, with an empty value. The owner's decision, 2026-08-02: *"the bill format should be exactly like a KOT bill — just the top changes"*, and the real printer does exactly the same. **Do not file this.** Seen on screen: it reads as a marker line, not as a missing value |

## D · who is online · `P104235`–`P104238`

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P104235 | the list carries no money field anywhere (the admin sees no earnings) | search the whole answer | ✅ | clean |
| P104236 | every person is named WITH their restaurant, not just an id | every row has `restaurantName` | ✅ | present on every row |
| P104237 | the filter lists only restaurants that actually have somebody online | compare the picker against the live set | ✅ | picker 0, live set 0 — agrees. No dozens of empty restaurants |
| P104238 | nobody in the list was last seen outside the 3-minute window it claims | check each `last_seen_at` against 180s | ✅ | window holds; no password hash or PIN rides along |

## E · the remembered new-restaurant setup · `P104239`–`P104241`

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P104239 | it answers the shape the create form reads | GET; expect a `defaults` key | ✅ | `{panels:{manager,kitchen,tablet:true, owner:false}, seedMenu:false}` |
| P104240 | "nothing remembered yet" is told apart from a failed read | `defaults:null` only for a missing row; a read error is an error | ✅ | the T20 sweep-#7 correction still holds |
| P104241 | it remembers no PERMISSION set (sweep T6, 2026-08-06 — one stale set pre-filled every restaurant) | search the stored value for access/permission/grant keys | ✅ | panels + seedMenu only |

## F · one restaurant's full report · `P104242`–`P104246`

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P104242 | none of the three windows carries food money | drive `range=today\|7d\|30d`; search each answer | ✅ | counts only: orders, orderItems, activityLogEvents, waiterCalls, sessions, tables, menuItemCount, staff, activityVolume |
| P104243 | the per-role staff counts add up to the staff total it prints | sum `staffByRole` against `staffTotal` | ✅ | owner 5 + manager 4 + kitchen 2 + tablet 1 = 12 = the total |
| P104244 | the "activity volume" proxy really is the sum of its four named parts | recompute from the answer | ✅ | 14,735 = orders + orderItems + activityLogEvents + sessions |
| P104245 | a SECOND restaurant's report is its own numbers, and names itself | drive it for Aangan (read-only control — a GET changes nothing) | ✅ | Aangan 192 orders vs French House 5,044; "AANGAN GARDEN RESTAURANT" in its own header. No #1 leak |
| P104246 | an unknown `range` is not echoed back as if it had been used | `?range=banana`; read `range` in the answer | ❌→✅ | the NUMBERS are right — `rangeBounds` falls back to 7 days — but the answer echoes `range:"banana"`, and the sibling `/api/admin/usage` states the rule this breaks: *"`range` echoes back what was actually used, so the page labels its own columns from the server's answer"*. Listed as item 6 rather than fixed on the first pass, because no screen sends a fourth value so nobody got a wrong number. **Fixed 2026-09-15** on the owner's word. Driven across every input: `today`→today · `7d`→7d · `30d`→30d · `banana`→7d · absent→7d · `30D`→7d, each with the matching figures |

## G · judgment, and the cross-checks · `P104247`–`P104250`

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P104247 | every one of the 26 routes requires being signed in, before it touches the database | drive all 24 GETs with no cookie; `verify:admin-api-a` rule 1 for the order | ✅ | 24/24 answered 401. `find app/api/admin -name route.ts \| wc -l` = 51 = the count that greps `tokenIsValid` — CLAUDE.md's invariant holds |
| P104248 | no answer in the territory hands the console a database sentence | drive all 24 GETs plus 31 unknown-row refusals; match against the `adminFail` shapes | ✅ | 55/55 clean. Every refusal is a plain English sentence |
| P104249 | an unknown-but-valid id is refused, never answered as a silent success | 31 write and read paths across 13 routes, each with a valid uuid matching no row | ✅ | 31/31 · every one a 404/409 naming what is missing, in words a person can act on |
| P104250 | a purged restaurant really does leave every list the admin can see | bin then purge this run's own probe restaurant; re-read both lists | ✅ | gone from the live list AND from the recycle bin (the 2026-08-16 `purged_at` filter) — driven, not read |

## H · the picked follow-ups — items 6, 7 and 10 · `P104251`–`P104256`

Written 2026-09-15 after the owner picked from Part 4: *"do 6,7,and 9 … and 8 want problem fix if
there any"*. Ids from this terminal's own block; `P104257`–`P104300` remain free.

**Item 9 is NOT in this table, and that is the honest result: terminal 29 fixed it first.** It landed
in PR #1368 while this work was in progress — the owner had said it might ("maybe done by other
terminal right now"), and he was right. T29 moved T1's colliding band to `P120001`–`P120517` and
anchored the guard's registry read on `^\*\*next free ID…\`P…\``, which is the same shape this
terminal had reached independently. **Verified on a clean checkout of `origin/main`, not taken on
trust: `verify:ledger-index` exits 0 — 46 ledgers, no collisions, and `T1.md` carries zero `P110xxx`
rows.** This terminal's own two commits for it were dropped rather than merged; re-landing a repair
somebody else has already made is how a ledger ends up with two corrections for one fault.

**Item 8 is not here either.** Its six routes belong to terminal 26, which is mid-run on them right
now — 7 commits in flight and uncommitted work in its worktree. Read-only inspection shows it is
working through exactly this: `printing` is already clean and `owners` is down from 9 hits to 3.
Editing another live session's files is the one thing the owner's standing rules put ask-first.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P104251 | the kitchen-ticket preview is laid out like its two siblings | screenshot all three at 1280×900; measure the bar, the paper and the note | ❌→✅ | was: paper hard left on bare white, toolbar trapped in the 280px ticket column, note wrapped to 4 lines. Now bar **1280px of a 1280px viewport**, paper centred (280px at x=500) on the same `#e8e8ea` backdrop as the bill, note on **2 lines**. No sideways scroll at 1280 or at 360×780 (A35); no console or page errors |
| P104252 | …and the paper ends where the ticket ends | measure the body box against the page height | ✅ | 280×294px inside a 900px page, as the bill's does (310×841). A first attempt used `min-height:100vh` and left a tall empty white column under a three-line ticket — it read as a page that had failed to load, so it was dropped |
| P104253 | **the REAL printed ticket is untouched by that** | build `kotDocHtml` from `origin/main` and from this branch on the same ticket, and compare | ✅ | **byte-for-byte IDENTICAL with no `note`** — the only way a panel ever prints one (hidden iframe, no toolbar, no note). The preview differs by 384 chars, all inside a new `@media screen` block; every `@media print` rule unchanged; `billDocHtml` identical too. Two independent reasons it cannot reach paper: the rules are screen-only, and `kotBarCss()` is emitted only when a `note` is passed |
| P104254 | the paper guard is not red on a date nobody chose | `npm run verify:print-paper`, on the 15th of the month | ❌→✅ | it walked the 15th of every month of a hard-coded **2026** — and today is 2026-09-15, so September asked `kotWhen` about *this* business day, which correctly answers with the time alone (check 2 of that same guard is the reason it does). It called that a wrongly-printed month: `SEP → "11:30 AM"`. **Red for one day a month, current month only, with the paper right the whole time.** Sample pinned to a year always in the past |
| P104255 | …and that check still catches the fault it exists for | abbreviate September "SEPT" in `kotWhen` and run it | ✅ | exit 1 — `SEP → "15 SEPT 11:30 AM" · a five-day-old ticket printed "10 SEPT 11:28 PM"`. Green when restored. Not merely passing |
| P104256 | the report echoes the window it used, for every input a screen or a URL can send | drive `today`, `7d`, `30d`, an unknown word, absent, and wrong case | ✅ | `today`→today · `7d`→7d · `30d`→30d · `banana`→7d · absent→7d · `30D`→7d, and the order counts match the window named in each (9 · 5,053 · 7,381 · 5,053 · 5,053 · 5,053) |
