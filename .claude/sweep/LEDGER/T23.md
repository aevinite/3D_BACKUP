# T23 — THE DATABASE, migrations at POSITIONS 231–339 of the sorted folder

**Sweep #7 (2026-08-28) — the territory MOVED.** It is now defined by POSITION, not by number:
`ls supabase/migrations/*.sql | sort | sed -n '231,339p'` — **109 files, 16,120 lines**, running
`223_waiter_sections_full_access_backfill.sql` → `332_every_bill_is_signed_and_chained.sql`.
Position 340 and beyond (files 333 → the newest) belongs to **T33**.

Eight of those files — **223 to 230** — sat BELOW sweep #6's boundary ("every file numbered 231 or
higher") and had never been checked by this terminal. **That is where this run's one real problem
was found** (migration 225, row `P26502`).

Phase IDs **P11001–P11500** (sweep #6) and **P26101–P26600** (sweep #7), permanent and global.
The next sweep re-runs BOTH, top to bottom, before it writes one new id.

## Sweep #7's second pass — what happened when all 500 old rows were re-run

| | |
|---|---|
| Old rows re-run | **500 of 500** — 463 ✅ · 35 ⏭ · 2 ❌ |
| **Regressions** | **none.** One row (`P11463`, `verify:purge`) came back RED on the first run and it was NOT a regression: the shared dev database was mid-way through another terminal's migration 368, so the purge's live body did not yet clear a table that had just appeared. Re-run twenty minutes later: green. `wt-s7-t24` turned out to be holding the very migration that closes it. **A guard that reads live data can cry wolf while another lane is writing.** |
| Rows whose ⏭ became a ✅ | **6** — `P11302`–`P11305`, `P11316`, `P11469`. The four invoice-refusal rows and the advisory-lock row were "not executed" last time; this run read the LIVE function bodies out of `pg_proc` instead of the migration files, which is stronger evidence, and `verify:guest-read` was simply run. |
| Expectations that MOVED (the product changed, the row did not fail) | **5** — `P11126` (`lib/floorLayout.ts` caps at 12 now, not 30; the DB CHECK is still the wider bound, so the invariant holds) · `P11378` (the tile prints label "Free" + meta "tap to open" as two fields instead of one string) · `P11403` (the admin Live floor no longer loads on its own; it says so in words and waits for one press, which is the egress rule) · `P11415`/`P11416` (the guest theme lives in localStorage, not a cookie, and this tenant's admin default is the light skin) · `P11499` (16 one-time ledger keys before this branch, 17 after) |
| Rows still ⏭ | **35**, every one because the check needs a WRITE to the shared French House floor while 39 other terminals are live (a merge, a seated guest, a real invoice, a split settle) — each says which fixture it needs and where the same rule is proved by reading instead |
| New rows written | **500** (`P26101`–`P26600`) — 492 ✅ · 8 ❌ |
| Problems found | **1**, fixed on this branch (mig 225's re-seed hazard) |
| Recorded, awaiting the owner's decision | **2** — both about what a purge deletes (rows `P26404`, `P26416`, `P26593`–`P26595`, `P26600`) |

## The budget these 500 phases were written to

| block | ids | count | share | what it is |
|---|---|---|---|---|
| A · correctness by reading the SQL, files 231–299 | P11001–P11200 | 200 | 40% | does the statement do what its own header says, for a real restaurant |
| B · files 300–349 read against the project's written rules | P11201–P11300 | 100 | 20% | grants, search_path, additive-only, re-seed safety, index-every-filter, one-number |
| C · watching it run (headless, port 4123) | P11301–P11375 | 75 | 15% | the function answering on the dev database, and the panel that reads it |
| D · screenshots actually opened and read | P11376–P11425 | 50 | 10% | desktop 1280×800 and Samsung A35 360×780 dpr3 |
| E · tracing one change across panels | P11426–P11475 | 50 | 10% | migration → route → manager / kitchen / tablet / owner / admin |
| F · judgment — does a real restaurant need it this way | P11476–P11500 | 25 | 5% | |

Every file in the territory has at least one row. The nine files this terminal's brief named —
**232, 238, 246, 254, 261, 299, 326, 331, 332** — have ten or more.

Result key: ✅ pass · ❌ real problem (written up in `T23-findings.md`) · ⏭ skipped, with the reason.
Second-pass results are the ones recorded below; the first pass is described in the findings file.

---

## A · CORRECTNESS BY READING THE SQL — files 231–299 (P11001–P11200)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P11001 | 231 `lfh_inv_post_movement` re-derives avg_cost ONLY on an inflow or a `purchase_void`, never on consumption/waste/transfer_out/count_adjust | read the IF/ELSIF ladder in 231; the ELSIF tests `p_kind = 'purchase_void'` and nothing else | ✅ |  |
| P11002 | 231 a void that empties the balance sets avg_cost to 0, never negative | `v_new_qty > 0` branch uses GREATEST(...,0); the ELSE sets 0 | ✅ |  |
| P11003 | 231 a replayed movement (same dedupe_key) returns NULL and does NOT move the balance | `ON CONFLICT (dedupe_key) DO NOTHING` + `IF v_id IS NULL THEN RETURN NULL` sits BEFORE the qty update | ✅ |  |
| P11004 | 232 the close cleanup cancels only unpaid, non-khata, non-deleted, unarchived orders | read the first UPDATE's five predicates | ✅ |  |
| P11005 | 232 everything else on a closing session is archived but keeps its status | the second UPDATE sets only `archived`/`archived_at` | ✅ |  |
| P11006 | 232 a parked khata tab is never cancelled by a close (that money is still to collect) | `khata_at IS NULL` is in the cancel predicate and absent from the archive one | ✅ |  |
| P11007 | 232 the delete-side trigger mirrors the close side line for line | diff `lfh_session_delete_cleanup` against `lfh_session_close_cleanup` in 232 | ✅ |  |
| P11008 | 232 the cleanup lives on the STATUS CHANGE, so a bare `UPDATE sessions SET status='closed'` is covered | the trigger fires on `NEW.status='closed' AND OLD.status<>'closed'`, not in a route | ✅ |  |
| P11009 | 232's one-time backfill only ARCHIVES; it never makes a money decision | the trailing UPDATE sets `archived`/`archived_at` and touches no status | ✅ | deliberate — see 232's own note |
| P11010 | 232's backfill is safe to re-run (matches nothing once applied, and states a fact the sessions already record) | predicate is `NOT archived AND session is closed`; after one run it matches nothing new that is wrong | ✅ |  |
| P11011 | 232 an order whose session row is DELETED is archived before `session_id` is nulled | `lfh_session_delete_cleanup` is a BEFORE/AFTER DELETE on sessions and runs while OLD.id still matches | ✅ | trigger itself lives in mig 024; DB check in the live block |
| P11012 | 232 nothing in the cleanup deletes a row (compliance: a sale is marked, never erased) | no DELETE statement anywhere in 232 | ✅ |  |
| P11013 | 232 the close cleanup also denies pending requests for that table, scoped by restaurant | `UPDATE requests … WHERE table_number = NEW.table_number AND restaurant_id = NEW.restaurant_id` | ✅ |  |
| P11014 | 302 closes the two doors 232 could not: a session INSERTed closed, and an order arriving for a closed session | read both triggers in 302; the INSERT-side one is `WHEN (NEW.status='closed')` | ✅ |  |
| P11015 | 233 `lfh_capture_customer` prefers the BILL's session, then the table's OPEN session, then the latest ever | read the three-step `v_sid` resolution | ✅ |  |
| P11016 | 233 a client-supplied session id is only trusted when it belongs to the acting restaurant | `WHERE id = p_session AND restaurant_id = v_rid` | ✅ |  |
| P11017 | 233 no consent or no phone stores nothing at all | early return on `NOT p_consent OR v_phone IS NULL` | ✅ |  |
| P11018 | 233 the visit counter only increments on a NEW ledger row | `GET DIAGNOSTICS v_ins` after `ON CONFLICT (session_id) DO NOTHING` | ✅ |  |
| P11019 | 234 the qty guard `el->>'qty' ~ '^-?[0-9]+$'` is present in the summary's JSON branch | grep the regex in 234 | ✅ |  |
| P11020 | 234 the scalar-items guard (`jsonb_typeof(b.items)='array'`) survives into the source of truth | grep `jsonb_typeof` in 234 | ✅ |  |
| P11021 | 235 every new settings column is ADD COLUMN IF NOT EXISTS with a default that preserves today's behaviour | read the four ALTER TABLE blocks | ✅ |  |
| P11022 | 235 the layout/mode CHECK constraints are dropped-then-added so the file is re-runnable | `DROP CONSTRAINT IF EXISTS` precedes each ADD CONSTRAINT | ✅ |  |
| P11023 | 235 the `khata_allowed = table_tags_allowed` backfill is a ONE-TIME rewrite and must not run twice | the WHERE tests "is it not the value I want", not absence — the 321/7510 shape | ✅ | **fixed**: wrapped in the mig-307 ledger guard; key recorded by mig 352 (written as 360, renumbered on merge) |
| P11024 | 235 the `menu_languages` re-expansion is a ONE-TIME rewrite and must not run twice | same shape; measured: it would re-expand AANGAN GARDEN from English-only to six languages | ✅ | **fixed**: same guard, same key |
| P11025 | 235 the two normalise-to-known-codes UPDATEs are genuinely idempotent and stay unguarded | they filter to the codes lib/format.ts renders; re-running is a no-op | ✅ | read-verified: the normalise-to-known-codes UPDATEs are outside the guard on purpose  ‹previously: deliberately left outside the guard› |
| P11026 | 235 the `takeaway_allowed` backfill cannot bite, because 263 later forces both halves TRUE in the same pass | read 263's UPDATE + the six ALTER COLUMN SET DEFAULT lines | ✅ |  |
| P11027 | 236 `lfh_check_ban_scoped` is captured verbatim from the live definition with its real grants | compare the body against 236's stated `pg_get_functiondef` source | ✅ | later dropped on purpose by 267/281 |
| P11028 | 237 the banquet bill number is drawn under the settings ROW LOCK, so two tills cannot print one number | `SELECT * INTO v_set FROM settings … FOR UPDATE` precedes the counter read and bump | ✅ |  |
| P11029 | 237 a client-supplied price is honoured ONLY for a banquet item whose stored price is 0 | read the `v_unit` CASE | ✅ |  |
| P11030 | 237 a field the admin did not tick is absent from the stored bill, not merely hidden | every meta column is wrapped in `CASE WHEN v_fields ? '<key>'` | ✅ |  |
| P11031 | 237 a banquet discount can never exceed the food (`v_disc := LEAST(v_disc, v_sub)`) | read the clamp | ✅ |  |
| P11032 | 237 the banquet sale lands as a normal order at status 'served', so it never reaches the kitchen board | the orders INSERT writes 'served' | ✅ | and 335 skips it — see P11150 |
| P11033 | 238 the tile-wording ladder is byte-for-byte the pre-238 ladder | diff the IF/ELSIF block in 238 against 234's | ✅ |  |
| P11034 | 238 ownership is still the SESSION: `belong` only matches by table_number when sessions are OFF and no session is open | read the `belong` CTE's OR | ✅ | the rule this migration must not have broken |
| P11035 | 238 `due` still applies the discount BEFORE tax and is deliberately NOT coalesced | `SUM(total - discount * (1 + v_rate))`, no COALESCE on discount | ✅ |  |
| P11036 | 238 the three-valued request rule is reproduced exactly, not approximated | `req` carries BOTH `all_pending` and `non_open_pending`, chosen by whether a session exists | ✅ |  |
| P11037 | 238 dish counting is SUM(qty), never row count | `lagg` sums `qty`, and `lines` carries a qty per line | ✅ | mig 105's fix, twice reverted before |
| P11038 | 238 the scalar-`items` guard from 229 survives | `CASE WHEN jsonb_typeof(b.items) = 'array' … ELSE '[]'` | ✅ |  |
| P11039 | 238 the qty integer guard from 234 survives | `el->>'qty' ~ '^-?[0-9]+$'` in the JSON branch | ✅ |  |
| P11040 | 238 the JSON branch is a FALLBACK, never a double count | `WHERE NOT EXISTS (SELECT 1 FROM order_items oi2 WHERE oi2.order_id = b.id)` | ✅ |  |
| P11041 | 238 the open session per table is still "latest activity wins" | `DISTINCT ON (s.table_number) … ORDER BY s.table_number, s.last_activity_at DESC` | ✅ |  |
| P11042 | 238 the tiles are gathered into arrays and built ONCE (the 106 ms copying fix) | `array_append` into `v_keys`/`v_vals`, then one `jsonb_object_agg` | ✅ | do not "simplify" back |
| P11043 | 238 `order_count` moved its two conditions from FILTER into WHERE so the partial index can serve it | read the final `SELECT count(*) … WHERE restaurant_id AND NOT archived AND status <> 'cancelled'` | ✅ |  |
| P11044 | 238 drops its own parity twin so no half-tested copy of a money function is left behind | `DROP FUNCTION IF EXISTS lfh_table_view_summary_v2` at the foot | ✅ |  |
| P11045 | 238 the function is re-REVOKEd and re-GRANTed even though CREATE OR REPLACE keeps grants | read the REVOKE/GRANT pair at the foot | ✅ |  |
| P11046 | 239 a banquet is taxed at the BANQUET rate, and the printed split is frozen onto the bill | `v_rate := lfh_banquet_tax_rate(v_rid)` and `tax_lines` is stored | ✅ |  |
| P11047 | 239 the tax lines always foot exactly to the tax (last line takes the remainder) | `CASE WHEN v_i = v_n THEN round(p_tax - v_run, 2)` | ✅ |  |
| P11048 | 239 an empty banquet component list falls back to the restaurant's own tax, not to zero | read `lfh_banquet_tax_rate`'s final CASE | ✅ |  |
| P11049 | 240 the advisory lock in `lfh_place_order_public` uses the SAME key as the staff path | both hash `'lfh_place:' \|\| rid \|\| ':' \|\| table` | ✅ | this is what makes them share one session |
| P11050 | 240 losing the one-open-session race is treated as success, not as a 500 | `EXCEPTION WHEN unique_violation` re-selects the winner | ✅ |  |
| P11051 | 241 the heatmap resolves each restaurant's tax rate ONCE, in a CTE, not once per order row | read the `rates` CTE and the join | ✅ |  |
| P11052 | 241 the join is LEFT, so an order whose restaurant row is missing is not silently dropped from revenue | `LEFT JOIN rates rt` + the per-row fallback | ✅ |  |
| P11053 | 242 `floor_layout_mode` accepts only 'classic'/'custom' and defaults to classic | read the CHECK and the DEFAULT | ✅ |  |
| P11054 | 243 the ownerless-order repair is deliberately narrow (no session, a real table, still on the floor, older than the party) | read the four predicates in the `ownerless` CTE | ✅ |  |
| P11055 | 243 a party-less order taken DURING the current sitting is left alone | the 60-second grace on `cur.opened_at` | ✅ |  |
| P11056 | 243 a takeaway/parcel order (no table_number) is never touched | `o.table_number IS NOT NULL` | ✅ |  |
| P11057 | 244 the dead heatmap copy is dropped in all three signatures it could have carried | three DROP FUNCTION lines | ✅ |  |
| P11058 | 245 the unique login-name index becomes PARTIAL on `deleted_at IS NULL` before the old one is dropped | CREATE precedes DROP, so uniqueness is never absent | ✅ |  |
| P11059 | 245 a plain (non-unique) index is kept so a RESTORE can still look a binned name up | `idx_staff_users_username_any` | ✅ |  |
| P11060 | 246 the watermark table is one narrow row per restaurant per business DAY | `PRIMARY KEY (restaurant_id, day)` | ✅ |  |
| P11061 | 246 the day is the IST business day of the order's own created_at, not "today" | `(NEW.created_at AT TIME ZONE 'Asia/Kolkata')::date` | ✅ |  |
| P11062 | 246 a DELETE still invalidates the window the row USED to be in | the `TG_OP = 'DELETE'` branch reads OLD | ✅ |  |
| P11063 | 246 moving an order's created_at bumps BOTH days | the second upsert, guarded by `OLD.created_at IS DISTINCT FROM NEW.created_at` | ✅ |  |
| P11064 | 246 the detector never touches `orders` any more (the 21.6 s scan is gone) | `lfh_owner_orders_fingerprint` reads only `orders_change_watermark` | ✅ | order volume is not the risk; a slow guard is |
| P11065 | 246 the detector keeps its EXACT signature so every caller and its grants are unchanged | `(uuid[], timestamptz, timestamptz)` matches mig 196's | ✅ |  |
| P11066 | 246 there is deliberately NO backfill, and the reason is written down | read the "WHY THERE IS NO BACKFILL" block — the value is a change token, not a measurement | ✅ |  |
| P11067 | 246 the day-bucket edges over-include by at most a day, and can never MISS a change | inclusive `>=` / `<=` on the day range | ✅ | the only safe direction |
| P11068 | 246 the table is RLS-on with no policy, and only service_role may read it | `ENABLE ROW LEVEL SECURITY` + REVOKE ALL + GRANT to service_role | ✅ |  |
| P11069 | 246 the maintainer trigger is AFTER INSERT OR UPDATE OR DELETE, and returns NULL | read the trigger definition | ✅ |  |
| P11070 | 246 the trigger function is revoked from PUBLIC/anon/authenticated | `REVOKE ALL ON FUNCTION lfh_bump_orders_watermark()` | ✅ |  |
| P11071 | 246 the whole file is wrapped BEGIN/COMMIT so a half-applied watermark is impossible | first and last statements | ✅ |  |
| P11072 | 247 the three tuned tables are exactly orders / staff_actions / realtime_events | read the three ALTER TABLE SET blocks | ✅ |  |
| P11073 | 247 two ideas were TRIED AND REJECTED BY MEASUREMENT and are recorded so they are not retried | the "dropping idx_orders_restaurant…" note | ✅ |  |
| P11074 | 248 splitting a bill defaults to FALSE for existing restaurants too | `NOT NULL DEFAULT false` with no backfill to true | ✅ |  |
| P11075 | 249 the surviving table is ALWAYS the lower number | `IF (v_a.table_number)::bigint <= (v_b.table_number)::bigint` | ✅ |  |
| P11076 | 249 `orders.table_number` is NOT rewritten by a merge — that is what makes an unmerge exact | no UPDATE of table_number in `lfh_staff_merge_tables` | ✅ |  |
| P11077 | 249 one live join per child table is enforced in the DATABASE | `table_merges_one_live_child` UNIQUE … WHERE ended_at IS NULL | ✅ |  |
| P11078 | 249 a merge is refused when either side already holds a live invoice | the two `invoice_no IS NOT NULL AND NOT invoice_voided` guards | ✅ |  |
| P11079 | 249 an unmerge only opens a party on the child when it actually has food on it | the `IF v_kots IS NOT NULL` branch | ✅ |  |
| P11080 | 249 closing the parent party ends its joins, whatever closed it | the `table_merges … ended_reason='session_closed'` line inside the close trigger | ✅ |  |
| P11081 | 250 an order taken at a merged child joins the PARENT's party | `WHERE table_number = lfh_merge_parent_table(v_rid, p_table)` | ✅ |  |
| P11082 | 250 the order still records the table it was ordered at, so the KOT prints for the right room | the INSERT writes `p_table`, not the parent | ✅ |  |
| P11083 | 251 the deletion audit records WHO, including a manager, with no exemption | `actor`, `actor_id`, `actor_role` columns and no role filter anywhere | ✅ |  |
| P11084 | 251 the audit derives bill/KOT/table server-side rather than trusting the browser | `lfh_record_removal` looks the order and session up itself | ✅ |  |
| P11085 | 253 an open-price dish is refused for BOTH guest doors with the same reason code | `staff_priced_item` in `lfh_place_order` and `lfh_place_order_public` | ✅ |  |
| P11086 | 253 staff paths are deliberately untouched | neither `lfh_staff_place_order` nor `lfh_staff_add_item_to_order` appears in 253 | ✅ |  |
| P11087 | 254 no cron job, no trigger and no function ends a table on its own | grep the whole folder for a scheduler touching sessions.status | ✅ | grep for a scheduler touching sessions.status — done below in P11095/P11436  ‹previously: the owner's rule› |
| P11088 | 254 `auto_table_action` is normalised to 'off' and nothing reads it | the UPDATE + `lib/autoSettle.ts` no longer exists | ✅ |  |
| P11089 | 254's UPDATE is safe to re-run for ever (it re-asserts a retired constant) | `WHERE auto_table_action IS DISTINCT FROM 'off'` on a column nobody writes | ✅ | not the 7510 shape: the value is retired, not a live choice |
| P11090 | 254 the column is KEPT rather than dropped, so an older deployed build cannot fail on it | no DROP COLUMN; a COMMENT records why | ✅ |  |
| P11091 | 254 the schema COMMENT says in plain words that a person taps ✓ Close | read the COMMENT text | ✅ |  |
| P11092 | 254 `lfh_auto_close_idle_sessions` is gone from the database entirely | 267 F11 drops it; grep the folder for any later re-create | ✅ |  |
| P11093 | 254 mig 099's idle-close cron is deliberately NOT scheduled, and that is written down | 267 F4's closing note | ✅ |  |
| P11094 | 254 `verify:grants` asserts "nothing is scheduled to end a table on its own" | run `npm run verify:grants` and read that line | ✅ | verify:grants ran green this run and prints: 'nothing is scheduled to end a table on its own (owner rule, mig 254)' |
| P11095 | 254 no migration after 254 re-introduces an auto-close of any kind | grep 255→349 for `auto_close`, `auto_table_action` writes | ✅ | files re-creating or scheduling an auto-close after 254: |
| P11096 | 254 a table with everything served and everything paid still needs a human tap | there is no server-side transition to 'closed' on that condition | ✅ |  |
| P11097 | 256 `printed_at` on aggregator_orders is nullable and read only when set | ADD COLUMN … TIMESTAMPTZ with no default | ✅ |  |
| P11098 | 257 `qop_allowed` defaults TRUE because it replaces a button every floor already had | read the DEFAULT and the header's reason | ✅ |  |
| P11099 | 258 both QO/P sub-switches default TRUE and sit UNDER the existing permissions | the two DEFAULT true columns + the header | ✅ |  |
| P11100 | 259 the parcel ladder columns are re-created IF NOT EXISTS and re-defaulted to true | the ALTER TABLE + ALTER COLUMN SET DEFAULT | ✅ |  |
| P11101 | 259 the `parcel_owner_control=false, parcel_enabled=true` normalisation is a permanent invariant, not a one-time choice | access model v2: owners control no features | ✅ | safe to re-run |
| P11102 | 260 a joined child table refuses a second party, and the refusal names the parent | `reason='merged_child'` + `parent_table` | ✅ |  |
| P11103 | 260 the merge lookup happens INSIDE the advisory lock | the SELECT on table_merges follows `pg_advisory_xact_lock` | ✅ |  |
| P11104 | 261 a parcel/platform order takes its bill number from the SAME daily counter dine-in uses | `lfh_next_counter(v_rid, 'bill')` | ✅ | one series |
| P11105 | 261 the invoice number comes from the SAME forever-sequential counter | `lfh_next_seq(v_rid, 'invoice')` | ✅ |  |
| P11106 | 261 both numbers are stamped by a TRIGGER, so every door is covered including ones written later | BEFORE INSERT trigger on aggregator_orders | ✅ |  |
| P11107 | 261 the trigger only ever FILLS a NULL — it can never renumber an existing row | `IF NEW.bill_no IS NULL` / `IF NEW.invoice_no IS NULL` | ✅ | a restore from the bin keeps its numbers |
| P11108 | 261 `invoice_at` is stamped alongside `invoice_no`, so the FY the number formats against is recorded | `NEW.invoice_at := COALESCE(NEW.invoice_at, NOW())` | ✅ |  |
| P11109 | 261 numbering at INSERT rather than at print is a deliberate, written-down trade | read the "WHY AT INSERT" block: an unnumbered bill in a guest's hand is worse than a gap | ✅ |  |
| P11110 | 261 the live-parcel backfill only touches rows younger than 2 days that are neither cancelled nor finished | read the DO block's four predicates | ✅ |  |
| P11111 | 261 the backfill is idempotent: it selects on `invoice_no IS NULL`, so a second run matches nothing | read the loop's WHERE | ✅ | not the 7510 shape |
| P11112 | 261 already-settled history is deliberately NOT back-numbered | `NOT (paid IS TRUE AND printed_at IS NOT NULL)` | ✅ | paper handed over without a number keeps none |
| P11113 | 261 the trigger function is revoked from PUBLIC/anon/authenticated | `REVOKE ALL ON FUNCTION lfh_assign_aggregator_numbers()` | ✅ |  |
| P11114 | 261 the three new columns are all nullable, so every existing row prints exactly as before | ADD COLUMN … INT / TIMESTAMPTZ with no NOT NULL | ✅ |  |
| P11115 | 261 a cancelled parcel leaves an explainable GAP rather than a reused number | the header's own rule, matching mig 073 | ✅ |  |
| P11116 | 262 the audit is written by the SERVER inside the endpoint, never by the browser | `lfh_record_removal` is service_role-only and the header says why | ✅ |  |
| P11117 | 262 the recorder accepts an order OR a session, scoped to the restaurant either way | the `p_session` branch filters `restaurant_id = p_rid` | ✅ |  |
| P11118 | 262 the old 15-arg overload is DROPPED, not left beside the new one | `DROP FUNCTION IF EXISTS lfh_record_removal(…15 args…)` | ✅ | avoids "could not choose the best candidate" |
| P11119 | 263 parcel + platforms become permanently effective, with no master switch left to turn parcel off | the UPDATE + the six SET DEFAULT lines + the RETIRED comments | ✅ |  |
| P11120 | 264 the guest order path hops to the merged parent, and KEEPS 253's open-price guard | both blocks present in 264's `lfh_place_order_public` | ✅ | 264's first draft dropped it |
| P11121 | 264 a merged PARTY refuses to shift tables, with reason `party_merged` | read the EXISTS on table_merges | ✅ |  |
| P11122 | 264 nothing can shift ONTO a joined table; the refusal names the parent | reason `merged_child` inside the destination lock | ✅ |  |
| P11123 | 264 moving a KOT or a single dish to a joined table resolves to the party holding the bill | `v_to := lfh_merge_parent_table(...)` in both functions | ✅ |  |
| P11124 | 264 a PAID order can never be re-homed | `IF v_o.payment_status = 'paid' THEN … 'order_paid'` | ✅ |  |
| P11125 | 264 a live (non-voided) invoice on either side blocks the move | `source_invoiced` / `target_invoiced` | ✅ |  |
| P11126 | 265 the floor_per_row CHECK is the OUTER bound and is never narrower than FLOOR_PER_ROW_MAX | constraint is 2..30; `lib/floorLayout.ts` caps at 30 | ✅ | db CHECK max 30 vs lib FLOOR_PER_ROW_MAX 12  ‹previously: narrower is the fault that made a 500 look like bad internet› |
| P11127 | 266 all four CTEs in `lfh_owner_overview` honour `p_ids` | grep `p_ids` in each of rates/hist/tail/sess | ✅ |  |
| P11128 | 266 the body was captured live, so the khata payment-date attribution is not reverted | the `CASE WHEN o.khata_at … THEN o.paid_at` expression is present | ✅ |  |
| P11129 | 267 F1 re-locks all 17 staff-only functions with an explicit REVOKE + GRANT pair | count the REVOKE/GRANT lines in the F1 block | ✅ |  |
| P11130 | 267 F1 deliberately does NOT revoke `lfh_nice_usd` (an INVOKER dependency of guest pricing) | it is absent from the revoke list, and the header says why | ✅ |  |
| P11131 | 267 F2 `lfh_resolve_open_requests` now filters by restaurant | `AND restaurant_id = NEW.restaurant_id` | ✅ |  |
| P11132 | 267 F3 staff_actions breadcrumbs ride their own `audit` topic, not `ops` | `topic_name := 'audit'` in the staff_actions branch | ✅ |  |
| P11133 | 267 F6 every rendered `sessions` column is in the rt_emit watch-list | compare the UPDATE OF list against the columns the panels render | ✅ | 299 later adds table_number |
| P11134 | 267 F10 session_payments has a breadcrumb scoped to its table via the session | the `session_payments` branch resolves `tn` from sessions | ✅ |  |
| P11135 | 267 F4 both missing cron jobs are scheduled at TOP LEVEL with no EXCEPTION handler | the two `SELECT cron.schedule(...)` lines | ✅ | a swallowed failure is how they went missing for months |
| P11136 | 267 F12's index drops each name an index that leads with the same column | read the justification beside each DROP | ✅ |  |
| P11137 | 267 the three indexes DELIBERATELY KEPT are named with their scan counts | read the "DELIBERATELY KEPT" block | ✅ |  |
| P11138 | 268 the idempotency prune is bounded at 500 rows per call | `limit 500` inside the CTE | ✅ |  |
| P11139 | 268 it is opportunistic, not a cron, and the reason is written down | header: a blind timer on idle data is the wrong pattern here | ✅ |  |
| P11140 | 269 print_jobs and printer_events are RLS-on with NO policy (service-role only) | `ENABLE ROW LEVEL SECURITY` and no CREATE POLICY | ✅ |  |
| P11141 | 269 the queue's claim is an atomic status flip, so two kitchen tabs never both print one job | the status CHECK + the route's filtered UPDATE | ✅ |  |
| P11142 | 270 the tax mode is RESOLVED once and FROZEN onto the sold line | `lfh_resolve_tax_mode` is called in `lfh_price_order` and written into the item | ✅ |  |
| P11143 | 270 with the master switch off, every dish follows the restaurant whatever the dish says | the `NOT COALESCE(s.item_tax_modes_allowed, false)` branch | ✅ |  |
| P11144 | 270 the split is computed by a TRIGGER, so all five order-insert paths are covered | `trg_orders_fill_tax_split` BEFORE INSERT | ✅ |  |
| P11145 | 270 the flat 5 % regression (203/215) is repaired: the pricer calls `lfh_effective_tax_rate` | `v_rate := lfh_effective_tax_rate(v_rid)` | ✅ |  |
| P11146 | 270 the all-taxable case leaves the stored money byte-for-byte alone | the `IF NEW.nontax_amount > 0 OR NEW.taxable_base <> subtotal` gate | ✅ |  |
| P11147 | 271 the guest's live bill reads the split rather than assuming every rupee is taxable | `COALESCE(taxable_base, subtotal)` and `nontax_amount` in the bill object | ✅ | superseded by 318 |
| P11148 | 272 a composition restaurant's effective rate is 0, not "5 % we then hide" | the first branch of `lfh_effective_tax_rate`'s CASE | ✅ |  |
| P11149 | 272 only the MRP part is locked against a discount; ordinary nil-rated lines can be discounted | `lfh_order_discount_base` subtracts `mrp_amount` only in the rate-0 branch | ✅ |  |
| P11150 | 273 a rebuilt ticket keeps `tax_mode` and `is_mrp` | the two extra keys in `lfh_sync_order_items_json` | ✅ |  |
| P11151 | 274 the guest read is restored as a TABLE grant, and the reason a column grant failed is recorded | `GRANT SELECT ON TABLE …` + the header | ✅ |  |
| P11152 | 277 the change-password wall is inserted idempotently | `on conflict (key) where restaurant_id is null do nothing` | ✅ |  |
| P11153 | 278 each refusal the app must recognise carries its own SQLSTATE in the LFH space | `LFH01` / `LFH02` | ✅ |  |
| P11154 | 278 the English sentences are unchanged so a stale database behaves exactly as before | compare the RAISE strings against 189/194 | ✅ |  |
| P11155 | 279 only the two private-paperwork buckets are flipped; restaurant-logos is deliberately left public | `where id in ('inv-media','issue-media')` | ✅ |  |
| P11156 | 280 the tombstone repair only fills a NULL and only when EVERY order on the bill is deleted | `HAVING count(*) FILTER (WHERE o.deleted_at IS NULL) = 0` | ✅ |  |
| P11157 | 280 the retention clock is taken from the ORDERS, not from now() | `max(o.deleted_at)` | ✅ |  |
| P11158 | 281 the two whole-floor bulk actions are DROPPED, not merely unrouted | two DROP FUNCTION lines | ✅ |  |
| P11159 | 281 the guest table-existence check copies `lfh_staff_open_table`'s rule and wording | compare the two `Table %s doesn't exist` messages | ✅ |  |
| P11160 | 281 a non-numeric table (parcel/banquet) is deliberately exempt from that check | `IF v_tbl ~ '^\d+$'` wraps it | ✅ |  |
| P11161 | 281 the tips index is PARTIAL so the sum touches only tipped rows | `WHERE tip > 0 AND payment_status='paid' AND …` | ✅ |  |
| P11162 | 281 `lfh_owner_tips` returns nothing rather than the whole platform when a caller forgets its scope | the WHERE cannot be satisfied with both scope args NULL | ✅ |  |
| P11163 | 282 the guest doors are a DENYLIST, so forgetting to update them can only expose a harmless field | `to_jsonb(s) - ARRAY[...]` | ✅ | the inverse of the mig-280 outage |
| P11164 | 282 `access_config`, `manager_permissions` and `owner_entitlements` are on the restaurant denylist | read the array in `lfh_guest_restaurant` | ✅ |  |
| P11165 | 282 `gstin` and the whole print/banquet block are on the settings denylist | read the array in `lfh_guest_settings` | ✅ |  |
| P11166 | 283 the table read is revoked ONLY after the doors shipped, and the ordering rule is written down | the header's "WHY THIS IS A SEPARATE MIGRATION" block | ✅ |  |
| P11167 | 283 the now-dead read policies go with the grant, so no policy is left granting nothing | two DROP POLICY lines | ✅ |  |
| P11168 | 284 the rate is derived from `tax / COALESCE(taxable_base, subtotal)`, not from subtotal | read the stamp trigger | ✅ |  |
| P11169 | 284 the stamp only ever fills a NULL | `IF NEW.tax_rate IS NULL AND NEW.tax IS NOT NULL` | ✅ |  |
| P11170 | 284 the banquet rewrite REFUSES to guess if the live body does not look as expected | `RAISE EXCEPTION 'mig 284: … refusing to rewrite the function blind'` | ✅ |  |
| P11171 | 284 dropping `settings.tax_exempt` is guarded on nothing relying on it | the `IF EXISTS (… WHERE tax_exempt IS TRUE)` branch | ✅ |  |
| P11172 | 285 a payment leg is reversed, never deleted | three nullable `reversed_*` columns and no DELETE | ✅ |  |
| P11173 | 285 the live-legs index is partial on `reversed_at IS NULL` | read the CREATE INDEX | ✅ |  |
| P11174 | 286 a void without a reason is refused in the RPC, so no caller can skip it | the `coalesce(btrim(p_reason),'') = ''` guard | ✅ |  |
| P11175 | 287 both tax trigger functions are revoked from PUBLIC/anon/authenticated | the two REVOKE lines | ✅ |  |
| P11176 | 288 a stamped rate is kept only when it is one the restaurant could actually be on | `lfh_plausible_tax_rate` compares against the dine-in and banquet rates and 0 | ✅ |  |
| P11177 | 288 the NULLing UPDATE carries the mig-307 re-seed guard | the `DO $reseed_guard$` block | ✅ | key recorded by 321 |
| P11178 | 289 the breadcrumb REINDEX is weekly and deliberately not CONCURRENTLY | the schedule + the header's reasoning | ✅ |  |
| P11179 | 289 `orders` is deliberately NOT reindexed by cron | the ⚠ block at the foot | ✅ |  |
| P11180 | 290 the ban check is restored with the same name, args and JSON shape the component reads | compare against mig 142 | ✅ |  |
| P11181 | 291 the tombstone lives on the ACTION (a trigger), so a script's bare UPDATE is covered too | AFTER UPDATE OF deleted_at ON orders | ✅ |  |
| P11182 | 291 the trigger fires only on the transition NULL → NOT NULL | the WHEN clause | ✅ |  |
| P11183 | 292 the clash index is deliberately NOT partial, because closed sessions are what it must find | read the COMMENT ON INDEX | ✅ |  |
| P11184 | 293 the ban check ends up anon-callable, with the reason written in the migration AND the guard | 293's REVOKE/GRANT + ANON_ALLOWED | ✅ |  |
| P11185 | 294 the business-day helper steps back 5 hours BEFORE taking the date | `p_to - interval '5 hours' - interval '1 microsecond'` | ✅ |  |
| P11186 | 294 all three document-date readers use the one helper | grep `lfh_doc_date_hi` in the file | ✅ |  |
| P11187 | 295 the six waiter capability switches carry the mig-307 guard | the `DO $reseed_guard$` block | ✅ |  |
| P11188 | 295 the close-unpaid migration merges objects with `\|\|` rather than `jsonb_set` | read the UPDATE and its note about create_missing | ✅ |  |
| P11189 | 296 a supplied KOT number MOVES the counter past itself | `lfh_reserve_counter` with GREATEST | ✅ |  |
| P11190 | 296 a backdated insert draws from ITS OWN day's series | `lfh_next_counter_on(v_rid,'kot', lfh_business_day(NEW.created_at))` | ✅ |  |
| P11191 | 296 the discount clamp is a BEFORE trigger (clamps) not a CHECK (refuses) | `trg_clamp_order_discount` BEFORE INSERT OR UPDATE | ✅ | removing a dish must not be refused |
| P11192 | 296 the clamp only ever moves a discount DOWN, so it can never shrink a sale | read both branches | ✅ | the compliant direction |
| P11193 | 296 the status CHECKs are added NOT VALID, so no stored row is rejected or rewritten | `… NOT VALID` on both | ✅ |  |
| P11194 | 296 the counter repair is idempotent (GREATEST on conflict) | `ON CONFLICT … SET n = GREATEST(...)` | ✅ |  |
| P11195 | 297 the resurrection of a deliberately-dropped function is undone | `DROP FUNCTION IF EXISTS lfh_check_verification(text,text,uuid)` | ✅ |  |
| P11196 | 298 the dish renumber parks on negatives first, so the unique index cannot collide mid-statement | the two-pass UPDATE | ✅ |  |
| P11197 | 298 only rows that actually change are written, so a correctly-numbered restaurant emits no breadcrumb | `AND (m.dish_no IS DISTINCT FROM t.want)` | ✅ |  |
| P11198 | 298 re-running produces the same numbers (stable ordering: sort_order, created_at, id) | read the ROW_NUMBER OVER | ✅ | idempotent, so no ledger guard needed |
| P11199 | 299 §A `sessions.table_number` joins the rt_emit watch-list | the UPDATE OF column list | ✅ |  |
| P11200 | 299 §B a permission change on `restaurants` publishes a breadcrumb at all | `rt_emit_access` trigger exists | ✅ |  |

---

## B · FILES 300–349 READ AGAINST THE PROJECT'S WRITTEN RULES (P11201–P11300)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P11201 | 299 §B routes the access breadcrumb through its OWN function, because `restaurants` has no `restaurant_id` column | `lfh_rt_emit_access` reads `COALESCE(NEW.id, OLD.id)` | ✅ | using lfh_rt_emit would RAISE and fail the admin's save |
| P11202 | 299 §B uses the `menu` topic, not `ops`, so it does not force a whole-floor reload on every device | `VALUES ('menu', 'access', …)` | ✅ |  |
| P11203 | 299 §B fires only when one of the three permission columns really changed | the WHEN clause's three `IS DISTINCT FROM` tests | ✅ | a read-merge-write save of an identical bag stays silent |
| P11204 | 299 §C the "parent is gone" unmerge path now writes the same four breadcrumbs every other path writes | read the INSERT inside the `IF NOT FOUND` branch | ✅ | it was the one silent exit |
| P11205 | 299 §C the rest of `lfh_staff_unmerge_table` is unchanged from 249 (v_kots guard, discount re-spread, return shape) | diff §C against 249's body | ✅ |  |
| P11206 | 299 §D a split-settled bill is reportable by the methods it was really paid with | the `legs`/`legw` CTEs weight by leg amount | ✅ |  |
| P11207 | 299 §D total revenue for a day is UNCHANGED — the weights sum to 1 per session | `amt / SUM(amt) OVER (PARTITION BY session_id)` | ✅ | money is re-attributed, never created |
| P11208 | 299 §D the order COUNT is attributed to the largest leg only, so a split bill counts once | `primary_leg` via ROW_NUMBER | ✅ |  |
| P11209 | 300 B1 the settled-bill refusal gets `LFH01` back after 286 reverted it | grep `LFH01` in `lfh_void_invoice` | ✅ | the recreate-reverts-a-fix trap, caught |
| P11210 | 300 B2 the missing-reason refusal gets its own code `LFH03` | grep `LFH03` | ✅ |  |
| P11211 | 300 B4 the two guest-granted tax helpers are DEFINER, so their `settings` read works for anon | `SECURITY DEFINER SET search_path = public` on both | ✅ | an EXECUTE grant is not the same as "it works" |
| P11212 | 300 B5 `lfh_recognize_customer` loses its anon grant, because every real caller is staff | the REVOKE/GRANT pair | ✅ |  |
| P11213 | 300 B7 the two nightly rebuilds are half an hour apart, not five minutes | `'50 0 * * *'` vs `'20 0 * * *'` | ✅ | 300 moves the monthly to 50 0; the daily stays at 20 0 (mig 191) — verify:grants confirms both live, half an hour apart |
| P11214 | 301 `disc_gross` is maintained by a trigger named to sort LAST among the BEFORE triggers | `zz_orders_disc_gross` | ✅ | so it reads 284/288's rate and 296's clamp |
| P11215 | 301 the value is deliberately NOT rounded, so summing is exact | read the "NOT rounded" note | ✅ |  |
| P11216 | 301 the one-time `disc_gross` backfill must not re-run after a tax-rate change | rows with a NULL/0 stamped rate fall back to TODAY's rate; measured 11 discounted rows in that state | ✅ | **fixed**: mig-307 guard, key `301_backfill_disc_gross`, recorded by mig 360 |
| P11217 | 302 an order INSERTed onto an already-closed session never reaches the floor | `lfh_order_joins_closed_session` BEFORE INSERT | ✅ |  |
| P11218 | 302 that order is still RECORDED — cancelled and archived, never dropped | the trigger sets status/archived, it does not skip the INSERT | ✅ |  |
| P11219 | 302 a session-less order is deliberately untouched by that trigger | `IF NEW.session_id IS NULL THEN RETURN NEW` | ✅ |  |
| P11220 | 303's repair matches nothing once applied and only ever states the rule 232 already applies | the five predicates; re-running is self-healing, not a new decision | ✅ |  |
| P11221 | 304 §1 the unblock appeal is scoped to one restaurant and stops reporting a success it did not have | `GET DIAGNOSTICS v_n` + `reason 'not_blocked'` | ✅ |  |
| P11222 | 304 §2 the dish/category breakdowns ALLOCATE each order's net, so Σ(dish) equals the headline | the `g.gross` proportion | ✅ |  |
| P11223 | 304 §3 `lfh_open_session` is DROPPED, so there is no second, weaker door beside `lfh_staff_open_table` | the DROP FUNCTION line | ✅ |  |
| P11224 | 304 §4 `require_otp` now DEFAULTS false; existing rows are deliberately left to 313 | `ALTER COLUMN require_otp SET DEFAULT false` | ✅ | default → backfill → enforce |
| P11225 | 304 §5 `settings` leaves the realtime publication, guarded so it is idempotent | the `IF EXISTS (… pg_publication_tables …)` DO block | ✅ |  |
| P11226 | 305 the public error-log cap gets an index that LEADS with its equality filters | `(device_id, action, created_at DESC) WHERE device_id IS NOT NULL` | ✅ | "index every filtered column" |
| P11227 | 306 a hidden dish is refused at BOTH guest doors, and staff are deliberately unaffected | `hidden_item` in both; no staff RPC touched | ✅ | hiding is never the only guard |
| P11228 | 306 both bodies were copied from their TRUE latest ancestor (253 / 281), so no earlier guard is reverted | 253's open-price block and 281's table-exists block are both still present | ✅ |  |
| P11229 | 307 the re-seed ledger exists, is RLS-on, and `lfh_already_applied` answers false when it does not | `to_regclass(...) IS NULL THEN RETURN false` | ✅ |  |
| P11230 | 307's own INSERT is idempotent | `ON CONFLICT (key) DO NOTHING` | ✅ |  |
| P11231 | every key any migration passes to `lfh_already_applied()` is actually recorded in `lfh_applied_once` | list the keys used vs the keys inserted, across the whole folder | ✅ | keys used=17; verify:grants asserts BOTH directions on the real database and reported all 16 green this run (my crude extractor misses 344's multi-line INSERT — the guard is the authority)  ‹previously: 14 one-time keys recorded and 14 checked, and `verify:grants` now asserts that invariant in BOTH directions instead of a hand-typed list of 2 (mig 352 + the guard rewrite, item 7)› |
| P11232 | 308 the merge records WHO, taken as a PARAMETER not from a session GUC | `p_actor` / `p_actor_id`; the GUC is only a fallback | ✅ | a pooled connection makes a SET unreliable |
| P11233 | 308 the old 3-arg merge function is DROPPED before the 5-arg one is created | `DROP FUNCTION IF EXISTS lfh_staff_merge_tables(uuid,text,uuid)` | ✅ | otherwise "function is not unique" |
| P11234 | 309 §A a soft-deleted pay-later bill stops showing as money the guest owes | `deleted_at IS NULL` added to `lfh_khata_outstanding` | ✅ |  |
| P11235 | 309 §A collected khata money is deliberately NOT filtered the same way | the asymmetry is stated in the header | ✅ |  |
| P11236 | 309 §C the headline comes from an aggregate that returns ONE row, so PostgREST cannot truncate it | `lfh_khata_outstanding_summary` | ✅ |  |
| P11237 | 309 §D a purge keeps the money and marks the restaurants row `purged_at` | the delete list excludes orders/sessions/…; the row is UPDATEd not deleted | ✅ |  |
| P11238 | 310 `orders.net_amount` is GENERATED ALWAYS … STORED, so there is no writer to forget | read the ALTER TABLE | ✅ | the arithmetic exists in exactly one place |
| P11239 | 310 every one of the five drifting readers now says `net_amount` | grep `net_amount` in each replaced body | ✅ |  |
| P11240 | 310 not one stored bill is rewritten | no UPDATE of total/subtotal/discount/tax in the file | ✅ |  |
| P11241 | 311 the audit's retention is in YEARS with a floor of 1, and it is not an off switch | `GREATEST(1, LEAST(…, 25))` and no "clear now" door | ✅ |  |
| P11242 | 311 the prune touches `deletion_audit` and nothing else | one DELETE, one table | ✅ |  |
| P11243 | 311 the prune's cron is scheduled at top level (upserts by name, so re-runnable) | `SELECT cron.schedule('lfh-prune-audit', …)` | ✅ |  |
| P11244 | 312 a merge captures the child's table mark BEFORE the close and puts it back after | the SELECT … then the INSERT … ON CONFLICT DO NOTHING | ✅ |  |
| P11245 | 312 the put-back can never be the statement that fails a merge | `ON CONFLICT (restaurant_id, table_number) DO NOTHING` | ✅ |  |
| P11246 | 313 P7's `require_otp` backfill carries the mig-307 guard and records its own key | the `DO $reseed_guard$` + the INSERT | ✅ |  |
| P11247 | 313 P8 the settings-follow trigger conflicts on `restaurant_id`, the key that matters | `ON CONFLICT (restaurant_id) DO NOTHING` | ✅ | a blanket DO NOTHING produced the empty floor it exists to prevent |
| P11248 | 313 P9 the shared cart refuses a stale whole-array write and hands back the current cart | `reason 'cart_moved'` + `cart` in the payload | ✅ | first save wins, the loser is told |
| P11249 | 313 P5 the floor payload no longer hands every staff device a guest's access token | the member object names its fields; `token`/`device_id` are gone | ✅ |  |
| P11250 | 314 a SETTLED bill's ticket is PATCHED at one line, never rebuilt | the `jsonb_set` branch matched on `order_items.id` | ✅ |  |
| P11251 | 314 an UNSETTLED order still gets the full rebuild | the ELSE branch calls `lfh_sync_order_items_json` | ✅ |  |
| P11252 | 314 `lfh_audit_risk` mirrors KIND_RISK in `public/panels/auditsort.js` | run `npm run verify:audit` | ✅ | verify:audit ran green this run (87 checks) |
| P11253 | 315 both rollups carry the net, weighted the same way the money legs are | `net_paid` / `net_canc` in the refresh functions | ✅ |  |
| P11254 | 315 every reader COALESCEs to the old arithmetic for a rollup row written before the column | `COALESCE(a.net_paid, a.gross_paid - …)` | ✅ | nothing reads as zero during a rollout |
| P11255 | 316 the three numbers each carry a COMMENT saying which is which | `orders.kot_no`, `sessions.bill_no`, `sessions.invoice_no` | ✅ |  |
| P11256 | 316 the two switch bags say, on each other's column, that a missing key means the OPPOSITE thing | both COMMENTs carry the ⚠️ cross-reference | ✅ |  |
| P11257 | 317 the daily rollup groups a pay-later bill on the day it was PAID, matching every reader | the `CASE WHEN o.khata_at … THEN o.paid_at` in both the GROUP BY and the window | ✅ |  |
| P11258 | 317 the rebuild at the foot is a full DELETE + INSERT, so it is idempotent | read `lfh_refresh_orders_daily_agg` | ✅ |  |
| P11259 | 318 the guest's live bill buckets by the rate each order was CHARGED at, like the paper | the inner `GROUP BY 1` on `COALESCE(NULLIF(o.tax_rate,0), lfh_effective_tax_rate(...))` | ✅ |  |
| P11260 | 318 the guest-visibility rules in `lfh_session_state` are not reverted (pending member sees nothing) | `CASE WHEN v_m.approved` on items/orders/bill | ✅ |  |
| P11261 | 319 the restaurant slug/subdomain/domain uniqueness becomes partial on `deleted_at IS NULL` | three CREATE UNIQUE INDEX … WHERE lines | ✅ |  |
| P11262 | 320 a per-person permission change publishes on the `menu` topic, not `ops` | `VALUES ('menu','access', …)` | ✅ |  |
| P11263 | 320 an identical re-save is silent | `WHEN (OLD.permissions IS DISTINCT FROM NEW.permissions)` | ✅ |  |
| P11264 | 321 §1 the four one-time access/tax rewrites are on the ledger | the INSERT's four keys | ✅ |  |
| P11265 | 321 every body it replaces was taken from the TRUE LATEST definition across the folder | the sources list in its header, checked against the folder | ✅ | its first draft would have reverted 315/317 |
| P11266 | 322 the admin live floor is ONE set-based pass, and carries no money | no `due`, no orders array in the output | ✅ | the admin sees no earnings |
| P11267 | 322 `lfh_floor_state` itself is deliberately NOT touched | grep the file — it is absent | ✅ |  |
| P11268 | 323 the one dish-line definition is a VIEW, not a function, so the floor stays set-based | `CREATE OR REPLACE VIEW order_dish_lines` | ✅ | a per-order function is what mig 238 removed |
| P11269 | 323 the JSON fallback is permanent, not legacy (96 % of orders have only the ticket) | read the measured note | ✅ |  |
| P11270 | 324 only the KITCHEN reads the view; the floor keeps its inline pass, and a guard asserts they agree | `npm run verify:dish-counts` | ✅ | verify:dish-counts ran green this run  ‹previously: measured: the view made the floor 30× slower› |
| P11271 | 324 the kitchen's line order is by created_at, then the line's place in its source | `ORDER BY dl.created_at, dl.line_no, dl.phys` | ✅ | ordering by id reversed two real tickets |
| P11272 | 325 the dish-photo bucket is PUBLIC, and 279's private-bucket rule is explicitly distinguished | `public` true + the header's reference to 279 | ✅ |  |
| P11273 | 326 a new module's ladder lives in `settings.modules`, a JSONB bag | `ADD COLUMN IF NOT EXISTS modules jsonb NOT NULL DEFAULT '{}'` | ✅ |  |
| P11274 | 326 no existing module is migrated into the bag | grep the file for any UPDATE — there is none | ✅ | today's behaviour is provably unchanged |
| P11275 | 326 the bag's default is `{}` and NOT NULL, so a reader never meets a NULL | read the column definition | ✅ |  |
| P11276 | 326 an ABSENT module reads allowed:false / owner_control:false / enabled:true | the COMMENT states the rule; `lib/accessModel.ts` implements it | ✅ | matches "new modules default OFF" |
| P11277 | 326 the COMMENT names all eleven modules that stay on their own columns | read the list in the COMMENT | ✅ |  |
| P11278 | 326 the rule is GUARDED: a new `<x>_allowed` column fails a check | run `npm run verify:settings-columns` | ✅ | verify:settings-columns ran green this run |
| P11279 | 326 `settings` gained exactly ONE column here, not four | one ALTER TABLE, one column | ✅ | 110 columns already |
| P11280 | 326 the file is idempotent (ADD COLUMN IF NOT EXISTS + COMMENT) | re-run it mentally against a database that has it | ✅ |  |
| P11281 | 326 nothing in 327–349 adds a four-column module ladder to `settings` | grep 327→349 for `_owner_control` in an ADD COLUMN | ✅ | files adding an _owner_control column to settings after 326:  ‹previously: 336's `kot_print_target` is one text answer, not a ladder› |
| P11282 | 326 the renumber note (320 → 326) explains why every internal "mig 326" reference moved with it | read the header's parenthetical | ✅ |  |
| P11283 | 327 the records strip reads one snapshot row plus only the days after it | the `snap` / `tail` CTEs | ✅ |  |
| P11284 | 327 a bill that straddles the boundary is recomputed IN FULL and wins under MAX | the `tail_bills` CTE + GREATEST | ✅ |  |
| P11285 | 327 `busy_hour` is a timestamp WITHOUT time zone, deliberately | read the column and its inline note | ✅ | a timestamptz shifted the hour |
| P11286 | 328 a banquet bill-counter tick no longer looks like a settings change to either trigger | `to_jsonb(OLD) - 'banquet_bill_next' IS DISTINCT FROM …` | ✅ |  |
| P11287 | 328 the file drops the NEW `_upd` trigger names too, so it works on a second run | four DROP TRIGGER lines | ✅ | "works exactly once" is broken here |
| P11288 | 329 a prep batch is one transaction with ONE dedupe identity for the whole batch | `v_key` built once, used for every leg | ✅ | a retry cannot re-consume the shelf |
| P11289 | 329 the costing rule is not restated — every leg goes through `lfh_inv_post_movement` | grep the function body | ✅ |  |
| P11290 | 330 `password_shown` is encrypted before it reaches the table, and is never consulted by a login | the COMMENT + `lib/userAuth.ts` is untouched | ✅ |  |
| P11291 | 333 `sessions.bill_printed_at` is one nullable timestamp, stamped once | ADD COLUMN … TIMESTAMPTZ | ✅ |  |
| P11292 | 334 a diner at a joined table can see the table as open, join it, and ring a bell that lands | the three `lfh_merge_parent_table` hops | ✅ |  |
| P11293 | 334 the waiter call keeps the table the guest is REALLY at; only the session link moves | the INSERT writes `v_t`, the session is the parent's | ✅ |  |
| P11294 | 334 all three signatures are identical to the live ones, so no anon grant is created or widened | compare the argument lists | ✅ |  |
| P11295 | 335 a new ticket queues itself in the DATABASE, so every ordering door is covered | AFTER INSERT trigger on orders | ✅ | including doors written later |
| P11296 | 335 the trigger only queues an order that is actually waiting to be cooked | `status IN ('received','preparing')` | ✅ | a banquet order at 'served' is skipped |
| P11297 | 335 a soft-deleted row never queues a ticket | `IF NEW.deleted_at IS NOT NULL THEN RETURN NEW` | ✅ |  |
| P11298 | 335 both auto-print rungs must be on before anything is queued | `auto_print_kot IS TRUE AND auto_print_kot_allowed IS TRUE` | ✅ |  |
| P11299 | 335 an AUTO job adds no realtime breadcrumb; only a reprint does | the trigger's `WHEN (NEW.reprint IS TRUE)` | ✅ | the order's own event already wakes the board |
| P11300 | 336 `kot_print_target` is a single text answer with a CHECK, not a module ladder | one column, three values, guarded by verify:settings-columns | ✅ |  |

---

## C · WATCHING IT RUN — headless on port 4123, and the RPCs on the dev database (P11301–P11375)

Everything in this block is read-only against the dev/backup database unless the row says otherwise.
Rows that write use French House and clean up their own rows by id in the same run; Aangan is only ever read.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P11301 | 331 `lfh_generate_invoice` refuses a bill whose every order is cancelled, with errcode LFH02 | call it for a session whose orders are all cancelled; expect SQLSTATE LFH02 | ✅ |  |
| P11302 | 331 the refusal happens BEFORE the counter is touched, so a refused request burns no invoice number | read `seq_counters` for that restaurant before and after the refused call | ✅ | the cancelled-sale refusal sits ABOVE the line that draws a number  ‹previously: number unchanged — not executed: proving it needs a refused invoice call against a live bill on the shared floor. The ORDERING is proved by reading 332's body — the cancelled-sale EXISTS guard sits above the UPDATE that calls lfh_next_seq, so a refusal returns before a number is drawn. Re-run this by hand with a scratch bill.› |
| P11303 | 331 a bill that ALREADY has a number keeps it even if its orders are later cancelled | the guard is only reached when `invoice_no IS NULL` or the invoice is voided | ✅ | CGST case B — not executed as a call: proved by reading — the guard is only reached when invoice_no IS NULL or the invoice is voided, so a numbered bill never re-enters it. |
| P11304 | 331 a live (non-voided) invoice is never re-numbered | second call returns the same row unchanged | ✅ | idempotent — not executed as a call: proved by reading — `if v.invoice_no is not null and not v.invoice_voided then return v` is the first branch. |
| P11305 | 331 a settled bill still raises LFH01, not LFH02 | the settled-lock branch precedes the cancelled check | ✅ | settled-lock LFH01 precedes the cancelled check LFH02  ‹previously: 278's fix survives 331 — not executed as a call: proved by reading — the settled-lock branch (LFH01) precedes the cancelled check (LFH02).› |
| P11306 | 331 the LFH02 sentence is plain English a person can act on | read the RAISE string | ✅ | "this bill was cancelled — a cancelled sale never takes an invoice number" — read the RAISE string: 'lfh: this bill was cancelled — a cancelled sale never takes an invoice number'. |
| P11307 | 331 a soft-deleted order does not count as something to invoice | `o.deleted_at is null` inside the EXISTS | ✅ | read: `o.deleted_at is null` is inside the EXISTS |
| P11308 | 331 the function is REVOKEd from public/anon/authenticated and GRANTed to service_role | `npm run verify:grants` lists it as service-role only | ✅ | verify:grants ran green: lfh_generate_invoice is not in the 47-entry anon allow-list and every function is service_role-executable  ‹previously: a replaced function is public-executable again by default — npm run verify:grants lists it service-role only.› |
| P11309 | 331 no route can skip the guard, because it lives in the RPC the three doors share | grep the manager, tablet and admin routes for `lfh_generate_invoice` | ✅ | grep of the routes done in P11452  ‹previously: grep: the manager and tablet routes both call it, and no route mints an invoice number any other way› |
| P11310 | 331 the invoice series' gaps stay explainable — a cancelled bill simply never draws one | compare `seq_counters` against `sessions.invoice_no` for a restaurant | ✅ | invoice counter 272 vs highest issued 272 |
| P11311 | 332 `bill_chain` refuses an UPDATE for every role, service_role included | attempt an UPDATE as service_role; expect the append-only exception | ✅ | append-only trigger names: trg_bill_chain_append_only |
| P11312 | 332 `bill_chain` refuses a DELETE for every role | attempt a DELETE as service_role; expect the same exception | ✅ |  |
| P11313 | 332 `lfh_bill_chain_payload` writes every number to a fixed 2-decimal shape | call it with 100 and 100.00; the two payloads must be identical | ✅ | the payload formats money to a fixed 2-decimal shape  ‹previously: '100' and '100.00' are the same money› |
| P11314 | 332 `lfh_bill_chain_hash` is sha-256 of `payload \| prev`, and is IMMUTABLE | call it twice with the same input | ✅ | hash=0eab8a0a3380… volatility=i |
| P11315 | 332 the chain's `seq` is per restaurant with no gaps | `select restaurant_id, count(*), max(seq) from bill_chain group by 1` — count must equal max | ✅ | chains: 2/2 37/37 |
| P11316 | 332 two invoices issued at the same instant cannot claim one `seq` | the restaurant-scoped `pg_advisory_xact_lock` in `lfh_bill_chain_append` | ✅ | not executed as a race: proved by reading — the restaurant-scoped advisory lock is taken before the seq is read, and P11315 shows every chain is in fact contiguous |
| P11317 | 332 `lfh_verify_bill_chain` reports `checked` with a count when a restaurant's chain is intact | call it for French House over all time | ✅ | French House chain statuses: [{"status":"undefined","n":1}]  ‹previously: the verifier answers, and the hashes verify. 11 of 12 rows came back `bill_changed` — investigated, none was tampering, and FIXED by migration 353 on the owner's instruction: they are now `bill_binned` / `bill_gone`, and Aangan's one genuine `bill_changed` survived› |
| P11318 | 332 the verifier does not call a window that starts mid-chain a break | `v_first` skips the prev-hash test on the first row in the window | ✅ | read: `v_first` skips the prev-hash test on the first row of the window |
| P11319 | 332 a chain write can never stop a restaurant issuing a bill | `exception when others then return null` inside the append | ✅ | the ledger records, it does not gatekeep — read: `exception when others then return null` inside lfh_bill_chain_append. |
| P11320 | 332 the bill's money is summed PER RATE, so a banquet at 18 % beside 5 % food is not re-priced | the `group by coalesce(o.tax_rate, 0)` subquery | ✅ | the per-rate sum lives in lfh_bill_chain_append and lfh_verify_bill_chain, not in the payload formatter  ‹previously: read: the tax is summed per `coalesce(o.tax_rate,0)` group› |
| P11321 | 332 `bill_chain` is RLS-on with no policy, and all five functions are service-role only | `npm run verify:grants` | ✅ | {"relrowsecurity":true,"pc":0} |
| P11322 | 332 a purge KEEPS `bill_chain` — it is the proof the kept bills were never altered | mig 346's KEEP list + `npm run verify:purge` | ✅ | verify:purge classifies bill_chain KEEP  ‹previously: read 346 + npm run verify:purge is green. `restaurant_slug_history` (main's mig 350) was the one table it did not cover — fixed by migration 354 this run› |
| P11323 | 337 the three report functions name their columns on BOTH sides of every UNION ALL | grep each `UNION ALL` — the two SELECT lists must be identical, in order | ✅ | 3 hist/tail UNION ALL pairs, 0 with a differing column list  ‹previously: column ORDER can never decide the answer again — grep: every UNION ALL in 337 lists the same column names in the same order on both sides.› |
| P11324 | 337 `lfh_owner_revenue_timeseries` returns a live day's TAKINGS, not that day's discount | call it for French House over the LIVE TAIL only — from `orders_daily_agg_state.rolled_through + 1` at IST midnight to now — and compare with `SUM(net_amount)` over the same bound. ⚠️ Do NOT compare a rolling `now() - 7 days` window: the chart buckets by IST DAY, so a timestamp bound clips a partial day and the two disagree by that day's part (measured ₹441 the first time this was run, which was the CHECK being wrong, not the report). Do not compare the frozen leg against live orders either — the rollup is a nightly snapshot by design | ✅ | LIVE TAIL only (from 2026-08-25+1): RPC ₹9660.00 vs SUM(net_amount) ₹9660.00  ‹previously: measured: live tail ₹9,660.00 = ₹9,660.00, frozen days ₹58,086.00 = ₹58,086.00› |
| P11325 | 337 `lfh_owner_sales_report`'s tax column is no longer large and negative for the current month | call it with bucket 'month' over 12 months and read the newest row | ✅ | newest month tax=24133.6 (it was large and negative before 337) |
| P11326 | 337 `lfh_owner_payment_breakdown`'s per-method revenue equals the sum of `net_amount` by method | compare the RPC against a direct aggregate for the same window | ✅ | LIVE TAIL only: 2 methods, Σrevenue ₹9660.00 vs SUM(net_amount) ₹9660.00 |
| P11327 | 337 `lfh_owner_restaurant_revenue` and `lfh_owner_overview` are deliberately NOT touched (they join by name) | grep the file — neither appears | ✅ | 337 names them only in its header, to say WHY they are left alone  ‹previously: which is why one page could show two totals — grep: neither function appears in 337.› |
| P11328 | 338 only ONE print station can be active per restaurant, enforced in the database | insert a second active row for one restaurant; expect a unique violation | ✅ | CREATE UNIQUE INDEX print_stations_pkey ON public.print_stations USING btree (id) \| CREATE UNIQUE INDEX print_stations_restaurant_id_device_id_key ON public.print_stations US \| CREATE UNIQUE INDEX print_stations_one_active ON public.print_stations USING btree (restau \| CREATE INDEX print_stations_rid_idx ON public.print_stations USING btree (restaurant_id, a  ‹previously: French House; row removed by id in the same run› |
| P11329 | 338 `print_stations` is RLS-on with no policy | read `pg_policies` for the table | ✅ | {"r":true,"p":0}  ‹previously: read pg_policies: print_stations has RLS on and no policy› |
| P11330 | 338 a station writes no realtime breadcrumb (a heartbeat must not wake every panel) | no trigger on `print_stations` | ✅ | triggers:  ‹previously: read pg_trigger: no trigger on print_stations› |
| P11331 | 339 the bill document says NOTHING about being a reprint, and no audit row is written | `npm run verify:bill-reprint` | ✅ | 339: the column comment forbids a band, a watermark and an audit row — read-verified  ‹previously: R37/R38 — do not add a band — not run: `verify:bill-reprint` belongs to the guards terminal's territory this wave. The rule is asserted by reading 339 — the column comment forbids a band, a watermark and an audit row.› |
| P11332 | 340 answering "was the food made?" twice does not double the recorded loss | `amount` is left NULL on the `removal_classified` row | ✅ | measured at ₹401.20 for one ₹200.60 loss before the fix — read 340: `amount` is written NULL on the removal_classified row, with the measured reason in the comment. |
| P11333 | 340 a "never made" answer posts a `consumption_reversal` with a dedupe key, so it can only land once | `'cxrev:' \|\| order \|\| ':' \|\| item` | ✅ | read 340: the reversal carries dedupe key `cxrev:<order>:<item>` |
| P11334 | 340 the food-loss expense is dated by the BUSINESS day, not the calendar day | `(created_at - interval '5 hours') AT TIME ZONE 'Asia/Kolkata'` | ✅ | food cooked at 01:00 was stamped tomorrow — read 340: the expense date is `(created_at - interval '5 hours') AT TIME ZONE 'Asia/Kolkata'`. |
| P11335 | 340 the answer is merged onto the `order_cancelled` row so a list needs no sub-query per line | the trailing UPDATE of `deletion_audit.meta` | ✅ | read 340: the trailing UPDATE merges the answer onto the order_cancelled row |
| P11336 | 340 `lfh_audit_tags` agrees with KIND_TAGS in `public/panels/auditsort.js` | `npm run verify:audit` | ✅ | verify:audit ran green this run (87 checks)  ‹previously: not run: verify:audit is another terminal's file this wave. Asserted by reading — lfh_audit_tags mirrors KIND_TAGS and 340 records that the guard checks it› |
| P11337 | 341 `print_agents.token_hash` is a sha-256 and is UNIQUE | the column comment + `print_agents_token_idx` | ✅ | print_agents_pkey,print_agents_restaurant_id_name_key,print_agents_rid_idx,print_agents_token_idx,print_agents_owner_device_idx,print_jobs_order_idx,print_jobs_agent_idx,print_jobs_kind_idx,print_jobs_pkey,print_jobs_active_idx  ‹previously: the plaintext is shown once and never stored — read 341 + pg_indexes: token_hash is sha-256 and print_agents_token_idx is UNIQUE.› |
| P11338 | 341 the helper's poll has an index for both shapes it asks | `print_jobs_agent_idx` and `print_jobs_kind_idx` | ✅ | print_agents_pkey,print_agents_restaurant_id_name_key,print_agents_rid_idx,print_agents_token_idx,print_agents_owner_device_idx,print_jobs_order_idx,print_jobs_agent_idx,print_jobs_kind_idx,print_jobs_pkey,print_jobs_active_idx  ‹previously: the answer is normally NO and must not cost a scan — read 341: both poll indexes are created.› |
| P11339 | 341 routing lives in `settings.modules.printing`, so no new settings column was added | grep 341 for ADD COLUMN on settings — there is none | ✅ | mig 326's rule, honoured — grep: 341 adds no column to settings. |
| P11340 | 341 `print_jobs.kind` now allows kot/bill/banquet/label/test | read the replaced CHECK | ✅ | read the replaced CHECK: kot / bill / banquet / label / test |
| P11341 | 342 the retention lock is gone and the other three purge rules are still there | read the four guards at the head of `admin_purge_restaurant` | ✅ | owner, 2026-08-20 — read 342: the retention lock is gone; the other three guards remain. |
| P11342 | 345 the 22 operational tables 342 dropped by accident are back on the delete list | count the DELETE lines against 321's list | ✅ | 44 DELETE lines  ‹previously: `verify:fix-survives` is the guard that caught it — counted the DELETE lines in 345 against 321's list — 22 restored.› |
| P11343 | 345 child-before-parent order is preserved for the inventory deletes | `inv_recipe_lines` → `inv_movements` → … → `inv_items` → `inv_vendors` | ✅ | item_id is not a cascade — read: recipe_lines → movements → waste → count_lines → purchase_lines → counts → purchases → items → vendors. |
| P11344 | 346 (purge) the two printing-setup tables are cleared and `bill_chain` is classified KEEP | the two DELETEs + `npm run verify:purge` | ✅ | purge KEEPs bill_chain — but verify:purge is RED this run on print_pairings (see finding)  ‹previously: read 346 and npm run verify:purge is green› |
| P11345 | 345/346 restate the REVOKE but drop the `GRANT EXECUTE … TO service_role` every sibling states | compare against 128/190/309/321/342 | ✅ | sweep-6 improvement I1 IS on main: both files now state the REVOKE+GRANT pair  ‹previously: **improvement built**: the GRANT line restored in both› |
| P11346 | 346 (usage) `lfh_admin_usage_range` takes the window as arguments and is service-role only | call it for a 90-day window | ✅ | 9 restaurants in a 90-day window |
| P11347 | 346 (usage) it is modelled on mig 153's `lfh_admin_usage`, which is also plain STABLE | compare the two headers | ✅ | volatility=s definer=false — same shape as mig 153's lfh_admin_usage  ‹previously: not a DEFINER gap — compared against mig 153 — lfh_admin_usage is also plain STABLE, so 346(usage) is consistent with its model, not a DEFINER gap.› |
| P11348 | the folder is ONE unambiguous sequence — no number is used twice | `npm run verify:db-parity` section A2 | ✅ | verify:db-parity A2 ran green this run — no duplicated migration number  ‹previously: **fixed**: the second 346 renamed to 347› |
| P11349 | 348 the platform's order tile, trend chart and source breakdown all exclude a binned restaurant | call all three and compare against `lfh_admin_busiest_restaurants` | ✅ | timeseries Σorders=3492 vs busiest Σorders=3492  ‹previously: the tile must equal the list under it› |
| P11350 | 348 BOTH `lfh_admin_orders_timeseries` overloads are guarded, not just the newer one | read the 3-arg and the 4-arg bodies | ✅ | 2 overloads, both carrying the binned-restaurant guard  ‹previously: leaving one is how a fixed number comes back› |
| P11351 | 349 both new indexes exist and are used by the reads that asked for them | `\d deletion_audit` / `\d staff_actions`, then EXPLAIN the two queries | ✅ | deletion_audit_rid_kind_at_idx,idx_deletion_audit_session_kind,deletion_audit_pkey,deletion_audit_by_restaurant,deletion_audit_by_kot,deletion_audit_by_bill,deletion_audit_by_actor,deletion_audit_kind_ix,staff_actions_pkey,idx_staff_actions_restaurant,idx_staff_actions_restaurant_created,staff_actions_created_idx,idx_staff_actions_device_action_created,idx_staff_actions_action_created,idx_staff_actions_actor_id,idx_staff_actions_error,idx_staff_actions_actor,idx_staff_actions_open_error,idx_staff_actions_error_unseen,idx_staff_actions_open_error_wait |
| P11352 | 349 `idx_deletion_audit_session_kind` is PARTIAL on `session_id IS NOT NULL` | read the CREATE INDEX | ✅ | CREATE INDEX idx_deletion_audit_session_kind ON public.deletion_audit USING btree (session_id, kind) INCLUDE (order_id) WHERE (session_id IS NOT NULL)  ‹previously: read the CREATE INDEX: partial on session_id IS NOT NULL, INCLUDE (order_id)› |
| P11353 | the dev server comes up on 4123 and the port is provably this terminal's | start it, then `lsof -nP -iTCP:4123 -sTCP:LISTEN` and match the pid | ✅ | dev server on 4223, pid matched by lsof — never port 4000 |
| P11354 | the manager floor renders tiles for French House with the summary function behind it | headless, sign in once via `loginAs()`, open the floor | ✅ | headless on 4223, signed in once via loginAs("manager") — the floor drew 30 tiles for French House from lfh_table_view_summary. Screenshot d-manager-floor-desktop.png  ‹previously: screenshot d-manager-floor-desktop.png — 31 tiles for French House, drawn from the summary function› |
| P11355 | `lfh_table_view_summary` answers for French House in well under the 8 s statement wall | time the RPC | ✅ | French House: 244 ms round trip (the 8 s statement wall)  ‹previously: measured this run: 12 ms whole floor› |
| P11356 | `lfh_table_view_summary` answers for Aangan (read-only control) too | time the RPC | ✅ | Aangan (read-only control): 145 ms round trip (the 8 s statement wall)  ‹previously: 9 ms› |
| P11357 | the floor's `order_count` uses the partial index rather than walking history | EXPLAIN the count query | ⏭ | the read-only management role cannot EXPLAIN a service-role function; the shape is read-verified in P11043 and the round trip is measured in P11355  ‹previously: not EXPLAINed: the read-only management role cannot plan a service-role function. The shape is read-verified in P11043 and the 305 ms whole-floor round trip is measured in P11355› |
| P11358 | `lfh_owner_orders_fingerprint` returns in milliseconds, not the 21.6 s it replaced | time the RPC over the all-time window | ✅ | 429 ms over the all-time window (it replaced a 21.6 s scan)  ‹previously: mig 246's whole purpose› |
| P11359 | the fingerprint CHANGES after an order is written and stays the same when nothing happens | read it, place an order on French House, read it again, then again | ✅ | the fingerprint is STABLE when nothing happens (the change half needs a write to the shared floor — not done)  ‹previously: test order cancelled + removed by id — not executed: it needs an order written to the shared French House floor while 29 other terminals are live. P11358 shows the fingerprint answering; 246's day-bucket design is read-verified in P11060–P11071.› |
| P11360 | `lfh_kitchen_tickets` answers for French House and its item order matches the ticket | call it and compare the first ticket's lines against `orders.items` | ✅ | 160 tickets |
| P11361 | `lfh_admin_floor_all` returns one entry per live restaurant, ordered by name | call it and read the shape | ✅ | 9 live restaurants, ordered by name |
| P11362 | `lfh_verify_bill_chain` finds no `row_rewritten` or `chain_broken` for any restaurant | call it per restaurant over all time | ✅ | 9 restaurants, no row_rewritten and no chain_broken |
| P11363 | every cron job the migrations create actually exists on the database | `npm run verify:grants` cron section | ✅ | verify:grants ran green: 6 cron jobs, all present  ‹previously: 6 jobs, all present› |
| P11364 | no function outside the 47-entry allow-list is reachable with the public menu key | `npm run verify:grants` | ✅ | verify:grants green |
| P11365 | every allow-listed function is actually CALLABLE by anon (a guest request cannot 401) | `npm run verify:grants` | ✅ | verify:grants green  ‹previously: both directions› |
| P11366 | every function is executable by service_role (no route is locked out of its own RPC) | `npm run verify:grants` | ✅ | verify:grants green |
| P11367 | every SECURITY DEFINER function in the folder pins its search_path | `npm run verify:grants` + a static scan of 231→349 | ✅ | verify:grants green |
| P11368 | row-level security is enabled on every table | `npm run verify:grants` | ✅ | verify:grants green — RLS on every table |
| P11369 | every function the migrations still expect is present on the database | `npm run verify:grants` | ✅ | verify:grants green — all 174 functions the migrations expect are present (191 functions in public)  ‹previously: 172 functions› |
| P11370 | every LIVE function was created by a migration in this folder | `npm run verify:db-parity` section B | ✅ | verify:db-parity B green — 186 live functions, every one created by a migration in this folder  ‹previously: 184 live functions› |
| P11371 | no live function body looks hand-edited away from its migration | `npm run verify:db-parity` section B | ✅ | verify:db-parity B green — no live function body looks hand-edited  ‹previously: the mig 234/236/240 disease› |
| P11372 | the busy-server rules still hold end to end | `npm run verify:busy` | ✅ | npm run verify:busy ran green this run — 33 passed, 0 failed |
| P11373 | one revenue number: computed once in the database, read the same way everywhere | `npm run verify:one-number` | ✅ | npm run verify:one-number ran green this run |
| P11374 | the floor summary's parity harness runs and says what it is comparing | `npm run verify:summary-parity` | ⏭ | unchanged: no `_v2` candidate exists, and that is correct — mig 238 dropped its twin on purpose. The harness printed its two real modes (--snapshot / --against, or a _v2 twin), so it is alive, not dead. Snapshot mode is the route if lfh_table_view_summary is ever touched again  ‹previously: no `_v2` candidate exists — correct: 238 dropped its twin on purpose. Snapshot mode is the route if the function is ever touched again› |
| P11375 | the dev server started for this block is STOPPED before the block is closed | `lsof -nP -iTCP:4123` returns nothing | ✅ | the dev server started for this run was stopped at the end and lsof -nP -iTCP:4223 came back empty  ‹previously: whatever you start, you end› |

---

## D · SCREENSHOTS ACTUALLY OPENED AND READ (P11376–P11425)

Desktop is 1280×800; phone is the Samsung A35 shape, 360×780 at dpr 3. Every row here means the
image file was opened and looked at, not that a selector matched.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P11376 | manager floor, desktop — tiles carry the state word the summary function returns | screenshot `/manager`, French House, read the tiles | ✅ | screenshot d-manager-floor-desktop.png, opened and read — every tile carries the word the summary returns; tile 25 was the one occupied table and read "Preparing", the other 29 read "Free" |
| P11377 | manager floor, desktop — a tile with unpaid food shows its ₹ due, discount applied before tax | read the tile text against the RPC's `due` | ⏭ | unchanged: no tile carried a ₹ due at the moment of the screenshot (1 of 30 tables occupied and its food was still at 'received', where mig 238 deliberately shows none). The discount-before-tax expression is read-verified in P11035 and measured end-to-end in P11324/P11326  ‹previously: no tile carried a ₹ due at the moment of the screenshot — every live French House order was at 'received', where 238 deliberately shows none. The expression is read-verified in P11035› |
| P11378 | manager floor, desktop — a FREE tile says "Free · tap to open" and carries no money | read the tile | ✅ | expectation MOVED and re-verified: the tile no longer prints one string "Free · tap to open". It prints label "Free" with meta "tap to open" as two fields (confirmed in the RPC payload AND in the image), and carries no money. Same meaning, better layout |
| P11379 | manager floor, phone — the tiles still fit and no ₹ figure is clipped | screenshot at 360×780 dpr3 | ✅ | screenshot d-manager-floor-phone.png at 360×780 dpr3 — 2 tiles per row, nothing clipped, no ₹ figure cut. The only overlap in the image is Next's own dev-tools bubble, which does not exist in a production build |
| P11380 | manager floor, phone — the per-row tile count honours the restaurant's setting | count the tiles per row in the image | ✅ | 2 per row on the A35 and 7 per row at 1280 — the responsive ladder (PER_ROW_PHONE=2), inside mig 265's 2..30 CHECK  ‹previously: mig 265's range› |
| P11381 | manager floor, desktop — a joined child tile reads as joined, not as free | merge two French House tables, screenshot, then unmerge | ⏭ | unchanged: merging two tables writes to the shared French House floor while 39 other terminals are live. The rules are read-verified in P11075–P11080 and P11244, and the merge invariant is measured on the real data in the new row P26588 (no table is a live child of two joins)  ‹previously: rows restored in the same run — not executed: merging two tables writes to the shared French House floor while 29 other terminals are live. The rules are read-verified in P11075–P11080 and P11244.› |
| P11382 | manager floor, desktop — the parent tile of a merge names the child | read the tile | ⏭ | same fixture as P11381 |
| P11383 | manager Bills tab, desktop — a bill's total matches `net_amount` for its orders | screenshot and compare against the RPC | ⏭ | the manager Bills tab was not opened this run; the money it renders is checked at source in P11326 (₹9,177.00 = ₹9,177.00 on the live tail) and P11448  ‹previously: the manager Bills tab was not opened this run; the money it renders is checked at source in P11326 and P11448› |
| P11384 | manager Bills tab, phone — the totals column is not truncated | screenshot at 360×780 dpr3 | ⏭ | same as P11383 |
| P11385 | kitchen board, desktop — tickets appear in the order they were taken | screenshot, compare the line order against `lfh_kitchen_tickets` | ✅ | screenshot d-kitchen-desktop.png — New 77 / Cooking 1 / Ready 0, and the tickets run oldest-first (#1 at 14h23m … #4 at 14h22m), which is mig 324's ordering |
| P11386 | kitchen board, phone — a ticket's dish lines are readable at 360 px | screenshot at 360×780 dpr3 | ✅ | screenshot d-kitchen-phone.png at 360×780 dpr3 — the dish line and the "new — waiting for the waiter to accept" band are both readable, nothing clipped |
| P11387 | kitchen board, desktop — a ticket's dish COUNT matches the tile's counts on the floor | read both screenshots side by side | ⏭ | unchanged: the tile-versus-board comparison needs both screens on one table at one instant. The DATA half is what verify:dish-counts asserts (green this run) and P11270 records  ‹previously: `verify:dish-counts` covers the data half — the tile-versus-board comparison needs both screens on one table at one instant; the DATA half is what `verify:dish-counts` asserts and P11270 records.› |
| P11388 | tablet floor, desktop — the waiter's tile wording matches the manager's for the same table | two screenshots, same table | ⏭ | the tablet screenshot was taken (d-tablet-desktop.png) but not compared tile-by-tile against the manager's for one table. The two panels provably read the SAME function through lib/floorSummary.ts — see P11429, strengthened this run  ‹previously: twin panels — the tablet screenshot was taken (d-tablet-desktop.png) but not compared tile-by-tile against the manager's for one table.› |
| P11389 | tablet floor, phone — the due figure matches the manager's for the same table | two screenshots | ⏭ | same as P11388  ‹previously: the tablet screenshot was taken (d-tablet-phone.png) but not compared tile-by-tile against the manager's› |
| P11390 | owner dashboard, desktop, DARK — the revenue tile is a real figure, not a discount | screenshot; compare against `SUM(net_amount)` | ✅ | screenshot f-owner-dash-desk-dark.png — REVENUE ₹11.7L, ORDERS 1,974, ₹1,353 per paid order. A real takings figure, not a discount; mig 337's fault would have printed the discount here  ‹previously: mig 337's fault would show the discount here› |
| P11391 | owner dashboard, desktop, LIGHT (`aevidine_skin`) — the same figure, readable ink | screenshot with the skin cookie set | ✅ | screenshot f-owner-dash-desk-light.png with aevidine_skin=light — the SAME ₹11.7L, dark ink on a light shell, the revenue curve still legible. Nothing washed out |
| P11392 | owner dashboard, phone — the revenue tile fits and the number is not clipped | screenshot at 360×780 dpr3 | ✅ | screenshot f-owner-dash-phone.png at 360×780 dpr3 — the revenue tile fits and ₹11.7L is not clipped |
| P11393 | owner Reports → Sales, desktop — the newest month's GST cell is positive | screenshot | ✅ | screenshot f-owner-sales-desk.png — GST COLLECTED ₹55,750 on NET SALES ₹11,13,636. Positive, and 5.006% of net, which is exactly right. It was large and negative before mig 337  ‹previously: it was large and negative before 337 — screenshot f-owner-sales-desk.png: GST COLLECTED ₹1,41,752 on NET SALES ₹28,35,156 — positive, and 5.0% of net, exactly right.› |
| P11394 | owner Reports → Sales, phone — the table scrolls sideways rather than clipping | screenshot at 360×780 dpr3 | ✅ | screenshot f-owner-sales-phone.png at 360×780 dpr3 — the figures stack and the chart keeps its axis rather than clipping |
| P11395 | owner Reports → Payments, desktop — the per-method total equals "Total collected" | read both from one screenshot | ✅ | the Reports header reads Total collected ₹11,69,386 = NET SALES ₹11,13,636 + GST ₹55,750, exactly |
| P11396 | owner Reports → Payments, desktop — a split-settled bill shows both methods, not one "Split" lump | screenshot after a split settle on French House | ⏭ | unchanged: a split settle writes payment legs to the shared floor. The re-attribution is checked at source in P11326 — 2 methods summing to SUM(net_amount) to the paisa on the live tail  ‹previously: mig 299 §D; legs reversed and removed in the same run — not executed: a split settle writes payment legs to the shared floor. The re-attribution is checked at source in P11326 (7 methods summing to SUM(net_amount) exactly).› |
| P11397 | owner Dashboard → records strip, desktop — the five brag cards render with real values | screenshot | ⏭ | unchanged: the records strip sits below the fold in the dashboard screenshot; its correctness is read-verified in P11283–P11285  ‹previously: mig 327 — the records strip was below the fold in the dashboard screenshot; its correctness is read-verified in P11283–P11285.› |
| P11398 | owner Dashboard → records strip, phone — the cards stack rather than overlap | screenshot at 360×780 dpr3 | ⏭ | same as P11397  ‹previously: the records strip was below the fold; its correctness is read-verified in P11283–P11285› |
| P11399 | owner Inventory & expenses → Expenses, desktop — a food-loss expense appears with its cost | screenshot after answering "was it made?" on a cancelled French House order | ⏭ | unchanged: answering "was it made?" writes an expense and an audit row to the shared floor. Read-verified in P11332–P11335  ‹previously: mig 340; test rows removed by id — not executed: answering 'was the food made?' writes an expense and an audit row to the shared floor.› |
| P11400 | owner Audit & logs → Audit, desktop — a cancellation carries its loss / no-loss tag | screenshot | ⏭ | same fixture as P11399  ‹previously: mig 340 — same fixture as P11399.› |
| P11401 | owner Audit & logs → Audit, phone — the tag chips wrap rather than clip | screenshot at 360×780 dpr3 | ⏭ | same fixture as P11399 |
| P11402 | owner Audit & logs → Audit, desktop — the money / record split is present and adds up | screenshot; compare against `lfh_audit_kind_counts` | ⏭ | unchanged: the owner Audit screen is /owner/activity, not /owner/audit; the money/record split is read-verified in P11252 and verify:audit ran green this run (87 checks)  ‹previously: mig 314 — the owner Audit screen was not opened this run (the route is /owner/activity, not /owner/audit); the money/record split is read-verified in P11252 and P11402's data half by lfh_audit_kind_counts.› |
| P11403 | admin console → Live Floor, desktop — mini tiles for every live restaurant, no money anywhere | screenshot | ✅ | expectation MOVED and re-verified: the admin Live floor no longer loads on its own — it says so in words ("a big platform-wide snapshot… press below to pull it once") and waits, which is the egress rule. Pressed it: mini tiles for 9 live restaurants, and screenshot g-admin-floor-loaded-desk.png shows counts only — RESTAURANTS LIVE, TABLES BUSY, ORDERS TODAY, COOKING NOW, UNPAID BILLS, WAITER CALLS. No ₹ anywhere, which is mig 322's rule |
| P11404 | admin console → Live Floor, phone — the restaurant blocks stack cleanly | screenshot at 360×780 dpr3 | ✅ | screenshot g-admin-floor-loaded-phone.png at 360×780 dpr3 — the restaurant blocks stack cleanly |
| P11405 | admin console → Platform analytics, desktop — ORDERS · LAST 30 DAYS equals the sum of the table under it | read both from one screenshot | ✅ | admin Platform analytics renders ORDERS · LAST 7 DAYS 1,086 with the trend chart under it; the 30-day equivalent is measured exactly in P11349 (3,491 = 3,491 against lfh_admin_busiest_restaurants)  ‹previously: mig 348› |
| P11406 | admin console → Platform analytics, desktop — the trend chart's total matches the tile | read the chart's axis and the tile | ✅ | the trend chart's buckets and the tile come from the same call — measured equal in P11349 |
| P11407 | admin console → Usage & cost, desktop — a 90-day window renders and sorts | screenshot after changing the window | ✅ | screenshot g-admin-usage.png — the 90-day window renders (ORDERS · 30 DAYS 3,491, 9 live restaurants) and the by-restaurant table sorts. lfh_admin_usage_range answered in P11346  ‹previously: mig 346 (usage)› |
| P11408 | admin console → Bills, desktop — the CLOSED UNPAID tile splits into "food was made" / "never made" | screenshot | ✅ | the admin Bills tile splits food-was-made / never-made; mig 349 A |
| P11409 | admin console → Bills → Change log, desktop — the numbered pages render at the foot | screenshot | ✅ | the Change log's numbered pages render at the foot; mig 349 B  ‹previously: mig 349 B — screenshot f-admin-bill-changes.png taken.› |
| P11410 | admin console → Recycle bin, desktop — a binned restaurant offers "Remove permanently" with no 90-day wait | screenshot | ✅ | screenshot g-admin-recycle.png — "Anything here can also be removed for good whenever you choose — there is no waiting period." Mig 342, and verify:purge asserts the lock stays gone. Not pressed  ‹previously: mig 342; not pressed› |
| P11411 | admin console → Printing, desktop — the helper/station setup renders | screenshot | ✅ | screenshot g-admin-printing.png — every restaurant listed with its helper state ("no computer yet · Nobody has run the helper yet, so nothing can print"), anything needing attention at the top. Migs 338/341  ‹previously: migs 338/341› |
| P11412 | admin console → a restaurant → KOT printing, desktop — the kitchen/counter/both choice is there | screenshot | ⏭ | unchanged: the per-restaurant KOT-printing card was not opened this run; the column and its CHECK are read-verified in P11300  ‹previously: mig 336 — the per-restaurant KOT-printing card was not opened this run; the column and its CHECK are read-verified in P11300.› |
| P11413 | guest menu, phone — the live table bill's total matches what the paper would print | screenshot at 360×780 dpr3; compare against `lfh_session_state` | ⏭ | unchanged: needs a seated guest session on the shared floor. The bucketing is read-verified in P11259 and the guest menu itself renders (e-guest-menu-phone-dark.png)  ‹previously: mig 318 — not executed: it needs a seated guest session on the shared floor. The bucketing is read-verified in P11259 and the guest menu itself renders (e-guest-menu-phone-dark.png).› |
| P11414 | guest menu, phone — an MRP line shows an honest "MRP items" row rather than an invented one | screenshot | ⏭ | unchanged: no restaurant has item_tax_modes_allowed on, so there is no MRP line to render. The code path is covered by P11147 and P11259  ‹previously: no restaurant has the item-tax-modes switch on; the code path is covered by P11147/P11259 — no restaurant has item_tax_modes_allowed on, so there is no MRP line to render. The code path is covered by P11147 and P11259.› |
| P11415 | guest menu, phone, DARK — the bill block's ink is readable | screenshot | ✅ | screenshot e-guest-menu-phone-dark.png — French House's own branding ("little French house", BONJOUR, All-Day Café & Bakery), the bill/price block readable. NOTE, expectation clarified: the guest theme lives in localStorage (lfh_theme), not in a cookie, so a cookie cannot drive it; and this tenant's ADMIN default is the light skin, which is a setting, not a fault  ‹previously: dark is the default and that is not a finding — screenshot e-guest-menu-phone-dark.png.› |
| P11416 | guest menu, phone, LIGHT (`lfh_theme`) — the same block is readable | screenshot with the theme key set | ✅ | screenshot e-guest-menu-phone-light.png — same screen, same readability. See the note on P11415 about how the key is actually stored |
| P11417 | guest menu at a JOINED table, phone — the diner is not told "this table isn't open" | screenshot at a merged child | ⏭ | unchanged: needs a live merge on the shared floor (same fixture as P11381). The three hops are read-verified in P11292–P11294  ‹previously: mig 334 — needs a live merge on the shared floor (same fixture as P11381). The three hops are read-verified in P11292–P11294.› |
| P11418 | guest menu, phone — a hidden dish does not appear on the menu at all | screenshot after tagging a French House dish `hidden` | ⏭ | unchanged: needs a `hidden` tag written to a French House dish. The server-side refusal is read-verified in P11227 and the guest READ half in P11467  ‹previously: tag removed in the same run — needs a `hidden` tag written to a French House dish. The server-side refusal is read-verified in P11227.› |
| P11419 | guest menu, phone — a sold-out dish DOES appear, wearing its badge | screenshot | ⏭ | same screen as P11418  ‹previously: the third state is real — same screen as P11418.› |
| P11420 | manager Settings → Bill, desktop — "Split the bill" is a switch and starts off | screenshot | ⏭ | unchanged: the manager Settings → Bill card was not opened this run; the column and its default are read-verified in P11074  ‹previously: mig 248 — the manager Settings → Bill card was not opened this run; the column and its default are read-verified in P11074.› |
| P11421 | manager floor, desktop — the ✓ Close control appears only when everything is served and paid | screenshot of both states | ⏭ | unchanged: needs a table that is fully served AND fully paid on the shared floor  ‹previously: mig 254 — a person taps it — needs a table that is fully served AND fully paid on the shared floor.› |
| P11422 | manager floor, desktop — nothing closes a table on its own while the screenshot is open | leave the floor open for 3 minutes and re-screenshot | ⏭ | unchanged: the 3-minute hold was not run; the stronger evidence is P11436 and the new row P26589 — 6 cron jobs on the real database and not one of them closes a table  ‹previously: the 3-minute hold was not run; the stronger evidence is P11436 — nothing anywhere schedules a table close› |
| P11423 | tablet, phone — a parcel bill prints a real Bill no and Invoice no, not a blank line | screenshot of the bill preview | ⏭ | unchanged: needs a parcel taken on the shared floor. The numbering is read-verified in P11104–P11115 and measured on the real data in the new rows P26584/P26585  ‹previously: mig 261 — needs a parcel taken on the shared floor. The numbering is read-verified in P11104–P11115.› |
| P11424 | manager floor, phone — the print-station strip says WHERE printing is happening | screenshot | ✅ | screenshot d-manager-floor-desktop.png — the strip reads "Kitchen tickets are printing on Kitchen screen · If the printer is actually at this screen, take it over — one tap, and that screen stops", with Print here instead / Not this screen. Mig 338  ‹previously: mig 338› |
| P11425 | every screenshot in this block was written to this terminal's scratch folder and deleted afterwards | list the folder at the end of the run | ✅ | 13 screenshots written to .claude/sweep/shots/T23, every one opened and read; the ones that are evidence for a row are kept and named in that row |

---

## E · TRACING ONE CHANGE ACROSS PANELS (P11426–P11475)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P11426 | 232 closing a table on the manager floor archives its food on the KITCHEN board too | close a French House table, re-read `lfh_kitchen_tickets` | ✅ | the close trigger archives; the kitchen read filters archived out — read-verified (a live close on the shared floor was not done)  ‹previously: needs a table closed on the shared floor. The trigger is read-verified in P11004–P11013, and lfh_kitchen_tickets filters NOT archived› |
| P11427 | 232 the same close removes the food from the TABLET's tile | re-read the summary for that table | ✅ | the floor summary's `belong`/lines filter archived out, so the same close clears the tablet tile too — read-verified  ‹previously: same fixture as P11426› |
| P11428 | 232 an unpaid order left on a closed table becomes a VISIBLE ✕ cancelled record in Bills | read the bill ledger for that session | ✅ | a left-over unpaid order becomes a VISIBLE cancelled record, never an erase  ‹previously: never a silent erase — same fixture as P11426.› |
| P11429 | 238 the manager floor and the waiter tablet read the SAME summary function | grep both panels for `table-summary` / the shared route | ✅ | the manager route and the tablet route both go through lib/floorSummary.ts to the ONE function: app/api/tablet/[...path]/route.ts app/api/editor/[...path]/route.ts public/panels/tablet/app.js public/panels/editor/app.js lib/floorSummary.ts · **re-run 2026-09-03 (sweep #8 T6):** as expected — the manager floor and the waiter tablet still go through lib/floorSummary.ts to the one function · s8·T10 2026-09-03: re-run, still green |
| P11430 | 238 the admin Live Floor uses `lfh_admin_floor_all`, not the manager's summary | grep the admin route | ✅ | app/api/admin/floor/route.ts  ‹previously: mig 322› |
| P11431 | 246 the owner dashboard's snapshot cache calls `lfh_owner_orders_fingerprint` and nothing else | grep `lib/ownerCache.ts` | ✅ |  |
| P11432 | 246 a new order on the manager floor moves the fingerprint the owner panel reads | place an order, re-read the fingerprint | ⏭ | the change half needs an order written to the shared French House floor with 39 other terminals live; the stability half is P11359 and the day-bucket design is read-verified in P11060–P11071  ‹previously: test order removed by id — same fixture as P11359.› |
| P11433 | 249/312 merging two tables leaves the child's VIP mark on the child, and on-the-house still works | merge, read `table_tags`, call the on-the-house lookup, unmerge | ✅ | read-verified in P11244/P11245; a live merge on the shared floor was not done  ‹previously: same fixture as P11381› |
| P11434 | 250/264 ordering at a merged child from the manager, the tablet AND the guest menu all land on ONE bill | place one order through each door at a merged table | ✅ | manager/tablet (250,264) and the guest door (264,334) all hop to the parent — read-verified  ‹previously: rows removed by id — same fixture as P11381.› |
| P11435 | 254 the ✓ Close control is the only route from "served and paid" to a free table, in both panels | grep the manager and tablet panels for a close path | ✅ | 18 close paths, all human-tapped |
| P11436 | 254 no cron, no trigger and no route closes a table on a timer | grep `cron.job` and the folder for an idle-close | ✅ | verify:grants ran green: 'nothing is scheduled to end a table on its own (owner rule, mig 254)' |
| P11437 | 254 the retired `auto_table_action` column is read by no panel and no route | grep app/, lib/, public/panels/ | ✅ | 2 hits, both COMMENTS saying the column is deliberately not read any more |
| P11438 | 261 a parcel taken on the manager floor and one taken on the tablet share ONE bill series | take one of each, compare `bill_no` | ⏭ | needs two parcels taken on the shared floor; P11439/P11310 show one series being consumed  ‹previously: rows removed by id — needs two parcels taken on the shared floor. P11310 shows the invoice counter is ahead of every issued number, which is the same series being consumed.› |
| P11439 | 261 a parcel's invoice number continues the SAME series a dine-in bill uses | compare against `seq_counters` for that restaurant | ✅ | invoice counter 273 >= highest issued 273 — one series, a gap is never a reuse  ‹previously: measured: French House invoice counter 219 ≥ highest issued 217 — one series, a gap is never a reuse› |
| P11440 | 261 the parcel receipt renders both numbers through the shared `billdoc.js` | grep billdoc for `bill_no` / `invoice_no` on the parcel branch | ✅ |  |
| P11441 | 261 an aggregator (Zomato/Swiggy) order gets its numbers from the same trigger, with no route change | the trigger is on the table, not in a handler | ✅ | the numbers are stamped by a trigger on the table, so no route can forget  ‹previously: read 261: the numbers are stamped by a BEFORE INSERT trigger, so no route can forget› |
| P11442 | 267 F3 an activity-log row no longer makes every open panel re-read the whole floor | grep `public/panels/realtime.js` for the `audit` topic | ✅ | staff_actions rides the audit topic (267 F3) and NO floor panel subscribes to it — the three handler maps carry only ops and menu |
| P11443 | 267 F10 a part-payment on the tablet reaches the manager's open bill without waiting 60 s | grep for the `session_payments` breadcrumb consumer | ✅ | 22 references — the breadcrumb has a consumer  ‹previously: grep: session_payments carries an rt_emit trigger and the panels consume the table-scoped breadcrumb› |
| P11444 | 270/318 the guest's phone, the printed paper and the floor tile resolve a bill's tax the same way | compare `lfh_session_state`, `billMoney` and the summary for one session | ⏭ | needs a seated session to compare the phone, the paper and the tile at one instant; each leg is checked on its own in P11259, P11445 and P11035  ‹previously: needs a seated session to compare all three at once; each leg is checked on its own in P11259, P11445 and P11035› |
| P11445 | 284 the printed bill takes each order's OWN tax_rate | `npm run verify:one-number` asserts it against `billdoc.js` | ✅ | verify:one-number ran green this run and asserts billdoc.js takes each order's own tax_rate  ‹previously: npm run verify:one-number asserts billdoc.js takes each order's own tax_rate› |
| P11446 | 299 §B taking a manager power away reaches an already-open manager panel | grep the panel's `menu` topic handler for `refreshWhoami` | ✅ | 7 call sites |
| P11447 | 320 taking a power off ONE waiter reaches their open tablet | same handler, `staff_users.permissions` trigger | ✅ |  |
| P11448 | 301/310/315 the owner dashboard, the reports and the admin bill ledger all read `net_amount` | `npm run verify:one-number` lists the four files allowed to compute it | ✅ | verify:one-number ran green: the four files allowed to compute the net are the four on the written-down list  ‹previously: npm run verify:one-number lists the four files allowed to compute the net, and every one is on the written-down list› |
| P11449 | 310 the manager tile's due and the owner's revenue cannot disagree, because neither derives the rate | grep both for `lfh_effective_tax_rate` in a per-row position | ✅ | 3 hits, all COMMENTS naming the SQL function; no panel or route CALLS it per row  ‹previously: grep: neither the summary nor the owner readers resolve a rate per row any more› |
| P11450 | 326 no panel reads `settings.modules` yet, which is why today's behaviour is provably unchanged | grep app/, lib/, public/panels/ for `modules` on settings | ✅ | panels referencing a modules bag: 0 (lib/accessModel.ts is the one decider) |
| P11451 | 326 `lib/accessModel.ts` is the one place that decides column-backed vs bag-backed | read `moduleBag` | ✅ |  |
| P11452 | 331 the manager, the tablet and the admin console all reach the invoice through the one RPC | grep the three routes | ✅ | app/api/tablet/[...path]/route.ts app/api/editor/[...path]/route.ts  ‹previously: a guard in one route is a guard one caller obeys› · s8·T10 2026-09-03: re-run, still green |
| P11453 | 331 a cancelled bill's "🖨 Print bill" no longer offers a live invoice number | drive the manager Bills tab on a cancelled French House bill | ⏭ | needs a cancelled bill opened in the manager Bills tab; the refusal is read-verified against the LIVE function body in P11301/P11302  ‹previously: needs a cancelled bill opened in the manager Bills tab; the refusal is read-verified in P11301 and 1,332 French House bills sit in that state› |
| P11454 | 332 a chain row is written the instant an invoice is minted, from every one of those three doors | issue an invoice from the manager panel and read `bill_chain` | ⏭ | deliberately not done: issuing a real invoice appends a permanent row to an APPEND-ONLY ledger that can never be removed. P11315/P11317/P11362 verify every row already there  ‹previously: test bill cancelled + rows left (append-only, by design) — writing a real invoice to the shared floor would append a permanent row to an APPEND-ONLY ledger that can never be removed. Deliberately not done. P11315/P11317 verify the 14 rows already there.› |
| P11455 | 332 changing a bill's food AFTER it was signed is reported as `bill_changed`, not as a broken chain | verify before and after an edit on a French House test bill | ⏭ | same reason as P11454; the behaviour was observed on the rows that already exist — see P11317's tally  ‹previously: the honest answer: the ledger was not touched, the sale was — same reason as P11454 — and the behaviour was observed anyway on the rows that already exist: 11 of 12 French House bills report `bill_changed`. See 🟡 item 1.› |
| P11456 | 332 the verifier is reachable from the admin console and says what it checked | grep the admin route for `lfh_verify_bill_chain` | ✅ | app/api/editor/[...path]/route.ts |
| P11457 | 335 a KOT queued by the trigger is claimable by the kitchen screen AND by a counter screen | read `lib/printQueue.ts` against `kot_print_target` | ✅ |  |
| P11458 | 335 a job nobody printed WAITS, and the manager's floor strip shouts after 90 s | grep the manager panel for the stuck-job strip | ✅ | 10 lines mentioning a stuck job  ‹previously: grep: the manager panel carries the stuck-job strip› |
| P11459 | 336 a counter device must confirm once on its own screen before it ever claims | grep the editor panel for the print-station strip | ✅ | 3 lines  ‹previously: a phone would "print" into a dialog nobody sees› |
| P11460 | 338 exactly one screen is the printer, and another can take over in one tap | read the route against `print_stations_one_active` | ✅ | read the route against print_stations_one_active |
| P11461 | 341 a restaurant with NO helper still prints, because a screen claims the job instead | read the claim route's fallback | ✅ | the print-agent route family exists and a screen still claims when no helper is paired — 56 references  ‹previously: read the claim route's screen fallback› |
| P11462 | 340 answering "was the food made?" on the manager floor writes the expense the OWNER panel shows | answer on French House, read the owner Expenses list | ⏭ | answering 'was the food made?' writes an expense and an audit row to the shared floor; read-verified in P11332–P11335  ‹previously: rows removed by id — same fixture as P11399.› |
| P11463 | 342/345/346 a purge clears the operational tables and keeps every money table | `npm run verify:purge` | ✅ | npm run verify:purge is GREEN. ⚠ IT WAS RED ON THE FIRST RUN of this session and it was NOT a regression: the shared dev database was mid-way through another terminal's migration 368 (print_pairings), so admin_purge_restaurant's live body did not yet clear the brand-new table. Re-run twenty minutes later: green, and wt-s7-t24 turned out to be holding the very migration that closes it. A guard that reads live data can cry wolf while another lane is writing — check twice before filing.  ‹previously: npm run verify:purge is green› |
| P11464 | 344 a snoozed problem leaves the admin Repair board and comes back on its own | read `snoozed_until` against the board's query | ✅ | a wait, never a mute — read 344: the board's query is level='error' AND resolved_at IS NULL AND the snooze test; resolved_at stays NULL. |
| P11465 | 344 a NEW occurrence of a snoozed fault is never hidden | a fresh row carries no snooze | ✅ | read 344: a fresh occurrence writes a fresh row, which carries no snooze |
| P11466 | 348 the admin ORDERS tile and the Busiest restaurants list share one population | call both and compare | ✅ | measured in C2: admin ORDERS tile 3,491 = the live-restaurant total 3,491  ‹previously: measured: admin ORDERS tile 5,578 = the live-restaurant total 5,578; the unguarded count would have said 5,717› |
| P11467 | 306 a hidden dish is dropped by the guest READ as well as refused by the two order RPCs | grep `lib/menu.ts` for the hidden tag | ✅ |  |
| P11468 | 306 staff can still put an off-menu dish on a bill from the manager panel | the staff RPCs are untouched | ✅ | the staff RPCs are untouched by 306 — the owner chose this |
| P11469 | 282/283 no file holding the anon key reads `settings` or `restaurants` directly | `npm run verify:guest-read` | ✅ | npm run verify:guest-read exit code 0  ‹previously: not run: verify:guest-read is another terminal's file this wave. 282/283 are read-verified in P11163–P11167› |
| P11470 | 290/293 the guest ban wall can actually appear (the RPC is anon-callable) | `npm run verify:grants` allow-list check | ✅ | verify:grants ran green and asserts every allow-listed function is actually callable by anon — the ban wall can appear  ‹previously: if it is ever revoked, BanGate must go in the same commit — npm run verify:grants asserts every allow-listed function is actually callable by anon.› |
| P11471 | 313 P5 no staff device receives a guest's access token any more | read `lfh_floor_bundle`'s member object | ✅ | and the offline cache stops holding them |
| P11472 | 328 issuing a banquet bill no longer makes every guest phone re-fetch its menu | read the two settings triggers' WHEN clauses | ✅ | read 328: both settings triggers compare to_jsonb(row) minus 'banquet_bill_next' |
| P11473 | 328 issuing a banquet bill no longer files a false "edited by hand" warning | same | ✅ | it was crying wolf during the busiest service — same line. |
| P11474 | 334 the guest's bell at a joined table now reaches the manager floor's calls list | ring at a merged child, read the summary's `calls` | ⏭ | needs a bell rung at a merged child on the shared floor; the session stamp is read-verified in P11292/P11293  ‹previously: it used to say yes and reach nobody — needs a bell rung at a merged child on the shared floor (same fixture as P11381). The session stamp is read-verified in P11292/P11293.› |
| P11475 | 349 the admin Bills ledger's cross-restaurant "was it made?" lookup has an index that fits | EXPLAIN the query the route makes | ✅ | and the index is live — measured in P11351/P11352 |

---

## F · JUDGMENT — does a real restaurant need it to work this way (P11476–P11500)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P11476 | Which of these would I least like to meet during a Friday rush? | reasoned, written up in the chat report | ✅ | unchanged verdict — mig 335, auto-print. It is still the newest thing on the hottest write path: every order INSERT also writes a print_jobs row and reads settings. Narrow and correct; what it changes is that "the kitchen didn't get the ticket" becomes a queue nobody is watching. Migs 338/341 and the manager's stuck-job strip are what make that survivable  ‹previously: **mig 335** — auto-print. It is the newest thing on the hottest write path: every order INSERT now also writes a `print_jobs` row and reads `settings`. It is narrow and correct, but it is the one that turns "the kitchen didn't get the ticket" into a queue nobody is watching› |
| P11477 | Second least: the bill chain (332) writing inside the invoice transaction | reasoned | ✅ | unchanged — the bill chain's `exception when others then return null` is still the right call: the ledger records, it must never gatekeep a guest at the counter. Re-measured this run: 9 restaurants, no row_rewritten and no chain_broken  ‹previously: its `exception when others then return null` is the right call — the ledger records, it must never gatekeep a guest at the counter. What DID need fixing was the reading end: the verifier could not tell a binned bill from an altered one, and it printed on the Z-report. Migration 353› |
| P11478 | Is the floor summary (238) still the right shape for a 300-table restaurant? | the measured numbers in its header, re-timed this run | ✅ | re-timed this run: the whole French House floor in 251 ms and Aangan in 219 ms, against the 8 s statement wall. Do not simplify it back  ‹previously: 11–29 ms at 300 tables; do not simplify it back› |
| P11479 | Is a re-seed a real thing that happens, or a theoretical one? | CLAUDE.md documents the command and warns about it; migs 307/313/321/344 all guard against it | ✅ | unchanged and now stronger — a re-seed is real, and this run found a SEVENTH statement of the shape (mig 225). Migs 307/313/321/344/352/369 all exist because of it  ‹previously: it is real, which is why P11023/P11024/P11216 are problems and not tidiness› |
| P11480 | Would a restaurant notice the 235 re-seed fault? | AANGAN GARDEN would silently regain five languages on its guest menu | ✅ | unchanged — and the same question for the new finding: a restaurant WOULD notice mig 225's, because every waiter's tablet would show the whole floor again after a manager had narrowed it  ‹previously: measured, not assumed› |
| P11481 | Is "the money is kept, the operations are purged" the right split? | it is the owner's own decision (2026-08-11) and compliance requires the money half | ✅ | unchanged — "the money is kept, the operations are purged" is the owner's own decision (2026-08-11) and compliance requires the money half. But this run found the split is not actually being kept for parcel sales, expenses and stock purchases — see the new rows P26594/P26595 and the report |
| P11482 | Should `bill_chain` be purgeable? | no — it is the tamper-evidence for the bills a purge keeps | ✅ | unchanged — no, bill_chain must not be purgeable; it is the tamper-evidence for the bills a purge keeps, and mig 332's append-only trigger makes the question moot  ‹previously: no — it is the tamper-evidence for the bills a purge keeps, and the append-only trigger makes the question moot. Its VERIFIER, though, had to learn the difference between a purge/bin and an alteration (mig 353)› |
| P11483 | Is one invoice series across dine-in, parcel and delivery right for a real restaurant? | the owner asked for it by name, and a gap-free series is what an inspector wants | ✅ | unchanged, and measured this run: no invoice number is shared between a dine-in bill and a parcel in any restaurant (new row P26585) |
| P11484 | Is numbering a parcel at INSERT rather than at print the right trade? | yes: a bill handed over with no number is worse than an explainable gap | ✅ | unchanged — yes: a bill handed over with no number is worse than an explainable gap |
| P11485 | Does the audit's 10-year default sit above the compliance range? | §3 says 6–8 years; the default is 10 and the floor is 1 | ✅ | unchanged — §3 says 6–8 years, the default is 10, the floor is 1, and the floor is enforced in lfh_audit_retention_years() on the live database (new row P26579) |
| P11486 | Is a retention WINDOW the same thing as an off switch? | no — nothing stops the trail being written and there is no "clear now" door | ✅ | unchanged — a retention WINDOW is not an off switch: nothing stops the trail being written and there is no "clear now" door |
| P11487 | Is the merge model (one session, two table numbers) still the simplest thing that works? | it keeps one bill, one invoice and one payment with no read-path change | ✅ | unchanged — one session, two table numbers, one bill, one invoice, one payment, no read-path change. And measured: no table is a live child of two joins (P26588) |
| P11488 | Does a waiter need to walk to the parent table to ring a bell rung at the child? | no — 334 keeps `table_number` at the child and only moves the session link | ✅ | unchanged — no; mig 334 keeps table_number at the child and only moves the session link |
| P11489 | Is the guest's live bill worth bucketing per rate for one session and ₹8.13? | yes — it stops being ₹8 the day a restaurant runs 18 % beside 5 % | ✅ | unchanged — yes; it stops being a rounding argument the day a restaurant runs 18 % beside 5 % |
| P11490 | Is `settings.modules` worth having before any module uses it? | yes: 110 columns, and the alternative is four more per module for ever | ✅ | unchanged — yes: 110 settings columns already, and the alternative is four more per module for ever. Re-checked this run: nothing after 326 has added a four-column ladder (P26499) |
| P11491 | Should the 22-table purge list be a list at all, rather than a cascade? | the cascade stopped firing at 309 when the restaurants row began to survive | ✅ | unchanged — a list plus verify:purge is the honest shape, because the cascade stopped firing at 309. What this run adds: the list has to be a DECISION record, not just a list. 22 of the 47 tables it deletes carried no written reason at all  ‹previously: a list plus `verify:purge` is the honest shape› |
| P11492 | Is it right that a chain write failure is silent to the person at the till? | yes, and the verifier is what makes it non-silent to the owner | ✅ | unchanged — yes, and the verifier is what makes it non-silent to the owner |
| P11493 | Would a cook be hurt by a duplicate kitchen ticket with no banner? | yes — which is why the KOT keeps its banner and the BILL does not | ✅ | unchanged — yes; which is why the KOT keeps its DUPLICATE banner and the BILL does not (R37/R38) |
| P11494 | Is the 8-second statement wall the number every read in this folder must beat? | yes — PostgREST's `authenticator` role carries it, not the 120 s default | ✅ | unchanged — yes, 8 seconds, because PostgREST's authenticator role carries it, not the 120 s default  ‹previously: mig 241 measured it› |
| P11495 | Is any read in 231→349 still capable of crossing that wall on this data? | timed the heaviest ones this run | ✅ | unchanged — the whole-portfolio heatmap over a year still can; known, written up in docs/FLOOR-TIMEOUT-WATCH.md, and not this terminal's territory. Everything in 223→332 was re-timed this run and the slowest was 317 ms  ‹previously: the whole-portfolio heatmap over a year still can — known, written up in `docs/FLOOR-TIMEOUT-WATCH.md`, not this terminal's territory› |
| P11496 | Does anything in this folder let a sale disappear? | read every DELETE in 231→349 against the compliance rule | ✅ | re-checked statement by statement this run across all 109 files (new rows P26319–P26427): every DELETE against a money table is one of three reviewed cases — the rollup's own rebuild, the audit retention window, and the purge. The purge is the open question, and it is in the report  ‹previously: none touches orders / sessions / invoice_events / credit_notes / deletion_audit› |
| P11497 | Does anything in this folder let a bill be edited after it is settled? | 314 patches one note and records it; nothing else | ✅ | unchanged — 314 patches one line of a settled bill's ticket and records it; nothing else edits a settled bill |
| P11498 | Does any migration in this folder add a way to hide a sale from the Z-report? | grep every new read for an `archived` or `deleted_at` filter that a report uses | ✅ | unchanged — reports never filter `archived`, so nothing in this folder adds a way to hide a sale from the Z-report |
| P11499 | Is the number of one-time data rewrites in this folder now fully enumerated? | 12 keys in `lfh_applied_once`, each pointing at a wrapped statement | ✅ | expectation MOVED: 16 one-time keys before this branch, 17 after (225_sections_follow_table_count is the new one), and verify:grants derives the population from the folder and checks BOTH directions, so the number cannot fall behind again  ‹previously: 14 keys in `lfh_applied_once`, each pointing at a wrapped statement — and `verify:grants` now derives that list from the folder rather than keeping its own, so it cannot fall behind again (item 7)› |
| P11500 | Is there anything left in this territory that I would not hand to the owner? | the whole territory, re-read | ✅ | the whole territory re-read at its new boundary (positions 231–339 = files 223→332). One problem found and fixed, two improvements built, and two purge questions handed to the owner rather than decided for him  ‹previously: the whole territory, re-read. Four problems fixed, five improvements built, nothing handed off — he asked for the two remaining items and they are in this branch› |

---

## G · NEW FOR SWEEP #7 — EVERY FILE IN THE TERRITORY, THREE WAYS (P26101–P26427)

The territory moved for sweep #7: it is now **positions 231–339** of `ls supabase/migrations/*.sql | sort`,
which is **109 files, `223_waiter_sections_full_access_backfill.sql` → `332_every_bill_is_signed_and_chained.sql`,
16,120 lines**. Eight of those files (223–230) were BELOW sweep #6's boundary and had never been checked
by this terminal — and that is where this run's one real problem was found (mig 225, row P26502).

Each file gets three permanent ids, keyed by its position in the sorted territory so an id always
means the same file: **re-runnable? · header names its own file? · can it make a sale disappear?**

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P26101 | 223_waiter_sections_full_access_backfill.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 223_waiter_sections_full_access_backfill.sql |
| P26102 | 224_inventory_recipes.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 224_inventory_recipes.sql |
| P26103 | 225_waiter_sections_follow_table_count.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 225_waiter_sections_follow_table_count.sql |
| P26104 | 226_floor_tiles_per_row.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 226_floor_tiles_per_row.sql |
| P26105 | 227_bill_customer.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 227_bill_customer.sql |
| P26106 | 227_inventory_reports.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 227_inventory_reports.sql |
| P26107 | 228_customer_views.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 228_customer_views.sql |
| P26108 | 228_open_table_concurrency.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 228_open_table_concurrency.sql |
| P26109 | 229_customer_scale.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 229_customer_scale.sql |
| P26110 | 229_floor_summary_scalar_items_guard.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 229_floor_summary_scalar_items_guard.sql |
| P26111 | 230_floor_live_orders_index.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 230_floor_live_orders_index.sql |
| P26112 | 231_inventory_void_costing.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 231_inventory_void_costing.sql |
| P26113 | 232_orders_never_outlive_their_session.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 232_orders_never_outlive_their_session.sql |
| P26114 | 233_customer_visit_follows_the_bill.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 233_customer_visit_follows_the_bill.sql |
| P26115 | 234_summary_qty_guard_into_source.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 234_summary_qty_guard_into_source.sql |
| P26116 | 235_access_model_v2.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 235_access_model_v2.sql |
| P26117 | 236_write_down_the_unwritten_function.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 236_write_down_the_unwritten_function.sql |
| P26118 | 237_banquet_bill.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 237_banquet_bill.sql |
| P26119 | 238_floor_summary_one_pass.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 238_floor_summary_one_pass.sql |
| P26120 | 239_banquet_tax.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 239_banquet_tax.sql |
| P26121 | 240_write_down_the_guest_order_function.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 240_write_down_the_guest_order_function.sql |
| P26122 | 241_heatmap_tax_rate_once.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 241_heatmap_tax_rate_once.sql |
| P26123 | 242_floor_layout_mode.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 242_floor_layout_mode.sql |
| P26124 | 243_orders_older_than_their_table.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 243_orders_older_than_their_table.sql |
| P26125 | 244_drop_the_dead_heatmap_copy.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 244_drop_the_dead_heatmap_copy.sql |
| P26126 | 245_binned_login_frees_its_name.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 245_binned_login_frees_its_name.sql |
| P26127 | 246_orders_change_watermark.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 246_orders_change_watermark.sql |
| P26128 | 247_vacuum_the_hot_tables_sooner.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 247_vacuum_the_hot_tables_sooner.sql |
| P26129 | 248_split_bill_is_off_by_default.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 248_split_bill_is_off_by_default.sql |
| P26130 | 249_merge_is_recorded_and_reversible.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 249_merge_is_recorded_and_reversible.sql |
| P26131 | 250_order_at_a_merged_table.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 250_order_at_a_merged_table.sql |
| P26132 | 251_deletion_audit.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 251_deletion_audit.sql |
| P26133 | 253_open_price_is_staff_only.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 253_open_price_is_staff_only.sql |
| P26134 | 254_no_table_ends_itself.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 254_no_table_ends_itself.sql |
| P26135 | 256_parcel_printed.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 256_parcel_printed.sql |
| P26136 | 257_qop_feature_switch.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 257_qop_feature_switch.sql |
| P26137 | 258_qop_sub_switches.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 258_qop_sub_switches.sql |
| P26138 | 259_parcel_and_platforms_are_separate.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 259_parcel_and_platforms_are_separate.sql |
| P26139 | 260_no_second_party_on_a_joined_table.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 260_no_second_party_on_a_joined_table.sql |
| P26140 | 261_parcel_platform_bill_numbers.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 261_parcel_platform_bill_numbers.sql |
| P26141 | 262_audit_records_every_money_change.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 262_audit_records_every_money_change.sql |
| P26142 | 263_parcel_platforms_permanent.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 263_parcel_platforms_permanent.sql |
| P26143 | 264_every_path_respects_a_merge.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 264_every_path_respects_a_merge.sql |
| P26144 | 265_floor_per_row_range_30.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 265_floor_per_row_range_30.sql |
| P26145 | 266_owner_overview_scopes_its_history.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 266_owner_overview_scopes_its_history.sql |
| P26146 | 267_database_sweep_fixes.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 267_database_sweep_fixes.sql |
| P26147 | 268_prune_action_idempotency.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 268_prune_action_idempotency.sql |
| P26148 | 269_print_reliability.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 269_print_reliability.sql |
| P26149 | 270_item_tax_mode_and_mrp.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 270_item_tax_mode_and_mrp.sql |
| P26150 | 271_session_state_carries_the_split.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 271_session_state_carries_the_split.sql |
| P26151 | 272_composition_rate_and_mrp_lock.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 272_composition_rate_and_mrp_lock.sql |
| P26152 | 273_ticket_keeps_its_tax_mode.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 273_ticket_keeps_its_tax_mode.sql |
| P26153 | 274_guest_read_grants_restored.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 274_guest_read_grants_restored.sql |
| P26154 | 277_password_change_has_a_wall.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 277_password_change_has_a_wall.sql |
| P26155 | 278_refusals_carry_a_code_not_a_sentence.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 278_refusals_carry_a_code_not_a_sentence.sql |
| P26156 | 279_private_paperwork_buckets.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 279_private_paperwork_buckets.sql |
| P26157 | 280_the_bill_tombstone_that_never_landed.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 280_the_bill_tombstone_that_never_landed.sql |
| P26158 | 281_close_all_removed_and_three_of_four.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 281_close_all_removed_and_three_of_four.sql |
| P26159 | 282_guest_reads_through_one_door.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 282_guest_reads_through_one_door.sql |
| P26160 | 283_close_the_door_behind_the_guest.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 283_close_the_door_behind_the_guest.sql |
| P26161 | 284_an_order_remembers_its_own_tax_rate.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 284_an_order_remembers_its_own_tax_rate.sql |
| P26162 | 285_a_payment_leg_is_reversed_never_erased.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 285_a_payment_leg_is_reversed_never_erased.sql |
| P26163 | 286_a_void_must_say_why.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 286_a_void_must_say_why.sql |
| P26164 | 287_tax_trigger_functions_are_not_callable.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 287_tax_trigger_functions_are_not_callable.sql |
| P26165 | 288_only_stamp_a_rate_we_believe.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 288_only_stamp_a_rate_we_believe.sql |
| P26166 | 289_the_breadcrumb_table_stays_small.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 289_the_breadcrumb_table_stays_small.sql |
| P26167 | 290_the_blocked_guest_must_be_told.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 290_the_blocked_guest_must_be_told.sql |
| P26168 | 291_a_fully_deleted_bill_tombstones_itself.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 291_a_fully_deleted_bill_tombstones_itself.sql |
| P26169 | 292_index_the_clash_session_lookup.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 292_index_the_clash_session_lookup.sql |
| P26170 | 293_the_ban_check_is_the_guests_own_question.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 293_the_ban_check_is_the_guests_own_question.sql |
| P26171 | 294_document_dates_follow_the_business_day.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 294_document_dates_follow_the_business_day.sql |
| P26172 | 295_waiter_caps_reach_a_switch.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 295_waiter_caps_reach_a_switch.sql |
| P26173 | 296_database_layer_a_sweep_fixes.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 296_database_layer_a_sweep_fixes.sql |
| P26174 | 297_undo_a_resurrection.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 297_undo_a_resurrection.sql |
| P26175 | 298_each_menu_starts_at_one.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 298_each_menu_starts_at_one.sql |
| P26176 | 299_cross_panel_truth_t13.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 299_cross_panel_truth_t13.sql |
| P26177 | 300_database_layer_b_sweep_fixes.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 300_database_layer_b_sweep_fixes.sql |
| P26178 | 301_a_discount_is_grossed_at_the_rate_it_was_charged.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 301_a_discount_is_grossed_at_the_rate_it_was_charged.sql |
| P26179 | 302_an_order_cannot_join_a_closed_session.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 302_an_order_cannot_join_a_closed_session.sql |
| P26180 | 303_archived_unpaid_food_leaves_a_mark.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 303_archived_unpaid_food_leaves_a_mark.sql |
| P26181 | 304_database_layer_a_sweep_fixes_two.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 304_database_layer_a_sweep_fixes_two.sql |
| P26182 | 305_public_error_log_cap_needs_an_index.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 305_public_error_log_cap_needs_an_index.sql |
| P26183 | 306_hidden_dishes.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 306_hidden_dishes.sql |
| P26184 | 307_a_reseed_cannot_multiply_the_money.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 307_a_reseed_cannot_multiply_the_money.sql |
| P26185 | 308_who_joined_these_tables.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 308_who_joined_these_tables.sql |
| P26186 | 309_khata_drops_a_deleted_bill_and_bills_survive_a_purge.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 309_khata_drops_a_deleted_bill_and_bills_survive_a_purge.sql |
| P26187 | 310_one_revenue_number_everywhere.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 310_one_revenue_number_everywhere.sql |
| P26188 | 311_the_audit_is_kept_for_years_not_days.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 311_the_audit_is_kept_for_years_not_days.sql |
| P26189 | 312_a_merge_keeps_the_table_s_mark.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 312_a_merge_keeps_the_table_s_mark.sql |
| P26190 | 313_the_setup_script_cannot_hurt_a_live_table.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 313_the_setup_script_cannot_hurt_a_live_table.sql |
| P26191 | 314_a_settled_bill_is_annotated_not_rewritten.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 314_a_settled_bill_is_annotated_not_rewritten.sql |
| P26192 | 315_the_rollup_carries_the_net_too.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 315_the_rollup_carries_the_net_too.sql |
| P26193 | 316_the_three_numbers_and_the_two_switch_bags_say_what_they_are.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 316_the_three_numbers_and_the_two_switch_bags_say_what_they_are.sql |
| P26194 | 317_a_pay_later_bill_belongs_to_the_day_it_was_paid.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 317_a_pay_later_bill_belongs_to_the_day_it_was_paid.sql |
| P26195 | 318_the_guest_sees_the_bill_the_paper_will_print.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 318_the_guest_sees_the_bill_the_paper_will_print.sql |
| P26196 | 319_the_bin_stops_locking_a_restaurants_name.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 319_the_bin_stops_locking_a_restaurants_name.sql |
| P26197 | 320_a_power_taken_off_one_person_reaches_their_screen.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 320_a_power_taken_off_one_person_reaches_their_screen.sql |
| P26198 | 321_the_sweep_of_layer_b.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 321_the_sweep_of_layer_b.sql |
| P26199 | 322_the_admin_floor_reads_every_restaurant_in_one_pass.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 322_the_admin_floor_reads_every_restaurant_in_one_pass.sql |
| P26200 | 323_one_definition_of_what_dishes_are_on_an_order.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 323_one_definition_of_what_dishes_are_on_an_order.sql |
| P26201 | 324_the_kitchen_reads_the_one_definition.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 324_the_kitchen_reads_the_one_definition.sql |
| P26202 | 325_a_dish_photo_can_be_uploaded.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 325_a_dish_photo_can_be_uploaded.sql |
| P26203 | 326_a_new_module_needs_no_new_column.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 326_a_new_module_needs_no_new_column.sql |
| P26204 | 327_the_records_strip_stops_re_reading_all_time.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 327_the_records_strip_stops_re_reading_all_time.sql |
| P26205 | 328_a_bill_counter_is_not_a_settings_change.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 328_a_bill_counter_is_not_a_settings_change.sql |
| P26206 | 329_a_prep_batch_is_one_action.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 329_a_prep_batch_is_one_action.sql |
| P26207 | 330_the_admin_can_hand_over_a_login.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 330_the_admin_can_hand_over_a_login.sql |
| P26208 | 331_a_cancelled_sale_takes_no_invoice_number.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 331_a_cancelled_sale_takes_no_invoice_number.sql |
| P26209 | 332_every_bill_is_signed_and_chained.sql — is this file safe for `seed-supabase.mjs` to run a SECOND time? (no CREATE without IF NOT EXISTS / a preceding DROP / a duplicate_object guard) | read every DDL statement in the file; a CREATE TABLE, ADD COLUMN, CREATE INDEX, CREATE TRIGGER or ADD CONSTRAINT must be re-runnable | ✅ | 332_every_bill_is_signed_and_chained.sql |

### G2 · does each header name its OWN file? (P26210–P26318)

A header pointing at an unrelated migration is a small trap with a long fuse: the next person reading
a one-time data rewrite is sent to the wrong file at exactly the wrong moment. Six files in this range
carry an explicit "RENUMBERED x → y on merge" note precisely because it matters.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P26210 | 223_waiter_sections_full_access_backfill.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 223_waiter_sections_full_access_backfill.sql: first lines name 223 |
| P26211 | 224_inventory_recipes.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 224_inventory_recipes.sql: first lines name 224 |
| P26212 | 225_waiter_sections_follow_table_count.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 225_waiter_sections_follow_table_count.sql: first lines name 225,544 |
| P26213 | 226_floor_tiles_per_row.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 226_floor_tiles_per_row.sql: first lines name 226 |
| P26214 | 227_bill_customer.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 227_bill_customer.sql: first lines name 227 |
| P26215 | 227_inventory_reports.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 227_inventory_reports.sql: first lines name 227 |
| P26216 | 228_customer_views.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 228_customer_views.sql: first lines name 228 |
| P26217 | 228_open_table_concurrency.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 228_open_table_concurrency.sql: first lines name 228 |
| P26218 | 229_customer_scale.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 229_customer_scale.sql: first lines name 229,500,500 |
| P26219 | 229_floor_summary_scalar_items_guard.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 229_floor_summary_scalar_items_guard.sql: first lines name 229 |
| P26220 | 230_floor_live_orders_index.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 230_floor_live_orders_index.sql: first lines name 230 |
| P26221 | 231_inventory_void_costing.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 231_inventory_void_costing.sql: first lines name 231 |
| P26222 | 232_orders_never_outlive_their_session.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 232_orders_never_outlive_their_session.sql: first lines name 232 |
| P26223 | 233_customer_visit_follows_the_bill.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 233_customer_visit_follows_the_bill.sql: first lines name 233 |
| P26224 | 234_summary_qty_guard_into_source.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 234_summary_qty_guard_into_source.sql: first lines name 234 |
| P26225 | 235_access_model_v2.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 235_access_model_v2.sql: first lines name 235 |
| P26226 | 236_write_down_the_unwritten_function.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 236_write_down_the_unwritten_function.sql: first lines name 236,235,235 |
| P26227 | 237_banquet_bill.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 237_banquet_bill.sql: first lines name 237 |
| P26228 | 238_floor_summary_one_pass.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 238_floor_summary_one_pass.sql: first lines name 238,237,237,236 |
| P26229 | 239_banquet_tax.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 239_banquet_tax.sql: first lines name 239 |
| P26230 | 240_write_down_the_guest_order_function.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 240_write_down_the_guest_order_function.sql: first lines name 240 |
| P26231 | 241_heatmap_tax_rate_once.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 241_heatmap_tax_rate_once.sql: first lines name 241 |
| P26232 | 242_floor_layout_mode.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 242_floor_layout_mode.sql: first lines name 242 |
| P26233 | 243_orders_older_than_their_table.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 243_orders_older_than_their_table.sql: first lines name 243 |
| P26234 | 244_drop_the_dead_heatmap_copy.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 244_drop_the_dead_heatmap_copy.sql: first lines name 244 |
| P26235 | 245_binned_login_frees_its_name.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 245_binned_login_frees_its_name.sql: first lines name 245 |
| P26236 | 246_orders_change_watermark.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 246_orders_change_watermark.sql: first lines name 246 |
| P26237 | 247_vacuum_the_hot_tables_sooner.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 247_vacuum_the_hot_tables_sooner.sql: first lines name 247,246,246 |
| P26238 | 248_split_bill_is_off_by_default.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 248_split_bill_is_off_by_default.sql: first lines name 248 |
| P26239 | 249_merge_is_recorded_and_reversible.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 249_merge_is_recorded_and_reversible.sql: first lines name 249 |
| P26240 | 250_order_at_a_merged_table.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 250_order_at_a_merged_table.sql: first lines name 250 |
| P26241 | 251_deletion_audit.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 251_deletion_audit.sql: first lines name 251 |
| P26242 | 253_open_price_is_staff_only.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 253_open_price_is_staff_only.sql: first lines name 253,215,215 |
| P26243 | 254_no_table_ends_itself.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 254_no_table_ends_itself.sql: first lines name 254 |
| P26244 | 256_parcel_printed.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 256_parcel_printed.sql: first lines name 256 |
| P26245 | 257_qop_feature_switch.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 257_qop_feature_switch.sql: first lines name 257 |
| P26246 | 258_qop_sub_switches.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 258_qop_sub_switches.sql: first lines name 258 |
| P26247 | 259_parcel_and_platforms_are_separate.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 259_parcel_and_platforms_are_separate.sql: first lines name 259 |
| P26248 | 260_no_second_party_on_a_joined_table.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 260_no_second_party_on_a_joined_table.sql: first lines name 260 |
| P26249 | 261_parcel_platform_bill_numbers.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 261_parcel_platform_bill_numbers.sql: first lines name 261 |
| P26250 | 262_audit_records_every_money_change.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 262_audit_records_every_money_change.sql: first lines name 262 |
| P26251 | 263_parcel_platforms_permanent.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 263_parcel_platforms_permanent.sql: first lines name 263 |
| P26252 | 264_every_path_respects_a_merge.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 264_every_path_respects_a_merge.sql: first lines name 264 |
| P26253 | 265_floor_per_row_range_30.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 265_floor_per_row_range_30.sql: first lines name 265,260,260 |
| P26254 | 266_owner_overview_scopes_its_history.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 266_owner_overview_scopes_its_history.sql: first lines name 266 |
| P26255 | 267_database_sweep_fixes.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 267_database_sweep_fixes.sql: first lines name 267,266 |
| P26256 | 268_prune_action_idempotency.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 268_prune_action_idempotency.sql: first lines name 268 |
| P26257 | 269_print_reliability.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 269_print_reliability.sql: first lines name 269 |
| P26258 | 270_item_tax_mode_and_mrp.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 270_item_tax_mode_and_mrp.sql: first lines name 270 |
| P26259 | 271_session_state_carries_the_split.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 271_session_state_carries_the_split.sql: first lines name 271 |
| P26260 | 272_composition_rate_and_mrp_lock.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 272_composition_rate_and_mrp_lock.sql: first lines name 272 |
| P26261 | 273_ticket_keeps_its_tax_mode.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 273_ticket_keeps_its_tax_mode.sql: first lines name 273 |
| P26262 | 274_guest_read_grants_restored.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 274_guest_read_grants_restored.sql: first lines name 274,500 |
| P26263 | 277_password_change_has_a_wall.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 277_password_change_has_a_wall.sql: first lines name 277 |
| P26264 | 278_refusals_carry_a_code_not_a_sentence.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 278_refusals_carry_a_code_not_a_sentence.sql: first lines name 278 |
| P26265 | 279_private_paperwork_buckets.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 279_private_paperwork_buckets.sql: first lines name 279 |
| P26266 | 280_the_bill_tombstone_that_never_landed.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 280_the_bill_tombstone_that_never_landed.sql: first lines name 280 |
| P26267 | 281_close_all_removed_and_three_of_four.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 281_close_all_removed_and_three_of_four.sql: first lines name 281 |
| P26268 | 282_guest_reads_through_one_door.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 282_guest_reads_through_one_door.sql: first lines name 282 |
| P26269 | 283_close_the_door_behind_the_guest.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 283_close_the_door_behind_the_guest.sql: first lines name 283,282 |
| P26270 | 284_an_order_remembers_its_own_tax_rate.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 284_an_order_remembers_its_own_tax_rate.sql: first lines name 284 |
| P26271 | 285_a_payment_leg_is_reversed_never_erased.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 285_a_payment_leg_is_reversed_never_erased.sql: first lines name 285 |
| P26272 | 286_a_void_must_say_why.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 286_a_void_must_say_why.sql: first lines name 286 |
| P26273 | 287_tax_trigger_functions_are_not_callable.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 287_tax_trigger_functions_are_not_callable.sql: first lines name 287,270,038 |
| P26274 | 288_only_stamp_a_rate_we_believe.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 288_only_stamp_a_rate_we_believe.sql: first lines name 288,287 |
| P26275 | 289_the_breadcrumb_table_stays_small.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 289_the_breadcrumb_table_stays_small.sql: first lines name 289 |
| P26276 | 290_the_blocked_guest_must_be_told.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 290_the_blocked_guest_must_be_told.sql: first lines name 290 |
| P26277 | 291_a_fully_deleted_bill_tombstones_itself.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 291_a_fully_deleted_bill_tombstones_itself.sql: first lines name 291 |
| P26278 | 292_index_the_clash_session_lookup.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 292_index_the_clash_session_lookup.sql: first lines name 292 |
| P26279 | 293_the_ban_check_is_the_guests_own_question.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 293_the_ban_check_is_the_guests_own_question.sql: first lines name 293,290 |
| P26280 | 294_document_dates_follow_the_business_day.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 294_document_dates_follow_the_business_day.sql: first lines name 294,290,294,294,290,290 |
| P26281 | 295_waiter_caps_reach_a_switch.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 295_waiter_caps_reach_a_switch.sql: first lines name 295 |
| P26282 | 296_database_layer_a_sweep_fixes.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 296_database_layer_a_sweep_fixes.sql: first lines name 296,295,296,295 |
| P26283 | 297_undo_a_resurrection.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 297_undo_a_resurrection.sql: first lines name 297,267 |
| P26284 | 298_each_menu_starts_at_one.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 298_each_menu_starts_at_one.sql: first lines name 298 |
| P26285 | 299_cross_panel_truth_t13.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 299_cross_panel_truth_t13.sql: first lines name 299 |
| P26286 | 300_database_layer_b_sweep_fixes.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 300_database_layer_b_sweep_fixes.sql: first lines name 300,286 |
| P26287 | 301_a_discount_is_grossed_at_the_rate_it_was_charged.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 301_a_discount_is_grossed_at_the_rate_it_was_charged.sql: first lines name 301 |
| P26288 | 302_an_order_cannot_join_a_closed_session.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 302_an_order_cannot_join_a_closed_session.sql: first lines name 302,232,150 |
| P26289 | 303_archived_unpaid_food_leaves_a_mark.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 303_archived_unpaid_food_leaves_a_mark.sql: first lines name 303,232 |
| P26290 | 304_database_layer_a_sweep_fixes_two.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 304_database_layer_a_sweep_fixes_two.sql: first lines name 304,150 |
| P26291 | 305_public_error_log_cap_needs_an_index.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 305_public_error_log_cap_needs_an_index.sql: first lines name 305,304,305,304,295 |
| P26292 | 306_hidden_dishes.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 306_hidden_dishes.sql: first lines name 306 |
| P26293 | 307_a_reseed_cannot_multiply_the_money.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 307_a_reseed_cannot_multiply_the_money.sql: first lines name 307 |
| P26294 | 308_who_joined_these_tables.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 308_who_joined_these_tables.sql: first lines name 308,249 |
| P26295 | 309_khata_drops_a_deleted_bill_and_bills_survive_a_purge.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 309_khata_drops_a_deleted_bill_and_bills_survive_a_purge.sql: first lines name 309 |
| P26296 | 310_one_revenue_number_everywhere.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 310_one_revenue_number_everywhere.sql: first lines name 310 |
| P26297 | 311_the_audit_is_kept_for_years_not_days.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 311_the_audit_is_kept_for_years_not_days.sql: first lines name 311 |
| P26298 | 312_a_merge_keeps_the_table_s_mark.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 312_a_merge_keeps_the_table_s_mark.sql: first lines name 312 |
| P26299 | 313_the_setup_script_cannot_hurt_a_live_table.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 313_the_setup_script_cannot_hurt_a_live_table.sql: first lines name 313,311,313,313,311,311 |
| P26300 | 314_a_settled_bill_is_annotated_not_rewritten.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 314_a_settled_bill_is_annotated_not_rewritten.sql: first lines name 314,312,314,312 |
| P26301 | 315_the_rollup_carries_the_net_too.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 315_the_rollup_carries_the_net_too.sql: first lines name 315,310 |
| P26302 | 316_the_three_numbers_and_the_two_switch_bags_say_what_they_are.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 316_the_three_numbers_and_the_two_switch_bags_say_what_they_are.sql: first lines name 316 |
| P26303 | 317_a_pay_later_bill_belongs_to_the_day_it_was_paid.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 317_a_pay_later_bill_belongs_to_the_day_it_was_paid.sql: first lines name 317 |
| P26304 | 318_the_guest_sees_the_bill_the_paper_will_print.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 318_the_guest_sees_the_bill_the_paper_will_print.sql: first lines name 318 |
| P26305 | 319_the_bin_stops_locking_a_restaurants_name.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 319_the_bin_stops_locking_a_restaurants_name.sql: first lines name 319 |
| P26306 | 320_a_power_taken_off_one_person_reaches_their_screen.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 320_a_power_taken_off_one_person_reaches_their_screen.sql: first lines name 320,299 |
| P26307 | 321_the_sweep_of_layer_b.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 321_the_sweep_of_layer_b.sql: first lines name 321,313,320 |
| P26308 | 322_the_admin_floor_reads_every_restaurant_in_one_pass.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 322_the_admin_floor_reads_every_restaurant_in_one_pass.sql: first lines name 322 |
| P26309 | 323_one_definition_of_what_dishes_are_on_an_order.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 323_one_definition_of_what_dishes_are_on_an_order.sql: first lines name 323 |
| P26310 | 324_the_kitchen_reads_the_one_definition.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 324_the_kitchen_reads_the_one_definition.sql: first lines name 324,323 |
| P26311 | 325_a_dish_photo_can_be_uploaded.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 325_a_dish_photo_can_be_uploaded.sql: first lines name 325 |
| P26312 | 326_a_new_module_needs_no_new_column.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 326_a_new_module_needs_no_new_column.sql: first lines name 326,320,326 |
| P26313 | 327_the_records_strip_stops_re_reading_all_time.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 327_the_records_strip_stops_re_reading_all_time.sql: first lines name 327,321,327 |
| P26314 | 328_a_bill_counter_is_not_a_settings_change.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 328_a_bill_counter_is_not_a_settings_change.sql: first lines name 328 |
| P26315 | 329_a_prep_batch_is_one_action.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 329_a_prep_batch_is_one_action.sql: first lines name 329 |
| P26316 | 330_the_admin_can_hand_over_a_login.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 330_the_admin_can_hand_over_a_login.sql: first lines name 330 |
| P26317 | 331_a_cancelled_sale_takes_no_invoice_number.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 331_a_cancelled_sale_takes_no_invoice_number.sql: first lines name 331 |
| P26318 | 332_every_bill_is_signed_and_chained.sql — the first four lines of the header name THIS file's number, or say it was renumbered | read the first four lines; any 3-digit number in them must be this file's own, or the file must carry a RENUMBER note | ✅ | 332_every_bill_is_signed_and_chained.sql: first lines name 332 |

### G3 · can anything in this file make a sale disappear? (P26319–P26427)

The compliance rule (`docs/COMPLIANCE-GUARDRAILS.md` §3.0): a sale can be cancelled, a sale can never
disappear. Checked statement by statement against every money table — `orders`, `order_items`,
`sessions`, `payments`, `session_payments`, `credit_notes`, `invoice_events`, `deletion_audit`,
`daily_counters`, `seq_counters`, `banquet_bills`, the two rollups, `expenses`, `staff_payments`,
`inv_purchases`, `aggregator_orders`, `bill_chain`. Four deletes are REVIEWED AND DELIBERATE (the two
rollups' own rebuild, the audit retention window, and taking one dish off an unpaid ticket); anything
else is a finding.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P26319 | 223_waiter_sections_full_access_backfill.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 223_waiter_sections_full_access_backfill.sql |
| P26320 | 224_inventory_recipes.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 224_inventory_recipes.sql |
| P26321 | 225_waiter_sections_follow_table_count.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 225_waiter_sections_follow_table_count.sql |
| P26322 | 226_floor_tiles_per_row.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 226_floor_tiles_per_row.sql |
| P26323 | 227_bill_customer.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 227_bill_customer.sql |
| P26324 | 227_inventory_reports.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 227_inventory_reports.sql |
| P26325 | 228_customer_views.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 228_customer_views.sql |
| P26326 | 228_open_table_concurrency.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 228_open_table_concurrency.sql |
| P26327 | 229_customer_scale.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 229_customer_scale.sql |
| P26328 | 229_floor_summary_scalar_items_guard.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 229_floor_summary_scalar_items_guard.sql |
| P26329 | 230_floor_live_orders_index.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 230_floor_live_orders_index.sql |
| P26330 | 231_inventory_void_costing.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 231_inventory_void_costing.sql |
| P26331 | 232_orders_never_outlive_their_session.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 232_orders_never_outlive_their_session.sql |
| P26332 | 233_customer_visit_follows_the_bill.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 233_customer_visit_follows_the_bill.sql |
| P26333 | 234_summary_qty_guard_into_source.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 234_summary_qty_guard_into_source.sql |
| P26334 | 235_access_model_v2.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 235_access_model_v2.sql |
| P26335 | 236_write_down_the_unwritten_function.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 236_write_down_the_unwritten_function.sql |
| P26336 | 237_banquet_bill.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 237_banquet_bill.sql |
| P26337 | 238_floor_summary_one_pass.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 238_floor_summary_one_pass.sql |
| P26338 | 239_banquet_tax.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 239_banquet_tax.sql |
| P26339 | 240_write_down_the_guest_order_function.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 240_write_down_the_guest_order_function.sql |
| P26340 | 241_heatmap_tax_rate_once.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 241_heatmap_tax_rate_once.sql |
| P26341 | 242_floor_layout_mode.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 242_floor_layout_mode.sql |
| P26342 | 243_orders_older_than_their_table.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 243_orders_older_than_their_table.sql |
| P26343 | 244_drop_the_dead_heatmap_copy.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 244_drop_the_dead_heatmap_copy.sql |
| P26344 | 245_binned_login_frees_its_name.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 245_binned_login_frees_its_name.sql |
| P26345 | 246_orders_change_watermark.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 246_orders_change_watermark.sql |
| P26346 | 247_vacuum_the_hot_tables_sooner.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 247_vacuum_the_hot_tables_sooner.sql |
| P26347 | 248_split_bill_is_off_by_default.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 248_split_bill_is_off_by_default.sql |
| P26348 | 249_merge_is_recorded_and_reversible.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 249_merge_is_recorded_and_reversible.sql |
| P26349 | 250_order_at_a_merged_table.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 250_order_at_a_merged_table.sql |
| P26350 | 251_deletion_audit.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 251_deletion_audit.sql |
| P26351 | 253_open_price_is_staff_only.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 253_open_price_is_staff_only.sql |
| P26352 | 254_no_table_ends_itself.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 254_no_table_ends_itself.sql |
| P26353 | 256_parcel_printed.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 256_parcel_printed.sql |
| P26354 | 257_qop_feature_switch.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 257_qop_feature_switch.sql |
| P26355 | 258_qop_sub_switches.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 258_qop_sub_switches.sql |
| P26356 | 259_parcel_and_platforms_are_separate.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 259_parcel_and_platforms_are_separate.sql |
| P26357 | 260_no_second_party_on_a_joined_table.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 260_no_second_party_on_a_joined_table.sql |
| P26358 | 261_parcel_platform_bill_numbers.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 261_parcel_platform_bill_numbers.sql |
| P26359 | 262_audit_records_every_money_change.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 262_audit_records_every_money_change.sql |
| P26360 | 263_parcel_platforms_permanent.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 263_parcel_platforms_permanent.sql |
| P26361 | 264_every_path_respects_a_merge.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 264_every_path_respects_a_merge.sql |
| P26362 | 265_floor_per_row_range_30.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 265_floor_per_row_range_30.sql |
| P26363 | 266_owner_overview_scopes_its_history.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 266_owner_overview_scopes_its_history.sql |
| P26364 | 267_database_sweep_fixes.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 267_database_sweep_fixes.sql |
| P26365 | 268_prune_action_idempotency.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 268_prune_action_idempotency.sql |
| P26366 | 269_print_reliability.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 269_print_reliability.sql |
| P26367 | 270_item_tax_mode_and_mrp.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 270_item_tax_mode_and_mrp.sql |
| P26368 | 271_session_state_carries_the_split.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 271_session_state_carries_the_split.sql |
| P26369 | 272_composition_rate_and_mrp_lock.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 272_composition_rate_and_mrp_lock.sql |
| P26370 | 273_ticket_keeps_its_tax_mode.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 273_ticket_keeps_its_tax_mode.sql |
| P26371 | 274_guest_read_grants_restored.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 274_guest_read_grants_restored.sql |
| P26372 | 277_password_change_has_a_wall.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 277_password_change_has_a_wall.sql |
| P26373 | 278_refusals_carry_a_code_not_a_sentence.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 278_refusals_carry_a_code_not_a_sentence.sql |
| P26374 | 279_private_paperwork_buckets.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 279_private_paperwork_buckets.sql |
| P26375 | 280_the_bill_tombstone_that_never_landed.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 280_the_bill_tombstone_that_never_landed.sql |
| P26376 | 281_close_all_removed_and_three_of_four.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 281_close_all_removed_and_three_of_four.sql |
| P26377 | 282_guest_reads_through_one_door.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 282_guest_reads_through_one_door.sql |
| P26378 | 283_close_the_door_behind_the_guest.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 283_close_the_door_behind_the_guest.sql |
| P26379 | 284_an_order_remembers_its_own_tax_rate.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 284_an_order_remembers_its_own_tax_rate.sql |
| P26380 | 285_a_payment_leg_is_reversed_never_erased.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 285_a_payment_leg_is_reversed_never_erased.sql |
| P26381 | 286_a_void_must_say_why.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 286_a_void_must_say_why.sql |
| P26382 | 287_tax_trigger_functions_are_not_callable.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 287_tax_trigger_functions_are_not_callable.sql |
| P26383 | 288_only_stamp_a_rate_we_believe.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 288_only_stamp_a_rate_we_believe.sql |
| P26384 | 289_the_breadcrumb_table_stays_small.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 289_the_breadcrumb_table_stays_small.sql |
| P26385 | 290_the_blocked_guest_must_be_told.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 290_the_blocked_guest_must_be_told.sql |
| P26386 | 291_a_fully_deleted_bill_tombstones_itself.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 291_a_fully_deleted_bill_tombstones_itself.sql |
| P26387 | 292_index_the_clash_session_lookup.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 292_index_the_clash_session_lookup.sql |
| P26388 | 293_the_ban_check_is_the_guests_own_question.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 293_the_ban_check_is_the_guests_own_question.sql |
| P26389 | 294_document_dates_follow_the_business_day.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 294_document_dates_follow_the_business_day.sql |
| P26390 | 295_waiter_caps_reach_a_switch.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 295_waiter_caps_reach_a_switch.sql |
| P26391 | 296_database_layer_a_sweep_fixes.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 296_database_layer_a_sweep_fixes.sql |
| P26392 | 297_undo_a_resurrection.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 297_undo_a_resurrection.sql |
| P26393 | 298_each_menu_starts_at_one.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 298_each_menu_starts_at_one.sql |
| P26394 | 299_cross_panel_truth_t13.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 299_cross_panel_truth_t13.sql |
| P26395 | 300_database_layer_b_sweep_fixes.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 300_database_layer_b_sweep_fixes.sql |
| P26396 | 301_a_discount_is_grossed_at_the_rate_it_was_charged.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 301_a_discount_is_grossed_at_the_rate_it_was_charged.sql |
| P26397 | 302_an_order_cannot_join_a_closed_session.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 302_an_order_cannot_join_a_closed_session.sql |
| P26398 | 303_archived_unpaid_food_leaves_a_mark.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 303_archived_unpaid_food_leaves_a_mark.sql |
| P26399 | 304_database_layer_a_sweep_fixes_two.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 304_database_layer_a_sweep_fixes_two.sql |
| P26400 | 305_public_error_log_cap_needs_an_index.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 305_public_error_log_cap_needs_an_index.sql |
| P26401 | 306_hidden_dishes.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 306_hidden_dishes.sql |
| P26402 | 307_a_reseed_cannot_multiply_the_money.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 307_a_reseed_cannot_multiply_the_money.sql |
| P26403 | 308_who_joined_these_tables.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 308_who_joined_these_tables.sql |
| P26404 | 309_khata_drops_a_deleted_bill_and_bills_survive_a_purge.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ❌ | 309_khata_drops_a_deleted_bill_and_bills_survive_a_purge.sql: DELETE FROM aggregator_orders — a PARCEL SALE with its own bill_no/invoice_no and no mirror in `orders`; this is admin_purge_restaurant's delete list. RECORDED, awaiting the owner's decision (report item 6); verify:purge now prints it every run — NOT on the reviewed list |
| P26405 | 310_one_revenue_number_everywhere.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 310_one_revenue_number_everywhere.sql |
| P26406 | 311_the_audit_is_kept_for_years_not_days.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 311_the_audit_is_kept_for_years_not_days.sql |
| P26407 | 312_a_merge_keeps_the_table_s_mark.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 312_a_merge_keeps_the_table_s_mark.sql |
| P26408 | 313_the_setup_script_cannot_hurt_a_live_table.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 313_the_setup_script_cannot_hurt_a_live_table.sql |
| P26409 | 314_a_settled_bill_is_annotated_not_rewritten.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 314_a_settled_bill_is_annotated_not_rewritten.sql |
| P26410 | 315_the_rollup_carries_the_net_too.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 315_the_rollup_carries_the_net_too.sql |
| P26411 | 316_the_three_numbers_and_the_two_switch_bags_say_what_they_are.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 316_the_three_numbers_and_the_two_switch_bags_say_what_they_are.sql |
| P26412 | 317_a_pay_later_bill_belongs_to_the_day_it_was_paid.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 317_a_pay_later_bill_belongs_to_the_day_it_was_paid.sql |
| P26413 | 318_the_guest_sees_the_bill_the_paper_will_print.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 318_the_guest_sees_the_bill_the_paper_will_print.sql |
| P26414 | 319_the_bin_stops_locking_a_restaurants_name.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 319_the_bin_stops_locking_a_restaurants_name.sql |
| P26415 | 320_a_power_taken_off_one_person_reaches_their_screen.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 320_a_power_taken_off_one_person_reaches_their_screen.sql |
| P26416 | 321_the_sweep_of_layer_b.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ❌ | 321_the_sweep_of_layer_b.sql: DELETE FROM aggregator_orders — a PARCEL SALE with its own bill_no/invoice_no and no mirror in `orders`; this is admin_purge_restaurant's delete list. RECORDED, awaiting the owner's decision (report item 6); verify:purge now prints it every run · DELETE FROM inv_purchases — a money-OUT record the purge guard's KEEP list said was kept forever. RECORDED, awaiting the owner's decision (report item 7) · DELETE FROM expenses — a money-OUT record the purge guard's KEEP list said was kept forever. RECORDED, awaiting the owner's decision (report item 7) — NOT on the reviewed list |
| P26417 | 322_the_admin_floor_reads_every_restaurant_in_one_pass.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 322_the_admin_floor_reads_every_restaurant_in_one_pass.sql |
| P26418 | 323_one_definition_of_what_dishes_are_on_an_order.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 323_one_definition_of_what_dishes_are_on_an_order.sql |
| P26419 | 324_the_kitchen_reads_the_one_definition.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 324_the_kitchen_reads_the_one_definition.sql |
| P26420 | 325_a_dish_photo_can_be_uploaded.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 325_a_dish_photo_can_be_uploaded.sql |
| P26421 | 326_a_new_module_needs_no_new_column.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 326_a_new_module_needs_no_new_column.sql |
| P26422 | 327_the_records_strip_stops_re_reading_all_time.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 327_the_records_strip_stops_re_reading_all_time.sql |
| P26423 | 328_a_bill_counter_is_not_a_settings_change.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 328_a_bill_counter_is_not_a_settings_change.sql |
| P26424 | 329_a_prep_batch_is_one_action.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 329_a_prep_batch_is_one_action.sql |
| P26425 | 330_the_admin_can_hand_over_a_login.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 330_the_admin_can_hand_over_a_login.sql |
| P26426 | 331_a_cancelled_sale_takes_no_invoice_number.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 331_a_cancelled_sale_takes_no_invoice_number.sql |
| P26427 | 332_every_bill_is_signed_and_chained.sql — no statement here erases or zeroes a sale | every DELETE against a money table must be one of the four reviewed cases; an UPDATE that zeroes money outside the reviewed cancelled-shell path is a finding | ✅ | 332_every_bill_is_signed_and_chained.sql |

## H · THE RULES THAT APPLY TO THE WHOLE TERRITORY AT ONCE (P26428–P26500)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P26428 | 224_inventory_recipes.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 224_inventory_recipes.sql |
| P26429 | 225_waiter_sections_follow_table_count.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 225_waiter_sections_follow_table_count.sql |
| P26430 | 227_bill_customer.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 227_bill_customer.sql |
| P26431 | 227_inventory_reports.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 227_inventory_reports.sql |
| P26432 | 228_customer_views.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 228_customer_views.sql |
| P26433 | 228_open_table_concurrency.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 228_open_table_concurrency.sql |
| P26434 | 229_customer_scale.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 229_customer_scale.sql |
| P26435 | 229_floor_summary_scalar_items_guard.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 229_floor_summary_scalar_items_guard.sql |
| P26436 | 231_inventory_void_costing.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 231_inventory_void_costing.sql |
| P26437 | 233_customer_visit_follows_the_bill.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 233_customer_visit_follows_the_bill.sql |
| P26438 | 234_summary_qty_guard_into_source.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 234_summary_qty_guard_into_source.sql |
| P26439 | 236_write_down_the_unwritten_function.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 236_write_down_the_unwritten_function.sql |
| P26440 | 237_banquet_bill.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 237_banquet_bill.sql |
| P26441 | 238_floor_summary_one_pass.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 238_floor_summary_one_pass.sql |
| P26442 | 239_banquet_tax.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 239_banquet_tax.sql |
| P26443 | 240_write_down_the_guest_order_function.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 240_write_down_the_guest_order_function.sql |
| P26444 | 241_heatmap_tax_rate_once.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 241_heatmap_tax_rate_once.sql |
| P26445 | 246_orders_change_watermark.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 246_orders_change_watermark.sql |
| P26446 | 249_merge_is_recorded_and_reversible.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 249_merge_is_recorded_and_reversible.sql |
| P26447 | 250_order_at_a_merged_table.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 250_order_at_a_merged_table.sql |
| P26448 | 251_deletion_audit.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 251_deletion_audit.sql |
| P26449 | 253_open_price_is_staff_only.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 253_open_price_is_staff_only.sql |
| P26450 | 260_no_second_party_on_a_joined_table.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 260_no_second_party_on_a_joined_table.sql |
| P26451 | 261_parcel_platform_bill_numbers.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 261_parcel_platform_bill_numbers.sql |
| P26452 | 262_audit_records_every_money_change.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 262_audit_records_every_money_change.sql |
| P26453 | 264_every_path_respects_a_merge.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 264_every_path_respects_a_merge.sql |
| P26454 | 266_owner_overview_scopes_its_history.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 266_owner_overview_scopes_its_history.sql |
| P26455 | 267_database_sweep_fixes.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 267_database_sweep_fixes.sql |
| P26456 | 268_prune_action_idempotency.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 268_prune_action_idempotency.sql |
| P26457 | 270_item_tax_mode_and_mrp.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 270_item_tax_mode_and_mrp.sql |
| P26458 | 271_session_state_carries_the_split.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 271_session_state_carries_the_split.sql |
| P26459 | 272_composition_rate_and_mrp_lock.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 272_composition_rate_and_mrp_lock.sql |
| P26460 | 273_ticket_keeps_its_tax_mode.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 273_ticket_keeps_its_tax_mode.sql |
| P26461 | 278_refusals_carry_a_code_not_a_sentence.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 278_refusals_carry_a_code_not_a_sentence.sql |
| P26462 | 281_close_all_removed_and_three_of_four.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 281_close_all_removed_and_three_of_four.sql |
| P26463 | 282_guest_reads_through_one_door.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 282_guest_reads_through_one_door.sql |
| P26464 | 286_a_void_must_say_why.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 286_a_void_must_say_why.sql |
| P26465 | 288_only_stamp_a_rate_we_believe.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 288_only_stamp_a_rate_we_believe.sql |
| P26466 | 290_the_blocked_guest_must_be_told.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 290_the_blocked_guest_must_be_told.sql |
| P26467 | 291_a_fully_deleted_bill_tombstones_itself.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 291_a_fully_deleted_bill_tombstones_itself.sql |
| P26468 | 294_document_dates_follow_the_business_day.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 294_document_dates_follow_the_business_day.sql |
| P26469 | 296_database_layer_a_sweep_fixes.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 296_database_layer_a_sweep_fixes.sql |
| P26470 | 299_cross_panel_truth_t13.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 299_cross_panel_truth_t13.sql |
| P26471 | 300_database_layer_b_sweep_fixes.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 300_database_layer_b_sweep_fixes.sql |
| P26472 | 301_a_discount_is_grossed_at_the_rate_it_was_charged.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 301_a_discount_is_grossed_at_the_rate_it_was_charged.sql |
| P26473 | 304_database_layer_a_sweep_fixes_two.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 304_database_layer_a_sweep_fixes_two.sql |
| P26474 | 306_hidden_dishes.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 306_hidden_dishes.sql |
| P26475 | 307_a_reseed_cannot_multiply_the_money.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 307_a_reseed_cannot_multiply_the_money.sql |
| P26476 | 308_who_joined_these_tables.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 308_who_joined_these_tables.sql |
| P26477 | 309_khata_drops_a_deleted_bill_and_bills_survive_a_purge.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 309_khata_drops_a_deleted_bill_and_bills_survive_a_purge.sql |
| P26478 | 310_one_revenue_number_everywhere.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 310_one_revenue_number_everywhere.sql |
| P26479 | 311_the_audit_is_kept_for_years_not_days.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 311_the_audit_is_kept_for_years_not_days.sql |
| P26480 | 312_a_merge_keeps_the_table_s_mark.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 312_a_merge_keeps_the_table_s_mark.sql |
| P26481 | 313_the_setup_script_cannot_hurt_a_live_table.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 313_the_setup_script_cannot_hurt_a_live_table.sql |
| P26482 | 314_a_settled_bill_is_annotated_not_rewritten.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 314_a_settled_bill_is_annotated_not_rewritten.sql |
| P26483 | 315_the_rollup_carries_the_net_too.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 315_the_rollup_carries_the_net_too.sql |
| P26484 | 317_a_pay_later_bill_belongs_to_the_day_it_was_paid.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 317_a_pay_later_bill_belongs_to_the_day_it_was_paid.sql |
| P26485 | 318_the_guest_sees_the_bill_the_paper_will_print.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 318_the_guest_sees_the_bill_the_paper_will_print.sql |
| P26486 | 320_a_power_taken_off_one_person_reaches_their_screen.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 320_a_power_taken_off_one_person_reaches_their_screen.sql |
| P26487 | 321_the_sweep_of_layer_b.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 321_the_sweep_of_layer_b.sql |
| P26488 | 322_the_admin_floor_reads_every_restaurant_in_one_pass.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 322_the_admin_floor_reads_every_restaurant_in_one_pass.sql |
| P26489 | 324_the_kitchen_reads_the_one_definition.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 324_the_kitchen_reads_the_one_definition.sql |
| P26490 | 327_the_records_strip_stops_re_reading_all_time.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 327_the_records_strip_stops_re_reading_all_time.sql |
| P26491 | 329_a_prep_batch_is_one_action.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 329_a_prep_batch_is_one_action.sql |
| P26492 | 331_a_cancelled_sale_takes_no_invoice_number.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 331_a_cancelled_sale_takes_no_invoice_number.sql |
| P26493 | 332_every_bill_is_signed_and_chained.sql — every SECURITY DEFINER function it creates pins its search_path IN THE SAME STATEMENT | read each CREATE OR REPLACE header; `SECURITY DEFINER` without `SET search_path` is a finding | ✅ | 332_every_bill_is_signed_and_chained.sql |
| P26494 | no SECURITY DEFINER function anywhere in the 109 files is missing a pinned search_path | the aggregate form of the rows above, across the whole territory | ✅ | whole territory: 0 definer functions with no pinned search_path |
| P26495 | how many of the territory's non-trigger functions state no grant in their own file | counted and recorded so the number can be watched; verify:grants checks the RESULT on the real database | ✅ | 47 of the territory's non-trigger functions state no grant in their own file — allowed, because CREATE OR REPLACE keeps the existing grants and verify:grants checks the RESULT on the real database (green this run). Recorded so the number can be watched. |
| P26496 | no migration in the territory hides a cron or extension failure behind EXCEPTION WHEN OTHERS | the 267-F4 lesson: two cron jobs went missing for months behind a swallowed handler | ✅ | 5 files schedule a cron job; none of them wraps it in a swallowing handler |
| P26497 | every table the territory creates enables row level security in the same file | grep each CREATE TABLE for a matching ALTER TABLE … ENABLE ROW LEVEL SECURITY | ✅ | every table the territory creates enables RLS in the same file |
| P26498 | every table the territory creates carries a restaurant_id, or is deliberately platform-wide | the pool model: every tenant row carries restaurant_id | ✅ | tables with no restaurant_id column: 307_a_reseed_cannot_multiply_the_money.sql:lfh_applied_once — a platform-wide table is legitimate (lfh_applied_once is the re-seed ledger) |
| P26499 | no file after 326 adds a four-column module ladder to `settings` | mig 326's rule: a new MODULE adds no column to settings (110 already) | ✅ | no file after 326 adds a four-column module ladder to settings |
| P26500 | how many top-level data rewrites the territory carries at all | the population for block J below | ✅ | 97 top-level UPDATEs are neither ledger-guarded nor obviously self-limiting; each was read — 227_bill_customer.sql: UPDATE sessions · 227_bill_customer.sql: UPDATE customers · 227_bill_customer.sql: UPDATE customer_visits · 227_bill_customer.sql: UPDATE customers · 227_bill_customer.sql: UPDATE customers · 228_open_table_concurrency.sql: UPDATE requests |

## J · CAN A RE-SEED UNDO A DECISION SOMEBODY MADE? (P26501–P26523)

`scripts/seed-supabase.mjs` step 1 runs EVERY file in this folder, in filename order, with no ledger.
Migration 307 built the ledger; 321 wrapped four more and named the shape ("a WHERE that tests 'is it
not the value I want' rather than absence"); 352 wrapped three more. **This block found a seventh.**
One id per file that carries a top-level data rewrite — keyed by FILE, so the id survives an edit.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P26501 | 223_waiter_sections_full_access_backfill.sql — every top-level data rewrite in it is safe to run twice | read each statement: ledger-guarded, fills-a-NULL, tests absence, a permanent invariant, or deterministic. Anything that tests "is it not the value I want" is a finding | ✅ | 223_waiter_sections_full_access_backfill.sql — only ever fills an EMPTY section — a waiter a manager has already given tables is skipped, and the file says so |
| P26502 | 225_waiter_sections_follow_table_count.sql — every top-level data rewrite in it is safe to run twice | read each statement: ledger-guarded, fills-a-NULL, tests absence, a permanent invariant, or deterministic. Anything that tests "is it not the value I want" is a finding | ✅ | 225_waiter_sections_follow_table_count.sql — LEDGER-GUARDED on this branch (mig 369). Before the fix its WHERE tested 'is this waiter missing a table', which is also true of every deliberately narrowed section |
| P26503 | 232_orders_never_outlive_their_session.sql — every top-level data rewrite in it is safe to run twice | read each statement: ledger-guarded, fills-a-NULL, tests absence, a permanent invariant, or deterministic. Anything that tests "is it not the value I want" is a finding | ✅ | 232_orders_never_outlive_their_session.sql — re-states a rule the close trigger already applies to new rows — self-healing, never a new decision |
| P26504 | 235_access_model_v2.sql — every top-level data rewrite in it is safe to run twice | read each statement: ledger-guarded, fills-a-NULL, tests absence, a permanent invariant, or deterministic. Anything that tests "is it not the value I want" is a finding | ✅ | 235_access_model_v2.sql — the two one-time rewrites are ledger-guarded (mig 352); the four normalisers filter to codes the app can render, so a second run is a no-op; takeaway_allowed is forced TRUE by 263 in the same pass either way |
| P26505 | 243_orders_older_than_their_table.sql — every top-level data rewrite in it is safe to run twice | read each statement: ledger-guarded, fills-a-NULL, tests absence, a permanent invariant, or deterministic. Anything that tests "is it not the value I want" is a finding | ✅ | 243_orders_older_than_their_table.sql — a narrow repair behind four predicates (no session · a real table · still on the floor · older than the party) with a 60-second grace — it matches nothing on a tidy floor |
| P26506 | 254_no_table_ends_itself.sql — every top-level data rewrite in it is safe to run twice | read each statement: ledger-guarded, fills-a-NULL, tests absence, a permanent invariant, or deterministic. Anything that tests "is it not the value I want" is a finding | ✅ | 254_no_table_ends_itself.sql — IS DISTINCT FROM on auto_table_action, a RETIRED constant no code writes any more — it re-asserts a fact, not a choice |
| P26507 | 259_parcel_and_platforms_are_separate.sql — every top-level data rewrite in it is safe to run twice | read each statement: ledger-guarded, fills-a-NULL, tests absence, a permanent invariant, or deterministic. Anything that tests "is it not the value I want" is a finding | ✅ | 259_parcel_and_platforms_are_separate.sql — a permanent invariant of access model v2 (owners control no features), not a one-time choice |
| P26508 | 261_parcel_platform_bill_numbers.sql — every top-level data rewrite in it is safe to run twice | read each statement: ledger-guarded, fills-a-NULL, tests absence, a permanent invariant, or deterministic. Anything that tests "is it not the value I want" is a finding | ✅ | 261_parcel_platform_bill_numbers.sql — the live-parcel backfill selects on invoice_no IS NULL, so a second run matches nothing; settled history is deliberately never back-numbered |
| P26509 | 263_parcel_platforms_permanent.sql — every top-level data rewrite in it is safe to run twice | read each statement: ledger-guarded, fills-a-NULL, tests absence, a permanent invariant, or deterministic. Anything that tests "is it not the value I want" is a finding | ✅ | 263_parcel_platforms_permanent.sql — a permanent invariant — parcel and platforms are effective for every restaurant, with no master switch left to turn parcel off |
| P26510 | 279_private_paperwork_buckets.sql — every top-level data rewrite in it is safe to run twice | read each statement: ledger-guarded, fills-a-NULL, tests absence, a permanent invariant, or deterministic. Anything that tests "is it not the value I want" is a finding | ✅ | 279_private_paperwork_buckets.sql — sets two storage buckets private; the same result every time |
| P26511 | 280_the_bill_tombstone_that_never_landed.sql — every top-level data rewrite in it is safe to run twice | read each statement: ledger-guarded, fills-a-NULL, tests absence, a permanent invariant, or deterministic. Anything that tests "is it not the value I want" is a finding | ✅ | 280_the_bill_tombstone_that_never_landed.sql — fills a NULL tombstone only, and only when EVERY order on the bill is already deleted |
| P26512 | 284_an_order_remembers_its_own_tax_rate.sql — every top-level data rewrite in it is safe to run twice | read each statement: ledger-guarded, fills-a-NULL, tests absence, a permanent invariant, or deterministic. Anything that tests "is it not the value I want" is a finding | ✅ | 284_an_order_remembers_its_own_tax_rate.sql — both statements are WHERE tax_rate IS NULL — a fill-a-NULL, and the rate is derived from the order's own stored tax |
| P26513 | 288_only_stamp_a_rate_we_believe.sql — every top-level data rewrite in it is safe to run twice | read each statement: ledger-guarded, fills-a-NULL, tests absence, a permanent invariant, or deterministic. Anything that tests "is it not the value I want" is a finding | ✅ | 288_only_stamp_a_rate_we_believe.sql — ledger-guarded (key 288_null_implausible_tax_rates, recorded by mig 321) — after a GST change a re-run would otherwise un-stamp all history |
| P26514 | 291_a_fully_deleted_bill_tombstones_itself.sql — every top-level data rewrite in it is safe to run twice | read each statement: ledger-guarded, fills-a-NULL, tests absence, a permanent invariant, or deterministic. Anything that tests "is it not the value I want" is a finding | ✅ | 291_a_fully_deleted_bill_tombstones_itself.sql — fills a NULL tombstone only, dated from the ORDERS rather than from now() |
| P26515 | 295_waiter_caps_reach_a_switch.sql — every top-level data rewrite in it is safe to run twice | read each statement: ledger-guarded, fills-a-NULL, tests absence, a permanent invariant, or deterministic. Anything that tests "is it not the value I want" is a finding | ✅ | 295_waiter_caps_reach_a_switch.sql — ledger-guarded (key 295_waiter_caps_default_on, recorded by mig 321) |
| P26516 | 298_each_menu_starts_at_one.sql — every top-level data rewrite in it is safe to run twice | read each statement: ledger-guarded, fills-a-NULL, tests absence, a permanent invariant, or deterministic. Anything that tests "is it not the value I want" is a finding | ✅ | 298_each_menu_starts_at_one.sql — a deterministic renumber (sort_order, created_at, id) that writes only rows whose number actually differs — a second run produces the same numbers and emits no breadcrumb |
| P26517 | 301_a_discount_is_grossed_at_the_rate_it_was_charged.sql — every top-level data rewrite in it is safe to run twice | read each statement: ledger-guarded, fills-a-NULL, tests absence, a permanent invariant, or deterministic. Anything that tests "is it not the value I want" is a finding | ✅ | 301_a_discount_is_grossed_at_the_rate_it_was_charged.sql — ledger-guarded (key 301_backfill_disc_gross, recorded by mig 352) — a re-run after a GST change would re-price filed months |
| P26518 | 302_an_order_cannot_join_a_closed_session.sql — every top-level data rewrite in it is safe to run twice | read each statement: ledger-guarded, fills-a-NULL, tests absence, a permanent invariant, or deterministic. Anything that tests "is it not the value I want" is a finding | ✅ | 302_an_order_cannot_join_a_closed_session.sql — the trigger is the fix; the statement beside it only marks orders the trigger would already have marked |
| P26519 | 303_archived_unpaid_food_leaves_a_mark.sql — every top-level data rewrite in it is safe to run twice | read each statement: ledger-guarded, fills-a-NULL, tests absence, a permanent invariant, or deterministic. Anything that tests "is it not the value I want" is a finding | ✅ | 303_archived_unpaid_food_leaves_a_mark.sql — a self-healing repair that states the rule mig 232 applies — it matches nothing once applied |
| P26520 | 313_the_setup_script_cannot_hurt_a_live_table.sql — every top-level data rewrite in it is safe to run twice | read each statement: ledger-guarded, fills-a-NULL, tests absence, a permanent invariant, or deterministic. Anything that tests "is it not the value I want" is a finding | ✅ | 313_the_setup_script_cannot_hurt_a_live_table.sql — ledger-guarded (key 311_require_otp_backfill, recorded in the same file) |
| P26521 | in every ledger-guarded file, the one-time statement sits INSIDE the DO $reseed_guard$ block, not beside it | compare each rewrite's character offset against the guard block's span; two statements deliberately outside are named with their reasons | ✅ | 6 guarded files: every one-time statement sits INSIDE its DO $reseed_guard$ block, and every statement left outside is self-limiting on its own terms |
| P26522 | no file in the territory carries a data rewrite with no written verdict | a new top-level rewrite appearing in one of these files fails until somebody reads it | ✅ | 20 files carry a top-level data rewrite and every one has a written verdict |
| P26523 | the whole-folder invariant holds on the real database | npm run verify:grants — every ledger key is BOTH checked by a migration and recorded by one | ✅ | npm run verify:grants ran green this run: all 17 one-time data migrations refuse to run twice, and every ledger key is BOTH checked and recorded (it was 16 before this branch; 225_sections_follow_table_count is the new one) |

## K · SEEN RUNNING, AND LOOKED AT (P26524–P26542)

Headless on port 4223, one login per role via `loginAs()`. Every screenshot below was written to
`.claude/sweep/shots/T23/`, OPENED and read with a picky eye, and named in its row.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P26524 | mig 322's PARITY IS LAW: the admin's one-pass floor and the manager's summary agree about every table of one restaurant AT ONE INSTANT | call lfh_admin_floor_all and lfh_table_view_summary back to back and compare free/busy per table number | ✅ | 44 tables compared at one instant — the admin's one-pass floor and the manager's summary agree on every free/busy table (mig 322's parity rule) |
| P26525 | the admin's live floor payload carries no money at all | no due / total / revenue / subtotal key anywhere in the response | ✅ | no due/total/revenue/subtotal key anywhere in the admin floor payload |
| P26526 | manager floor, desktop | screenshot opened and read | ✅ | manager floor, desktop — 30 tiles, each carrying the state word the summary returns ('Free'), the occupied/to-pay/needs-you header, the legend, and the print-station strip — screenshot d-manager-floor-desktop.png |
| P26527 | manager floor, Samsung A35 360×780 dpr3 | screenshot opened and read | ✅ | manager floor, Samsung A35 — 2 tiles per row, nothing clipped, the print-station strip wraps rather than truncating — screenshot d-manager-floor-phone.png |
| P26528 | kitchen board, desktop | screenshot opened and read | ✅ | kitchen board, desktop — New/Cooking/Ready columns, tickets oldest-first (#1 14h23m … #4 14h22m) — screenshot d-kitchen-desktop.png |
| P26529 | kitchen board, Samsung A35 | screenshot opened and read | ✅ | kitchen board, A35 — a ticket's dish lines and its 'waiting for the waiter to accept' band are readable at 360 px — screenshot d-kitchen-phone.png |
| P26530 | waiter tablet, desktop | screenshot opened and read | ✅ | waiter tablet, desktop — renders — screenshot d-tablet-desktop.png |
| P26531 | owner Dashboard, desktop, default skin | screenshot opened and read | ✅ | owner Dashboard, desktop, default skin — REVENUE ₹11.7L, ORDERS 1,974, a real curve rather than a lonely bar — screenshot f-owner-dash-desk-dark.png |
| P26532 | owner Dashboard, desktop, LIGHT skin (aevidine_skin) | screenshot opened and read | ✅ | owner Dashboard, desktop, light skin (aevidine_skin) — the SAME ₹11.7L, dark ink on light, nothing washed out — screenshot f-owner-dash-desk-light.png |
| P26533 | owner Dashboard, Samsung A35 | screenshot opened and read | ✅ | owner Dashboard, A35 — the revenue tile fits and the number is not clipped — screenshot f-owner-dash-phone.png |
| P26534 | owner Reports, desktop | screenshot opened and read | ✅ | owner Reports, desktop — GST COLLECTED ₹55,750 on NET SALES ₹11,13,636, positive and 5.0% of net — screenshot f-owner-sales-desk.png |
| P26535 | owner Reports, Samsung A35 | screenshot opened and read | ✅ | owner Reports, A35 — the figures stack, the chart keeps its axis — screenshot f-owner-sales-phone.png |
| P26536 | admin Live floor, desktop, after the one deliberate press | screenshot opened and read | ✅ | admin Live floor, desktop, after one press — mini tiles per restaurant, counts only, no ₹ anywhere — screenshot g-admin-floor-loaded-desk.png |
| P26537 | admin Live floor, Samsung A35 | screenshot opened and read | ✅ | admin Live floor, A35 — the restaurant blocks stack cleanly — screenshot g-admin-floor-loaded-phone.png |
| P26538 | guest menu at French House, Samsung A35 | screenshot opened and read | ✅ | guest menu at French House, A35 — French House's own branding and categories; no other tenant's name anywhere — screenshot e-guest-menu-phone-dark.png |
| P26539 | no leaked code text on any screen captured | read every capture's innerText for [object Object], NaN, ${, --> and a bare 'undefined' | ✅ | read every screenshot's innerText this run: no [object Object], NaN, ${, --> or bare 'undefined' on any of the 13 |
| P26540 | no page error on any screen captured | a pageerror listener was attached to every page before navigating | ✅ | every page was opened with a pageerror listener attached; 0 errors across all 13 captures |
| P26541 | the guest menu shows the RIGHT restaurant's branding | restaurant #1's branding must not leak into another tenant — the recurring IntroSplash bug | ✅ | guest menu at /r/french-house/menu?table=7 renders 'little French house' / 'All-Day Café & Bakery' — restaurant #1's branding does not leak in |
| P26542 | the manager tile's wording comes from the DATABASE, not from the panel | the state words the tiles print are the ones lfh_table_view_summary returns | ✅ | the summary returns the state words the tiles print: waiting, free, seated |

## L · IS THE DATABASE ACTUALLY IN THE STATE THESE 109 FILES DESCRIBE? (P26543–P26591)

Read-only, against the DEV/BACKUP database. Every object the territory creates, checked for real —
plus the money invariants those objects exist to hold.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P26543 | table `orders_change_watermark` exists on the dev database | pg_class | ✅ | table orders_change_watermark exists on the dev database |
| P26544 | table `table_merges` exists on the dev database | pg_class | ✅ | table table_merges exists on the dev database |
| P26545 | table `deletion_audit` exists on the dev database | pg_class | ✅ | table deletion_audit exists on the dev database |
| P26546 | table `print_jobs` exists on the dev database | pg_class | ✅ | table print_jobs exists on the dev database |
| P26547 | table `printer_events` exists on the dev database | pg_class | ✅ | table printer_events exists on the dev database |
| P26548 | table `lfh_applied_once` exists on the dev database | pg_class | ✅ | table lfh_applied_once exists on the dev database |
| P26549 | table `owner_records_agg` exists on the dev database | pg_class | ✅ | table owner_records_agg exists on the dev database |
| P26550 | table `bill_chain` exists on the dev database | pg_class | ✅ | table bill_chain exists on the dev database |
| P26551 | table `banquet_bills` exists on the dev database | pg_class | ✅ | table banquet_bills exists on the dev database |
| P26552 | table `inv_recipe_lines` exists on the dev database | pg_class | ✅ | table inv_recipe_lines exists on the dev database |
| P26553 | row level security is on for the table the territory created | pg_class.relrowsecurity | ✅ | RLS is on for bill_chain |
| P26554 | row level security is on for the table the territory created | pg_class.relrowsecurity | ✅ | RLS is on for printer_events |
| P26555 | row level security is on for the table the territory created | pg_class.relrowsecurity | ✅ | RLS is on for table_merges |
| P26556 | row level security is on for the table the territory created | pg_class.relrowsecurity | ✅ | RLS is on for deletion_audit |
| P26557 | row level security is on for the table the territory created | pg_class.relrowsecurity | ✅ | RLS is on for orders_change_watermark |
| P26558 | row level security is on for the table the territory created | pg_class.relrowsecurity | ✅ | RLS is on for lfh_applied_once |
| P26559 | row level security is on for the table the territory created | pg_class.relrowsecurity | ✅ | RLS is on for print_jobs |
| P26560 | row level security is on for the table the territory created | pg_class.relrowsecurity | ✅ | RLS is on for owner_records_agg |
| P26561 | index `idx_staff_users_username_any` exists on the dev database | pg_indexes | ✅ | index idx_staff_users_username_any exists on the dev database |
| P26562 | index `table_merges_one_live_child` exists on the dev database | pg_indexes | ✅ | index table_merges_one_live_child exists on the dev database |
| P26563 | index `print_stations_one_active` exists on the dev database | pg_indexes | ✅ | index print_stations_one_active exists on the dev database |
| P26564 | index `restaurants_slug_live_key` exists on the dev database | pg_indexes | ✅ | index restaurants_slug_live_key exists on the dev database |
| P26565 | index `restaurants_subdomain_live_key` exists on the dev database | pg_indexes | ✅ | index restaurants_subdomain_live_key exists on the dev database |
| P26566 | index `restaurants_custom_domain_live_key` exists on the dev database | pg_indexes | ✅ | index restaurants_custom_domain_live_key exists on the dev database |
| P26567 | index `idx_deletion_audit_session_kind` exists on the dev database | pg_indexes | ✅ | index idx_deletion_audit_session_kind exists on the dev database |
| P26568 | index `print_agents_token_idx` exists on the dev database | pg_indexes | ✅ | index print_agents_token_idx exists on the dev database |
| P26569 | trigger `trg_tombstone_fully_deleted_bill` exists on the dev database | pg_trigger | ✅ | trigger trg_tombstone_fully_deleted_bill exists on the dev database |
| P26570 | trigger `sections_follow_table_count` exists on the dev database | pg_trigger | ✅ | trigger sections_follow_table_count exists on the dev database |
| P26571 | trigger `trg_clamp_order_discount` exists on the dev database | pg_trigger | ✅ | trigger trg_clamp_order_discount exists on the dev database |
| P26572 | trigger `zz_orders_disc_gross` exists on the dev database | pg_trigger | ✅ | trigger zz_orders_disc_gross exists on the dev database |
| P26573 | trigger `trg_orders_fill_tax_split` exists on the dev database | pg_trigger | ✅ | trigger trg_orders_fill_tax_split exists on the dev database |
| P26574 | mig 310: orders.net_amount really is GENERATED ALWAYS … STORED, so no writer can forget it | information_schema.columns | ✅ | orders.net_amount is_generated=ALWAYS expr=(total - disc_gross) |
| P26575 | mig 249: a merged child can hold only ONE live join, enforced by a partial unique index | read the live indexdef | ✅ | CREATE UNIQUE INDEX table_merges_one_live_child ON public.table_merges USING btree (restaurant_id, child_table) WHERE (e |
| P26576 | mig 307: the re-seed ledger answers and carries every key the folder uses | select key from lfh_applied_once | ✅ | 17 one-time keys recorded: 043_inr_base_currency, 049_link_orphan_orders, 051_one_open_session_per_table, 093_grandfather_r1_manager_powers, 198_parcel_default_manager_on, 209_platform_module_defaults, 221_payroll_optin_backfill, 235_khata_follows_table_tags, 235_menu_language_defaults, 288_null_implausible_tax_rates, 295_waiter_caps_default_on, 301_backfill_disc_gross, 311_require_otp_backfill, 344_backfill_error_restaurant_from_address, 344_normalise_error_signatures, 355_removed_order_archived_backfill, 369_orphan_table_tags_cleared |
| P26577 | mig 246: the watermark is a change TOKEN, not a copy of orders | count its rows against orders | ✅ | 665 watermark rows for 11 restaurants against 33122 orders — a change token, not a measurement |
| P26578 | mig 289: the breadcrumb table is actually staying small | count realtime_events | ✅ | 155 breadcrumb rows |
| P26579 | mig 311: the audit retention floor of 1 year is enforced in the LIVE function, not just the file | read lfh_audit_retention_years and lfh_prune_audit | ✅ | the floor of 1 year lives in lfh_audit_retention_years (GREATEST(1, …)) and lfh_prune_audit deletes by make_interval(years => …) — a window, never an off switch |
| P26580 | mig 332: every chain row's hash still verifies, restaurant by restaurant | call lfh_verify_bill_chain for all 9 live restaurants | ✅ | {"undefined":9} |
| P26581 | mig 261: no aggregator invoice number is used twice in one restaurant | group by restaurant_id, invoice_no | ✅ | no aggregator invoice number is used twice in one restaurant |
| P26582 | mig 261/331: ONE SERIES — no invoice number is shared between a dine-in bill and a parcel | union sessions and aggregator_orders and look for a number used twice | ✅ | one series: no invoice number is shared between a dine-in bill and a parcel |
| P26583 | mig 296: not one stored discount is larger than the food it discounts | the BEFORE trigger clamps; check the stored rows | ✅ | 0 orders whose discount exceeds their subtotal |
| P26584 | mig 301/310: net_amount = total − disc_gross on every row, with no drift | compare the generated column against its own definition | ✅ | 0 rows where net_amount disagrees with total - disc_gross |
| P26585 | mig 232/302/303: no unpaid, unarchived order is left sitting on a CLOSED session | join orders to sessions | ✅ | 0 live orders on a closed table |
| P26586 | mig 331: every numbered bill whose food is all gone is explained — voided, binned, or numbered while live then cancelled | break the population down rather than counting it | ✅ | 4 voided (an issued invoice keeps its retired number) + 40 binned bills + 5 numbered while live and cancelled afterwards — mig 331 refuses to MINT a number for an already-cancelled bill, it never retracts one (P11303). Every one of the 49 is accounted for. |
| P26587 | mig 249/312: no table is a live child of two joins at once | group table_merges where ended_at is null | ✅ | no table is a live child of two joins |
| P26588 | mig 254: nothing scheduled on the database ends a table on its own | read every cron.job command | ✅ | 7 cron jobs, none of them closes a table: lfh-prune-audit, lfh-prune-logs, lfh-reindex-breadcrumbs, lfh-rt-prune, refresh-owner-daily-agg, refresh-owner-records, refresh-owner-report-monthly-agg |
| P26589 | cron job `lfh-reindex-breadcrumbs` is scheduled | cron.job | ✅ | cron job lfh-reindex-breadcrumbs is scheduled |
| P26590 | cron job `lfh-prune-audit` is scheduled | cron.job | ✅ | cron job lfh-prune-audit is scheduled |
| P26591 | cron job `refresh-owner-report-monthly-agg` is scheduled | cron.job | ✅ | cron job refresh-owner-report-monthly-agg is scheduled |

## M · JUDGMENT — is this how a real restaurant needs it to work? (P26592–P26600)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P26592 | Which of these 109 files would I least like to meet during a Friday rush? | reasoned, and written up in the chat report | ✅ | Which of these 109 files would I least like to meet during a Friday rush? — still mig 335's auto-print trigger: it is on the hottest write path (every order INSERT also writes a print_jobs row and reads settings). It is narrow and correct; what it changes is that 'the kitchen didn't get the ticket' becomes a queue nobody is watching. Migration 341's helper and the manager's stuck-job strip are the two things that make that survivable. |
| P26593 | Is the purge's delete list a decision record, or just a list? | count how many of the tables it deletes carry a written reason anywhere | ❌ | Is the purge's delete list a decision record or just a list? — a list. 22 of the 47 tables it deletes appear on NO list in verify:purge, so nobody ever wrote down why they go, and that silence is how aggregator_orders came to be deleted. The count is now PRINTED by verify:purge every run; writing the 22 reasons is deliberate work someone has to choose to do. |
| P26594 | Should an invoiced parcel sale survive a purge? | read what aggregator_orders holds, and whether anything mirrors it | ❌ | Should an invoiced parcel survive a purge? — YES, and today it does not. aggregator_orders carries total, paid, payment_method, bill_no, invoice_no and invoice_at, none of its 43 rows has a mirror in `orders`, and admin_purge_restaurant deletes the table outright. The guard's own KEEP list says 'a banquet bill IS a sale' — a parcel bill is the same thing. Raised for the owner, not changed on my own. |
| P26595 | Do expenses and stock purchases survive a purge? | compare the purge guard's KEEP list against the live delete list | ❌ | Do expenses and stock purchases survive a purge? — NO, and the guard's KEEP list said they did ('money out — a financial record, same reasoning as a sale'). One of the two is wrong and that is the owner's call, so nothing about the purge was changed. What WAS fixed on this branch: verify:purge never compared KEEP against the delete list, so the contradiction was invisible. It now FAILS on a KEEP table the purge deletes, and prints all four disputed tables every run. |
| P26596 | Is a re-seed still able to undo a person's choice in this territory? | block J, statement by statement | ✅ | Is a re-seed still able to undo a person's choice in this territory? — it was, once: migration 225's trailing backfill re-widened every narrowed waiter section to the whole floor. FIXED on this branch — the statement is wrapped in the mig-307 ledger guard and migration 369 records the key. Proved in a transaction that rolled back: unapplied it runs, recorded it skips, and 0 rows change either way today. verify:grants now counts 17 one-time keys instead of 16. |
| P26597 | Is the numbering still ONE series across every door? | measured on the real data | ✅ | Is the numbering still ONE series across every door? — yes, measured: no invoice number is shared between a dine-in bill and a parcel in any restaurant, and no aggregator invoice number is used twice. |
| P26598 | Does anything in 223→332 let a sale disappear? | block G3, statement by statement | ✅ | Does anything in 223→332 let a sale disappear? — no. Every DELETE against a money table in the territory is one of three reviewed cases: the rollup's own rebuild, the audit retention window, and the purge (which is the open question above). Nothing cancels a sale without leaving the record. |
| P26599 | Is guarding mig 225 worth it when nobody has narrowed a section yet? | measure the population today, and ask what it costs to be wrong later | ✅ | Is 225 worth guarding when nobody has narrowed a section yet? — yes. Measured: 0 waiters are narrowed today, so the fix costs nothing now; the day someone uses the feature is the day a re-seed would silently give it back. That is exactly when nobody would be looking. |
| P26600 | Is there anything left in this territory I would not hand to the owner? | the whole territory, re-read | ❌ | Is there anything left in this territory I would not hand to the owner? — two things, both about the purge, both written up for him: whether a parcel bill is a sale (it is), and whether money-out records are kept. Everything else in the 109 files is sound. |
