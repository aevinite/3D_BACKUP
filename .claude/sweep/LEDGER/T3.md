# Sweep 6 — Terminal 3 ledger · P01001–P01500

**Territory:** the guest basket, the table session and placing an order.
`components/CartPanel.tsx` · `MiniCart.tsx` · `SessionGate.tsx` · `SessionCartSync.tsx` ·
`SessionOwner.tsx` · `SessionStatusWidget.tsx` · `SessionTableBill.tsx` · `OrderConfirmModal.tsx` ·
`OrderTracker.tsx` · `ChefCallButton.tsx` · `ChefPopup.tsx` · `CustomerGreeter.tsx` ·
`GuestOutboxChip.tsx` · `lib/menu.ts` · `lib/tenantStorage.ts` · `lib/restaurant-context.tsx` ·
`lib/guestOutbox.ts`

**Branch:** `sweep6/t3-guest-cart-session` · **worktree** `~/Documents/Projects/sweep6/T3` ·
**port** 4103 (never 4000).

## How the 500 are budgeted

| block | ids | count | share | what it is |
|-------|-----|-------|-------|------------|
| 1 | P01001–P01200 | 200 | 40% | reading the code for correctness, file by file |
| 2 | P01201–P01300 | 100 | 20% | conformance to the project rules named in the prompt + CLAUDE.md |
| 3 | P01301–P01375 | 75 | 15% | watching it run — headless Playwright against my own port 4103 |
| 4 | P01376–P01425 | 50 | 10% | screenshots I actually Read: desktop 1280×800 **and** Samsung A35 360×780 dpr3 |
| 5 | P01426–P01475 | 50 | 10% | tracing one change across panels (guest → manager/kitchen/tablet → server) |
| 6 | P01476–P01500 | 25 | 5% | judgment: does a real restaurant need it to work this way |

Every file in the territory has at least one row; the files named in the scope have ten or more.

Result key: ✅ pass · ❌ real problem (see `T3-findings.md`) · ⏭ skipped, with the reason written in.

---

## Block 1 — reading the code for correctness (P01001–P01200)

### lib/restaurant-context.tsx (P01001–P01012)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01001 | `RestaurantProvider` resolves the active restaurant for EVERY guest door, including `/q/<code>` and `/view/<folder>` — not only `/r/<slug>` | read the slug derivation; assert a path with no `/r/` prefix does not silently answer "restaurant #1" when the tab is pinned to another tenant | ❌ · s7 ✅ | FIX-1 — `/q/<code>` answered restaurant #1 for every global widget |
| P01002 | Bare `/menu` and `/item` still resolve to restaurant #1 (they ARE #1's own URLs) | read the path test; assert `/menu` and `/item/x` map to `DEFAULT_RESTAURANT_SLUG`/`DEFAULT_RESTAURANT_ID` | ✅ · s7 ✅ | held before and after FIX-1 |
| P01003 | The slug is available SYNCHRONOUSLY so a widget can build a `/r/<slug>/…` link before the id lands | read `slug`; assert it is never `undefined` on first render | ✅ · s7 ✅ | |
| P01004 | `ready` is false while a tenant id is still being looked up, and true the moment the answer (or the fallback) is known | read the `ready` transitions on success AND on `.catch` | ✅ · s7 ✅ | flips true on failure too — deliberate |
| P01005 | A failed slug lookup can never leave a widget waiting forever | read `.catch(() => setReady(true))` | ✅ · s7 ✅ | |
| P01006 | `name` is reset to null on a tenant change so a previous restaurant's name cannot flash | read `setName(null)` at the top of the resolve effect | ✅ · s7 ✅ | |
| P01007 | The resolve effect is guarded by an `alive` flag so a late reply for restaurant A cannot land on restaurant B | read the cleanup `alive = false` | ✅ · s7 ✅ | |
| P01008 | The context value is memoised so every consumer does not re-render on an unrelated parent render | read `useMemo` deps `[id, slug, name, ready]` | ✅ · s7 ✅ | |
| P01009 | There is exactly ONE rule deciding "which restaurant is this tab on", shared with `lib/tenantStorage.ts` | grep both files; assert the provider and `tenantSlug()` cannot disagree | ❌ · s7 ✅ | FIX-1 — two rules existed; the provider now calls `tenantSlug()` |
| P01010 | `useRestaurantId()` and `useRestaurantMeta()` read the same context object (no second provider) | grep `createContext` in the file; assert one | ✅ · s7 ✅ | |
| P01011 | Rendering on the server cannot read `sessionStorage` (it would crash the build) | read every `sessionStorage` access; assert it is inside an effect or `typeof window` guarded | ✅ · s7 ✅ | after FIX-1: pin is read only inside the effect |
| P01012 | The provider does not fetch anything for restaurant #1's own routes (no wasted lookup) | read the early return before `getRestaurantBySlug` | ✅ · s7 ✅ | |

### lib/tenantStorage.ts (P01013–P01022)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01013 | Every per-restaurant guest key is suffixed with the tenant slug | read `tkey()`; assert `${base}:${tenantSlug()}` | ✅ · s7 ✅ | |
| P01014 | `/r/<slug>` pins the tab's tenant so a later non-prefixed page in the same tab stays on that restaurant | read the `sessionStorage.setItem(LAST_SLUG_KEY, …)` on the `/r/` branch | ✅ · s7 ✅ | |
| P01015 | Bare `/menu` and `/item` re-pin the tab to restaurant #1 even if it visited another restaurant earlier | read the second branch | ✅ · s7 ✅ | |
| P01016 | A non-prefixed page (`/q/<code>`, `/view/<folder>`) uses the pinned tenant, never a guess | read the third branch | ✅ · s7 ✅ | `/q` pins it server-side before hydration |
| P01017 | The legacy-key migration runs at most once per device and never overwrites an already-scoped value | read `migrateLegacyOnce`; assert the `localStorage.getItem(scoped) === null` guard | ✅ · s7 ✅ | |
| P01018 | The legacy migration deletes the old unscoped key so no other restaurant can read it | read `localStorage.removeItem(base)` | ✅ · s7 ✅ | |
| P01019 | Every read/write is try/catch'd so private mode or a full quota cannot crash the menu | read `tget`/`tset`/`tremove`/`tgetFor`/`tsetFor` | ✅ · s7 ✅ | |
| P01020 | `tgetFor`/`tsetFor` let the outbox write an order's tracker entry under the order's OWN restaurant, not the page's | read the explicit-slug variants and their caller in `guestOutbox.recordActive` | ✅ · s7 ✅ | |
| P01021 | `isTKey` only matches this tab's restaurant, so another restaurant's tab writing its own copy cannot wake us | read `isTKey`; assert it compares against `tkey(base)` | ✅ · s7 ✅ | |
| P01022 | Device-wide preferences (theme, language, currency, device id) are deliberately NOT tenant-scoped | grep the LEGACY_KEYS list; assert none of those four is scoped | ✅ · s7 ✅ | deliberate — belongs to the phone, not the restaurant |

### lib/guestOutbox.ts (P01023–P01056)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01023 | A saved order is written to IndexedDB, and the caller is told honestly whether it really reached storage | read `idbWrite` returning true/false and `enqueueGuestOrder` returning `persisted` | ✅ · s7 ✅ | |
| P01024 | The "Saved — we'll send it automatically" wording is only used when `persisted` is true | grep `q.persisted ?` in CartPanel / SessionGate / ChefPopup | ✅ · s7 ✅ | all three branch on it |
| P01025 | An order saved for later reuses the SAME at-most-once id as the online attempt that lost its reply | read `actionId` threading from CartPanel → `enqueueGuestOrder` | ✅ · s7 ✅ | |
| P01026 | A queue that holds anything ALWAYS has a timer behind it | read `ensureRetry()` and its call sites (enqueue, retry, restore, flush finally) | ✅ · s7 ✅ | |
| P01027 | Reporting offline does not drop the last timer | read `if (isOffline()) { scheduleRetry(false); return; }` | ✅ · s7 ✅ | the scar this line exists for |
| P01028 | The retry beat backs off and is jittered per phone, so N phones do not pulse the server together | read `scheduleRetry`'s `0.75 + Math.random() * 0.5` and the ×2 growth | ✅ · s7 ✅ | |
| P01029 | The timer stops entirely when the queue is empty | read `if (!queued.length) { retryStep = 0; … return; }` | ✅ · s7 ✅ | |
| P01030 | A server `retryAfter` hint can only LENGTHEN the wait, never shorten it | read `Math.max(base…, serverAskedWaitMs)` in `scheduleRetry` | ✅ · s7 ✅ | |
| P01031 | A server `retryAfter` is capped so the queue always comes back within 5 minutes | read `SERVER_WAIT_CAP_MS` | ✅ · s7 ✅ | |
| P01032 | Every send carries a deadline, guarded against an older phone where reading `AbortSignal.timeout` throws | read `sendDeadline()` | ✅ · s7 ✅ | |
| P01033 | A request that never completes is bounded — it cannot retry in silence for the life of the tab | read `NET_MAX_TRIES` and the catch branch | ✅ · s7 ✅ | |
| P01034 | A 5xx is bounded the same way | read `SERVER_MAX_TRIES` and the `res.status >= 500` branch | ✅ · s7 ✅ | |
| P01035 | A repeating 409 "still handling this id" is bounded too | read `BUSY_MAX_TRIES` | ✅ · s7 ✅ | |
| P01036 | All three attempt counters are PERSISTED, so a reload cannot reset them to zero and retry forever | read `await persist(item)` on each `idx++` path | ✅ · s7 ✅ | |
| P01037 | "Try again" clears all THREE counters, not just the 5xx one | read `retryGuestFailed`: `tries = 0; netTries = 0; busyTries = 0` | ✅ · s7 ✅ | |
| P01038 | "Try again" also resets the backoff so it goes now rather than in two minutes | read `retryStep = 0` in `retryGuestFailed` | ✅ · s7 ✅ | |
| P01039 | One stuck order does not hold the next — the queue is WALKED, not head-blocked | read the `while (idx < queued.length)` loop and every `idx++` | ✅ · s7 ✅ | |
| P01040 | The response body is read exactly once (a re-read yields null and loses a clash message) | read the single `await res.json()` before the branches | ✅ · s7 ✅ | |
| P01041 | A 409 carrying a clash message surfaces the server's plain sentence to the diner | read `j?.clash?.plain` branch | ✅ · s7 ✅ | |
| P01042 | Every refusal that reaches this file is worded in the past tense ("while you were offline") | read `reasonMsg(…, { queued: true })` at every `moveToFailed` call | ✅ · s7 ✅ | |
| P01043 | A duplicate reply that says `ok:false` is surfaced as a refusal, not silently removed | read the `if (j.ok === false)` guard inside the duplicate branch | ✅ · s7 ✅ | |
| P01044 | An unrecognised refusal code is never echoed to a diner | read `reasonMsg`'s `default:` | ✅ · s7 ✅ | |
| P01045 | `rate_limited` never tells a diner to try again (it would trip the same limit and alert the owner) | read `case "rate_limited"` | ✅ · s7 ✅ | |
| P01046 | Callers branch on the server's CODE, never on the prose of its message | read `refusalOf()` and grep for `/sold_out/i.test` style matching in the callers | ✅ · s7 ✅ | one regex, in one place |
| P01047 | `unknown_item` resolves the server's id to a NAME off the basket, or names nothing | read `dishFor()` | ✅ · s7 ✅ | |
| P01048 | `dishFor` is handed the list that actually has names on it (`lines`), not the server payload (`items`) | read `namedLines()` and every `dishFor(` call | ✅ · s7 ✅ | |
| P01049 | "Order the rest" drops by line ID where one is known, falling back to the title | read `orderRestWithout` / `blockedLineId` | ✅ · s7 ✅ | |
| P01050 | "Order the rest" refuses rather than re-queueing an identical basket it could not change | read `if (keptLines.length === allLines.length) return { ok: false … }` | ✅ · s7 ✅ | |
| P01051 | "Order the rest" keeps the REAL quantities in the diner's own summary | read `qtyOf()` | ✅ · s7 ✅ | |
| P01052 | "Order the rest" mints a NEW at-most-once id (the old one carries the server's memory of the refusal) | read that it calls `enqueueGuestOrder` with no `actionId` | ✅ · s7 ✅ | |
| P01053 | A stale waiter call is never delivered — checked on the phone before it leaves | read `STALE_CALL_MS` in the flush loop | ✅ · s7 ✅ | |
| P01054 | Repeated bell taps in a dead spot collapse into ONE saved call, keeping the newest note | read `enqueueGuestCall`'s `sameTable` collapse | ✅ · s7 ✅ | |
| P01055 | One phone cannot pile up unbounded saved work; the OLDEST is moved to failed and the diner told | read `MAX_QUEUED` and both `while (queued.length > MAX_QUEUED)` loops | ✅ · s7 ✅ | |
| P01056 | A phone that never queued anything does not gain an IndexedDB database | read the `indexedDB.databases()` probe in `ensureStarted` | ✅ · s7 ✅ | |

### lib/menu.ts (P01057–P01068)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01057 | Every guest order goes through our own endpoint, so the at-most-once guard and the deadline always apply | read `postGuestOrder` and assert `createOrder`/`placeSessionOrderSafe` both call it | ✅ · s7 ✅ | |
| P01058 | "The restaurant couldn't take it" (dropped / timeout / 5xx / 409-retry) is a BUSY error the caller saves | read `busyError` and its four throw sites | ✅ · s7 ✅ | |
| P01059 | A refusal (sold out, table closed, over a limit) is a plain error the diner must see — never queued | read the final `throw new Error("Order failed: …")` | ✅ · s7 ✅ | |
| P01060 | Reading `AbortSignal.timeout` cannot be mis-reported as "the restaurant is busy" on an older phone | read `orderDeadline()`'s try/catch returning `undefined` | ✅ · s7 ✅ | |
| P01061 | No price ever travels from the browser with an order | read `OrderInput` and the bodies built in `createOrder` / `placeSessionOrderSafe` | ✅ · s7 ✅ | |
| P01062 | `getSettings` is cached per restaurant AND dedups simultaneous callers into one request | read `settingsCache` + `settingsInflight` | ✅ · s7 ✅ | ~9 widgets, one read |
| P01063 | A realtime toggle can force a genuine re-read (a cache in front of a breadcrumb is where updates die) | read `invalidateSettings()` and assert it drops both maps | ✅ · s7 ✅ | |
| P01064 | Settings degrade rather than break when the server omits a key | read every `data ? … : default` mapping | ✅ · s7 ✅ | |
| P01065 | The tax rate the cart quotes comes from ONE rule (`lib/tax.ts`), never a second formula here | read `effectiveTaxRate` / `priceTaxMode` / `taxRulesOf` | ✅ · s7 ✅ | |
| P01066 | `DEFAULT_TAX_RULES` is today's behaviour exactly, so the quote never changes shape while settings load | read the constant | ✅ · s7 ✅ | |
| P01067 | Open-price and hidden dishes are filtered out of the guest read, and their own pages 404 | read `getMenuItems`'s filter and `getMenuItem`'s two early returns | ✅ · s7 ✅ | |
| P01068 | Every read is column-listed and `.limit()`ed (egress rule) | read all four `supabase.from(...)` calls | ✅ · s7 ✅ | |

### components/CartPanel.tsx (P01069–P01094)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01069 | A corrupt or legacy saved cart cannot crash the bill | read `normalize()`'s filter + defaults | ✅ · s7 ✅ | |
| P01070 | A saved quantity is clamped to a whole 1..99 so the quote can never exceed what the kitchen makes | read the `Math.min(99, Math.max(1, Math.floor(…)))` | ✅ · s7 ✅ | |
| P01071 | The "+" at the 99 ceiling SAYS so instead of returning in silence | read the toast in `increment` | ✅ · s7 ✅ | the tap-guard rule |
| P01072 | `commit()` is the only thing that changes the cart, and it always announces `lfh:cart-updated` | grep every `saveCart`/`setCart` pair | ✅ · s7 ✅ | |
| P01073 | The full menu is fetched lazily on the first cart OPEN, not on every guest page load | read `menuLoadedRef` + `loadMenuOnce` | ✅ · s7 ✅ | |
| P01074 | The lazy menu fetch reuses the server-cached bundle, not a fresh `select *` | read the `fetch('/api/r/<slug>/menu-data')` | ✅ · s7 ✅ | |
| P01075 | A failed menu fetch lets a later open retry rather than wedging | read `.catch(() => { menuLoadedRef.current = false; })` | ✅ · s7 ✅ | |
| P01076 | Pruning stale cart lines reads localStorage, not the `cart` state, so an async fetch cannot act on a stale closure | read `pruneCartToMenu` | ✅ · s7 ✅ | |
| P01077 | Pruning only ever runs on a NON-EMPTY menu, so a bad payload cannot wipe a real basket | read `if (items.length) pruneCartToMenu(items)` | ✅ · s7 ✅ | |
| P01078 | Pruning SAYS what it removed | read the toast in `pruneCartToMenu` | ✅ · s7 ✅ | |
| P01079 | A cross-tab cart change reaches this tab (the same-tab event does not cross tabs) | read `handleStorageCart` + `isTKey` | ✅ · s7 ✅ | |
| P01080 | A cross-tab wake-up ignores ANOTHER restaurant's scoped key | read `isTKey(e.key, "lfh_cart")` | ✅ · s7 ✅ | |
| P01081 | The order-wide allergy list is not restored when the restaurant has allergies switched OFF | read the `if (features.allergies)` restore branch | ✅ · s7 ✅ | |
| P01082 | The first persist of `declared` is skipped so the restore cannot be clobbered by the empty default | read `declaredHydrated` | ✅ · s7 ✅ | |
| P01083 | Nothing gated by a switch reaches the wire from a line saved before the switch was turned off | read `orderItems()` and `allergyPayload()` | ✅ · s7 ✅ | last gate before the wire |
| P01084 | The at-most-once key changes when the ALLERGY list changes, not only the items | read `sig = JSON.stringify({ t, i, a })` | ✅ · s7 ✅ | |
| P01085 | A double tap on Place Order cannot fire two orders before React disables the button | read `placingRef` (sync) alongside `placing` (state) | ✅ · s7 ✅ | |
| P01086 | The sold-out guard names the exact dish instead of a generic "try again" | read the `soldLines` branch | ✅ · s7 ✅ | |
| P01087 | The session path's `onDone` listener only reacts to ITS OWN action and cannot be stolen by a gated add | read `if (d?.action !== "order") return;` before `removeEventListener` | ✅ · s7 ✅ | |
| P01088 | A cancelled or refused session order re-enables the button (the gate always reports with `action`) | trace every `fireDone` in SessionGate; assert each carries `action` | ✅ · s7 ✅ | |
| P01089 | The session path sends `lines` (id AND name) so a refusal can name a dish and "order the rest" can work | read the `lfh:session-do` payload | ✅ · s7 ✅ | |
| P01090 | A busy server on the QR path saves the basket under the SAME key rather than losing it | read the `isServerBusy(err) && orderKeyRef.current` branch | ✅ · s7 ✅ | |
| P01091 | If saving on the device ALSO fails, the diner gets the honest error, not a false promise | read the inner `catch` that falls through | ✅ · s7 ✅ | |
| P01092 | The stored order total is USD and the displayed total is display-currency — one domain each | read `totalUsd` vs `total` and where each is written | ✅ · s7 ✅ | the ₹48,550 scar |
| P01093 | GST is added only to lines priced NET; a tax-inclusive or MRP line is never taxed twice | read `onTopBase` (splitBill over `excl` lines only) | ✅ · s7 ✅ | |
| P01094 | A ₹0 GST row is removed rather than printed, and the reason is said instead | read `showTaxRow` and the explanatory line under the total | ✅ · s7 ✅ | |

### components/SessionGate.tsx (P01095–P01118)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01095 | The restaurant id a handler uses is the one on screen when it fires, not the one captured at mount | read `ridRef` + its sync effect; assert every server call reads `ridRef.current` | ✅ · s7 ✅ | the 2026-07-05 scar |
| P01096 | The restaurant's SETTINGS are also the ones on screen when the flow runs | read `settingsRef` caching | ❌ · s7 ✅ | FIX-3 — cached for the life of the mount, never keyed by restaurant |
| P01097 | The gate header shows the restaurant's OWN name, never a hardcoded brand | read `brandLabel` | ✅ · s7 ✅ | |
| P01098 | Dismissing the sheet always reports the action as cancelled so the caller's button un-sticks | read `close()` → `fireDone({ ok:false, reason:"cancelled", action })` | ✅ · s7 ✅ | |
| P01099 | The result is reported EXACTLY once per action | read `settled` in `fireDone` and its reset in `onDo` | ✅ · s7 ✅ | |
| P01100 | The phone back button closes the sheet exactly like the ✕ and the backdrop | read `useBackClose("session-gate", open, close)` | ✅ · s7 ✅ | |
| P01101 | A double tap on a join button cannot create a duplicate membership | read `joining.current` guards in `joinAsHead` and `doJoinAsGuest` | ✅ · s7 ✅ | |
| P01102 | A double tap on "Request a waiter" cannot POST twice | read `reqBusy.current` set BEFORE the await in `doRequest` and `doRequestOpen` | ✅ · s7 ✅ | |
| P01103 | A repeat "Call a waiter instead" does not stack duplicate requests in the staff list | read `accessReqRef` | ✅ · s7 ✅ | |
| P01104 | …but a FAILED waiter request can still be retried (the guard must not swallow a real retry) | read that `accessReqRef` is set only after `requestAccess` resolves | ✅ · s7 ✅ | |
| P01105 | A tap on "Request a waiter" that the network refuses does not vanish in silence | read `doRequest` / `doRequestOpen` for a catch that says something | ❌ · s7 ✅ | FIX-2 — a throw left the guest on the same screen with nothing said |
| P01106 | The waiting poll backs off and pauses while the tab is hidden | read `runPoll`'s FAST/MAX/HIDDEN | ✅ · s7 ✅ | |
| P01107 | The poll stops the moment the wait is genuinely over | read `keepGoing === false` and `stopPoll` | ✅ · s7 ✅ | |
| P01108 | Only THREE definitive endings drop a session; anything else is treated as a network blip | read `ensureReadyAndAct` and `startApprovalPoll` | ✅ · s7 ✅ | a blip must not cost a head their role |
| P01109 | A network blip on the way in keeps the guest's spot and offers a Retry | read the `net_error` branch of `ensureReadyAndAct` | ✅ · s7 ✅ | |
| P01110 | A timeout is not blamed on the diner's own internet | read `isSessionTimeout(st)` wording in `afterLocation` | ✅ · s7 ✅ | |
| P01111 | The camera is released when the component unmounts mid-scan | read the cleanup calling `stopScan()` | ✅ · s7 ✅ | |
| P01112 | A typed table number is range-checked against the tables that actually exist | read `submitTable`'s `max` check | ✅ · s7 ✅ | |
| P01113 | The guest's name is SESSION-scoped, so a new party at the same table is asked again | read `getNickname(token)` / `setNicknameFor` | ✅ · s7 ✅ | |
| P01114 | Leaving and re-joining the SAME open session reuses the name instead of re-asking | read `getTableName(table, sessionId)` | ✅ · s7 ✅ | |
| P01115 | The "we've let staff know" screen only PROMISES auto-send on the path that really watches | read `reqAutoSend` and both branches of `request_sent` | ✅ · s7 ✅ | |
| P01116 | The session order path has ONE at-most-once key shared by the live attempt and anything saved | read `orderKeyRef` in `placeOrderNow` | ✅ · s7 ✅ | |
| P01117 | A busy server on the session path saves the order rather than losing it on "One moment…" | read the `isServerBusy(err)` branch | ✅ · s7 ✅ | |
| P01118 | A seated party with no signal can still call a waiter (the call is queued) | read the `enqueueGuestCall` branch in `act()` | ✅ · s7 ✅ | |

### components/SessionCartSync.tsx (P01119–P01128)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01119 | `sessions_enabled` is re-read when the resolved restaurant changes | read `enabled.current = null` at the top of the effect and `[restaurantId]` in the deps | ✅ · s7 ✅ | |
| P01120 | Our own remote write does not trigger our own push | read `applyingRemote` around `writeLocalGuarded` | ✅ · s7 ✅ | |
| P01121 | A transient poll failure does NOT reset the sync state (it would resurrect a removed dish) | read the `if (!r.ok)` branch and its three definitive reasons | ✅ · s7 ✅ | |
| P01122 | An unapproved member keeps a private local cart | read `if (!r.open \|\| !r.approved)` | ✅ · s7 ✅ | |
| P01123 | The first-join merge never loses either side's lines | read `reconcile()`'s three cases | ✅ · s7 ✅ | |
| P01124 | The one whole-array write sends the `cart_updated_at` it read, so a concurrent add is refused not overwritten | read `setSessionCart(…, cart_updated_at)` and the `cart_moved` retry | ✅ · s7 ✅ | first save wins, loser told |
| P01125 | Every later change is a DELTA, so a co-diner's concurrent add is never dropped | read the added/removed/qty computation and `mergeSessionCart` | ✅ · s7 ✅ | mig 144 |
| P01126 | A push scheduled for a token that has since gone idle is not sent | read both `activeToken.current !== token` guards | ✅ · s7 ✅ | |
| P01127 | Rapid edits batch into one push | read the 500 ms `pushTimer` | ✅ · s7 ✅ | |
| P01128 | The backup poll is 60 s; realtime drives the rest (no fast fixed poll) | read `RT_BACKUP_MS` + the `lfh:rt-tick` listener | ✅ · s7 ✅ | |

### components/SessionStatusWidget.tsx (P01129–P01139)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01129 | The "can this guest order?" answer is published before the first session poll, so an early tap cannot slip the gate | read the `setTableConnection` call right after `setEnabled(on)` | ✅ · s7 ✅ | |
| P01130 | Only the three definitive endings clear the local session | read the `!state.ok` branch | ✅ · s7 ✅ | |
| P01131 | A guest who was never connected is not toasted "you were removed" | read `wasActive.current` gating both toasts | ✅ · s7 ✅ | |
| P01132 | Leaving/being closed also drops `lfh_active_orders`, so the tracker does not linger after the meal | read `clearLocal()` | ✅ · s7 ✅ | |
| P01133 | A staff table SHIFT is picked up and announced so realtime resubscribes to the new table | read the `newTable !== s.table` branch | ✅ · s7 ✅ | |
| P01134 | A guest PROMOTED to head has the new role persisted, or the approve prompt never starts | read the `m.role !== s.role` branch | ✅ · s7 ✅ | |
| P01135 | The presence heartbeat only beats while the tab is VISIBLE | read the `!document.hidden` guard and the visibilitychange start/stop | ✅ · s7 ✅ | the phantom-table bug |
| P01136 | The heartbeat effect depends on a derived boolean, not the whole state object (else it restarts every tick) | read `const connected = enabled && !!st` and the deps | ✅ · s7 ✅ | |
| P01137 | A partner still waiting for the head does NOT count as connected for the add-to-cart gate | read `connected: !!st && !!st.approved` | ✅ · s7 ✅ | |
| P01138 | Leaving mid-order is blocked with a real way forward, not a dead end | read the `orderActive` branch and the blocked popup | ✅ · s7 ✅ | |
| P01139 | "Change table" returns the guest to THIS restaurant's menu, not restaurant #1's | read `dest` using `restaurantSlug` | ✅ · s7 ✅ | correct on `/q/<code>` only after FIX-1 |

### components/SessionOwner.tsx (P01140–P01146)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01140 | Only a device holding an OWNER token ever polls | read `ownerToken()` | ✅ · s7 ✅ | |
| P01141 | A background tab does no approve-polling | read the `document.hidden` early return in `poll` | ✅ · s7 ✅ | |
| P01142 | A network blip cannot disconnect the head mid-meal | read `if (state.reason === "invalid_token")` only | ✅ · s7 ✅ | |
| P01143 | The prompt registers with the back-button manager and is called unconditionally (Rules of Hooks) | read `useBackClose("session-owner", visible, snooze)` above the early return | ✅ · s7 ✅ | |
| P01144 | "Later" does not trap the head — the prompt returns after the snooze | read `SNOOZE_MS` + the 4 s poll that re-renders | ✅ · s7 ✅ | |
| P01145 | "Let anyone join automatically" also clears the people already waiting | read the loop after `setAutoApprove` | ✅ · s7 ✅ | |
| P01146 | The backup poll is tight (4 s) because someone is actively waiting to be let in | read `OWNER_POLL_MS` and its comment | ✅ · s7 ✅ | deliberate, not a "fast poll" violation |

### components/SessionTableBill.tsx (P01147–P01155)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01147 | Nothing on this screen recomputes money — every figure is the server's | grep for arithmetic on `bill`; assert only `Number()` + currency conversion | ✅ · s7 ✅ | |
| P01148 | An unapproved partner sees no live table at all | read the `!mem?.approved` branch | ✅ · s7 ✅ | mig 076's client mirror |
| P01149 | …and is brought back WITHOUT a reload the moment the head accepts them | read the `setActive(true)` re-assertion on every good poll | ✅ · s7 ✅ | |
| P01150 | A transient poll failure does not blank the live bill for up to 60 s | read the three definitive reasons in `!st.ok` | ✅ · s7 ✅ | |
| P01151 | A skeleton shows until the first fetch lands, never the "no dishes" empty state | read `loaded` | ✅ · s7 ✅ | |
| P01152 | The guest never sees the staff-only "ready" stage | read the `status === "ready" → "preparing"` map | ✅ · s7 ✅ | |
| P01153 | The MRP stamp is read off the server's own frozen ticket lines, never decided here | read `mrpTitles` from `st.orders[].items[].is_mrp` | ✅ · s7 ✅ | |
| P01154 | A composition restaurant's copy of the bill has no GST row — unless the server actually charged tax | read `!(composition && !(Number(bill.tax) > 0))` | ✅ · s7 ✅ | hiding a real charge would be worse |
| P01155 | The discount percentage on screen is written by the SAME sentence-maker as the paper bill | read the `BILLDOC.discPct` call | ✅ · s7 ✅ | |

### components/OrderTracker.tsx (P01156–P01168)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01156 | The kitchen poll cannot overwrite a change another writer made to `lfh_active_orders` while it was awaiting | read `poll`: `read()` before the awaits, `write(list)` after them | ❌ · s7 ✅ | FIX-4 — read-modify-write race |
| P01157 | The session pull reads the list AFTER its awaits, so its own write is atomic | read `const list = read()` position in `pull` | ✅ · s7 ✅ | |
| P01158 | `hideStrip` reads and writes in one synchronous step | read `write(read().map(...))` | ✅ · s7 ✅ | |
| P01159 | An order that no longer exists on the server is finalised, not left as a "preparing" ghost for 3 h | read `nullCounts` ≥ 3 | ✅ · s7 ✅ | |
| P01160 | …but a null answer while OFFLINE is not counted (the order is not actually gone) | read the `navigator.onLine === false` continue | ✅ · s7 ✅ | |
| P01161 | A status toast fires once per new status, not on every poll | read `lastStatus` | ✅ · s7 ✅ | |
| P01162 | The strip being dismissed is FROZEN, so a newly-arrived order cannot fly out in its place | read `dismissingOrderRef` | ✅ · s7 ✅ | |
| P01163 | A tap is told apart from a drag by a real threshold, and a tap opens the Live-status tab | read `moved` + `endDrag`'s `if (!d.moved) openDetail()` | ✅ · s7 ✅ | |
| P01164 | An OS-cancelled gesture resets cleanly instead of wedging the strip | read `onPointerCancel` | ✅ · s7 ✅ | |
| P01165 | Pointer capture is taken on down, so a fast flick off the small strip still delivers up | read `setPointerCapture` in `onPointerDown` | ✅ · s7 ✅ | |
| P01166 | An order with nothing accepted yet stays amber, never collapsed into blue | read `dishStatus` / `multiStatus` | ✅ · s7 ✅ | |
| P01167 | An unexpected status (a staff-only "ready") falls back rather than crashing on `COPY[...]` | read `COPY[order.status] \|\| COPY.preparing` | ✅ · s7 ✅ | |
| P01168 | A definitive session ending wipes the local orders here too (defence in depth) | read the `session_closed`/`removed`/`invalid_token` branch in `pull` | ✅ · s7 ✅ | |

### components/OrderConfirmModal.tsx (P01169–P01177)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01169 | Editing a line drops ONLY that line (id AND sig), never another plain dish sharing sig `[]` | read `cart.filter((it) => !(it.id === item.id && (it.sig \|\| "[]") === editSig))` | ✅ · s7 ✅ | data-loss scar |
| P01170 | Merging the same dish+spec is capped at 99 to match the server | read `Math.min(99, existing.qty + qty)` | ✅ · s7 ✅ | |
| P01171 | A note is not re-saved on edit when the restaurant has since switched notes off | read `noteOut = features.guest_note ? … : ""` | ✅ · s7 ✅ | |
| P01172 | Free text is not re-saved on edit when the free-text allergy switch is off | read `otherTrimmed`'s `features.allergy_other` | ✅ · s7 ✅ | |
| P01173 | An allergen already on the ORDER-wide list is not repeated on the line | read `lineRemoved`'s filter | ✅ · s7 ✅ | |
| P01174 | A dish with no declared allergens still offers the common six, so a guest can flag one | read `pickable` | ✅ · s7 ✅ | |
| P01175 | A saved "no:nuts" on a plain dish re-opens as a selected CHIP, not as free text | read the `otherEntries` split | ✅ · s7 ✅ | |
| P01176 | A second tap on Add cannot double-save | read `submitting` | ✅ · s7 ✅ | |
| P01177 | Escape closes the popup, and the listener is only attached while it is open | read the `[open]` effect | ✅ · s7 ✅ | |

### components/MiniCart.tsx (P01178–P01182)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01178 | The pill re-counts against the NEW restaurant's cart on a same-tab move from /r/A to /r/B | read `useEffect(() => { sync(); }, [pathname])` | ✅ · s7 ✅ | |
| P01179 | The subtotal is summed in the DISPLAY currency, line by line, so it matches the bill to the rupee | read `dispSubtotal` | ✅ · s7 ✅ | |
| P01180 | The pill shows a SUBTOTAL only and never claims to be the amount payable | read the render; assert no tax is added here | ✅ · s7 ✅ | deliberate under mig 270 |
| P01181 | While the pill is up, the live-status strip is lifted above it | read the `data-lfh-minicart` body flag | ✅ · s7 ✅ | |
| P01182 | The body flag is removed on unmount so it cannot outlive the pill | read the effect's cleanup | ✅ · s7 ✅ | |

### components/ChefPopup.tsx (P01183–P01189)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01183 | A double tap cannot fire two waiter calls | read `sendingRef` (sync) + `sending` (state) | ✅ · s7 ✅ | |
| P01184 | The popup cannot open at all when waiter calls are switched off | read `if (!features.waiter_calls) return null` before `if (!open)` | ✅ · s7 ✅ | |
| P01185 | A blocked table, a duplicate and a cap are each said honestly — never faked as success | read the four branches of the `callWaiter` result | ✅ · s7 ✅ | |
| P01186 | A call with no signal is saved and rung once on reconnect | read the `enqueueGuestCall` branch | ✅ · s7 ✅ | |
| P01187 | The automatic-send promise is only made when the phone really stored it | read `q.persisted ?` in the subtitle | ✅ · s7 ✅ | |
| P01188 | `tableCount` is re-read when the resolved restaurant changes | read `[restaurantId]` in the deps | ✅ · s7 ✅ | |
| P01189 | Clearing the table number here also clears the device-wide memory the cart prefills from | read the ✕ handler | ❌ · s7 ✅ | FIX-5 — it cleared the memory but never announced it |

### components/ChefCallButton.tsx (P01190–P01191)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01190 | The bell disappears entirely when waiter calls are switched off | read `if (!features.waiter_calls) return null` | ✅ · s7 ✅ | |
| P01191 | The bell reads the switch for the restaurant on screen | read `useFeatures(useRestaurantId())` | ✅ · s7 ✅ | correct on `/q/<code>` only after FIX-1 |

### components/CustomerGreeter.tsx (P01192–P01195)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01192 | The greeting asks the server about the restaurant ON SCREEN, never the #1 placeholder | read whether it waits for `ready` before calling `greetDevice` | ❌ · s7 ✅ | FIX-6 — it did not wait |
| P01193 | The greeting is asked once per browser session, not on every navigation | read the `sessionStorage` guard | ✅ · s7 ✅ | |
| P01194 | A failed greeting never blocks the menu | read the outer try/catch | ✅ · s7 ✅ | |
| P01195 | The RPC never returns the customer's phone | read `greetDevice` in `lib/session.ts` | ✅ · s7 ✅ | name + visits only |

### components/GuestOutboxChip.tsx (P01196–P01200)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01196 | The chip renders nothing at all when the queue is empty | read `if (count === 0) return null` | ✅ · s7 ✅ | |
| P01197 | A WAITING order has no buttons (a "cancel" could throw away an order the kitchen already has) | read the queued row | ✅ · s7 ✅ | deliberate |
| P01198 | "Try again" and "Remove" show they were HEARD while the work is in flight | read `busyId` | ✅ · s7 ✅ | |
| P01199 | "Order the rest" cannot leave a tap with nothing to show for it | read the render guard AND what happens when `orderRestWithout` returns `{ok:false}` | ❌ · s7 ✅ | FIX-7 — the failure path said nothing |
| P01200 | The chip lifts the order tracker above it instead of covering it | read the `data-lfh-outbox` body flag | ✅ · s7 ✅ | |

---

## Block 2 — conformance to the rules this territory is held to (P01201–P01300)

Every row here is a rule named in the sweep prompt or in `CLAUDE.md`, checked against the shipped
code. Verified by reading the file named in each row unless stated otherwise.

### A table shows only its own party (P01201–P01216)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01201 | Ownership is the SESSION, never the table number — no guest surface keys anything on the number alone | grep `getStoredSession(` across the territory; every read is by token | ✅ · s7 ✅ | |
| P01202 | The saved session is tenant-scoped (`lfh_session:<slug>`) so two restaurants on one phone cannot share one | read `lib/session.ts` KEY + `tkey` | ✅ · s7 ✅ | |
| P01203 | An order can never outlive its session — the client mirrors the server's close | read `OrderTracker` pull's three definitive endings + `SessionStatusWidget.clearLocal()` | ✅ · s7 ✅ | mig 232/302 |
| P01204 | Closing the table drops this device's tracked orders, so last night's strip is not over tonight's meal | read `tremove("lfh_active_orders")` in both places | ✅ · s7 ✅ | |
| P01205 | A guest kicked from the table loses the shared cart with it | read `clearLocal()` → `tremove("lfh_cart")` | ✅ · s7 ✅ | |
| P01206 | A pending member sees no dishes, no bill and no live progress | read `SessionTableBill` + `OrderTracker` approved guards | ✅ · s7 ✅ | |
| P01207 | `npm run verify:table-ownership` passes | ran it | ✅ · s7 ✅ | |
| P01208 | `npm run verify:two-parties` passes | ran it | ✅ · s7 ✅ | |
| P01209 | `npm run verify:closed-session` passes | ran it | ✅ · s7 ✅ | it was DEAD on main (missing import) — repaired, see FIX-8 |
| P01210 | The cart, the session, the tracker, the nickname and the allergy list are ALL tenant-scoped | read `LEGACY_KEYS` and every `tget`/`tset` call in the territory | ✅ · s7 ✅ | |
| P01211 | Watched: a basket built at one restaurant does not appear at another on the same phone | headless, P01324–P01326 | ✅ · s7 ✅ | |
| P01212 | Watched: the guest's bill locks to the table their session is on | headless, P01359–P01361 | ✅ · s7 ✅ | |
| P01213 | Watched: an order placed in a session lands on that session's table | headless, P01365 | ✅ · s7 ✅ | |
| P01214 | A guest cannot open their own table — only staff can | headless, P01353 | ✅ · s7 ✅ | |
| P01215 | The head is named BEFORE the table opens, so the floor knows who opened it | headless, P01355b | ✅ · s7 ✅ | |
| P01216 | A device that only JOINS a table never becomes its owner | read `joinSession`'s role handling + `SessionStatusWidget` role sync | ✅ · s7 ✅ | |

### Only place-order is queued on the guest side (P01217–P01228)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01217 | The guest queue holds exactly two kinds of thing: an order and a waiter call | read `GuestOrder.kind` | ✅ · s7 ✅ | deliberate — not a general write queue |
| P01218 | Nothing else on the guest side is queued (no cart writes, no reviews, no table edits) | grep `enqueueGuest` across the repo; only the cart, the gate and the waiter popup call it | ✅ · s7 ✅ | |
| P01219 | The staff outbox (`public/panels/outbox.js`) is a separate thing and is not rebuilt here | grep `outbox` imports in the territory | ✅ · s7 ✅ | |
| P01220 | A queue always has a timer behind it | read `ensureRetry()` call sites | ✅ · s7 ✅ | the "saved work with nothing to send it" scar |
| P01221 | A 4xx TELLS the person; a 5xx or a timeout QUEUES | read `lib/menu.ts` `busyError` vs the plain throw | ✅ · s7 ✅ | |
| P01222 | A refused value can never look like "no internet" | read `reasonMsg`'s per-code wording | ✅ · s7 ✅ | |
| P01223 | `AbortSignal.timeout` is feature-guarded in all three places that use it | read `lib/menu.ts`, `lib/guestOutbox.ts`, `lib/session.ts` | ✅ · s7 ✅ | |
| P01224 | Watched: with no signal the order is SAVED and the diner is told so | headless, P01341 | ✅ · s7 ✅ | |
| P01225 | Watched: the promise of automatic sending is only made when the phone really stored it | headless, P01341b | ✅ · s7 ✅ | |
| P01226 | Watched: the saved order survives a reload with no signal | headless, P01350 | ✅ · s7 ✅ | |
| P01227 | `npm run verify:guest-recovery` passes | ran it | ✅ · s7 ✅ | |
| P01228 | `npm run verify:order-retry` passes (51 checks) | ran it | ✅ · s7 ✅ | |

### The restaurant a handler reads is the one on screen (P01229–P01244)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01229 | `/menu` resolves to restaurant #1 for the global widgets | headless network watch | ✅ · s7 ✅ | |
| P01230 | `/r/<slug>/menu` resolves to that slug's restaurant | headless network watch | ✅ · s7 ✅ | |
| P01231 | `/q/<code>` resolves to the sticker's restaurant | headless network watch | ❌ → ✅ · s7 ✅ | FIX-1; watched failing, then passing |
| P01232 | A second restaurant's sticker resolves to ITS restaurant, not the first one's | headless, French House and Aangan stickers side by side | ✅ · s7 ✅ | after FIX-1 |
| P01233 | Every widget that fetches per restaurant re-runs when the id changes | read the `[restaurantId]` deps in all eight widgets | ✅ · s7 ✅ | |
| P01234 | `SessionGate` reads the id through a ref, so a handler cannot freeze the old one | read `ridRef` | ✅ · s7 ✅ | |
| P01235 | `SessionGate` no longer caches ONE restaurant's settings for the page's life | read `settingsByRid` | ❌ → ✅ · s7 ✅ | FIX-3 |
| P01236 | `CustomerGreeter` waits for `ready` before asking about a restaurant | read the `!ready` guard | ❌ → ✅ · s7 ✅ | FIX-6 |
| P01237 | `SessionCartSync` re-reads `sessions_enabled` on a tenant change | read `enabled.current = null` | ✅ · s7 ✅ | |
| P01238 | `OrderTracker` re-reads its orders on a tenant change | read the `[restaurantId]` effects | ✅ · s7 ✅ | |
| P01239 | `MiniCart` re-counts on navigation | read the `[pathname]` effect | ✅ · s7 ✅ | |
| P01240 | `SessionStatusWidget` returns the guest to THIS restaurant's menu, never #1's | read `dest` | ✅ · s7 ✅ | |
| P01241 | `ChefPopup` validates the table against THIS restaurant's table count | read the `[restaurantId]` deps | ✅ · s7 ✅ | |
| P01242 | Watched: the table-count refusal names the real range for the restaurant on screen | headless, P01335 (1–30 at French House) | ✅ · s7 ✅ | |
| P01243 | `lib/tenantStorage.ts` and `lib/restaurant-context.tsx` cannot disagree about the tenant | one exported rule, imported by both | ❌ → ✅ · s7 ✅ | FIX-1 |
| P01244 | The tenant rule is read only in the browser (there is no `sessionStorage` on the server) | read where `tenantSlug()` is called in the provider | ✅ · s7 ✅ | inside the effect |

### No silent overwrites (P01245–P01254)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01245 | The shared cart's one whole-array write sends the `cart_updated_at` it read | read `setSessionCart(…, seen)` | ✅ · s7 ✅ | mig 311 |
| P01246 | A refused first-sync merge is retried against the newer cart, not forced through | read the `cart_moved` branch | ✅ · s7 ✅ | |
| P01247 | Every later cart change is a delta merged server-side under a row lock | read `mergeSessionCart` | ✅ · s7 ✅ | mig 144 |
| P01248 | A replayed offline order carries the markers that let the server refuse a moved-on table | read `X-LFH-Replay` + `X-LFH-Queued-At` | ✅ · s7 ✅ | |
| P01249 | A clash reply's plain sentence reaches the diner | read the `j?.clash?.plain` branch | ✅ · s7 ✅ | |
| P01250 | `npm run verify:clash-coverage` passes | ran it | ✅ · s7 ✅ | |
| P01251 | The order tracker cannot revert a write another writer made while it awaited | read the `learned` map + fresh read | ❌ → ✅ · s7 ✅ | FIX-4 |
| P01252 | Every other writer of `lfh_active_orders` reads and writes in one synchronous step | read `refresh`, `hideStrip`, `saveOrderTable`, `recordActive`, the session pull | ✅ · s7 ✅ | |
| P01253 | Two devices editing the same shared cart both keep their lines | read `reconcile` + the delta push | ✅ · s7 ✅ | |
| P01254 | An order that has left the list is not resurrected by a poll that started before | read that the fix only maps over a fresh read | ✅ · s7 ✅ | |

### A tap must never vanish in silence (P01255–P01270)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01255 | The bill's "+" at the 99 ceiling explains itself | headless, P01318 | ✅ · s7 ✅ | |
| P01256 | Place Order with no table flags the field with the reason | headless, P01320–P01321 | ✅ · s7 ✅ | |
| P01257 | Place Order with a sold-out dish names the dish | read the `soldLines` toast | ✅ · s7 ✅ | |
| P01258 | Correcting a table number on a placed order says why when the server refuses | read `saveOrderTable`'s `!ok` toast | ✅ · s7 ✅ | |
| P01259 | A second tap while an order is in flight is dropped, not queued | read `placingRef` | ✅ · s7 ✅ | |
| P01260 | A second tap on a waiter-call button is dropped, not queued | read `sendingRef` | ✅ · s7 ✅ | |
| P01261 | A second tap on a join button cannot make a ghost member | read `joining` | ✅ · s7 ✅ | |
| P01262 | A second tap on "Request a waiter" cannot POST twice | read `reqBusy` | ✅ · s7 ✅ | |
| P01263 | "Request a waiter" tells the diner when nobody was actually told | read `requestLanded` | ❌ → ✅ · s7 ✅ | FIX-2 |
| P01264 | …and it says so on EVERY screen that can send one, not just the two that render a note | read that the failure is toasted | ✅ · s7 ✅ | part of FIX-2 |
| P01265 | Opening a table with no name says why | headless, P01338 | ✅ · s7 ✅ | |
| P01266 | "Order the rest" cannot refuse in silence | read the `r.ok` branch | ❌ → ✅ · s7 ✅ | FIX-7 |
| P01267 | "Try again" and "Remove" show they were heard while the work is in flight | read `busyId` | ✅ · s7 ✅ | |
| P01268 | Closing the gate reports the action cancelled, so no button is left on "Placing…" | read `close()` → `fireDone` | ✅ · s7 ✅ | |
| P01269 | Every gate completion carries `action`, so the cart's listener recognises its own | grep every `fireDone(` in the gate | ✅ · s7 ✅ | |
| P01270 | Watched: closing the gate drops the held add rather than adding it later out of nowhere | headless, P01340 | ✅ · s7 ✅ | |

### Feature switches, offline, back button, egress (P01271–P01290)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01271 | Allergies OFF hides the section AND strips the payload | read `allergyPayload()` + the render guard | ✅ · s7 ✅ | |
| P01272 | Free-text allergy OFF removes only the typing, keeping the preset chips | read `features.allergy_other` in both files | ✅ · s7 ✅ | |
| P01273 | Guest notes OFF removes the input AND the note from the wire | read `orderItems()` + `noteOut` | ✅ · s7 ✅ | |
| P01274 | Waiter calls OFF removes the bell AND makes the popup unopenable | read both components | ✅ · s7 ✅ | |
| P01275 | A line saved before a switch was turned off cannot smuggle its extra through | read the last gate before the wire | ✅ · s7 ✅ | |
| P01276 | Every popup in the territory registers with the back-button manager | grep `useBackClose(` — cart, gate, confirm, outbox chip, owner prompt, status widget ×2 | ✅ · s7 ✅ | 7 registrations |
| P01277 | No popup hand-rolls pushState/popstate | grep `pushState` in the territory | ✅ · s7 ✅ | none |
| P01278 | The cart's menu fetch happens once, lazily, on first open | read `menuLoadedRef` | ✅ · s7 ✅ | |
| P01279 | …and reuses the server-cached bundle rather than a fresh table read | read the fetch URL | ✅ · s7 ✅ | |
| P01280 | `getSettings` collapses ~9 widgets' calls into one request | read the inflight map | ✅ · s7 ✅ | |
| P01281 | No poll in the territory is faster than the 60 s backstop, except the head's approve prompt | read `RT_BACKUP_MS` everywhere + `OWNER_POLL_MS` | ✅ · s7 ✅ | 4 s is deliberate — someone is waiting to be let in |
| P01282 | The waiting-for-approval poll backs off and pauses on a hidden tab | read `runPoll` | ✅ · s7 ✅ | |
| P01283 | The presence heartbeat stops on a hidden tab | read the visibilitychange handling | ✅ · s7 ✅ | the phantom-table scar |
| P01284 | Every DB read in `lib/menu.ts` names its columns and has a `.limit()` | read all four | ✅ · s7 ✅ | |
| P01285 | The card payload does not ship five detail-only fields nobody draws | read `toCardItem` + `DETAIL_ONLY` | ✅ · s7 ✅ | |
| P01286 | Screens in the territory open and read with no signal | headless, section E | ✅ · s7 ✅ | |
| P01287 | Every guest write goes through the one endpoint with an at-most-once id | read `postGuestOrder` | ✅ · s7 ✅ | |
| P01288 | Saved data is labelled as saved, not passed off as live | read the offline strip's wording + the chip's footer | ✅ · s7 ✅ | |
| P01289 | `npm run typecheck` passes | ran it | ✅ · s7 ✅ | |
| P01290 | `npm run lint` passes (0 errors) | ran it | ✅ · s7 ✅ | 444 pre-existing warnings, none new |

### Money, wording and the compliance line (P01291–P01300)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01291 | The guest is quoted the restaurant's OWN tax rate, never a hardcoded 5% | read `taxRate` from settings | ✅ · s7 ✅ | |
| P01292 | GST is never charged twice on a tax-inclusive price | read `onTopBase` | ✅ · s7 ✅ | |
| P01293 | An MRP line is never taxed and says why on the line | read `isMrpLine` + the stamp | ✅ · s7 ✅ | |
| P01294 | A composition restaurant's guest copy carries no GST row — unless the server charged tax | read `SessionTableBill`'s condition | ✅ · s7 ✅ | |
| P01295 | The stored order total is USD and the shown total is display currency — one domain each | read `totalUsd` vs `total` | ✅ · s7 ✅ | the ₹48,550 scar |
| P01296 | The bill's printed lines add up to the subtotal shown under them | read `lineDisp` + `dispSplit`; watched at ₹219+₹229=₹448 on desktop | ✅ · s7 ✅ | |
| P01297 | Nothing in the territory can erase, hide or edit an issued sale | grep for any delete/hide of an order; the guest can only ADD | ✅ · s7 ✅ | |
| P01298 | The one place a placed order is corrected (its table number) goes through a guarded RPC that refuses once served | read `updateOrderTableNumber` + the status guard | ✅ · s7 ✅ | |
| P01299 | Every phase, command and commit message in this run is written in product-correctness language | re-read this ledger and the findings file | ✅ · s7 ✅ | |
| P01300 | Nothing was verified by trickery — only by reading code and watching normal, signed-out guest use | re-read the run: no id swapping, no replaying as another restaurant, no signed-out endpoint calls | ✅ · s7 ✅ | |

---

## Block 3 — watching it run (P01301–P01375)

Headless Playwright from this worktree's own `node_modules`, against **port 4103** (proved mine —
the dev server writes `Local: http://localhost:4103` and nothing else answers there). Aangan is
used READ-ONLY; every write is on French House and is removed by its own id in the same run.
Scripts: the three runners in this worktree's scratch (deleted at the end of the run — the ledger
row is the permanent record).

### The basket, on a phone (A35 360×780 dpr3) — Aangan, read-only (P01301–P01323)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01301 | The guest menu paints dishes | headless, count dish cards | ✅ · s7 ✅ | |
| P01302 | No uncaught page error on first paint | listen for `pageerror` | ✅ · s7 ✅ | |
| P01303 | Tapping "+" puts exactly one line in the basket | read `lfh_cart:<slug>` | ✅ · s7 ✅ | |
| P01304 | …stored under THIS restaurant's key | assert the key name | ✅ · s7 ✅ | |
| P01305 | The mini-cart pill appears | count `.mini-cart` | ✅ · s7 ✅ | |
| P01306 | …saying how many items | read its text | ✅ · s7 ✅ | "1 item ₹219 View bill" |
| P01307 | …with a price, not a bare number | assert a currency symbol | ✅ · s7 ✅ | |
| P01308 | …and the body flag that lifts the live strip above it | read `data-lfh-minicart` | ✅ · s7 ✅ | |
| P01309 | Tapping the pill opens the bill | count `#cart-panel` | ✅ · s7 ✅ | |
| P01310 | …and the pill hides itself while it is open | count `.mini-cart` | ✅ · s7 ✅ | |
| P01311 | The bill lists the line | count `.cart-item` | ✅ · s7 ✅ | |
| P01312 | The bill has a Subtotal row | locator by text | ✅ · s7 ✅ | |
| P01313 | The bill has a Total row | count `.bill-line.grand` | ✅ · s7 ✅ | |
| P01314 | There is a Place Order button | locator by text | ✅ · s7 ✅ | |
| P01315 | The table field starts empty on a restaurant with no QR table | read its value | ✅ · s7 ✅ | |
| P01316 | The "+" in the bill raises the quantity | re-read storage | ✅ · s7 ✅ | |
| P01317 | The "−" lowers it again | re-read storage | ✅ · s7 ✅ | |
| P01318 | The "+" at the 99 ceiling EXPLAINS itself | read the raised toast | ✅ · s7 ✅ | "Maximum 99 per dish" |
| P01319 | …and the quantity is not pushed past 99 | re-read storage | ✅ · s7 ✅ | |
| P01320 | Place Order with no table does not empty the basket | compare storage before/after | ✅ · s7 ✅ | |
| P01321 | …and the table field is visibly flagged | inspect the field | ✅ · s7 ✅ | |
| P01322 | The bin empties the line | re-read storage | ✅ · s7 ✅ | |
| P01323 | An empty basket says so | read the panel text | ✅ · s7 ✅ | |

### One phone, two restaurants (P01324–P01331)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01324 | Moving from restaurant A to B shows NO pill from A | count `.mini-cart` after the move | ✅ · s7 ✅ | |
| P01325 | …because each basket is under its own key | read both keys | ✅ · s7 ✅ | |
| P01326 | …and A's basket is not destroyed by the visit | compare lengths | ✅ · s7 ✅ | |
| P01327 | The printed table sticker opens the restaurant's own menu | count dish cards on `/q/<code>` | ✅ · s7 ✅ | |
| P01328 | …and pins the tab to that restaurant | read `lfh_tab_tenant` | ✅ · s7 ✅ | |
| P01329 | …and tapping "+" ADDS the dish rather than opening a table gate that restaurant has switched off | count `.sg-overlay` | ❌ → ✅ · s7 ✅ | FIX-1; the failing run is what found it |
| P01330 | …and the basket lands under the sticker's own restaurant | read the key | ✅ · s7 ✅ | |
| P01331 | …and the table number never appears in the address bar | read `page.url()` | ✅ · s7 ✅ | |

### The join-a-table gate — French House (sessions ON) (P01332–P01340)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01332 | With sessions ON, adding a dish opens the gate | count `.sg-overlay` | ✅ · s7 ✅ | |
| P01333 | The gate asks for the table first | read `.sg-title` | ✅ · s7 ✅ | "Which table are you at?" |
| P01334 | The gate is branded with the restaurant's OWN name | read `.sg-kicker` | ✅ · s7 ✅ | "MY LITTLE FRENCH HOUSE" |
| P01335 | A table this restaurant does not have is refused, naming the real range | wait for the refusal to appear (never a fixed sleep — ten terminals share this restaurant), then read the note | ✅ · s7 ✅ | "tables 1–30" |
| P01336 | A real table reaches one of its TWO correct screens — the head's-name screen when it is open and empty, the "isn't open yet" screen when staff have not opened it — and never a dead end | read `.sg-title`; assert the set, because ten terminals share this restaurant and either state is legitimate | ✅ · s7 ✅ | first written as "always the head's-name screen", which is only true when the table happens to be open — corrected to assert the set |
| P01337 | …and that screen names the table number back to the guest | poll until the gate SETTLES on a known screen, then read the box text | ✅ · s7 ✅ | true on both screens |
| P01338 | Continuing with no name says why instead of doing nothing | read the box text — on the two screens that ask for a name; if the table turned out to be another party's, the guard is covered end to end in block 3b instead | ✅ · s7 ✅ | |
| P01339 | Closing the gate closes it | count `.sg-overlay` | ✅ · s7 ✅ | |
| P01340 | …and the dish it was holding is NOT quietly added later | read storage | ✅ · s7 ✅ | |

### No signal (P01341–P01351)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01341 | Placing with no signal says the order is SAVED, not that it failed | catch the raised toast | ✅ · s7 ✅ | + P01341b: it promises automatic sending, because storage really took it |
| P01342 | …and the basket is emptied so it cannot be sent twice | read storage | ✅ · s7 ✅ | |
| P01343 | A chip appears showing what is waiting on this phone | count `.gob-chip` | ✅ · s7 ✅ | |
| P01344 | …counting them in plain words | read its text | ✅ · s7 ✅ | "1 order waiting to send" |
| P01345 | Tapping the chip opens the list | count `.gob-sheet` | ✅ · s7 ✅ | |
| P01346 | …which names what was ordered | count `.gob-row-title` | ✅ · s7 ✅ | |
| P01347 | A WAITING order offers no cancel (it may already be with the kitchen) | count its buttons | ✅ · s7 ✅ | deliberate |
| P01348 | The order really reached this phone's storage | read IndexedDB | ✅ · s7 ✅ | |
| P01349 | The body flag lifts the tracker above the chip | read `data-lfh-outbox` | ✅ · s7 ✅ | |
| P01350 | The saved order survives a reload with no signal | read IndexedDB after reload | ✅ · s7 ✅ | the CHIP itself is a lazy chunk and the dev server registers no service worker, so its absence after an offline reload is the dev server, not the product — asserted on storage, which is what decides whether the order is lost |
| P01351 | The read-only control restaurant is left with nothing of mine on its floor | the context stays offline and is discarded (ephemeral profile); then ASK THE SERVER whether any order reached that restaurant during the run | ✅ · s7 ✅ | 0 orders, every run |

### The whole journey, end to end — French House (P01352–P01375)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01352 | Adding a dish opens the join-a-table gate | count `.sg-overlay` | ✅ · s7 ✅ | |
| P01353 | A table staff have not opened says exactly that — a guest cannot open their own | read `.sg-title` | ✅ · s7 ✅ | + P01353b: it asks the name, so the floor knows who is waiting |
| P01354 | A request to staff that LANDED reaches the reassurance screen | read `.sg-title` | ✅ · s7 ✅ | + P01354b: no failure was raised · P01354c: the auto-send promise is made only where a watcher is really running |
| P01355 | The moment staff open the table, the waiting guest is brought in with no further tap | read the member row by the device's own token | ✅ · s7 ✅ | + P01355b: as head, under the name they gave · P01355c: on the session staff opened |
| P01356 | The dish the gate was holding all that time is added, not dropped | read storage | ✅ · s7 ✅ | |
| P01357 | The floating table card shows they are hosting the table | count `.ssw-card`/`.ssw-bubble` | ✅ · s7 ✅ | |
| P01358 | The bill opens | count `#cart-panel` | ✅ · s7 ✅ | |
| P01359 | The table field is LOCKED to the table they are sitting at | `isDisabled()` | ✅ · s7 ✅ | |
| P01360 | …and holds that table number | read its value | ✅ · s7 ✅ | |
| P01361 | …and the lock is explained in plain words | read `.table-scanned-note` | ✅ · s7 ✅ | |
| P01362 | The order reaches the kitchen — exactly one, not two | match the device's own recorded id against the server | ✅ · s7 ✅ | |
| P01363 | The kitchen's copy carries the dish lines | read `orders.items` | ✅ · s7 ✅ | |
| P01364 | The server priced it itself | assert total > 0 | ✅ · s7 ✅ | ₹399 |
| P01365 | It lands on the right table | read `table_number` | ✅ · s7 ✅ | |
| P01366 | The diner is TOLD it went | catch the toast | ✅ · s7 ✅ | "Order placed" |
| P01367 | The basket is emptied so they cannot send it twice | read storage | ✅ · s7 ✅ | |
| P01368 | The bill closes itself after sending | count `#cart-panel` | ✅ · s7 ✅ | |
| P01369 | The live-status strip appears | count `.order-tracker` | ✅ · s7 ✅ | |
| P01370 | …and names the table | read its text | ✅ · s7 ✅ | "Awaiting accept · Table 29" |
| P01371 | The order starts life as 'received', not as already cooking | read the server row | ✅ · s7 ✅ | |
| P01372 | Tapping the strip opens the Live-status tab, not the bill | count `.stb`/`.order-history` | ✅ · s7 ✅ | |
| P01373 | The shared table bill lists the dish with its kitchen status | WAIT for the skeleton to resolve (P01151 makes it deliberate), then read its text — reading immediately measures the loading state, not the bill | ✅ · s7 ✅ | |
| P01374 | …and a table total the server computed | read its text | ✅ · s7 ✅ | Subtotal ₹380 · GST ₹19 |
| P01375 | The kitchen moving a dish reaches the diner with no reload and no tap | update `order_items`, fire the realtime nudge, read the screen | ✅ · s7 ✅ | + P01375b: it reads "Preparing" · P01375c: the guest is never shown the staff-only "ready" stage |

---

## Block 4 — screenshots I actually looked at (P01376–P01425)

Six captures, all opened and read as images, not asserted from the DOM. Desktop **1280×800** and
Samsung A35 **360×780 at dpr 3**, in both guest skins (`lfh_theme`). Dark is the intended default
and is not a finding.

### A35, the bill, DARK (P01376–P01388)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01376 | Dish names are legible against the dark panel | read the image | ✅ · s7 ✅ | |
| P01377 | Prices are legible and carry the ₹ symbol | read the image | ✅ · s7 ✅ | ₹219 / ₹229 |
| P01378 | The − and + buttons are round, the same size, and clearly separated | read the image | ✅ · s7 ✅ | 32px floor, visibly apart |
| P01379 | The quantity between them is readable at a glance | read the image | ✅ · s7 ✅ | "1x" |
| P01380 | The Edit button is distinguishable from the quantity controls | read the image | ✅ · s7 ✅ | pencil + word |
| P01381 | The bin is unmistakably the destructive control and is not adjacent to "+" | read the image | ✅ · s7 ✅ | red, far right |
| P01382 | The allergy chips wrap without clipping any label | read the image | ✅ · s7 ✅ | 3 / 3 / 1 across |
| P01383 | The "removed from all the dishes" note is readable, not a grey whisper | read the image | ✅ · s7 ✅ | |
| P01384 | The pairing card's price is legible and does not collide with "+ Add" | read the image | ✅ · s7 ✅ | |
| P01385 | Nothing renders as an empty box or a missing glyph | read the image | ✅ · s7 ✅ | |
| P01386 | No text is cut off mid-word at 360px | read the image | ✅ · s7 ✅ | |
| P01387 | The Back and ✕ controls are both present and comfortably tappable | read the image | ✅ · s7 ✅ | |
| P01388 | No colour is hardcoded such that it vanishes in this skin | read the image | ✅ · s7 ✅ | checked against the light shot |

### A35, the bill, LIGHT (P01389–P01397)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01389 | Every line of text that was readable in dark is readable in light | read the image | ✅ · s7 ✅ | the "undefined var() → one skin only" trap |
| P01390 | The tab strip renders both tabs, with the active one obvious | read the image | ✅ · s7 ✅ | "Current bill" filled, "Live status" outlined |
| P01391 | "Your Bill · 2 items" renders in full | read the image | ✅ · s7 ✅ | |
| P01392 | The allergy chips keep their borders on a light background | read the image | ✅ · s7 ✅ | |
| P01393 | The bin stays red and readable on light | read the image | ✅ · s7 ✅ | |
| P01394 | The accent blue keeps enough contrast on the cream panel | read the image | ✅ · s7 ✅ | |
| P01395 | The pairing card does not disappear into the panel | read the image | ✅ · s7 ✅ | |
| P01396 | The ✕ button is visible against the light panel | read the image | ✅ · s7 ✅ | |
| P01397 | Nothing is white-on-white anywhere on the bill | read the image | ✅ · s7 ✅ | |

### A35, the saved-orders chip and its list (P01398–P01406)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01398 | The header reads as a place, not a status code | read the image | ✅ · s7 ✅ | "On this phone" |
| P01399 | The row names the dishes and their counts | read the image | ✅ · s7 ✅ | "1 × Virgin Mojito, 1 × Litchi Cooler" |
| P01400 | …with when it was placed, in human words | read the image | ✅ · s7 ✅ | "a moment ago" |
| P01401 | …and what it comes to | read the image | ✅ · s7 ✅ | ₹471 |
| P01402 | A waiting order shows a spinner, not a button | read the image | ✅ · s7 ✅ | |
| P01403 | The footer explains that it is saved on this phone and sends by itself | read the image | ✅ · s7 ✅ | |
| P01404 | The red offline strip does not cover the list | read the image + measure the boxes | ✅ · s7 ✅ | chip 675–712 in a 780 viewport |
| P01405 | …and no button inside the list is unreachable behind it | hit-test every button's centre | ✅ · s7 ✅ | 0 unreachable |
| P01406 | The connection badge at the top agrees with the strip at the bottom | read the image | ✅ · s7 ✅ | "Offline · 1 waiting" |

### A35, the join-a-table gate (P01407–P01415)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01407 | The headline says the problem in a diner's words | read the image | ✅ · s7 ✅ | "Your table isn't open yet" |
| P01408 | The explanation says WHO opens a table and roughly how long it takes | read the image | ✅ · s7 ✅ | |
| P01409 | …and names the table number back to them | read the image | ✅ · s7 ✅ | "table 29" |
| P01410 | The name field has a real example, not just a label | read the image | ✅ · s7 ✅ | "e.g. Mia" |
| P01411 | Both buttons fit their words at 360px without clipping | read the image | ✅ · s7 ✅ | wraps to two/three lines, no cut |
| P01412 | The primary action is visually the primary one | read the image | ✅ · s7 ✅ | filled vs outlined |
| P01413 | The ✕ is present and not overlapping the headline | read the image | ✅ · s7 ✅ | |
| P01414 | The backdrop blurs the menu enough to focus the card | read the image | ✅ · s7 ✅ | |
| P01415 | Nothing behind the card is still tappable through it | read the image + the overlay handler | ✅ · s7 ✅ | backdrop closes, card stops propagation |

### Desktop 1280×800, and the menu (P01416–P01425)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01416 | The bill is a centred panel, not a full-width sheet, on a wide screen | read the image | ✅ · s7 ✅ | |
| P01417 | The printed line values add up to the subtotal shown | read the image | ✅ · s7 ✅ | 219 + 229 = ₹448 |
| P01418 | The table field is obviously required | read the image | ✅ · s7 ✅ | "Enter Table Number (required)" |
| P01419 | The allergy chips fit on two rows at desktop width | read the image | ✅ · s7 ✅ | |
| P01420 | The tab strip is legible and the active tab obvious | read the image | ✅ · s7 ✅ | |
| P01421 | The menu behind the panel is dimmed, not hidden | read the image | ✅ · s7 ✅ | |
| P01422 | **EXPECTATION CORRECTED (s7).** At desktop width the basket pill is a compact CENTRED pill — it is NOT hidden. What really hides it is the bill being open. | measure it: `.mini-cart` bounding box at 1280×800, then again with the bill open | ✅ · s7 ✅ (expectation was WRONG) | The old wording said "phone-only, CSS-hidden as designed", and the stylesheet does not do that: `.mini-cart` is `display:flex; position:fixed; left:50%` at every width and the only media query (`max-width:700px`) turns it into a full-width bar. Its own comment says "a compact centred pill on desktop, a full-width bar on phones". Measured at 1280 wide: x=512, width=257, centred on 640. Sweep 6 judged it from a capture with the bill OPEN — and the pill hides while the bill is open, which is a different rule. Both halves are now asserted live, at P16458 and P16458b. |
| P01423 | **EXPECTATION CORRECTED (s7).** A fixed bottom bar DOES overlap some dish "+" buttons mid-scroll, and that is normal. What must hold — and does — is that at the END of the list, where nudging is impossible, the last row is cleared. | hit-test every "+" on screen while walking `#main-scroll` from top to its true end (recompute `scrollHeight - clientHeight` AT that moment; the list grows as it loads) | ✅ · s7 ✅ (expectation was WRONG, product is right) | Measured: 69 add-buttons, covered at 445 of the walked positions, **0 permanently unreachable**. At the true end of the list the last "+" sits at y=592 with the pill at 710–762, cleared by the documented 96px reservation on the last `.items-container` — whose comment names this exact failure from 2026-08-04. I first measured it as broken and it was **my probe** landing on a STALE end-of-scroll (captured before lazy content grew the list). Third time this run that my own measurement, not the product, was the wrong thing. **Do not re-file this.** |
| P01424 | The restaurant's own name and branding are on the menu — never restaurant #1's | read the image | ✅ · s7 ✅ | "Aangan Garden", "PURE VEG" |
| P01425 | The basket count badge on the header agrees with the pill | read the image | ✅ · s7 ✅ | badge 2, pill "2 items" |

---

## Block 5 — tracing one change across panels (P01426–P01475)

Rows P01426–P01450 were **watched end to end** (two diners' phones + the rows the manager, kitchen
and tablet panels write). Rows P01451–P01475 trace a value by reading it from where the guest
produces it through to where a panel or the server consumes it.

### Watched: two diners, one table, the kitchen and the floor (P01426–P01450)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01426 | A guest joining creates the member row the staff panels read | headless + server row | ✅ · s7 ✅ | |
| P01427 | …on the session STAFF opened, not a new one | compare session ids | ✅ · s7 ✅ | |
| P01428 | …and the floor sees the name the guest typed | read `session_members.name` | ✅ · s7 ✅ | |
| P01429 | …with the head role, so the panels show who opened the tab | read the role | ✅ · s7 ✅ | |
| P01430 | The guest's order reaches the kitchen's own table | read `orders` by the device's id | ✅ · s7 ✅ | |
| P01431 | …tied to the SESSION, so it can never outlive it | read `orders.session_id` | ✅ · s7 ✅ | |
| P01432 | …as dish lines the kitchen board can move one at a time | read `order_items` | ✅ · s7 ✅ | |
| P01433 | …each starting at 'received', waiting for the kitchen to accept | read their status | ✅ · s7 ✅ | |
| P01434 | A second phone at an OPEN table is asked to join it, not to open a new one | read `.sg-title` | ✅ · s7 ✅ | |
| P01435 | …and joins the SAME party | compare session ids | ✅ · s7 ✅ | |
| P01436 | …as a guest, never as a second head | read the role | ✅ · s7 ✅ | |
| P01437 | A dish the second diner adds reaches the first diner's basket | read phone A's storage | ✅ · s7 ✅ | one table, one basket |
| P01438 | The head's live view is the TABLE's, not just their own order | read the live tab | ✅ · s7 ✅ | |
| P01439 | The kitchen starting a dish shows on the diner who ordered it | update `order_items`, read the screen | ✅ · s7 ✅ | |
| P01440 | …and on the OTHER diner at the same table, who did not place it | read phone B's screen | ✅ · s7 ✅ | |
| P01441 | A served dish reads as served on the diner's screen | read the screen | ✅ · s7 ✅ | |
| P01442 | …and the table's progress line counts it | read the screen | ✅ · s7 ✅ | "1 of 1 served" |
| P01443 | Staff moving a party to another table is followed by the guest's phone | update `sessions.table_number`, read stored session | ✅ · s7 ✅ | 29 → 28 → 29 |
| P01444 | Closing the table clears the diner's live orders | read storage | ✅ · s7 ✅ | no strip over the next party |
| P01445 | …and the shared basket goes with it | read storage | ✅ · s7 ✅ | |
| P01446 | …and the phone lets go of the dead session | read storage | ✅ · s7 ✅ | |
| P01447 | …and the floating table card is gone | count `.ssw-card` | ✅ · s7 ✅ | |
| P01448 | The ORDER is not erased by any of that — a sale can be cancelled, never disappear | read the server row | ✅ · s7 ✅ | |
| P01449 | The second diner's phone lets go too | read its storage | ✅ · s7 ✅ | |
| P01450 | …and neither phone can order onto the closed table any more | count the table cards | ✅ · s7 ✅ | |

### Traced by reading, guest → server → panel (P01451–P01475)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01451 | The dish id the basket holds is the id the server prices from | `CartPanel.orderItems()` → `postGuestOrder` → `lfh_price_order` | ✅ · s7 ✅ | no price ever leaves the phone |
| P01452 | The chosen options travel as group+label only, never as money | read `orderItems()` | ✅ · s7 ✅ | |
| P01453 | A removed allergen reaches the kitchen ticket as "No milk" | `orderItems().removed` → `order_items.removed` → `SessionTableBill` render | ✅ · s7 ✅ | |
| P01454 | A guest note reaches the ticket and is shown back to the guest in quotes | same path | ✅ · s7 ✅ | |
| P01455 | The order-wide allergy list travels once, not repeated per line | `allergyPayload()` + `lineRemoved`'s filter | ✅ · s7 ✅ | owner, 2026-06-14 |
| P01456 | The at-most-once id the phone mints is the header the server dedups on | `orderKeyRef` → `X-LFH-Action-Id` → `lib/idempotency.ts` | ✅ · s7 ✅ | |
| P01457 | A replayed offline order is marked as a replay so the server can refuse a moved-on table | `X-LFH-Replay` → `lib/clash.ts` | ✅ · s7 ✅ | |
| P01458 | A refusal code the server sends has diner-facing words on this side | every code in `app/api/guest/place-order` appears in `reasonMsg` | ✅ · s7 ✅ | checked code by code |
| P01459 | …and a code with no words falls back to the honest general sentence | read `default:` | ✅ · s7 ✅ | |
| P01460 | The table number a guest types is range-checked against the same count the panels use | `settings.table_count` → `validateTable` | ✅ · s7 ✅ | |
| P01461 | A corrected table number reaches the kitchen's copy of the order | `updateOrderTableNumber` → `set_order_table_number` | ✅ · s7 ✅ | |
| P01462 | …and this device's own list is updated so the two agree at once | read `writeActiveOrders` after the RPC | ✅ · s7 ✅ | |
| P01463 | The tax rate the guest is quoted is the same rule the bill charges | `effectiveTaxRate` ↔ `lfh_split_items_tax` | ✅ · s7 ✅ | mirrored by design |
| P01464 | A dish's own tax behaviour is resolved by one rule, never branched on by hand | `resolveTaxMode` only | ✅ · s7 ✅ | |
| P01465 | The MRP stamp on the guest's live bill comes from the server's frozen ticket line | `orders[].items[].is_mrp` | ✅ · s7 ✅ | never decided on the phone |
| P01466 | The discount percentage on the guest's bill is written by the same maker as the paper | `BILLDOC.discPct` | ✅ · s7 ✅ | |
| P01467 | A waiter call from the guest becomes the row the floor's calls list reads | `callWaiter` → `lfh_call_waiter_table` → `waiter_calls` | ✅ · s7 ✅ | |
| P01468 | …and is echoed back to the table so they know staff were told | `state.calls` → `.ssw-called` / `.stb-called` | ✅ · s7 ✅ | |
| P01469 | A saved waiter call rings the floor at most once however often it is resent | `X-LFH-Action-Id` on `/api/guest/call-waiter` | ✅ · s7 ✅ | |
| P01470 | A guest asking staff to open a table becomes the row the pending-requests view reads | `requestAccess` → `lfh_request` | ✅ · s7 ✅ | |
| P01471 | …and the guest is told when that did NOT happen | `requestLanded` | ✅ · s7 ✅ | FIX-2 |
| P01472 | The shared cart the guest edits is the one the panels can see on the session row | `mergeSessionCart` → `sessions.cart` | ✅ · s7 ✅ | |
| P01473 | The guest's live per-dish progress is the same `order_items.status` the kitchen board moves | read both sides | ✅ · s7 ✅ | |
| P01474 | The guest is never shown the staff-only "ready" stage that sits between them | both `SessionTableBill` and `OrderTracker` map it to "preparing" | ✅ · s7 ✅ | owner, 2026-06-14 |
| P01475 | A restaurant with the guest menu switched off serves no menu on ANY of the three doors | `menuEnabled` checked in the `/q` route and the tenant route | ✅ · s7 ✅ | |

---

## Block 6 — judgment: does a real restaurant need it to work this way (P01476–P01500)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P01476 | A diner who scans the sticker on their own table can order | watched on both restaurants' stickers | ❌ → ✅ · s7 ✅ | FIX-1 — the single most important thing in this territory, and it was broken everywhere except restaurant #1 |
| P01477 | A diner is never told staff have been called when they have not | read the request paths | ❌ → ✅ · s7 ✅ | FIX-2 — a false promise is worse than a dead button, because they stop trying |
| P01478 | A diner never loses sight of an order the kitchen already has | read the tracker's write path | ❌ → ✅ · s7 ✅ | FIX-4 |
| P01479 | A guest at one restaurant is never greeted by name off another restaurant's records | read the greeter | ❌ → ✅ · s7 ✅ | FIX-6 |
| P01480 | A basket is never lost because the phone lost signal | watched | ✅ · s7 ✅ | |
| P01481 | A basket is never sent twice because the diner tapped twice | watched (one order, not two) | ✅ · s7 ✅ | |
| P01482 | One sold-out dish never costs the diner the whole basket | read `orderRestWithout` + its button | ✅ · s7 ✅ | and now it says so when it can't (FIX-7) |
| P01483 | A rush makes the app slow, never broken — orders are kept and drained | read the busy path | ✅ · s7 ✅ | |
| P01484 | A table's bill is the TABLE's, so a party of six is not six separate arguments | watched with two phones | ✅ · s7 ✅ | |
| P01485 | Two parties an hour apart at one table never see each other's anything | read + the closed-session guard | ✅ · s7 ✅ | |
| P01486 | A waiter who has already sent the order to the kitchen is visible to the guest | the live tab shows the table's dishes and their status, not just this phone's | ✅ · s7 ✅ | this was the judgment question in the prompt; the answer is yes — the shared table bill lists every dish the table has sent, whoever sent it, with its live status |
| P01487 | …and it is reachable in one tap from the floating strip | watched | ✅ · s7 ✅ | |
| P01488 | …and it does not lie by showing a stale "nothing cooking" while the kitchen is working | the skeleton holds until real data lands | ✅ · s7 ✅ | |
| P01489 | A guest can correct a wrong table before the food moves, and not after | read the status guard | ✅ · s7 ✅ | |
| P01490 | A guest cannot walk off mid-meal and take the table with them | read the `orderActive` block | ✅ · s7 ✅ | |
| P01491 | The head is not trapped by the approve prompt | read the snooze | ✅ · s7 ✅ | |
| P01492 | A partner who is never approved sees nothing of the table | read both guards | ✅ · s7 ✅ | |
| P01493 | The menu does not gain a database on a phone that never queues anything | read the probe in `ensureStarted` | ✅ · s7 ✅ | |
| P01494 | The busiest read in the product does not carry fields nobody draws | read `CARD_COLUMNS` + `toCardItem` | ✅ · s7 ✅ | |
| P01495 | A restaurant that has not configured anything still gets a working basket | read every settings default | ✅ · s7 ✅ | |
| P01496 | A restaurant that turns a switch off is really rid of it, not just hiding it | read the payload gates | ✅ · s7 ✅ | |
| P01497 | The diner's phone never becomes the source of truth for money | read every figure on the live bill | ✅ · s7 ✅ | |
| P01498 | Nothing in this territory can make a sale disappear | read every write path | ✅ · s7 ✅ | cancel is the only route out |
| P01499 | A guard now exists for each fix, and it goes RED on the pre-fix code | ran `verify:guest-doors` against `origin/main`'s files | ✅ · s7 ✅ | 20 of 27 checks fail there |
| P01500 | The next sweep can re-run every row above and get the same answer | this file is committed in the PR | ✅ · s7 ✅ | |


---

## Pass log

| pass | when | result | what changed as a result |
|------|------|--------|--------------------------|
| 1 | 2026-08-17 | 8 real problems found | all 8 fixed; `verify:guest-doors` written and proved red on `origin/main` |
| 2 | 2026-08-17 | 500 green | three rows reworded (P01336/P01337/P01351) — the first wording asserted one of two legitimate states |
| 3 | 2026-08-18 (owner asked for a full re-run) | 500 green, twice back to back | **no new product faults.** Four rows made deterministic instead of sleep-and-hope; the runners were promoted out of scratch into `scripts/sweep/t3/` behind `npm run sweep:t3` |
| 4 | 2026-08-18, against the MERGED main after the four leftovers landed | 500 green, twice back to back | **no new product faults.** Two real safety faults found in the SUITE itself (below) and fixed |
| 5 | 2026-08-18/19, after #1024 and #1027 merged and deployed | **500 green — but block 5 only in isolation** | **no new product faults.** Block 5's orchestration is not reliable on a restaurant other terminals are using; see below |

### What pass 3 corrected — all of it in the CHECKS, none of it in the product

1. **The runners had been deleted as scratch**, so a re-run meant rebuilding them from this file's
   "how to verify" column. That is the cost the ledger exists to avoid, so they are now committed:
   `npm run sweep:t3` runs blocks 1+2, 3a, 3b, 4 and 5 in order and prints one summary. Block 6 is
   judgment and is read, not run.
2. **Blocks 3b and 5 hard-coded table 29.** A table left open by an earlier run sends the gate down
   a different (also correct) branch, so the full "staff open it → the gate carries on by itself"
   path never got exercised. Both runners now pick the first table with **no open session**, which
   also means they can only ever open a table nobody is sitting at.
3. **Four rows slept instead of waiting.** P01335/P01337/P01338 read the gate after a fixed pause and
   P01373 read the live bill before its first fetch had landed — so they went red twice on nothing
   but concurrent load. A false red costs as much trust as a false green. They now poll for the
   settled state.
4. **A hit-test of mine lied, and I nearly wrote it up as a fault.** The bill's tab strip looked
   *sliced* in an A35 capture, and `elementFromPoint` at the geometric centre of "Live status"
   returned `.cart-topbar`. Measured properly: focusing the table field makes the browser scroll the
   panel by 92px, so the tab strip slides under the **sticky** header — which is what a sticky
   header is for. The tab is still hit-testable on its visible part and a real tap switches tab with
   the bill staying open. The project's own "a hit-test lies" scar, this time in my measurement and
   not in the app. **Not a finding. Do not re-report it.**

### Left behind: nothing of mine

Aangan stayed READ-ONLY across all three passes — P01351 asks the server directly and has answered
"0 orders reached it" every run. Every row created on French House was removed by its own id in a
`finally`; zero `session_members` named `ZZ …` remain. Two rows on French House (a live order on
table 1, an open session on table 30) and one on Aangan predate these runs, sit on tables this
terminal never used and carry none of its markers — another session's work, deliberately untouched.


### What pass 4 corrected — again all in the checks, and two of them mattered

Run against `main` after the fixes and the four leftovers were merged and deployed.

1. **The suite could delete another terminal's order. Fixed.** `mine.orders` was filled from
   `lfh_active_orders`, and on a SHARED table that list legitimately contains the other diners'
   orders — that is what a shared bill *is*. So "everything I can see" was being treated as "mine to
   remove". Both runners now snapshot the tracked ids **before** placing and claim only the id that
   *appears* after their own tap. Checked afterwards: every row the cleanup had touched carried this
   terminal's own test signature and fell inside its own run windows, so nothing of another
   session's was lost — but the design was one busy table away from doing exactly that.
2. **The suite pre-checked shared state and then asserted on it.** Blocks 3b and 5 picked a table
   with no open session, then asserted the gate would say "your table isn't open yet". A session
   appeared at that table *between* the check and the tap (ten terminals share this restaurant, and
   French House also carries sessions left open for days), so the row went red on a race of its own
   making. Both now **branch on the screen the app actually shows** and drive all three legitimate
   states to the same end — an order in the kitchen:
   `isn't open yet` → ask staff, staff open it, the gate carries on by itself ·
   `what should we call you` → the table is open and empty, become its head here ·
   `already open` → someone else's party, back out without touching it.
3. **The gate CLOSING is a correct outcome, not a crash.** After a request to staff, the watcher can
   carry the guest in the instant the table opens — so the sheet finishes the job and disappears.
   Reading `.sg-title` unconditionally timed out on a gate that had simply succeeded, which is the
   very thing P01355 exists to prove.

### Worth knowing about the dev stack (not a product fault)

French House's test tables carry a lot of debris from many sweeps — table 25 alone has ~90 CLOSED
sessions, and tables 20 and 29 have sessions still marked `open` from 2026-08-06 and 2026-08-17.
That is why any assumption about a table's state keeps breaking. Deliberately **not** cleared here:
closing sessions in bulk is a "delete whatever is there" filter over other sessions' work, which is
exactly what this ledger's own rules forbid.


### Pass 5 — what it settled, and the one thing it did not

Run against the merged, deployed `main`. **Blocks 1+2, 3a (52), 3b (31) and 4 green in the combined
run**; block 5's 25 rows green when run on their own. So every one of the 500 has passed on this
build — but not all five blocks in a single sitting.

**Block 5 is the problem, and it is the HARNESS.** It failed three times in three different ways,
each time for an environmental reason and never for a product reason:

1. the app's own per-table guest limits, tripped by the cumulative load of running five blocks in
   six minutes (the thing CLAUDE.md forbids). Fixed: `run-all.mjs` now waits out the limit window —
   75 s before the end-to-end block, 165 s before block 5, which joins twice and orders.
2. a table it had pre-checked as free acquiring a session before the tap. Fixed: the INSERT is now
   the check (`idx_one_open_session_per_table` means the database refuses a second open session, so
   trying is atomic where asking-then-trying is a race).
3. its own move-the-party test relocating its own session mid-run, after which later rows compared
   against the table number they had started with. Fixed: only assert the move when the update was
   actually accepted, and pick a destination the database will take.

**WHAT I GOT WRONG ABOUT THIS, and corrected the same day.** I wrote block 5's contention up as
"needs a decision about the shared test stack" and listed it for the owner. That was wrong, and he
said so: *"I still didn't understand what you need permission for."* Nothing here was his to decide.
Block 5 is a test script in this repo; making it survive a fixture it shares with ten other terminals
is the script's job, not a product choice, and there was no trade-off for him to weigh either way.
Escalating it put work back on him that was mine.

**How it is actually solved.** The whole attempt is now retryable (`runAttempt()` in
`scripts/sweep/t3/across-panels.mjs`): up to three goes, each cleaning up everything it made by id,
waiting 90 s for the restaurant's own limit window to roll over, and rotating to a different table so
a retry cannot walk back into the same contention. A REAL fault fails all three attempts; contention
passes on a later one. No dedicated restaurant, no cross-terminal convention, nothing for anyone to
approve.

---
---

# SWEEP 7 — Terminal 3 · P16101–P16600

**Branch** `sweep7/t3-basket-and-order` · **worktree** `../wt-s7-t3` · **port 4203** (proved mine —
the dev server printed `Local: http://localhost:4203` and nothing else answered there) · run
2026-08-22/23 against `origin/main` **b64951ad**.

## First: the 500 rows above were ALL re-run, and here is exactly how

**Every one of P01001–P01500 was re-executed. Every result cell above now carries `· s7 ✅`.**
Zero regressions. Two rows had their EXPECTATION corrected (they were wrong when written, not
broken now): **P01422** and **P01423** — both keep their ids, both say so in the row.

| the 500 rows | how they were re-executed |
|---|---|
| P01001–P01300 (code + rules) | (a) `npm run verify:guest-doors` — now 53 checks, all green; (b) the six named gates below; (c) a full re-read of all 17 files in the territory; (d) the 300 NEW static rows below, which re-assert most of the same invariants mechanically and from a different angle |
| P01207/8/9, P01227/8, P01250 | ran the named guards: `verify:table-ownership`, `verify:two-parties`, `verify:closed-session`, `verify:guest-recovery`, `verify:order-retry` (51 checks), `verify:clash-coverage` — all green |
| P01289, P01290 | `npm run typecheck` clean; `npm run lint` unchanged |
| P01301–P01351 (block 3a) | `node scripts/sweep/t3/basket-and-doors.mjs` — **52 passed, 0 failed** |
| P01352–P01375 (block 3b) | `node scripts/sweep/t3/order-end-to-end.mjs` — **31 passed, 0 failed** |
| P01376–P01425 (block 4) | `node scripts/sweep/t3/screens.mjs`, then I OPENED AND READ all eight captures |
| P01426–P01450 (block 5) | `node scripts/sweep/t3/across-panels.mjs` — **25 passed, 0 failed** |
| P01451–P01475 | traced by reading; most are also asserted mechanically now (section B and E below) |
| P01476–P01500 | judgment, re-read against today's code |

**`npm run sweep:t3` was green in ONE sitting — all five blocks — which sweep 6's own pass 5 could
not achieve** (its block 5 only passed when run alone). The retry logic sweep 6 added is what made
that possible.

**Two things had to be fixed before a re-run could mean anything at all** (item 1 of the PR):
the four runners hard-coded `http://localhost:4103` — sweep 6's port, which a later terminal either
finds empty or, far worse, finds serving ANOTHER live session's app; and the screenshots were
written to an absolute path inside one dead Claude session's scratch folder. Both are now
overridable (`T3_BASE`, `T3_SHOTS`) with the old values as defaults.

## How the 500 new rows are budgeted

| block | ids | count | what it is | how it runs |
|-------|-----|-------|-----------|-------------|
| A | P16101–P16400 | 300 | reading the code for correctness and rule-conformance | `npm run sweep:t3-static` |
| B | P16401–P16460 | 62 | watching the RENDERED screen, headless on 4203 | `npm run sweep:t3-live` |
| C | P16461–P16500 | 40 | captures I opened and looked at, and what measuring them settled | `node scripts/sweep/t3/s7-shots.mjs`, then read |
| D | P16501–P16540 | 40 | tracing one value guest → server → panel, by reading | read |
| E | P16541–P16570 | 30 | judgment: is this how a real restaurant needs it to work | read |
| F | P16571–P16600 | 30 | pre-empts, withdrawn candidates, and this run's own bookkeeping | read |

Blocks A and B print `P16xxx ok/FAIL` per row, so a re-run reproduces every result below by id.
Result key: ✅ pass · ❌ real problem (see `T3-findings.md`) · ⏭ skipped, reason written in ·
🔎 a candidate I measured and WITHDREW, with the reason, so nobody files it again.

## Block A — reading the code (P16101–P16400) · 300 rows, all ✅

Executed: `npm run sweep:t3-static` → **300 passed, 0 failed.** Each row's exact assertion is in
`scripts/sweep/t3/s7-checks.mjs`, labelled with its id; the sections are:

| ids | section | rows |
|---|---|---|
| P16101 | every file in the territory is present and non-empty | 1 |
| P16102–P16140 | **the saved-work queue's SECOND kind — a request for staff, end to end.** Sweep 6 checked the queue's HANDLING of a call thoroughly and never checked how one READS. This is where item 2 came from. | 39 |
| P16141–P16200 | **every awaited server call in the territory: is its answer read?** A systematic pass, because none of these calls throws — a timeout comes back as `{ ok:false, reason:"timed_out" }` — so "it didn't throw" is never evidence it worked. Items 4 and 5 came from here. | 60 |
| P16201–P16250 | **every skeleton, spinner and "one moment…": can it end?** Item 6 came from here. | 50 |
| P16251–P16300 | money and numbers on the guest's own screens: the two currency domains, the 99 ceiling, who is allowed to compute what | 50 |
| P16301–P16350 | which restaurant, which phone, which door — tenant scoping, re-asked from the other direction: not "do the two rules agree" but "is there any surface left that GUESSES" | 50 |
| P16351–P16400 | the project's own rules, re-asked of this territory: back-button registration, poll floors, batching, first-save-wins, feature switches, double-tap guards | 50 |

**Two of my own rows were wrong before the code was**, and both are recorded rather than quietly
loosened:

| row | what was wrong | what it asserts now |
|---|---|---|
| P16333 | matched the literal `[restaurantId]`, so it missed the basket, whose effect depends on `[restaurantId, features.allergies]` | is `restaurantId` in the dependency list **at all** |
| P16346 | asserted that no guest read uses `select("*")`, and two do — and **both are deliberate.** `getMenuItem()` reads ONE dish row by slug, and `mapRow()`'s `has(row, "col")` mechanism exists precisely to tell "column not selected" (omit the key) from "selected but null" (keep the default); its own comment says the narrowing "must shrink the CARD payload only". `getCategories()` is a six-column table capped at 300. Narrowing either would silently drop keys the dish page reads. | the rule that actually protects egress: the BUSIEST read is column-listed, and the two full-row reads are exactly that documented pair |

### Block A rows in full (P16101–P16400)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P16101 | every file in the territory is present and non-empty | `npm run sweep:t3-static` | ✅ |  |
| P16102 | the queue's item type declares the two kinds explicitly | `npm run sweep:t3-static` | ✅ |  |
| P16103 | a row written before `kind` existed is read as an order, not as neither | `npm run sweep:t3-static` | ✅ |  |
| P16104 | a call carries the words the diner tapped, not a code | `npm run sweep:t3-static` | ✅ |  |
| P16105 | a call is POSTed to the waiter endpoint, never to place-order | `npm run sweep:t3-static` | ✅ |  |
| P16106 | …carrying an at-most-once id so it rings the floor once however often it is resent | `npm run sweep:t3-static` | ✅ |  |
| P16107 | …and the time it was asked, so the floor can judge how old it is | `npm run sweep:t3-static` | ✅ |  |
| P16108 | an order | `npm run sweep:t3-static` | ✅ | unlike a call — is marked as a replay so the server can refuse a moved-on table |
| P16109 | a call older than ten minutes is dropped on the phone rather than delivered late | `npm run sweep:t3-static` | ✅ |  |
| P16110 | …and the diner is told that in their own words, not with a code | `npm run sweep:t3-static` | ✅ |  |
| P16111 | repeated bell taps in a dead spot collapse into ONE saved call | `npm run sweep:t3-static` | ✅ |  |
| P16112 | …keeping the NEWEST note rather than the first | `npm run sweep:t3-static` | ✅ |  |
| P16113 | …and matched on table AND token AND mode, so two tables never merge | `npm run sweep:t3-static` | ✅ |  |
| P16114 | a delivered call is removed from the queue, not left to send twice | `npm run sweep:t3-static` | ✅ |  |
| P16115 | the saved-work list can tell a call from an order | `npm run sweep:t3-static` | ✅ |  |
| P16116 | …and names what was asked for | `npm run sweep:t3-static` | ✅ |  |
| P16117 | …falling back to plain words when an old row carries no note | `npm run sweep:t3-static` | ✅ |  |
| P16118 | …and never prints the count of an empty basket for one | `npm run sweep:t3-static` | ✅ |  |
| P16119 | the row's second line says what will happen to a call, not 'waiting to send' | `npm run sweep:t3-static` | ✅ |  |
| P16120 | the chip counts requests for staff as requests | `npm run sweep:t3-static` | ✅ |  |
| P16121 | …and drops the noun entirely when the queue is mixed, rather than picking a wrong one | `npm run sweep:t3-static` | ✅ |  |
| P16122 | …and still says 'order' when that is all there is | `npm run sweep:t3-static` | ✅ |  |
| P16123 | the plural of 'request for staff' is not 'request for staffs' | `npm run sweep:t3-static` | ✅ |  |
| P16124 | a call has no price, so no money is invented on its row | `npm run sweep:t3-static` | ✅ |  |
| P16125 | 'Order the rest' cannot appear on a call (it has no lines and nothing blocked) | `npm run sweep:t3-static` | ✅ |  |
| P16126 | …and the queue refuses one anyway if it is asked | `npm run sweep:t3-static` | ✅ |  |
| P16127 | the sheet's screen-reader label covers both kinds | `npm run sweep:t3-static` | ✅ |  |
| P16128 | a call saved with no signal from the WAITER POPUP goes through the queue | `npm run sweep:t3-static` | ✅ |  |
| P16129 | …and one saved from inside a table session carries the session token instead of a table | `npm run sweep:t3-static` | ✅ |  |
| P16130 | both promise automatic sending ONLY when the phone really stored it | `npm run sweep:t3-static` | ✅ |  |
| P16131 | a call that could not even be saved says so honestly instead of promising | `npm run sweep:t3-static` | ✅ |  |
| P16132 | the session path falls through to a live attempt rather than lying, if saving throws | `npm run sweep:t3-static` | ✅ |  |
| P16133 | the queue is capped, so one phone cannot pile up unbounded saved calls | `npm run sweep:t3-static` | ✅ |  |
| P16134 | …and the OLDEST is the one retired, with a sentence written for a diner | `npm run sweep:t3-static` | ✅ |  |
| P16135 | a call and an order both reset all three attempt counters on 'Try again' | `npm run sweep:t3-static` | ✅ |  |
| P16136 | the waiter endpoint's refusals all have diner-facing words on this side | `npm run sweep:t3-static` | ✅ |  |
| P16137 | a call never reaches the tracker's active-orders list (it is not an order) | `npm run sweep:t3-static` | ✅ |  |
| P16138 | the bell disappears entirely when waiter calls are switched off | `npm run sweep:t3-static` | ✅ |  |
| P16139 | …and the popup cannot be opened even by the event, for the same restaurant | `npm run sweep:t3-static` | ✅ |  |
| P16140 | …both read the switch for the restaurant ON SCREEN, not a hardcoded one | `npm run sweep:t3-static` | ✅ |  |
| P16141 | the shared rpc helper never throws a timeout, it returns a code | `npm run sweep:t3-static` | ✅ |  |
| P16142 | …and an empty answer is a failure, not a silent success | `npm run sweep:t3-static` | ✅ |  |
| P16143 | …and a timeout has its own predicate, so callers can word it differently from a refusal | `npm run sweep:t3-static` | ✅ |  |
| P16144 | no handler in the whole territory awaits leaveSession and drops the answer | `npm run sweep:t3-static` | ✅ |  |
| P16145 | no handler awaits approveMember and drops the answer | `npm run sweep:t3-static` | ✅ |  |
| P16146 | no handler awaits removeMember and drops the answer | `npm run sweep:t3-static` | ✅ |  |
| P16147 | no handler awaits setAutoApprove and drops the answer | `npm run sweep:t3-static` | ✅ |  |
| P16148 | the join flow reads its answer and branches on every ending it can give | `npm run sweep:t3-static` | ✅ |  |
| P16149 | …and treats anything else as 'could not reach', keeping the guest's place | `npm run sweep:t3-static` | ✅ |  |
| P16150 | the waiter-call-in-session path reads its answer | `npm run sweep:t3-static` | ✅ |  |
| P16151 | …and tells a timeout apart from a refusal in what it says | `npm run sweep:t3-static` | ✅ |  |
| P16152 | …and never tells a diner to retry something the restaurant rate-limited | `npm run sweep:t3-static` | ✅ |  |
| P16153 | the request-to-staff path reads its answer before promising anything | `npm run sweep:t3-static` | ✅ |  |
| P16154 | …and 'already sent' counts as landed (staff have it; a second helps nobody) | `npm run sweep:t3-static` | ✅ |  |
| P16155 | …and a failure is TOASTED, so it shows on the screens that render no note | `npm run sweep:t3-static` | ✅ |  |
| P16156 | the public waiter call reads its answer and has a branch per ending | `npm run sweep:t3-static` | ✅ |  |
| P16157 | …and a thrown request still says something | `npm run sweep:t3-static` | ✅ |  |
| P16158 | correcting a placed order's table reads whether the server accepted it | `npm run sweep:t3-static` | ✅ |  |
| P16159 | …and says why, in words a diner can act on | `npm run sweep:t3-static` | ✅ |  |
| P16160 | …and only claims 'the kitchen has been told' after it agreed | `npm run sweep:t3-static` | ✅ |  |
| P16161 | the returning-guest greeting checks the answer before using the name | `npm run sweep:t3-static` | ✅ |  |
| P16162 | …and an empty name is not greeted as one | `npm run sweep:t3-static` | ✅ |  |
| P16163 | the shared-cart first merge reads whether it was refused, and re-merges rather than forcing | `npm run sweep:t3-static` | ✅ |  |
| P16164 | …and a later delta adopts whatever the server merged back | `npm run sweep:t3-static` | ✅ |  |
| P16165 | …and a transient pull failure does NOT reset the sync state (it would resurrect a removed dish) | `npm run sweep:t3-static` | ✅ |  |
| P16166 | the tracker's status poll reads each answer and counts a miss rather than assuming | `npm run sweep:t3-static` | ✅ |  |
| P16167 | …and does not count a miss while the phone is offline | `npm run sweep:t3-static` | ✅ |  |
| P16168 | …and only finalises after three consecutive misses | `npm run sweep:t3-static` | ✅ |  |
| P16169 | …and a real answer resets the miss counter | `npm run sweep:t3-static` | ✅ |  |
| P16170 | the presence heartbeat is deliberately fire-and-forget (nothing waits on it) | `npm run sweep:t3-static` | ✅ |  |
| P16171 | setMemberName is deliberately best-effort and cannot break the flow it sits in | `npm run sweep:t3-static` | ✅ |  |
| P16172 | …and the name is stored on the phone first, so the flow never depends on that call | `npm run sweep:t3-static` | ✅ |  |
| P16173 | placing an order reads the answer through one function, so every path is guarded alike | `npm run sweep:t3-static` | ✅ |  |
| P16174 | …and both order paths go through it | `npm run sweep:t3-static` | ✅ |  |
| P16175 | …so there is no way left to place a guest order without an at-most-once key | `npm run sweep:t3-static` | ✅ |  |
| P16176 | a dropped request is classed as BUSY, so the caller saves it | `npm run sweep:t3-static` | ✅ |  |
| P16177 | a 5xx is classed as busy too | `npm run sweep:t3-static` | ✅ |  |
| P16178 | a 409 that asks us to retry is busy, not a refusal | `npm run sweep:t3-static` | ✅ |  |
| P16179 | everything else is a refusal the diner must SEE, carrying the code and the dish | `npm run sweep:t3-static` | ✅ |  |
| P16180 | the caller reads that code rather than matching on the sentence | `npm run sweep:t3-static` | ✅ |  |
| P16181 | …and there is exactly ONE place that parses it | `npm run sweep:t3-static` | ✅ |  |
| P16182 | an unrecognised refusal code is never echoed to a diner | `npm run sweep:t3-static` | ✅ |  |
| P16183 | the busy path saves the basket rather than losing it, on the QR door | `npm run sweep:t3-static` | ✅ |  |
| P16184 | …and on the table-session door | `npm run sweep:t3-static` | ✅ |  |
| P16185 | …and if saving ALSO fails, the diner gets the honest error rather than a false promise | `npm run sweep:t3-static` | ✅ |  |
| P16186 | the gate reports its outcome exactly once per action | `npm run sweep:t3-static` | ✅ |  |
| P16187 | …and that flag is reset when a NEW action starts, or a second order could never report | `npm run sweep:t3-static` | ✅ |  |
| P16188 | dismissing the gate always reports the action as cancelled, so no button sticks on 'Placing…' | `npm run sweep:t3-static` | ✅ |  |
| P16189 | every completion carries `action`, so the cart's listener recognises its own | `npm run sweep:t3-static` | ✅ |  |
| P16190 | the cart ignores a completion for someone else's action | `npm run sweep:t3-static` | ✅ |  |
| P16191 | …and stops listening once its own has arrived | `npm run sweep:t3-static` | ✅ |  |
| P16192 | a cancelled session order re-enables Place Order | `npm run sweep:t3-static` | ✅ |  |
| P16193 | a queued session order clears the basket so it cannot be sent twice | `npm run sweep:t3-static` | ✅ |  |
| P16194 | a failed session order leaves the basket alone (the gate showed its own message) | `npm run sweep:t3-static` | ✅ |  |
| P16195 | the location check's every outcome has a sentence | `npm run sweep:t3-static` | ✅ |  |
| P16196 | a table-state read that fails is not blamed on the diner's own internet | `npm run sweep:t3-static` | ✅ |  |
| P16197 | only three definitive endings drop a session on the way in | `npm run sweep:t3-static` | ✅ |  |
| P16198 | …and a network blip keeps the guest's spot with a working Retry | `npm run sweep:t3-static` | ✅ |  |
| P16199 | the approval poll drops a session only on those same three | `npm run sweep:t3-static` | ✅ |  |
| P16200 | …and anything else keeps polling rather than costing a head their role | `npm run sweep:t3-static` | ✅ |  |
| P16201 | the live table bill can tell 'still loading' from 'cannot be loaded' | `npm run sweep:t3-static` | ✅ |  |
| P16202 | …and a FIRST failed read reaches an honest sentence, not three pulsing bars | `npm run sweep:t3-static` | ✅ |  |
| P16203 | …which says nothing is lost | `npm run sweep:t3-static` | ✅ |  |
| P16204 | …and offers a way to ask again | `npm run sweep:t3-static` | ✅ |  |
| P16205 | …and that retry clears the stalled state, so a success can paint | `npm run sweep:t3-static` | ✅ |  |
| P16206 | a good read always clears the stalled state | `npm run sweep:t3-static` | ✅ |  |
| P16207 | …and stalled is ONLY set when nothing has ever loaded (a blip must not blank a live bill) | `npm run sweep:t3-static` | ✅ |  |
| P16208 | the loading skeleton still exists for the ordinary first load | `npm run sweep:t3-static` | ✅ |  |
| P16209 | …and the true 'nothing ordered yet' message is still a separate, later state | `npm run sweep:t3-static` | ✅ |  |
| P16210 | the green live dot stops claiming a live connection while that message is up | `npm run sweep:t3-static` | ✅ |  |
| P16211 | the gate's 'One moment…' step is always left by one of a finite set of endings | `npm run sweep:t3-static` | ✅ |  |
| P16212 | …the order path leaves it by placing, refusing, saving or closing | `npm run sweep:t3-static` | ✅ |  |
| P16213 | …and a blocked order lands on its own screen rather than sitting on 'One moment' | `npm run sweep:t3-static` | ✅ |  |
| P16214 | …and the call path leaves it the same way | `npm run sweep:t3-static` | ✅ |  |
| P16215 | the waiting-for-approval poll pauses on a hidden tab instead of beating in the background | `npm run sweep:t3-static` | ✅ |  |
| P16216 | …and slows down the longer someone waits, rather than hammering at a fixed rate | `npm run sweep:t3-static` | ✅ |  |
| P16217 | …and the very first check is almost immediate, so a fast approval is not made to wait | `npm run sweep:t3-static` | ✅ |  |
| P16218 | …and it stops the moment the wait is genuinely over | `npm run sweep:t3-static` | ✅ |  |
| P16219 | …and a throwing tick keeps the poll alive rather than stranding the guest | `npm run sweep:t3-static` | ✅ |  |
| P16220 | the not-open watcher ends by itself when the action it was waiting for is gone | `npm run sweep:t3-static` | ✅ |  |
| P16221 | the gate closing IS a valid ending of the whole flow, not a stall | `npm run sweep:t3-static` | ✅ |  |
| P16222 | …and unmounting stops every timer and releases the camera | `npm run sweep:t3-static` | ✅ |  |
| P16223 | the camera is released when a scan is abandoned by tapping 'Type it instead' | `npm run sweep:t3-static` | ✅ |  |
| P16224 | …and when the camera cannot be opened at all, the guest is told to type instead | `npm run sweep:t3-static` | ✅ |  |
| P16225 | …and on a phone with no scanner at all, before any camera prompt | `npm run sweep:t3-static` | ✅ |  |
| P16226 | …and a frame that fails to decode is normal, not an error shown to anyone | `npm run sweep:t3-static` | ✅ |  |
| P16227 | the head's approve prompt cannot trap them | `npm run sweep:t3-static` | ✅ | 'Later' snoozes it |
| P16228 | …and it comes back afterwards, so a waiting friend is not forgotten | `npm run sweep:t3-static` | ✅ |  |
| P16229 | …and the back button snoozes rather than leaving the site | `npm run sweep:t3-static` | ✅ |  |
| P16230 | …registered unconditionally, above the early return (Rules of Hooks) | `npm run sweep:t3-static` | ✅ |  |
| P16231 | the tracker finalises an order the server no longer knows, instead of a 3-hour ghost | `npm run sweep:t3-static` | ✅ |  |
| P16232 | …and says so once, not on every poll | `npm run sweep:t3-static` | ✅ |  |
| P16233 | …and an unexpected status falls back rather than crashing on a missing label | `npm run sweep:t3-static` | ✅ |  |
| P16234 | a dismissing strip is frozen, so a newly-arrived order cannot fly out in its place | `npm run sweep:t3-static` | ✅ |  |
| P16235 | …and an OS-cancelled gesture resets cleanly instead of wedging it | `npm run sweep:t3-static` | ✅ |  |
| P16236 | …and pointer capture is taken on down, so a fast flick still delivers the release | `npm run sweep:t3-static` | ✅ |  |
| P16237 | a tap is told from a drag by a real threshold, not by luck | `npm run sweep:t3-static` | ✅ |  |
| P16238 | …and a tap opens the live tab rather than doing nothing | `npm run sweep:t3-static` | ✅ |  |
| P16239 | the offline saved-work list closes itself when the queue empties | `npm run sweep:t3-static` | ✅ |  |
| P16240 | …and draws nothing at all when there is nothing waiting | `npm run sweep:t3-static` | ✅ |  |
| P16241 | …and a tap on Try again / Remove shows it was HEARD while the work is in flight | `npm run sweep:t3-static` | ✅ |  |
| P16242 | …one at a time, so two rows cannot both claim to be working | `npm run sweep:t3-static` | ✅ |  |
| P16243 | the bill's own live list refreshes on a timer while open, and stops when closed | `npm run sweep:t3-static` | ✅ |  |
| P16244 | …and that timer only exists while the panel is open | `npm run sweep:t3-static` | ✅ |  |
| P16245 | a corrupt saved basket cannot leave the bill on a blank screen | `npm run sweep:t3-static` | ✅ |  |
| P16246 | …and a failed parse falls back to an empty basket rather than throwing | `npm run sweep:t3-static` | ✅ |  |
| P16247 | an empty basket says so in words | `npm run sweep:t3-static` | ✅ |  |
| P16248 | a failed menu fetch lets a later open retry rather than wedging the bill | `npm run sweep:t3-static` | ✅ |  |
| P16249 | the greeting can never block the menu, whatever the server does | `npm run sweep:t3-static` | ✅ |  |
| P16250 | …and it is asked at most once per browser session even if it fails | `npm run sweep:t3-static` | ✅ |  |
| P16251 | no price ever travels from the browser with an order | `npm run sweep:t3-static` | ✅ |  |
| P16252 | …the order body carries id, qty, options-by-name, removed and note, and nothing else | `npm run sweep:t3-static` | ✅ |  |
| P16253 | …and an option travels as group+label only, never as an amount | `npm run sweep:t3-static` | ✅ |  |
| P16254 | the STORED total is the USD domain | `npm run sweep:t3-static` | ✅ |  |
| P16255 | …and the DISPLAYED total is the display-currency domain | `npm run sweep:t3-static` | ✅ |  |
| P16256 | …and it is the USD one that is written into the tracker record | `npm run sweep:t3-static` | ✅ |  |
| P16257 | …and the session path passes the USD one too | `npm run sweep:t3-static` | ✅ |  |
| P16258 | GST is added only over the lines that are priced NET | `npm run sweep:t3-static` | ✅ |  |
| P16259 | …so a tax-inclusive line is never taxed twice | `npm run sweep:t3-static` | ✅ |  |
| P16260 | a composition restaurant's guest quote carries no GST at all | `npm run sweep:t3-static` | ✅ |  |
| P16261 | …and no GST ROW is drawn when there is none to draw | `npm run sweep:t3-static` | ✅ |  |
| P16262 | the tax RATE comes from the restaurant's settings, never a hardcoded 5% in the maths | `npm run sweep:t3-static` | ✅ |  |
| P16263 | …and the placeholder before settings load is stated as one | `npm run sweep:t3-static` | ✅ |  |
| P16264 | …and the rules come from ONE resolver, not a second formula here | `npm run sweep:t3-static` | ✅ |  |
| P16265 | a dish's own tax behaviour is resolved by one rule | `npm run sweep:t3-static` | ✅ |  |
| P16266 | …and an MRP line is identified by that same rule, not by hand | `npm run sweep:t3-static` | ✅ |  |
| P16267 | the live table bill recomputes NOTHING | `npm run sweep:t3-static` | ✅ | every figure is the server's |
| P16268 | …its MRP stamp is read off the server's own frozen ticket lines | `npm run sweep:t3-static` | ✅ |  |
| P16269 | …its discount percentage is written by the same maker as the paper bill | `npm run sweep:t3-static` | ✅ |  |
| P16270 | …and the MRP money row is only drawn when the SERVER sends the split | `npm run sweep:t3-static` | ✅ |  |
| P16271 | …and a composition restaurant's GST row is hidden only when the server AGREES tax is 0 | `npm run sweep:t3-static` | ✅ |  |
| P16272 | the mini-cart shows a SUBTOTAL and never claims to be the amount payable | `npm run sweep:t3-static` | ✅ |  |
| P16273 | …summed line by line in the display currency, so it matches the bill to the rupee | `npm run sweep:t3-static` | ✅ |  |
| P16274 | …and its item count is the number of ITEMS, not the number of lines | `npm run sweep:t3-static` | ✅ |  |
| P16275 | the bill's header count agrees with that rule | `npm run sweep:t3-static` | ✅ |  |
| P16276 | a saved quantity is clamped to a whole number between 1 and 99 | `npm run sweep:t3-static` | ✅ |  |
| P16277 | …so the quote can never exceed what the kitchen would make | `npm run sweep:t3-static` | ✅ |  |
| P16278 | …and the ceiling is explained rather than returning in silence | `npm run sweep:t3-static` | ✅ |  |
| P16279 | …with a neutral style, because nothing went wrong | `npm run sweep:t3-static` | ✅ |  |
| P16280 | every OTHER add path respects the same 99 ceiling | `npm run sweep:t3-static` | ✅ |  |
| P16281 | an add-on's price is minor-rounded so base + extras equals the total shown | `npm run sweep:t3-static` | ✅ |  |
| P16282 | …and the menu card's own price treatment is used for a MENU price | `npm run sweep:t3-static` | ✅ |  |
| P16283 | the dish popup's unit is base plus the chosen extras | `npm run sweep:t3-static` | ✅ |  |
| P16284 | …and the line total is that unit times the quantity | `npm run sweep:t3-static` | ✅ |  |
| P16285 | the tracker's own item count sums quantities, not lines | `npm run sweep:t3-static` | ✅ |  |
| P16286 | a currency switch repaints every money surface rather than leaving a stale symbol | `npm run sweep:t3-static` | ✅ |  |
| P16287 | …and the live table bill reads the currency when it mounts | `npm run sweep:t3-static` | ✅ |  |
| P16288 | nothing in the territory can erase, hide or edit an issued sale | `npm run sweep:t3-static` | ✅ |  |
| P16289 | the one correction a guest can make goes through a guarded server call | `npm run sweep:t3-static` | ✅ |  |
| P16290 | …and it changes a table number only, never money | `npm run sweep:t3-static` | ✅ |  |
| P16291 | the guest is never shown the staff-only 'ready' stage on the live bill | `npm run sweep:t3-static` | ✅ |  |
| P16292 | …nor on the floating strip | `npm run sweep:t3-static` | ✅ |  |
| P16293 | a ₹0 GST row is removed rather than printed as zero | `npm run sweep:t3-static` | ✅ |  |
| P16294 | the pairing suggestion never offers something already on the bill | `npm run sweep:t3-static` | ✅ |  |
| P16295 | …nor anything sold out | `npm run sweep:t3-static` | ✅ |  |
| P16296 | …and it disappears entirely when the basket is empty | `npm run sweep:t3-static` | ✅ |  |
| P16297 | Place Order names the exact sold-out dish rather than a generic retry | `npm run sweep:t3-static` | ✅ |  |
| P16298 | …de-duplicated, so one dish twice is named once | `npm run sweep:t3-static` | ✅ |  |
| P16299 | the stored order total and the shown total are computed from separate line arrays | `npm run sweep:t3-static` | ✅ |  |
| P16300 | …and each carries the SAME resolved tax behaviour, so the two can never disagree | `npm run sweep:t3-static` | ✅ |  |
| P16301 | there is exactly ONE function that answers 'which restaurant is this tab on' | `npm run sweep:t3-static` | ✅ |  |
| P16302 | …and the provider imports it rather than carrying a second copy | `npm run sweep:t3-static` | ✅ |  |
| P16303 | …and there is no path-matching regex left in the provider deciding the tenant | `npm run sweep:t3-static` | ✅ |  |
| P16304 | the provider reads it inside an effect, never during render (there is no sessionStorage on the server) | `npm run sweep:t3-static` | ✅ |  |
| P16305 | …so the first render is identical on the server and the client | `npm run sweep:t3-static` | ✅ |  |
| P16306 | …and `ready` starts true only for the routes that genuinely ARE restaurant #1 | `npm run sweep:t3-static` | ✅ |  |
| P16307 | a late reply for restaurant A cannot land on restaurant B | `npm run sweep:t3-static` | ✅ |  |
| P16308 | …and a stale NAME cannot flash during the resolve window | `npm run sweep:t3-static` | ✅ |  |
| P16309 | a failed lookup can never leave a widget waiting for ever | `npm run sweep:t3-static` | ✅ |  |
| P16310 | the slug the provider hands out is folded to lower case, so one restaurant is one bucket | `npm run sweep:t3-static` | ✅ |  |
| P16311 | …and storage folds it the same way, so the two cannot disagree on spelling | `npm run sweep:t3-static` | ✅ |  |
| P16312 | …and the explicit-slug variants fold too, for the outbox flushing another restaurant's order | `npm run sweep:t3-static` | ✅ |  |
| P16313 | a /r/<slug> visit PINS the tab, so a later un-prefixed page stays on that restaurant | `npm run sweep:t3-static` | ✅ |  |
| P16314 | bare /menu and /item RE-pin to restaurant #1, even after visiting another | `npm run sweep:t3-static` | ✅ |  |
| P16315 | any other door uses the pin, never a guess | `npm run sweep:t3-static` | ✅ |  |
| P16316 | every per-restaurant guest key is suffixed with the slug | `npm run sweep:t3-static` | ✅ |  |
| P16317 | the basket is one of them | `npm run sweep:t3-static` | ✅ |  |
| P16318 | the tracked orders are one of them | `npm run sweep:t3-static` | ✅ |  |
| P16319 | the table session is one of them | `npm run sweep:t3-static` | ✅ |  |
| P16320 | the declared allergy list is one of them | `npm run sweep:t3-static` | ✅ |  |
| P16321 | the guest's chosen name is one of them | `npm run sweep:t3-static` | ✅ |  |
| P16322 | the location consent is one of them | `npm run sweep:t3-static` | ✅ |  |
| P16323 | …and the four DEVICE-wide preferences are deliberately NOT scoped | `npm run sweep:t3-static` | ✅ |  |
| P16324 | a cross-tab wake-up ignores ANOTHER restaurant's scoped copy of the same key | `npm run sweep:t3-static` | ✅ |  |
| P16325 | …and the basket uses it rather than a bare key comparison | `npm run sweep:t3-static` | ✅ |  |
| P16326 | the one-time legacy migration cannot overwrite an already-scoped value | `npm run sweep:t3-static` | ✅ |  |
| P16327 | …and it deletes the old unscoped key so no other restaurant can read it | `npm run sweep:t3-static` | ✅ |  |
| P16328 | …and it runs at most once per device | `npm run sweep:t3-static` | ✅ |  |
| P16329 | every storage read and write is wrapped, so private mode cannot crash the menu | `npm run sweep:t3-static` | ✅ |  |
| P16330 | the outbox writes a flushed order's tracker entry under the ORDER's restaurant | `npm run sweep:t3-static` | ✅ |  |
| P16331 | …and remembers that restaurant when the order is first saved | `npm run sweep:t3-static` | ✅ |  |
| P16332 | …and the basket passes it explicitly rather than relying on where the tab is later | `npm run sweep:t3-static` | ✅ |  |
| P16333 | every widget that asks the server something per-restaurant re-asks on a tenant change | `npm run sweep:t3-static` | ✅ |  |
| P16334 | the gate reads the restaurant through a ref, so a handler cannot freeze an old one | `npm run sweep:t3-static` | ✅ |  |
| P16335 | …and every server call in it reads that ref rather than the closed-over value | `npm run sweep:t3-static` | ✅ |  |
| P16336 | the gate shows the restaurant's OWN name, never a hardcoded brand for another tenant | `npm run sweep:t3-static` | ✅ |  |
| P16337 | 'Change table' returns the guest to THIS restaurant's menu | `npm run sweep:t3-static` | ✅ |  |
| P16338 | the greeting waits until the restaurant is actually known | `npm run sweep:t3-static` | ✅ |  |
| P16339 | …and is remembered per restaurant, so two tenants each get their own greeting | `npm run sweep:t3-static` | ✅ |  |
| P16340 | the basket's lazy menu fetch asks for THIS tab's restaurant | `npm run sweep:t3-static` | ✅ |  |
| P16341 | …once only, on the first open, not on every page load | `npm run sweep:t3-static` | ✅ |  |
| P16342 | …and it reuses the server-cached bundle rather than a fresh table read | `npm run sweep:t3-static` | ✅ |  |
| P16343 | pruning stale basket lines reads storage, not a possibly-stale render value | `npm run sweep:t3-static` | ✅ |  |
| P16344 | …and only ever runs on a NON-EMPTY menu, so a bad payload cannot wipe a real basket | `npm run sweep:t3-static` | ✅ |  |
| P16345 | …and it SAYS what it removed | `npm run sweep:t3-static` | ✅ |  |
| P16346 | the busiest guest read names its columns, and the two full-row reads are the documented pair | `npm run sweep:t3-static` | ✅ |  |
| P16347 | …and has a limit | `npm run sweep:t3-static` | ✅ |  |
| P16348 | …and the busiest read does not ship detail-only fields nobody draws | `npm run sweep:t3-static` | ✅ |  |
| P16349 | settings are read through one cache that dedups ~9 widgets into one request | `npm run sweep:t3-static` | ✅ |  |
| P16350 | …and a realtime breadcrumb can force a genuine re-read of them | `npm run sweep:t3-static` | ✅ |  |
| P16351 | every popup in the territory registers with the back-button manager | `npm run sweep:t3-static` | ✅ |  |
| P16352 | …including BOTH of the table card's own dialogs | `npm run sweep:t3-static` | ✅ |  |
| P16353 | …each with its own name, so two layers cannot collide | `npm run sweep:t3-static` | ✅ |  |
| P16354 | no popup hand-rolls history handling | `npm run sweep:t3-static` | ✅ |  |
| P16355 | the confirm popup also closes on Escape, and only listens while open | `npm run sweep:t3-static` | ✅ |  |
| P16356 | no poll in the territory is faster than the 60s backstop, bar the head's approve prompt | `npm run sweep:t3-static` | ✅ |  |
| P16357 | …and that 4-second one is justified in words, because someone is waiting to be let in | `npm run sweep:t3-static` | ✅ |  |
| P16358 | …and it does not poll a background tab at all | `npm run sweep:t3-static` | ✅ |  |
| P16359 | realtime drives the updates and the timer is only the backstop | `npm run sweep:t3-static` | ✅ |  |
| P16360 | …and every one of those listeners is removed on unmount | `npm run sweep:t3-static` | ✅ |  |
| P16361 | the presence heartbeat only beats while the tab is visible | `npm run sweep:t3-static` | ✅ |  |
| P16362 | …and stops and restarts on the visibility change rather than checking inside a tick | `npm run sweep:t3-static` | ✅ |  |
| P16363 | …and its effect depends on a derived boolean, not the whole state object | `npm run sweep:t3-static` | ✅ |  |
| P16364 | rapid basket edits batch into ONE push to the shared cart | `npm run sweep:t3-static` | ✅ |  |
| P16365 | …and a push scheduled for a session that has since gone idle is not sent | `npm run sweep:t3-static` | ✅ |  |
| P16366 | …and our own write of the local basket does not trigger our own push | `npm run sweep:t3-static` | ✅ |  |
| P16367 | the first join MERGES rather than either side winning outright | `npm run sweep:t3-static` | ✅ |  |
| P16368 | …with a line identity that keeps the same dish with different options apart | `npm run sweep:t3-static` | ✅ |  |
| P16369 | …and the one whole-array write tells the server what it was editing FROM | `npm run sweep:t3-static` | ✅ |  |
| P16370 | …so a concurrent add is REFUSED and re-merged, never overwritten | `npm run sweep:t3-static` | ✅ |  |
| P16371 | every later change is a DELTA, so a co-diner's add is never dropped | `npm run sweep:t3-static` | ✅ |  |
| P16372 | …computed against the last state we synced, not against a render value | `npm run sweep:t3-static` | ✅ |  |
| P16373 | …and a delta with nothing in it is not sent at all | `npm run sweep:t3-static` | ✅ |  |
| P16374 | an unapproved member keeps a private local basket, not the table's | `npm run sweep:t3-static` | ✅ |  |
| P16375 | …and sees no live table bill at all | `npm run sweep:t3-static` | ✅ |  |
| P16376 | …and no live per-dish progress | `npm run sweep:t3-static` | ✅ |  |
| P16377 | …and does not count as connected for the add-to-cart gate | `npm run sweep:t3-static` | ✅ |  |
| P16378 | the answer to 'can this guest order?' is published BEFORE the first poll | `npm run sweep:t3-static` | ✅ |  |
| P16379 | a definitive session ending drops this device's tracked orders | `npm run sweep:t3-static` | ✅ |  |
| P16380 | …and the shared basket with it | `npm run sweep:t3-static` | ✅ |  |
| P16381 | …and the remembered table, so the next party is not pre-filled with it | `npm run sweep:t3-static` | ✅ |  |
| P16382 | …and a guest who was never connected is not toasted 'you were removed' | `npm run sweep:t3-static` | ✅ |  |
| P16383 | a staff table SHIFT is followed by the phone | `npm run sweep:t3-static` | ✅ |  |
| P16384 | …and announced, so realtime resubscribes to the new table | `npm run sweep:t3-static` | ✅ |  |
| P16385 | a guest PROMOTED to head has the new role persisted | `npm run sweep:t3-static` | ✅ |  |
| P16386 | leaving mid-order is blocked with a real way forward, not a dead end | `npm run sweep:t3-static` | ✅ |  |
| P16387 | …on BOTH the leave and the change-table buttons | `npm run sweep:t3-static` | ✅ |  |
| P16388 | an allergy the guest declared order-wide is not repeated on the line | `npm run sweep:t3-static` | ✅ |  |
| P16389 | editing a line drops ONLY that line, matched on id AND its option signature | `npm run sweep:t3-static` | ✅ |  |
| P16390 | a dish with no declared allergens still offers the common six | `npm run sweep:t3-static` | ✅ |  |
| P16391 | a saved free-text allergy re-opens as free text, not as a phantom chip | `npm run sweep:t3-static` | ✅ |  |
| P16392 | a note is not re-saved on edit when notes have since been switched off | `npm run sweep:t3-static` | ✅ |  |
| P16393 | …nor free text when the free-text switch is off | `npm run sweep:t3-static` | ✅ |  |
| P16394 | a switched-off extra cannot reach the wire from a line saved before it was switched off | `npm run sweep:t3-static` | ✅ |  |
| P16395 | …and the order-wide allergy list is emptied entirely when allergies are off | `npm run sweep:t3-static` | ✅ |  |
| P16396 | a second tap on Add cannot double-save a line | `npm run sweep:t3-static` | ✅ |  |
| P16397 | a second tap on Place Order cannot fire two orders before React disables the button | `npm run sweep:t3-static` | ✅ |  |
| P16398 | a second tap on a join button cannot create a duplicate membership | `npm run sweep:t3-static` | ✅ |  |
| P16399 | a second tap on 'Request a waiter' cannot POST twice | `npm run sweep:t3-static` | ✅ |  |
| P16400 | …and the guard is taken BEFORE the await, not after it | `npm run sweep:t3-static` | ✅ |  |

## Block B — watching the rendered screen (P16401–P16460) · 62 rows, all ✅

Executed: `T3_BASE=http://localhost:4203 npm run sweep:t3-live` → **62 passed, 0 failed.**
Every row reads what the screen actually shows — visible text, a count, a bounding box — never
"the source contains it". Writes nothing to any restaurant: the control restaurant is read or
driven OFFLINE, and the two rows that need a session seed one in the PHONE's own storage and block
the read that would confirm it.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P16401 | the control restaurant's menu paints its own dishes | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16402 | …with ITS OWN name on the page, never restaurant #1's | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16403 | …and no uncaught error on first paint | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16404 | tapping + adds the dish rather than opening a table gate this restaurant switched off | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16405 | …and the pill appears | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16406 | …naming a count and a price in the same breath | `npm run sweep:t3-live` on port 4203 | ✅ | 1 item ₹219 View bill |
| P16407 | …and no leaked code text anywhere on the pill | `npm run sweep:t3-live` on port 4203 | ✅ | 1 item ₹219 View bill |
| P16408 | the pill opens the bill | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16409 | …which carries no leaked code text either | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16410 | …and no zero-priced line | `npm run sweep:t3-live` on port 4203 | ✅ | Back Your Bill · 1 item Current bill Live status Virgin Mojito − 1x + Edit ₹219 ✨ Goes wel |
| P16411 | the bill offers the two tabs | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16412 | …with exactly one of them active | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16413 | with table sessions OFF, the Live-status tab shows no shared table bill | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16414 | …and does not leave a pulsing skeleton behind either | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16415 | switching back shows the bill again | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16416 | the + at the ceiling explains itself | `npm run sweep:t3-live` on port 4203 | ✅ | YOUR ORDER • Maximum 99 per dish THAT'S THE MOST WE CAN ADD TO ONE LINE · thank |
| P16417 | …and does NOT wear a success tick | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16418 | …and the quantity is not pushed past 99 | `npm run sweep:t3-live` on port 4203 | ✅ | 99 |
| P16419 | Place Order with no table number does not empty the basket | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16420 | …and the table field is visibly flagged | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16421b | the bill offers a bin on the line | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16421 | the bin empties the line | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16422 | …and the empty basket says so in words rather than showing nothing | `npm run sweep:t3-live` on port 4203 | ✅ | Back Your Bill Current bill Live status Your cart is empty |
| P16423 | …and the pill is gone with it | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16424 | …and the body no longer claims the corner is occupied | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16425 | no uncaught error across the whole basket journey | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16426 | the sessions-ON restaurant paints its own dishes | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16427 | tapping + opens the join-a-table gate | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16428 | …asking for the table first | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16429 | …branded with the restaurant's OWN name | `npm run sweep:t3-live` on port 4203 | ✅ | MY LITTLE FRENCH HOUSE |
| P16430 | …and nothing on that card is leaked code | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16431 | a table this restaurant does not have is refused | `npm run sweep:t3-live` on port 4203 | ✅ | ✕ MY LITTLE FRENCH HOUSE Which table are you at? Scan the QR sticker on your table, or type the table number to start yo |
| P16432 | …naming a real upper bound, not a placeholder | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16433 | the ✕ on the table box clears the remembered table, not just the box | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16434 | …and the box itself is empty | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16435 | continuing with no table number says why instead of doing nothing | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16436 | closing the gate closes it | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16437 | …and the dish it was holding is NOT quietly added afterwards | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16438 | …and the pill does not appear either | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16439 | the phone's back button closes the gate rather than leaving the site | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16440 | no uncaught error across the whole gate journey | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16441 | a saved request for staff appears in the corner | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16442 | …counted as a REQUEST, not as an order | `npm run sweep:t3-live` on port 4203 | ✅ | 1 request for staff waiting to send |
| P16443 | …and the row names what was asked for | `npm run sweep:t3-live` on port 4203 | ✅ | Water |
| P16444 | …never the count of an empty basket | `npm run sweep:t3-live` on port 4203 | ✅ | Water |
| P16445 | …and says what will happen to it | `npm run sweep:t3-live` on port 4203 | ✅ | Staff will be called · a moment ago |
| P16446 | …with no price invented for it | `npm run sweep:t3-live` on port 4203 | ✅ | Staff will be called · a moment ago |
| P16447 | …and no cancel button on something already on its way | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16448 | the sheet explains that it is saved on this phone and sends by itself | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16449 | …and it really is in this phone's own storage, as a call | `npm run sweep:t3-live` on port 4203 | ✅ | call:Water |
| P16450 | the red offline strip is up at the same time, so the two agree the phone is offline | `npm run sweep:t3-live` on port 4203 | ✅ | ⚠️ No internet — showing what's saved on this device. Your order is saved and will send by itself. |
| P16451 | …and it does not cover the saved-work sheet | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16452 | nothing of this offline run reached the read-only control restaurant | `npm run sweep:t3-live` on port 4203 | ✅ | the whole block runs offline; no request left the phone |
| P16453 | the shared table bill is on screen | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16454 | …and it is NOT still shimmering with nothing to show | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16455 | …it says the restaurant's system cannot be reached | `npm run sweep:t3-live` on port 4203 | ✅ | YOUR TABLE We can't reach the restaurant's system right now, so we can't show what your table has ordered. Not |
| P16456 | …that nothing is lost | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16457 | …and offers a way to ask again | `npm run sweep:t3-live` on port 4203 | ✅ |  |
| P16458 | at desktop width the pill is a compact CENTRED pill, not a full-width bar | `npm run sweep:t3-live` on port 4203 | ✅ | 512,257 |
| P16458b | …and it hides while the bill is open | `npm run sweep:t3-live` on port 4203 | ✅ | which is the rule that really governs it |
| P16459 | …and the bill is a centred panel, not a full-width sheet | `npm run sweep:t3-live` on port 4203 | ✅ | 480 |
| P16460 | …and it fits inside the window | `npm run sweep:t3-live` on port 4203 | ✅ | 400,480 |

## Block C — captures I opened and looked at, and what measuring them settled (P16461–P16500)

Five NEW captures, in `.claude/sweep/shots/T3/` (`node scripts/sweep/t3/s7-shots.mjs`), plus the
eight from block 4. All thirteen were OPENED AND READ as images, not asserted from the DOM. Two
surfaces here had never been captured by any sweep: a saved REQUEST FOR STAFF, and the live table
bill with nothing to read.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P16461 | A saved request for staff names what was asked for, on a 360×780 phone, DARK | read `s7-a35-saved-request-dark.png` | ✅ | "Bring the bill / Staff will be called · a moment ago" |
| P16462 | …and the same in LIGHT | read `s7-a35-saved-request-light.png` | ✅ | identical wording, dark text on cream, nothing white-on-white |
| P16463 | …with the spinner, not a button, on something already on its way | read both images | ✅ | |
| P16464 | …and no price invented for it | read both images | ✅ | the order row above it in the mixed capture DOES show ₹230; the request shows none |
| P16465 | …and the row does not wrap or clip at 360px | read both images | ✅ | "Bring the bill" is the longest label the popup offers |
| P16466 | The sheet header reads as a place, not a status code | read both images | ✅ | "On this phone" |
| P16467 | …and the footer explains that it sends by itself | read both images | ✅ | wording covers both kinds, unchanged |
| P16468 | A MIXED queue labels each row for what it is | read `s7-a35-mixed-queue.png` | ✅ | "1 × Virgin Mojito · ₹230" above "Water / Staff will be called" |
| P16469 | …and the two rows are separated clearly enough to read as two things | read the image | ✅ | hairline rule between them |
| P16470 | …and the connection badge at the top agrees on the count | read the image | ✅ | "Offline · 2 waiting" |
| P16471 | The live table bill's unreachable state is fully readable, DARK | read `s7-a35-table-unreachable-dark.png` | ✅ | four lines, no clipping, at French House's own dark palette |
| P16472 | …and in LIGHT | read `s7-a35-table-unreachable-light.png` | ✅ | |
| P16473 | …the "Try again" is visibly a control, not part of the sentence | read both images | ✅ | underlined, and it is the only underlined text in the card |
| P16474 | …and it is comfortably tappable at 360px | read both images + measure | ✅ | on its own line, ~150×40 |
| P16475 | …and the green "live" dot has stopped claiming a live connection | read both images | ✅ | grey, no pulse, in both skins — this is what item 6's last commit added |
| P16476 | …and the tab strip still shows which tab you are on | read both images | ✅ | "Live status" filled |
| P16477 | …with the restaurant's OWN palette, not restaurant #1's | read both images | ✅ | French House cream/brown, "little French house" wordmark |
| P16478 | No leaked code text in any of the five new captures | read all five | ✅ | no `undefined`, `NaN`, `[object Object]`, `${`, `-->` |
| P16479 | No text is cut off mid-word at 360px in any of them | read all five | ✅ | |
| P16480 | Nothing renders as an empty box or a missing glyph | read all five | ✅ | |
| P16481 | The eight block-4 captures still read correctly — the bill, DARK | read `a35-bill-dark.png` | ✅ · s7 | prices ₹219/₹229, the − and + round and separated, the bin red and far from "+" |
| P16482 | …the bill, LIGHT | read `a35-bill-light.png` | ✅ · s7 | every line readable, chips keep their borders |
| P16483 | …the saved-orders chip in situ | read `a35-outbox-chip.png` | ✅ · s7 | "1 order waiting to send" — correct wording for an ORDER, which is what it is |
| P16484 | …the chip's list | read `a35-outbox-sheet.png` | ✅ · s7 | "1 × Virgin Mojito, 1 × Litchi Cooler · ₹471" |
| P16485 | …the join-a-table gate | read `a35-gate-notopen.png` | ✅ · s7 | white card, brown text, "OPENING TABLE 29", "e.g. Mia" |
| P16486 | …the gate's other screen | read `a35-gate-table.png` | ✅ · s7 | |
| P16487 | …the menu with the pill up | read `a35-menu-pill.png` | ✅ · s7 | the pill is mid-rise in this capture, which is what sent me measuring — see P16490 |
| P16488 | …and the desktop bill | read `desk-bill-dark.png` | ✅ · s7 | centred panel, 219 + 229 = ₹448, chips on two rows |
| P16489 | The Next.js dev badge in the bottom-left of every capture is not a product element | read any capture + check a production build has none | ✅ | dark circle with an "N"; it overlaps the offline strip's ⚠ in several shots. An artefact of the dev server, not the app |
| P16490 | 🔎 **WITHDRAWN: "the basket bar covers a dish's + at the end of the list".** | hit-test every "+" while walking `#main-scroll` to its true end | ✅ 🔎 | I measured this THREE ways. (1) Immediately after adding, one "+" at y=727 resolved to `.mini-cart` — real, but mid-scroll. (2) Walking all 69 add-buttons: covered at 445 positions, **0 permanently unreachable**. (3) At the list's true end the last "+" sits at y=592 with the pill at 710–762 — clear. My first "broken at the end" reading came from a probe that set `scrollTop` to an end captured BEFORE lazy content grew the list. **Not a fault. Do not re-file it.** |
| P16491 | …and the protection that makes it not a fault is real and documented | read `app/globals.css` `.items-container` clearance | ✅ | 96px + safe-area on the last container, with a comment naming this exact failure from 2026-08-04: "the FINAL row's add button sat permanently under the bell with nowhere left to scroll" |
| P16492 | …and it applies to the layout the guest is actually on | measure `paddingBottom` of the matched container in gallery mode | ✅ | the rule matched 1 element, 96px, with `lfh_menu_layout = gallery` |
| P16493 | …and the pill clears the waiter bell on the right rather than covering it | measure both boxes | ✅ | pill right edge 276 of 360; `right: 84px` exists for this |
| P16494 | …and the pill does not cover the live-status strip | read the body flag + the stylesheet | ✅ | `body[data-lfh-minicart] .order-tracker { bottom: 84px }` |
| P16495 | …and the saved-work chip clears BOTH of them | read the stylesheet | ✅ | `body[data-lfh-minicart] .gob-chip` adds the offline strip's own height too |
| P16496 | The bottom-left corner stack has exactly one owner per layer, so two things cannot land on each other | read the three body flags and `--lfh-offbar-h` | ✅ | pill → tracker → chip → offline strip, each reserving the next one's height |
| P16497 | A capture of an ANIMATING element is not evidence about the settled screen | re-measure after the rise finishes | ✅ | the lesson behind P16490: the pill looked half-transparent and mis-positioned in `a35-menu-pill.png` purely because it was mid-animation |
| P16498 | Both guest skins were checked for a colour that only works in one | read every new capture in both | ✅ | the one fixed colour I added (the muted dot) reads correctly in both, because it is `var(--muted)` |
| P16499 | Nothing in any capture shows another restaurant's branding | read all thirteen | ✅ | Aangan captures say Aangan; French House captures say French House |
| P16500 | Every capture kept as evidence is committed; the rest were looked at and deleted | `ls .claude/sweep/shots/T3` | ✅ | five kept — the two new surfaces in both skins, plus the mixed queue |

## Block D — tracing one value across panels, by reading (P16501–P16540)

Sweep 6's block 5 WATCHED two phones, the kitchen and the floor end to end, and those 25 rows were
re-run green above. This block traces the values that block never followed: the ones that only
matter when something goes WRONG — a refusal code, a replay marker, a stale table number.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P16501 | Every refusal code the place-order route can send has diner-facing words on the guest side | list the codes in `app/api/guest/place-order/route.ts`, then find each in `reasonMsg` | ✅ | checked code by code |
| P16502 | …and each has a SECOND wording for when it comes back to a saved order ("while you were offline") | read `reasonMsg`'s `queued` branch | ✅ | past tense, because the moment has gone |
| P16503 | …and a code with no wording falls back to an honest general sentence, never the code itself | read `default:` | ✅ | |
| P16504 | The `server_busy` refusal the route sends is classed as BUSY by the client, not as a refusal | `route.ts` sends 502 + `server_busy`; client reads `res.status >= 500` → `busyError` | ✅ | so it is SAVED, not shown as a failure |
| P16505 | …and its `retryAfter` reaches the queue's own timer | route sends `retryAfter`; `noteServerRetryAfter` reads it | ✅ | |
| P16506 | …where it can only LENGTHEN the wait, never shorten it | `Math.max(base, serverAskedWaitMs)` | ✅ | a server cannot talk a phone into hammering it |
| P16507 | …and is capped, so the queue always comes back within five minutes | `SERVER_WAIT_CAP_MS` | ✅ | |
| P16508 | A replayed offline order is marked as a replay, so the server can refuse a table that moved on | `X-LFH-Replay: 1` → `replayMarkers` → `replayClash` | ✅ | |
| P16509 | …and carries WHEN it was queued, which is what the refusal is judged against | `X-LFH-Queued-At` | ✅ | |
| P16510 | …and a LIVE order carries neither, so a clash check can never fire on one | `replayMarkers(req)` returns null with no markers → `replayClash` returns null | ✅ | this is why `postGuestOrder` not reading `clash.plain` is CORRECT, not a gap — see P16571 |
| P16511 | A clash reply's plain sentence reaches the diner when it CAN happen (the queued path) | `j?.clash?.plain` → `moveToFailed` → the chip's `gob-why` | ✅ | |
| P16512 | The at-most-once id the phone mints is the header the server dedups on | `orderKeyRef` → `X-LFH-Action-Id` → `lib/idempotency.ts` | ✅ | |
| P16513 | …and it changes when the ALLERGY list changes, not only when the dishes do | `sig = JSON.stringify({ t, i, a })` | ✅ | otherwise a corrected allergy would be deduped away as "the same order" |
| P16514 | …and a saved order reuses the SAME id as the live attempt that lost its reply | `actionId` threaded from the basket into `enqueueGuestOrder` | ✅ | one order, not two |
| P16515 | …while "Order the rest" mints a NEW one | it calls `enqueueGuestOrder` with no `actionId` | ✅ | the old id carries the server's memory of the refusal |
| P16516 | A waiter call carries its own at-most-once id, so the floor rings once however often it is resent | `X-LFH-Action-Id` on `/api/guest/call-waiter` | ✅ | |
| P16517 | The dish id the basket holds is the id the server prices from | `orderItems()` → `postGuestOrder` → `lfh_price_order` | ✅ | no price leaves the phone |
| P16518 | A removed allergen reaches the kitchen ticket and comes back to the guest as "No milk" | `orderItems().removed` → `order_items.removed` → `SessionTableBill` | ✅ | |
| P16519 | A guest note reaches the ticket and is shown back in quotes | same path | ✅ | |
| P16520 | The order-wide allergy list travels ONCE, not repeated per line | `allergyPayload()` + `lineRemoved`'s filter | ✅ | owner, 2026-06-14 |
| P16521 | The tax rate the guest is QUOTED is the same rule the bill CHARGES | `effectiveTaxRate` ↔ `lfh_split_items_tax` | ✅ | mirrored by design, one source |
| P16522 | An MRP line's stamp on the guest's live bill comes from the server's frozen ticket line | `orders[].items[].is_mrp` → `mrpTitles` | ✅ | never decided on the phone |
| P16523 | The discount percentage on the guest's bill is written by the same maker as the paper bill | `BILLDOC.discPct` | ✅ | one sentence-maker, two screens |
| P16524 | A corrected table number reaches the kitchen's copy of the order | `updateOrderTableNumber` → `set_order_table_number` | ✅ | |
| P16525 | …and this device's own list is updated in the same breath, so the two agree at once | `writeActiveOrders` right after the RPC | ✅ | |
| P16526 | …and the correction is refused once the food has moved | the status guard in the RPC | ✅ | |
| P16527 | …and the refusal reaches the diner as a sentence they can act on | `saveOrderTable`'s `!ok` toast | ✅ | "it may already be served — please ask a member of staff" |
| P16528 | A guest's request to open a table becomes the row the staff pending-requests view reads | `requestAccess` → `lfh_request` | ✅ | |
| P16529 | …and the guest is told when that did NOT happen | `requestLanded` | ✅ | sweep 6 FIX-2, still holding |
| P16530 | A guest LEAVING a table resolves their own pending waiter calls, so the floor stops flagging them | `lfh_leave_session` (mig 026) `UPDATE waiter_calls SET resolved = true` | ✅ | |
| P16531 | …and the head leaving hands the table over rather than closing it | mig 026: the next approved member becomes owner; the session stays open | ✅ | only staff close a table |
| P16532 | …so a guest can never end a table from their phone | no guest surface closes a session | ✅ | |
| P16533 | …and if the leave never reaches the restaurant, the guest is now TOLD | item 4 | ✅ | was silent before this run |
| P16534 | The shared basket the guest edits is the one the panels can read on the session row | `mergeSessionCart` → `sessions.cart` | ✅ | |
| P16535 | …and the delta is applied under a row lock, so two phones cannot lose each other's lines | mig 144 `SELECT … FOR UPDATE` | ✅ | |
| P16536 | …and an added line that already exists has its quantities SUMMED, not replaced | mig 144's `p_added` branch | ✅ | which is exactly why a blind retry is unsafe — see P16572 |
| P16537 | The guest's live per-dish progress is the same `order_items.status` the kitchen board moves | read both sides | ✅ | |
| P16538 | …and the staff-only "ready" stage is hidden on BOTH guest surfaces, not just one | `SessionTableBill` and `OrderTracker` both map it to "preparing" | ✅ | owner, 2026-06-14 |
| P16539 | A restaurant with the guest menu switched off serves no menu on ANY of the three doors | `menuEnabled` checked in the `/q` route and the tenant route | ✅ | |
| P16540 | An old printed web address still finds the restaurant, on the menu door and the three scoped panels | `slugMovedTo` → 307, from one place (`lib/panelGate`) | ✅ | asserted by `verify:guest-doors` section 7 |

## Block E — judgment: is this how a real restaurant needs it to work (P16541–P16570)

Not "does the code do what it says" but "should it". Read, not run.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P16541 | A diner who asks for water in a dead spot is told what is waiting, in the words they used | the queue's list | ❌ → ✅ | item 2. It said "0 items". A diner cannot act on that, and it made the app look broken at the exact moment it was working |
| P16542 | …and is not told they have an "order" waiting when they asked for a napkin | the chip's label | ❌ → ✅ | item 2 |
| P16543 | …and a phone holding BOTH is not made to pick a wrong word for one of them | the mixed-queue label | ❌ → ✅ | item 2b: it drops the noun, matching the badge above it |
| P16544 | A diner is never told they have left a table the restaurant still has them at | the leave handlers | ❌ → ✅ | item 4. The worst version was "Change table": it sent them off to sit elsewhere while the old table still held them |
| P16545 | …but a dead connection can never TRAP someone at a table either | the local clean-up still runs on failure | ✅ | both halves matter; the fix keeps both |
| P16546 | A host who taps "Let them in" learns whether their friend got in | the three head actions | ❌ → ✅ | item 5. A second person is physically standing there waiting for the answer |
| P16547 | A diner looking at the shared bill on a dead connection is told so, not shown a pulsing nothing | the Live-status tab | ❌ → ✅ | item 6 |
| P16548 | …and is told nothing is lost, because that is the actual question they have | the sentence | ✅ | |
| P16549 | …and the screen does not claim to be "live" while saying it cannot reach the restaurant | the dot | ✅ | item 6's second commit |
| P16550 | A restaurant that adds tables is not fighting its own diners' open pages | the gate's settings | ❌ → ✅ | item 3. A diner at the new table 35 was refused with "tables 1–30" |
| P16551 | A diner who scans the sticker on their own table can order | watched, both restaurants' stickers | ✅ | sweep 6 FIX-1, still holding — the single most important thing in this territory |
| P16552 | A basket is never lost because a phone lost signal | watched | ✅ | |
| P16553 | A basket is never sent twice because a diner tapped twice | watched: one order, not two | ✅ | |
| P16554 | One sold-out dish never costs a diner the whole basket | "Order the rest", and it says so when it cannot | ✅ | |
| P16555 | A rush makes the app slow, never broken — orders are kept and drained | the busy path on both doors | ✅ | |
| P16556 | A table of six argues over ONE bill, not six | the shared table bill | ✅ | |
| P16557 | Two parties an hour apart at one table never see each other's anything | the closed-session guard, watched | ✅ | |
| P16558 | A diner can correct a wrong table before the food moves, and not after | the status guard | ✅ | correct in both directions |
| P16559 | A diner cannot walk off mid-meal and take the table with them | the `orderActive` block, on both buttons | ✅ | |
| P16560 | The head is not trapped by the approve prompt | the snooze, and it returns | ✅ | |
| P16561 | A partner who is never approved sees nothing of the table | four separate guards, all present | ✅ | |
| P16562 | The diner's phone is never the source of truth for money | every figure on the live bill is the server's | ✅ | |
| P16563 | Nothing in this territory can make a sale disappear | no delete, no soft-delete, no hide; the guest can only ADD | ✅ | cancel is the only route out, and it is not here |
| P16564 | A restaurant that has configured nothing still gets a working basket | every settings default | ✅ | |
| P16565 | A restaurant that switches something off is really rid of it, not just hiding it | the payload gates before the wire | ✅ | |
| P16566 | The busiest read in the product does not carry fields nobody draws | `CARD_COLUMNS` + `toCardItem` | ✅ | and the two full-row reads are deliberate — P16346 |
| P16567 | A phone that never queued anything does not gain a database | the `indexedDB.databases()` probe | ✅ | |
| P16568 | The 99-per-dish ceiling is a limit a real kitchen would want, and reads as one | the neutral toast | ✅ | sweep 6 improvement #4, built |
| P16569 | Every fix in this run has a guard that goes RED without it | ran `verify:guest-doors` against `origin/main`'s five files | ✅ | 25 of the 26 new checks fail there |
| P16570 | The next sweep can re-run every row above and get the same answer, by id | this file, plus two committed runners | ✅ | `npm run sweep:t3-static`, `npm run sweep:t3-live` |

## Block F — pre-empts, withdrawn candidates, and this run's bookkeeping (P16571–P16600)

**Read these before reporting anything about this territory.** Each is something that LOOKS like a
fault and is not, or something I nearly filed and measured my way out of.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P16571 | 🔎 **NOT A FAULT: the live order path ignores a clash message.** | `replayMarkers(req)` returns null without the replay headers, so `replayClash` returns null | ✅ 🔎 | `postGuestOrder` handles a 409-retry but never reads `clash.plain`, while the queued path does. That is correct: only a REPLAY carries the markers a clash is judged on, so a live order can never receive one. Do not "fix" it |
| P16572 | 🔎 **NOT A FAULT: a failed shared-basket sync is not retried.** | mig 144's `p_added` SUMS quantities when the line already exists | ✅ 🔎 | A one-off `mergeSessionCart` failure leaves the line on that phone only, with no retry until the next basket edit. Tempting to add a retry — and a blind one would ORDER TWO of everything whenever the merge actually succeeded and only its reply was lost. Nothing is lost today and the diner's own order still carries the dish. Fixing it properly needs an at-most-once key on the merge, which does not exist. Raised as an improvement, deliberately NOT patched |
| P16573 | 🔎 **NOT A FAULT: `ActiveOrder.itemCount` is inconsistent after "Order the rest".** | grep every consumer of `itemCount` | ✅ 🔎 | The basket writes the SUM of quantities; `orderRestWithout` writes the LINE count. Nothing renders that field for an active order (the chip prefers `track.items`, which carries real quantities; the strip and the bill never read it). Dead data, not a wrong number on a screen. Recorded so nobody files a phantom |
| P16574 | 🔎 **NOT A FAULT: the head's approve prompt polls every 4 seconds.** | `OWNER_POLL_MS` + its comment | ✅ 🔎 | Deliberate and justified: a second person is standing at the table waiting to be let in. It also refuses to poll a hidden tab at all. Not a "fast poll" violation |
| P16575 | 🔎 **NOT A FAULT: the waiter popup writes the remembered table on every keystroke.** | the `onChange` handler | ✅ 🔎 | Typing "30" briefly remembers "3". Deliberate — it is the same memory the ✕ clears and the bill prefills from, and remembering as you type is what makes the two boxes agree. A guest who abandons mid-number can clear it with the ✕, which now announces the wipe |
| P16576 | 🔎 **NOT A FAULT: `setMemberName` is fire-and-forget.** | the `try { await … } catch {}` and what comes before it | ✅ 🔎 | The name is stored on the phone FIRST, so the flow never depends on the call; a failure costs the floor a display name, not the diner their order. The alternative — blocking the order on a cosmetic write — is worse |
| P16577 | 🔎 **NOT A FAULT: the presence heartbeat's result is discarded.** | it is a heartbeat | ✅ 🔎 | Nothing waits on it, and it stops entirely on a hidden tab (the phantom-table scar) |
| P16578 | 🔎 **STILL NOT A FINDING (third sweep): the saved-orders chip after an offline reload in DEV.** | sweep 6 measured it on the live site | ✅ 🔎 | The chip's chunk comes out of `lfh-asset-v9`; its absence in dev is the dev server registering no service worker. Sweep 6 closed this with "do not re-report" and I am not re-reporting it |
| P16579 | 🔎 **NOT A FAULT: `document.hidden` is not checked inside every poll tick.** | which polls are 60s and which are not | ✅ 🔎 | The 60-second backstops are cheap enough to run hidden; the two that are NOT (the approve prompt at 4s, the presence heartbeat) both check it |
| P16580 | 🔎 **NOT A FAULT: `ready` flips true even when the restaurant lookup FAILS.** | `.catch(() => setReady(true))` | ✅ 🔎 | Deliberate: the answer is then "it's #1", and a widget waiting for ever is worse than one asking about the fallback |
| P16581 | 🔎 **NOT A FAULT: theme, language, currency and device id are NOT tenant-scoped.** | the `LEGACY_KEYS` list | ✅ 🔎 | They belong to the PERSON's phone, not to a restaurant. Stated in `lib/tenantStorage.ts`'s own header |
| P16582 | 🔎 **NOT A FAULT: a WAITING order in the saved list has no cancel button.** | the queued row | ✅ 🔎 | Deliberate: it may already be with the kitchen, and a "cancel" here could throw away a real order |
| P16583 | 🔎 **NOT A FAULT: the mini-cart shows no tax.** | the render | ✅ 🔎 | It is a SUBTOTAL and never claims to be the amount payable — deliberate under mig 270 |
| P16584 | 🔎 **NOT A FAULT: Aangan behaves differently from French House.** | the standing pre-empt | ✅ 🔎 | Aangan is the read-only control at factory defaults. Sessions are OFF there; that is the point, and it is what makes the `/q/<code>` door testable |
| P16585 | The territory's own guard is alive and can fail | ran it against `origin/main`'s files | ✅ | 25 of 26 new checks red there, all green here — a guard that cannot fail protects nothing |
| P16586 | …and the six named gates this territory leans on are alive too | ran all six; each printed its own check count | ✅ | `verify:order-retry` alone prints 51 |
| P16587 | The sweep-6 runners still work and were not rewritten from prose | ran all four | ✅ | which is the whole reason they were committed instead of deleted as scratch |
| P16588 | …once their port stopped being another session's | item 1 | ❌ → ✅ | before this, a re-run on 4103 either measured nothing or measured another live session's app |
| P16589 | …and their screenshots stopped landing in a dead session's folder | item 1 | ❌ → ✅ | they now go to `.claude/sweep/shots/T3/` |
| P16590 | The suite's own summary note points at the block it names | `run-all.mjs` | ❌ → ✅ | it read `results[4][1]` — a positional index into a list I had just inserted two entries into. Found and fixed in the same commit that introduced it |
| P16591 | Nothing this run created was left on any restaurant | the runners' own cleanup lines, plus their end-of-run assertion | ✅ | "cleaned up 1 order(s) and 1 session(s), each by its own id · still mine and live: 0 order(s), 0 session(s)" |
| P16592 | …and the read-only control restaurant received nothing at all | P01351 asks the server directly | ✅ | 0 orders reached it |
| P16593 | …because every check against it is a read or runs OFFLINE | read the two new runners | ✅ | the offline blocks cannot reach any server by construction |
| P16594 | …and the two rows that need a session invent one in the PHONE's storage instead of creating one | read `s7-live.mjs` | ✅ | and block the read that would confirm it — which is exactly the state being tested |
| P16595 | No check in this run was done by trickery | re-read the run | ✅ | no id swapping, no replaying as another restaurant, no calling an endpoint without a login |
| P16596 | Every phase, command and commit message in this run is written in product-correctness language | re-read this file and the findings | ✅ | |
| P16597 | The improvement ideas went to the CHAT and to no file | grep the repo for a T3-improvements change | ✅ | the owner's instruction for sweep 7 |
| P16598 | `npm run typecheck` passes | ran it after every commit | ✅ | |
| P16599 | The fixes are six separate commits, each naming its item number | `git log` | ✅ | so item 5 can be vetoed without unpicking the branch |
| P16600 | This run ends at a pushed, OPEN pull request — not a merge, not a deploy | the PR link in the findings file | ✅ | the auto-deploy rule is suspended for sweep 7 by the owner's own decision |
