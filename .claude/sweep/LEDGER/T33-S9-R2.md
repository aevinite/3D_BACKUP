# T33 — ROUND 2, the database, migrations at POSITIONS 321 → THE END

**Sweep #9, round 2 (2026-09-17).** The owner's word after round 1 and items 4–7 were merged and
deployed: *"make it live and plan the whole 500 phases test again within the boundaries that you
have given and test it again so that if any errors are left still within the boundaries that you
have given, it can be solved"*.

Territory re-derived, not trusted: `ls supabase/migrations/*.sql | sort | sed -n '321,$p'` —
**85 files, 13,243 lines**, `314_a_settled_bill_is_annotated_not_rewritten.sql` →
`396_a_stamped_tax_rate_must_be_a_rate.sql`. (It was 81 files when round 1 measured it and has
grown three times since. A positional range is not a numeric one.)

**IDs: `P104851`–`P104900` (round 1's unspent remainder, contiguous) + `P152001`–`152456`
(this terminal's PRE-ALLOCATED round-2 block from INDEX.md, which runs to P153000).** Only ids this
terminal already holds were used — the *Next free ID* line was never read and never edited, which
is the half of the rule this registry has recorded nine collisions over.

**The count is 506, not a round 500, and it moves when the TERRITORY moves.** Blocks B and C
are generated one row per function and one per file, so a migration landing mid-round changes the
total: 396 added a file (+1), then 397 added a file and brought a function into range (+4), and one
realtime probe split in two when 397 changed what the right answer was (+1). A real check is not
trimmed to hit a round number — T17 (2026-09-02), T13 (2026-09-05) and T27 (2026-09-16) all recorded
that reasoning. **Ids come from one allocator (`scripts/sweep/t33/ids.mjs`) rather than a
hard-coded start per block**, which is the thing that made those shifts safe: hand-nudging four
starts is how an id collision happens, and it nearly did twice in one afternoon.

## What this round was aimed at, and why — the measurement first (S9-RULES rule 2b)

Round 1 wrote 50 checks and every one of them **read** the database: installed function bodies, the
catalogue, row counts. **Not one made the database DO the thing and looked at the answer.** That was
the thin corner, so 58 of this round's phases are driven — a real write, inside a transaction that
ROLLS BACK, with the result read out afterwards.

The by-subject count also exposed a fault in round 1's own rows, and it is the same one T28 wrote
into INDEX.md on 2026-09-16: **all 50 of them named a migration NUMBER and never a subject FILE**,
so the measurement rule 2b prescribes read this territory as still having ZERO rows. Fixed in this
round — every round-1 row now names its file — and after that the honest count was **36 of 83 files
with no row at all**. Every one of the 36 has rows now.

| block | ids | count | what it is |
|---|---|---|---|
| A · DRIVEN behaviour | `P104851`–`P152009` | 59 | a real write to French House inside a rolled-back transaction, then the answer read back |
| B+C · every function, three rules each · every file, one composite row | `P152010`–`P152343` | 334 | generated from the source, so the count is whatever the territory has |
| D · invariants over the REAL data | `P152344`–`P152371` | 28 | 41,159 orders and 62 restaurants, not a fixture |
| D2+E · one row per live restaurant, then judgment | `P152372`–`P152456` | 85 | does each restaurant only carry its own numbers · should it work this way |

## Result

| | |
|---|---|
| Phases | **506** — 505 ✅ · 1 ❌ · 0 ⏭ |
| Real faults found | **1**, fixed on this branch: ten orders stamped `tax_rate = 5` (500%) where 41,159 hold `0.05`, and **nothing validated the column**. Migration 396 + `verify:tax-rate-is-a-rate`. |
| Recorded, awaiting the owner's decision | **1** — `P152347`: 15 invoiced bills have had every order cancelled and carry no void mark and no credit note. Who issues the credit note and when is a billing rule, not a bug. |
| Regressions | **none.** |

## Eleven of my own probes went red before the product did — every one was the probe

Written down because in the output they are indistinguishable from a real fault, and because sweep
#6 found three of three "dead guard" hits were the detector too:

1. **An AFTER trigger's work is invisible inside the same statement.** Six numbering probes read
   `sessions.bill_no` as null while the trigger had assigned it. One statement per action, always.
   (KOT numbers passed throughout — those come from a BEFORE trigger and show up in `RETURNING`.)
2. **An order with an empty items array is repriced to zero** by `trg_orders_fill_tax_split`, so
   two money probes measured the pricing pipeline instead of the takings column. `taxable_base` is
   now supplied, the way a real write path arrives.
3. **`tax_rate` is the rate CHARGED, not the restaurant's current setting** — asserting it equalled
   `lfh_effective_tax_rate` was asserting the opposite of migration 288's whole point.
4. **Auto-print is entitled, not automatic.** Three print probes expected paper from a restaurant
   with the switch off. Both directions are asserted now.
5. **The slug history is written by the admin route, not a trigger**, and `lfh_slug_moved` returns
   the address it moved TO, not the restaurant id.
6. **The business day is "convert to IST, THEN take off five hours"** — doing it the other way
   round mapped 2026-08-04 and 2026-08-21 onto one day and reported 52 bill-number clashes on
   French House. And a **merged party legitimately shares one bill number**, which is the owner's
   own standing rule.
7. **An invoice already issued KEEPS its number when the sale is cancelled** (CLAUDE.md, in those
   words). Asserting "no invoice on a cancelled bill" filed 18 correct rows as faults.
8. **A cancellation nobody timed cannot say whether it preceded the invoice** — migration 389 chose
   that deliberately, so the invariant only judges rows the data can answer.
9. **The zero uuid is a "no restaurant" sentinel, not a guess at French House** —
   `lfh_remember_error_signature` coalesces to `…0000`.
10. **A judgment whose ANSWER is "no" is not a failed check.** Three rows where the product is
    right to do the opposite were filed ❌ purely because the answer was no.
11. **A guard must not fail on a test that proves the fault is impossible** — `verify:tax-rate-is-a-rate`
    half C flagged two deliberate negative tests in `scripts/sweep/t27r2/` until it was scoped to
    the shipped paths.

## How to re-run all 500

`node scripts/sweep/t33/s9r2-run.mjs` — four committed modules, read-only except block A, which
writes only inside `BEGIN … ROLLBACK`. Nothing is ever committed to the shared dev database, so
there is no fixture left for another terminal to trip over and none of mine to delete by id.
`--write` rewrites this file from the live results.

Transport is `curl`, not `fetch`: five terminals share that one management endpoint and it measured
4–9 seconds to connect during this run, where Node's undici gives up at a fixed 10s and tries IPv6
first. Busy is treated like offline — generous deadline, jittered backoff.

| id | check | how to verify | result | note |
|---|---|---|---|---|
| P104851 | `328_a_bill_counter_is_not_a_settings_change.sql` — opening a table burns NO bill number — the counter only moves when food is actually ordered | insert a session with no order; read its bill_no in the NEXT statement | ✅ | bill_no at open = NONE |
| P104852 | `391_a_table_that_receives_an_order_gets_its_bill_number.sql` — the FIRST order landing on a table gives that table its bill number | insert one order, then read the session's bill_no in a later statement | ✅ | bill_no after the first order = 25 |
| P104853 | `391_a_table_that_receives_an_order_gets_its_bill_number.sql` — a SECOND order does NOT give the same party a second number — one party, one bill | insert a second order; the bill_no must be unchanged | ✅ | 25 → 25 |
| P104854 | `329_a_prep_batch_is_one_action.sql` — each order gets its own KOT number | read kot_no off both inserted orders | ✅ | kot_no 153 and 154 |
| P104855 | `329_a_prep_batch_is_one_action.sql` — the second KOT number is the next one, not a jump | compare the two | ✅ | 153 → 154 |
| P104856 | `316_the_three_numbers_and_the_two_switch_bags_say_what_they_are.sql` — the bill number and the KOT number are DIFFERENT series — they are not one counter wearing two names | compare the bill number a party drew against the KOT numbers its orders drew | ✅ | bill 25 vs KOT 153 |
| P104857 | `391_a_table_that_receives_an_order_gets_its_bill_number.sql` — moving an order onto a NUMBERLESS table gives that table a bill number — the unmerge gap 391 was written for | re-link an order to a fresh party, then read that party's bill_no | ✅ | child party: NONE → 26 |
| P104858 | `391_a_table_that_receives_an_order_gets_its_bill_number.sql` — the party it came FROM keeps the number it already drew | read the original party's bill_no after the move | ✅ | original party: 25 → 25 |
| P104859 | `391_a_table_that_receives_an_order_gets_its_bill_number.sql` — the child's number is its OWN, not a copy of the parent's | compare the two | ✅ | 25 vs 26 |
| P104860 | `391_a_table_that_receives_an_order_gets_its_bill_number.sql` — a party that receives nothing is not numbered by the trigger firing elsewhere | open a third party in the same transaction and read it | ✅ | the untouched party still has no bill number |
| P104861 | `391_a_table_that_receives_an_order_gets_its_bill_number.sql` — an ordinary order UPDATE does not fire it — the trigger is scoped to a change of party, not to any edit | change the order's status and confirm the party's number is unchanged | ✅ | bill_no stayed 26 across a status change |
| P104862 | `331_a_cancelled_sale_takes_no_invoice_number.sql` — a bill whose every order is cancelled is REFUSED an invoice number, by its own error code | call lfh_generate_invoice on such a party through t33_try | ✅ | → LFH02 lfh: this bill was cancelled — a cancelled sale never takes an invoice number |
| P104863 | `331_a_cancelled_sale_takes_no_invoice_number.sql` — and it draws NO number at all — not a number that is then voided | read that party's invoice_no after the refusal | ✅ | invoice_no on the cancelled bill = NONE |
| P104864 | `331_a_cancelled_sale_takes_no_invoice_number.sql` — a bill with a live order IS given one, so the refusal is not blanket | call it on a party holding a served order | ✅ | → ok, invoice_no 318 |
| P104865 | `331_a_cancelled_sale_takes_no_invoice_number.sql` — asking twice does not mint a second invoice number for one bill | call it again and compare invoice_no | ✅ | 318 → 318 |
| P104866 | `332_every_bill_is_signed_and_chained.sql` — issuing an invoice writes one row into the signed chain, carrying both hashes | count bill_chain before and after, and check the new row's prev_hash and chain_hash | ✅ | chain 70 → 71, the new row is hashed and linked |
| P104867 | `332_every_bill_is_signed_and_chained.sql` — the chain refuses a DELETE — it is the proof a kept bill was never altered | attempt to delete the row just written | ✅ | → P0001 bill_chain is append-only — a signed bill ledger may never be updated or deleted |
| P104868 | `332_every_bill_is_signed_and_chained.sql` — …and an UPDATE too, so a figure cannot be edited after signing | attempt to change its total | ✅ | → P0001 bill_chain is append-only — a signed bill ledger may never be updated or deleted |
| P104869 | `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — the exact row 390 was written for — total 5.76, discount 5.49 at 5% — no longer reads BELOW ZERO | insert that order and read net_amount | ✅ | net_amount = 0 (before 390 this arithmetic gave −0.0045) |
| P104870 | `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — …and it rounds to paise, so no money figure carries four decimals | read scale(net_amount) on that row | ✅ | scale = 0 |
| P104871 | `301` via `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — the SOURCE column stays exact — the grossed discount is not rounded, which migration 301 measured and chose | read disc_gross on the same row | ✅ | disc_gross = 5.7645, filled by the trigger above the raw 5.49 |
| P104872 | `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — a bill given away entirely reads as ZERO collected, not as minus the whole bill | insert a 100% discount and read net_amount | ✅ | net_amount = 0 |
| P104873 | `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — and the oddity stays FINDABLE — the discount and the total are untouched on the row | read discount and total on that row | ✅ | discount 100 and total 0.00 are both still there to find |
| P104874 | `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — on an ordinary paid bill with real dishes on it, the takings are exactly the installed expression of the stored figures | compare net_amount against GREATEST(round(total − disc_gross, 2), 0) read off the same row | ✅ | net_amount 945.00 = GREATEST(round(total 1050.00 − disc_gross, 2), 0) = 945.00 |
| P104875 | `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — the takings column cannot be WRITTEN by hand — it is derived, so no route can set it | attempt to UPDATE net_amount through t33_try | ✅ | → 428C9 column "net_amount" can only be updated to DEFAULT |
| P104876 | `337_a_report_reads_the_takings_not_the_discount.sql` — a new order is stamped with the rate the tax was ACTUALLY CHARGED at — tax ÷ taxable base — not with a hard-coded 5% | read tax_rate off the row and compare it to tax ÷ coalesce(taxable_base, subtotal) on that same row | ✅ | stamped tax_rate 0.050000 = the rate charged on this bill 0.050000 |
| P104877 | `337_a_report_reads_the_takings_not_the_discount.sql` — …and it is the CHARGED rate, not the restaurant's current setting forced onto the row — that is what lets a historical bill survive a GST change | compare the stamped rate to lfh_effective_tax_rate, and record which question each answers | ✅ | stamped 0.050000 (charged on this bill) vs 0.05 (the restaurant's setting today) — the reader falls back to the setting only when the stamp is null |
| P104878 | `388_a_money_function_reads_like_it_behaves.sql` — an order with NO dishes on it is repriced to zero rather than kept at the figures it was handed | insert an order whose items array is empty and read its total afterwards | ✅ | total = 0.00 — trg_orders_fill_tax_split recomputes the split from the dishes, so a ticket with none is worth nothing |
| P104879 | `388_a_money_function_reads_like_it_behaves.sql` — removing the LAST dish from a ticket cancels the ticket instead of leaving a ₹0 ghost on the bill | insert one item, call lfh_delete_order_item, read the order's status | ✅ | → ok, status now 'cancelled', total 0.00 |
| P104880 | `388_a_cancelled_order_records_when_it_was_cancelled.sql` — and it records WHEN it was cancelled, so nothing has to guess later | read cancelled_at on the shell | ✅ | cancelled_at is stamped |
| P104881 | `353_a_binned_bill_is_not_a_tampered_bill.sql` — the ticket is still THERE — a sale can be cancelled, it can never disappear | count the order row after the cancellation | ✅ | the row still exists, zeroed and marked cancelled |
| P104882 | `389_a_cancelled_order_is_not_a_waiters_takings.sql` — and that cancelled ticket is NOT counted as money the waiter brought in | read lfh_staff_performance for that person before and after the cancellation | ✅ | value_punched 0.00 → 0 for staff e58a15c7… |
| P104883 | `397_a_platform_event_is_announced_to_nobodys_restaurant.sql` — a platform-level admin event IS announced, so the admin console refreshes instantly | insert a staff_actions row with a null restaurant and count its breadcrumbs | ✅ | breadcrumbs for a no-restaurant event = 1 |
| P104884 | `397_a_platform_event_is_announced_to_nobodys_restaurant.sql`, `395_an_event_with_no_restaurant_is_announced_to_nobody.sql` — …and it is keyed to NOBODY'S restaurant, so no per-restaurant listener can match it | read the topic_rid and restaurant_id of that breadcrumb | ✅ | topic_rid 'audit:platform', restaurant_id NULL — the unscoped admin console (topic=eq.audit) matches it, a restaurant's topic_rid=eq.audit:<own id> does not |
| P104885 | `395_an_event_with_no_restaurant_is_announced_to_nobody.sql` — …while an event that DOES belong to a restaurant still announces itself, so nothing was silenced wholesale | insert the same shape with a restaurant and count | ✅ | breadcrumbs for a tenant event = 1 |
| P104886 | `395_an_event_with_no_restaurant_is_announced_to_nobody.sql` — and BOTH activity rows are still recorded — the early exit did not swallow the write | count the staff_actions rows themselves | ✅ | 2 activity rows written; an audit table must never quietly lose one |
| P104887 | `321_the_sweep_of_layer_b.sql` — an activity event rides the `audit` topic, not `ops` — no floor tile reloads for a log line | read the topic of the tenant event's breadcrumb | ✅ | topic = audit |
| P104888 | `321_the_sweep_of_layer_b.sql` — its key is the topic AND the restaurant together, so a per-restaurant listener gets only its own | read topic_rid | ✅ | topic_rid = audit:00000000-0000-0000-0000-000000000001 |
| P104889 | `321_the_sweep_of_layer_b.sql` — an order writes TWO breadcrumbs — the floor-wide one and one scoped to its own table | insert an order and read the distinct topics of its breadcrumbs | ✅ | topics = ops,table:T33G |
| P104890 | `321_the_sweep_of_layer_b.sql` — …and both carry the restaurant in their key | read the distinct topic_rid values | ✅ | keys = ops:00000000-0000-0000-0000-000000000001,table:T33G:00000000-0000-0000-0000-000000000001 |
| P104891 | `342_the_recycle_bin_stops_holding_the_door.sql` — a restaurant that is NOT in the recycle bin cannot be purged — it has to be removed first | create a live restaurant and call the purge on it through t33_try | ✅ | → P0001 Restaurant is not in the recycle bin — delete it first |
| P104892 | `345_a_purge_clears_the_operational_tables_again.sql` — a purge of a binned restaurant succeeds | bin it, then call the purge | ✅ | → ok |
| P104893 | `309` via `345_a_purge_clears_the_operational_tables_again.sql` — THE MONEY SURVIVES A PURGE — its orders are still there afterwards, which is the compliance rule, not an oversight | count that restaurant's orders before and after the purge | ✅ | orders 1 → 1; sessions 1 also kept (both are money) |
| P104894 | `345_a_purge_clears_the_operational_tables_again.sql` — …while the OPERATIONAL rows do go — feedback is how the restaurant ran, not what it sold | count its feedback before and after | ✅ | feedback 1 → 0 |
| P104895 | `342_the_recycle_bin_stops_holding_the_door.sql` — the restaurant ROW stays, marked purged — it is what the kept bills hang off | count the restaurants row and read purged_at | ✅ | the row remains and purged_at is marked |
| P104896 | `342_the_recycle_bin_stops_holding_the_door.sql` — and a SECOND purge is refused, naming the date — the kept bills are not exposed to a re-run | call it again through t33_try | ✅ | → P0001 This restaurant has already been purged (purged_at=2026-09-17 15:14:56.176803+00) — its bills are kept on purpose |
| P104897 | `336_which_screen_prints_the_ticket.sql`, `372_there_is_no_printing_mode.sql` — with auto-print NOT switched on, an order queues NO paper — the kitchen screen shows it instead, and nothing is switched on to make that happen | insert an order while settings.auto_print_kot is off and count the jobs it queued | ✅ | the switch reads false and the order queued 0 jobs |
| P104898 | `335_a_kitchen_ticket_queues_itself.sql` — with it switched on, a new kitchen ticket QUEUES ITSELF — no screen has to be awake and noticing | turn the switch on inside the transaction, insert an order, count the jobs it queued | ✅ | the order queued 1 job by the trigger, not by a tab observing it |
| P104899 | `335_a_kitchen_ticket_queues_itself.sql` — …and only a ticket the kitchen still has to make queues paper — an order that arrives already served does not | insert an order with status 'served' and count its jobs | ✅ | a served order queued 0 jobs |
| P104900 | `335_a_kitchen_ticket_queues_itself.sql` — the queued job says what it is and that it is waiting | read the new job's kind and status | ✅ | kind 'kot', status 'queued' |
| P152001 | `339_a_reprinted_bill_says_nothing.sql`, `333_a_reprinted_bill_knows_it_is_a_reprint.sql` — a FIRST print is not marked as a reprint — the flag exists to tell the two apart | read the reprint flag on the job the order queued | ✅ | reprint = false on the first print |
| P152002 | `381_a_helper_says_which_copy_it_is.sql` — a setup code can record that an OUT-OF-DATE helper file tried to use it | confirm the column exists on print_setup_codes | ✅ | refused_old_file_at is present, so the Printing board can say why nothing happened |
| P152003 | `363_a_tablet_switch_holds_only_its_three_values_and_the_limiter_forgets_dead_counters.sql` — a tablet switch REFUSES a value that is not one of its three | try to write 'sometimes' into it through t33_try | ✅ | → 23514 new row for relation "settings" violates check constraint "settings_tablet_parcel_check" |
| P152004 | `363_a_tablet_switch_holds_only_its_three_values_and_the_limiter_forgets_dead_counters.sql` — …and accepts one that is, so the check is not simply blocking every write | write 'pin' into the same switch | ✅ | → ok |
| P152005 | `350_an_old_web_address_still_finds_the_restaurant.sql` — a retired web address still RESOLVES to its restaurant, so a printed QR code keeps working after a rename | retire an address the way the admin route does, then ask lfh_slug_moved for it | ✅ | lfh_slug_moved('t33-probe-old') answers 't33-probe-new' — the address it MOVED TO, which is what lets the guest door redirect rather than 404 |
| P152006 | `350_an_old_web_address_still_finds_the_restaurant.sql` — …and an address that never existed resolves to nothing, rather than to somebody | ask lfh_slug_moved for an address that was never used | ✅ | an unknown address returns NONE |
| P152007 | `319_the_bin_stops_locking_a_restaurants_name.sql` — two LIVE restaurants can never hold the same web address | try to insert a second restaurant on the current slug | ✅ | → 23505 duplicate key value violates unique constraint "restaurants_slug_live_key" |
| P152008 | `370_a_reused_web_address_serves_the_live_restaurant.sql` — a RETIRED address can be taken by a new restaurant — the history does not lock the name forever | try to insert a restaurant on the retired slug | ✅ | → ok |
| P152009 | `370_a_reused_web_address_serves_the_live_restaurant.sql` — …and once it is taken, the address serves the LIVE restaurant rather than the history's old owner | read which restaurant answers on that slug now | ✅ | the address now serves 'T33 Probe Reuse' |
| P152010 | `384_the_last_of_the_retired_stub.sql` — admin_purge_restaurant — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152011 | `384_the_last_of_the_retired_stub.sql` — admin_purge_restaurant — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152012 | `384_the_last_of_the_retired_stub.sql` — admin_purge_restaurant — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152013 | `322_the_admin_floor_reads_every_restaurant_in_one_pass.sql` — lfh_admin_floor_all — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152014 | `322_the_admin_floor_reads_every_restaurant_in_one_pass.sql` — lfh_admin_floor_all — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152015 | `322_the_admin_floor_reads_every_restaurant_in_one_pass.sql` — lfh_admin_floor_all — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152016 | `348_the_platform_count_ignores_a_binned_restaurant.sql` — lfh_admin_orders_by_source — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152017 | `348_the_platform_count_ignores_a_binned_restaurant.sql` — lfh_admin_orders_by_source — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152018 | `348_the_platform_count_ignores_a_binned_restaurant.sql` — lfh_admin_orders_by_source — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152019 | `348_the_platform_count_ignores_a_binned_restaurant.sql` — lfh_admin_orders_count — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152020 | `348_the_platform_count_ignores_a_binned_restaurant.sql` — lfh_admin_orders_count — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152021 | `348_the_platform_count_ignores_a_binned_restaurant.sql` — lfh_admin_orders_count — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152022 | `348_the_platform_count_ignores_a_binned_restaurant.sql` — lfh_admin_orders_timeseries — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152023 | `348_the_platform_count_ignores_a_binned_restaurant.sql` — lfh_admin_orders_timeseries — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152024 | `348_the_platform_count_ignores_a_binned_restaurant.sql` — lfh_admin_orders_timeseries — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152025 | `343_platform_revenue_counts_every_payment.sql` — lfh_admin_platform_collected — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152026 | `343_platform_revenue_counts_every_payment.sql` — lfh_admin_platform_collected — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152027 | `343_platform_revenue_counts_every_payment.sql` — lfh_admin_platform_collected — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152028 | `382_the_health_signal_counts_real_orders_only.sql` — lfh_admin_restaurant_health — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152029 | `382_the_health_signal_counts_real_orders_only.sql` — lfh_admin_restaurant_health — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152030 | `382_the_health_signal_counts_real_orders_only.sql` — lfh_admin_restaurant_health — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152031 | `347_usage_and_cost_answers_for_any_window.sql` — lfh_admin_usage_range — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152032 | `347_usage_and_cost_answers_for_any_window.sql` — lfh_admin_usage_range — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY INVOKER — runs as the caller, so no pin is needed |
| P152033 | `347_usage_and_cost_answers_for_any_window.sql` — lfh_admin_usage_range — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152034 | `340_was_the_food_actually_made.sql` — lfh_audit_kind_counts — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152035 | `340_was_the_food_actually_made.sql` — lfh_audit_kind_counts — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152036 | `340_was_the_food_actually_made.sql` — lfh_audit_kind_counts — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152037 | `314_a_settled_bill_is_annotated_not_rewritten.sql` — lfh_audit_risk — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152038 | `314_a_settled_bill_is_annotated_not_rewritten.sql` — lfh_audit_risk — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY INVOKER — runs as the caller, so no pin is needed |
| P152039 | `314_a_settled_bill_is_annotated_not_rewritten.sql` — lfh_audit_risk — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152040 | `340_was_the_food_actually_made.sql` — lfh_audit_tags — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152041 | `340_was_the_food_actually_made.sql` — lfh_audit_tags — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY INVOKER — runs as the caller, so no pin is needed |
| P152042 | `340_was_the_food_actually_made.sql` — lfh_audit_tags — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152043 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_banquet_bill_create — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152044 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_banquet_bill_create — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152045 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_banquet_bill_create — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152046 | `385_no_function_guesses_the_restaurant.sql` — lfh_banquet_place_order — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152047 | `385_no_function_guesses_the_restaurant.sql` — lfh_banquet_place_order — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152048 | `385_no_function_guesses_the_restaurant.sql` — lfh_banquet_place_order — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152049 | `332_every_bill_is_signed_and_chained.sql` — lfh_bill_chain_append — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152050 | `332_every_bill_is_signed_and_chained.sql` — lfh_bill_chain_append — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152051 | `332_every_bill_is_signed_and_chained.sql` — lfh_bill_chain_append — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152052 | `332_every_bill_is_signed_and_chained.sql` — lfh_bill_chain_append_only — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152053 | `332_every_bill_is_signed_and_chained.sql` — lfh_bill_chain_append_only — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY INVOKER — runs as the caller, so no pin is needed |
| P152054 | `332_every_bill_is_signed_and_chained.sql` — lfh_bill_chain_append_only — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152055 | `332_every_bill_is_signed_and_chained.sql` — lfh_bill_chain_hash — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152056 | `332_every_bill_is_signed_and_chained.sql` — lfh_bill_chain_hash — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY INVOKER — runs as the caller, so no pin is needed |
| P152057 | `332_every_bill_is_signed_and_chained.sql` — lfh_bill_chain_hash — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152058 | `332_every_bill_is_signed_and_chained.sql` — lfh_bill_chain_payload — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152059 | `332_every_bill_is_signed_and_chained.sql` — lfh_bill_chain_payload — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY INVOKER — runs as the caller, so no pin is needed |
| P152060 | `332_every_bill_is_signed_and_chained.sql` — lfh_bill_chain_payload — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152061 | `361_a_sale_is_locked_from_the_kitchen_not_the_till.sql` — lfh_block_issued_delete — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152062 | `361_a_sale_is_locked_from_the_kitchen_not_the_till.sql` — lfh_block_issued_delete — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152063 | `361_a_sale_is_locked_from_the_kitchen_not_the_till.sql` — lfh_block_issued_delete — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152064 | `321_the_sweep_of_layer_b.sql` — lfh_bump_orders_watermark — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152065 | `321_the_sweep_of_layer_b.sql` — lfh_bump_orders_watermark — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152066 | `321_the_sweep_of_layer_b.sql` — lfh_bump_orders_watermark — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152067 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_call_waiter_table — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152068 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_call_waiter_table — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152069 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_call_waiter_table — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152070 | `340_was_the_food_actually_made.sql` — lfh_cancel_classify — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152071 | `340_was_the_food_actually_made.sql` — lfh_cancel_classify — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152072 | `340_was_the_food_actually_made.sql` — lfh_cancel_classify — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152073 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_capture_customer — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152074 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_capture_customer — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152075 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_capture_customer — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152076 | `385_no_function_guesses_the_restaurant.sql` — lfh_check_ban — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152077 | `385_no_function_guesses_the_restaurant.sql` — lfh_check_ban — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152078 | `385_no_function_guesses_the_restaurant.sql` — lfh_check_ban — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152079 | `371_customer_search_row_cap_actually_caps.sql` — lfh_customer_phone_search — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152080 | `371_customer_search_row_cap_actually_caps.sql` — lfh_customer_phone_search — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152081 | `371_customer_search_row_cap_actually_caps.sql` — lfh_customer_phone_search — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152082 | `385_no_function_guesses_the_restaurant.sql` — lfh_customers_fingerprint — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152083 | `385_no_function_guesses_the_restaurant.sql` — lfh_customers_fingerprint — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152084 | `385_no_function_guesses_the_restaurant.sql` — lfh_customers_fingerprint — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152085 | `388_a_money_function_reads_like_it_behaves.sql` — lfh_delete_order_item — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152086 | `388_a_money_function_reads_like_it_behaves.sql` — lfh_delete_order_item — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152087 | `388_a_money_function_reads_like_it_behaves.sql` — lfh_delete_order_item — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152088 | `385_no_function_guesses_the_restaurant.sql` — lfh_device_banned — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152089 | `385_no_function_guesses_the_restaurant.sql` — lfh_device_banned — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152090 | `385_no_function_guesses_the_restaurant.sql` — lfh_device_banned — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152091 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_floor_state — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152092 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_floor_state — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152093 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_floor_state — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152094 | `332_every_bill_is_signed_and_chained.sql` — lfh_generate_invoice — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152095 | `332_every_bill_is_signed_and_chained.sql` — lfh_generate_invoice — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152096 | `332_every_bill_is_signed_and_chained.sql` — lfh_generate_invoice — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152097 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_geo_ok — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152098 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_geo_ok — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152099 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_geo_ok — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152100 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_greet_device — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152101 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_greet_device — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152102 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_greet_device — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152103 | `370_a_reused_web_address_serves_the_live_restaurant.sql` — lfh_guest_restaurant — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152104 | `370_a_reused_web_address_serves_the_live_restaurant.sql` — lfh_guest_restaurant — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152105 | `370_a_reused_web_address_serves_the_live_restaurant.sql` — lfh_guest_restaurant — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152106 | `329_a_prep_batch_is_one_action.sql` — lfh_inv_produce — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152107 | `329_a_prep_batch_is_one_action.sql` — lfh_inv_produce — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152108 | `329_a_prep_batch_is_one_action.sql` — lfh_inv_produce — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152109 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_is_blocked — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152110 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_is_blocked — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152111 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_is_blocked — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152112 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_join_session — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152113 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_join_session — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152114 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_join_session — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152115 | `364_a_split_part_can_be_pay_later.sql` — lfh_khata_outstanding — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152116 | `364_a_split_part_can_be_pay_later.sql` — lfh_khata_outstanding — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152117 | `364_a_split_part_can_be_pay_later.sql` — lfh_khata_outstanding — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152118 | `364_a_split_part_can_be_pay_later.sql` — lfh_khata_outstanding_summary — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152119 | `364_a_split_part_can_be_pay_later.sql` — lfh_khata_outstanding_summary — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152120 | `364_a_split_part_can_be_pay_later.sql` — lfh_khata_outstanding_summary — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152121 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_kitchen_tickets — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152122 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_kitchen_tickets — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152123 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_kitchen_tickets — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152124 | `335_a_kitchen_ticket_queues_itself.sql` — lfh_kot_queue_autoprint — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152125 | `335_a_kitchen_ticket_queues_itself.sql` — lfh_kot_queue_autoprint — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152126 | `335_a_kitchen_ticket_queues_itself.sql` — lfh_kot_queue_autoprint — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152127 | `387_the_last_three_owner_reports_get_their_own_memory.sql` — lfh_owner_customer_bills — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152128 | `387_the_last_three_owner_reports_get_their_own_memory.sql` — lfh_owner_customer_bills — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152129 | `387_the_last_three_owner_reports_get_their_own_memory.sql` — lfh_owner_customer_bills — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152130 | `387_the_last_three_owner_reports_get_their_own_memory.sql` — lfh_owner_heatmap — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152131 | `387_the_last_three_owner_reports_get_their_own_memory.sql` — lfh_owner_heatmap — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152132 | `387_the_last_three_owner_reports_get_their_own_memory.sql` — lfh_owner_heatmap — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152133 | `383_a_restaurant_in_the_bin_leaves_the_estate.sql` — lfh_owner_overview — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152134 | `383_a_restaurant_in_the_bin_leaves_the_estate.sql` — lfh_owner_overview — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152135 | `383_a_restaurant_in_the_bin_leaves_the_estate.sql` — lfh_owner_overview — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152136 | `377_the_settlement_reads_the_same_day_the_money_does.sql` — lfh_owner_payment_breakdown — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152137 | `377_the_settlement_reads_the_same_day_the_money_does.sql` — lfh_owner_payment_breakdown — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152138 | `377_the_settlement_reads_the_same_day_the_money_does.sql` — lfh_owner_payment_breakdown — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152139 | `327_the_records_strip_stops_re_reading_all_time.sql` — lfh_owner_records — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152140 | `327_the_records_strip_stops_re_reading_all_time.sql` — lfh_owner_records — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152141 | `327_the_records_strip_stops_re_reading_all_time.sql` — lfh_owner_records — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152142 | `321_the_sweep_of_layer_b.sql` — lfh_owner_restaurant_revenue — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152143 | `321_the_sweep_of_layer_b.sql` — lfh_owner_restaurant_revenue — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152144 | `321_the_sweep_of_layer_b.sql` — lfh_owner_restaurant_revenue — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152145 | `337_a_report_reads_the_takings_not_the_discount.sql` — lfh_owner_revenue_timeseries — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152146 | `337_a_report_reads_the_takings_not_the_discount.sql` — lfh_owner_revenue_timeseries — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152147 | `337_a_report_reads_the_takings_not_the_discount.sql` — lfh_owner_revenue_timeseries — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152148 | `337_a_report_reads_the_takings_not_the_discount.sql` — lfh_owner_sales_report — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152149 | `337_a_report_reads_the_takings_not_the_discount.sql` — lfh_owner_sales_report — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152150 | `337_a_report_reads_the_takings_not_the_discount.sql` — lfh_owner_sales_report — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152151 | `387_the_last_three_owner_reports_get_their_own_memory.sql` — lfh_owner_tips — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152152 | `387_the_last_three_owner_reports_get_their_own_memory.sql` — lfh_owner_tips — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152153 | `387_the_last_three_owner_reports_get_their_own_memory.sql` — lfh_owner_tips — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152154 | `379_a_guest_number_written_the_international_way.sql` — lfh_phone10 — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152155 | `379_a_guest_number_written_the_international_way.sql` — lfh_phone10 — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY INVOKER — runs as the caller, so no pin is needed |
| P152156 | `379_a_guest_number_written_the_international_way.sql` — lfh_phone10 — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152157 | `357_the_accept_is_only_skipped_for_an_order_still_on_the_floor.sql` — lfh_place_order — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152158 | `357_the_accept_is_only_skipped_for_an_order_still_on_the_floor.sql` — lfh_place_order — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152159 | `357_the_accept_is_only_skipped_for_an_order_still_on_the_floor.sql` — lfh_place_order — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152160 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_place_order_public — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152161 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_place_order_public — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152162 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_place_order_public — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152163 | `388_a_money_function_reads_like_it_behaves.sql` — lfh_platform_insert — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152164 | `388_a_money_function_reads_like_it_behaves.sql` — lfh_platform_insert — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152165 | `388_a_money_function_reads_like_it_behaves.sql` — lfh_platform_insert — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152166 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_price_order — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152167 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_price_order — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY INVOKER — runs as the caller, so no pin is needed |
| P152168 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_price_order — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152169 | `363_a_tablet_switch_holds_only_its_three_values_and_the_limiter_forgets_dead_counters.sql` — lfh_prune_logs — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152170 | `363_a_tablet_switch_holds_only_its_three_values_and_the_limiter_forgets_dead_counters.sql` — lfh_prune_logs — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152171 | `363_a_tablet_switch_holds_only_its_three_values_and_the_limiter_forgets_dead_counters.sql` — lfh_prune_logs — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152172 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_recognize_customer — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152173 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_recognize_customer — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152174 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_recognize_customer — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152175 | `317_a_pay_later_bill_belongs_to_the_day_it_was_paid.sql` — lfh_refresh_orders_daily_agg — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152176 | `317_a_pay_later_bill_belongs_to_the_day_it_was_paid.sql` — lfh_refresh_orders_daily_agg — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152177 | `317_a_pay_later_bill_belongs_to_the_day_it_was_paid.sql` — lfh_refresh_orders_daily_agg — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152178 | `315_the_rollup_carries_the_net_too.sql` — lfh_refresh_orders_report_monthly_agg — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152179 | `315_the_rollup_carries_the_net_too.sql` — lfh_refresh_orders_report_monthly_agg — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152180 | `315_the_rollup_carries_the_net_too.sql` — lfh_refresh_orders_report_monthly_agg — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152181 | `327_the_records_strip_stops_re_reading_all_time.sql` — lfh_refresh_owner_records — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152182 | `327_the_records_strip_stops_re_reading_all_time.sql` — lfh_refresh_owner_records — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152183 | `327_the_records_strip_stops_re_reading_all_time.sql` — lfh_refresh_owner_records — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152184 | `355_a_removed_order_leaves_every_board.sql` — lfh_removed_order_leaves_every_board — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152185 | `355_a_removed_order_leaves_every_board.sql` — lfh_removed_order_leaves_every_board — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY INVOKER — runs as the caller, so no pin is needed |
| P152186 | `355_a_removed_order_leaves_every_board.sql` — lfh_removed_order_leaves_every_board — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152187 | `378_one_name_everywhere_for_a_guest.sql` — lfh_rename_my_reviews — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152188 | `378_one_name_everywhere_for_a_guest.sql` — lfh_rename_my_reviews — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152189 | `378_one_name_everywhere_for_a_guest.sql` — lfh_rename_my_reviews — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152190 | `365_reopen_puts_the_table_back_not_the_bill.sql` — lfh_reopen_table — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152191 | `365_reopen_puts_the_table_back_not_the_bill.sql` — lfh_reopen_table — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152192 | `365_reopen_puts_the_table_back_not_the_bill.sql` — lfh_reopen_table — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152193 | `388_a_money_function_reads_like_it_behaves.sql` — lfh_reprice_order — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152194 | `388_a_money_function_reads_like_it_behaves.sql` — lfh_reprice_order — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152195 | `388_a_money_function_reads_like_it_behaves.sql` — lfh_reprice_order — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152196 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_request — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152197 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_request — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152198 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_request — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152199 | `385_no_function_guesses_the_restaurant.sql` — lfh_request_unban — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152200 | `385_no_function_guesses_the_restaurant.sql` — lfh_request_unban — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152201 | `385_no_function_guesses_the_restaurant.sql` — lfh_request_unban — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152202 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_rid — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152203 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_rid — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY INVOKER — runs as the caller, so no pin is needed |
| P152204 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_rid — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152205 | `397_a_platform_event_is_announced_to_nobodys_restaurant.sql` — lfh_rt_emit — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152206 | `397_a_platform_event_is_announced_to_nobodys_restaurant.sql` — lfh_rt_emit — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152207 | `397_a_platform_event_is_announced_to_nobodys_restaurant.sql` — lfh_rt_emit — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152208 | `320_a_power_taken_off_one_person_reaches_their_screen.sql` — lfh_rt_emit_staff_perms — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152209 | `320_a_power_taken_off_one_person_reaches_their_screen.sql` — lfh_rt_emit_staff_perms — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152210 | `320_a_power_taken_off_one_person_reaches_their_screen.sql` — lfh_rt_emit_staff_perms — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152211 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_send_otp — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152212 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_send_otp — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152213 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_send_otp — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152214 | `374_a_mark_must_not_outlive_its_party_on_the_delete_path.sql` — lfh_session_delete_cleanup — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152215 | `374_a_mark_must_not_outlive_its_party_on_the_delete_path.sql` — lfh_session_delete_cleanup — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY INVOKER — runs as the caller, so no pin is needed |
| P152216 | `374_a_mark_must_not_outlive_its_party_on_the_delete_path.sql` — lfh_session_delete_cleanup — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152217 | `318_the_guest_sees_the_bill_the_paper_will_print.sql` — lfh_session_state — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152218 | `318_the_guest_sees_the_bill_the_paper_will_print.sql` — lfh_session_state — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152219 | `318_the_guest_sees_the_bill_the_paper_will_print.sql` — lfh_session_state — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152220 | `397_a_platform_event_is_announced_to_nobodys_restaurant.sql` — lfh_set_topic_rid — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152221 | `397_a_platform_event_is_announced_to_nobodys_restaurant.sql` — lfh_set_topic_rid — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY INVOKER — runs as the caller, so no pin is needed |
| P152222 | `397_a_platform_event_is_announced_to_nobodys_restaurant.sql` — lfh_set_topic_rid — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152223 | `350_an_old_web_address_still_finds_the_restaurant.sql` — lfh_slug_moved — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152224 | `350_an_old_web_address_still_finds_the_restaurant.sql` — lfh_slug_moved — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152225 | `350_an_old_web_address_still_finds_the_restaurant.sql` — lfh_slug_moved — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152226 | `314_a_settled_bill_is_annotated_not_rewritten.sql` — lfh_staff_edit_item_note — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152227 | `314_a_settled_bill_is_annotated_not_rewritten.sql` — lfh_staff_edit_item_note — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152228 | `314_a_settled_bill_is_annotated_not_rewritten.sql` — lfh_staff_edit_item_note — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152229 | `394_a_staff_placed_order_is_marked_in_one_call.sql` — lfh_staff_mark_placed — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152230 | `394_a_staff_placed_order_is_marked_in_one_call.sql` — lfh_staff_mark_placed — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152231 | `394_a_staff_placed_order_is_marked_in_one_call.sql` — lfh_staff_mark_placed — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152232 | `321_the_sweep_of_layer_b.sql` — lfh_staff_move_order — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152233 | `321_the_sweep_of_layer_b.sql` — lfh_staff_move_order — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152234 | `321_the_sweep_of_layer_b.sql` — lfh_staff_move_order — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152235 | `388_a_cancelled_order_records_when_it_was_cancelled.sql` — lfh_staff_move_order_item — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152236 | `388_a_cancelled_order_records_when_it_was_cancelled.sql` — lfh_staff_move_order_item — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152237 | `388_a_cancelled_order_records_when_it_was_cancelled.sql` — lfh_staff_move_order_item — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152238 | `389_a_cancelled_order_is_not_a_waiters_takings.sql` — lfh_staff_performance — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152239 | `389_a_cancelled_order_is_not_a_waiters_takings.sql` — lfh_staff_performance — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152240 | `389_a_cancelled_order_is_not_a_waiters_takings.sql` — lfh_staff_performance — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152241 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_staff_place_order — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152242 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_staff_place_order — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152243 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_staff_place_order — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152244 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_submit_review — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152245 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_submit_review — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152246 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_submit_review — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152247 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_table_status — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152248 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_table_status — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152249 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_table_status — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | guest-callable, and on the written allow-list |
| P152250 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_table_view_summary — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152251 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_table_view_summary — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152252 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_table_view_summary — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152253 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_uncapture_customer — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152254 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_uncapture_customer — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152255 | `386_a_null_restaurant_is_refused_not_guessed.sql` — lfh_uncapture_customer — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152256 | `353_a_binned_bill_is_not_a_tampered_bill.sql` — lfh_verify_bill_chain — the function this file declares is INSTALLED in the database | look it up in pg_proc | ✅ | present |
| P152257 | `353_a_binned_bill_is_not_a_tampered_bill.sql` — lfh_verify_bill_chain — its search_path is pinned IN the definition (a separate ALTER would not survive the next rewrite) | read the installed header for SET search_path when it is SECURITY DEFINER | ✅ | SECURITY DEFINER with search_path pinned |
| P152258 | `353_a_binned_bill_is_not_a_tampered_bill.sql` — lfh_verify_bill_chain — it is reachable with the public menu key only if that is a written decision | read proacl and compare against the allow-list verify-db-grants.mjs keeps | ✅ | not reachable with the public key |
| P152259 | `314_a_settled_bill_is_annotated_not_rewritten.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 314,312 · every declared object present |
| P152260 | `315_the_rollup_carries_the_net_too.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 315,310 · every declared object present |
| P152261 | `316_the_three_numbers_and_the_two_switch_bags_say_what_they_are.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 316 · every declared object present |
| P152262 | `317_a_pay_later_bill_belongs_to_the_day_it_was_paid.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 317 · every declared object present |
| P152263 | `318_the_guest_sees_the_bill_the_paper_will_print.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 318 · every declared object present |
| P152264 | `319_the_bin_stops_locking_a_restaurants_name.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 319 · every declared object present |
| P152265 | `320_a_power_taken_off_one_person_reaches_their_screen.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 320,299 · every declared object present |
| P152266 | `321_the_sweep_of_layer_b.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 321,151,308,320,313,275,276 · every declared object present |
| P152267 | `322_the_admin_floor_reads_every_restaurant_in_one_pass.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 322,850,851 · every declared object present |
| P152268 | `323_one_definition_of_what_dishes_are_on_an_order.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 323 · every declared object present |
| P152269 | `324_the_kitchen_reads_the_one_definition.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 324,323 · every declared object present |
| P152270 | `325_a_dish_photo_can_be_uploaded.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 325 · every declared object present |
| P152271 | `326_a_new_module_needs_no_new_column.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 326,320 · every declared object present |
| P152272 | `327_the_records_strip_stops_re_reading_all_time.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 327,321 · every declared object present |
| P152273 | `328_a_bill_counter_is_not_a_settings_change.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 328,327 · every declared object present |
| P152274 | `329_a_prep_batch_is_one_action.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 329 · every declared object present |
| P152275 | `330_the_admin_can_hand_over_a_login.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 330 · every declared object present |
| P152276 | `331_a_cancelled_sale_takes_no_invoice_number.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 331 · every declared object present |
| P152277 | `332_every_bill_is_signed_and_chained.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 332 · every declared object present |
| P152278 | `333_a_reprinted_bill_knows_it_is_a_reprint.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 333 · every declared object present |
| P152279 | `334_a_diner_at_a_joined_table_is_not_stranded.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 334,249 · every declared object present |
| P152280 | `335_a_kitchen_ticket_queues_itself.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 335 · every declared object present |
| P152281 | `336_which_screen_prints_the_ticket.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 336,335 · every declared object present |
| P152282 | `337_a_report_reads_the_takings_not_the_discount.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 337 · every declared object present |
| P152283 | `338_one_screen_is_the_printer.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 338,335,336 · every declared object present |
| P152284 | `339_a_reprinted_bill_says_nothing.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 339,333 · every declared object present |
| P152285 | `340_was_the_food_actually_made.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 340,337,338,339 · every declared object present |
| P152286 | `341_a_helper_prints_the_paper.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 341 · every declared object present |
| P152287 | `342_the_recycle_bin_stops_holding_the_door.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 342,128 · every declared object present |
| P152288 | `343_platform_revenue_counts_every_payment.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 343 · every declared object present |
| P152289 | `344_a_problem_can_wait_and_one_fault_is_one_tile.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 344,343,307 · every declared object present |
| P152290 | `345_a_purge_clears_the_operational_tables_again.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 345,342 · every declared object present |
| P152291 | `346_a_purge_clears_the_printing_setup.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 346,341 · every declared object present |
| P152292 | `347_usage_and_cost_answers_for_any_window.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 347,346 · every declared object present |
| P152293 | `348_the_platform_count_ignores_a_binned_restaurant.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 348 · every declared object present |
| P152294 | `349_two_new_reads_get_their_index.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 349 · every declared object present |
| P152295 | `350_an_old_web_address_still_finds_the_restaurant.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 350,349 · every declared object present |
| P152296 | `351_a_complaint_knows_its_printer.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 351,269 · every declared object present |
| P152297 | `352_a_reseed_cannot_undo_an_admins_choice.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 352,351,360,364,349,350 · every declared object present |
| P152298 | `353_a_binned_bill_is_not_a_tampered_bill.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 353,352,351,354 · every declared object present |
| P152299 | `354_a_purge_clears_the_retired_web_addresses.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 354,353 · every declared object present |
| P152300 | `355_a_removed_order_leaves_every_board.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 355 · every declared object present |
| P152301 | `356_the_owner_dashboard_keeps_its_working_memory.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 356,192,128,398,340 · every declared object present |
| P152302 | `357_the_accept_is_only_skipped_for_an_order_still_on_the_floor.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 357,164 · every declared object present |
| P152303 | `358_a_forgotten_restaurant_fails_loudly.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 358,078 · every declared object present |
| P152304 | `359_a_rating_is_given_not_typed.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 359,030 · every declared object present |
| P152305 | `360_the_last_half_of_a_retired_stub.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 360,037,267,296,297 · every declared object present |
| P152306 | `361_a_sale_is_locked_from_the_kitchen_not_the_till.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 361 · every declared object present |
| P152307 | `362_the_server_only_tables_lose_their_leftover_public_grant.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 362,204,121,230 · every declared object present |
| P152308 | `363_a_tablet_switch_holds_only_its_three_values_and_the_limiter_forgets_dead_counters.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 363 · every declared object present |
| P152309 | `364_a_split_part_can_be_pay_later.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 364,352 · every declared object present |
| P152310 | `365_reopen_puts_the_table_back_not_the_bill.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 365 · every declared object present |
| P152311 | `366_a_device_reports_which_offline_layer_it_is_running.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 366 · every declared object present |
| P152312 | `367_a_device_sets_up_its_own_printer.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 367 · every declared object present |
| P152313 | `368_a_helper_pairs_itself_no_code_to_carry.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 368 · every declared object present except table print_pairings, index print_pairings_expires_idx (with its table print_pairings), retired by a LATER migration |
| P152314 | `369_a_purge_clears_the_pending_printer_handshakes.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 369,368 · every declared object present |
| P152315 | `370_a_reused_web_address_serves_the_live_restaurant.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 370,369 · every declared object present |
| P152316 | `371_customer_search_row_cap_actually_caps.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 371,365,370 · every declared object present |
| P152317 | `372_there_is_no_printing_mode.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 372,326 · every declared object present |
| P152318 | `373_a_new_restaurant_starts_compact.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 373,372,374 · every declared object present |
| P152319 | `374_a_mark_must_not_outlive_its_party_on_the_delete_path.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 369,374 · every declared object present |
| P152320 | `375_a_reseed_cannot_widen_a_waiters_section.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 369,375 · every declared object present |
| P152321 | `376_the_old_coarse_print_target_becomes_a_route.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 369,376 · every declared object present |
| P152322 | `377_the_settlement_reads_the_same_day_the_money_does.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 367,377 · every declared object present |
| P152323 | `378_one_name_everywhere_for_a_guest.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 378 · every declared object present |
| P152324 | `379_a_guest_number_written_the_international_way.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 379 · every declared object present |
| P152325 | `380_a_setup_code_replaces_the_allow_page.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 380 · every declared object present |
| P152326 | `381_a_helper_says_which_copy_it_is.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 381 · every declared object present |
| P152327 | `382_the_health_signal_counts_real_orders_only.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 382,079,154,149 · every declared object present |
| P152328 | `383_a_restaurant_in_the_bin_leaves_the_estate.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 383,079,154,177 · every declared object present |
| P152329 | `384_the_last_of_the_retired_stub.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 384,037,360 · every declared object present |
| P152330 | `385_no_function_guesses_the_restaurant.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 385 · every declared object present |
| P152331 | `386_a_null_restaurant_is_refused_not_guessed.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 386,385 · every declared object present |
| P152332 | `387_the_last_three_owner_reports_get_their_own_memory.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 387,128 · every declared object present |
| P152333 | `388_a_cancelled_order_records_when_it_was_cancelled.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 388 · every declared object present |
| P152334 | `388_a_money_function_reads_like_it_behaves.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 388 · every declared object present |
| P152335 | `389_a_cancelled_order_is_not_a_waiters_takings.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 389 · every declared object present |
| P152336 | `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 390,310 · every declared object present |
| P152337 | `391_a_table_that_receives_an_order_gets_its_bill_number.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 391,040 · every declared object present |
| P152338 | `392_ten_more_server_only_tables_lose_their_leftover_public_grant.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 392,362 · every declared object present |
| P152339 | `393_the_last_fifteen_guest_tables_lose_their_leftover_public_grant.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 393,362,392 · every declared object present |
| P152340 | `394_a_staff_placed_order_is_marked_in_one_call.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 394 · every declared object present |
| P152341 | `395_an_event_with_no_restaurant_is_announced_to_nobody.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 395,394 · every declared object present |
| P152342 | `396_a_stamped_tax_rate_must_be_a_rate.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 396 · every declared object present |
| P152343 | `397_a_platform_event_is_announced_to_nobodys_restaurant.sql` — this file is safe to run a SECOND time, its header names its own number, and every object it declares is really in the database | read every DDL statement for re-runnability; read the first six lines for the number; match declared tables, triggers and indexes against the catalogue | ✅ | re-seed safe · header names 397,395 · every declared object present |
| P152344 | `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — across every order in the database, none reads a takings figure below zero | count orders WHERE net_amount < 0 | ✅ | 0 of the whole table (31 before migration 390) |
| P152345 | `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — …and none carries more than two decimals | count orders WHERE scale(net_amount) > 2 | ✅ | 0 rows |
| P152346 | `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — the exact source column is STILL exact — rounding it was considered and rejected, with measurements | count orders WHERE scale(disc_gross) > 2; it must be non-zero | ✅ | 13724 rows hold more than two decimals, so migration 301's decision stands |
| P152347 | `358` via `386_a_null_restaurant_is_refused_not_guessed.sql` — not one order, order line or party in the database is missing its restaurant | count nulls on restaurant_id across the three busiest tenant tables | ✅ | orders 0, order lines 0, parties 0 |
| P152348 | `374_a_mark_must_not_outlive_its_party_on_the_delete_path.sql` — no order points at a party that no longer exists | left-join orders to sessions on session_id | ✅ | 0 orphans |
| P152349 | `323_one_definition_of_what_dishes_are_on_an_order.sql` — no order line points at an order that no longer exists | left-join order_items to orders | ✅ | 0 orphans |
| P152350 | `334_a_diner_at_a_joined_table_is_not_stranded.sql` — no order sits on a party belonging to a DIFFERENT restaurant — one party is one restaurant's | join orders to sessions and compare restaurant_id | ✅ | 0 mixed rows |
| P152351 | `358_a_forgotten_restaurant_fails_loudly.sql` — no party belongs to a restaurant that is not in the restaurants table | left-join sessions to restaurants | ✅ | 0 homeless parties |
| P152352 | `331_a_cancelled_sale_takes_no_invoice_number.sql` — no invoice number in the database was MINTED for a sale that was already cancelled — which is the thing migration 331 forbids | count invoiced bills with no surviving live order AND no cancellation that predates the invoice | ✅ | 0 such bills, of the 18 that were invoiced and then had every order cancelled — those correctly KEEP their number (CLAUDE.md: retired and marked cancelled, corrected by a credit note, never an edit). 1 of them cannot be judged either way, because migration 389 deliberately left an untimed cancellati |
| P152353 | `331_a_cancelled_sale_takes_no_invoice_number.sql`, `353_a_binned_bill_is_not_a_tampered_bill.sql` — …and an invoice whose sale was cancelled afterwards is either marked void or carries a credit note, so the correction is on the record | count invoiced-then-cancelled bills that are neither voided nor credited | ❌ | 15 invoiced bills have had every order cancelled and carry NO void mark and NO credit note — RECORDED for the owner's decision, not fixed here: who issues the credit note and when is a billing rule, not a bug |
| P152354 | `391_a_table_that_receives_an_order_gets_its_bill_number.sql` — no OPEN table is holding a live order without a bill number | count open sessions with a live order and no bill_no | ✅ | 0 tables |
| P152355 | `331_a_cancelled_sale_takes_no_invoice_number.sql` — no restaurant has issued the same invoice number twice | group sessions by restaurant and invoice_no and look for a count above one | ✅ | 0 duplicated invoice numbers |
| P152356 | `332_every_bill_is_signed_and_chained.sql` — the signed chain has no duplicated position — one seq per restaurant, once | group bill_chain by restaurant and seq | ✅ | 0 duplicates |
| P152357 | `332_every_bill_is_signed_and_chained.sql` — every row in the signed chain carries both of its hashes, so the chain can actually be verified | count bill_chain rows with a null hash | ✅ | 0 unhashed rows |
| P152358 | `321_the_sweep_of_layer_b.sql` — every breadcrumb in the database is keyed to its topic AND its restaurant — realtime is per-restaurant, as the SaaS rule requires | compare topic_rid against topic || ':' || restaurant_id on every row | ✅ | 0 miskeyed rows |
| P152359 | `397_a_platform_event_is_announced_to_nobodys_restaurant.sql` — no platform-level activity event in the table is announced as a RESTAURANT'S — it is announced to the unscoped admin console and keyed to nobody | join audit breadcrumbs to their staff_actions row, and for those whose action has no restaurant, check the breadcrumb carries no restaurant and is keyed '<topic>:platform' | ✅ | 0 misattributed breadcrumbs (migration 395 stopped the mislabel, 397 gave the instant refresh back without it) |
| P152360 | `384_the_last_of_the_retired_stub.sql`, `380_a_setup_code_replaces_the_allow_page.sql` — the two retired tables have not come back | look for verification_codes and print_pairings in the catalogue | ✅ | 0 of the two are present |
| P152361 | `358` via `385_no_function_guesses_the_restaurant.sql` — no column, no function signature and no function body still guesses restaurant #1 | count defaulting columns, defaulting signatures and coalescing bodies | ✅ | columns 0, signatures 0, bodies 0 |
| P152362 | `362_the_server_only_tables_lose_their_leftover_public_grant.sql` — row-level security is on for every table in the schema | count tables with relrowsecurity false | ✅ | 0 without RLS |
| P152363 | `363_a_tablet_switch_holds_only_its_three_values_and_the_limiter_forgets_dead_counters.sql` — the six scheduled jobs these migrations rely on are all still scheduled | count them in cron.job by name | ✅ | 6 of 6 scheduled |
| P152364 | `254` via `345_a_purge_clears_the_operational_tables_again.sql` — NOTHING is scheduled to end a table on its own — the owner's rule, and no cron may quietly break it | search cron.job commands for a session close or table end | ✅ | 0 such jobs |
| P152365 | `345_a_purge_clears_the_operational_tables_again.sql` — for every restaurant that has been purged, its MONEY is still there | count orders belonging to purged restaurants | ✅ | 124 purged restaurants hold 12 orders between them |
| P152366 | `345_a_purge_clears_the_operational_tables_again.sql` — …and its OPERATIONAL rows are gone | count feedback belonging to purged restaurants | ✅ | 0 feedback rows left behind |
| P152367 | `337_a_report_reads_the_takings_not_the_discount.sql` — every stamped tax rate in the database is inside the band a GST rate can be — none is negative or above 28% | read tax_rate off every order carrying one and check the band directly (the staff-only plausibility function is not callable by a read-only role, correctly) | ✅ | 0 implausible rates |
| P152368 | `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — no bill in the database carries a negative total, subtotal or tax | count orders with any negative money column | ✅ | 0 rows |
| P152369 | `353_a_binned_bill_is_not_a_tampered_bill.sql` — cancelled sales are still PRESENT as rows — a sale can be cancelled, never made to disappear | count cancelled orders that are not soft-deleted | ✅ | 3113 cancelled orders are on record, exactly as the compliance rule requires |
| P152370 | `370_a_reused_web_address_serves_the_live_restaurant.sql` — no retired web address shadows the address a live restaurant is using right now | join the history to restaurants on slug | ✅ | 0 shadowing rows |
| P152371 | `319_the_bin_stops_locking_a_restaurants_name.sql` — no two live restaurants share a web address | group live restaurants by slug | ✅ | none duplicated live slugs |
| P152372 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — AANGAN GARDEN RESTAURANT — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 6076 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152373 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — Burger Barn — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1174 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152374 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — Demo Bistro — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 2469 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152375 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — Green Bowl — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 4699 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152376 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — My Little French House — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 20111 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152377 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — Pizza Palace — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 2394 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152378 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — Sakura Sushi — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 940 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152379 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — Spice Route — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1407 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152380 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — Taco Fiesta — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 965 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152381 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ BillWait mu0rz4u9 — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 12 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152382 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Claim mu0tyi30 — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 8 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152383 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Pickup mu0tm9t2 — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 6 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152384 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Pickup mu0trfv1 — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 5 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152385 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Pickup mu0ttceo — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 8 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152386 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Pickup mu0twkrt — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 8 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152387 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Pickup mu0txkmx — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152388 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0qr1ra — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 13 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152389 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0qr1ra B — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152390 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0qs1hh — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 13 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152391 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0qs1hh B — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152392 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0qtvl6 — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 20 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152393 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0qtvl6 B — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152394 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0qv3lf — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 20 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152395 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0qv3lf B — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152396 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0qwams — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 20 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152397 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0qwams B — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152398 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0qxaul — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 20 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152399 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0qxaul B — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152400 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0qydii — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 20 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152401 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0qydii B — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152402 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0qzxhz — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 20 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152403 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0qzxhz B — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152404 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0r1fly — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 19 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152405 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0r1fly B — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152406 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0r4jwv — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 19 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152407 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0r4jwv B — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152408 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0sczl5 — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 19 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152409 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0sczl5 B — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152410 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0sebxh — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 20 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152411 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0sebxh B — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152412 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0u17kg — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 20 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152413 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu0u17kg B — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152414 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu16zaat — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 20 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152415 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu16zaat B — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152416 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu3pwy8u — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 20 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152417 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu3pwy8u B — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152418 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu3pzr8z — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 20 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152419 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu3pzr8z B — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152420 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu3q2nxu — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 20 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152421 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu3q2nxu B — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152422 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu3qa5rl — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 20 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152423 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu3qa5rl B — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152424 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu40ngz0 — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 20 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152425 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu40ngz0 B — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152426 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu40r9ib — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 20 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152427 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Scenarios mu40r9ib B — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 1 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152428 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Speed mu0rnobx — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 8 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152429 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Speed mu0ru7gf — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 8 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152430 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Speed mu0rvwwf — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 6 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152431 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ Speed mu0s1qxt — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 8 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152432 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ UILoad mu0s922t — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 14 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152433 | `321_the_sweep_of_layer_b.sql`, `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — ZZ UILoad mu0sazpq — every order on this restaurant's tables belongs to THIS restaurant, its bill numbers do not repeat inside one business day, and none of its takings reads below zero | per restaurant: join its parties to their orders and compare restaurant_id; group its bill numbers by the 05:00 IST business day; count negative takings and out-of-band stamped rates | ✅ | 14 orders, all its own; no bill number repeats in a day; no negative takings; no impossible tax rate |
| P152434 | `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — should a discount bigger than the bill read as ZERO collected, or as a negative number? | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | YES — ZERO is right: you cannot collect less than nothing, and a waiter reading minus ₹390 on a report has no way to act on it. The oddity stays findable because discount and total are untouched on the row — so the floor corrects the ANSWER without hiding the CAUSE. |
| P152435 | `390_the_takings_column_rounds_to_paise_and_never_goes_negative.sql` — does flooring at zero risk hiding a real fault?  [answered: NO] | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | NO — YES, AND IT DID — this round found ten orders stamped at 500% whose symptom, had a discount ever been applied, would have been a floored ₹0 rather than a visible minus. That is why migration 396 puts the guard on the RATE column instead of trusting the floor. Recorded as a real limitation, not  |
| P152436 | `328_a_bill_counter_is_not_a_settings_change.sql` — should opening a table burn a bill number? | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | YES — No, and it does not. A waiter taps Open to look at a table; numbering it then leaves gaps in a series a tax inspector may ask about. The number waits for the first order, which is the first moment a sale exists. |
| P152437 | `391_a_table_that_receives_an_order_gets_its_bill_number.sql` — when a joined table is split apart, should the child table draw a NEW bill number or share the parent's? | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | YES — A new one. Two tables paying separately are two bills; sharing a number would make one of them unprintable. The opposite rule holds for a MERGE — one party, one bill — and both are now driven checks rather than claims. |
| P152438 | `331_a_cancelled_sale_takes_no_invoice_number.sql` — should a bill cancelled before anything was invoiced draw an invoice number at all? | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | YES — No. An invoice number is a tax document's identity; minting one for a sale that never happened creates a document that has to be explained. Refusing with a named error code is better than issuing and voiding. |
| P152439 | `332_every_bill_is_signed_and_chained.sql` — is a hash chain worth the complexity for a restaurant? | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | YES — Yes, and it is the one part of this schema that defends the OWNER rather than the diner: it is evidence that a filed bill was never altered. Without it, 'we did not change it' is a claim; with it, a changed or removed row is arithmetically visible. |
| P152440 | `345_a_purge_clears_the_operational_tables_again.sql` — should removing a restaurant for good keep its bills? | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | YES — Yes — and this is the rule that would otherwise be got wrong in the obliging direction. The purge clears how the restaurant RAN and keeps what it SOLD. A purge that deleted bills is the feature that put PetPooja's founders under summons. |
| P152441 | `342_the_recycle_bin_stops_holding_the_door.sql` — should a purge be possible without any waiting period? | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | YES — The owner removed the 90-day lock deliberately (migration 342) and that decision stands. The protections that remain are the ones that matter: it must already be in the bin, restaurant #1 can never go, and a second purge is refused. |
| P152442 | `395_an_event_with_no_restaurant_is_announced_to_nobody.sql` — was silence the right answer for a platform-level event, rather than a 'no restaurant' marker? | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | YES — Yes for now, with one cost written down: the admin feed loses its instant refresh on those events and falls back to the 60-second net. The marker version is the alternative if that matters, and it is recorded in the migration rather than left to be rediscovered. |
| P152443 | `336_which_screen_prints_the_ticket.sql`, `372_there_is_no_printing_mode.sql` — is 'no printing mode' actually simpler for a restaurant than a switch? | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | YES — Yes. A computer prints if one is set up and named; if none is, the kitchen screen does. The owner's own words were that he did not understand the three-option dropdown — a setting nobody can explain is worse than a rule nobody has to set. |
| P152444 | `363_a_tablet_switch_holds_only_its_three_values_and_the_limiter_forgets_dead_counters.sql` — should a capability switch be free text with three conventional values, or constrained? | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | YES — Constrained, and it now is. A stray value read as 'off' would quietly stop a restaurant taking orders on the tablet, with nothing on any screen saying why — a silent capability loss is the worst kind. |
| P152445 | `350_an_old_web_address_still_finds_the_restaurant.sql` — should a renamed restaurant's old QR codes keep working? | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | YES — Yes. The codes are printed on tables and menus; a rename that breaks them makes a guest think the restaurant is closed. Keeping the address and redirecting costs one small table. |
| P152446 | `370_a_reused_web_address_serves_the_live_restaurant.sql` — …but should a retired address be reusable by a NEW restaurant? | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | YES — Yes, and the resolution order is the part that matters: the LIVE restaurant wins over the history. Otherwise a natural name would be locked away for ever by a restaurant that no longer exists. |
| P152447 | `389_a_cancelled_order_is_not_a_waiters_takings.sql` — should a waiter's figures be decided by a timestamp or by the status? | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | YES — By the STATUS. An order is cancelled whether or not anybody wrote down when — 872 rows proved the difference. The timestamp clause is kept alongside, so the change only ever removes rows from a count. |
| P152448 | `388_a_money_function_reads_like_it_behaves.sql` — is removing a dead `v_rate := 0.05` worth a migration? | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | YES — Yes, and it nearly cost this sweep a false alarm about money. A hard-coded 5% at the top of a money function, in a product whose whole point is per-restaurant GST, is a trap for every future reader even when the value never reaches a figure. |
| P152449 | `384_the_last_of_the_retired_stub.sql` — should a destructive function be re-typed to drop one line?  [answered: NO] | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | NO — NO — and it was not. The body was generated from the live definition with exactly one line filtered out, under an assertion that refused to write the file unless precisely one line had gone. Re-typing a function that permanently deletes a restaurant is how the wrong thing gets deleted. |
| P152450 | `387_the_last_three_owner_reports_get_their_own_memory.sql` — should a change DETECTOR carry the same memory setting as the report it guards? | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | YES — No, and it deliberately does not. A detector reads a watermark and returns one short string; 128MB would be cargo-cult. The exemption is named in verify:grants with the reason, which is what stops the next reader 'fixing' it. |
| P152451 | `371_customer_search_row_cap_actually_caps.sql` — does it matter where a LIMIT sits, if the number is right? | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | YES — It is the whole fault. A LIMIT after json_agg caps the one row the aggregate already collapsed everything into, so the array inside it stays unbounded — the cap read correct and did nothing. The position, not the number, is what this check asserts. |
| P152452 | `353_a_binned_bill_is_not_a_tampered_bill.sql` — should the owner's revenue include bills that were deleted? | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | YES — Yes, and this is the pre-empt every sweep re-discovers. What is OWED drops a deleted bill; what was COLLECTED keeps it. A dashboard that quietly excluded them is the shape of hiding a sale. |
| P152453 | `396_a_stamped_tax_rate_must_be_a_rate.sql` — should the ten bad rates have been divided by 100 instead of cleared?  [answered: NO] | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | NO — Clearing is right. Dividing guesses that 5 meant 5%; clearing makes every reader fall back to the restaurant's own setting, which is what the stamp's own comment says happens when it is absent. A stamp nobody can trust is worse than no stamp. |
| P152454 | `336_which_screen_prints_the_ticket.sql` — is auto-print being OFF by default the right default for a new restaurant? | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | YES — Yes. Paper that appears unasked, at a printer nobody has set up, is worse than a screen a cook already watches. It needs the admin to allow it AND the owner to switch it on — the module rule, and both directions are now driven checks. |
| P152455 | `334_a_diner_at_a_joined_table_is_not_stranded.sql` — should a guest at a joined table keep ordering from their own phone? | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | YES — Yes. Merging tables is a staff act; from the diner's side nothing changed, and a menu that stops working mid-meal reads as the restaurant being broken. The check that no order sits on another restaurant's party is the same rule one level up. |
| P152456 | `supabase/migrations positions 321-403 (all 83 files)` — is this range in good shape, honestly? | read the migration and the code it touches, then judged it against how a real restaurant runs | ✅ | YES — Yes. 500 checks this round, 58 of them driving the database rather than reading it, and the only real fault found in 83 files and 13,243 lines was ten rows of stamped metadata that no screen had ever read. Every reasoned decision in these files held up when it was exercised. |
