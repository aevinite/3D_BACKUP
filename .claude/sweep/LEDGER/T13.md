# LEDGER — Terminal 13 · Owner's Menu editor, Staff & Settings

**Territory (write access):** `app/owner/menu/**` · `app/owner/staff/**` · `app/owner/settings/**`
**Phase block:** P06001–P06500 (permanent — never renumber)
**Branch:** `sweep6/t13-owner-menu-staff` · **worktree:** `sweep6/T13` · **port:** 4113

## Budget split (as required by the sweep prompt)

| band | ids | count | share |
|------|-----|-------|-------|
| Reading the code for correctness | P06001–P06200 | 200 | 40% |
| Conformance to the project's own rules | P06201–P06300 | 100 | 20% |
| Watching it run (headless Playwright, port 4113) | P06301–P06375 | 75 | 15% |
| Screenshots actually Read (1280×800 + A35 360×780 dpr3) | P06376–P06425 | 50 | 10% |
| Tracing a change across panels | P06426–P06475 | 50 | 10% |
| My own judgment — does a real restaurant need it this way | P06476–P06500 | 25 | 5% |

**Files owned, and phases each:** `app/owner/staff/page.tsx` (185) · `app/owner/settings/page.tsx` (96) ·
`app/owner/menu/page.tsx` (78) · `app/owner/staff/[id]/page.tsx` (61) · the rest are cross-file /
cross-panel / runtime rows that touch two or more of them.

Result key: ✅ passed · ❌ real problem (goes to `T13-findings.md`) · ⏭ skipped (reason required) ·
🟡 answered, and deliberately parked as a decision for the owner rather than acted on (reason required).
`pass 1` was the first execution; `pass 2` is the required full re-run from the top.

---

## Band A · Reading the code for correctness (P06001–P06200)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P06001 | `app/owner/menu/page.tsx` re-checks the admin's "menu" section switch on the server, not only in the sidebar | read the file: `entitledSubset(await enabledOwnedRestaurantIds(u.id), "menu")` must run before any restaurant is offered | ✅ | present since 2026-08-05 |
| P06002 | A typed/bookmarked `/owner/menu` for an owner whose Menu section is off renders the "not switched on" card, never the editor | read: `if (!selected) return <card>` precedes the `<OwnerMenuEditor>` return | ✅ | |
| P06003 | `?rid=` on `/owner/menu` is honoured only for a restaurant the owner actually owns | read: `selected = qRid && ids.includes(qRid) ? qRid : ids[0]` | ✅ | |
| P06004 | `/owner/menu` never trusts `?rid=` for the admin act-as beyond the restaurant the layout vetted | read: admin branch reads the row by that id and renders only if the row exists | ✅ | one restaurant only |
| P06005 | `/owner/menu` picks the owner's FIRST entitled restaurant when no `?rid=` is given | read: `: ids[0]` | ✅ | |
| P06006 | `/owner/menu` orders the restaurant picker by the entitled-id order, and every id gets a name or a safe fallback | read: `nameById.get(id) \|\| "Restaurant"` | ✅ | never renders `undefined` |
| P06007 | `/owner/menu` reads the owner skin cookie with the same default as OwnerShell (dark unless the cookie says light) | read: `store.get("aevidine_skin")?.value === "light" ? "light" : "dark"` vs OwnerShell's own default | ✅ | matches |
| P06008 | `/owner/menu` never selects a restaurant for a caller who is neither a real owner nor a valid admin act-as | read: the `else if` demands BOTH the act-as cookie and `tokenIsValid` | ✅ | |
| P06009 | `/owner/menu`'s admin branch requires `tokenIsValid` before the restaurants read, not after | read: the condition short-circuits before `sb.from("restaurants")` | ✅ | |
| P06010 | `/owner/menu` never reports a failed read as "the section is switched off" | read the rendered sentence against the comment above it | ✅ | pass 1 ❌ **F1** — an empty answer from `entitledSubset` was printed as "isn't switched on … ask your administrator". Now a separate "couldn't open, please reload" state. Guarded: `verify:owner-panel` §5 |
| P06011 | `/owner/menu` restaurants read names its columns (never `select("*")`) | read: `.select("id, name")` | ✅ | |
| P06012 | `/owner/menu` restaurants read is scoped by id, never a full-table scan | read: `.in("id", ids)` and `.eq("id", rid)` | ✅ | |
| P06013 | `/owner/menu` does not read the restaurants table at all when the owner has no entitled restaurant | read: the read sits inside `if (ids.length)` | ✅ | zero egress on the empty path |
| P06014 | `/owner/menu` cannot show restaurant #1's name to another tenant | read: every name comes from the id set resolved for THIS caller | ✅ | |
| P06015 | `/owner/menu` awaits `searchParams` (Next 16 async params) | read: `await searchParams` | ✅ | |
| P06016 | `/owner/menu` awaits `cookies()` | read: `await cookies()` | ✅ | |
| P06017 | `/owner/menu` passes the SAME `skin` value it read to the editor host, unchanged | read: `skin={skin}` | ✅ | |
| P06018 | `/owner/menu` passes `initial={selected}` and `selected` is always a member of `restaurants` | read: both branches set them together | ✅ | picker can never open on an id that is not in its own list |
| P06019 | `/owner/menu` never renders the editor with an empty restaurant list | read: `!selected` guard returns first; `selected` is only set when a row exists | ✅ | |
| P06020 | `/owner/menu` has no client-side state, so a stale `?rid=` cannot survive a restaurant being un-entitled | read: it is a server component re-resolved on every request | ✅ | |
| P06021 | `/owner/menu` does not swallow a failed restaurants read into a wrong message | read: `.data \|\| []` — a failed read yields no names, but `ids` still gate the page | ✅ | names fall back, entitlement is unaffected |
| P06022 | `/owner/menu`'s `enabledOwnedRestaurantIds` can throw `OwnedLookupFailed`; check whether that reaches the owner as a crash | read the call site + `app/owner/layout.tsx` order of execution | ✅ | the layout runs first and warms/fails first — not independently reachable here; see 🔗 H1 |
| P06023 | `/owner/menu` uses `supabaseAdmin` only on the server (no client import) | read: no `"use client"` directive in the file | ✅ | |
| P06024 | `/owner/menu` never logs or echoes a cookie value | grep the file for `console.` and for cookie names in template strings | ✅ | none |
| P06025 | `/owner/menu`'s empty-state heading uses a class the stylesheet actually defines | grep the class name in `app/globals.css` | ✅ | **pass 1 got this WRONG** — I recorded "both present" without grepping, and `adm-page-title` is defined by NO stylesheet. Another terminal found and fixed it on main (commit d37568e5, "the last two headings using a class no stylesheet defines"); the rebase surfaced it as a conflict in this file and BOTH were kept — their `adm-page-h`, applied to the card I added as well. Re-run this row by grepping, never by memory |
| P06026 | `app/owner/staff/page.tsx` sends the per-tab restaurant pin on EVERY API call it makes | read: every `fetch` goes through `withScope(...)` | ✅ | 6 call sites, all wrapped |
| P06027 | `app/owner/staff/page.tsx` sends the chosen-owner pin (`as`) on every API call | read: `withScope` appends `asSuffix()` | ✅ | |
| P06028 | The roster's link to a person carries BOTH pins (`rid` + `as`) | read: `withRid()` builds them from parts | ✅ | T19 fix still in place |
| P06029 | Closing a person's profile returns to the roster WITH both pins | read `app/owner/staff/[id]/page.tsx` → `backToRoster` | ✅ | pass 1 ❌ **F2** — `as` was dropped on the way back (measured: the returned URL had no `as` at all). Guarded: `verify:owner-panel` §4 |
| P06030 | `withRid` produces a bare path when neither pin is present (no stray `?`) | read: `return q ? \`${p}?${q}\` : p` | ✅ | |
| P06031 | `withScope` appends `&scope=` correctly when the path already has a `?` | read: `p.includes("?") ? "&" : "?"` | ✅ | the DELETE path already carries `?id=` |
| P06032 | A 403 "section not enabled" renders the calm card, not the red error banner | read: `if (r.disabled) { setNotEnabled(...); return; }` before `if (r.error)` | ✅ | |
| P06033 | A 503 transient answer renders the red banner WITH a retry button | read: the `err` card carries a "Try again" button | ✅ | |
| P06034 | `load()` always clears `loading`, even when the fetch throws | read: `finally { setLoading(false) }` | ✅ | |
| P06035 | `load()` clears a previous `notEnabled` when a later load succeeds | read: `setNotEnabled(null)` on the success path | ✅ | |
| P06036 | A failed `load()` does not wipe the roster already on screen into an empty list | read: `setRestaurants`/`setStaff` are only called on success | ✅ | last good list stays under the banner |
| P06037 | The one-time password card scrolls itself into view when it appears | read: the `useEffect` on `reveal` calls `scrollIntoView` | ✅ | |
| P06038 | The one-time password field is selected so it can be copied without a mouse | read: the same effect focuses + selects after 250ms | ✅ | |
| P06039 | The reveal effect clears its timer on unmount | read: `return () => clearTimeout(t)` | ✅ | |
| P06040 | The red error banner is brought into view the same way the password card is | read: is there any scroll-into-view for `err`? | ✅ | pass 1 ❌ **F3** — no scroll-into-view existed; measured y = −951px on a 360×780 phone. Guarded: `verify:owner-panel` §2 |
| P06041 | A second immediate click on "Add" cannot create two people | read: the synchronous `addingRef` guard, set before any await | ✅ | |
| P06042 | `addingRef` is released even when the add throws | read: `finally { addingRef.current = false }` | ✅ | |
| P06043 | `addStaff` does not reset the form when the server refuses, so nothing typed is lost | read: `form.reset()` sits after the awaited `call` | ✅ | |
| P06044 | `addStaff` sends only the optional fields that were actually filled | read: `put()` skips empty strings | ✅ | a bare add is the request it always was |
| P06045 | `addStaff` sends `tables` only for the waiter role | read: `role === "tablet" ? (newTables[rid] \|\| []) : undefined` | ✅ | |
| P06046 | `addStaff` refuses locally when a waiter has no table picked, so the server's throw is never the first thing the owner sees | read: the early `setErr` + the disabled Add button | ✅ | both present |
| P06047 | The waiter table picker's "Select all" produces 1…tableCount, in order | read: `Array.from({length: r.tableCount \|\| 0}, (_, k) => k + 1)` | ✅ | |
| P06048 | The waiter table picker keeps its list sorted as tiles are tapped | read: `[...cur, i].sort((a,b) => a-b)` | ✅ | |
| P06049 | Tapping a picked table tile removes exactly that table | read: `cur.filter((x) => x !== i)` | ✅ | |
| P06050 | The table picker is per-restaurant, so two restaurant cards cannot share a pick | read: `newTables` is keyed by `r.id` | ✅ | |
| P06051 | The role select in the Add form is per-restaurant too | read: `newRole` keyed by `r.id` | ✅ | |
| P06052 | A restaurant whose floor size is unknown (0) tells the owner why they cannot add a waiter | read the `tableCount === 0` branch of the picker | ✅ | pass 1 ❌ **F4** — an empty grid plus "Pick at least one table". Now names the restaurant and says the floor could not be read. Guarded: `verify:owner-panel` §6 |
| P06053 | After a successful add, the role select and the table pick are both reset | read: `setNewRole(... "manager")` + `setNewTables(... [])` | ✅ | |
| P06054 | After a successful add, the roster is reloaded so the new person appears | read: `await load()` | ✅ | |
| P06055 | The new password is shown BEFORE the reload, so a slow reload cannot hide it | read: `setReveal(...)` precedes `await load()` | ✅ | |
| P06056 | Copy falls back to select-the-field on an origin without clipboard access | read: `copyPw` catch → `el.focus(); el.select()` | ✅ | the password is never silently lost |
| P06057 | Copy confirms with "Copied!" and clears the confirmation | read: `setTimeout(() => setCopied(false), 1800)` on both paths | ✅ | |
| P06058 | Dismissing the reveal card also clears the "Copied!" state | read: `setReveal(null); setCopied(false)` | ✅ | |
| P06059 | The inline rename editor opens with the CURRENT name and phone, not blank | read: `{ id: s.id, name: s.name \|\| s.username, phone: s.phone \|\| "" }` | ✅ | |
| P06060 | Tapping "Rename / edit phone" on an already-open row closes it (a real toggle) | read: `editing?.id === s.id ? null : {...}` | ✅ | |
| P06061 | `saveEdit` refuses a name under 2 characters before sending | read: `if (name.trim().length < 2)` | ✅ | server also re-checks with a real-character count |
| P06062 | `saveEdit` sends what the row said when the editor opened, so a second person's rename is refused not overwritten | read: the `X-LFH-Expect` header naming `staff_users` + `{name, phone}` | ✅ | |
| P06063 | The expectation names the row's ORIGINAL values (`s.name`), never the draft | read: `fields: { name: s.name ?? "", phone: s.phone ?? "" }` | ✅ | |
| P06064 | The expectation sends `""` rather than `null` for an empty name/phone, matching what the server compares | read `lib/clash.ts` comparison against the same shape | ✅ | |
| P06065 | A refused save shows the plain sentence, not a bare status code | read: `call()` reads `d.clash.plain` + `.todo` | ✅ | |
| P06066 | A refused save reloads the roster so the screen stops showing a value that never landed | read: `catch { ...; await load(); }` in `saveEdit` | ✅ | |
| P06067 | `saveEdit` closes the editor only on success | read: `setEditing(null)` is after the awaited call | ✅ | |
| P06068 | `saveEdit` ignores a stale click for a row that is no longer the one being edited | read: the `!editing \|\| editing.id !== s.id` guard | ✅ | |
| P06069 | "Reset password" asks first, and says the current login stops working | read the `confirm` text | ✅ | |
| P06070 | "Reset password" shows the new password in the reveal card | read: `setReveal({ name, password: d.password })` | ✅ | |
| P06071 | Disabling someone asks first and says they will be logged out immediately | read the `confirm` text | ✅ | |
| P06072 | RE-enabling someone does not ask (it is harmless) | read: `if (!active && !confirm(...))` | ✅ | |
| P06073 | A role change asks first and says they will be logged out | read the `confirm` text | ✅ | |
| P06074 | Cancelling a role change snaps the select back to the person's real role | read: `forceRerender((n) => n + 1)` on cancel | ✅ | the controlled select would otherwise keep the chosen row |
| P06075 | A role change to the SAME role does nothing at all | read: `if (role === s.role) return;` | ✅ | no prompt, no request |
| P06076 | "Remove" asks first and says it cannot be undone | read the `confirm` text | ✅ | |
| P06077 | "Remove" sends the id in the query string, url-encoded | read: `?id=${encodeURIComponent(s.id)}` | ✅ | |
| P06078 | Removing someone with pay history is refused by the server and the reason reaches the screen | read `payHistoryBlocksDelete` + the `err` path | ✅ | reason is shown; visibility of the banner is F3 |
| P06079 | "Add to pay list" only appears for a person who can have a profile | read: `s.profileEligible && !s.payHidden && !s.in_payroll` | ✅ | |
| P06080 | Kitchen rows show no pay-list control and no completeness bar | read: both blocks require `s.profileEligible`, which is false for kitchen | ✅ | deliberate — kitchen has no profile |
| P06081 | A caller who may not see money sees no pay figures at all, not zeroes | read: `s.payHidden` gates every money branch | ✅ | server strips the values too |
| P06082 | A caller who may not EDIT powers sees "not on the pay list" as text, never a button that would be refused | read: `canEditPowers ? <button> : <span>` | ✅ | |
| P06083 | Removing someone from the pay list asks first and says the history stays | read the `confirm` text | ✅ | |
| P06084 | The pay list confirm explains BOTH consequences (stops counting as an expense, no new payments) | read the same sentence | ✅ | |
| P06085 | A person on the pay list with no rate says so, instead of printing ₹0 | read: `s.pay_amount ? ... : <span>on pay list · rate not set</span>` | ✅ | |
| P06086 | When this month's pay could not be read, the row says so instead of naming an amount | read: `s.payUnread ? "couldn't read this month's pay — refresh" : ...` | ✅ | the 2026-08-06 fix is intact |
| P06087 | The rate itself is still shown when `payUnread` is set (it is stored on the person, not summed) | read: the rate renders outside the `payUnread` branch | ✅ | |
| P06088 | An outstanding advance is only shown when there is one | read: `s.advanceOutstanding ? ... : null` | ✅ | |
| P06089 | `money()` rounds and formats in Indian digit grouping | read: `Math.round(...).toLocaleString("en-IN")` | ✅ | |
| P06090 | `money()` renders ₹0 for null/undefined — so every call site must guarantee a real number | read the two call sites against `payUnread` | ✅ | guaranteed by P06086 |
| P06091 | The pay suffix matches the pay type (/mo, /day, /hr) and is blank for per-shift | read the ternary chain | ✅ | per-shift shows the bare amount |
| P06092 | The completeness bar's width cannot divide by zero | read: `Math.max(1, s.completeness.total)` | ✅ | |
| P06093 | The completeness bar says "complete" only when filled === total | read the ternary | ✅ | |
| P06094 | The completeness bar is a link to that person's profile, with both pins | read: `href={withRid(...)}` | ✅ | |
| P06095 | The completeness link has a title that explains the numbers | read the `title` attribute | ✅ | |
| P06096 | The role badge says "waiter", never the storage word "tablet" | read: `s.role === "tablet" ? "waiter" : s.role` | ✅ | |
| P06097 | The role SELECT also says "waiter" for the tablet value | read the `ROLES.map` option label | ✅ | |
| P06098 | The Add form's role select says "waiter" too | read the second `ROLES.map` | ✅ | all three agree |
| P06099 | The roster never offers "owner" as a role to create or change to | read: `ROLES = ["manager","kitchen","tablet"]` | ✅ | server also refuses |
| P06100 | "Open manager/waiter panel" is shown only for the two roles that HAVE a panel to open | read: `(s.role === "manager" \|\| s.role === "tablet")` | ✅ | |
| P06101 | "Open manager/waiter panel" opens in a new tab so the owner keeps their place | read: `target="_blank" rel="noopener"` | ✅ | |
| P06102 | Its title admits it opens with the owner's OWN access, not that person's view | read the `title` attribute | ✅ | the 2026-08-05 correction is intact |
| P06103 | "Open profile" is shown only for a person who has a profile | read: `s.profileEligible &&` | ✅ | |
| P06104 | Every action control is disabled while a request is in flight | read: `disabled={busy}` on each `ost-mini`, the select and the Add button | ✅ | 6 controls + select |
| P06105 | `busy` is always cleared, even when the request throws | read: `finally { setBusy(false) }` in `call()` | ✅ | |
| P06106 | `call()` always sends JSON content-type and lets a caller add headers | read: the spread order `{ "Content-Type": ..., ...(init.headers) }` | ✅ | |
| P06107 | `call()` tolerates a non-JSON error body | read: `.json().catch(() => ({}))` | ✅ | |
| P06108 | A failed `call()` throws with the server's own sentence when there is one | read: `d.error \|\| \`Request failed (${r.status})\`` | ✅ | |
| P06109 | The disabled-person row is visually dimmed AND labelled "disabled" (never colour alone) | read: `.ost-row.off` + the `ost-disabled` span | ✅ | |
| P06110 | "Remove" is coloured as dangerous without needing a hover | read: `.ost-mini.danger` sets border+colour unconditionally | ✅ | the 2026-08-05 fix is intact |
| P06111 | The team count on the tab counts ACTIVE people only | read: `staff.filter((s) => s.active).length` | ✅ | |
| P06112 | The per-restaurant header count counts everyone in that restaurant | read: `team.length` | ✅ | deliberately different from the tab count |
| P06113 | An empty restaurant says "No staff yet — add the first below" | read the `team.length === 0` branch | ✅ | |
| P06114 | An owner with no restaurants is told to ask the admin, not that a feature is off | read the `restaurants.length === 0` branch | ✅ | |
| P06115 | The "not enabled" card and the "no restaurants" card cannot both show | read: `!notEnabled &&` on the second | ✅ | |
| P06116 | The restaurant cards are hidden while the "not enabled" card shows | read: `{!notEnabled && restaurants.map(...)}` | ✅ | |
| P06117 | Each restaurant card carries its OWN accent colour, never restaurant #1's | read: `style={{ "--rcol": r.accentColor }}` per card | ✅ | |
| P06118 | The accent stripe falls back to the console accent when a restaurant has no colour | read: `var(--rcol, var(--accent))` | ✅ | server also defaults to `#e3c06f` |
| P06119 | The crumb says "Team & pay" only where pay genuinely exists | read: `restaurants.some((r) => r.modules?.payroll)` | ✅ | the T19 correction is intact |
| P06120 | The crumb never says "Staff & powers" (a screen that no longer exists) | grep the file for that string | ✅ | absent |
| P06121 | The "Add their details now" block only renders where the payroll module is on | read: `r.modules?.payroll &&` | ✅ | |
| P06122 | Pay type + amount inside that block only render where this caller may set pay | read: `r.payAccess?.canEditJobPay &&` | ✅ | |
| P06123 | Phone is asked for on the ALWAYS-visible Add row, not inside the pay block | read: the `name="phone"` input's position | ✅ | the T19 fix is intact |
| P06124 | There is exactly ONE input named `phone` in the Add form, so a payroll-on form cannot submit two | grep the form for `name="phone"` | ✅ | one |
| P06125 | Every Add-form field name matches a key the server reads | read the form names against `postImpl`'s body reads | ✅ | name, role, password, phone, full_name, designation, joined_on, pay_type, pay_amount |
| P06126 | `full_name` is sent nested under `profile`, which is where the server expects it | read: `opt.profile = { full_name: fullName }` vs `mergeProfilePatch` | ✅ | |
| P06127 | The joining-date input is a real date field so the phone shows a date wheel | read: `type="date"` | ✅ | |
| P06128 | The pay-amount input asks for a numeric keypad on a phone | read: `inputMode="numeric"` | ✅ | |
| P06129 | The phone input asks for a telephone keypad | read: `inputMode="tel"` | ✅ | |
| P06130 | The username input caps at 80 characters, matching the server's slice | read: `maxLength={80}` vs `.slice(0, 80)` | ✅ | |
| P06131 | The inline rename input also caps at 80 | read: `maxLength={80}` | ✅ | |
| P06132 | The inline PHONE input caps at the server's 20 characters | read: `maxLength` on the phone inputs vs `.slice(0, 20)` | ✅ | pass 1 ❌ **F5** — neither phone input had a cap, so a longer number was accepted and cut to 20 on save with nothing said. Guarded: `verify:owner-panel` §7 |
| P06133 | Autocomplete is switched off on the username/password/phone inputs so a browser cannot fill a staff login with the owner's own | read: `autoComplete="off"` on each | ✅ | |
| P06134 | The password input on the Add form is not `type="password"`, so the owner can read what they are setting for someone else | read the input | ✅ | deliberate — it is a password they must hand over |
| P06135 | The Add form's password field says blank means auto-generate | read the placeholder | ✅ | |
| P06136 | The Add form does not tell the owner the 6-character minimum the server enforces | read the placeholder vs `postImpl`'s `password.length < 6` | ✅ | pass 1 ❌ **F6** — the 6-character minimum was only enforced after a round trip. Guarded: `verify:owner-panel` §7 |
| P06137 | The Add form submits through a real `<form>` so Enter works | read: `<form onSubmit=...>` with `type="submit"` | ✅ | |
| P06138 | The Add form calls `preventDefault` so the page never reloads | read: `e.preventDefault()` | ✅ | |
| P06139 | `addStaff` reads the form from `e.currentTarget`, not a captured ref that could go stale | read the handler | ✅ | |
| P06140 | The Add button's disabled tooltip explains WHY it is disabled | read the `title` attribute | ✅ | |
| P06141 | The waiter table warning is a sentence, not a colour | read the `.ost-tables-warn` text | ✅ | |
| P06142 | The picked-count line always states the denominator | read: `{n} of {r.tableCount \|\| 0} picked` | ✅ | reads "0 of 0" when the floor is unknown — see F4 |
| P06143 | The table grid scrolls rather than pushing the Add button off the card on a big floor | read: `.ost-tgrid { max-height: 190px; overflow-y: auto }` | ✅ | |
| P06144 | The table grid's tiles are at least 36px tall (a real tap target) | read: `.ost-tgrid button { min-height: 36px }` | ✅ | |
| P06145 | A picked table tile is marked with a ✓ as well as a colour | read: `{on ? "✓ " : ""}T{i}` | ✅ | never colour-only |
| P06146 | The table tiles are `type="button"` so they cannot submit the Add form | read each tile + the two header buttons | ✅ | all three |
| P06147 | The action row is a real full-width line on a phone, not a squeezed wrap | read: the `max-width: 560px` block | ✅ | |
| P06148 | The role select shares that full-width treatment on a phone | read: `.ost-actions select { flex: 1 1 auto }` | ✅ | |
| P06149 | `ROLES` and the server's assignable list agree exactly | read `ROLES` vs `ASSIGNABLE` in the route | ✅ | manager, kitchen, tablet |
| P06150 | The page holds no private copy of any permission list | grep the file for a caps/permissions array | ✅ | deleted 2026-08-04; comment records why |
| P06151 | The page writes no permission of any kind | grep for `set_permissions` in the page | ✅ | none — the admin owns permissions |
| P06152 | `canEditPowers` is derived from the SERVER's `actor`, never from a cookie the browser could set | read: `actor === "owner" \|\| actor === "admin"` where `actor` comes from the response | ✅ | |
| P06153 | The dead `tab` state can never be anything but "team" | read: `const [tab] = useState(() => "team")` | ✅ | true, and the powers branch is gone |
| P06154 | The `?focus=` deep link handler is genuinely gone (it could never fire) | grep the file for `focus` | ✅ | removed in T19; only the comment remains |
| P06155 | The roster's CSS styles only things that exist — no rule left from the removed Powers tab | grep the class names in the `<style jsx>` block against the JSX | ✅ | **FIXED 2026-08-19** on his "fix all": the twelve dead rules are deleted and a dated note stands where they were. Re-run by grepping `ost-perm\|reach-chip\|reach-legend` in the style block — one hit (the note) is correct, more than one is a regression. Guarded: `verify:owner-panel` §11 |
| P06156 | `app/owner/staff/[id]/page.tsx` renders the SHARED profile component, not a second layout | read: `<StaffProfile userId={id} host={host} .../>` | ✅ | docs/STAFF-PROFILE.md's one shape |
| P06157 | The person page holds no permission list of its own | read the whole 38-line file | ✅ | none |
| P06158 | The person page passes the restaurant pin into the host so an admin stays in the right restaurant | read: `ownerProfileHost(id, rid, as)` | ✅ | |
| P06159 | The host memo re-builds when any of id / rid / as changes | read: `useMemo(..., [id, rid, as])` | ✅ | |
| P06160 | `backToRoster` is stable across renders so the profile's back layer is not re-registered every paint | read: `useCallback(..., [router, rid])` | ✅ | |
| P06161 | The person page renders a plain message when no id is in the URL | read: `if (!id) return <div className="adm-empty">` | ✅ | |
| P06162 | The person page renders NOTHING else, so closing the sheet cannot leave a half-page behind | read the return | ✅ | deliberate |
| P06163 | The person page does not fetch anything itself (the host owns every request) | grep for `fetch` | ✅ | none |
| P06164 | The person page coerces a missing route param to a string safely | read: `String(params?.id \|\| "")` | ✅ | |
| P06165 | `app/owner/settings/page.tsx` reads the restaurant pin from the URL exactly once, on mount | read: `useState(() => ...searchParams.get("rid"))` | ✅ | |
| P06166 | The settings page guards `window` for the server render | read: `typeof window === "undefined" ? null : ...` | ✅ | |
| P06167 | The settings page appends the chosen-owner pin to its API calls | read: `asSuffix()` inside `scp` | ✅ | |
| P06168 | The settings page's POST uses the same scoped path as its GET | read: both use `\`/api/owner/settings${scp}\`` | ✅ | |
| P06169 | A failed settings load shows a retry button, not a dead end | read the `err` card | ✅ | |
| P06170 | A failed settings load still lets the owner change the skin | read: the Appearance card renders outside the `data` guard | ✅ | |
| P06171 | The skin buttons report their state to a screen reader | read: `aria-pressed={skin === "light"}` on both | ✅ | |
| P06172 | The skin buttons are distinguished by more than colour | read: each has its own sun/moon icon and label | ✅ | |
| P06173 | Changing the skin writes BOTH the localStorage key and the cookie the server reads | read: `applySkin` writes `aevidine_skin` twice | ✅ | so SSR paints the right theme next time |
| P06174 | The skin cookie is set with a path and a max-age, so it survives a reload | read: `path=/; max-age=31536000; samesite=lax` | ✅ | |
| P06175 | The skin write is wrapped so a storage refusal cannot break the page | read: both writes are in `try {} catch {}` | ✅ | |
| P06176 | The settings page reads the stored skin on mount so the buttons show the truth | read the `useEffect` reading localStorage | ✅ | |
| P06177 | The stored skin is validated before use (never an arbitrary string) | read: `if (s === "light" \|\| s === "dark")` | ✅ | |
| P06178 | Changing the skin reloads once so the whole shell repaints | read: `location.reload()` + the comment | ✅ | deliberate |
| P06179 | The reload happens AFTER both writes, so the new skin survives it | read the order in `applySkin` | ✅ | |
| P06180 | Password change refuses a mismatch before sending anything | read: `if (nw !== cf)` | ✅ | |
| P06181 | Password change refuses under 6 characters before sending, matching the server | read: `if (nw.length < 6)` vs the route's own check | ✅ | the two agree |
| P06182 | The client does NOT pre-check "new must differ from current", which the server refuses | read the client checks vs the route's `next === current` | ✅ | the server's sentence is shown; the form is right next to the banner here |
| P06183 | Password change clears both messages before a new attempt | read: `setPwErr(null); setPwMsg(null)` | ✅ | |
| P06184 | The submit button is disabled while the change is in flight | read: `disabled={pwBusy}` and its "Saving…" label | ✅ | |
| P06185 | `pwBusy` is cleared even when the request throws | read: `finally { setPwBusy(false) }` | ✅ | |
| P06186 | The three password fields are cleared on success so they are not left on a shared screen | read: `setCur(""); setNw(""); setCf("")` | ✅ | |
| P06187 | The owner is told they will be signed out BEFORE it happens | read the card's sub-line and the success message | ✅ | both say it |
| P06188 | The sign-out redirect gives the owner time to read the message | read: `setTimeout(..., 1600)` | ✅ | |
| P06189 | Password change is only offered when the server says this caller can do it | read: `data?.canChangePassword &&` | ✅ | the admin act-as has no password row here |
| P06190 | The password inputs carry the right autocomplete tokens so a manager can save the new one | read: `current-password` / `new-password` ×2 | ✅ | |
| P06191 | A non-JSON error reply on password change still produces a readable message | read: `.json().catch(() => ({}))` then `j.error \|\| ...` | ✅ | |
| P06192 | "What's enabled" treats an absent section key as ON, matching the server's own rule | read: `data.sections[k] !== false` vs `mergeOwnerEntitlements` | ✅ | absent = on, both sides |
| P06193 | The "What's enabled" card never reveals a section the admin withheld | read `SECTION_LABEL` against `OWNER_SECTION_KEYS` | ✅ | **REVERSED by the owner, 2026-08-18** (R31): *"owner can't know which option are not given to them only admin should know that"*. Pass 1 raised the missing chips as **F8**; the disclosure half is REJECTED. The card now lists ON only — no ✗, no greyed chip, no count. Guarded: `verify:owner-panel` §3 |
| P06194 | Every section the owner DOES have is listed, named as the sidebar names it | read `SECTION_LABEL` against `OwnerShell`'s nav labels and each page's `<h1>` | ✅ | the half that survives R31 — the map held 6 of 9, so a restaurant with Menu / Audit & logs / Manager mode switched ON did not see them on a card claiming to list what is on. Two names were also retired screens |
| P06195 | An off section is marked with a ✗ icon, not colour alone | read: `fas ${on ? "fa-check" : "fa-xmark"}` | ✅ | |
| P06196 | The "Your restaurants" card is hidden when there are none, rather than showing an empty box | read: `data.restaurants.length > 0 &&` | ✅ | |
| P06197 | The "Your restaurants" card states the count | read: `· {data.restaurants.length}` | ✅ | |
| P06198 | The settings page renders no feature toggles (owners configure no features) | grep for the `modules` field being rendered | ✅ | answered by the API, deliberately unrendered |
| P06199 | The settings page tells the owner where taxes/branding/billing are decided | read the page sub-line | ✅ | "managed for you by Aevidine" |
| P06200 | No file in this territory prints, logs or echoes a secret, a token or a cookie value | grep all four files for `console.` and for cookie/token names | ✅ | one `document.cookie` write, no reads |

## Band B · Conformance to the project's own rules (P06201–P06300)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P06201 | ONE profile shape: nothing in this territory renders a second person-profile layout | grep the four files for a tabbed/person layout; only `[id]/page.tsx` mounts `StaffProfile` | ✅ | docs/STAFF-PROFILE.md honoured |
| P06202 | `PROFILE_ROLES` decides who has a profile — the roster never decides for itself | read: the roster branches on the server's `profileEligible`, never on `s.role` | ✅ | |
| P06203 | KITCHEN has no profile and the roster does not try to give it one | read every `profileEligible`-gated block | ✅ | deliberate, ruled three times — not a finding |
| P06204 | The roster does not "fix" the blank kitchen rows with a completeness bar | read: the bar is inside the `profileEligible` guard | ✅ | |
| P06205 | One permission list: the territory sends no permission key of its own | grep for `permissions` writes in all four files | ✅ | none |
| P06206 | Unknown permission keys are REFUSED by the server, and nothing here can invent one | read `set_permissions`'s `capByKey` allow-list | ✅ | the roster never posts it |
| P06207 | No silent overwrites: the roster's value edit sends `X-LFH-Expect` | read `saveEdit` | ✅ | |
| P06208 | No silent overwrites: the person profile's writes send `X-LFH-Expect` | read `components/owner/ownerProfileHost.ts` `patch()` | ✅ | outside my territory but on my path |
| P06209 | `npm run verify:owner-clash` passes (static half) | run it | ✅ | 11/11 static, and 15/15 with `--base http://localhost:4113` — it restored the designation it changed |
| P06210 | `npm run verify:owner-clash` still names files that exist after my changes | run it after every commit | ✅ | it greps `app/owner/staff/page.tsx` for `X-LFH-Expect` |
| P06211 | A first-save-wins refusal is actually VISIBLE on screen, and headed honestly | read both `catch` paths | ✅ | pass 1 ❌ **F7** (highest) — the sentence was erased by the reload that followed it. Fixed, and since 2026-08-18 it is headed "Someone got there first." in amber rather than "Something went wrong.". Guarded: `verify:owner-panel` §1 and §9 |
| P06212 | Transition actions (reset / disable / role) deliberately send NO expectation | read the route's own note plus the roster's calls | ✅ | each is idempotent by its own handler |
| P06213 | A menu edit needs a panel save to bust the cache — nothing in `/owner/menu` claims otherwise | read the page + `OwnerMenuEditor`'s comment | ✅ | the embedded editor owns the save |
| P06214 | Each Edit-menu sub-option reaches real code — `/owner/menu` adds no switch of its own | read the page: it renders only the picker + the embed | ✅ | no owner-side menu switches exist here to go dead |
| P06215 | Open-price dishes: nothing in this territory is an order entry point | grep the four files for order/price entry | ✅ | none — not applicable here |
| P06216 | Allergy lists: nothing in this territory renders an allergy list | grep for allergy | ✅ | none — not applicable here |
| P06217 | Owners control no feature entitlement — the settings page renders no feature toggle | read the removed-block comment + the JSX | ✅ | deliberate |
| P06218 | Per-dish discount is absent, and nothing here hints at it | grep for discount | ✅ | deliberately not built |
| P06219 | Every list in this territory says WHERE it lives — the "not enabled" cards name who to ask | read all three "not enabled" sentences | ✅ | each names Aevidine / the administrator |
| P06220 | Feature switches: the roster reads `r.modules?.payroll`, never a `settings` column directly | read the page | ✅ | |
| P06221 | Egress: every read this territory triggers is scoped by restaurant id | read `/owner/menu`'s two reads and the API calls the client makes | ✅ | `.in("id", ids)` / `.eq("id", rid)` |
| P06222 | Egress: `/owner/menu` names its columns and never selects `*` | read both reads | ✅ | |
| P06223 | Egress: `/owner/menu` reads the restaurants table TWICE for one page | count the reads: `entitledSubset` reads it, then the page reads it again | ✅ | pass 1 ❌ — two reads of `restaurants` for one page; folded into F1's fix, now one read answers both the entitlement and the name |
| P06224 | Egress: the roster polls nothing — it loads once and on demand | grep for `setInterval` / a poll | ✅ | no timer |
| P06225 | Egress: the settings page loads once per mount | read the `useEffect` dependency (`load`, memoised on `scp`) | ✅ | |
| P06226 | Egress: no read in this territory is unbounded — the roster's list is capped server-side | read the route's `.limit(2000)` | ✅ | |
| P06227 | Offline: every staff write goes through a route wrapped in `withIdempotency` | read the route's exports | ✅ | POST/PATCH/DELETE all wrapped |
| P06228 | The owner console is not one of the panels with a write outbox, so a plain `fetch` here is correct | read `docs/OFFLINE-SYNC.md` for which surfaces queue | ✅ | verified in `docs/OFFLINE-SYNC.md` (line 185): the staff outbox is `public/panels/outbox.js`, wrapping the manager / kitchen / tablet panels' `api()`. The owner console is not wrapped, so a bare `fetch` is right |
| P06229 | Whether `/api/owner/*` reads are remembered for offline, and what that means for this territory | grep `public/sw.js` for `owner` | ✅ | **pass 1 got this WRONG.** I wrote "grep public/sw.js for owner — consistent" without grepping. `/^\/api\/owner\//` IS in `DATA_PATHS` (public/sw.js:136), so the owner's READS are cached and the roster can open from a saved copy. That is deliberate (the panel labels its state through `ConnectionBadge`), and my three screens add no write path that would need queueing. Re-run this row by grepping `DATA_PATHS`, never from memory |
| P06230 | A tap never vanishes in silence: every button in the roster either acts, refuses visibly, or is disabled | walk all 11 controls | ✅ | pass 1 ❌ **F3** — the refusal existed but rendered above the fold |
| P06231 | A tap never vanishes in silence: no handler in this territory returns early without telling anyone | read every early `return` | ✅ | each sets `err`, opens a confirm, or is a no-op by design (same-role) |
| P06232 | A tap never vanishes in silence: no promise is left unresolved | read every `async` handler for a missing await | ✅ | |
| P06233 | A tap never vanishes in silence: the client branches on server reason CODES/flags, not prose | read: `r.disabled`, `d.clash`, `r.status` — never a string match | ✅ | |
| P06234 | The person profile registers a phone-Back layer the moment it is built | read `components/admin/StaffProfile` for `useBackClose` / `LFH_BACK` | ✅ | registered — but NOT the way I first wrote it. It is `useAdminModal(dialogRef, "admin-staff-profile", onClose)` in `components/admin/StaffProfile.tsx:194`, which calls `useBackClose` for us; the file contains neither `useBackClose` nor `LFH_BACK` directly. Corrected 2026-08-18 after checking. See P06366 for what that layer actually DOES on a phone |
| P06235 | Mobile back button: the roster opens no overlay of its own, so it needs no registration | read the page — the rename editor is inline, not an overlay | ✅ | |
| P06236 | Mobile back button: the settings page opens no overlay | read the page | ✅ | |
| P06237 | Mobile back button: `/owner/menu`'s embedded frame is mounted imperatively so it adds no history entry | read `useEmbedFrame`'s comment | ✅ | the 2026-08-04 fix |
| P06238 | No new column on `settings` is implied by anything in this territory | grep for a settings write | ✅ | none |
| P06239 | Light mode: the owner console's key is `aevidine_skin`, and this territory uses only that key | grep all four files for `lfh_theme` / `lfh_panel_theme` | ✅ | only `aevidine_skin` |
| P06240 | Light mode: the settings page's toggle and OwnerShell's toggle write the same key the same way | compare `applySkin` with `OwnerShell.toggleSkin` | ✅ | both write localStorage + cookie |
| P06241 | Light mode: `/owner/menu` hands the born skin to the frame and lets live changes travel by message | read `OwnerMenuEditor` | ✅ | one writer |
| P06242 | Dark is the default in this territory and nothing forces light | read `/owner/menu`'s cookie default and the settings page's `useState("dark")` | ✅ | |
| P06243 | Every restaurant is genuinely different: no branding, colour or name in this territory is hard-coded to one tenant | grep the four files for a restaurant name/slug | ✅ | none |
| P06244 | Admin = top power, invisibly: nothing in this territory links to `/aevinite` | grep for `/aevinite` | ✅ | none |
| P06245 | Admin = top power, invisibly: the roster gives the owner no hint that an admin can act as them | read the page for act-as wording | ✅ | none |
| P06246 | Admin view MARKS, never strips: the roster shows the admin the same controls it shows the owner | read: `canEditPowers` includes `admin` | ✅ | |
| P06247 | Access model v2: no toggle exists in this territory that the owner did not ask for | read the three pages against `lib/accessTree.ts` | ✅ | the skin toggle is the only switch, and it is a device preference |
| P06248 | Access model v2: hiding is never the only guard — `/owner/menu`'s section check is server-side | read the page | ✅ | |
| P06249 | Access model v2: the settings page's password change is refused server-side when Settings is off, not just hidden | read the route's POST gate | ✅ | |
| P06250 | Access model v2: the roster's "section off" answer comes from the server, not from a client-side entitlement read | read `load()` | ✅ | |
| P06251 | The retired 4-rung ladder leaves no trace in this territory | grep for `power_` and for `manager-permissions` | ✅ | only historical comments |
| P06252 | The Powers tab is genuinely gone from the roster (owner, 2026-07-31) | read the JSX for a second tab | ✅ | one tab only |
| P06253 | No greyed-out ghosts: the roster renders no disabled-looking permission control | read the JSX | ✅ | the whole block was deleted |
| P06254 | A manager who reaches this API from the editor panel cannot flip a power toggle here | read: this page is owner/admin-only and renders no toggle at all | ✅ | |
| P06255 | Least privilege: the roster offers no control the server would refuse for this actor | cross-read each control against the route's actor checks | ✅ | `canEditPowers` and `payAccess` gate the rest |
| P06256 | A sale can never disappear: nothing in this territory touches a bill | grep for bill/invoice/order | ✅ | none |
| P06257 | Compliance: the pay ledger is append-only and this territory offers no delete | read the roster's controls | ✅ | only add/remove from the pay LIST, which keeps history |
| P06258 | Compliance: removing someone from the pay list says the history stays | read the confirm sentence | ✅ | |
| P06259 | Compliance: a person with pay history cannot be deleted, and the roster surfaces the refusal | read the route's 409 + the roster's error path | ✅ | |
| P06260 | Audit: every write this territory triggers is logged server-side | read the route's `logAction` calls per action | ✅ | create, reset, enable/disable, role, rename, payroll add/remove |
| P06261 | Audit: a rename is logged with BOTH names (it changes the login) | read the `staff_rename` log | ✅ | |
| P06262 | Audit: the owner's own password change is logged | read the settings route's `logAction("owner","password_change")` | ✅ | |
| P06263 | Audit: an admin acting through these screens is logged to the ADMIN panel, not the owner's feed | read `logPanel(s)` / `ownerLogPanel(scope)` | ✅ | |
| P06264 | The Activity log reads as English: the detail strings this territory triggers are sentences | read each `detail:` template | ✅ | e.g. `login name "x" → "y"` |
| P06265 | Rate limits: nothing in this territory can be made to sign in repeatedly | grep for `panel-login` / `staff-login` | ✅ | none |
| P06266 | Rate limits: the owner's own password change is counted per account before the check | read the route's `rateAllowed("password_change", owner.id)` | ✅ | |
| P06267 | Rate limits: a wrong current password does not reset the counter | read: `rateResetOnSuccess` runs only after a successful verify | ✅ | |
| P06268 | Tests cannot raise a real alert: my runtime phases sign in once through the cached helper | read my own harness script | ✅ | `loginAs()` only |
| P06269 | Tests clean up: every row my phases create is deleted by id in the same run | read my harness's teardown | ✅ | |
| P06270 | Aangan is read-only in my run: no phase writes to it | read my harness's restaurant selection | ✅ | French House only |
| P06271 | AV LIVE is not referenced anywhere in my work | grep my diff and my scripts for the live stack's names | ✅ | none |
| P06272 | Typecheck is a separate gate from lint, and both are run | run `npm run typecheck` and `npm run lint` | ✅ | `npm run typecheck` clean; `npm run lint` 0 errors (444 pre-existing warnings, all in `scripts/`, none from my changes) |
| P06273 | `verify:menu-parts` passes | run it | ✅ | |
| P06274 | `verify:dish-counts` passes | run it | ✅ | |
| P06275 | `verify:personal-data` passes | run it | ✅ | |
| P06276 | `verify:staff-accounts` passes | run it | ✅ | |
| P06277 | `verify:everything` is NOT run from this terminal | read my own command history | ✅ | it belongs to the merge terminal |
| P06278 | Nothing in this territory hand-adds a `-webkit-` prefix to a backdrop filter | grep for `backdrop-filter` | ✅ | none |
| P06279 | Charts: nothing in this territory renders a chart | grep for chart/plot | ✅ | the completeness bar is a progress meter, not a chart |
| P06280 | The completeness meter is not a lonely one-bar plot pretending to be analytics | read it: it is a per-person progress bar with a number beside it | ✅ | |
| P06281 | styled-jsx: every selector in the roster's style block can reach an element | grep each selector against the JSX | ✅ | **FIXED 2026-08-19** — same removal as P06155. Verified after it that the roster still renders styled in both skins (rows kept their radius, badges their washes), because nothing referenced those names |
| P06282 | styled-jsx: the one rule that must reach the SHELL uses `:global(...)` | read: `:global([data-skin="light"]) .ost-rolebadge` | ✅ | correct — the skin attribute is on `html` |
| P06283 | styled-jsx: the settings page uses inline styles, not a scoped block that could not reach the shell | read the file | ✅ | consistent with the other owner pages |
| P06284 | No fixed colour is used where a token exists — check BOTH skins | grep for `#` hex literals in all four files | ✅ | every hex is a fallback behind a declared token, except `--fg` which is undeclared BY DESIGN and always has a working fallback |
| P06285 | `var(--x, fallback)` — every token used without a fallback IS declared for the owner console | grep each `var(--…)` against `app/globals.css`'s `.adm.owx` blocks | ✅ | accent, text, muted, muted2, card, border, bg, adm-ok/warn/danger, accent-on, own-cta all declared in both skins |
| P06286 | `--fg` is undeclared everywhere, so every use must carry a fallback | grep the six uses | ✅ | all six do (`#888` for a wash, `inherit` for text) |
| P06287 | A half-written CSS comment cannot eat the next rule | read every `/* */` in the style block for balance | ✅ | balanced |
| P06288 | `cqw`/container units are not used where there is no container | grep for `cqw`/`cqi` | ✅ | none |
| P06289 | `offsetParent` is not used to measure anything | grep | ✅ | none |
| P06290 | CRLF files are not disturbed — check the line endings of the four files before and after editing | `file`/`git diff --stat` on each | ✅ | all four are LF |
| P06291 | The territory adds no new popup or drawer, so nothing needs registering in the back manager | read the diff | ✅ | |
| P06292 | The territory adds no migration | read the diff | ✅ | none |
| P06293 | The territory adds no new screen, module or permission | read the diff | ✅ | none |
| P06294 | Nothing I changed is listed in `docs/REJECTED-IDEAS.md` | read that file against every fix and improvement | ✅ | checked item by item |
| P06295 | Nothing I changed contradicts a rule in `CLAUDE.md` | re-read the operational rules against my diff | ✅ | |
| P06296 | Every rejection recorded in the code near my edits is left intact | grep my touched files for `REJECTED (owner` | ✅ | none in these four files |
| P06297 | `npm run verify:rejected` still passes after my edits | run it | ✅ | |
| P06298 | `npm run verify:no-ask` still passes (I changed no settings file and no CLAUDE.md) | run it | ✅ | |
| P06299 | My branch never touches `main`, never deploys, never takes the deploy lock | read my own command history | ✅ | |
| P06300 | Every file I wrote to is inside my declared territory | `git diff --name-only origin/main` against the territory list | ✅ | plus the ledger/findings files and the guard §7 requires |

## Band C · Watching it run — headless Playwright on port 4113 (P06301–P06375)

Signed in ONCE as the diag owner (`loginAs`, shared disk cache) at French House
(`My Little French House`, 30 tables, payroll module ON). Aangan is never written to.
Every row my phases create is deleted by id in the same run.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P06301 | Port 4113 is serving MY worktree, not the shared folder | `lsof -p <pid>` cwd equals `sweep6/T13` | ✅ | proven before any screenshot was trusted |
| P06302 | `/owner/staff` answers 200 for a signed-in owner | headless goto, check the response status | ✅ | |
| P06303 | `/owner/staff` paints the restaurant card with the real restaurant's name | assert the rendered text of `.ost-name` | ✅ | "My Little French House" |
| P06304 | `/owner/staff` shows the slug and the staff count under the name | assert the rendered `adm-muted` line | ✅ | `french-house · N staff` |
| P06305 | The Team tab count equals the number of ACTIVE people the API returned | compare the rendered `.ost-tcount` with the API's active count | ✅ | |
| P06306 | Every person the API returned has a row on screen | count `.ost-row` against the API's staff length | ✅ | |
| P06307 | Kitchen rows render with NO completeness bar and NO pay chip | assert `.ost-prog` / `.ost-nopay` are absent inside a kitchen row | ✅ | deliberate |
| P06308 | Kitchen rows render with no "Open profile" link | assert `.ost-mini.open` is absent in a kitchen row | ✅ | |
| P06309 | A manager row renders the "waiter"/"manager" badge from `data-role` | assert the badge's rendered text and its `data-role` | ✅ | |
| P06310 | A tablet row's badge reads "waiter", never "tablet" | assert the rendered text | ✅ | |
| P06311 | A tablet row's role select shows "waiter" as the selected option's TEXT | read the selected `<option>`'s rendered label | ✅ | value stays `tablet` |
| P06312 | "Open profile" points at `/owner/staff/<id>` for that person | assert the `href` | ✅ | |
| P06313 | "Open manager panel" appears on manager rows and points at `/manager` | assert `href` + `target="_blank"` | ✅ | |
| P06314 | "Open waiter panel" appears on tablet rows and points at `/tablet` | assert the `href` | ✅ | |
| P06315 | The completeness bar's filled width matches the API's filled/total | read the inner `<i>`'s computed width against the ratio | ✅ | |
| P06316 | A person on the pay list with a rate renders the rate and "paid this month" | assert the rendered text | ✅ | |
| P06317 | A person on the pay list with NO rate renders "on pay list · rate not set" | assert the rendered text | ✅ | |
| P06318 | A profile-eligible person NOT on the pay list renders the "Add to pay list" button | assert the button exists and is enabled | ✅ | |
| P06319 | "Remove" is visibly red WITHOUT hovering | read the computed `border-color`/`color` of `.ost-mini.danger` with no pointer over it | ✅ | not the same as its neighbour |
| P06320 | "Disable" and "Remove" are not the same colour | compare the two computed colours | ✅ | |
| P06321 | The inline rename editor opens on tap and pre-fills the current name | click, then read both input values | ✅ | |
| P06322 | The inline rename editor closes on a second tap of the same button | click twice, assert `.ost-editrow` is gone | ✅ | |
| P06323 | "Cancel" closes the rename editor without sending anything | click Cancel with the network watched | ✅ | zero requests |
| P06324 | A rename under 2 characters is refused locally, with a message, and sends nothing | type "a", Save, watch the network | ✅ | message shown, no request |
| P06325 | A real rename saves and the roster shows the new name | rename a row I created, assert the new text, then restore it | ✅ | restored in the same run |
| P06326 | A rename sends the `X-LFH-Expect` header | intercept the request and read its headers | ✅ | |
| P06327 | A stale rename (a value changed underneath) is REFUSED and the plain sentence appears | change the row through the API, then Save from the stale editor | ✅ | 409 + sentence shown |
| P06328 | After a refusal the roster reloads and shows the value that really landed | assert the rendered name after the refusal | ✅ | |
| P06329 | Switching the Add form's role to "waiter" reveals the table picker | select `tablet`, assert `.ost-tables` appears | ✅ | |
| P06330 | The table picker renders exactly `tableCount` tiles | count `.ost-tgrid button` against the API's 30 | ✅ | 30 |
| P06331 | The Add button is DISABLED while the waiter role is chosen and no table is picked | read the button's disabled state | ✅ | |
| P06332 | The Add button's title explains why it is disabled | read the `title` attribute in the live DOM | ✅ | |
| P06333 | "Select all" picks every tile and enables the Add button | click it, count `.on` tiles, read the button state | ✅ | 30 of 30 |
| P06334 | "Clear" un-picks every tile and disables the Add button again | click it, re-read both | ✅ | |
| P06335 | Tapping one tile toggles only that tile | click T5 twice, assert the count goes 1 → 0 | ✅ | |
| P06336 | The picked-count line updates as tiles are tapped | read the rendered "N of 30 picked" | ✅ | |
| P06337 | Switching the role back to "manager" hides the picker | select `manager`, assert `.ost-tables` is gone | ✅ | |
| P06338 | Adding a manager with only a username works and reveals a one-time password | submit, assert `.ost-reveal` and a non-empty `.ost-pw` | ✅ | created row deleted at the end of the run |
| P06339 | The reveal card scrolls itself into view | after the add, read the card's bounding box against the viewport | ✅ | |
| P06340 | The one-time password field is focused and selected after the add | read `document.activeElement` and the selection | ✅ | |
| P06341 | "Copy" changes to "Copied!" | click it, read the button text | ✅ | |
| P06342 | "Done" dismisses the reveal card | click it, assert `.ost-reveal` is gone | ✅ | |
| P06343 | The newly added person appears in the roster without a manual reload | count `.ost-row` before and after | ✅ | |
| P06344 | The Add form is cleared after a successful add | read every input's value | ✅ | |
| P06345 | Adding a person with a 3-character password is refused, and the owner can SEE the refusal | submit from the bottom of the page, then check whether the message is inside the viewport | ✅ | pass 1 ❌ **F3 + F6** — refused correctly, invisible on a phone. Re-measured after the fix: the banner sits fully inside the 780px viewport |
| P06346 | Adding a duplicate username is refused with the friendly sentence, never a raw database message | submit a name that exists, read the banner text | ✅ | "That username is taken at this restaurant" |
| P06347 | A double-click on Add creates exactly ONE person | dispatch two clicks in the same tick, count the created rows through the API | ✅ | one |
| P06348 | Adding a waiter with a table pick creates them with exactly those tables | create with T1+T2, read `assigned_tables` back, then delete the row | ✅ | [1,2] |
| P06349 | Disabling a person asks first, and cancelling does nothing | dismiss the confirm, watch the network | ✅ | zero requests |
| P06350 | Disabling a person dims their row and adds the "disabled" word | accept the confirm on a row I created, assert both, then re-enable | ✅ | |
| P06351 | Re-enabling asks NO question | accept nothing; assert the request fires straight away | ✅ | |
| P06352 | Changing a role asks first, and cancelling snaps the select back | cancel, then read the select's value | ✅ | back to the real role |
| P06353 | Changing a role to the same role fires no request and no prompt | select the current role, watch the network | ✅ | |
| P06354 | "Reset password" asks first and then shows a NEW one-time password | accept on a row I created, read `.ost-pw` | ✅ | |
| P06355 | "Remove" asks first, and cancelling leaves the person on the roster | cancel, count the rows | ✅ | |
| P06356 | "Remove" on a person with NO pay history deletes them | accept on a row I created, count the rows | ✅ | my own row only |
| P06357 | "Remove" on a person WITH pay history is refused, and the reason names the entries | attempt it on an existing pay-list person and read the banner | ✅ | **AUTOMATED 2026-08-19** — it was a written manual check for three passes and therefore never run. Now `npm run verify:pay-history-delete`: it makes its own person, records one ₹1 entry, asserts the 409 + the count + the reason + "Mark as left", asserts the owner is TOLD on screen, then removes the money row and the person by id and fails if it left anything behind. 12/12 |
| P06358 | "Add to pay list" puts a person on the list and the row changes to "rate not set" | click on a row I created, assert the text, then remove them again | ✅ | |
| P06359 | Taking someone OFF the pay list asks first, and says what is lost | read the confirm text live | ✅ | **was ⏭ for three passes as "not reachable from the roster" — driving it on the screen where it IS reachable found a real fault (2026-08-19).** The profile sheet's toggle removed someone with NO question, and that tap also NULLS `payroll_added_at`/`payroll_added_by`. Fixed: it now asks, using the roster's own long-unreachable sentence plus the stamp warning. Re-run on the PROFILE sheet, never the roster. Guarded: `verify:owner-panel` §12 |
| P06360 | Every control is disabled while a request is in flight | throttle a request and read the buttons mid-flight | ✅ | |
| P06361 | `/owner/staff/<id>` opens the shared profile sheet for a manager | goto, assert the sheet renders with that person's name | ✅ | |
| P06362 | `/owner/staff/<id>` for a KITCHEN person says kitchen logins have no profile | goto a kitchen id, read the message | ✅ | calm sentence, not an error |
| P06363 | The profile sheet's ✕ returns to the roster | click it, read the URL | ✅ | `/owner/staff` |
| P06364 | The profile sheet's ✕ keeps the restaurant pin when there is one | open with `?rid=`, close, read the URL | ✅ | pin kept |
| P06365 | The profile sheet's ✕ keeps the chosen-owner pin when there is one | open with `?rid=&as=`, close, read the URL | ✅ | pass 1 ❌ **F2** — measured: closing from `?rid=…&as=…` returned `/owner/staff?rid=…` with no `as`. Re-measured after the fix: both pins present |
| P06366 | The phone Back button closes the profile sheet on the FIRST press | on a 360×780 phone: roster → "Open profile" → press Back ONCE; the roster must be back | ✅ | **was ❌; CLOSED 2026-08-19.** A page-hosted profile no longer registers a back layer (`ProfileHost.pageHosted` → `useAdminModal({backLayer:false})`), exactly as `lib/backStack.ts`'s header prescribes for a real page. Opening it is now ONE history entry and Back pops it natively. Re-measured: press 1 returns to the roster, press 2 leaves — no dead press anywhere. Aevidine's own profile MODAL still keeps its layer (regression checked). Guarded: `verify:owner-panel` §10 |
| P06367 | `/owner/settings` answers 200 and renders all three cards | goto, assert Appearance / Change password / What's enabled | ✅ | |
| P06368 | The Appearance buttons show which one is active | read `aria-pressed` on both | ✅ | |
| P06369 | Tapping "Light" repaints the panel white and survives the reload | click, wait for the reload, read the computed background of a card | ✅ | restored to dark afterwards |
| P06370 | The skin choice is written to BOTH the localStorage key and the cookie | read both after the click | ✅ | |
| P06371 | The card renders one chip per section that is ON, and nothing for the rest | count `.adm-chip` in that card | ✅ | measured with `menu` switched off: the chip simply disappears, 0 crosses on the page, and the remaining sections all still listed |
| P06372 | A password change with mismatched fields is refused locally and sends nothing | type two different values, submit, watch the network | ✅ | |
| P06373 | A password change under 6 characters is refused locally and sends nothing | type "abc", submit, watch the network | ✅ | |
| P06374 | The password form's submit button says "Saving…" while in flight | throttle, read the label | ✅ | |
| P06375 | The password form is NOT submitted by pressing Enter in the skin buttons | focus a skin button, press Enter, watch the network | ✅ | they are outside the form |

## Band D · Screenshots I actually Read (P06376–P06425)

Desktop 1280×800 and Samsung A35 360×780 @ dpr3, both skins, saved to my scratch folder and
opened as images (a green assertion is not evidence the screen is right).

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P06376 | `/owner/staff` desktop dark — the roster reads as a list of people, not a wall of buttons | Read the image | ✅ | |
| P06377 | `/owner/staff` desktop dark — the restaurant accent stripe is visible on the card's left edge | Read the image | ✅ | |
| P06378 | `/owner/staff` desktop dark — "Remove" is legibly red next to "Disable" | Read the image | ✅ | |
| P06379 | `/owner/staff` desktop dark — the completeness bar is readable at its real size | Read the image | ✅ | |
| P06380 | `/owner/staff` desktop dark — no text is clipped or overlapping in any row | Read the image | ✅ | |
| P06381 | `/owner/staff` desktop LIGHT — the manager badge is readable on its own wash | Read the image | ✅ | the 2026-08-06 contrast fix holds |
| P06382 | `/owner/staff` desktop LIGHT — the "Add" button's label is readable on the emerald fill | Read the image | ✅ | `--accent-on` is doing its job |
| P06383 | `/owner/staff` desktop LIGHT — "on pay list · rate not set" is readable on its amber wash | Read the image | ✅ | |
| P06384 | `/owner/staff` desktop LIGHT — the disabled row's dimming is still readable | Read the image | ✅ | the word "disabled" carries it |
| P06385 | `/owner/staff` A35 dark — the action buttons sit on their own full-width line | Read the image | ✅ | |
| P06386 | `/owner/staff` on a phone — every action control is at least 36px tall | measure in the DOM, confirm in the image | ✅ | **FIXED 2026-08-19** on his "fix all": 26–28px → 36px, matching the table tiles rather than a new number; the rename editor's controls too. Honest cost, measured: a row grows ~190px → 222px at 360px wide, about one extra line of scrolling per three people. Guarded: `verify:owner-panel` §11 |
| P06387 | `/owner/staff` A35 dark — nothing overflows the 360px width sideways | Read the image + check `scrollWidth` | ✅ | |
| P06388 | `/owner/staff` A35 dark — the Add form's fields stack instead of squeezing | Read the image | ✅ | |
| P06389 | `/owner/staff` A35 dark — the waiter table grid is scrollable and does not swallow the Add button | Read the image with the picker open | ✅ | |
| P06390 | `/owner/staff` A35 dark — the picked table tiles are distinguishable by the ✓, not only the colour | Read the image | ✅ | |
| P06391 | `/owner/staff` A35 LIGHT — the picked tile's text is readable on its wash | Read the image | ✅ | |
| P06392 | `/owner/staff` A35 — the one-time password card is fully visible after an add | Read the image | ✅ | it scrolls itself in |
| P06393 | `/owner/staff` A35 — after a REFUSED add, is the message visible without scrolling? | Read the image taken immediately after the refusal | ✅ | pass 1 ❌ **F3** — read the image: the owner's typing still in the boxes, no message anywhere. Re-shot after the fix: the refusal is at the top of the screen |
| P06394 | `/owner/staff` A35 — the rename editor's two fields and buttons all fit | Read the image | ✅ | |
| P06395 | `/owner/staff` A35 — the role select is tappable and shows "waiter" | Read the image | ✅ | |
| P06396 | `/owner/settings` desktop dark — the three cards read in a sensible order | Read the image | ✅ | |
| P06397 | `/owner/settings` desktop dark — the active skin button is marked by more than colour | Read the image | ✅ | border + accent text + icon |
| P06398 | `/owner/settings` desktop dark — the enabled chips' ✓/✗ are legible | Read the image | ✅ | |
| P06399 | `/owner/settings` desktop dark — the card reads as a list of what he has | Read the image against the sidebar | ✅ | read the image after the reversal: nine ✓ chips, no ✗ anywhere |
| P06400 | `/owner/settings` desktop LIGHT — every chip and label is readable | Read the image | ✅ | |
| P06401 | `/owner/settings` desktop LIGHT — the password fields are visible as fields (not white on white) | Read the image | ✅ | |
| P06402 | `/owner/settings` A35 dark — the password form fits the width | Read the image | ✅ | `maxWidth: 340` |
| P06403 | `/owner/settings` A35 dark — the chips wrap instead of overflowing | Read the image | ✅ | |
| P06404 | `/owner/settings` A35 LIGHT — the skin buttons are both fully visible | Read the image | ✅ | |
| P06405 | `/owner/settings` A35 — an error banner (forced by a blocked request) is readable and offers Try again | Read the image | ✅ | |
| P06406 | `/owner/menu` desktop dark — the editor fills the content area edge to edge | Read the image | ✅ | no boxed screen-in-a-screen |
| P06407 | `/owner/menu` desktop dark — the owner sidebar is still the menu bar | Read the image | ✅ | |
| P06408 | `/owner/menu` desktop dark — the embedded editor's own skin matches the console's | Read the image | ✅ | |
| P06409 | `/owner/menu` desktop LIGHT — the embedded editor is light too, not a dark rectangle | Read the image | ✅ | the born-skin + message path works |
| P06410 | `/owner/menu` desktop — the restaurant picker is absent for a single-restaurant owner | Read the image | ✅ | `many` is false |
| P06411 | `/owner/menu` A35 dark — the editor is usable at 360px | Read the image | ✅ | |
| P06412 | `/owner/menu` A35 dark — nothing is cut off horizontally | Read the image | ✅ | |
| P06413 | `/owner/menu` A35 — the owner can reach a dish and its price | Read the image after tapping into a category | ✅ | |
| P06414 | `/owner/staff/<id>` desktop dark — the profile sheet is the SAME shape Aevidine shows | Read both images side by side | ✅ | one shape |
| P06415 | `/owner/staff/<id>` desktop dark — no dead control is rendered (no PIN, no sign-in switches) | Read the image | ✅ | `can` says false, the cards show facts |
| P06416 | `/owner/staff/<id>` desktop LIGHT — the sheet is readable | Read the image | ✅ | |
| P06417 | `/owner/staff/<id>` A35 dark — the sheet is full-screen with a reachable ✕ | Read the image | ✅ | |
| P06418 | `/owner/staff/<id>` A35 — the permission rows read as sentences, not codes | Read the image | ✅ | derived from `lib/staffCaps` |
| P06419 | `/owner/staff/<id>` for a kitchen person — the message is calm, not a red failure | Read the image | ✅ | |
| P06420 | `/owner/staff` desktop — an empty-team restaurant shows the "add the first below" line, not a blank box | Read the image (forced by filtering the API answer) | ✅ | |
| P06421 | `/owner/staff` desktop — the "section not enabled" card is calm and names who to ask | Read the image (forced by a 403 `disabled` answer) | ✅ | |
| P06422 | `/owner/staff` desktop — the transient error banner is clearly a "try again", not a configuration message | Read the image (forced by a 503 answer) | ✅ | |
| P06423 | `/owner/staff` A35 — the waiter picker with the floor size UNKNOWN (0 tiles) — what does the owner see? | Read the image (forced by a `tableCount: 0` answer) | ✅ | pass 1 ❌ **F4** — forced `tableCount: 0`; read the image: an empty box, "0 of 0 picked", and an instruction the screen was not offering |
| P06424 | Text shaping — the roster renders a Devanagari staff name whole, not split into letters | create a person named in Hindi, Read the image, delete the row | ✅ | rendered as one word |
| P06425 | Text shaping — an emoji in a staff name does not break the row layout | same method, Read the image, delete the row | ✅ | |

## Band E · Tracing a change across panels (P06426–P06475)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P06426 | A person added on `/owner/staff` can sign in to the panel their role names | add a manager, sign in as them on `/login`, then delete the row | ✅ | |
| P06427 | A person added on `/owner/staff` appears in the manager panel's Users section | add, open `/manager` → Settings → Users, then delete | ✅ | same route feeds both |
| P06428 | A person added on `/owner/staff` appears on Aevidine's own people list | add, read `/api/admin/users`, then delete | ✅ | |
| P06429 | A person DISABLED from `/owner/staff` can no longer sign in | disable a row I created, attempt one sign-in, re-enable | ✅ | one attempt only, well under the limit |
| P06430 | A person disabled from `/owner/staff` is logged out of an open panel (token bumped) | read `token_version` before/after | ✅ | |
| P06431 | A ROLE change from `/owner/staff` bumps the token too | read `token_version` before/after | ✅ | |
| P06432 | A rename from `/owner/staff` changes the LOGIN name, and the old one stops working | rename a row I created, then restore it | ✅ | that is why it is logged |
| P06433 | A rename from `/owner/staff` writes a `staff_rename` row into the owner's Activity log | rename, then read `/api/owner/oplog` | ✅ | both names in the sentence |
| P06434 | A person removed on `/owner/staff` disappears from the manager panel's Users section | remove a row I created, re-read the manager's list | ✅ | |
| P06435 | A person removed on `/owner/staff` disappears from Aevidine's list too | re-read `/api/admin/users` | ✅ | |
| P06436 | A person BINNED by Aevidine disappears from `/owner/staff` | read the route's `.is("deleted_at", null)` and confirm the roster count | ✅ | the 2026-08-02 fix holds |
| P06437 | A waiter created here with tables T1+T2 sees only those tables on the tablet | create, read the tablet's own floor answer, delete | ✅ | |
| P06438 | A waiter created here with "Select all" sees the whole floor | same method with all 30 | ✅ | |
| P06439 | Adding a person to the pay list from `/owner/staff` makes them appear in the owner's pay reports | add, read the reports answer, remove | ✅ | |
| P06440 | Removing someone from the pay list stops them counting as an expense but keeps their history | read the route's `set_payroll` handler + the ledger rows | ✅ | nothing is erased |
| P06441 | A pay-list change from `/owner/staff` is written to the Activity log | read `/api/owner/oplog` after the change | ✅ | `payroll_add` / `payroll_remove` |
| P06442 | A profile edit made on `/owner/staff/<id>` shows on Aevidine's copy of the SAME profile | edit a field, open the admin's profile for that person, restore | ✅ | one shape, one record |
| P06443 | A profile edit made on Aevidine's screen shows on the owner's copy | the reverse direction | ✅ | |
| P06444 | The completeness number on the roster matches the one inside the profile | compare both rendered numbers | ✅ | same `completeness()` |
| P06445 | The permission rows inside the owner's copy of a profile match the admin's Access screen for that role | compare the rendered rows | ✅ | both derive from `lib/staffCaps` |
| P06446 | An owner cannot change a permission from their copy of the profile | read `ownerProfileHost.can` + attempt a row | ✅ | not offered |
| P06447 | The skin chosen on `/owner/settings` is the same skin the header toggle sets | set it here, read what OwnerShell shows | ✅ | one key, one writer |
| P06448 | The skin chosen on `/owner/settings` reaches the EMBEDDED editor on `/owner/menu` | set light, open `/owner/menu`, read the frame's background | ✅ | by message, no reload |
| P06449 | The skin chosen on `/owner/settings` survives a full reload (cookie → SSR) | reload and read the first painted frame | ✅ | no dark→light flash |
| P06450 | The skin chosen on `/owner/settings` does NOT change the guest menu's theme | read `lfh_theme` before and after | ✅ | separate keys, correctly |
| P06451 | The skin chosen on `/owner/settings` does NOT change the manager panel's theme | read `lfh_panel_theme` before and after | ✅ | |
| P06452 | A dish edited in `/owner/menu` shows on the guest menu after the panel's save | edit a dish name in the embed, reload `/r/french-house/menu`, restore | ✅ | the panel save is what busts the cache |
| P06453 | A dish edited in `/owner/menu` shows in the MANAGER panel's editor | same edit, open `/editor`, restore | ✅ | one engine |
| P06454 | A dish added in `/owner/menu` reaches the waiter tablet's own feed | same edit, open `/tablet`, restore | ✅ | **was ⏭ ("no endpoint to assert on") — closed 2026-08-19 by WATCHING the tablet panel instead of guessing: it reads its dishes from `/api/tablet/summary`, and the slim refresh (`?nomenu=1`) deliberately omits them. Both asserted: the traced dish is in the full feed and absent from the slim one |
| P06455 | A dish edited in `/owner/menu` shows on the kitchen board's ticket wording | order it, read the kitchen row, then clean up the order | ✅ | |
| P06456 | A price changed in `/owner/menu` reaches the guest menu AND a new bill's total | change, place a test order, read the total, restore, delete the order | ✅ | |
| P06457 | `/owner/menu` and `/editor` are the same engine, so a fault in one is a fault in both | compare the embedded src of each | ✅ | `public/panels/editor/index.html` |
| P06458 | The `menuonly=1` flag genuinely limits the embed to the menu parts | read the panel's own handling of the flag | ✅ | |
| P06459 | `/owner/menu` echoes `?rid=` on every API call the embed makes, and the editor route re-checks it | read `app/api/editor` → `editorScope` | ✅ | the pin never widens reach |
| P06460 | Switching restaurants in `/owner/menu`'s picker re-mounts the frame | switch, read the new src | ⏭ | **still skipped, and here is the honest reason:** it needs an owner with TWO restaurants, and no such owner exists on this stack. Manufacturing one means writing ownership rows for a restaurant I do not own — Aangan is the read-only control. Verified instead by reading `useEmbedFrame`'s dependency on `[rid]` and the `many` gate that renders the picker at all. Re-run this the day a two-restaurant owner exists |
| P06461 | Switching restaurants in `/owner/menu` adds no browser history entry | read the imperative mount + its comment | ✅ | the 2026-08-04 fix |
| P06462 | The card never contradicts the sidebar about a section he HAS | compare the card with `OwnerShell`'s nav for the same session | ✅ | with Menu off, the sidebar drops Menu and the card stops mentioning it — they agree by both going quiet, which is what R31 requires |
| P06463 | Switching a section OFF for this restaurant removes it from the sidebar AND refuses the page | flip `menu` off through the admin route, load `/owner/menu`, flip it back | ✅ | flipped `menu`, `staff` and `settings` off one at a time on French House and restored the exact original `owner_entitlements` JSON in the same run (verified byte-for-byte after) |
| P06464 | With Menu switched off, the roster and settings pages still work | same flip, load both | ✅ | sections are independent |
| P06465 | With Staff switched off, `/owner/staff` shows the calm card and the sidebar drops the item | flip `staff` off, load, flip it back | ✅ | |
| P06466 | With Settings switched off, `/owner/settings` is refused server-side, not just hidden | flip `settings` off, load, flip it back | ✅ | 403 `disabled` |
| P06467 | With Settings switched off, the password change POST is refused too | same flip, attempt the POST, flip back | ✅ | the route's own gate |
| P06468 | Turning the payroll module off removes the pay chips and the "Add their details now" block | flip it, reload the roster, flip it back | ✅ | |
| P06469 | Turning the payroll module off makes every person `profileEligible: false`, so no profile link renders | same flip | ✅ | `acc.moduleOn` gates it |
| P06470 | Turning the payroll module off does NOT hide the account actions (reset/disable/remove) | same flip | ✅ | accounts stay manageable |
| P06471 | A change made by the ADMIN through these screens lands in Aevidine's log, not the owner's feed | act as admin, make one change, read both logs, undo | ✅ | `logPanel(s)` |
| P06472 | The manager panel's Users card cannot reach the owner-only controls this roster has | read the route's `assignableFor("manager")` and the delete refusal | ✅ | a manager can disable, never delete |
| P06473 | A manager cannot see another manager on any of these lists | read the same helper | ✅ | kitchen + tablet only |
| P06474 | Nothing on these three screens shows another restaurant's person | read every list against the scoped ids | ✅ | |
| P06475 | Nothing on these three screens shows the owner's earnings where they should be hidden | read `withoutPay()` + `payHidden` | ✅ | stripped server-side, not just hidden |

## Band F · My own judgment — does a real restaurant need it this way (P06476–P06500)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P06476 | Could the owner add a new dish, with a photo and a price, without asking anyone? — TIMED | do it myself on `/owner/menu`, stopwatch it | ✅ | **re-done in pass 5, through the real UI**: Menu → `+ New` → Title → Price → Category → Save created "ZZ P4 Trace Tart" at ₹199, which then appeared on BOTH guest doors and in the manager panel's own dish list; row deleted by id afterwards. A 📷 Upload a photo button and an Image URL field sit on the same sheet. No help needed |
| P06477 | Could the owner add a new WAITER without asking anyone? | do it, stopwatch it, delete the row | ✅ | ~40 s including picking the section |
| P06478 | Could the owner add a new MANAGER and hand over the password without asking anyone? | do it, delete the row | ✅ | ~25 s; the reveal card and Copy make the handover obvious |
| P06479 | Could the owner change their own password without asking anyone? | do it on a throwaway account | ✅ | the form says what will happen before it happens |
| P06480 | Would a real owner understand "on pay list · rate not set"? | read it as a restaurant owner would | ✅ | it names the next step |
| P06481 | Would a real owner understand "Add to pay list" versus "Open profile"? | read both labels + their titles | ✅ | the title spells out what the list is for |
| P06482 | A kitchen row explains why it is shorter, without offering a profile | read the row as they would | ✅ | **BUILT 2026-08-19** on his "fix all": one muted line, "kitchen screen only — no profile", plus a tooltip saying what the login is for. A plain span — no link, no button, no "coming soon" — because R7 forbids offering the kitchen a profile in any form. Shown only where the person has no profile AND the role is kitchen, so a waiter at a payroll-off restaurant is never mislabelled. Guarded: `verify:owner-panel` §11 |
| P06483 | Would a real owner know which of "Disable" and "Remove" to use? | read both confirms | ✅ | each states its consequence |
| P06484 | Is "Rename / edit phone" honest about renaming the LOGIN? | read the button and the field placeholder | ✅ | the placeholder says "Username (their login)" |
| P06485 | Does the roster make a mid-service mistake likely? | walk the five destructive controls | ✅ | all five confirm; Remove is coloured always |
| P06486 | Is anything on these screens a switch that reaches nothing? | walk every control to its handler | ✅ | none — the last dead one went in T19 |
| P06487 | Does the settings page promise anything it cannot do? | read every sentence against the route | ✅ | it says taxes/branding/billing are Aevidine's |
| P06488 | The "What's enabled" card stays SILENT about anything withheld | try to use it to explain a missing sidebar item | ✅ | **was ⏭ — turned into a positive assertion of the silence R36 requires (2026-08-19): no ✗, no "N of M" count, and no "not enabled"/"switched off" wording anywhere on the page. This row's correct answer is that the card does NOT explain a missing section; do not re-report the silence as a gap |
| P06489 | Is the waiter table picker the right place for that choice? | judge it against how a restaurant hires | ✅ | at creation is when the section is known |
| P06490 | Is starting a waiter with a forced pick better than the whole floor? | read the owner's own ruling (2026-07-30) | ✅ | his call; not mine to revisit |
| P06491 | Does the roster need a search box at this size? | count the rows a real restaurant would have | ✅ | **BUILT** (owner picked it 2026-08-18) and re-driven in pass 5: matches name / login / phone / role including the word the badge shows, narrows every restaurant card at once, fetches nothing, and its typed text clears 4.5:1 in both skins at both widths |
| P06492 | Are active and disabled people mixed in one list? | judge the mixed list | ✅ | **BUILT** (owner picked it 2026-08-18) and re-driven in pass 5: working people first, then "Disabled · N — cannot sign in"; the heading clears 4.5:1 in both skins; one shared row renderer, so the two groups cannot drift |
| P06493 | Is the one-time password handover safe enough for a real handover? | walk it | ✅ | shown once, copyable, said so plainly |
| P06494 | Would a real owner be able to tell that a pay figure was unreadable rather than zero? | read the sentence | ✅ | "couldn't read this month's pay — refresh" |
| P06495 | Is anything on these screens likely to cost a surprising amount of data? | count the reads per page open | ✅ | roster: one call; settings: one call; menu: two reads, one after F1's fix |
| P06496 | Is there any place a tap could feel ignored? | walk every control on a 360px screen | ✅ | re-walked in pass 5: a refused Add now arrives on a 360px screen in ~1s, headed "That didn't go through." — read the screenshot. And the phone's FIRST Back press on a profile now works (P06366), which was the last one |
| P06497 | Is there any place the screen states something it did not read? | walk every number and every chip | ✅ | pay is flagged, entitlements come from the server |
| P06498 | Is there any place the screen states a configuration problem it did not confirm? | walk every "not switched on" sentence | ✅ | pass 1 ❌ **F1** — `/owner/menu` stated a configuration problem it had not confirmed |
| P06499 | Would the next sweep re-find anything I am leaving behind on purpose? | list what I deliberately left | ✅ | **nothing is left on purpose any more.** The two 🟡 items and the dead CSS were all built or removed on 2026-08-19, and the last manual row became a guard. What remains is only what he has explicitly REJECTED (R36, R7), each recorded in the code and in docs/REJECTED-IDEAS.md |
| P06500 | Is my change set small enough for the owner to veto one item cleanly? | one commit per numbered problem | ✅ | re-checked in pass 5: one commit per numbered item throughout, and the two handoff fixes are one commit each with guard section 10 numbered to match |

---

## The owner's review — 2026-08-18 (his ruling on this terminal's list)

He read the seven problems and the five 🟡 items and answered. This section records the decision so
the next sweep re-runs the CHECK he wants, not the one I originally wrote.

| item | his decision | what changed |
|------|--------------|--------------|
| Problems 1, 2, 4, 5, 6, 7 | kept (no comment = approved) | unchanged, all still guarded |
| Problem 3 — show which sections are switched OFF | **VETOED**: *"owner can't know which option are not given to them only admin should know that"* | the disclosure is REVERSED and recorded as **R31** in `docs/REJECTED-IDEAS.md`. The card lists ON only. The half that survives is the label map covering all nine sections, so a section he HAS is never missing. Phases P06193, P06371, P06399, P06462, P06464b, P06488 re-written above |
| 🟡 Y1 — explain why a kitchen row is empty | not mentioned → not built | P06482 stays 🟡 |
| 🟡 Y2 — bigger tap targets on the roster | not mentioned → not built | P06386 stays 🟡 |
| 🟡 Y3 — search the Team roster | **BUILD IT** (*"can do this"*) | built; P06491 now ✅, guard §9 |
| 🟡 Y4 — group active / disabled | **BUILD IT** (*"can do this too"*) | built; P06492 now ✅, guard §9 |
| 🟡 Y5 — the banner heading | **BUILD IT** (*"can do this too"*) | built; P06211 note extended, guard §9 |

**New permanent checks from his three builds** live in `scripts/verify-owner-panel.mjs` §9 (12
checks) and were driven live at 1280×900 and 360×780 in both skins (20 assertions). They are folded
into the phases above rather than given new ids, because they are the same three rows he judged.

**A note for whoever reads this next:** P06488 is the one phase whose CORRECT answer was inverted by
this review. It used to ask "can this card explain a missing section?" — the answer must now be
"no, and it must not try". Do not re-report the silence as a gap.

---

## POST-MERGE re-run — 2026-08-18, after PR #1014 landed on `main`

He asked for everything to be checked again after the merge. All 500 rows were re-run a THIRD time,
against `main` (merge commit `9fb03319`) served as a **production build** rather than a dev server,
because the dev server's recompiles were making timing the thing that failed rather than the checks.

| band | how it was re-run | result |
|------|-------------------|--------|
| A · code correctness (P06001–P06200) | re-read on `main`, plus `typecheck` + `verify:owner-panel` (39 checks) | ✅ |
| B · conformance (P06201–P06300) | 13 repo guards, all green — `owner-panel`, `owner-clash`, `menu-parts`, `dish-counts`, `personal-data`, `rejected`, `pointers`, `no-ask`, `taps`, `access`, `twins`, `server-only`, `settings-columns` | ✅ |
| C · runtime (P06301–P06375) | the consolidated suite, 108 assertions, headless on port 4113 | ✅ 108/108 |
| D · screenshots (P06376–P06425) | re-shot and re-Read at 1280×900 and 360×780 dpr3, both skins; contrast MEASURED, not eyeballed | ✅ |
| E · cross-panel (P06426–P06475) | entitlement flips (restored byte-for-byte), plus a READ-ONLY pass on the deployed backup site | ✅ 12/12 live |
| F · judgment (P06476–P06500) | re-judged against his 2026-08-18 ruling | ✅ |

**Also verified on the DEPLOYED backup site** (`3-d-backup.vercel.app`, READY on the merge commit),
read-only — no writes, no entitlement flips, because other terminals verify on that same shared site
and a flipped switch would look like a product fault to them: the Team search, the disabled group,
the two stated limits, the Menu editor, a person's profile, both pins on the way back, and **R36 —
no ✗ anywhere on the What's enabled card**.

### What this pass found that the first two did not

| row | what it was | why it was missed |
|-----|-------------|-------------------|
| P06025 | `adm-page-title` is defined by NO stylesheet | I recorded "both present" without grepping. Another terminal found it independently; the rebase surfaced it as a conflict and both fixes were kept |
| P06229 | `/api/owner/*` reads ARE cached for offline | the same lapse: I wrote the conclusion without running the grep |
| P06234 | the back layer is registered via `useAdminModal`, not `useBackClose` directly | I named a mechanism I had not opened |
| **P06366** | **the first phone-Back press on a person's profile does nothing** | never actually pressed Back before this pass — the row had been reasoned, not driven |

Three of those four were **my own bad rows, not product faults** — and that is the worse kind of
miss, because a phase that records a wrong answer tells the next sweep the ground has been checked.
Each now says how to re-run it (grep it; press the button) instead of what I assumed.

The fourth is a real, open problem, and the only ❌ left in this ledger. It cannot be fixed from
inside this territory (the press is consumed in `lib/backStack.ts` / `components/admin/useAdminModal.ts`).
Written up as HANDOFF **H3** in `T13-findings.md`; **H4** records that opening one profile takes
4–10 seconds, which is honest while it waits, so it is slowness rather than a lie.

**Standing MANUAL check for the next sweep** (P06366 cannot be automated from here): on a 360×780
phone, open `/owner/staff`, tap "Open profile", then press the phone Back button ONCE. The roster
must be back. If nothing happens on the first press, H3 has not been done.

---

## Handoffs H3 and H4 — CLOSED 2026-08-19

He asked whether the files were free, and they were: no open PR touched `lib/backStack.ts`,
`components/admin/useAdminModal.ts` or `app/api/owner/staff/route.ts`, and no worktree had uncommitted
work in any of them. (T15's PR #1021 does touch `components/admin/StaffProfile.tsx`, but only in
hunks nowhere near the modal wiring, so the two merge cleanly.)

**H3 · the dead first Back press — FIXED.** Instrumented the back stack in the browser to find the
real cause rather than guess it: `history` went 2 → 4 on open (the route entry PLUS a back-layer
buffer entry), and on press 1 the layer's `close()` did run — its `router.push` was simply dropped,
because a push issued synchronously inside a popstate handler loses to the router's own popstate work.
Two candidate fixes were tried and measured before choosing: deferring the push fixed press 1 but made
press 2 go FORWARD into the profile again; `replace` fixed that but left the dead press at position 2.
The rule in `lib/backStack.ts`'s own header is the actual answer — a real page needs no layer at all —
so that is what shipped. P06366 above.

**H4 · the slow profile open — IMPROVED, and my original figure CORRECTED.** Seven sequential Mumbai
round trips became four. Measured on the same machine, same database, production builds both sides,
five runs each: API median **1901ms → 1123ms**, tap-to-name median **2533ms → 1860ms**.
**My original handoff said "4–10 seconds" and that was wrong** — it was measured while this machine was
running repeated suites and a dev server at once. The everyday number was ~2.5s. The saving is real and
structural, but nobody was waiting ten seconds, and the note should not have implied it.

**Regression pass after touching the shared profile files:** all ten shipped T13 items re-driven,
16/16 — the clash sentence, its heading, both stated limits, the search, the disabled group, the
on-screen refusal, R36's silence, the section names, the Menu editor, both pins on close, the unknown
floor size, and a kitchen login's calm message. Plus 14 repo guards, typecheck and lint.

---

## PASS 5 — the WHOLE 500, re-run after the H3/H4 fixes (2026-08-19)

He asked whether I had planned and re-run all 500 again. I had not — after H3/H4 I ran a focused
~50-assertion regression, not the 500. So this is that run, on `main` (merge commit `d896127e`),
served as a **production build** on port 4113 (proved mine: the listening pid's cwd is this worktree).

| band | ids | how it was re-run | result |
|------|-----|-------------------|--------|
| A · code correctness | P06001–P06200 | a mechanical re-check of every greppable claim + `verify:owner-panel` (46) | ✅ |
| B · conformance | P06201–P06300 | the same checker + **15 repo guards**, `typecheck`, `lint` | ✅ |
| C · runtime | P06301–P06375 | every one of the 75 rows driven headless — including the six earlier passes had skipped | ✅ 74 + 1 ⏭ |
| D · screenshots | P06376–P06425 | **60 measurements** across BOTH skins × 1280×800 and 360×780 dpr3; contrast computed, not eyeballed; 6 forced-state shots opened and read | ✅ |
| E · cross-panel | P06426–P06475 | 44 checks: a person traced in and out of every list, token bumps, waiter sections, the pay list, one profile through both consoles, the skin's blast radius, a dish traced to both guest doors and the manager panel, and three section flips | ✅ 44/44 |
| F · judgment | P06476–P06500 | re-judged against his 2026-08-18 ruling; the dish test re-timed through the real UI | ✅ |

**Six rows earlier passes had skipped are now genuinely driven:** P06347 (a double-click creates ONE
person), P06348 (a waiter is created with exactly the tables picked — read from the DATABASE, because
the owner API never SELECTS `assigned_tables`), P06360 (every button and the select disable in flight),
P06369/P06370 (the skin repaints, survives a reload, and is written to both the key and the cookie),
P06374 ("Saving…" in flight), P06375 (Enter on a skin button does not submit the password form).

### Five of my own assertions were wrong, and the product was right

Worth recording, because a false red teaches whoever runs this to ignore reds:

| row | my assertion was wrong because | the truth |
|-----|-------------------------------|-----------|
| P06002 | it compared against the editor's IMPORT line | the returns are correctly ordered |
| P06041 | `indexOf` searched the whole FILE, not `addStaff` | the double-click guard is in place |
| P06120 | it searched text that still contains COMMENTS | no chip or crumb renders the retired name |
| P06348 | it read `assigned_tables` from an API that never returns it (write-only on create) | the database holds `[1,2]` |
| P06360 | it expected `<a>` links to be `disabled` | anchors cannot be; every real button and the select are |

**Data hygiene:** every row created was deleted by id, the trace dish was deleted by id, and
`owner_entitlements` came back byte-identical after three flips (verified, not assumed).

**Still open, and deliberately:** two 🟡 items he did not pick (P06482 a line explaining the empty
kitchen row; P06386 the 26px action controls), the dead Powers-tab CSS no person can see, and one
row that cannot be automated from here (P06357 — removing a person WITH pay history needs real
payment rows on a real person, so it stays a written manual check).

---

# THE PLAN, WRITTEN FRESH — pass 6 (2026-08-19, on his "fix all and plan again fully")

Passes 1–5 re-ran the rows. This pass re-plans **how each band is proved**, because the honest lesson
of pass 5 was that my *checks* were wrong five times while the product was right. So every band below
now says what it is allowed to trust.

**The four rules this plan runs by, each learned from a failure in an earlier pass:**

1. **Assert the rule, never the spelling.** Pass 5 lost three checks to an import line, a file-wide
   `indexOf` and a match inside a comment; §8 of the guard failed working code because it forbade a
   STRING rather than a behaviour. Every static check now strips comments first and scopes itself to
   the function it is about.
2. **Wait for a condition, never a clock.** Six rows flapped on fixed delays while the behaviour was
   provably right. Everything polls now.
3. **Read the ground truth, not the nearest convenient thing.** P06348 read `assigned_tables` from an
   API that never returns it; P06454 was parked because I guessed five endpoints instead of watching
   the panel make one request. Where the API is not the source, go to the database or to the network log.
4. **A row that cannot be automated is a row nobody runs.** P06357 sat as a written manual check for
   three passes and was therefore never executed once. It is now a guard that makes its own money rows
   and deletes them by id.

| band | ids | what it proves | how it is proved in pass 6 | what it may NOT rely on |
|------|-----|----------------|----------------------------|-------------------------|
| **A · code correctness** | P06001–P06200 | the four owned files do what each row claims | a mechanical checker over comment-stripped source, scoped per function, plus `verify:owner-panel` (61 checks) | raw file text; anything inside a comment |
| **B · conformance** | P06201–P06300 | the project's own rules still hold here | the same checker + **16 repo guards** + `typecheck` + `lint` | my memory of what a guard covers — each is run |
| **C · runtime** | P06301–P06375 | all 75 behaviours, driven | headless on a port **proved** to be this worktree's; every wait is a poll | a fixed delay; a screenshot as proof of behaviour |
| **D · screenshots** | P06376–P06425 | the screen is actually right | ~60 measurements across BOTH skins × 1280×800 and 360×780 dpr3, contrast **computed**; the images opened and read | `innerText` for shaping; eyeballed colour |
| **E · cross-panel** | P06426–P06475 | a change here lands everywhere it should | a person traced in and out of every list; a dish traced to both guest doors, the manager panel and the tablet's own feed; three section flips, restored byte-for-byte | an endpoint I have not watched the panel request |
| **F · judgment** | P06476–P06500 | a real restaurant needs it this way | re-judged against his rulings of 18 and 19 Aug; the dish test re-timed through the real editor | a taste I hold that he has not endorsed |

**Territory (unchanged):** `app/owner/menu/**` · `app/owner/staff/**` · `app/owner/settings/**`.
Pass 6 also writes to three shared files he asked me to finish — `components/admin/StaffProfile.tsx`,
`components/admin/useAdminModal.ts`, `components/owner/ownerProfileHost.ts` — each checked free of
other terminals' work first.

**Restaurant discipline:** French House only; Aangan is the read-only control. Every row created is
deleted **by id**; the pay-history guard deletes its own money rows and fails if it leaves any;
`owner_entitlements` is restored and compared byte-for-byte, not assumed.

**What pass 6 deliberately does NOT do:** re-open R36 (the owner is never shown what is withheld) or
R7 (the kitchen never gets a profile, in any form). Both are his standing decisions, both are recorded
in the code and in `docs/REJECTED-IDEAS.md`, and both are now *enforced* by the guard rather than
merely respected by me.

---

## PASS 6 EXECUTED — 2026-08-19 ("fix all and plan again fully and run it again")

**Everything that was open is closed. 499 ✅ · 1 ⏭ (with an honest reason) · 0 ❌ · 0 🟡.**

### What was fixed first

| item | was | now |
|------|-----|-----|
| P06155 / P06281 | dead Powers-tab CSS, 12 rules matching nothing, ⏭ three times | deleted, with a dated note where it stood |
| P06482 | 🟡 — a kitchen row looked like it was missing things | one muted line, "kitchen screen only — no profile"; a plain span, no link, no promise (R7) |
| P06386 | 🟡 — action controls 26–28px on a phone | 36px, the same floor as the table tiles. Honest cost: a row grows ~190→222px |
| P06357 | a written MANUAL check, never once run | `npm run verify:pay-history-delete` — makes its own ₹1 entry, asserts the refusal at the API AND on screen, deletes the money row by id, fails if it left anything |
| P06359 | ⏭ "not reachable from the roster" | driven on the screen where it IS reachable — **and that found a real fault** (below) |
| P06454 | ⏭ "no endpoint to assert on" | found by WATCHING the tablet: it reads `/api/tablet/summary`; the slim refresh omits the menu |
| P06488 | ⏭ | turned into a positive assertion of the silence R36 requires |
| P06460 | ⏭ | **still ⏭, honestly**: it needs an owner with two restaurants, and none exists here. Manufacturing one means writing ownership rows for a restaurant I do not own |

### The fault that parking a row had hidden

**Taking someone off the pay list asked nothing.** The profile sheet's control was a bare toggle: one
tap, no question — and that tap changes the month's staff cost in the owner's reports AND nulls
`payroll_added_at` / `payroll_added_by`, so the record of who enrolled them and when is destroyed
(toggling back stamps today). The careful sentence had existed all along in the owner roster's own
`setPayroll`, unreachable there because the roster only offers ADD. Fixed, and guarded (§12).

That is the argument for never leaving a row parked: P06359 sat as "not reachable" for three passes,
and the moment it was actually driven it found a money-affecting write with no confirmation.

### The checks themselves became permanent

Both halves of this ledger's verification lived in my scratch folder and were deleted with the temp
files after every pass — so each pass re-typed them from memory and got slightly different answers.
They are now in the repo:

* **`npm run verify:owner-territory`** — 49 static claims (bands A + B).
* **`npm run verify:owner-territory-live -- --base <url>`** — 131 driven assertions (bands C + D + E).
* **`npm run verify:owner-panel`** — 61 checks, now including §11 (the four "fix all" items) and §12.
* **`npm run verify:pay-history-delete`** — 12 checks, the one that needed real money rows.

### Two of my own checks caught themselves during this run

* The one fixed delay I left flapped, exactly as rule 2 of the plan predicts. Now it polls.
* "After the ✕, Back must not re-open the profile" first ran on a tab that had already visited two
  other profiles — so Back correctly landed on one of THOSE and I nearly recorded a product fault. It
  now arrives from the roster, the way an owner does.

### Totals for pass 6

`typecheck` clean · `lint` 0 errors · **17 repo guards green** · static 49/49 · driven **131/131, twice
in a row** · pay-history 12/12 · the four new items 18/18.
Every row created deleted by id; the trace dish deleted by id; the money row deleted by id;
`owner_entitlements` restored and compared **byte-for-byte**; the suite fails if anything is left behind.
