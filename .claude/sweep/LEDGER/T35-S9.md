# SWEEP #9 — TERMINAL 35 · THE ACCESS AND PERMISSION LIBRARIES · phases P105001–P105050

**Territory (write access):** `lib/accessConfig.ts` · `lib/accessModel.ts` · `lib/accessState.ts` ·
`lib/accessTree.ts` · `lib/staffAuth.ts` · `lib/staffCaps.ts` · `lib/staffProfile.ts` ·
`lib/staffProfileShared.ts` · `lib/staffProfileShared.test.mjs` · `lib/revealGate.ts`.
Ten files, **3,103 lines** — re-derived with `wc -l`, not inherited from the prompt (which named
seven and gave `accessTree.ts` as 1,620 where it is 1,651). `lib/staffCaps.ts` and
`lib/staffProfileShared.ts` are here because the territory line says "every remaining `lib/access*` /
`lib/staff*`"; `lib/revealGate.ts` is the file §2b names as new ground.

**Branch:** `sweep9/t35-access-and-permission-libraries` · **worktree:** `../wt-s9-t35` · **port:** 4435
**ID block:** `P105001`–`P105100`, pre-allocated. Fifty used; `P105051`–`P105100` stay free.

## Where these fifty point, and why — MEASURED before one was written (rule 2b)

Rows whose **subject** is one of my ten files, counted across all 44 ledgers on disk:

| rows | per kloc | file | lines |
|---:|---:|---|---:|
| **0** | 0.0 | `lib/revealGate.ts` | 115 |
| **1** | 25.6 | `lib/staffAuth.ts` | 39 |
| **2** | 42.6 | `lib/accessConfig.ts` | 47 |
| **2** | 18.7 | `lib/accessState.ts` | 107 |
| **2** | 7.7 | `lib/staffCaps.ts` | 259 |
| **2** | 22.5 | `lib/staffProfile.ts` | 89 |
| **2** | 24.1 | `lib/staffProfileShared.test.mjs` | 83 |
| 6 | 22.3 | `lib/staffProfileShared.ts` | 269 |
| 7 | 15.8 | `lib/accessModel.ts` | 444 |
| 15 | 9.1 | `lib/accessTree.ts` | 1,651 |

So the fifty go: **14 to `revealGate.ts`** (zero rows, and the only file in the territory added since
the sweep #8 map was drawn — `git log --diff-filter=A --since=2026-09-01` names it and nothing else),
6 to `staffAuth.ts`, 6 to `accessState.ts`, 8 to `staffCaps.ts`, 4 to `accessConfig.ts`,
8 to the two staff-profile files, 4 to the two big model files — which already carry 22 rows between
them and are covered by `verify:access`'s 56 checks on top.

**Result key:** ✅ pass · ❌ a problem, named and fixed unless the note says otherwise · ⏭ skipped,
with a written reason. Twelve of the fifty were driven LIVE against a real server on port 4435
(a real admin sign-in, the real routes) rather than read from source.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P105001 | with nobody having typed the password, the admin console reports the passwords as covered | live on :4435 — `GET /api/admin/reveal` with only the sign-in cookie | ✅ | `{configured:true, unlocked:false, until:null, ttlMs:300000}` |
| P105002 | the uncover door cannot even be knocked on without being signed in to the console first | live — `GET /api/admin/reveal` with no cookie at all | ✅ | 401, before any counting of tries |
| P105003 | sending no password at all is answered in plain words, not a crash | live — `POST` with `{}` | ✅ | 400 · "Type the admin password." |
| P105004 | one wrong password counts down and never hints how close it was | live — ONE wrong `POST` (the correct one after it clears the counter) | ✅ | 403 · "That isn't the admin password. 4 tries left." — no mention of length or prefix |
| P105005 | the right password uncovers for five minutes, and the cookie cannot be read by a script | live — correct `POST`, then read the Set-Cookie attributes | ✅ | `HttpOnly; SameSite=lax; Max-Age=300; Path=/` and `until` five minutes out |
| P105006 | the cookie is not the password, nor anything you could work it back from | live — inspect the minted value | ✅ | `<13-digit expiry>.<64 hex>`; the password's characters appear nowhere in it |
| P105007 | with the cookie, the console reports unlocked and an expiry still in the future | live — `GET` carrying the reveal cookie | ✅ | unlocked:true, until > now, ttlMs 300000 |
| P105008 | a cookie whose expiry has been re-dated to tomorrow does not uncover anything | live — `GET` with the same signature and a `now + 24h` expiry | ✅ | unlocked:false — the expiry is inside the signature |
| P105009 | eight damaged cookie shapes all read as covered, and none of them 500s | live — `""`, `"."`, `abc`, `abc.def`, `<ms>.`, `.<sig>`, `NaN.zz`, `<ms>.ffff…` | ✅ | 200 + unlocked:false, eight times; never an exception |
| P105010 | "Cover them again" clears it, and cannot be done by someone not signed in | live — `DELETE` with and without the sign-in cookie | ✅ | 200 + `Max-Age=0`; 401 without |
| P105011 | while the passwords are covered, the per-person password door refuses with the one sentence | live — `POST /api/admin/reveal/password` | ✅ | 423 · "Passwords are covered. Type the admin password to uncover them." |
| P105012 | an unlock whose five minutes are up reads as covered, and does not throw | node: `mintRevealToken(-1000)` → `revealUnlockedUntil` | ✅ | null, and `revealUnlocked` false |
| P105013 | changing the uncover secret retires every cookie already issued | node: mint on secret A, read on secret B, read again on A | ✅ | valid → null → valid; nothing survives the rotation |
| P105014 | with no secret set anywhere on the deployment, nothing can be uncovered | node: unset REVEAL/ADMIN/STAFF/EDITOR passwords | ✅ | `revealConfigured()` false, a genuine token reads null, `revealPasswordMatches("")` false |
| P105015 | only the right hash opens the staff door — not the password itself, not a different case | node: six shapes against a known password | ✅ | `[true,false,false,false,false,false]` |
| P105016 | with no password configured the staff door stays shut for every token, including the hash of "" | node: unset all three names | ✅ | false, false, and `adminPassword()` is "" |
| P105017 | the constant-time compare is length-checked first and answers right for every shape | node: equal, one-character apart, and both length mismatches | ✅ | `[true,true,false,false,false,false]` |
| P105018 | `ADMIN_PASSWORD` wins over the two older names, and they fall back in order | node: set all three, then remove them one at a time | ✅ | one → two → three |
| P105019 | the hash matches node's own digest, including for text that is not English | node: "", abc, an em dash, Devanagari, an emoji | ✅ | identical to `createHash("sha256")` for all five |
| P105020 | the readable UI-hint cookie is a different name from the real gate, and from the uncover cookie | node: compare the three constants | ✅ | `lfh_staff_auth` · `lfh_is_staff` · `lfh_reveal` |
| P105021 | the restaurant read names its three columns and is scoped to one id | read lib/accessState.ts | ✅ | `.select("manager_permissions, owner_entitlements, access_config").eq("id", rid)` |
| P105022 | the settings read names its columns, de-duplicates them, and is scoped to one restaurant | read lib/accessState.ts | ✅ | `Array.from(new Set(cols))` then `.eq("restaurant_id", rid)` — no `select("*")` |
| P105023 | an unknown restaurant is refused, and a malformed id never reaches the database | live — `GET /api/admin/restaurants/access-tree` with an unused uuid, then with `not-a-uuid` | ✅ | 404 "Restaurant not found." · 400 "Invalid restaurant_id." · a real restaurant answers 4,161 bytes with masked creds only. The screen itself was rendered at 1280px and at 360×780 (Samsung A35): five sections, no overlap, no horizontal scroll, and none of `undefined` / `NaN` / `[object Object]` / `${` / `-->` on the page |
| P105024 | a settings read that FAILS must not be handed back as "this restaurant is at its defaults" | read lib/accessState.ts — is `.error` looked at? | ❌→✅ | **FIXED, item 1.** The reply's `.data` was taken with nothing looking at `.error`, and in this model an absent value means "use the row's own default" — so one blipped read would have drawn the whole Access screen at factory values over a restaurant whose settings say otherwise. Now returns null, like the restaurants read has always done. Guarded by `verify:access` check 56 |
| P105025 | a feature, grant or section that was never stored is left unset, so the row's own default still decides | read the three loops | ✅ | `if (k in …)` / `if (f in mp)` / `typeof oe[k] === "boolean"` |
| P105026 | a delivery channel with nothing stored reads OFF, the same way every other reader treats it | read the channel loop against `ch_website`'s `def: true` | ✅ | deliberate: the node default is ON but `settingsClone` seeds the row, so the screen and the readers agree on a stored value rather than on two different absences |
| P105027 | every grant row on the Access screen is seeded into a brand-new restaurant | node: `GRANT_FLAGS` ⊂ `MP_DEFAULT` | ✅ | all 8 |
| P105028 | what a new restaurant is seeded with is what `managerGrantValue` answers for it | node: compare all 20 seeded flags | ✅ | no disagreement — the seed is derived, not typed |
| P105029 | the three pay powers are NOT seeded, so the Payroll module's own rule still decides them | node: check MP_DEFAULT | ✅ | `see_staff_pay` / `record_staff_payment` / `edit_staff_profiles` absent, as the file's comment promises |
| P105030 | every power the enforcement list knows is either seeded or owned by Payroll | node: `MANAGER_POWER_FLAGS` minus the three | ✅ | nothing left unseeded |
| P105031 | every key one person can be given is a real storage key an enforcer reads | node: manager keys ⊂ GRANT_FLAGS, waiter keys ⊂ TABLET_COLS ∪ `cap:`CAP_TABLET_IDS | ✅ | 8 manager, 9 waiter, no stray |
| P105032 | no two rows on one person's page share a storage key | node: manager, waiter and owner lists | ✅ | 27 / 10 / 14 rows, all distinct |
| P105033 | a kitchen person has no permission rows at all — ruled three times (R7) | node: `capsForRole("kitchen")` | ✅ | 0 rows, 0 groups, 0 writable keys |
| P105034 | "On, but ask a manager PIN" is offered only on the rows that really have it | node: `isCapValue` both ways, plus which rows carry `pin` | ✅ | refused on a non-pin row; 0 manager rows offer it; the waiter money rows do |
| P105035 | a person's own answer beats the restaurant's, and "default" falls back to it | node: `effectiveCap` with on / default / nothing | ✅ | on · off · off |
| P105036 | before the restaurant's settings have loaded, a row says nothing rather than guessing | node: `roleDefault` / `roleValueLabel` / `effectiveCap` with a null state | ✅ | null, three times — no invented "Default (on)" |
| P105037 | a row whose Feature half is off disappears from a person's page, and its sub-rows go with it | node: `capVisible` with `config.give_discounts.on = false` | ✅ | the manager row, its % ceiling, and the waiter row all vanish; all three return when it comes back |
| P105038 | every folder in the Manager section reaches a person's page | node: compare `mgrMenu` children against the three the walk names | ✅ | all three named today. The walk lists its folders where the waiter walk sweeps up leftovers — an asymmetry, listed as an improvement, not a fault |
| P105039 | with Payroll switched off, no rung of the pay ladder opens for anybody | node: `payAccessWith` for owner/admin/manager, module off and on | ✅ | all false when off; a blank manager gets record+edit but NOT see-pay |
| P105040 | the "this person has pay on record" refusal reads as English and counts right | node: 1, 4 and "couldn't check" | ✅ | "1 pay entry" / "4 pay entries" / no `-1` shown, and all three point at "Mark as left" |
| P105041 | when the pay ledger cannot be read, the login is KEPT rather than removed | node: a stub client that errors, one that counts 0, one that counts 3 | ✅ | blocked+count −1 · not blocked · blocked with 3 |
| P105042 | a manager can never be handed the job & pay SETUP, however the grants are written | node: four grant shapes including all three powers on | ✅ | `canEditJobPay` false every time |
| P105043 | a recorded payment must be a real amount and can never be dated in the future | node: 0, −5, "abc", 8 crore, tomorrow, an unknown mode, an unknown kind | ✅ | all seven refused; a good one rounds ₹1,250.559 → 1250.56 |
| P105044 | a pay period must name a month that exists | node: `2026-00`, `2026-13`, `2026-99` | ❌→✅ | **FIXED, item 2.** All three became a date Postgres refuses (`2026-13-01`), so the payment vanished behind "That payment didn't save." with nothing saying which field was wrong — from the function whose contract is that nothing else reaches the database. Nothing on screen sends this field today. Locked by `lib/staffProfileShared.test.mjs` |
| P105045 | an unknown pay type, employment type or mode is refused rather than quietly stored | node: five bad patches, then a good one | ✅ | all refused; "₹ 28,000" → 28000, weekly_off de-duplicated to `["mon","tue"]`, a `01-06-2025` date dropped rather than guessed |
| P105046 | an unknown personal-details key is dropped, an empty value clears the field, and a person cannot write the owner's private note | node: `mergeProfilePatch` with a stray key, with "", and against `SELF_PROFILE_FIELDS` | ✅ | the stray key never lands; `notes` and `id_verified` are refused to a person editing themselves |
| P105047 | a waiter never prints an invoice, and a column nobody listed stays ON | node: `resolveWaiterCaps` / `waiterCapValue` / `waiterConfigCapValue` | ✅ | `tablet_invoice` forced off; an unlisted column "on"; the walk-out row defaults to "ask a manager PIN" |
| P105048 | with the discount feature off for the restaurant, the tablet's own column is turned off too | node: `resolveWaiterCaps({tablet_discount:"on"}, {give_discounts:{on:false}})` | ✅ | off — and no `cap:…` key is invented in the settings object (no waiter cap-row shares a Feature half today; if one ever does, that branch would write one — listed as an improvement) |
| P105049 | every row name survives being put in a request header | node: `nodeExpect` + `expectHeader` over all 113 rows, plus an em dash by hand | ✅ | pure ASCII throughout; `expectHeader(null)` answers "null" rather than throwing |
| P105050 | `lib/accessModel.ts` carries no helper from the retired panel that nothing imports | node: every `export` in the file against every `import … from "@/lib/accessModel"` in the repo | ❌ | **NOT a fault and NOT fixed — an improvement, listed for his decision (item 3).** Six exports are imported; twelve are not (`GROUPS`, `PERM_BY_ID`, `PERM_BY_POWER`, `GROUP_BY_ID`, `permsOf`, `maxReach`, `NEW_POWER_FLAGS`, `AccessState`, `allowed`, `tabletValue`, `reachLevel`, `subState`) and `git log -S` shows no change to any of them since 2026-08-01 — they are the 4-rung ladder's display helpers, retired with the panel on 2026-07-31 |

## Structural audit run alongside the fifty (no id — it is one assertion over every row)

`nodeValue` → `nodePatch` → `applyPatch` → `nodeValue` round-trips for all 113 rows without a single
disagreement; no duplicate ids; every `choice`/`list`/`limit` default is one of that row's own options;
every tri-state default is a tri-state; `pin` appears only on waiter rows; every `has`/`capTablet` id is
in the write allow-list; `ancestorsOn` finds every row. **Clean.**

## Existing rows re-run (subject = a file I own) — 34 rows, 0 regressions

`T15` P07001 · P07002 · P07005 · P07007 · P07064 · P07071 · P07109 · P07128 · P07201 · P07222 ·
P22240 · P22298 · P22299 — all ✅.
`T25` P12198 · P12201 · P12202 · P12203 · P12271 · P12272 · P12487 — all ✅; P46270 stays ⏭ (still no
kitchen person on French House's staff list to open, so there is no kitchen ROW to look inside; the
rule itself is asserted by P105033 and the unit test).
`T23` P11276 · P11450 · P11451 · `T23-S8` P77160 · `T24` P11786 · `T29` P14036 ·
`T30` P29825 · P29826 · P29875 · `T9` P04343 · P19416 · `T7` P03291 · P03446 — all ✅.

Two of those came back red on the first pass and were **my harness being wrong, not the product**:
P22298's grep counted three guard scripts that READ the declaration by regex as if they re-typed it,
and P11450's counted a log label (`"owner/settings.modules"`) and a comment as if a panel read the
modules bag. Both re-ran green once the matcher was narrowed, and both are recorded here so the next
run does not repeat the false alarm.
