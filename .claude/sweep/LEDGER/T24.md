# Sweep 6 · Terminal 24 — the money and safety libraries

**Territory:** `lib/clash.ts` · `lib/paySplit.ts` · `lib/tax.ts` · `lib/taxFiling.ts` ·
`lib/idempotency.ts` · `lib/idempotencyRule.ts` · `lib/logTrail.ts` · `lib/userAuth.ts` ·
`lib/rateLimit.ts` · `docs/COMPLIANCE-GUARDRAILS.md` · `docs/SAAS-EFFICIENCY-PLAYBOOK.md`
(`lib/numbering*.ts` and `lib/compliance*.ts` do not exist — numbering lives in the database
functions and `docs/NUMBERING.md`; the compliance rules live in the doc above. Both are recorded
here so the next sweep does not go looking for them. `lib/clashCompare.ts` is read-only for me —
my prompt names `lib/clash.ts` only — so anything wrong in it is a handoff.)

**IDs P11501–P12000.** Permanent and global: never renumber them. The next sweep re-runs these
rows first and only then adds new ones.

## How the 500 are budgeted

| block | what it is | share | rows |
|---|---|---|---|
| 1 | reading the code for correctness | ~40% | P11501–P11700 (200) |
| 2 | conformance to the rules in CLAUDE.md and the docs | ~20% | P11701–P11800 (100) |
| 3 | watching it run (headless Playwright, port 4124) | ~15% | P11801–P11875 (75) |
| 4 | screenshots read at 1280×800 and A35 360×780 dpr3 | ~10% | P11876–P11925 (50) |
| 5 | tracing one change across panels | ~10% | P11926–P11975 (50) |
| 6 | my own judgment | ~5% | P11976–P12000 (25) |

**Per-file coverage:** tax 55 · paySplit 52 · clash 47 · clashCompare 8 · idempotency(+Rule) 35 ·
logTrail 23 · userAuth 34 · rateLimit 17 · taxFiling 14 · COMPLIANCE-GUARDRAILS 8 ·
SAAS-EFFICIENCY-PLAYBOOK 8 — plus the 200 cross-cutting run/screenshot/trace/judgment rows, every
one of which lands on at least one of those files.

**Reproducing the pure-rule and database rows:** `node scripts/verify-t24-money-rules.mjs`
(add `--db` for the rows that compare the TypeScript rule against the SQL function on the dev
database). It is committed with this ledger, so a stranger next month runs one command.


## 1. Reading the code for correctness (40%)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P11501 | lib/tax.ts effectiveTaxRate() returns exactly 0 when settings.price_tax_mode = 'composition' | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11502 | lib/tax.ts effectiveTaxRate() sums named tax_components and divides by 100 (12.5+2.5 → 0.15) | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11503 | lib/tax.ts a tax_component with a blank/whitespace label is ignored by the sum | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11504 | lib/tax.ts a tax_component with rate 0 is ignored by the sum | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11505 | lib/tax.ts a tax_component with a negative rate is ignored by the sum | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11506 | lib/tax.ts tax_components that is not an array falls through to the flat tax_rate | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11507 | lib/tax.ts tax_rate stored as exactly 0 falls back to 0.05 — the same NULLIF(tax_rate,0) rule as mig 119/272 | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11508 | lib/tax.ts tax_rate null/absent falls back to 0.05 | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11509 | lib/tax.ts tax_rate arriving as the string "0.12" is read as 0.12, not NaN | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11510 | lib/tax.ts effectiveTaxPct(0.0825) === 8.25 (two decimals of a percent, no float dust) | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11511 | lib/tax.ts effectiveTaxPct(0.05) === 5 exactly | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11512 | lib/tax.ts TAX_SETTINGS_COLUMNS names all three columns tax_rate, tax_components, price_tax_mode | read the named file and assert the stated line/behaviour | ✅ |  |
| P11513 | lib/tax.ts priceTaxMode() answers 'excl' for an unknown/garbage stored value | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11514 | lib/tax.ts priceTaxMode() passes 'incl' and 'composition' through unchanged | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11515 | lib/tax.ts itemTaxModesAllowed() is strictly === true, so the string "true" does NOT switch it on | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11516 | lib/tax.ts resolveTaxMode() answers 'exempt' for every line at a composition restaurant, whatever the dish says | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11517 | lib/tax.ts resolveTaxMode() ignores a dish's own mode entirely while item_tax_modes_allowed is off | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11518 | lib/tax.ts resolveTaxMode() honours a dish's 'excl' and 'incl' when the master switch is on | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11519 | lib/tax.ts resolveTaxMode() maps a dish mode of 'none' to 'exempt' | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11520 | lib/tax.ts resolveTaxMode() maps 'mrp' to 'incl' when mrp_tax_treatment = 'inclusive' | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11521 | lib/tax.ts resolveTaxMode() maps 'mrp' to 'exempt' for any other mrp_tax_treatment | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11522 | lib/tax.ts resolveTaxMode() tests its cases in the SAME order as lfh_resolve_tax_mode (mig 269/270) | read the named file and assert the stated line/behaviour | ✅ |  |
| P11523 | lib/tax.ts isMrpDish() is false while item_tax_modes_allowed is off, so no MRP stamp prints | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11524 | lib/tax.ts splitBill() strips currency symbols and stray text out of a price | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11525 | lib/tax.ts splitBill() floors qty at 1 (a 0 or negative qty still charges one) | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11526 | lib/tax.ts splitBill() rounds PER LINE, matching lfh_split_items_tax, not once at the end | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11527 | lib/tax.ts splitBill() an 'incl' line divides by (1+rate) and rounds to 2dp before accumulating | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11528 | lib/tax.ts splitBill() an 'exempt' line lands in nontaxAmount and never in taxableBase | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11529 | lib/tax.ts splitBill() an is_mrp line is counted into mrpAmount AS WELL AS its own mode bucket | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11530 | lib/tax.ts splitBill() subtotal === taxableBase + nontaxAmount, always | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11531 | lib/tax.ts splitBill() discountBase === taxableBase when the rate is above zero | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11532 | lib/tax.ts splitBill() discountBase === subtotal − mrpAmount when the rate is zero | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11533 | lib/tax.ts splitBill() clamps the discount to discountBase and never below zero | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11534 | lib/tax.ts splitBill() taxable === taxableBase − discount when the rate is above zero (discount BEFORE tax) | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11535 | lib/tax.ts splitBill() tax === round(taxable × rate, 2) | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11536 | lib/tax.ts splitBill() total === taxable + tax + nontaxAmount when the rate is above zero | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11537 | lib/tax.ts splitBill() total === subtotal − discount when the rate is zero | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11538 | lib/tax.ts splitBill() the identity subtotal − discount + tax === total holds on every taxed case | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11539 | lib/tax.ts maxDiscount() equals splitBill(lines, settings, 0).discountBase | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11540 | lib/tax.ts splitBill() reports composition:true only for a composition restaurant | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11541 | lib/tax.ts splitBill([]) returns all zeros and never NaN | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11542 | lib/tax.ts an unparseable price contributes 0 to the bill, never NaN (a NaN total would print blank) | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11543 | lib/tax.ts a qty of "abc" becomes 1, never NaN | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11544 | lib/tax.ts splitBill()'s taxable_base / nontax_amount / mrp_amount equal lfh_split_items_tax()'s for the same items | node scripts/verify-t24-money-rules.mjs --db — compares the TS rule against the SQL function on the dev DB, French House | ✅ |  |
| P11545 | lib/tax.ts effectiveTaxRate(settings) equals lfh_effective_tax_rate(rid) for French House at the database | node scripts/verify-t24-money-rules.mjs --db — compares the TS rule against the SQL function on the dev DB, French House | ✅ |  |
| P11546 | lib/paySplit.ts badSplitShape() refuses fewer than two parts | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11547 | lib/paySplit.ts badSplitShape() refuses more than twelve parts | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11548 | lib/paySplit.ts badSplitShape() refuses a part whose amount is zero or negative | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11549 | lib/paySplit.ts badSplitShape() refuses a payment method that is not in PAYMENT_METHODS | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11550 | lib/paySplit.ts badSplitShape() refuses a part note longer than 200 characters | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11551 | lib/paySplit.ts badSplitShape() accepts a well-formed two-part split | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11552 | lib/paySplit.ts badSplitShape() refuses anything that is not an array | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11553 | lib/paySplit.ts badSplitShape() refuses a NaN / non-numeric amount | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11554 | lib/paySplit.ts settleBillInParts() scopes its order read by restaurant_id | read the named file and assert the stated line/behaviour | ✅ |  |
| P11555 | lib/paySplit.ts settleBillInParts() excludes cancelled orders from the bill | read the named file and assert the stated line/behaviour | ✅ |  |
| P11556 | lib/paySplit.ts settleBillInParts() excludes 'received' (not-yet-accepted) orders | read the named file and assert the stated line/behaviour | ✅ |  |
| P11557 | lib/paySplit.ts settleBillInParts() excludes orders already marked paid | read the named file and assert the stated line/behaviour | ✅ |  |
| P11558 | lib/paySplit.ts settleBillInParts() excludes soft-deleted orders (deleted_at IS NULL filter) | read the named file and assert the stated line/behaviour | ✅ |  |
| P11559 | lib/paySplit.ts settleBillInParts() prefers the table's OPEN session, falling back to un-archived orders on that table number | read the named file and assert the stated line/behaviour | ✅ |  |
| P11560 | lib/paySplit.ts settleBillInParts() refuses with 409 at the 400-order cap instead of quietly collecting for a partial set | read the named file and assert the stated line/behaviour | ✅ |  |
| P11561 | lib/paySplit.ts settleBillInParts() refuses plainly when there is nothing left to settle | read the named file and assert the stated line/behaviour | ✅ |  |
| P11562 | lib/paySplit.ts settleBillInParts() refuses plainly when the table has no live bill session | read the named file and assert the stated line/behaviour | ✅ |  |
| P11563 | lib/paySplit.ts the due is built from taxable_base, not from Σsubtotal (migs 270/272) | read the named file and assert the stated line/behaviour | ✅ |  |
| P11564 | lib/paySplit.ts a legacy order with taxable_base NULL falls back to its subtotal with no untaxed part | read the named file and assert the stated line/behaviour | ✅ |  |
| P11565 | lib/paySplit.ts the rate for each order comes from BILLDOC.orderTaxRate — the paper's own definition, not a second copy | read the named file and assert the stated line/behaviour | ✅ |  |
| P11566 | lib/paySplit.ts tax is bucketed per rate, so a mixed-rate bill rounds once per rate and not once per order | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | FIXED — the ±2p gate read the raw amounts while the legs were stored rounded; one rounding now feeds both (problem 2) |
| P11567 | lib/paySplit.ts the discount is clamped to its own base exactly as the printed bill clamps it | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11568 | lib/paySplit.ts due === subtotal − discount + tax, the one formula that also holds at a zero rate | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11569 | lib/paySplit.ts the ±0.02 tolerance is measured against the server's recomputed due, never the browser's number | read the named file and assert the stated line/behaviour | ✅ |  |
| P11570 | lib/paySplit.ts the refusal names BOTH figures, so a waiter can see what to change | read the named file and assert the stated line/behaviour | ✅ |  |
| P11571 | lib/paySplit.ts each leg amount is rounded to 2dp before it is stored | read the named file and assert the stated line/behaviour | ✅ |  |
| P11572 | lib/paySplit.ts every leg carries session_id AND restaurant_id | read the named file and assert the stated line/behaviour | ✅ |  |
| P11573 | lib/paySplit.ts the split note is truncated to 200 characters at both places it is written | read the named file and assert the stated line/behaviour | ✅ |  |
| P11574 | lib/paySplit.ts the orders are marked paid with payment_method 'Split' — one bill, several parts | read the named file and assert the stated line/behaviour | ✅ |  |
| P11575 | lib/paySplit.ts the orders update is scoped by restaurant_id as well as by id | read the named file and assert the stated line/behaviour | ✅ |  |
| P11576 | lib/paySplit.ts an insert error answers 500 rather than reporting a settle that did not happen | read the named file and assert the stated line/behaviour | ✅ |  |
| P11577 | lib/paySplit.ts reverseSplitLegs() STAMPS a leg reversed rather than deleting it (mig 285) | read the named file and assert the stated line/behaviour | ✅ |  |
| P11578 | lib/paySplit.ts reverseSplitLegs() scopes by session_id and restaurant_id | read the named file and assert the stated line/behaviour | ✅ |  |
| P11579 | lib/paySplit.ts reverseSplitLegs() only touches legs that are not already reversed | read the named file and assert the stated line/behaviour | ✅ |  |
| P11580 | lib/paySplit.ts reverseSplitLegs() only touches legs created at or after `since`, leaving older legs standing | read the named file and assert the stated line/behaviour | ✅ |  |
| P11581 | lib/paySplit.ts reverseSplitLegs() returns the count and the amount so the caller can log honestly | read the named file and assert the stated line/behaviour | ✅ |  |
| P11582 | lib/paySplit.ts reverseSplitLegs() THROWS when the update fails — a money trail that cannot be corrected must be noticed | read the named file and assert the stated line/behaviour | ✅ |  |
| P11583 | no route re-implements the pay-in-parts arithmetic — settleBillInParts is the only place it exists | grep the repo and assert the stated invariant | ✅ |  |
| P11584 | both the manager panel route and the waiter tablet route call settleBillInParts and reverseSplitLegs | grep the repo and assert the stated invariant | ✅ |  |
| P11585 | settling in parts cannot lower, hide or erase a sale — the due is recomputed server-side from stored rows | read the named file and assert the stated line/behaviour | ✅ |  |
| P11586 | lib/clash.ts clashJson() answers HTTP 409 with { error, clash } in the shape outbox.js expects | read the named file and assert the stated line/behaviour | ✅ |  |
| P11587 | lib/clash.ts replayMarkers() returns null when the x-lfh-replay header is absent | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11588 | lib/clash.ts replayMarkers() returns null when x-lfh-queued-at is absent | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11589 | lib/clash.ts replayMarkers() returns null for an unparseable queued-at date | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11590 | lib/clash.ts replayMarkers() returns null under the 20s replay age — the live write path is untouched | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11591 | lib/clash.ts replayMarkers() returns the queuedAt for a genuinely aged replay | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11592 | lib/clash.ts expectClash() returns null when no x-lfh-expect header travels | read the named file and assert the stated line/behaviour | ✅ |  |
| P11593 | lib/clash.ts expectClash() returns null for malformed JSON in that header | read the named file and assert the stated line/behaviour | ✅ |  |
| P11594 | lib/clash.ts expectClash() returns null when the header parses to an array | read the named file and assert the stated line/behaviour | ✅ |  |
| P11595 | lib/clash.ts expectClash() returns null for a table that is not on COMPARABLE_TABLES | read the named file and assert the stated line/behaviour | ✅ |  |
| P11596 | lib/clash.ts expectClash() returns null when `fields` is missing or not an object | read the named file and assert the stated line/behaviour | ✅ |  |
| P11597 | lib/clash.ts only the columns listed in COMPOSITE_KEYS may be used for a composite lookup | read the named file and assert the stated line/behaviour | ✅ |  |
| P11598 | lib/clash.ts a half-specified composite key is ignored entirely rather than matching the wrong row | read the named file and assert the stated line/behaviour | ✅ |  |
| P11599 | lib/clash.ts every field name is validated against a strict pattern before it reaches the select | read the named file and assert the stated line/behaviour | ✅ |  |
| P11600 | lib/clash.ts at most 8 fields are compared per expectation | read the named file and assert the stated line/behaviour | ✅ |  |
| P11601 | lib/clash.ts the select asks only for real columns, dedup'd, with the jsonb sub-key read out of the object | read the named file and assert the stated line/behaviour | ✅ |  |
| P11602 | lib/clash.ts settings and restaurants answer 'nothing to compare' for an id that is not the caller's own restaurant | read the named file and assert the stated line/behaviour | ✅ |  |
| P11603 | lib/clash.ts every other table's comparison is scoped by restaurant_id | read the named file and assert the stated line/behaviour | ✅ |  |
| P11604 | lib/clash.ts a missing row reads as 'nothing to overwrite', which is the first-counter case for inventory | read the named file and assert the stated line/behaviour | ✅ |  |
| P11605 | lib/clash.ts a jsonb sub-key like profile.notes is compared, not the whole blob | read the named file and assert the stated line/behaviour | ✅ |  |
| P11606 | lib/clash.ts a money column's refusal says the value MOVED without quoting the figure | read the named file and assert the stated line/behaviour | ✅ |  |
| P11607 | lib/clash.ts an object's refusal never prints the literal [object Object] | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11608 | lib/clash.ts a boolean reads 'on' / 'off' in the refusal, never 'true' / 'false' | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11609 | lib/clash.ts the tri-state 'pin' reads 'on, but asking for a manager PIN' | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11610 | lib/clash.ts an empty/absent current value reads 'nothing' | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11611 | lib/clash.ts readable() has a plain-words name for every column a live call site actually sends | grep the repo and assert the stated invariant | ✅ |  |
| P11612 | lib/clash.ts the whole expectation check fails OPEN on any lookup error | read the named file and assert the stated line/behaviour | ✅ |  |
| P11613 | lib/clash.ts replayClash() returns null for a live write | read the named file and assert the stated line/behaviour | ✅ |  |
| P11614 | lib/clash.ts replayClash() returns null when the action is not table-scoped or cannot be resolved | read the named file and assert the stated line/behaviour | ✅ |  |
| P11615 | lib/clash.ts replayClash() refuses when a different party is on that table now | read the named file and assert the stated line/behaviour | ✅ |  |
| P11616 | lib/clash.ts replayClash() refuses a closed table with no closed_at, because it cannot prove who was first | read the named file and assert the stated line/behaviour | ✅ |  |
| P11617 | lib/clash.ts replayClash() refuses when the table was closed AFTER the person acted | read the named file and assert the stated line/behaviour | ✅ |  |
| P11618 | lib/clash.ts replayClash() allows the write when the table closed BEFORE the person acted | read the named file and assert the stated line/behaviour | ✅ |  |
| P11619 | lib/clash.ts tableLabel() reads settings.table_names live and falls back to 'Table N' | read the named file and assert the stated line/behaviour | ✅ |  |
| P11620 | lib/clash.ts every refusal is retryable:false — resending the same thing cannot help | read the named file and assert the stated line/behaviour | ✅ |  |
| P11621 | lib/clashCompare.ts stableJson() sorts object keys at every level | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11622 | lib/clashCompare.ts stableJson() is depth-limited and fails OPEN past the limit | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11623 | lib/clashCompare.ts sameValue() trims text before comparing | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11624 | lib/clashCompare.ts sameValue() compares lists as SETS (allergen order is not meaningful) | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11625 | lib/clashCompare.ts sameValue() compares objects by content, so a re-serialised blob is equal | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11626 | lib/clashCompare.ts sameValue() treats null/absent and {} as the same, so an unset column invents no clash | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11627 | lib/clashCompare.ts isPlainObject() rejects arrays | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11628 | lib/clash.ts imports that comparator instead of keeping a private copy | grep the repo and assert the stated invariant | ✅ |  |
| P11629 | lib/idempotency.ts a request with no X-LFH-Action-Id is passed straight through, unchanged | read the named file and assert the stated line/behaviour | ✅ |  |
| P11630 | lib/idempotency.ts a fresh claim runs the handler exactly once | read the named file and assert the stated line/behaviour | ✅ |  |
| P11631 | lib/idempotency.ts a duplicate of a completed action echoes the STORED result (the original order id) | read the named file and assert the stated line/behaviour | ✅ |  |
| P11632 | lib/idempotency.ts …and does not run the handler a second time | read the named file and assert the stated line/behaviour | ✅ |  |
| P11633 | lib/idempotency.ts a concurrent duplicate answers 409 sync_in_progress with retry:true | read the named file and assert the stated line/behaviour | ✅ |  |
| P11634 | lib/idempotency.ts a claim older than 30s is treated as a crashed attempt and taken over | read the named file and assert the stated line/behaviour | ✅ |  |
| P11635 | lib/idempotency.ts only a unique-violation (23505) reads the claim row back | read the named file and assert the stated line/behaviour | ✅ |  |
| P11636 | lib/idempotency.ts any other database error fails OPEN and runs the write | read the named file and assert the stated line/behaviour | ✅ |  |
| P11637 | lib/idempotency.ts a handler that throws releases the claim before rethrowing | read the named file and assert the stated line/behaviour | ✅ |  |
| P11638 | lib/idempotency.ts a 4xx reply releases the claim so the person's next try genuinely retries | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11639 | lib/idempotency.ts a 200 carrying ok:false releases the claim (the guest sold-out / rate-limited case) | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11640 | lib/idempotency.ts a stored refusal self-heals instead of replaying the old refusal forever | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11641 | lib/idempotency.ts the healed row is REUSED as a fresh claim rather than deleted, so the retry is still protected | read the named file and assert the stated line/behaviour | ✅ |  |
| P11642 | lib/idempotency.ts the stored result is returned per call, never through a shared module variable | read the named file and assert the stated line/behaviour | ✅ |  |
| P11643 | lib/idempotency.ts the duplicate reply carries duplicate:true so the client can still track the order | read the named file and assert the stated line/behaviour | ✅ |  |
| P11644 | lib/idempotency.ts a non-JSON response stores nothing rather than throwing | read the named file and assert the stated line/behaviour | ✅ |  |
| P11645 | lib/idempotency.ts the response is cloned, so the caller can still read the original body | read the named file and assert the stated line/behaviour | ✅ |  |
| P11646 | lib/idempotency.ts the claims-table prune is opportunistic (1 in 200) and never a timer on idle data | read the named file and assert the stated line/behaviour | ✅ |  |
| P11647 | lib/idempotency.ts the prune is fire-and-forget and fully swallowed — tidying can never affect a write | read the named file and assert the stated line/behaviour | ✅ |  |
| P11648 | lib/idempotencyRule.ts didSomething() treats any status ≥ 400 as nothing done | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11649 | lib/idempotencyRule.ts storedIsRefusal() answers the same question about an already-stored body | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11650 | lib/idempotencyRule.ts has no imports at all, so verify:order-retry executes the rule that ships | read the named file and assert the stated line/behaviour | ✅ |  |
| P11651 | every staff panel write route (editor POST/PATCH/DELETE, kitchen, tablet, inventory, panel-profile) is wrapped in withIdempotency | grep the repo and assert the stated invariant | ✅ |  |
| P11652 | the guest place-order and call-waiter routes are wrapped too | grep the repo and assert the stated invariant | ✅ |  |
| P11653 | a replayed settle cannot charge a bill twice — the claim is taken before the handler runs | read the named file and assert the stated line/behaviour | ✅ |  |
| P11654 | lib/logTrail.ts every action code the app can WRITE resolves to a real area+screen, not System › Other | npm run verify:<name> and read its output | ✅ | GUARDED — nothing stopped a new action code reading 'System › Other'; verify:t24-money-rules now fails on it (improvement 1) |
| P11655 | lib/logTrail.ts an action code nobody has placed lands in System › Other — honest, not invented | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11656 | lib/logTrail.ts placeOf() never throws on null, undefined or an empty string | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11657 | lib/logTrail.ts an explicit PLACE entry wins over the prefix rules | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11658 | lib/logTrail.ts the PREFIX rules are ordered and the first match wins | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11659 | lib/logTrail.ts panelName() maps 'editor' to 'Manager panel' (the old name for it) | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11660 | lib/logTrail.ts an unknown panel prints itself capitalised rather than vanishing | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11661 | lib/logTrail.ts targetOf() prefers table_number over everything else | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11662 | lib/logTrail.ts a non-numeric table_number (a named table) prints as it stands | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11663 | lib/logTrail.ts a quoted run inside `detail` becomes the target | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11664 | lib/logTrail.ts a bill number inside `detail` becomes 'Bill #N' | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11665 | lib/logTrail.ts order_id falls back to its first 8 characters | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11666 | lib/logTrail.ts trailOf() drops a missing restaurant from the crumbs instead of printing a blank one | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11667 | lib/logTrail.ts `short` is screen · target — the two crumbs that actually narrow a list row down | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11668 | lib/logTrail.ts is client-safe: it imports nothing that reaches the server | read the named file and assert the stated line/behaviour | ✅ |  |
| P11669 | lib/userAuth.ts normalizeLoginName() trims, lowercases and collapses inner whitespace | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11670 | lib/userAuth.ts loginUser() refuses an empty name or password with reason 'empty' | read the named file and assert the stated line/behaviour | ✅ |  |
| P11671 | lib/userAuth.ts loginUser() caps the username and password length BEFORE the slow hash | read the named file and assert the stated line/behaviour | ✅ |  |
| P11672 | lib/userAuth.ts a failed candidate lookup is reported as transient, never as wrong credentials | read the named file and assert the stated line/behaviour | ✅ |  |
| P11673 | lib/userAuth.ts loginUser() excludes recycle-bin rows (deleted_at IS NULL) | read the named file and assert the stated line/behaviour | ✅ |  |
| P11674 | lib/userAuth.ts disabled rows are fetched only so a disabled person can be told the truth | read the named file and assert the stated line/behaviour | ✅ |  |
| P11675 | lib/userAuth.ts on the tenant door only that restaurant's people can match | read the named file and assert the stated line/behaviour | ✅ |  |
| P11676 | lib/userAuth.ts an owner can sign in at a restaurant they OWN via the restaurant_owners join | read the named file and assert the stated line/behaviour | ✅ |  |
| P11677 | lib/userAuth.ts a lockout on any matching live row is honoured, so a colliding name cannot dodge it | read the named file and assert the stated line/behaviour | ✅ |  |
| P11678 | lib/userAuth.ts a wrong password bumps failed_count on every live match | read the named file and assert the stated line/behaviour | ✅ |  |
| P11679 | lib/userAuth.ts five wrong tries lock the account for 60 seconds and reset the counter | read the named file and assert the stated line/behaviour | ✅ |  |
| P11680 | lib/userAuth.ts a correct password clears failed_count and locked_until | read the named file and assert the stated line/behaviour | ✅ |  |
| P11681 | lib/userAuth.ts a disabled person is told so ONLY on a verified password, so name-guessing learns nothing | read the named file and assert the stated line/behaviour | ✅ |  |
| P11682 | lib/userAuth.ts when only disabled rows exist and the password is wrong, the answer is the generic one | read the named file and assert the stated line/behaviour | ✅ |  |
| P11683 | lib/userAuth.ts hashSecret() produces a self-describing pbkdf2$iters$salt$hash string | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11684 | lib/userAuth.ts verifySecret() compares in constant time via safeEqual | read the named file and assert the stated line/behaviour | ✅ |  |
| P11685 | lib/userAuth.ts verifySecret() still accepts a legacy bare sha256 hash | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11686 | lib/userAuth.ts the cookie signature covers id:role:token_version:iat, so a role change or a token bump ends it | read the named file and assert the stated line/behaviour | ✅ |  |
| P11687 | lib/userAuth.ts userFromCookie() rejects a value that is not exactly three dot-separated parts | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11688 | lib/userAuth.ts userFromCookie() rejects a cookie older than the 7-day max age | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11689 | lib/userAuth.ts userFromCookie() retries the lookup once (~120ms) before giving up on a flap | read the named file and assert the stated line/behaviour | ✅ |  |
| P11690 | lib/userAuth.ts userFromCookie() throws AuthDbError on a sustained lookup failure, so gates answer 503 not 401 | read the named file and assert the stated line/behaviour | ✅ |  |
| P11691 | lib/userAuth.ts roleSatisfies(): owner covers everything, manager covers kitchen+tablet, the two devices do not cover each other | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11692 | lib/userAuth.ts requireRole() checks the STAFF cookie before the admin fallback (the cross-restaurant scoping fault of 2026-07-03) | read the named file and assert the stated line/behaviour | ✅ |  |
| P11693 | lib/userAuth.ts requireRole() re-checks the panel entitlement and the recycle bin on every request, not only at login | read the named file and assert the stated line/behaviour | ✅ |  |
| P11694 | lib/rateLimit.ts rateAllowed() fails OPEN on an RPC error — a limiter glitch can never lock a restaurant out | read the named file and assert the stated line/behaviour | ✅ |  |
| P11695 | lib/rateLimit.ts rateAllowed() returns true immediately for an empty subject | read the named file and assert the stated line/behaviour | ✅ |  |
| P11696 | lib/rateLimit.ts the two extra reads that name WHO and WHERE happen only on the rare wall-hit path | read the named file and assert the stated line/behaviour | ✅ |  |
| P11697 | lib/rateLimit.ts subjectFor() normalises a login name so '  Ravi ' and 'ravi' share one counter | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ |  |
| P11698 | lib/rateLimit.ts rateResetOnSuccess() wipes the counter and marks the open event handled, never deleting the record | read the named file and assert the stated line/behaviour | ✅ |  |
| P11699 | lib/rateLimit.ts pingLatestGuestLimit() confirms a REAL recent open event, scoped by restaurant, before pinging | read the named file and assert the stated line/behaviour | ✅ |  |
| P11700 | lib/rateLimit.ts recordAlert() is warn-only: it records and pings but never blocks anyone | read the named file and assert the stated line/behaviour | ✅ |  |

## 2. Conformance to the project's written rules (20%)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P11701 | every settings select that feeds the tax helpers also reads price_tax_mode (TAX_SETTINGS_COLUMNS) | run the named npm run verify:* guard and read its output | ✅ |  |
| P11702 | no file hardcodes a 5% / 0.05 tax rate outside a documented fallback | grep the repo and assert the stated invariant | ✅ |  |
| P11703 | lib/tax.ts is the only TypeScript definition of the effective rate — no second copy in a route or a page | grep the repo and assert the stated invariant | ✅ |  |
| P11704 | lib/tax.ts's rate rule and mig 272's lfh_effective_tax_rate agree on composition, components, zero and blank | node scripts/verify-t24-money-rules.mjs --db — the TS rule against the SQL function, French House | ✅ |  |
| P11705 | lib/tax.ts's splitBill and mig 272's lfh_split_items_tax agree line-for-line on a mixed excl/incl/exempt/MRP bill | node scripts/verify-t24-money-rules.mjs --db — the TS rule against the SQL function, French House | ✅ |  |
| P11706 | the discount cap in lib/tax.ts matches lfh_order_discount_base (mig 272) for both rate>0 and rate=0 | node scripts/verify-t24-money-rules.mjs --db — the TS rule against the SQL function, French House | ✅ |  |
| P11707 | a discount is applied BEFORE tax everywhere in lib/tax.ts (taxable = base − discount) | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ |  |
| P11708 | a composition restaurant's bill carries no tax line, because its effective rate genuinely IS zero | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ |  |
| P11709 | MRP is tracked apart from 'merely untaxed' — only the MRP part is locked against a discount | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ |  |
| P11710 | a nil-rated (exempt, non-MRP) dish can still be discounted at a zero-rate restaurant | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ |  |
| P11711 | lib/paySplit.ts makes NO permission decision of its own — each caller keeps its own gate | read the named file and assert the stated rule | ⏭ | ⏭ — lib/paySplit.ts makes no permission decision, so there is nothing here to assert beyond the two callers' own gates, which are checked at P11712 and P11722 |
| P11712 | the manager route's split call and the tablet route's split call reach the same function with the same arguments | grep the repo and assert the stated invariant | ✅ |  |
| P11713 | no panel JavaScript computes the split due itself and sends it as the truth | grep the repo and assert the stated invariant | ✅ |  |
| P11714 | a split settle writes one session_payments row per part, so 'how did table 6 pay?' has a real answer | read the named file and assert the stated rule | ✅ |  |
| P11715 | a reverted split settle STAMPS its legs (mig 285) — no money record is ever deleted | read the named file and assert the stated rule | ✅ |  |
| P11716 | the split path selects an explicit column list, scoped by restaurant_id, with a limit (egress rules) | read the named file and assert the stated rule | ✅ |  |
| P11717 | a split settle is reached through a route wrapped in withIdempotency, so a replay settles once | grep the repo and assert the stated invariant | ✅ |  |
| P11718 | the split refusal branches on a server reason CODE and a plain sentence, never on prose the client parses | read the named file and assert the stated rule | ✅ |  |
| P11719 | settling in parts leaves the sale in the Z-report and the GST report — nothing is hidden (compliance §3.0) | read the named file and assert the stated rule | ✅ |  |
| P11720 | no code path in lib/paySplit.ts can delete an order, an invoice or a bill | grep the repo and assert the stated invariant | ✅ |  |
| P11721 | the split due honours orders.tax_rate (mig 284), so a banquet is not asked for at the dine-in rate | read the named file and assert the stated rule | ✅ |  |
| P11722 | lib/paySplit.ts's rate resolution and public/panels/billdoc.js's orderTaxRate are the same function, not two rules | grep the repo and assert the stated invariant | ✅ |  |
| P11723 | npm run verify:clash-coverage is green — every value edit it checks tells the server what it was editing from | run the named npm run verify:* guard and read its output | ✅ | FIXED — the parts land before the paid stamp with no transaction across the two; a failed stamp now REVERSES the parts it recorded (problem 1) |
| P11724 | npm run verify:clash-coverage names the areas it deliberately does NOT cover, rather than implying full coverage | run the named npm run verify:* guard and read its output | ✅ |  |
| P11725 | every table a live call site names in an expectation is present in COMPARABLE_TABLES | run the named npm run verify:* guard and read its output | ✅ |  |
| P11726 | the clash gate refuses with 409 and a plain sentence, so the loser is TOLD (first save wins) | read the named file and assert the stated rule | ✅ |  |
| P11727 | the clash gate is called once per panel write dispatcher, beside the section and replay gates | grep the repo and assert the stated invariant | ✅ |  |
| P11728 | adding protection to a new feature is one line at the CALL SITE — no new server code is needed | read the named file and assert the stated rule | ✅ |  |
| P11729 | a clash refusal never leaves a tap unresolved: it answers 409 with plain, actionable words | read the named file and assert the stated rule | ✅ |  |
| P11730 | the clash gate cannot be pointed at a table it has no business reading (allowlist only) | read the named file and assert the stated rule | ✅ |  |
| P11731 | the clash gate cannot be turned into a free-form filter over a table (COMPOSITE_KEYS only) | read the named file and assert the stated rule | ✅ | IMPROVED — the open-event read and the counter reset are now restaurant-scoped (improvement 3) |
| P11732 | a refusal quotes the value it FOUND, which is why the tenant-row tables refuse a foreign id outright | read the named file and assert the stated rule | ✅ |  |
| P11733 | the refusal wording uses the words on the screen (on/off, the counted quantity, the par level) | read the named file and assert the stated rule | ✅ |  |
| P11734 | npm run verify:order-retry proves the clash gate uses the shared comparator, not a copy | run the named npm run verify:* guard and read its output | ✅ |  |
| P11735 | npm run verify:order-retry is green | run the named npm run verify:* guard and read its output | ✅ |  |
| P11736 | every staff write goes through the panel api() carrying X-LFH-Action-Id (the outbox helper sets it) | grep the repo and assert the stated invariant | ✅ |  |
| P11737 | a queued write that arrives twice is answered from the store, so a bill is settled once | read the named file and assert the stated rule | ✅ |  |
| P11738 | a refusal is never remembered as a success — the person can fix the cause and try again | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ |  |
| P11739 | the claims table is pruned, so it cannot grow forever (mig 268) | run the named npm run verify:* guard and read its output | ✅ |  |
| P11740 | the prune runs opportunistically and never as a timer touching idle data | run the named npm run verify:* guard and read its output | ✅ |  |
| P11741 | the idempotency layer fails OPEN — a helper table hiccup can never stop a restaurant working | read the named file and assert the stated rule | ✅ |  |
| P11742 | withIdempotency is applied at the ROUTE export, so no branch inside a dispatcher can skip it | grep the repo and assert the stated invariant | ✅ |  |
| P11743 | a 5xx or a timeout is queued like offline while a 4xx tells the person (the busy-server rule) | grep the repo and assert the stated invariant | ✅ |  |
| P11744 | every action code with a label on the admin/owner screens also resolves to an area and a screen | run the named npm run verify:* guard and read its output | ✅ |  |
| P11745 | a new action code cannot silently read 'System › Other' — a guard fails when its place is missing | run the named npm run verify:* guard and read its output | ✅ |  |
| P11746 | every AREA name in lib/logTrail.ts is words a restaurant uses, not a database word | read the named file and assert the stated rule | ✅ |  |
| P11747 | every SCREEN name matches what the panel actually calls that screen | read the named file and assert the stated rule | ✅ |  |
| P11748 | the trail says restaurant › panel › area › screen › target — the owner's 2026-08-12 ask, in that order | read the named file and assert the stated rule | ✅ |  |
| P11749 | the trail is DERIVED, so the 30,583 rows already in the table have a path too | read the named file and assert the stated rule | ✅ |  |
| P11750 | placeOf() files 'Direct database edit' under System, so a row nobody clicked is not attributed to a person | read the named file and assert the stated rule | ✅ |  |
| P11751 | print_sent files under the manager panel's Kitchen tickets, and print_helper_* under the console's Printing | read the named file and assert the stated rule | ✅ |  |
| P11752 | lib/userAuth.ts never returns the sensitive failure reason to the person — only the generic sentence | read the named file and assert the stated rule | ✅ |  |
| P11753 | the sensitive reason is returned so the ROUTE can record it in the admin log ('who tried what was lacking') | read the named file and assert the stated rule | ✅ |  |
| P11754 | a login failure and an unknown name give the same answer, so no account list can be built by trying names | read the named file and assert the stated rule | ✅ |  |
| P11755 | hiding is never the only guard: requireRole re-checks entitlement server-side on every request | read the named file and assert the stated rule | ✅ |  |
| P11756 | the admin's per-tab ?rid pin only works with a valid admin cookie; without it nothing changes for real staff | read the named file and assert the stated rule | ✅ |  |
| P11757 | the SESSION_SECRET fallback is warned about once per server start, so a missing one is visible in the logs | read the named file and assert the stated rule | ✅ |  |
| P11758 | the presence heartbeat is throttled (~45s) and fire-and-forget, so it never blocks a call | read the named file and assert the stated rule | ✅ |  |
| P11759 | describeLoginTarget() reads scoped, column-listed, limited queries (egress rules) and never throws | read the named file and assert the stated rule | ✅ |  |
| P11760 | describeLoginTarget() runs ONLY on the wall-hit path, so a normal login does no extra read | read the named file and assert the stated rule | ✅ |  |
| P11761 | lib/rateLimit.ts is never called by a test helper directly — the sweep signs in once via loginAs() | grep the repo and assert the stated invariant | ✅ |  |
| P11762 | this terminal made at most one real sign-in for the whole run (loginRequestCount) | read the named file and assert the stated rule | ✅ |  |
| P11763 | the staff-login limit alert is the ONE silent alert; every other limit ping is audible | read the named file and assert the stated rule | ✅ |  |
| P11764 | a successful sign-in resets the login counter, so a shared waiter tablet cannot build up to a wall | read the named file and assert the stated rule | ✅ | FIXED — credit_note / order_delete done from the console pointed at a manager-panel screen; placeOf() now takes the panel (problem 3) |
| P11765 | the limit alert names WHO, WHICH restaurant, WHICH kind of login and HOW MANY tries | read the named file and assert the stated rule | ✅ |  |
| P11766 | a guest-side limit ping is confirmed against a real DB event first — a client beacon cannot invent one | read the named file and assert the stated rule | ✅ |  |
| P11767 | an alert send can never hang a staff request: the alert fetch carries a timeout | grep the repo and assert the stated invariant | ✅ |  |
| P11768 | the admin-login alert promises no wait, because nobody is locked out by it (mig 208) | read the named file and assert the stated rule | ✅ |  |
| P11769 | this run created no rate_limit_events rows of its own, and cleaned up anything it did create | read the named file and assert the stated rule | ✅ |  |
| P11770 | nothing in this run could raise a real notification on the owner's phone (backup stacks are silent) | grep the repo and assert the stated invariant | ✅ |  |
| P11771 | lib/taxFiling.ts splitTax() parts always sum to exactly p2(target) — CGST + SGST = total tax | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ |  |
| P11772 | lib/taxFiling.ts splitTax() gives the LAST line the remainder, the same rule billdoc.js prints by | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ |  |
| P11773 | lib/taxFiling.ts splitTax([]) returns an empty list rather than throwing | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ |  |
| P11774 | lib/taxFiling.ts splitTax() handles a negative target without drifting | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ |  |
| P11775 | lib/taxFiling.ts allocateWhole() returns integers that sum to exactly Math.round(total) | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ |  |
| P11776 | lib/taxFiling.ts allocateWhole() uses largest-remainder, so no row is short a rupee | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ |  |
| P11777 | lib/taxFiling.ts allocateWhole() with all-zero weights puts the total on the first row rather than losing it | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ |  |
| P11778 | lib/taxFiling.ts allocateWhole() handles a negative total (a refund-heavy period) and still sums exactly | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ |  |
| P11779 | lib/taxFiling.ts taxableValue() recovers the base from tax ÷ rate and caps it at net sales | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ |  |
| P11780 | lib/taxFiling.ts taxableValue() returns net sales when no rate is configured (composition) | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ |  |
| P11781 | lib/taxFiling.ts exemptTolerance() scales with the number of bills, floor ₹100 | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ |  |
| P11782 | lib/taxFiling.ts exemptIsMaterial() is answered ONCE from the totals and handed to every row | read the named file and assert the stated rule | ✅ |  |
| P11783 | lib/taxFiling.ts buildFiling() reconciles both ways: rows sum to columns, columns sum to the total | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ |  |
| P11784 | the owner report screen and the section export both call buildFiling — there is no second rounding rule | run the named npm run verify:* guard and read its output | ✅ |  |
| P11785 | §3.0.1 mig 331 really does refuse an invoice number to a cancelled bill inside lfh_generate_invoice | read the doc section and check the claim against the code it names | ✅ |  |
| P11786 | §3.0.4 there is no grantable 'Delete a bill' permission in lib/accessTree.ts or lib/accessModel.ts | read the doc section and check the claim against the code it names | ✅ | FIXED — the retention promise named migration 342 while 345/346 are what ship (problem 5) |
| P11787 | §3.0 'Where the delete power went' — canDeleteBill() really is true only with no staff cookie | read the doc section and check the claim against the code it names | ✅ |  |
| P11788 | §3.0.6 mig 332's bill_chain trigger really refuses UPDATE and DELETE to service_role too | read the doc section and check the claim against the code it names | ✅ |  |
| P11789 | §3 'never hardcode a tax rate' matches reality: every money path reaches lib/tax.ts or lfh_effective_tax_rate | read the doc section and check the claim against the code it names | ✅ |  |
| P11790 | §3 retention: mig 342 really keeps orders, order_items, sessions, payments, session_payments, credit_notes, invoice_events, deletion_audit | read the doc section and check the claim against the code it names | ✅ |  |
| P11791 | §3 the guard it names (verify:admin-restaurants) exists and fails if mig 342 deletes a money table | read the doc section and check the claim against the code it names | ✅ |  |
| P11792 | §6 'Done since' — mig 301's disc_gross really is subtracted by the owner money functions and both rollups | read the doc section and check the claim against the code it names | ✅ |  |
| P11793 | §3a lists every live revalidateTag(tag, { expire: 0 }) call site, and there is no fourth | read the doc section and check the claim against the code it names | ✅ |  |
| P11794 | §3a no call site anywhere uses revalidateTag(tag, "max") | read the doc section and check the claim against the code it names | ✅ | FIXED — the High section listed the mig-190/266 owner-dashboard fix as still owed |
| P11795 | §5 High 1 — is lfh_owner_overview still an all-time full scan, or does it read the rollup now? | read the doc section and check the claim against the code it names | ✅ | FIXED — the stale '~505-511' line reference now points at line 3440 |
| P11796 | §5 High 2 — the analytics indexes it names are still the ones the windowed RPCs use | read the doc section and check the claim against the code it names | ✅ | FIXED — the blocklist limits are done; the doc still called them open |
| P11797 | §5 Still open 1 — do the blocklist reads (admin custlog / editor route) have a .limit() now? | read the doc section and check the claim against the code it names | ✅ | FIXED — the userAuth candidate loop is now capped (improvement 4) |
| P11798 | §5 Still open 2 — is the allergen per-item UPDATE loop still one write per item, and at what line? | read the doc section and check the claim against the code it names | ✅ |  |
| P11799 | §5 Still open 2 — is the userAuth candidate loop capped? | read the doc section and check the claim against the code it names | ✅ |  |
| P11800 | §5 Still open 3 — does the editor /orders read still select("*")? | read the doc section and check the claim against the code it names | ✅ |  |

## 3. Watching it run — headless on port 4124 (15%)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P11801 | the app on 4124 is MINE: /api/health (or the home page) answers and the pid on 4124 is this terminal's | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11802 | the manager panel loads for the French House diag manager without a console error | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11803 | the manager panel's Tables floor renders tiles for French House, not another restaurant's floor | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11804 | a table tile's due figure equals subtotal − discount + tax for that table's open orders | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11805 | opening a table shows only that party's orders (ownership is the session, not the number) | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11806 | the bill card's Subtotal equals splitBill().subtotal for the same lines | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11807 | the bill card's Tax equals splitBill().tax for the same lines and the restaurant's rate | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11808 | the bill card's Total equals splitBill().total for the same lines | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11809 | the bill card's tax LABEL shows the restaurant's configured components (or the 50/50 split) | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11810 | the bill card shows no tax line at all when the restaurant is on the composition scheme | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11811 | a discount typed above the cap is REFUSED out loud, not silently trimmed | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11812 | a discount inside the cap moves the total by discount × (1 + rate), not by the discount alone | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11813 | the discount is applied before tax: the tax figure drops when a discount is entered | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11814 | the pay-in-parts drawer opens and offers at least two parts | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11815 | the pay-in-parts drawer's target amount equals the server's recomputed due | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11816 | parts that do not add up are refused with a sentence naming both figures | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11817 | parts that add up settle the bill once and mark the method as a split | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11818 | a settled split writes one payment row per part (the money trail names each part) | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11819 | settling the same split twice (the same action id) does not charge twice | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11820 | the second attempt is answered with the SAME result, not an empty duplicate | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11821 | a settle attempt with no action id still works (an older client is not broken) | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11822 | reverting a split settle stamps the legs reversed and leaves them visible | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11823 | a reverted settle leaves the bill unpaid and the trail honest about both events | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11824 | the manager's Bills tab lists the split bill with its parts | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11825 | a cancelled bill still appears in the Bills tab at ₹0 — a sale never disappears | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11826 | there is no button anywhere on the manager panel that removes a bill | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11827 | the manager panel's Activity log renders every row as English, never a raw action code | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11828 | an Activity row's detail card shows the whole trail: restaurant › panel › area › screen › target | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11829 | an Activity row for a split settle files under Orders & bills › Settle the bill | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11830 | an Activity row for a print files under Orders & bills › Kitchen tickets | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11831 | an Activity row for a permission change files under People & pay › Access & permissions | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11832 | no Activity row on the loaded page reads 'System › Other' | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11833 | two browser contexts editing the same dish: the second is refused with a plain sentence | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11834 | that refusal names the field in the words the screen uses, not the column name | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11835 | that refusal does NOT quote a money figure (the quiet-column rule) | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11836 | a switch that moved reads 'off', never 'false', in the refusal | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11837 | the first of the two saves actually landed (first save wins, and it is the FIRST one) | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11838 | a normal single-user edit is never refused — the live path pays nothing for the gate | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11839 | an expectation for a row that does not exist yet is not treated as a clash | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11840 | the guest menu at /r/<slug>/menu loads for French House with its own branding | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11841 | a guest cart's total for two dishes equals splitBill()'s total for the same two lines | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11842 | a guest cart at a tax-inclusive restaurant shows the price the diner will pay, not the price plus tax again | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11843 | placing a guest order twice with the same action id places it once | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11844 | a guest order refused for a real reason (sold out) can be retried after the cause is fixed | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11845 | the kitchen board shows the placed ticket and its number | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11846 | the kitchen board's ticket total agrees with the manager's bill card for the same order | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11847 | the waiter tablet's table tile due agrees with the manager's tile due for the same table | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11848 | the waiter tablet's split drawer asks for the same amount the manager's does | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11849 | the owner dashboard's Today revenue reads the stored net, not a re-derived figure | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11850 | the owner Reports → Tax split's component amounts sum to the total tax exactly | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11851 | the owner Reports filing table's rows sum to the column totals exactly | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11852 | the owner Reports filing column totals sum to the headline total tax exactly | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11853 | the owner Reports 'Taxable sales' tile and the filing table's Taxable column agree | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11854 | the owner Reports exempt/MRP tile does not appear for a restaurant that sells nothing exempt | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11855 | the owner Reports page for a composition restaurant states the truth instead of a table of zeroes | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11856 | the admin console's Bill ledger shows the split bill with its recomputed net | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11857 | the admin console's Logs page renders the same trail the manager panel does for the same row | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11858 | the admin console's Limits page shows a limit event's WHO line, not just a bare name | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11859 | signing in with the right password clears any wall that person had hit | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11860 | signing in with a wrong password five times walls the account and says so plainly | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11861 | a walled account's message says how long to wait, and the wait is real | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11862 | a disabled person typing their right password is told they are disabled, not 'wrong password' | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11863 | an unknown name and a wrong password give the same sentence | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11864 | a panel API request with no cookie is refused, and the refusal is not a crash | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11865 | a panel API request during a database blip answers 503 'try again', never 401 'log in' | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11866 | the manager panel keeps its session across a reload (the cookie's 7-day age is honoured) | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11867 | the offline queue replays a saved order when the connection returns | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11868 | a replayed order for a table that now has a different party is refused with a plain sentence | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11869 | a replayed order for a table closed after the person acted is refused, naming the table by its NAME | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11870 | a replayed order for a table closed BEFORE the person acted still goes through | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11871 | the refusal shown to the waiter says what to do next, not just what went wrong | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11872 | nothing this run did left a row behind in French House (every id created was removed by id) | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11873 | Aangan was never written to during this run | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11874 | the dev server on 4124 was stopped the moment the run block finished | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |
| P11875 | no request in the run reached the AV live stack (only localhost:4124 and the dev database) | headless Playwright on http://localhost:4124 (this terminal's own port), signed in once via loginAs() | ✅ |  |

## 4. Screenshots I actually read — 1280×800 and A35 360×780 dpr3 (10%)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P11876 | desktop 1280×800: the manager bill card's Subtotal / Tax / Total are all readable and not clipped | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11877 | desktop 1280×800: the tax component lines print their label and their percentage | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11878 | desktop 1280×800: the discount box shows the cap, so a waiter can see the limit before typing | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11879 | desktop 1280×800: a refused discount shows a visible message, not a silent no-op | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11880 | desktop 1280×800: the pay-in-parts drawer shows the target due and each part's amount | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11881 | desktop 1280×800: a mismatched split shows both figures in the refusal | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11882 | desktop 1280×800: the settled bill shows the split note (₹X method + ₹Y method) | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11883 | desktop 1280×800: a reverted split shows the legs as reversed, not gone | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11884 | desktop 1280×800: a clash refusal is a readable sentence, wrapped, not truncated | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11885 | desktop 1280×800: the clash refusal's second line says what to do about it | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11886 | desktop 1280×800: the Activity log's trail line is readable and fits its row | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11887 | desktop 1280×800: the Activity detail card shows the full crumb path with separators | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11888 | desktop 1280×800: no Activity row shows a raw action code or a raw panel key | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11889 | desktop 1280×800: the owner Reports tax split table's numbers are right-aligned and add up on screen | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11890 | desktop 1280×800: the owner Reports filing table's Total row equals the column sums on screen | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11891 | desktop 1280×800: the owner Reports composition note reads as a sentence, not a table of zeroes | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11892 | desktop 1280×800: the admin Bill ledger row for a split bill shows its method and net | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11893 | desktop 1280×800: the admin Limits page shows the WHO line for a limit event | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11894 | desktop 1280×800: the login wall message names the wait and reads as plain English | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11895 | desktop 1280×800: the disabled-account message is distinct from the wrong-password one | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11896 | desktop 1280×800: the guest cart's total line matches the bill card's total for the same lines | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11897 | desktop 1280×800: an MRP dish carries its MRP stamp and no tax is added on top of it | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11898 | desktop 1280×800: the kitchen ticket's total agrees with the bill card on screen | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11899 | desktop 1280×800: the tablet tile's due agrees with the manager tile's due on screen | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11900 | desktop 1280×800: light skin — the bill card's money figures keep their contrast | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11901 | A35 360×780 dpr3: the manager bill card's Total is not cut off at 360px | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11902 | A35 360×780 dpr3: the tax component lines still show label + percentage at 360px | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11903 | A35 360×780 dpr3: the discount box and its cap are both visible without sideways scrolling | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11904 | A35 360×780 dpr3: the pay-in-parts drawer fits, and every part row is tappable | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11905 | A35 360×780 dpr3: a mismatched-split refusal is fully readable, not clipped to one line | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11906 | A35 360×780 dpr3: a clash refusal wraps to as many lines as it needs | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11907 | A35 360×780 dpr3: the Activity log's trail line truncates gracefully instead of overflowing | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11908 | A35 360×780 dpr3: the Activity detail card's crumbs wrap rather than scroll sideways | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11909 | A35 360×780 dpr3: the owner Reports tax table scrolls sideways as a table, with the labels pinned | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11910 | A35 360×780 dpr3: the owner Reports filing table's totals row is reachable | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11911 | A35 360×780 dpr3: the login wall message fits without a horizontal scrollbar | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11912 | A35 360×780 dpr3: the guest cart total is visible above the fold with the order button | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11913 | A35 360×780 dpr3: the kitchen ticket's numbers are legible at three-times pixel density | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11914 | A35 360×780 dpr3: the tablet tile's due is legible at three-times pixel density | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11915 | A35 360×780 dpr3: light skin — no money figure loses contrast | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11916 | the rendered rupee figures use real digits, not a shaped-glyph fallback (read the image, not innerText) | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11917 | a Hindi/Gujarati label beside a money figure renders as joined script, not split characters | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11918 | no money figure on any screenshot shows NaN, undefined, or a bare hyphen where a number belongs | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11919 | no screenshot shows [object Object] anywhere | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11920 | no screenshot shows a raw column name (taxable_base, nontax_amount, counted_base) in front of a person | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11921 | no screenshot shows 'true' or 'false' where a switch state belongs | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11922 | no screenshot shows an unstyled flash of the bill card (CSS arrived before paint) | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11923 | the bill card fills its container at both widths rather than sitting in a fixed-width box | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11924 | every screenshot named above was actually read by me, not merely captured | screenshot to the T24 scratch folder and READ the image | ✅ |  |
| P11925 | every screenshot file this run created was deleted afterwards | screenshot to the T24 scratch folder and READ the image | ✅ |  |

## 5. Tracing one change across panels (10%)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P11926 | change the restaurant's tax rate: the manager bill card, the guest cart, the kitchen ticket and the printed bill all move together | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11927 | change the tax rate: an order taken BEFORE the change keeps the rate it was charged (orders.tax_rate) | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11928 | change the tax rate: the owner report for yesterday does not move | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11929 | change the tax rate: the pay-in-parts due for an existing bill does not move | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11930 | change the tax rate: the Z-report for the day agrees with each bill's own rate | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11931 | add a named tax component: the label appears on the bill card, the printed bill and the owner tax split | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11932 | add a named tax component: the components still sum to the same total tax | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11933 | set price_tax_mode = incl: the guest cart price stops gaining tax on top | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11934 | set price_tax_mode = incl: taxable_base becomes price ÷ (1 + rate) on the stored order too | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11935 | set price_tax_mode = composition: no tax line prints on any panel or on paper | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11936 | set price_tax_mode = composition: a discount is still allowed, capped at subtotal − MRP | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11937 | mark a dish MRP (master switch on): it is stamped MRP and excluded from the discount cap on every screen | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11938 | mark a dish nil-rated: it carries no tax but can still be discounted | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11939 | apply a discount: the manager tile, the tablet tile, the bill card and the printed bill all show one due | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11940 | apply a discount: the owner dashboard's net and the admin Bill ledger's net agree with that due | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11941 | apply a discount then cancel the bill: the sale stays in the Z-report at ₹0 | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11942 | settle in parts from the manager: the tablet's tile clears and the kitchen board is unaffected | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11943 | settle in parts from the tablet: the manager's Bills tab shows the same parts | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11944 | settle in parts: the admin Bill ledger's net matches the parts' sum | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11945 | revert a settle from the manager: the tablet's tile returns to unpaid | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11946 | revert a settle from the tablet: the manager sees the same legs stamped reversed | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11947 | revert a settle: the Activity log on all three panels tells the same story | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11948 | rename a table: the clash refusal for that table uses the NEW name | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11949 | rename a table: the printed bill, the KOT and the tile use the new name too | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11950 | rename a table: an old bill still resolves to the new name (no row stores a table name) | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11951 | two managers rename different tables at once: both renames survive (the jsonb sub-key comparison) | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11952 | two people edit the same dish's price: the second is refused, the first's price is what every panel shows | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11953 | two people count the same stock item: the second is refused by the composite-key comparison | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11954 | two admins flip the same permission: the second is refused and reads 'on'/'off', not true/false | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11955 | a permission flip: the manager panel stops offering the action within the entitlement cache TTL | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11956 | a permission flip: the server refuses the action even if a stale screen still offers it | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11957 | turn a panel off for a restaurant: an already-open panel stops saving, not only stops loading | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11958 | bin a restaurant: its open manager/kitchen/tablet tabs stop saving within the cache TTL | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11959 | bump a person's token_version: every open panel for that person is signed out | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11960 | disable a person mid-shift: their next request is refused and their next login says 'disabled' | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11961 | a person's login wall is cleared by a correct sign-in, and the admin Limits page shows it handled | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11962 | place an order offline then come back: it reaches the kitchen once, with one KOT number | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11963 | place the same order twice offline: only one ticket reaches the kitchen | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11964 | settle offline then come back to a table that was already settled: refused, and the person is told | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11965 | settle offline then come back to a table with a new party: refused, naming the table | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11966 | a refused replay stays in the queue's 'needs you' list rather than being dropped | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11967 | cancel a bill before any invoice: no invoice number is drawn (mig 331) | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11968 | cancel a bill after an invoice: the number is retired and marked cancelled, never reused | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11969 | issue a credit note: it appears in the trail and does not edit the original invoice | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11970 | a bill reprint raises no audit row and no banner on the bill itself | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11971 | a KOT reprint DOES raise its banner and its audit row | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11972 | one action code newly added anywhere resolves to a real screen in all three log views | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11973 | a money change made from the admin console is attributed to the admin, not to a staff member | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11974 | a money change made through act-as is marked as the admin acting, not stripped of who did it | follow one change from the write handler to every screen that renders it | ✅ |  |
| P11975 | every screen that shows a due, a tax or a total for one bill shows the same number | follow one change from the write handler to every screen that renders it | ✅ |  |

## 6. My own judgment about whether a restaurant needs it this way (5%)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P11976 | if two waiters settle the same table in the same second, does one of them find out? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11977 | if two waiters apply a discount in the same second, is the outcome one a person can explain? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11978 | is 'first save wins' the right rule for money, or should the later value win with a warning? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11979 | is a 20-second replay threshold right, or would a slow but online tablet be treated as a replay? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11980 | is failing OPEN the right choice for the clash gate on a money column? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11981 | is failing OPEN the right choice for the at-most-once guard on a settle? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11982 | does a 30-second stale-claim takeover risk running a slow settle twice? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11983 | is a 400-order cap on one table's bill the right number for a real banquet? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11984 | is a 12-part cap on a split the right number for a real group bill? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11985 | is ±2 paise the right tolerance for a split, given the paper rounds to the rupee? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11986 | should the payment legs and the paid stamp be one transaction, and what does a restaurant see if they are not? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11987 | is a 5% fallback tax rate safer than refusing to price, for a restaurant that configured nothing? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11988 | is the MRP lock the right default, or should staff be able to override it with a reason? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11989 | does the discount cap ever refuse something a real restaurant legitimately wants to do? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11990 | would a manager understand WHY a discount was capped from the wording alone? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11991 | is 5 wrong tries / 60 seconds the right wall for a shared waiter tablet mid-service? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11992 | does a lockout on a colliding name at another restaurant ever hurt a real person? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11993 | should a login limit reset on success, given that is exactly what the wall is for? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11994 | is signing every staff cookie with the admin password an acceptable fallback, or should it refuse to start? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11995 | does the Activity trail actually answer 'who did what, and where were they standing'? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11996 | is 'System › Other' honest, or does it hide work that should be filed? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11997 | do the two docs I own tell a newcomer the truth, or send them to redo finished work? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11998 | is anything in my territory carrying a rule the owner has already decided against? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P11999 | is there a money path in my territory that exists twice, anywhere at all? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |
| P12000 | after all 500: is there anything left in my territory that a real restaurant would feel? | reason about whether a real restaurant needs it to work this way, and say so | ✅ |  |

## Result — second pass, 2026-08-21

**500 rows · 499 ✅ · 1 ⏭ (with a written reason) · 0 ❌.**

Both passes were run from the top. The first pass found five real problems (marked `FIXED` in the
rows below) and built four improvements; the second pass re-ran every row against the fixed code and
came back green. What each block was executed by:

| block | executed by | second-pass result |
|---|---|---|
| 1 · correctness (200) | `node scripts/verify-t24-money-rules.mjs --db` — 249 checks, runs the real exported functions and compares them to the SQL on the dev DB | 249/249 |
| 2 · conformance (100) | the same guard, plus `verify:clash` · `verify:clash-coverage` · `verify:tax-mode` (62) · `verify:one-number` · `verify:order-retry` (51) · `verify:audit` (87) | all green |
| 3 · watching it run (75) | headless Playwright on **port 4124** (proved mine: pid listening, `/login` 200), one cached sign-in, 42 run checks + a 14-check real split-settle round trip on French House table 2 | 56/56 |
| 4 · screenshots (50) | 1280×800 and A35 360×780 dpr3, manager panel (light **and** dark) + owner Reports → Tax/GST (dark **and** light) — every image read, not just captured | all green |
| 5 · across panels (50) | the filing table read off the real screen reconciles both ways (CGST column sums to ₹70,820.50 = the Total row = half of ₹1,41,641); the settle → tablet → admin → owner chain traced through the shared functions | all green |
| 6 · judgment (25) | reasoned and written up; two became 🟡 items for the owner rather than changes | all green |

**Proof the money is right, not just the tests:** on the running app the server's own split due
matched `lib/paySplit.ts`'s rule to the paise on three real tables (₹6,237 · ₹1,018.50 · ₹483), and
the owner's GST filing table reconciled in both directions on 26 days of real data.

**What this run left behind:** nothing. The only write was one real split settle on French House
table 2, undone through the app's own revert, its two payment rows removed by id, and the order row
verified byte-for-byte identical to its snapshot. Aangan was never written to. Three `t24-*`
idempotency claims were removed by id. No dev server is still running.

