# Sweep 6 · Terminal 24 — the money and safety libraries

**Territory:** `lib/clash.ts` · `lib/paySplit.ts` · `lib/tax.ts` · `lib/taxFiling.ts` ·
`lib/idempotency.ts` · `lib/idempotencyRule.ts` · `lib/logTrail.ts` · `lib/userAuth.ts` ·
`lib/rateLimit.ts` · `docs/COMPLIANCE-GUARDRAILS.md` · `docs/SAAS-EFFICIENCY-PLAYBOOK.md`
(`lib/numbering*.ts` and `lib/compliance*.ts` do not exist — numbering lives in the database
functions and `docs/NUMBERING.md`; the compliance rules live in the doc above. Both are recorded
here so the next sweep does not go looking for them. `lib/clashCompare.ts` is read-only for me —
my prompt names `lib/clash.ts` only — so anything wrong in it is a handoff.)

**IDs P11501–P12000.** Permanent and global: never renumber them. The next sweep re-runs these
rows first and only then adds new ones.

## How the 500 are budgeted

| block | what it is | share | rows |
|---|---|---|---|
| 1 | reading the code for correctness | ~40% | P11501–P11700 (200) |
| 2 | conformance to the rules in CLAUDE.md and the docs | ~20% | P11701–P11800 (100) |
| 3 | watching it run (headless Playwright, port 4124) | ~15% | P11801–P11875 (75) |
| 4 | screenshots read at 1280×800 and A35 360×780 dpr3 | ~10% | P11876–P11925 (50) |
| 5 | tracing one change across panels | ~10% | P11926–P11975 (50) |
| 6 | my own judgment | ~5% | P11976–P12000 (25) |

**Per-file coverage:** tax 55 · paySplit 52 · clash 47 · clashCompare 8 · idempotency(+Rule) 35 ·
logTrail 23 · userAuth 34 · rateLimit 17 · taxFiling 14 · COMPLIANCE-GUARDRAILS 8 ·
SAAS-EFFICIENCY-PLAYBOOK 8 — plus the 200 cross-cutting run/screenshot/trace/judgment rows, every
one of which lands on at least one of those files.

**Reproducing the pure-rule and database rows:** `node scripts/verify-t24-money-rules.mjs`
(add `--db` for the rows that compare the TypeScript rule against the SQL function on the dev
database). It is committed with this ledger, so a stranger next month runs one command.


## 1. Reading the code for correctness (40%)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P11501 | lib/tax.ts effectiveTaxRate() returns exactly 0 when settings.price_tax_mode = 'composition' | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11502 | lib/tax.ts effectiveTaxRate() sums named tax_components and divides by 100 (12.5+2.5 → 0.15) | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11503 | lib/tax.ts a tax_component with a blank/whitespace label is ignored by the sum | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11504 | lib/tax.ts a tax_component with rate 0 is ignored by the sum | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11505 | lib/tax.ts a tax_component with a negative rate is ignored by the sum | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11506 | lib/tax.ts tax_components that is not an array falls through to the flat tax_rate | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11507 | lib/tax.ts tax_rate stored as exactly 0 falls back to 0.05 — the same NULLIF(tax_rate,0) rule as mig 119/272 | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11508 | lib/tax.ts tax_rate null/absent falls back to 0.05 | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11509 | lib/tax.ts tax_rate arriving as the string "0.12" is read as 0.12, not NaN | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11510 | lib/tax.ts effectiveTaxPct(0.0825) === 8.25 (two decimals of a percent, no float dust) | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11511 | lib/tax.ts effectiveTaxPct(0.05) === 5 exactly | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11512 | lib/tax.ts TAX_SETTINGS_COLUMNS names all three columns tax_rate, tax_components, price_tax_mode | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11513 | lib/tax.ts priceTaxMode() answers 'excl' for an unknown/garbage stored value | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11514 | lib/tax.ts priceTaxMode() passes 'incl' and 'composition' through unchanged | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11515 | lib/tax.ts itemTaxModesAllowed() is strictly === true, so the string "true" does NOT switch it on | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11516 | lib/tax.ts resolveTaxMode() answers 'exempt' for every line at a composition restaurant, whatever the dish says | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11517 | lib/tax.ts resolveTaxMode() ignores a dish's own mode entirely while item_tax_modes_allowed is off | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11518 | lib/tax.ts resolveTaxMode() honours a dish's 'excl' and 'incl' when the master switch is on | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11519 | lib/tax.ts resolveTaxMode() maps a dish mode of 'none' to 'exempt' | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11520 | lib/tax.ts resolveTaxMode() maps 'mrp' to 'incl' when mrp_tax_treatment = 'inclusive' | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11521 | lib/tax.ts resolveTaxMode() maps 'mrp' to 'exempt' for any other mrp_tax_treatment | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11522 | lib/tax.ts resolveTaxMode() tests its cases in the SAME order as lfh_resolve_tax_mode (mig 269/270) | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11523 | lib/tax.ts isMrpDish() is false while item_tax_modes_allowed is off, so no MRP stamp prints | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11524 | lib/tax.ts splitBill() strips currency symbols and stray text out of a price | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11525 | lib/tax.ts splitBill() floors qty at 1 (a 0 or negative qty still charges one) | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11526 | lib/tax.ts splitBill() rounds PER LINE, matching lfh_split_items_tax, not once at the end | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11527 | lib/tax.ts splitBill() an 'incl' line divides by (1+rate) and rounds to 2dp before accumulating | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11528 | lib/tax.ts splitBill() an 'exempt' line lands in nontaxAmount and never in taxableBase | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11529 | lib/tax.ts splitBill() an is_mrp line is counted into mrpAmount AS WELL AS its own mode bucket | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11530 | lib/tax.ts splitBill() subtotal === taxableBase + nontaxAmount, always | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11531 | lib/tax.ts splitBill() discountBase === taxableBase when the rate is above zero | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11532 | lib/tax.ts splitBill() discountBase === subtotal − mrpAmount when the rate is zero | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11533 | lib/tax.ts splitBill() clamps the discount to discountBase and never below zero | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11534 | lib/tax.ts splitBill() taxable === taxableBase − discount when the rate is above zero (discount BEFORE tax) | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11535 | lib/tax.ts splitBill() tax === round(taxable × rate, 2) | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11536 | lib/tax.ts splitBill() total === taxable + tax + nontaxAmount when the rate is above zero | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11537 | lib/tax.ts splitBill() total === subtotal − discount when the rate is zero | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11538 | lib/tax.ts splitBill() the identity subtotal − discount + tax === total holds on every taxed case | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11539 | lib/tax.ts maxDiscount() equals splitBill(lines, settings, 0).discountBase | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11540 | lib/tax.ts splitBill() reports composition:true only for a composition restaurant | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11541 | lib/tax.ts splitBill([]) returns all zeros and never NaN | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11542 | lib/tax.ts an unparseable price contributes 0 to the bill, never NaN (a NaN total would print blank) | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11543 | lib/tax.ts a qty of "abc" becomes 1, never NaN | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11544 | lib/tax.ts splitBill()'s taxable_base / nontax_amount / mrp_amount equal lfh_split_items_tax()'s for the same items | node scripts/verify-t24-money-rules.mjs --db — compares the TS rule against the SQL function on the dev DB, French House | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11545 | lib/tax.ts effectiveTaxRate(settings) equals lfh_effective_tax_rate(rid) for French House at the database | node scripts/verify-t24-money-rules.mjs --db — compares the TS rule against the SQL function on the dev DB, French House | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11546 | lib/paySplit.ts badSplitShape() refuses fewer than two parts | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11547 | lib/paySplit.ts badSplitShape() refuses more than twelve parts | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11548 | lib/paySplit.ts badSplitShape() refuses a part whose amount is zero or negative | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11549 | lib/paySplit.ts badSplitShape() refuses a payment method that is not in PAYMENT_METHODS | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11550 | lib/paySplit.ts badSplitShape() refuses a part note longer than 200 characters | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11551 | lib/paySplit.ts badSplitShape() accepts a well-formed two-part split | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11552 | lib/paySplit.ts badSplitShape() refuses anything that is not an array | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11553 | lib/paySplit.ts badSplitShape() refuses a NaN / non-numeric amount | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11554 | lib/paySplit.ts settleBillInParts() scopes its order read by restaurant_id | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11555 | lib/paySplit.ts settleBillInParts() excludes cancelled orders from the bill | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11556 | lib/paySplit.ts settleBillInParts() excludes 'received' (not-yet-accepted) orders | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11557 | lib/paySplit.ts settleBillInParts() excludes orders already marked paid | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11558 | lib/paySplit.ts settleBillInParts() excludes soft-deleted orders (deleted_at IS NULL filter) | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11559 | lib/paySplit.ts settleBillInParts() prefers the table's OPEN session, falling back to un-archived orders on that table number | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11560 | lib/paySplit.ts settleBillInParts() refuses with 409 at the 400-order cap instead of quietly collecting for a partial set | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11561 | lib/paySplit.ts settleBillInParts() refuses plainly when there is nothing left to settle | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11562 | lib/paySplit.ts settleBillInParts() refuses plainly when the table has no live bill session | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11563 | lib/paySplit.ts the due is built from taxable_base, not from Σsubtotal (migs 270/272) | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11564 | lib/paySplit.ts a legacy order with taxable_base NULL falls back to its subtotal with no untaxed part | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11565 | lib/paySplit.ts the rate for each order comes from BILLDOC.orderTaxRate — the paper's own definition, not a second copy | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11566 | lib/paySplit.ts tax is bucketed per rate, so a mixed-rate bill rounds once per rate and not once per order | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | FIXED — the ±2p gate read the raw amounts while the legs were stored rounded; one rounding now feeds both (problem 2) · re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11567 | lib/paySplit.ts the discount is clamped to its own base exactly as the printed bill clamps it | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11568 | lib/paySplit.ts due === subtotal − discount + tax, the one formula that also holds at a zero rate | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11569 | lib/paySplit.ts the ±0.02 tolerance is measured against the server's recomputed due, never the browser's number | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11570 | lib/paySplit.ts the refusal names BOTH figures, so a waiter can see what to change | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11571 | lib/paySplit.ts each leg amount is rounded to 2dp before it is stored | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11572 | lib/paySplit.ts every leg carries session_id AND restaurant_id | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11573 | lib/paySplit.ts the split note is truncated to 200 characters at both places it is written | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11574 | lib/paySplit.ts the orders are marked paid with payment_method 'Split' — one bill, several parts | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11575 | lib/paySplit.ts the orders update is scoped by restaurant_id as well as by id | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11576 | lib/paySplit.ts an insert error answers 500 rather than reporting a settle that did not happen | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11577 | lib/paySplit.ts reverseSplitLegs() STAMPS a leg reversed rather than deleting it (mig 285) | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11578 | lib/paySplit.ts reverseSplitLegs() scopes by session_id and restaurant_id | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11579 | lib/paySplit.ts reverseSplitLegs() only touches legs that are not already reversed | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11580 | lib/paySplit.ts reverseSplitLegs() only touches legs created at or after `since`, leaving older legs standing | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11581 | lib/paySplit.ts reverseSplitLegs() returns the count and the amount so the caller can log honestly | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11582 | lib/paySplit.ts reverseSplitLegs() THROWS when the update fails — a money trail that cannot be corrected must be noticed | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11583 | no route re-implements the pay-in-parts arithmetic — settleBillInParts is the only place it exists | grep the repo and assert the stated invariant | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11584 | both the manager panel route and the waiter tablet route call settleBillInParts and reverseSplitLegs | grep the repo and assert the stated invariant | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11585 | settling in parts cannot lower, hide or erase a sale — the due is recomputed server-side from stored rows | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11586 | lib/clash.ts clashJson() answers HTTP 409 with { error, clash } in the shape outbox.js expects | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11587 | lib/clash.ts replayMarkers() returns null when the x-lfh-replay header is absent | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11588 | lib/clash.ts replayMarkers() returns null when x-lfh-queued-at is absent | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11589 | lib/clash.ts replayMarkers() returns null for an unparseable queued-at date | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11590 | lib/clash.ts replayMarkers() returns null under the 20s replay age — the live write path is untouched | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11591 | lib/clash.ts replayMarkers() returns the queuedAt for a genuinely aged replay | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11592 | lib/clash.ts expectClash() returns null when no x-lfh-expect header travels | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11593 | lib/clash.ts expectClash() returns null for malformed JSON in that header | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11594 | lib/clash.ts expectClash() returns null when the header parses to an array | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11595 | lib/clash.ts expectClash() returns null for a table that is not on COMPARABLE_TABLES | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11596 | lib/clash.ts expectClash() returns null when `fields` is missing or not an object | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11597 | lib/clash.ts only the columns listed in COMPOSITE_KEYS may be used for a composite lookup | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11598 | lib/clash.ts a half-specified composite key is ignored entirely rather than matching the wrong row | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11599 | lib/clash.ts every field name is validated against a strict pattern before it reaches the select | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11600 | lib/clash.ts at most 8 fields are compared per expectation | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11601 | lib/clash.ts the select asks only for real columns, dedup'd, with the jsonb sub-key read out of the object | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11602 | lib/clash.ts settings and restaurants answer 'nothing to compare' for an id that is not the caller's own restaurant | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11603 | lib/clash.ts every other table's comparison is scoped by restaurant_id | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11604 | lib/clash.ts a missing row reads as 'nothing to overwrite', which is the first-counter case for inventory | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11605 | lib/clash.ts a jsonb sub-key like profile.notes is compared, not the whole blob | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11606 | lib/clash.ts a money column's refusal says the value MOVED without quoting the figure | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11607 | lib/clash.ts an object's refusal never prints the literal [object Object] | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11608 | lib/clash.ts a boolean reads 'on' / 'off' in the refusal, never 'true' / 'false' | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11609 | lib/clash.ts the tri-state 'pin' reads 'on, but asking for a manager PIN' | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11610 | lib/clash.ts an empty/absent current value reads 'nothing' | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11611 | lib/clash.ts readable() has a plain-words name for every column a live call site actually sends | grep the repo and assert the stated invariant | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11612 | lib/clash.ts the whole expectation check fails OPEN on any lookup error | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11613 | lib/clash.ts replayClash() returns null for a live write | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11614 | lib/clash.ts replayClash() returns null when the action is not table-scoped or cannot be resolved | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11615 | lib/clash.ts replayClash() refuses when a different party is on that table now | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11616 | lib/clash.ts replayClash() refuses a closed table with no closed_at, because it cannot prove who was first | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11617 | lib/clash.ts replayClash() refuses when the table was closed AFTER the person acted | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11618 | lib/clash.ts replayClash() allows the write when the table closed BEFORE the person acted | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11619 | lib/clash.ts tableLabel() reads settings.table_names live and falls back to 'Table N' | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11620 | lib/clash.ts every refusal is retryable:false — resending the same thing cannot help | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11621 | lib/clashCompare.ts stableJson() sorts object keys at every level | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11622 | lib/clashCompare.ts stableJson() is depth-limited and fails OPEN past the limit | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11623 | lib/clashCompare.ts sameValue() trims text before comparing | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11624 | lib/clashCompare.ts sameValue() compares lists as SETS (allergen order is not meaningful) | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11625 | lib/clashCompare.ts sameValue() compares objects by content, so a re-serialised blob is equal | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11626 | lib/clashCompare.ts sameValue() treats null/absent and {} as the same, so an unset column invents no clash | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11627 | lib/clashCompare.ts isPlainObject() rejects arrays | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11628 | lib/clash.ts imports that comparator instead of keeping a private copy | grep the repo and assert the stated invariant | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11629 | lib/idempotency.ts a request with no X-LFH-Action-Id is passed straight through, unchanged | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11630 | lib/idempotency.ts a fresh claim runs the handler exactly once | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11631 | lib/idempotency.ts a duplicate of a completed action echoes the STORED result (the original order id) | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11632 | lib/idempotency.ts …and does not run the handler a second time | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11633 | lib/idempotency.ts a concurrent duplicate answers 409 sync_in_progress with retry:true | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11634 | lib/idempotency.ts a claim older than 30s is treated as a crashed attempt and taken over | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11635 | lib/idempotency.ts only a unique-violation (23505) reads the claim row back | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11636 | lib/idempotency.ts any other database error fails OPEN and runs the write | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11637 | lib/idempotency.ts a handler that throws releases the claim before rethrowing | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11638 | lib/idempotency.ts a 4xx reply releases the claim so the person's next try genuinely retries | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11639 | lib/idempotency.ts a 200 carrying ok:false releases the claim (the guest sold-out / rate-limited case) | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11640 | lib/idempotency.ts a stored refusal self-heals instead of replaying the old refusal forever | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11641 | lib/idempotency.ts the healed row is REUSED as a fresh claim rather than deleted, so the retry is still protected | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11642 | lib/idempotency.ts the stored result is returned per call, never through a shared module variable | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11643 | lib/idempotency.ts the duplicate reply carries duplicate:true so the client can still track the order | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11644 | lib/idempotency.ts a non-JSON response stores nothing rather than throwing | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11645 | lib/idempotency.ts the response is cloned, so the caller can still read the original body | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11646 | lib/idempotency.ts the claims-table prune is opportunistic (1 in 200) and never a timer on idle data | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11647 | lib/idempotency.ts the prune is fire-and-forget and fully swallowed — tidying can never affect a write | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11648 | lib/idempotencyRule.ts didSomething() treats any status ≥ 400 as nothing done | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11649 | lib/idempotencyRule.ts storedIsRefusal() answers the same question about an already-stored body | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11650 | lib/idempotencyRule.ts has no imports at all, so verify:order-retry executes the rule that ships | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11651 | every staff panel write route (editor POST/PATCH/DELETE, kitchen, tablet, inventory, panel-profile) is wrapped in withIdempotency | grep the repo and assert the stated invariant | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11652 | the guest place-order and call-waiter routes are wrapped too | grep the repo and assert the stated invariant | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11653 | a replayed settle cannot charge a bill twice — the claim is taken before the handler runs | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11654 | lib/logTrail.ts every action code the app can WRITE resolves to a real area+screen, not System › Other | npm run verify:<name> and read its output | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11655 | lib/logTrail.ts an action code nobody has placed lands in System › Other — honest, not invented | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11656 | lib/logTrail.ts placeOf() never throws on null, undefined or an empty string | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11657 | lib/logTrail.ts an explicit PLACE entry wins over the prefix rules | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11658 | lib/logTrail.ts the PREFIX rules are ordered and the first match wins | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11659 | lib/logTrail.ts panelName() maps 'editor' to 'Manager panel' (the old name for it) | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11660 | lib/logTrail.ts an unknown panel prints itself capitalised rather than vanishing | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11661 | lib/logTrail.ts targetOf() prefers table_number over everything else | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11662 | lib/logTrail.ts a non-numeric table_number (a named table) prints as it stands | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11663 | lib/logTrail.ts a quoted run inside `detail` becomes the target | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11664 | lib/logTrail.ts a bill number inside `detail` becomes 'Bill #N' | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11665 | lib/logTrail.ts order_id falls back to its first 8 characters | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11666 | lib/logTrail.ts trailOf() drops a missing restaurant from the crumbs instead of printing a blank one | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11667 | lib/logTrail.ts `short` is screen · target — the two crumbs that actually narrow a list row down | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11668 | lib/logTrail.ts is client-safe: it imports nothing that reaches the server | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11669 | lib/userAuth.ts normalizeLoginName() trims, lowercases and collapses inner whitespace | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11670 | lib/userAuth.ts loginUser() refuses an empty name or password with reason 'empty' | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11671 | lib/userAuth.ts loginUser() caps the username and password length BEFORE the slow hash | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11672 | lib/userAuth.ts a failed candidate lookup is reported as transient, never as wrong credentials | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11673 | lib/userAuth.ts loginUser() excludes recycle-bin rows (deleted_at IS NULL) | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11674 | lib/userAuth.ts disabled rows are fetched only so a disabled person can be told the truth | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11675 | lib/userAuth.ts on the tenant door only that restaurant's people can match | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11676 | lib/userAuth.ts an owner can sign in at a restaurant they OWN via the restaurant_owners join | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11677 | lib/userAuth.ts a lockout on any matching live row is honoured, so a colliding name cannot dodge it | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11678 | lib/userAuth.ts a wrong password bumps failed_count on every live match | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11679 | lib/userAuth.ts five wrong tries lock the account for 60 seconds and reset the counter | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11680 | lib/userAuth.ts a correct password clears failed_count and locked_until | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11681 | lib/userAuth.ts a disabled person is told so ONLY on a verified password, so name-guessing learns nothing | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11682 | lib/userAuth.ts when only disabled rows exist and the password is wrong, the answer is the generic one | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11683 | lib/userAuth.ts hashSecret() produces a self-describing pbkdf2$iters$salt$hash string | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11684 | lib/userAuth.ts verifySecret() compares in constant time via safeEqual | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11685 | lib/userAuth.ts verifySecret() still accepts a legacy bare sha256 hash | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11686 | lib/userAuth.ts the cookie signature covers id:role:token_version:iat, so a role change or a token bump ends it | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11687 | lib/userAuth.ts userFromCookie() rejects a value that is not exactly three dot-separated parts | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11688 | lib/userAuth.ts userFromCookie() rejects a cookie older than the 7-day max age | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11689 | lib/userAuth.ts userFromCookie() retries the lookup once (~120ms) before giving up on a flap | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11690 | lib/userAuth.ts userFromCookie() throws AuthDbError on a sustained lookup failure, so gates answer 503 not 401 | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11691 | lib/userAuth.ts roleSatisfies(): owner covers everything, manager covers kitchen+tablet, the two devices do not cover each other | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11692 | lib/userAuth.ts requireRole() checks the STAFF cookie before the admin fallback (the cross-restaurant scoping fault of 2026-07-03) | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11693 | lib/userAuth.ts requireRole() re-checks the panel entitlement and the recycle bin on every request, not only at login | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11694 | lib/rateLimit.ts rateAllowed() fails OPEN on an RPC error — a limiter glitch can never lock a restaurant out | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11695 | lib/rateLimit.ts rateAllowed() returns true immediately for an empty subject | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11696 | lib/rateLimit.ts the two extra reads that name WHO and WHERE happen only on the rare wall-hit path | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11697 | lib/rateLimit.ts subjectFor() normalises a login name so '  Ravi ' and 'ravi' share one counter | node scripts/verify-t24-money-rules.mjs (pure-rule block) — runs the real exported function | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11698 | lib/rateLimit.ts rateResetOnSuccess() wipes the counter and marks the open event handled, never deleting the record | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11699 | lib/rateLimit.ts pingLatestGuestLimit() confirms a REAL recent open event, scoped by restaurant, before pinging | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
| P11700 | lib/rateLimit.ts recordAlert() is warn-only: it records and pings but never blocks anyone | read the named file and assert the stated line/behaviour | ✅ | re-run 2026-08-28 (sweep #7) — `node scripts/verify-t24-money-rules.mjs --db`, 249/249 green, plus a full re-read of the file this row names. |
## 2. Conformance to the project's written rules (20%)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P11701 | every settings select that feeds the tax helpers also reads price_tax_mode (TAX_SETTINGS_COLUMNS) | run the named npm run verify:* guard and read its output | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11702 | no file hardcodes a 5% / 0.05 tax rate outside a documented fallback | grep the repo and assert the stated invariant | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11703 | lib/tax.ts is the only TypeScript definition of the effective rate — no second copy in a route or a page | grep the repo and assert the stated invariant | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11704 | lib/tax.ts's rate rule and mig 272's lfh_effective_tax_rate agree on composition, components, zero and blank | node scripts/verify-t24-money-rules.mjs --db — the TS rule against the SQL function, French House | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11705 | lib/tax.ts's splitBill and mig 272's lfh_split_items_tax agree line-for-line on a mixed excl/incl/exempt/MRP bill | node scripts/verify-t24-money-rules.mjs --db — the TS rule against the SQL function, French House | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11706 | the discount cap in lib/tax.ts matches lfh_order_discount_base (mig 272) for both rate>0 and rate=0 | node scripts/verify-t24-money-rules.mjs --db — the TS rule against the SQL function, French House | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11707 | a discount is applied BEFORE tax everywhere in lib/tax.ts (taxable = base − discount) | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11708 | a composition restaurant's bill carries no tax line, because its effective rate genuinely IS zero | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11709 | MRP is tracked apart from 'merely untaxed' — only the MRP part is locked against a discount | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11710 | a nil-rated (exempt, non-MRP) dish can still be discounted at a zero-rate restaurant | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11711 | lib/paySplit.ts makes NO permission decision of its own — each caller keeps its own gate | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11712 | the manager route's split call and the tablet route's split call reach the same function with the same arguments | grep the repo and assert the stated invariant | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11713 | no panel JavaScript computes the split due itself and sends it as the truth | grep the repo and assert the stated invariant | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11714 | a split settle writes one session_payments row per part, so 'how did table 6 pay?' has a real answer | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11715 | a reverted split settle STAMPS its legs (mig 285) — no money record is ever deleted | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11716 | the split path selects an explicit column list, scoped by restaurant_id, with a limit (egress rules) | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11717 | a split settle is reached through a route wrapped in withIdempotency, so a replay settles once | grep the repo and assert the stated invariant | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11718 | the split refusal branches on a server reason CODE and a plain sentence, never on prose the client parses | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11719 | settling in parts leaves the sale in the Z-report and the GST report — nothing is hidden (compliance §3.0) | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11720 | no code path in lib/paySplit.ts can delete an order, an invoice or a bill | grep the repo and assert the stated invariant | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11721 | the split due honours orders.tax_rate (mig 284), so a banquet is not asked for at the dine-in rate | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11722 | lib/paySplit.ts's rate resolution and public/panels/billdoc.js's orderTaxRate are the same function, not two rules | grep the repo and assert the stated invariant | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. · s8·T11 2026-09-06: re-run ✅ — `node --experimental-strip-types scripts/sweep/t11/run.mjs --only P11722` (this row's subject is a file in T11's sweep-#8 territory) |
| P11723 | npm run verify:clash-coverage is green — every value edit it checks tells the server what it was editing from | run the named npm run verify:* guard and read its output | ✅ | FIXED — the parts land before the paid stamp with no transaction across the two; a failed stamp now REVERSES the parts it recorded (problem 1) · re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11724 | npm run verify:clash-coverage names the areas it deliberately does NOT cover, rather than implying full coverage | run the named npm run verify:* guard and read its output | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11725 | every table a live call site names in an expectation is present in COMPARABLE_TABLES | run the named npm run verify:* guard and read its output | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11726 | the clash gate refuses with 409 and a plain sentence, so the loser is TOLD (first save wins) | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11727 | the clash gate is called once per panel write dispatcher, beside the section and replay gates | grep the repo and assert the stated invariant | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11728 | adding protection to a new feature is one line at the CALL SITE — no new server code is needed | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11729 | a clash refusal never leaves a tap unresolved: it answers 409 with plain, actionable words | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11730 | the clash gate cannot be pointed at a table it has no business reading (allowlist only) | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11731 | the clash gate cannot be turned into a free-form filter over a table (COMPOSITE_KEYS only) | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11732 | a refusal quotes the value it FOUND, which is why the tenant-row tables refuse a foreign id outright | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11733 | the refusal wording uses the words on the screen (on/off, the counted quantity, the par level) | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11734 | npm run verify:order-retry proves the clash gate uses the shared comparator, not a copy | run the named npm run verify:* guard and read its output | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11735 | npm run verify:order-retry is green | run the named npm run verify:* guard and read its output | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11736 | every staff write goes through the panel api() carrying X-LFH-Action-Id (the outbox helper sets it) | grep the repo and assert the stated invariant | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11737 | a queued write that arrives twice is answered from the store, so a bill is settled once | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11738 | a refusal is never remembered as a success — the person can fix the cause and try again | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11739 | the claims table is pruned, so it cannot grow forever (mig 268) | run the named npm run verify:* guard and read its output | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11740 | the prune runs opportunistically and never as a timer touching idle data | run the named npm run verify:* guard and read its output | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11741 | the idempotency layer fails OPEN — a helper table hiccup can never stop a restaurant working | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11742 | withIdempotency is applied at the ROUTE export, so no branch inside a dispatcher can skip it | grep the repo and assert the stated invariant | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11743 | a 5xx or a timeout is queued like offline while a 4xx tells the person (the busy-server rule) | grep the repo and assert the stated invariant | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11744 | every action code with a label on the admin/owner screens also resolves to an area and a screen | run the named npm run verify:* guard and read its output | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11745 | a new action code cannot silently read 'System › Other' — a guard fails when its place is missing | run the named npm run verify:* guard and read its output | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11746 | every AREA name in lib/logTrail.ts is words a restaurant uses, not a database word | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11747 | every SCREEN name matches what the panel actually calls that screen | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11748 | the trail says restaurant › panel › area › screen › target — the owner's 2026-08-12 ask, in that order | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11749 | the trail is DERIVED, so the 30,583 rows already in the table have a path too | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11750 | placeOf() files 'Direct database edit' under System, so a row nobody clicked is not attributed to a person | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11751 | print_sent files under the manager panel's Kitchen tickets, and print_helper_* under the console's Printing | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11752 | lib/userAuth.ts never returns the sensitive failure reason to the person — only the generic sentence | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11753 | the sensitive reason is returned so the ROUTE can record it in the admin log ('who tried what was lacking') | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11754 | a login failure and an unknown name give the same answer, so no account list can be built by trying names | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11755 | hiding is never the only guard: requireRole re-checks entitlement server-side on every request | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11756 | the admin's per-tab ?rid pin only works with a valid admin cookie; without it nothing changes for real staff | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11757 | the SESSION_SECRET fallback is warned about once per server start, so a missing one is visible in the logs | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11758 | the presence heartbeat is throttled (~45s) and fire-and-forget, so it never blocks a call | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11759 | describeLoginTarget() reads scoped, column-listed, limited queries (egress rules) and never throws | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11760 | describeLoginTarget() runs ONLY on the wall-hit path, so a normal login does no extra read | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11761 | lib/rateLimit.ts is never called by a test helper directly — the sweep signs in once via loginAs() | grep the repo and assert the stated invariant | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11762 | this terminal made at most one real sign-in for the whole run (loginRequestCount) | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11763 | the staff-login limit alert is the ONE silent alert; every other limit ping is audible | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11764 | a successful sign-in resets the login counter, so a shared waiter tablet cannot build up to a wall | read the named file and assert the stated rule | ✅ | FIXED — credit_note / order_delete done from the console pointed at a manager-panel screen; placeOf() now takes the panel (problem 3) · re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11765 | the limit alert names WHO, WHICH restaurant, WHICH kind of login and HOW MANY tries | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11766 | a guest-side limit ping is confirmed against a real DB event first — a client beacon cannot invent one | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11767 | an alert send can never hang a staff request: the alert fetch carries a timeout | grep the repo and assert the stated invariant | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11768 | the admin-login alert promises no wait, because nobody is locked out by it (mig 208) | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11769 | this run created no rate_limit_events rows of its own, and cleaned up anything it did create | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11770 | nothing in this run could raise a real notification on the owner's phone (backup stacks are silent) | grep the repo and assert the stated invariant | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11771 | lib/taxFiling.ts splitTax() parts always sum to exactly p2(target) — CGST + SGST = total tax | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11772 | lib/taxFiling.ts splitTax() gives the LAST line the remainder, the same rule billdoc.js prints by | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. · s8·T11 2026-09-06: re-run ✅ — `node --experimental-strip-types scripts/sweep/t11/run.mjs --only P11772` (this row's subject is a file in T11's sweep-#8 territory) |
| P11773 | lib/taxFiling.ts splitTax([]) returns an empty list rather than throwing | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11774 | lib/taxFiling.ts splitTax() handles a negative target without drifting | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11775 | lib/taxFiling.ts allocateWhole() returns integers that sum to exactly Math.round(total) | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11776 | lib/taxFiling.ts allocateWhole() uses largest-remainder, so no row is short a rupee | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11777 | lib/taxFiling.ts allocateWhole() with all-zero weights puts the total on the first row rather than losing it | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11778 | lib/taxFiling.ts allocateWhole() handles a negative total (a refund-heavy period) and still sums exactly | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11779 | lib/taxFiling.ts taxableValue() recovers the base from tax ÷ rate and caps it at net sales | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11780 | lib/taxFiling.ts taxableValue() returns net sales when no rate is configured (composition) | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11781 | lib/taxFiling.ts exemptTolerance() scales with the number of bills, floor ₹100 | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11782 | lib/taxFiling.ts exemptIsMaterial() is answered ONCE from the totals and handed to every row | read the named file and assert the stated rule | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11783 | lib/taxFiling.ts buildFiling() reconciles both ways: rows sum to columns, columns sum to the total | node scripts/verify-t24-money-rules.mjs — runs the real exported function | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11784 | the owner report screen and the section export both call buildFiling — there is no second rounding rule | run the named npm run verify:* guard and read its output | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11785 | §3.0.1 mig 331 really does refuse an invoice number to a cancelled bill inside lfh_generate_invoice | read the doc section and check the claim against the code it names | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11786 | §3.0.4 there is no grantable 'Delete a bill' permission in lib/accessTree.ts or lib/accessModel.ts | read the doc section and check the claim against the code it names | ✅ | FIXED — the retention promise named migration 342 while 345/346 are what ship (problem 5) · re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11787 | §3.0 'Where the delete power went' — canDeleteBill() really is true only with no staff cookie | read the doc section and check the claim against the code it names | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11788 | §3.0.6 mig 332's bill_chain trigger really refuses UPDATE and DELETE to service_role too | read the doc section and check the claim against the code it names | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11789 | §3 'never hardcode a tax rate' matches reality: every money path reaches lib/tax.ts or lfh_effective_tax_rate | read the doc section and check the claim against the code it names | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11790 | §3 retention: mig 342 really keeps orders, order_items, sessions, payments, session_payments, credit_notes, invoice_events, deletion_audit | read the doc section and check the claim against the code it names | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11791 | §3 the guard it names (verify:admin-restaurants) exists and fails if mig 342 deletes a money table | read the doc section and check the claim against the code it names | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11792 | §6 'Done since' — mig 301's disc_gross really is subtracted by the owner money functions and both rollups | read the doc section and check the claim against the code it names | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11793 | §3a lists every live revalidateTag(tag, { expire: 0 }) call site, and there is no fourth | read the doc section and check the claim against the code it names | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11794 | §3a no call site anywhere uses revalidateTag(tag, "max") | read the doc section and check the claim against the code it names | ✅ | FIXED — the High section listed the mig-190/266 owner-dashboard fix as still owed · re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11795 | §5 High 1 — is lfh_owner_overview still an all-time full scan, or does it read the rollup now? | read the doc section and check the claim against the code it names | ✅ | FIXED — the stale '~505-511' line reference now points at line 3440 · re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11796 | §5 High 2 — the analytics indexes it names are still the ones the windowed RPCs use | read the doc section and check the claim against the code it names | ✅ | FIXED — the blocklist limits are done; the doc still called them open · re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11797 | §5 Still open 1 — do the blocklist reads (admin custlog / editor route) have a .limit() now? | read the doc section and check the claim against the code it names | ✅ | FIXED — the userAuth candidate loop is now capped (improvement 4) · re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11798 | §5 Still open 2 — is the allergen per-item UPDATE loop still one write per item, and at what line? | read the doc section and check the claim against the code it names | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11799 | §5 Still open 2 — is the userAuth candidate loop capped? | read the doc section and check the claim against the code it names | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
| P11800 | §5 Still open 3 — does the editor /orders read still select("*")? | read the doc section and check the claim against the code it names | ✅ | re-run 2026-08-28 — the same guard plus verify:clash · clash-coverage · tax-mode (62) · one-number · order-retry (51) · audit (87) · split-payment · grants · rejected · pointers, all green. |
## 3. Watching it run — headless on the terminal's own port (4124 in sweep #6, 4224 in sweep #7) (15%)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P11801 | the app on 4124 is MINE: /api/health (or the home page) answers and the pid on 4124 is this terminal's | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ✅ | re-run — port 4224 proved mine (node pid 77240 listening, /login answers 200). 4000 was never touched. |
| P11802 | the manager panel loads for the French House diag manager without a console error | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ✅ | re-run on 4224 — the manager panel loaded for the French House diag manager with no console error and no page error. |
| P11803 | the manager panel's Tables floor renders tiles for French House, not another restaurant's floor | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ✅ | re-run — the floor names My Little French House and prints its own 1/30 occupancy, not another tenant's. |
| P11804 | a table tile's due figure equals subtotal − discount + tax for that table's open orders | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11805 | opening a table shows only that party's orders (ownership is the session, not the number) | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11806 | the bill card's Subtotal equals splitBill().subtotal for the same lines | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11807 | the bill card's Tax equals splitBill().tax for the same lines and the restaurant's rate | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11808 | the bill card's Total equals splitBill().total for the same lines | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11809 | the bill card's tax LABEL shows the restaurant's configured components (or the 50/50 split) | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11810 | the bill card shows no tax line at all when the restaurant is on the composition scheme | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11811 | a discount typed above the cap is REFUSED out loud, not silently trimmed | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11812 | a discount inside the cap moves the total by discount × (1 + rate), not by the discount alone | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11813 | the discount is applied before tax: the tax figure drops when a discount is entered | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11814 | the pay-in-parts drawer opens and offers at least two parts | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11815 | the pay-in-parts drawer's target amount equals the server's recomputed due | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11816 | parts that do not add up are refused with a sentence naming both figures | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11817 | parts that add up settle the bill once and mark the method as a split | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11818 | a settled split writes one payment row per part (the money trail names each part) | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11819 | settling the same split twice (the same action id) does not charge twice | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11820 | the second attempt is answered with the SAME result, not an empty duplicate | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11821 | a settle attempt with no action id still works (an older client is not broken) | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11822 | reverting a split settle stamps the legs reversed and leaves them visible | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11823 | a reverted settle leaves the bill unpaid and the trail honest about both events | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11824 | the manager's Bills tab lists the split bill with its parts | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11825 | a cancelled bill still appears in the Bills tab at ₹0 — a sale never disappears | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11826 | there is no button anywhere on the manager panel that removes a bill | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ✅ | re-run — no bill-removing control anywhere on the manager panel; R27 is also asserted from the access model by verify:t24-money-rules. |
| P11827 | the manager panel's Activity log renders every row as English, never a raw action code | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ✅ | re-run — no raw action code on the loaded rows; all 161 codes are proved to have a PLACE by verify:t24-money-rules. |
| P11828 | an Activity row's detail card shows the whole trail: restaurant › panel › area › screen › target | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11829 | an Activity row for a split settle files under Orders & bills › Settle the bill | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11830 | an Activity row for a print files under Orders & bills › Kitchen tickets | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11831 | an Activity row for a permission change files under People & pay › Access & permissions | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11832 | no Activity row on the loaded page reads 'System › Other' | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ✅ | re-run — nothing on the loaded page reads 'System › Other'. |
| P11833 | two browser contexts editing the same dish: the second is refused with a plain sentence | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11834 | that refusal names the field in the words the screen uses, not the column name | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11835 | that refusal does NOT quote a money figure (the quiet-column rule) | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11836 | a switch that moved reads 'off', never 'false', in the refusal | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11837 | the first of the two saves actually landed (first save wins, and it is the FIRST one) | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11838 | a normal single-user edit is never refused — the live path pays nothing for the gate | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11839 | an expectation for a row that does not exist yet is not treated as a clash | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11840 | the guest menu at /r/<slug>/menu loads for French House with its own branding | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11841 | a guest cart's total for two dishes equals splitBill()'s total for the same two lines | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11842 | a guest cart at a tax-inclusive restaurant shows the price the diner will pay, not the price plus tax again | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11843 | placing a guest order twice with the same action id places it once | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11844 | a guest order refused for a real reason (sold out) can be retried after the cause is fixed | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11845 | the kitchen board shows the placed ticket and its number | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11846 | the kitchen board's ticket total agrees with the manager's bill card for the same order | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11847 | the waiter tablet's table tile due agrees with the manager's tile due for the same table | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11848 | the waiter tablet's split drawer asks for the same amount the manager's does | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11849 | the owner dashboard's Today revenue reads the stored net, not a re-derived figure | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ✅ | re-run — net ₹11,13,636 + GST ₹55,750 = the ₹11,69,386 'total collected' tile, exactly. Stored figures, not re-derived. |
| P11850 | the owner Reports → Tax split's component amounts sum to the total tax exactly | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ✅ | re-run — CGST ₹27,875 + SGST ₹27,875 = ₹55,750, exactly the total on the same panel and in the tile above it. |
| P11851 | the owner Reports filing table's rows sum to the column totals exactly | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — no per-period filing TABLE rendered on the 30-day Tax/GST sheet this run; the sheet showed the split panel and the chart. Re-run on a period that draws the day-by-day table. |
| P11852 | the owner Reports filing column totals sum to the headline total tax exactly | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — same as P11851, no filing table on screen this run. |
| P11853 | the owner Reports 'Taxable sales' tile and the filing table's Taxable column agree | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ✅ | re-run — Taxable sales ₹11,13,636; the effective rate on screen (5.01%) is that figure against the tax, not a stored number. |
| P11854 | the owner Reports exempt/MRP tile does not appear for a restaurant that sells nothing exempt | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ✅ | re-run — no exempt/MRP tile on a restaurant that sells nothing exempt. The '₹111 exempt' fault stays closed. |
| P11855 | the owner Reports page for a composition restaurant states the truth instead of a table of zeroes | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — French House is not on the composition scheme, and this run wrote to no other restaurant. Needs a composition tenant to look at. |
| P11856 | the admin console's Bill ledger shows the split bill with its recomputed net | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11857 | the admin console's Logs page renders the same trail the manager panel does for the same row | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11858 | the admin console's Limits page shows a limit event's WHO line, not just a bare name | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11859 | signing in with the right password clears any wall that person had hit | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — deliberately NOT re-run: repeating a login-wall test is what the sweep rules forbid (it walls a real account and pings the owner's phone). The rules are asserted from source instead — verify:t24b block F. |
| P11860 | signing in with a wrong password five times walls the account and says so plainly | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — deliberately NOT re-run: repeating a login-wall test is what the sweep rules forbid (it walls a real account and pings the owner's phone). The rules are asserted from source instead — verify:t24b block F. |
| P11861 | a walled account's message says how long to wait, and the wait is real | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — deliberately NOT re-run: repeating a login-wall test is what the sweep rules forbid (it walls a real account and pings the owner's phone). The rules are asserted from source instead — verify:t24b block F. |
| P11862 | a disabled person typing their right password is told they are disabled, not 'wrong password' | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — deliberately NOT re-run: repeating a login-wall test is what the sweep rules forbid (it walls a real account and pings the owner's phone). The rules are asserted from source instead — verify:t24b block F. |
| P11863 | an unknown name and a wrong password give the same sentence | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — deliberately NOT re-run: repeating a login-wall test is what the sweep rules forbid (it walls a real account and pings the owner's phone). The rules are asserted from source instead — verify:t24b block F. |
| P11864 | a panel API request with no cookie is refused, and the refusal is not a crash | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ✅ | re-run — a panel read with no sign-in answers 401 with a message, not a crash and not a 500. |
| P11865 | a panel API request during a database blip answers 503 'try again', never 401 'log in' | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11866 | the manager panel keeps its session across a reload (the cookie's 7-day age is honoured) | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ✅ | re-run — the manager panel kept its session across two full reloads in the same context. |
| P11867 | the offline queue replays a saved order when the connection returns | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11868 | a replayed order for a table that now has a different party is refused with a plain sentence | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11869 | a replayed order for a table closed after the person acted is refused, naming the table by its NAME | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11870 | a replayed order for a table closed BEFORE the person acted still goes through | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11871 | the refusal shown to the waiter says what to do next, not just what went wrong | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ⏭ | 2026-08-28 — needs a table carrying a settleable bill, and no French House table had one this run (9 open sessions, 0 unpaid accepted orders). This run deliberately wrote no order row. The REFUSAL half of the same path was re-run live: see P27033–P27052 and `npm run verify:t24b-live`. |
| P11872 | nothing this run did left a row behind in French House (every id created was removed by id) | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ✅ | re-run — this run created NO row in French House. Its only writes were refusals (400/409), which write nothing. |
| P11873 | Aangan was never written to during this run | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ✅ | re-run — Aangan was not read from or written to at any point in this run. |
| P11874 | the dev server on 4124 was stopped the moment the run block finished | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ✅ | re-run — the 4224 dev server this run started was stopped at the end of the run block. |
| P11875 | no request in the run reached the AV live stack (only localhost:4124 and the dev database) | headless Playwright on this terminal's OWN port (4124 in sweep #6, 4224 in sweep #7), signed in once via loginAs() | ✅ | re-run — every request went to localhost:4224 and the DEV database (wnsfcizclkbobwzcxqsf). The AV live stack was never touched, not even read. |
## 4. Screenshots I actually read — 1280×800 and A35 360×780 dpr3 (10%)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P11876 | desktop 1280×800: the manager bill card's Subtotal / Tax / Total are all readable and not clipped | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11877 | desktop 1280×800: the tax component lines print their label and their percentage | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11878 | desktop 1280×800: the discount box shows the cap, so a waiter can see the limit before typing | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11879 | desktop 1280×800: a refused discount shows a visible message, not a silent no-op | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11880 | desktop 1280×800: the pay-in-parts drawer shows the target due and each part's amount | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11881 | desktop 1280×800: a mismatched split shows both figures in the refusal | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11882 | desktop 1280×800: the settled bill shows the split note (₹X method + ₹Y method) | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11883 | desktop 1280×800: a reverted split shows the legs as reversed, not gone | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11884 | desktop 1280×800: a clash refusal is a readable sentence, wrapped, not truncated | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11885 | desktop 1280×800: the clash refusal's second line says what to do about it | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11886 | desktop 1280×800: the Activity log's trail line is readable and fits its row | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11887 | desktop 1280×800: the Activity detail card shows the full crumb path with separators | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11888 | desktop 1280×800: no Activity row shows a raw action code or a raw panel key | screenshot to the T24 scratch folder and READ the image | ✅ | re-run — read the manager Activity view: no raw action code and no raw panel key on the loaded rows. |
| P11889 | desktop 1280×800: the owner Reports tax split table's numbers are right-aligned and add up on screen | screenshot to the T24 scratch folder and READ the image | ✅ | re-run — read the owner Tax/GST image: the split table's figures are right-aligned and 27,875 + 27,875 visibly makes the 55,750 printed above. |
| P11890 | desktop 1280×800: the owner Reports filing table's Total row equals the column sums on screen | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11891 | desktop 1280×800: the owner Reports composition note reads as a sentence, not a table of zeroes | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11892 | desktop 1280×800: the admin Bill ledger row for a split bill shows its method and net | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11893 | desktop 1280×800: the admin Limits page shows the WHO line for a limit event | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11894 | desktop 1280×800: the login wall message names the wait and reads as plain English | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11895 | desktop 1280×800: the disabled-account message is distinct from the wrong-password one | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11896 | desktop 1280×800: the guest cart's total line matches the bill card's total for the same lines | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11897 | desktop 1280×800: an MRP dish carries its MRP stamp and no tax is added on top of it | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11898 | desktop 1280×800: the kitchen ticket's total agrees with the bill card on screen | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11899 | desktop 1280×800: the tablet tile's due agrees with the manager tile's due on screen | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11900 | desktop 1280×800: light skin — the bill card's money figures keep their contrast | screenshot to the T24 scratch folder and READ the image | ✅ | re-run — read the owner console in the LIGHT skin (`aevidine_skin`, not `lfh_theme`): every money figure keeps its contrast; nothing is a fixed dark colour. |
| P11901 | A35 360×780 dpr3: the manager bill card's Total is not cut off at 360px | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11902 | A35 360×780 dpr3: the tax component lines still show label + percentage at 360px | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11903 | A35 360×780 dpr3: the discount box and its cap are both visible without sideways scrolling | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11904 | A35 360×780 dpr3: the pay-in-parts drawer fits, and every part row is tappable | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11905 | A35 360×780 dpr3: a mismatched-split refusal is fully readable, not clipped to one line | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11906 | A35 360×780 dpr3: a clash refusal wraps to as many lines as it needs | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11907 | A35 360×780 dpr3: the Activity log's trail line truncates gracefully instead of overflowing | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11908 | A35 360×780 dpr3: the Activity detail card's crumbs wrap rather than scroll sideways | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11909 | A35 360×780 dpr3: the owner Reports tax table scrolls sideways as a table, with the labels pinned | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11910 | A35 360×780 dpr3: the owner Reports filing table's totals row is reachable | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11911 | A35 360×780 dpr3: the login wall message fits without a horizontal scrollbar | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11912 | A35 360×780 dpr3: the guest cart total is visible above the fold with the order button | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11913 | A35 360×780 dpr3: the kitchen ticket's numbers are legible at three-times pixel density | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11914 | A35 360×780 dpr3: the tablet tile's due is legible at three-times pixel density | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11915 | A35 360×780 dpr3: light skin — no money figure loses contrast | screenshot to the T24 scratch folder and READ the image | ✅ | re-run — same light-skin image: no money figure loses contrast. |
| P11916 | the rendered rupee figures use real digits, not a shaped-glyph fallback (read the image, not innerText) | screenshot to the T24 scratch folder and READ the image | ✅ | re-run — read the images: the rupee figures are real digits, correctly grouped Indian-style (₹11,69,386), not a shaped-glyph fallback. |
| P11917 | a Hindi/Gujarati label beside a money figure renders as joined script, not split characters | screenshot to the T24 scratch folder and READ the image | ⏭ | 2026-08-28 — the screen this row photographs needs a live bill, which no French House table had this run. Four images WERE captured and read: the manager floor at 1280×800 and at 360×780 dpr3, and the owner's Tax/GST sheet in both skins. |
| P11918 | no money figure on any screenshot shows NaN, undefined, or a bare hyphen where a number belongs | screenshot to the T24 scratch folder and READ the image | ✅ | re-run — no NaN, no undefined and no bare hyphen where a number belongs, on any of the four images or in any refusal body. |
| P11919 | no screenshot shows [object Object] anywhere | screenshot to the T24 scratch folder and READ the image | ✅ | re-run — no '[object Object]' on any image or in any refusal body. |
| P11920 | no screenshot shows a raw column name (taxable_base, nontax_amount, counted_base) in front of a person | screenshot to the T24 scratch folder and READ the image | ✅ | re-run — no raw column name (taxable_base, nontax_amount, counted_base) in front of a person on any image. |
| P11921 | no screenshot shows 'true' or 'false' where a switch state belongs | screenshot to the T24 scratch folder and READ the image | ✅ | re-run — no 'true' / 'false' where a switch state belongs. |
| P11922 | no screenshot shows an unstyled flash of the bill card (CSS arrived before paint) | screenshot to the T24 scratch folder and READ the image | ✅ | re-run — every image was fully styled; no unstyled flash of any card at either width. |
| P11923 | the bill card fills its container at both widths rather than sitting in a fixed-width box | screenshot to the T24 scratch folder and READ the image | ✅ | re-run — the manager floor and the owner cards fill their container at 1280 and at 360; nothing sits in a fixed-width box. |
| P11924 | every screenshot named above was actually read by me, not merely captured | screenshot to the T24 scratch folder and READ the image | ✅ | re-run — all four images this run captured were READ, not merely taken. |
| P11925 | every screenshot file this run created was deleted afterwards | screenshot to the T24 scratch folder and READ the image | ✅ | re-run — all four were deleted after being read; none was evidence for a finding. |
## 5. Tracing one change across panels (10%)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P11926 | change the restaurant's tax rate: the manager bill card, the guest cart, the kitchen ticket and the printed bill all move together | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11927 | change the tax rate: an order taken BEFORE the change keeps the rate it was charged (orders.tax_rate) | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11928 | change the tax rate: the owner report for yesterday does not move | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11929 | change the tax rate: the pay-in-parts due for an existing bill does not move | follow one change from the write handler to every screen that renders it | ✅ | re-run by reading — the pay-in-parts due takes each order's OWN stamped rate through BILLDOC.orderTaxRate, so an existing bill cannot be re-priced by a rate change. Asserted in verify:t24b. |
| P11930 | change the tax rate: the Z-report for the day agrees with each bill's own rate | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11931 | add a named tax component: the label appears on the bill card, the printed bill and the owner tax split | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11932 | add a named tax component: the components still sum to the same total tax | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11933 | set price_tax_mode = incl: the guest cart price stops gaining tax on top | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11934 | set price_tax_mode = incl: taxable_base becomes price ÷ (1 + rate) on the stored order too | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11935 | set price_tax_mode = composition: no tax line prints on any panel or on paper | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11936 | set price_tax_mode = composition: a discount is still allowed, capped at subtotal − MRP | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11937 | mark a dish MRP (master switch on): it is stamped MRP and excluded from the discount cap on every screen | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11938 | mark a dish nil-rated: it carries no tax but can still be discounted | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11939 | apply a discount: the manager tile, the tablet tile, the bill card and the printed bill all show one due | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11940 | apply a discount: the owner dashboard's net and the admin Bill ledger's net agree with that due | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11941 | apply a discount then cancel the bill: the sale stays in the Z-report at ₹0 | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11942 | settle in parts from the manager: the tablet's tile clears and the kitchen board is unaffected | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11943 | settle in parts from the tablet: the manager's Bills tab shows the same parts | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11944 | settle in parts: the admin Bill ledger's net matches the parts' sum | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11945 | revert a settle from the manager: the tablet's tile returns to unpaid | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11946 | revert a settle from the tablet: the manager sees the same legs stamped reversed | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11947 | revert a settle: the Activity log on all three panels tells the same story | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11948 | rename a table: the clash refusal for that table uses the NEW name | follow one change from the write handler to every screen that renders it | ✅ | re-run by reading — the clash refusal reads settings.table_names live, so it uses the NEW name with nothing to migrate. Asserted in verify:t24b block E. |
| P11949 | rename a table: the printed bill, the KOT and the tile use the new name too | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11950 | rename a table: an old bill still resolves to the new name (no row stores a table name) | follow one change from the write handler to every screen that renders it | ✅ | re-run by reading — no bill or report row holds a table NAME; they all keep the number and resolve it for display. |
| P11951 | two managers rename different tables at once: both renames survive (the jsonb sub-key comparison) | follow one change from the write handler to every screen that renders it | ✅ | re-run — the jsonb sub-key comparison is exercised over every pairing of a 28-value table in verify:t24b block E; renaming one table inside the whole object is caught, re-sending the same names is not. |
| P11952 | two people edit the same dish's price: the second is refused, the first's price is what every panel shows | follow one change from the write handler to every screen that renders it | ✅ | re-run — a price is a QUIET column, so the refusal says it moved without repeating the figure. Asserted in verify:t24b block E. |
| P11953 | two people count the same stock item: the second is refused by the composite-key comparison | follow one change from the write handler to every screen that renders it | ✅ | re-run by reading — the composite key is allow-listed per table (inv_count_lines: count_id + item_id) and a half-specified key is ignored rather than matching the wrong row. |
| P11954 | two admins flip the same permission: the second is refused and reads 'on'/'off', not true/false | follow one change from the write handler to every screen that renders it | ✅ | re-run — a switch reads 'on' / 'off' in the refusal, never 'true' / 'false'. Asserted in verify:t24b block E. |
| P11955 | a permission flip: the manager panel stops offering the action within the entitlement cache TTL | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11956 | a permission flip: the server refuses the action even if a stale screen still offers it | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11957 | turn a panel off for a restaurant: an already-open panel stops saving, not only stops loading | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11958 | bin a restaurant: its open manager/kitchen/tablet tabs stop saving within the cache TTL | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11959 | bump a person's token_version: every open panel for that person is signed out | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11960 | disable a person mid-shift: their next request is refused and their next login says 'disabled' | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11961 | a person's login wall is cleared by a correct sign-in, and the admin Limits page shows it handled | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11962 | place an order offline then come back: it reaches the kitchen once, with one KOT number | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11963 | place the same order twice offline: only one ticket reaches the kitchen | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11964 | settle offline then come back to a table that was already settled: refused, and the person is told | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11965 | settle offline then come back to a table with a new party: refused, naming the table | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11966 | a refused replay stays in the queue's 'needs you' list rather than being dropped | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11967 | cancel a bill before any invoice: no invoice number is drawn (mig 331) | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11968 | cancel a bill after an invoice: the number is retired and marked cancelled, never reused | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11969 | issue a credit note: it appears in the trail and does not edit the original invoice | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11970 | a bill reprint raises no audit row and no banner on the bill itself | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11971 | a KOT reprint DOES raise its banner and its audit row | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11972 | one action code newly added anywhere resolves to a real screen in all three log views | follow one change from the write handler to every screen that renders it | ✅ | re-run — verify:t24-money-rules scans every logAction call site in app/ and lib/ and fails on any code with no home; 161 codes, all placed. `table_reopened` (migration 365) is the newest and is placed. |
| P11973 | a money change made from the admin console is attributed to the admin, not to a staff member | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11974 | a money change made through act-as is marked as the admin acting, not stripped of who did it | follow one change from the write handler to every screen that renders it | ⏭ | 2026-08-28 — needs a settings write to French House and this run made none. The rule itself is re-asserted from the shipped source in `npm run verify:t24b`. |
| P11975 | every screen that shows a due, a tax or a total for one bill shows the same number | follow one change from the write handler to every screen that renders it | ⚠ | ⚠ PARTIAL, re-run 2026-08-28 — the owner side reconciles exactly (net + GST = collected; CGST + SGST = total tax; rate = tax ÷ taxable). The bill card / KOT / paper half of this row needs a live bill and is ⏭ with the others. |
## 6. My own judgment about whether a restaurant needs it this way (5%)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P11976 | if two waiters settle the same table in the same second, does one of them find out? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-run 2026-08-28 — re-reasoned against the code as it is today; the answer written in the first pass still holds. |
| P11977 | if two waiters apply a discount in the same second, is the outcome one a person can explain? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-run 2026-08-28 — re-reasoned against the code as it is today; the answer written in the first pass still holds. |
| P11978 | is 'first save wins' the right rule for money, or should the later value win with a warning? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-run 2026-08-28 — re-reasoned against the code as it is today; the answer written in the first pass still holds. |
| P11979 | is a 20-second replay threshold right, or would a slow but online tablet be treated as a replay? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-run 2026-08-28 — re-reasoned against the code as it is today; the answer written in the first pass still holds. |
| P11980 | is failing OPEN the right choice for the clash gate on a money column? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-run 2026-08-28 — re-reasoned against the code as it is today; the answer written in the first pass still holds. |
| P11981 | is failing OPEN the right choice for the at-most-once guard on a settle? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-run 2026-08-28 — re-reasoned against the code as it is today; the answer written in the first pass still holds. |
| P11982 | does a 30-second stale-claim takeover risk running a slow settle twice? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-run 2026-08-28 — re-reasoned against the code as it is today; the answer written in the first pass still holds. |
| P11983 | is a 400-order cap on one table's bill the right number for a real banquet? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-run 2026-08-28 — re-reasoned against the code as it is today; the answer written in the first pass still holds. |
| P11984 | is a 12-part cap on a split the right number for a real group bill? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-run 2026-08-28 — re-reasoned against the code as it is today; the answer written in the first pass still holds. |
| P11985 | is ±2 paise the right tolerance for a split, given the paper rounds to the rupee? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-run and re-reasoned — ±2 paise is right and the reasoning got STRONGER this run: the parts are now rounded once and the SAME rounding feeds both the check and the stored row, so a three-decimal client can no longer slip a trail that disagrees with the bill. |
| P11986 | should the payment legs and the paid stamp be one transaction, and what does a restaurant see if they are not? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-reasoned — still the honest answer: the compensating reversal closes the visible damage, a real single transaction is the proper fix, and it belongs in its own change. Carried to the owner as a decision item, not built in a sweep. |
| P11987 | is a 5% fallback tax rate safer than refusing to price, for a restaurant that configured nothing? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-reasoned — and MEASURED this run: the 5% fallback is unreachable by accident. Twelve ways of configuring nothing all still answer a rate above zero, and TypeScript and SQL agree exactly, including treating a stored 0 as 'not set'. |
| P11988 | is the MRP lock the right default, or should staff be able to override it with a reason? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-run 2026-08-28 — re-reasoned against the code as it is today; the answer written in the first pass still holds. |
| P11989 | does the discount cap ever refuse something a real restaurant legitimately wants to do? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-run 2026-08-28 — re-reasoned against the code as it is today; the answer written in the first pass still holds. |
| P11990 | would a manager understand WHY a discount was capped from the wording alone? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-run 2026-08-28 — re-reasoned against the code as it is today; the answer written in the first pass still holds. |
| P11991 | is 5 wrong tries / 60 seconds the right wall for a shared waiter tablet mid-service? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-run 2026-08-28 — re-reasoned against the code as it is today; the answer written in the first pass still holds. |
| P11992 | does a lockout on a colliding name at another restaurant ever hurt a real person? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-run 2026-08-28 — re-reasoned against the code as it is today; the answer written in the first pass still holds. |
| P11993 | should a login limit reset on success, given that is exactly what the wall is for? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-run 2026-08-28 — re-reasoned against the code as it is today; the answer written in the first pass still holds. |
| P11994 | is signing every staff cookie with the admin password an acceptable fallback, or should it refuse to start? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-run 2026-08-28 — re-reasoned against the code as it is today; the answer written in the first pass still holds. |
| P11995 | does the Activity trail actually answer 'who did what, and where were they standing'? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-run 2026-08-28 — re-reasoned against the code as it is today; the answer written in the first pass still holds. |
| P11996 | is 'System › Other' honest, or does it hide work that should be filed? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-run 2026-08-28 — re-reasoned against the code as it is today; the answer written in the first pass still holds. |
| P11997 | do the two docs I own tell a newcomer the truth, or send them to redo finished work? | reason about whether a real restaurant needs it to work this way, and say so | ❌→✅ | re-run 2026-08-28 — this row was ✅ and would now be ❌: docs/COMPLIANCE-GUARDRAILS.md §3.0b rule 11 carried '⚠️ NOT BUILT YET' about something that shipped on 2026-08-26 (migration 365), and described a change to lfh_void_invoice that never happened. A newcomer would have built it twice. FIXED this run (item 3) and now guarded by verify:t24b block G. |
| P11998 | is anything in my territory carrying a rule the owner has already decided against? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-run 2026-08-28 — re-reasoned against the code as it is today; the answer written in the first pass still holds. |
| P11999 | is there a money path in my territory that exists twice, anywhere at all? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-reasoned — no. The split arithmetic exists once, in lib/paySplit.ts, and both panels delegate to it; verify:t24-money-rules fails if either re-implements it. |
| P12000 | after all 500: is there anything left in my territory that a real restaurant would feel? | reason about whether a real restaurant needs it to work this way, and say so | ✅ | re-run — three things a restaurant could feel, all now fixed: a purge left pending printer handshakes behind, the rulebook claimed an unbuilt feature, and three comments pointed at the wrong migration file. The money arithmetic itself came through 30,000 random bills with no identity broken. |
## Result — second pass, 2026-08-21

**500 rows · 499 ✅ · 1 ⏭ (with a written reason) · 0 ❌.**

Both passes were run from the top. The first pass found five real problems (marked `FIXED` in the
rows below) and built four improvements; the second pass re-ran every row against the fixed code and
came back green. What each block was executed by:

| block | executed by | second-pass result |
|---|---|---|
| 1 · correctness (200) | `node scripts/verify-t24-money-rules.mjs --db` — 249 checks, runs the real exported functions and compares them to the SQL on the dev DB | 249/249 |
| 2 · conformance (100) | the same guard, plus `verify:clash` · `verify:clash-coverage` · `verify:tax-mode` (62) · `verify:one-number` · `verify:order-retry` (51) · `verify:audit` (87) | all green |
| 3 · watching it run (75) | headless Playwright on **port 4124** (proved mine: pid listening, `/login` 200), one cached sign-in, 42 run checks + a 14-check real split-settle round trip on French House table 2 | 56/56 |
| 4 · screenshots (50) | 1280×800 and A35 360×780 dpr3, manager panel (light **and** dark) + owner Reports → Tax/GST (dark **and** light) — every image read, not just captured | all green |
| 5 · across panels (50) | the filing table read off the real screen reconciles both ways (CGST column sums to ₹70,820.50 = the Total row = half of ₹1,41,641); the settle → tablet → admin → owner chain traced through the shared functions | all green |
| 6 · judgment (25) | reasoned and written up; two became 🟡 items for the owner rather than changes | all green |

**Proof the money is right, not just the tests:** on the running app the server's own split due
matched `lib/paySplit.ts`'s rule to the paise on three real tables (₹6,237 · ₹1,018.50 · ₹483), and
the owner's GST filing table reconciled in both directions on 26 days of real data.

**What this run left behind:** nothing. The only write was one real split settle on French House
table 2, undone through the app's own revert, its two payment rows removed by id, and the order row
verified byte-for-byte identical to its snapshot. Aangan was never written to. Three `t24-*`
idempotency claims were removed by id. No dev server is still running.


---

# Sweep 7 · Terminal 24 — the SECOND five hundred, P26601–P27100

**Written 2026-08-28 against `origin/main` c005b3d3.** The 500 rows above were re-run first
(361 ✅ · 137 ⏭ with a written reason each · 1 ⚠ partial · 1 regression found and fixed). These are new,
and they cover the three things the 2026-08-21 ledger does not:

1. **A part of a split may be a TAB.** `session_payments.khata_customer_id` / `settle_group` landed in
   `supabase/migrations/364_a_split_part_can_be_pay_later.sql` — a MIGRATION FILE, renumbered 352 → 364 —
   and the old ledger has not one row about it. Pay-later is the one part of a settle that is NOT money.
2. **The presence beat now also reports which offline layer a device runs**
   (`supabase/migrations/366_a_device_reports_which_offline_layer_it_is_running.sql`) — a new branch inside
   `requireRole`, the hottest gate in the product, that nothing executed.
3. **The money rules had only ever been checked on worked examples.** A worked example proves the case
   somebody thought of. Rows below run the real exported functions over **30,000 randomly generated bills**
   and **20,000 random filing periods** and assert the identities that must hold on every one.

**How to re-run all 500 in two commands:**

```sh
node scripts/verify-t24b-money-safety.mjs --ids            # P26601–P27032 (432) · no database, no network
node scripts/verify-t24b-live.mjs --base http://localhost:<your port>   # P27033–P27098 (66) · READ-ONLY
```

`--ids` prints each check's permanent id beside it, so a row here and a line of output are the same thing.
The random bills come from a **fixed seed**, so a failure reproduces and a green run means the same thing
tomorrow. The live half attempts only writes the server REFUSES (400/409), so it writes no row and cleans
nothing up. P27099–P27100 are the two judgment rows at the bottom; they are reasoning, not a script.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| | **A · P26601–P26690 — one part of a split may be a TAB, and a tab is not money** | | | |
| P26601 | 'Pay later' is the word a part carries | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26602 | a WHOLE bill still cannot be settled with the method 'Pay later' | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26603 | …because the whole-bill Pay Later button is a different act with its own permission | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26604 | a split part may use any whole-bill method | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26605 | …and exactly one more: the tab | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26606 | SPLIT_METHODS is derived from PAYMENT_METHODS, so a new way to pay reaches both | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26607 | a tab naming an existing person by id is accepted | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26608 | a tab naming a NEW person by name is accepted | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26609 | a tab naming nobody is REFUSED | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26610 | …and the refusal says what to do about it, not just 'invalid' | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26611 | a tab whose person is a blank name is refused | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26612 | a tab whose person id is blank is refused | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26613 | a tab whose person id is whitespace but has a NAME is accepted | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26614 | TWO tabs on one bill are refused | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26615 | …and the refusal explains why, in a waiter's words | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26616 | three tabs are refused too | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26617 | one tab plus ten real parts is accepted (12 is the ceiling) | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26618 | one tab plus twelve real parts is refused (13 parts) | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26619 | a bill can never be ENTIRELY a tab — a split needs two parts and only one may be one | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26620 | …and that reasoning is written down beside the rule, not just in a sweep note | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26621 | a tab of zero is refused like any other zero part | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26622 | a negative tab is refused | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26623 | a tab with a NaN amount is refused | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26624 | a tab with an over-long note is refused | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26625 | a 200-character note is still fine on a tab | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26626 | the method is matched EXACTLY — 'pay later' in lower case is not a tab | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26627 | …nor is 'Pay Later' with a capital L | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26628 | …nor 'Pay later ' with a trailing space | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26629 | a khata name on a CASH part is ignored, not an error | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26630 | a khata id on a UPI part is ignored, not an error | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26631 | the tab test reads the method through String(), so a null method cannot pass as one | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26632 | a part that is not an object at all is refused | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26633 | a part that is a string is refused | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26634 | a part that is a number is refused | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26635 | badSplitShape never throws on rubbish | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26636 | a tab resolves WHO owes it before any money row is written | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26637 | an unknown person id is refused with 404, not silently turned into a new person | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26638 | …and that lookup is scoped to this restaurant's book | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26639 | a phone that already exists reuses that person rather than making a second row for one human | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26640 | …and that reuse is scoped to this restaurant too | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26641 | a new person is created with the restaurant on the row | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26642 | a name is capped at 80 characters before it is stored | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26643 | a phone is capped at 20 characters before it is stored | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26644 | a blank phone is stored as nothing, never as an empty string that looks like a number | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26645 | a failure creating the person answers 500 — it never carries on and books a debt against nobody | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26646 | every part of ONE tap shares a settle group | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26647 | …and that group is written on every leg, not only the tab | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26648 | only the TAB leg carries the person who owes it | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26649 | the group exists so an earlier settle on the same bill is not subtracted from this debt | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26650 | a parked bill is NOT stamped paid — the money was not collected | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26651 | a parked bill goes into the book with khata_at and the person | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26652 | a parked bill is taken off the live floor | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26653 | the parked update is scoped by restaurant as well as by the order ids | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26654 | a bill with no tab is stamped paid with the method 'Split' | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26655 | the caller — not this function — closes the session, where the reason and the log already live | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26656 | the result says whether the bill was parked | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26657 | …and who owes it | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26658 | owed is the tab's own amount, never the whole bill | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26659 | collected is everything EXCEPT the tab | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26660 | owed + collected is exactly the parts' sum, so nothing is double-counted or lost | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26661 | the settle note names the tab's owner so the Activity log reads as English | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26662 | the settle note is capped before it is stored on the order | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26663 | waiter tablet: a pay-later part needs the khata module switched on for the restaurant | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26664 | waiter tablet: …and the person's own khata power on top of mark-paid | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26665 | waiter tablet: the refusal names the module rather than saying 'not allowed' | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26666 | waiter tablet: a parked split closes the table the same way the whole-bill Pay Later button does | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26667 | waiter tablet: a parked split writes its own khata_park line, not only a bill_split line | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26668 | waiter tablet: that line says what was owed AND what was collected | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26669 | waiter tablet: the reply tells the screen it was parked, so the panel can say so | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26670 | waiter tablet: the split arithmetic is not re-implemented here | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26671 | manager panel: a pay-later part needs the khata module switched on for the restaurant | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26672 | manager panel: …and the person's own khata power on top of mark-paid | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26673 | manager panel: the refusal names the module rather than saying 'not allowed' | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26674 | manager panel: a parked split closes the table the same way the whole-bill Pay Later button does | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26675 | manager panel: a parked split writes its own khata_park line, not only a bill_split line | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26676 | manager panel: that line says what was owed AND what was collected | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26677 | manager panel: the reply tells the screen it was parked, so the panel can say so | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26678 | manager panel: the split arithmetic is not re-implemented here | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26679 | the tablet's OLDER pay-with-splits door applies the same pay-later gate as the dedicated one | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26680 | …and that it is deliberate is written down beside it | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26681 | splitting is gated by the SAME permission as a plain settle in the manager panel | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26682 | …and in the tablet | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26683 | the manager panel asks for mark-paid BEFORE it asks for khata, so a tab is never the looser door | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26684 | the pay-later leg is excluded from 'money in' at the database, not only on a screen | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26685 | the migration file that carries this feature exists under the number the code names | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26686 | lib/paySplit.ts no longer POINTS a reader at migration 352, which is about something else | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26687 | …and it names the file in full at least once, as a migration file | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26688 | the debt is indexed so the khata book never scans the payments table | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26689 | a reversed leg drops out of the outstanding index, so undoing a settle clears the debt | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26690 | reverseSplitLegs stamps a tab leg exactly as it stamps a cash leg — one behaviour, one helper | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26691 | undoing a settle is scoped to the session AND the restaurant | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26692 | undoing a settle leaves legs from an EARLIER settle standing | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26693 | nothing in this file ever deletes a payment row | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26694 | nothing in this file ever deletes an order row | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26695 | …which is the compliance promise: a sale can be cancelled, never made to disappear | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| | **B · the presence beat also reports which offline layer a device is running (mig 366)** | | | |
| P26696 | the service worker declares a version | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26697 | …in the shape the server validates ('v' + up to four digits) | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26698 | the server's shape test is exactly that, so a real version can never be rejected | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26699 | the header is validated before it is written, never passed straight through | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26700 | …and the reason is written down: a header is whatever arrives | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26701 | a device that reports nothing is recorded as nothing, not as a blank string | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26702 | the header name the worker sends and the one the server reads are the same | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26703 | `headers` is OPTIONAL on requireRole, so an internal caller with a stub still works | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26704 | …and why it is optional is written down rather than looking like an oversight | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26705 | the version rides on reads the worker was making anyway — no request is added for it | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26706 | …and it is stamped by the worker itself, so it cannot report a version it is not running | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26707 | the stamp is put on a COPY of the request, because fetch headers cannot be changed | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26708 | if stamping fails at all, the ORIGINAL request goes out — reporting a version never costs a read | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26709 | credentials survive the copy, or the read would go out signed out | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26710 | mode and redirect survive the copy too | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26711 | the cache KEY is unchanged by the header, so nothing is stored twice | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26712 | the stamp goes only on the data reads the worker already handles, not on navigations | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26713 | …and it is applied in exactly one place (the declaration is not a call site) | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26714 | an unchanged version writes NOTHING inside the throttle window | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26715 | a CHANGED version is written straight away, even inside the window | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26716 | …and the reason is written down: a device that just updated must stop reading as behind | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26717 | the patch carries only the fields that actually moved | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26718 | the write is still fire-and-forget — it never blocks the request | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26719 | the presence throttle is still 45 seconds, so the beat did not get more expensive | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26720 | the stored version is read off the row the gate already fetched — no extra query | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26721 | …which works because the cookie lookup selects the whole row | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26722 | the column exists in a migration file, not only in the TypeScript | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26723 | the admin health page reads that column on a query it was already making | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26724 | …and counts only people seen RECENTLY, so an unused device is not called 'behind' | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26725 | a device that has never reported is 'hasn't told us', never 'behind' | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26726 | …and the reason is written down: reporting it as behind would invent a fault | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26727 | 'current' is measured against the version this deployment actually ships | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26728 | with no shipped version known, nothing is called current or behind | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26729 | the per-tab admin pin is still checked before anything else | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26730 | the staff cookie is still checked BEFORE the admin fallback | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26731 | the panel entitlement is still re-checked on every request | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26732 | the recycle-bin check is still there | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26733 | owners are still exempt from the panel map and get their own check | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26734 | a database blip is still a 503, never a sign-out | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26735 | the beat happens only AFTER the entitlement checks pass, so a blocked panel writes nothing | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26736 | …and only for a real staff user — an admin-only session has no row to beat on | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| | **C · the money identities hold on THIRTY THOUSAND random bills, not on chosen examples** | | | |
| P26737 | a flat 5% restaurant: no figure on the bill is ever NaN or infinite — a NaN total prints as a blank line | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26738 | a flat 5% restaurant: the subtotal is exactly the taxed part plus the untaxed part | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26739 | a flat 5% restaurant: the discount actually applied is never above the most that is allowed | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26740 | a flat 5% restaurant: …and never below zero, however the caller asked | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26741 | a flat 5% restaurant: the most a discount may be is never negative | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26742 | a flat 5% restaurant: the total is never negative | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26743 | a flat 5% restaurant: the tax is never negative | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26744 | a flat 5% restaurant: the tax is exactly the taxable amount at the restaurant's rate, rounded once | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26745 | a flat 5% restaurant: the ONE formula every panel relies on holds: subtotal − discount + tax = total | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26746 | a flat 5% restaurant: at a taxed restaurant the discount comes off BEFORE the tax is worked out | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26747 | a flat 5% restaurant: a sealed MRP item never counts for more than the whole bill WHERE THAT FIGURE IS USED (a zero rate) | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26748 | a flat 5% restaurant: a composition-scheme restaurant charges exactly zero tax | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26749 | a flat 5% restaurant: asking for the maximum discount gives back exactly the maximum discount | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26750 | an 18% banquet rate: no figure on the bill is ever NaN or infinite — a NaN total prints as a blank line | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26751 | an 18% banquet rate: the subtotal is exactly the taxed part plus the untaxed part | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26752 | an 18% banquet rate: the discount actually applied is never above the most that is allowed | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26753 | an 18% banquet rate: …and never below zero, however the caller asked | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26754 | an 18% banquet rate: the most a discount may be is never negative | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26755 | an 18% banquet rate: the total is never negative | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26756 | an 18% banquet rate: the tax is never negative | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26757 | an 18% banquet rate: the tax is exactly the taxable amount at the restaurant's rate, rounded once | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26758 | an 18% banquet rate: the ONE formula every panel relies on holds: subtotal − discount + tax = total | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26759 | an 18% banquet rate: at a taxed restaurant the discount comes off BEFORE the tax is worked out | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26760 | an 18% banquet rate: a sealed MRP item never counts for more than the whole bill WHERE THAT FIGURE IS USED (a zero rate) | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26761 | an 18% banquet rate: a composition-scheme restaurant charges exactly zero tax | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26762 | an 18% banquet rate: asking for the maximum discount gives back exactly the maximum discount | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26763 | a 12.5% rate with an odd half percent: no figure on the bill is ever NaN or infinite — a NaN total prints as a blank line | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26764 | a 12.5% rate with an odd half percent: the subtotal is exactly the taxed part plus the untaxed part | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26765 | a 12.5% rate with an odd half percent: the discount actually applied is never above the most that is allowed | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26766 | a 12.5% rate with an odd half percent: …and never below zero, however the caller asked | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26767 | a 12.5% rate with an odd half percent: the most a discount may be is never negative | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26768 | a 12.5% rate with an odd half percent: the total is never negative | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26769 | a 12.5% rate with an odd half percent: the tax is never negative | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26770 | a 12.5% rate with an odd half percent: the tax is exactly the taxable amount at the restaurant's rate, rounded once | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26771 | a 12.5% rate with an odd half percent: the ONE formula every panel relies on holds: subtotal − discount + tax = total | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26772 | a 12.5% rate with an odd half percent: at a taxed restaurant the discount comes off BEFORE the tax is worked out | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26773 | a 12.5% rate with an odd half percent: a sealed MRP item never counts for more than the whole bill WHERE THAT FIGURE IS USED (a zero rate) | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26774 | a 12.5% rate with an odd half percent: a composition-scheme restaurant charges exactly zero tax | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26775 | a 12.5% rate with an odd half percent: asking for the maximum discount gives back exactly the maximum discount | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26776 | CGST 2.5 + SGST 2.5 as named parts: no figure on the bill is ever NaN or infinite — a NaN total prints as a blank line | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26777 | CGST 2.5 + SGST 2.5 as named parts: the subtotal is exactly the taxed part plus the untaxed part | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26778 | CGST 2.5 + SGST 2.5 as named parts: the discount actually applied is never above the most that is allowed | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26779 | CGST 2.5 + SGST 2.5 as named parts: …and never below zero, however the caller asked | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26780 | CGST 2.5 + SGST 2.5 as named parts: the most a discount may be is never negative | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26781 | CGST 2.5 + SGST 2.5 as named parts: the total is never negative | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26782 | CGST 2.5 + SGST 2.5 as named parts: the tax is never negative | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26783 | CGST 2.5 + SGST 2.5 as named parts: the tax is exactly the taxable amount at the restaurant's rate, rounded once | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26784 | CGST 2.5 + SGST 2.5 as named parts: the ONE formula every panel relies on holds: subtotal − discount + tax = total | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26785 | CGST 2.5 + SGST 2.5 as named parts: at a taxed restaurant the discount comes off BEFORE the tax is worked out | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26786 | CGST 2.5 + SGST 2.5 as named parts: a sealed MRP item never counts for more than the whole bill WHERE THAT FIGURE IS USED (a zero rate) | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26787 | CGST 2.5 + SGST 2.5 as named parts: a composition-scheme restaurant charges exactly zero tax | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26788 | CGST 2.5 + SGST 2.5 as named parts: asking for the maximum discount gives back exactly the maximum discount | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26789 | CGST 9 + SGST 9 as named parts: no figure on the bill is ever NaN or infinite — a NaN total prints as a blank line | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26790 | CGST 9 + SGST 9 as named parts: the subtotal is exactly the taxed part plus the untaxed part | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26791 | CGST 9 + SGST 9 as named parts: the discount actually applied is never above the most that is allowed | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26792 | CGST 9 + SGST 9 as named parts: …and never below zero, however the caller asked | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26793 | CGST 9 + SGST 9 as named parts: the most a discount may be is never negative | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26794 | CGST 9 + SGST 9 as named parts: the total is never negative | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26795 | CGST 9 + SGST 9 as named parts: the tax is never negative | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26796 | CGST 9 + SGST 9 as named parts: the tax is exactly the taxable amount at the restaurant's rate, rounded once | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26797 | CGST 9 + SGST 9 as named parts: the ONE formula every panel relies on holds: subtotal − discount + tax = total | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26798 | CGST 9 + SGST 9 as named parts: at a taxed restaurant the discount comes off BEFORE the tax is worked out | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26799 | CGST 9 + SGST 9 as named parts: a sealed MRP item never counts for more than the whole bill WHERE THAT FIGURE IS USED (a zero rate) | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26800 | CGST 9 + SGST 9 as named parts: a composition-scheme restaurant charges exactly zero tax | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26801 | CGST 9 + SGST 9 as named parts: asking for the maximum discount gives back exactly the maximum discount | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26802 | a composition-scheme restaurant, which charges no GST at all: no figure on the bill is ever NaN or infinite — a NaN total prints as a blank line | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26803 | a composition-scheme restaurant, which charges no GST at all: the subtotal is exactly the taxed part plus the untaxed part | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26804 | a composition-scheme restaurant, which charges no GST at all: the discount actually applied is never above the most that is allowed | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26805 | a composition-scheme restaurant, which charges no GST at all: …and never below zero, however the caller asked | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26806 | a composition-scheme restaurant, which charges no GST at all: the most a discount may be is never negative | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26807 | a composition-scheme restaurant, which charges no GST at all: the total is never negative | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26808 | a composition-scheme restaurant, which charges no GST at all: the tax is never negative | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26809 | a composition-scheme restaurant, which charges no GST at all: the tax is exactly the taxable amount at the restaurant's rate, rounded once | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26810 | a composition-scheme restaurant, which charges no GST at all: the ONE formula every panel relies on holds: subtotal − discount + tax = total | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26811 | a composition-scheme restaurant, which charges no GST at all: at a taxed restaurant the discount comes off BEFORE the tax is worked out | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26812 | a composition-scheme restaurant, which charges no GST at all: a sealed MRP item never counts for more than the whole bill WHERE THAT FIGURE IS USED (a zero rate) | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26813 | a composition-scheme restaurant, which charges no GST at all: a composition-scheme restaurant charges exactly zero tax | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26814 | a composition-scheme restaurant, which charges no GST at all: asking for the maximum discount gives back exactly the maximum discount | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26815 | a restaurant that has configured nothing, so it falls back to 5%: no figure on the bill is ever NaN or infinite — a NaN total prints as a blank line | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26816 | a restaurant that has configured nothing, so it falls back to 5%: the subtotal is exactly the taxed part plus the untaxed part | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26817 | a restaurant that has configured nothing, so it falls back to 5%: the discount actually applied is never above the most that is allowed | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26818 | a restaurant that has configured nothing, so it falls back to 5%: …and never below zero, however the caller asked | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26819 | a restaurant that has configured nothing, so it falls back to 5%: the most a discount may be is never negative | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26820 | a restaurant that has configured nothing, so it falls back to 5%: the total is never negative | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26821 | a restaurant that has configured nothing, so it falls back to 5%: the tax is never negative | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26822 | a restaurant that has configured nothing, so it falls back to 5%: the tax is exactly the taxable amount at the restaurant's rate, rounded once | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26823 | a restaurant that has configured nothing, so it falls back to 5%: the ONE formula every panel relies on holds: subtotal − discount + tax = total | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26824 | a restaurant that has configured nothing, so it falls back to 5%: at a taxed restaurant the discount comes off BEFORE the tax is worked out | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26825 | a restaurant that has configured nothing, so it falls back to 5%: a sealed MRP item never counts for more than the whole bill WHERE THAT FIGURE IS USED (a zero rate) | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26826 | a restaurant that has configured nothing, so it falls back to 5%: a composition-scheme restaurant charges exactly zero tax | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26827 | a restaurant that has configured nothing, so it falls back to 5%: asking for the maximum discount gives back exactly the maximum discount | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26828 | a restaurant that is NOT on the composition scheme can never reach a rate of zero | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26829 | …and a composition restaurant forces EVERY line to exempt, whatever the dish says | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26830 | so a GST-inside MRP line and a zero rate cannot happen together on one bill | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26831 | an MRP dish with the OTHER treatment is exempt, and then its gross IS its net | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| | **D · a GST return reconciles in both directions, on twenty thousand random periods** | | | |
| P26832 | CGST + SGST at 2.5 each: the tax lines always add back to the tax total exactly | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26833 | CGST + SGST at 2.5 each: no tax line is ever NaN | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26834 | CGST + SGST at 2.5 each: there is one figure per tax line, always | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26835 | CGST + SGST at 2.5 each: a positive tax total never produces a negative line | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26836 | CGST + SGST at 2.5 each: every day's figure adds up to the month's headline figure | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26837 | CGST + SGST at 2.5 each: every day's tax lines add up to that day's tax | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26838 | CGST + SGST at 2.5 each: every column adds up to the grand total | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26839 | CGST + SGST at 9 each: the tax lines always add back to the tax total exactly | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26840 | CGST + SGST at 9 each: no tax line is ever NaN | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26841 | CGST + SGST at 9 each: there is one figure per tax line, always | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26842 | CGST + SGST at 9 each: a positive tax total never produces a negative line | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26843 | CGST + SGST at 9 each: every day's figure adds up to the month's headline figure | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26844 | CGST + SGST at 9 each: every day's tax lines add up to that day's tax | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26845 | CGST + SGST at 9 each: every column adds up to the grand total | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26846 | IGST alone at 18: the tax lines always add back to the tax total exactly | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26847 | IGST alone at 18: no tax line is ever NaN | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26848 | IGST alone at 18: there is one figure per tax line, always | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26849 | IGST alone at 18: a positive tax total never produces a negative line | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26850 | IGST alone at 18: every day's figure adds up to the month's headline figure | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26851 | IGST alone at 18: every day's tax lines add up to that day's tax | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26852 | IGST alone at 18: every column adds up to the grand total | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26853 | three lines including a cess: the tax lines always add back to the tax total exactly | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26854 | three lines including a cess: no tax line is ever NaN | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26855 | three lines including a cess: there is one figure per tax line, always | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26856 | three lines including a cess: a positive tax total never produces a negative line | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26857 | three lines including a cess: every day's figure adds up to the month's headline figure | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26858 | three lines including a cess: every day's tax lines add up to that day's tax | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26859 | three lines including a cess: every column adds up to the grand total | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26860 | two lines of UNEQUAL size: the tax lines always add back to the tax total exactly | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26861 | two lines of UNEQUAL size: no tax line is ever NaN | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26862 | two lines of UNEQUAL size: there is one figure per tax line, always | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26863 | two lines of UNEQUAL size: a positive tax total never produces a negative line | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26864 | two lines of UNEQUAL size: every day's figure adds up to the month's headline figure | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26865 | two lines of UNEQUAL size: every day's tax lines add up to that day's tax | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26866 | two lines of UNEQUAL size: every column adds up to the grand total | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26867 | spreading a month's rupees across its days always adds back to the month | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26868 | …in whole rupees, never a fraction of one | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26869 | …with one figure per day, always | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26870 | …and a positive month never produces a negative day | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26871 | …and a month with nothing to weigh by puts the whole figure on the first day rather than losing it | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26872 | no weight means no rows back, not a crash | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26873 | no tax lines means no figures back, not a crash | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26874 | tax lines that are all zero still add back to the total (the divide-by-zero guard) | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26875 | one tax line takes the whole total | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26876 | a zero total splits into zeros, never NaN | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26877 | the taxable value of a period is recovered from the tax, so MRP lines cannot inflate it | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26878 | …and it is capped at what was actually sold, so a rounding wobble cannot exceed it | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26879 | with no rate configured the whole of net sales is returned, to be labelled honestly | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26880 | net sales is subtotal minus discount, to the paise | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26881 | rounding dust is NOT dressed up as real exempt supply — the fault that printed '₹111 exempt' | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26882 | …while a genuine exempt portion IS reported | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26883 | the tolerance scales with the number of bills, because the drift is per bill | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26884 | with no rate at all, nothing is called exempt | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26885 | when nothing is materially exempt, the printed taxable value is the whole of net sales | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26886 | …and when something is, it is recovered from the tax instead | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26887 | none of the filing helpers throws on rubbish | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26888 | …and none of them returns a NaN for rubbish either | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| | **E · 'is this still the value your screen was editing from?' — the comparison, exhaustively** | | | |
| P26889 | a value always equals itself, whatever shape it is | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26890 | the comparison reads the same in either direction | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26891 | it never throws, on any pairing | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26892 | a value that has never been set and an empty object are the same thing, so a blank column cannot invent a clash | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26893 | spaces round a typed word are not a change | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26894 | a list's ORDER is not a change — an allergen list is a set | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26895 | a list's CAPITALS are not a change | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26896 | a list gaining an item IS a change | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26897 | an object's key order is not a change | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26898 | an object's CONTENT changing is a change | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26899 | a nested list inside an object is compared, not stringified to '[object Object]' | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26900 | two DIFFERENT objects no longer compare equal, which is the bug that made every jsonb column unprotectable | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26901 | renaming ONE table inside the whole table-names object is caught | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26902 | …while re-sending the SAME table names is not a clash | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26903 | a number and its string spelling are the same value, because a form sends strings | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26904 | a cycle can never hang a write | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26905 | …and a deep value stops descending rather than recursing forever | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26906 | a very deep value therefore compares EQUAL, which fails OPEN — the same direction as every other decision here | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26907 | a deep LIST stops descending too | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26908 | isPlainObject says yes to an object | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26909 | …no to a list | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26910 | …no to nothing | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26911 | …no to a word or a number | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26912 | stableJson gives the same string however the object was built | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26913 | stableJson never returns the literal words '[object Object]' | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26914 | the comparator has NO imports, so a guard can run the rule that actually ships | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26915 | only an allow-listed table may be compared at all | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26916 | a composite key may only use the columns named for that table | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26917 | every field name is checked against a strict pattern before it reaches a query | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26918 | at most eight fields are compared at a time | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26919 | the two tables whose own id IS the restaurant answer 'nothing to compare' for anyone else's id | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26920 | every other table's comparison is scoped to this restaurant | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26921 | a missing row reads as 'nothing to overwrite', which is the first-person-to-count case | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26922 | the whole check fails OPEN on any problem — a broken gate must never stop a restaurant working | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26923 | a money value's refusal says it MOVED without repeating the figure | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26924 | …because this gate runs before the per-action permission check | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26925 | an object's refusal takes the quiet form too, so nobody reads '[object Object]' | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26926 | a switch reads 'on' / 'off' in the refusal, never 'true' / 'false' | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26927 | the third state of a money row reads as a sentence | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26928 | an empty value reads as 'nothing' | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26929 | the refusal always says what to do next, not only what went wrong | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26930 | …and it says the change was NOT saved, so nobody assumes it half-happened | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26931 | a replayed change is only judged once it is genuinely old | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26932 | …so the live write path is untouched and costs no extra query | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26933 | a closed table with no timestamp is sent to a human rather than written to | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26934 | a table that has a DIFFERENT party now refuses, naming what the restaurant calls that table | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26935 | the table's name is read live from settings, so a rename lands everywhere with nothing to migrate | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26936 | …and it falls back to 'Table N' on any problem | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26937 | that extra read happens only on the path already about to refuse somebody | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26938 | 'couldn't tell which table' is never turned into a refusal | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26939 | the refusal shape is the one the panel's outbox expects | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26940 | every refusal says whether resending could ever help | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| | **F · a retry never charges twice, a refusal never sticks, and a wall belongs to ONE restaurant** | | | |
| P26941 | 'did this actually change anything?' answers the same way on every status and body shape | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26942 | an error status is never remembered, so the person can try again | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26943 | a refusal reported INSIDE a 200 is not remembered either — the diner's next tap must reach the kitchen | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26944 | …including the cruel one: over the ordering limit, wait a minute, tap again | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26945 | a real success IS remembered, so a replay cannot place the order twice | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26946 | a success with no body is remembered too | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26947 | `ok: 0` is not the same as `ok: false` — the test is strict | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26948 | the string 'false' is not `false` either | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26949 | a stored refusal is recognised so an old wrongly-remembered row heals itself | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26950 | …and a stored success is not disturbed | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26951 | nothing stored is not a refusal | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26952 | a stored list is not a refusal | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26953 | neither helper throws on rubbish | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26954 | the rule lives in a file with NO imports, so a guard can run the rule that ships | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26955 | a request with no action id goes straight through, unchanged | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26956 | the claim is taken BEFORE the handler runs | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26957 | a duplicate is answered with the ORIGINAL result, so a client can still track an order whose first reply was lost | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26958 | …and the stored result is read per call, never from a shared variable two requests could cross | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26959 | a request still being handled is told to retry, not answered wrongly | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26960 | a crashed attempt is released after 30 seconds so nothing is wedged for ever | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26961 | a handler that throws releases its claim before the error travels on | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26962 | a failed write releases the claim, so the next replay is a genuine retry | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26963 | the whole thing fails OPEN — a dedupe table hiccup must never break every staff write | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26964 | the response is CLONED before its body is read, so the caller still gets a readable reply | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26965 | a non-JSON reply stores nothing rather than crashing | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26966 | the claims table is kept from growing for ever | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26967 | …opportunistically, so an idle stack does no work at all | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26968 | …and the tidy-up is never awaited, so it can never affect a write | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26969 | a wrongly-remembered refusal from before the rule existed heals itself rather than replaying for ever | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26970 | …by reusing the row, so the healing attempt is still protected against a concurrent duplicate | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26971 | a login wall is read back for ONE restaurant, so two restaurants never share one wall | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26972 | clearing a counter after a good sign-in can be narrowed to one restaurant too | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26973 | …and it only narrows when the caller actually named one, so today's callers are unchanged | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26974 | a wall that was hit is marked handled, never deleted — the record of what happened stays | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26975 | the limiter fails OPEN — a glitch must never lock a real person out | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26976 | an empty subject is allowed rather than counted | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26977 | the two 'who is this really' reads happen only when a wall was actually hit | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26978 | …and they are scoped, column-listed and capped to one row | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26979 | the restaurant-name read is one row, two columns | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26980 | the guest beacon only pings when a real recent wall exists in the database | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26981 | …and the alert's words come from that row, so a caller cannot compose an alert | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26982 | the staff-login wall is the ONE silent alert; every other ping is audible | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26983 | the admin-login warning does not promise a wait that is not real | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26984 | every rate key has a name a person would recognise | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26985 | the alert names WHO, WHERE, how many tries and which device | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26986 | …and it does not repeat the restaurant when the 'who' line already names it | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26987 | a subject is normalised so '  Ravi ' and 'ravi' share one counter | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26988 | every value that reaches the database is length-capped | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26989 | an alert can never break a login — every path is swallowed | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26990 | one typed name can no longer become an unbounded read | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26991 | …nor an unbounded slow-hash loop, since only the rows fetched are verified | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26992 | …and reaching that ceiling is said out loud, because a login that quietly stopped matching is the worst way to find out | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26993 | a recycle-bin account is not a login | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26994 | a disabled person who types the RIGHT password is told the truth | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26995 | …and only a verified password unlocks that message, so guessing names learns nothing | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26996 | a database blip during a login says 'try again', never 'wrong password' | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26997 | an oversize name or password is refused BEFORE the slow hash burns any CPU | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26998 | a lockout on any matching live row is honoured, so a colliding name cannot dodge it | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P26999 | a cookie carries the role and the token version, so both can end a session | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27000 | a cookie past seven days is refused | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27001 | a missing signing key is warned about once per server start, not silently tolerated | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| | **G · the two documents I own still describe the product that exists** | | | |
| P27002 | the rulebook no longer says rule 11 is unbuilt | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27003 | …and it names the migration file that built it | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27004 | …as a MIGRATION FILE, in the owner's own required words | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27005 | that migration file actually exists | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27006 | …and it creates the function the rulebook names | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27007 | the rulebook is honest that lfh_void_invoice was NOT loosened | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27008 | …which matches the migration, whose own body only touches lfh_reopen_table | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27009 | the reopen refuses when every order on the bill was cancelled | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27010 | …and when another party is already on that table | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27011 | …and it is staff-only, per the mig-038 rule that a replaced function is public again by default | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27012 | the act has a home in the Activity log, so it never reads 'System › Other' | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27013 | …and it is a DIFFERENT sentence from voiding a live bill's invoice | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27014 | the section's own preamble no longer says one of the four rules is still to build | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27015 | §3.0 still states the rule everything hangs off: a sale can be cancelled, never made to disappear | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27016 | §3.0 still says nobody at the restaurant has a button that removes a bill | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27017 | the compliance doc still names the purge that ships | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27018 | the purge that ships names no money table | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27019 | …and it keeps the restaurants row, marked, so the kept bills still have a parent | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27020 | the newest purge migration clears the pending printer handshakes | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27021 | …and it exists as a file, not only in a commit message | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27022 | the efficiency playbook's 'cap the userAuth candidate loop' really is capped now | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27023 | the playbook still names egress, not effort, as this product's cost | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27024 | no money path spells a tax rate out instead of asking lib/tax.ts | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27025 | the ONE fallback rate lives in exactly one place in the TypeScript | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27026 | …and the columns needed to read a rate are a named constant, never a hand-written subset | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27027 | lib/paySplit.ts reads the rate through that constant | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27028 | …and takes each order's own charged rate from the same definition the printed bill uses | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27029 | the split's due is recomputed on the server, never taken from the browser | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27030 | the parts are rounded ONCE, and that same rounding is what gets stored | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27031 | a settle that cannot be stamped reverses its own money rows rather than claiming money nobody took | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| P27032 | …and still answers 500, so the person knows to try again | node scripts/verify-t24b-money-safety.mjs --ids — runs the REAL exported function or reads the file it names | ✅ | |
| | **the shape refusals, which happen before any read** | | | |
| P27033 | a 'split' with one part is refused | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27034 | …and it says why, in words a waiter can act on | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27035 | …and thirteen parts too | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27036 | …and it says why, in words a waiter can act on | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27037 | a part of zero is refused | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27038 | …and it says why, in words a waiter can act on | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27039 | a negative part is refused | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27040 | …and it says why, in words a waiter can act on | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27041 | a part with no number at all is refused | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27042 | …and it says why, in words a waiter can act on | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27043 | an unknown way to pay is refused | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27044 | …and it says why, in words a waiter can act on | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27045 | an over-long note on a part is refused | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27046 | …and it says why, in words a waiter can act on | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27047 | a tab owed by nobody is refused | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27048 | …and it says why, in words a waiter can act on | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27049 | two tabs on one bill are refused | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27050 | …and it says why, in words a waiter can act on | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27051 | a tab written in lower case is not a tab, it is an unknown way to pay | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27052 | …and it says why, in words a waiter can act on | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| | **a table with nothing settleable** | | | |
| P27053 | a table with nothing to settle is refused plainly, never with a crash | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27054 | …and it says what to do instead | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27055 | …and the refusal is a sentence, not a code | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27056 | a table number that does not exist is refused, never a 500 | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27057 | a table that is not a number is refused with a sentence | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| | **no refusal ever leaks code text at a person** | | | |
| P27058 | no refusal contains [object Object] | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27059 | no refusal contains undefined | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27060 | no refusal contains NaN | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27061 | no refusal contains ${ | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| | **the rendered manager floor** | | | |
| P27062 | the manager floor is not a blank screen | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27063 | the manager floor shows no leaked code text: [object Object] | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27064 | the manager floor shows no leaked code text: NaN | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27065 | the manager floor shows no leaked code text: ${ | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27066 | the manager floor shows no leaked code text: --> | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27067 | a rupee figure on the floor is never shown with more than two decimals | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| | **the same screen at phone size** | | | |
| P27068 | the manager floor is not blank at 360px | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27069 | nothing on the manager floor runs off the side of a phone | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27070 | the floor names THIS restaurant, not restaurant #1's branding | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27071 | the floor says how many tables are occupied out of how many | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27072 | a money figure on the floor is never shown as a bare 'undefined' | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27073 | the floor is readable at 360px too — it names the restaurant there as well | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27074 | the owner's Tax/GST screen is not blank | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27075 | …and it is the tax screen, not another tab | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27076 | the tax screen shows no leaked code text: [object Object] | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27077 | the tax screen shows no leaked code text: NaN | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27078 | the tax screen shows no leaked code text: ${ | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27079 | the tax screen shows no leaked code text: --> | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27080 | the tax screen shows no leaked code text: Infinity | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27081 | no money figure on the tax screen shows more than two decimals | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27082 | the tax screen states what was collected | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27083 | …and the taxable sales it was charged on | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27084 | …and how many bills that was over | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27085 | the split panel names each tax line the printed bill shows | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27086 | …with a rate against each one | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27087 | THE ONE THAT MATTERS: the tax lines add back to the total tax, to the rupee | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27088 | …and that total is the same figure as the tile above it | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27089 | …and the tax lines' rates add up to the restaurant's rate | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27090 | no tax line is negative | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27091 | the effective rate on screen is the collected tax over the taxable sales, not a stored number | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27092 | …and it lands within a whisker of the rate the restaurant actually set | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27093 | tax per bill is the collected tax divided by the bills, rounded | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27094 | the chart on this screen is not a lonely single bar | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27095 | …and its time axis is labelled with real dates, more than one | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27096 | …and it is not an empty box with no honest message either | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27097 | the tax screen still renders in the light skin | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| P27098 | …and shows the same figures, not a blank or an error | node scripts/verify-t24b-live.mjs --base <url> — headless, on the running app; READ-ONLY (every write it tries is refused) | ✅ | |
| | **my own judgment — reasoning, not a script** | | | |
| P27099 | the MRP figure accumulates the GROSS line amount while a GST-inside line contributes only its NET, so on an all-MRP bill of GST-inside lines it can exceed the subtotal — is that a fault a restaurant could feel? | reason about whether a real restaurant needs it to work this way, and say so | ✅ NOT a finding | Found by the random bills, 2026-08-28. It is only used in arithmetic at a ZERO rate, where a GST-inside line divides by 1 and gross and net are the same number. Two independent locks stop the other case existing at all: a GST-inside MRP line needs `mrp_tax_treatment = 'inclusive'`, and a restaurant that is not on the composition scheme can never reach a rate of zero (`NULLIF(tax_rate, 0)` falls back to 5%), while a composition restaurant forces every line to exempt. TypeScript and the SQL agree exactly, so 'fixing' one alone would break parity. **Both locks are now asserted** (P26825–P26828), so if either ever moves the guard says so instead of the figure quietly going wrong. Do not re-file this. |
| P27100 | the helper's own front door — `POST /api/print-agent/pair/start` — is unauthenticated and writes a row, with no ceiling of any kind. Its two public neighbours both got one. Should it? | reason about whether a real restaurant needs it to work this way, and say so | ⚠ reported, not fixed | `/api/guest/limit-hit` and `/api/log/client-error` are public in exactly the same way and both carry `withinMemoryCap` (60s / 6 per caller) for exactly this reason; this one carries nothing, so each call is an unbounded INSERT into `print_pairings`. Nothing is granted by it — the restaurant is chosen by the human who approves, never by the helper — and rows are pruned on each new start, so the TABLE stays small; it is the call RATE that has no bound. The fix is one line at the call site, but the file belongs to the printing territory, so it is carried to the owner as a decision rather than reached into during a 40-terminal sweep. · s8·T11 2026-09-06: re-run ✅ — `node --experimental-strip-types scripts/sweep/t11/run.mjs --only P27100` (this row's subject is a file in T11's sweep-#8 territory) |


## Result — sweep #7, 2026-08-28

**1,000 rows in this file now.** The first 500 re-run in place; 500 new ones below them.

| block | rows | executed by | result |
|---|---|---|---|
| sweep #6's 500, re-run | 500 | `verify-t24-money-rules.mjs --db` (249) · the named `verify:*` guards · headless on 4224 · four screenshots read | **361 ✅ · 137 ⏭ · 1 ⚠ partial · 1 regression (P11997), fixed** |
| A · a part of a split may be a tab | 95 | `verify:t24b` | 95 ✅ |
| B · the offline-layer beat | 41 | `verify:t24b` | 41 ✅ |
| C · 30,000 random bills | 95 | `verify:t24b` | 95 ✅ |
| D · 20,000 random filing periods | 57 | `verify:t24b` | 57 ✅ |
| E · the clash comparator, exhaustively | 52 | `verify:t24b` | 52 ✅ |
| F · retries, refusals and login walls | 61 | `verify:t24b` | 61 ✅ |
| G · the two docs I own, against today's code | 31 | `verify:t24b` | 31 ✅ |
| live · the refusals and the Tax/GST sheet | 66 | `verify:t24b-live --base …` | 66 ✅ |
| judgment | 2 | reasoning | 1 ✅ not-a-finding · 1 ⚠ reported |

**Why 137 rows are `⏭`, honestly.** 122 of them need a table carrying a **settleable bill**, and no French
House table had one during this run — nine open sessions, zero unpaid accepted orders. This run deliberately
wrote no order row, because an order cannot be *deleted* once written (migration 190's immutability trigger),
only cancelled, and a cancelled ₹0 bill would sit in the owner's real Z-report for ever. The REFUSAL half of
the same code was re-run live instead (P27033–P27052) and the rules those rows assert are all asserted from
the shipped source in `verify:t24b`. Five more are the login-wall rows, deliberately not re-run: walling a real
account pings the owner's phone, and the sweep rules forbid it. **To close them, seed one bill on French House
first** — that is a decision for the owner, and it is item 9 in this terminal's report.

**Proof the money is right, not just the tests.** On the running app the owner's Tax/GST sheet reconciled in
both directions on 864 real bills: CGST ₹27,875 + SGST ₹27,875 = the ₹55,750 in the tile above it; the two
lines' rates add to the restaurant's 5%; the effective rate printed on screen (5.01%) is ₹55,750 ÷ ₹11,13,636
computed, not stored; tax per bill (₹65) is that divided by 864; and net ₹11,13,636 + GST ₹55,750 is exactly
the ₹11,69,386 'total collected' tile. Across 30,000 randomly generated bills no money identity broke once.

**What this run left behind:** nothing. No row was written to any restaurant — every write it attempted was
one the server refuses. Aangan was never touched. The four screenshots were read and deleted. The 4224 dev
server was stopped. One migration was applied to the DEV database only:
`369_a_purge_clears_the_pending_printer_handshakes.sql`.

