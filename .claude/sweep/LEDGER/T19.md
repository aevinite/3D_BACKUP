# Sweep #6 · Terminal 19 — THE ADMIN SERVER ROUTES, PART A

**Territory:** `app/api/admin/**` — the first 25 `route.ts` files in alphabetical order
(`find app/api/admin -name route.ts | sort | head -25`).
**Phase block:** P09001–P09500 (permanent — never renumber).
**Branch:** `sweep6/t19-admin-api-a` · **worktree:** `sweep6/T19` · **port:** 4119.

## How the 500 are budgeted

| block | ids | phases | share | what it is |
|-------|-----|--------|-------|------------|
| A | P09001–P09025 | 25 | 5% | the sign-in gate, one row per file (conformance) |
| B | P09026–P09100 | 75 | 15% | conformance to the written project rules (columns · bounds · plain words) |
| C | P09101–P09300 | 200 | 40% | reading the code for correctness — 8 rows per file |
| D | P09301–P09375 | 75 | 15% | watching it run (headless on port 4119, admin cookie, zero sign-ins) |
| E | P09376–P09425 | 50 | 10% | screenshots actually opened and read (1280×800 **and** A35 360×780 dpr3) |
| F | P09426–P09475 | 50 | 10% | tracing a change across panels |
| G | P09476–P09500 | 25 | 5% | my own judgment about whether a real restaurant needs it this way |

Conformance total = A + B = 100 rows (20%). Every one of the 25 files owned here has **at least
ten** rows across A–C; the routes the scope names by hand have more.

**How to re-run this next sweep:** `node scripts/verify-admin-api-a.mjs` covers blocks A and B
outright. Blocks C–G are re-run by following the "how to verify" column; every live row uses
`adminHeaders()` from `scripts/sweep/login.mjs`, which makes **zero** sign-in requests.

---

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P09001 | Every exported handler in /api/admin/act-as/go requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09002 | Every exported handler in /api/admin/act-as requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09003 | Every exported handler in /api/admin/agent-runs requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09004 | Every exported handler in /api/admin/analytics requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09005 | Every exported handler in /api/admin/attention requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09006 | Every exported handler in /api/admin/audit requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09007 | Every exported handler in /api/admin/bill-audit requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09008 | Every exported handler in /api/admin/billing requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09009 | Every exported handler in /api/admin/bills requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09010 | Every exported handler in /api/admin/cancelled-today requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09011 | Every exported handler in /api/admin/custlog requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09012 | Every exported handler in /api/admin/customers requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09013 | Every exported handler in /api/admin/dashboard requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09014 | Every exported handler in /api/admin/error-memory requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09015 | Every exported handler in /api/admin/fix-request requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09016 | Every exported handler in /api/admin/floor requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09017 | Every exported handler in /api/admin/health requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09018 | Every exported handler in /api/admin/maintenance requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09019 | Every exported handler in /api/admin/notifications requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09020 | Every exported handler in /api/admin/oplog/ack requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09021 | Every exported handler in /api/admin/oplog/cleanup requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09022 | Every exported handler in /api/admin/oplog requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09023 | Every exported handler in /api/admin/owners requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09024 | Every exported handler in /api/admin/panels-health requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09025 | Every exported handler in /api/admin/rate-limits requires the admin sign-in cookie BEFORE its first sb.from()/sb.rpc() call | node scripts/verify-admin-api-a.mjs (rule 1) — parses each exported GET/POST/PATCH/DELETE body and asserts the gate call precedes any database call | ✅ | verify:admin-api-a rule 1 — gate call precedes the first database call |
| P09026 | /api/admin/act-as/go names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09027 | /api/admin/act-as names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09028 | /api/admin/agent-runs names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09029 | /api/admin/analytics names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09030 | /api/admin/attention names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09031 | /api/admin/audit names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09032 | /api/admin/bill-audit names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09033 | /api/admin/billing names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09034 | /api/admin/bills names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09035 | /api/admin/cancelled-today names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09036 | /api/admin/custlog names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09037 | /api/admin/customers names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09038 | /api/admin/dashboard names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09039 | /api/admin/error-memory names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09040 | /api/admin/fix-request names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09041 | /api/admin/floor names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09042 | /api/admin/health names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09043 | /api/admin/maintenance names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09044 | /api/admin/notifications names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09045 | /api/admin/oplog/ack names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09046 | /api/admin/oplog/cleanup names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09047 | /api/admin/oplog names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09048 | /api/admin/owners names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09049 | /api/admin/panels-health names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09050 | /api/admin/rate-limits names its columns on every table read — no select("*") on a money/hot path | node scripts/verify-admin-api-a.mjs (rule 2) — flags select("*") outside the two declared exceptions | ✅ | verify:admin-api-a rule 2 |
| P09051 | /api/admin/act-as/go bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09052 | /api/admin/act-as bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09053 | /api/admin/agent-runs bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09054 | /api/admin/analytics bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09055 | /api/admin/attention bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09056 | /api/admin/audit bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09057 | /api/admin/bill-audit bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09058 | /api/admin/billing bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09059 | /api/admin/bills bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09060 | /api/admin/cancelled-today bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09061 | /api/admin/custlog bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09062 | /api/admin/customers bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09063 | /api/admin/dashboard bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09064 | /api/admin/error-memory bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09065 | /api/admin/fix-request bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09066 | /api/admin/floor bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09067 | /api/admin/health bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09068 | /api/admin/maintenance bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09069 | /api/admin/notifications bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09070 | /api/admin/oplog/ack bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09071 | /api/admin/oplog/cleanup bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09072 | /api/admin/oplog bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09073 | /api/admin/owners bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09074 | /api/admin/panels-health bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09075 | /api/admin/rate-limits bounds every list read — .limit(), .range(), .maybeSingle(), .single() or head-count only | node scripts/verify-admin-api-a.mjs (rule 3) — every .select() that is not a head count carries an explicit ceiling | ✅ | verify:admin-api-a rule 3 — FIXED this pass (item 11): 24 reads had no ceiling |
| P09076 | /api/admin/act-as/go never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09077 | /api/admin/act-as never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09078 | /api/admin/agent-runs never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09079 | /api/admin/analytics never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09080 | /api/admin/attention never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09081 | /api/admin/audit never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09082 | /api/admin/bill-audit never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09083 | /api/admin/billing never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09084 | /api/admin/bills never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09085 | /api/admin/cancelled-today never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09086 | /api/admin/custlog never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09087 | /api/admin/customers never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09088 | /api/admin/dashboard never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09089 | /api/admin/error-memory never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09090 | /api/admin/fix-request never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09091 | /api/admin/floor never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09092 | /api/admin/health never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09093 | /api/admin/maintenance never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09094 | /api/admin/notifications never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09095 | /api/admin/oplog/ack never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09096 | /api/admin/oplog/cleanup never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09097 | /api/admin/oplog never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09098 | /api/admin/owners never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09099 | /api/admin/panels-health never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09100 | /api/admin/rate-limits never returns a raw Postgres sentence to the console — failures go through lib/adminFail | node scripts/verify-admin-api-a.mjs (rule 4) — grep for `error: <x>.error.message` / `bad(<x>.error.message` in a response body | ✅ | verify:admin-api-a rules 4a+4b |
| P09101 | /api/admin/act-as/go: rid is shape-checked before it reaches a uuid column, so a stale bookmark gets 400 not a database sentence | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09102 | /api/admin/act-as/go: `to` is matched against the five-path allow-list, so the redirect can only ever land on a panel of this app | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09103 | /api/admin/act-as/go: a restaurant in the recycle bin is refused with 409 and the words 'restore it first' | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09104 | /api/admin/act-as/go: the restaurant lookup is scoped by id with a column list and .limit(1) | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09105 | /api/admin/act-as/go: walking into a panel writes one admin_enter_panel row on the `admin` panel (never the restaurant's own log) | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09106 | /api/admin/act-as/go: the act-as cookie is set on the SAME response that redirects (a second NextResponse would drop Set-Cookie) | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09107 | /api/admin/act-as/go: the cookie is httpOnly + sameSite lax + secure in production, matching the POST twin and both login doors | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09108 | /api/admin/act-as/go: ?uid= rides along as ?as= only — no &view=real is ever appended (owner's rule: the admin keeps seeing everything) | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09109 | /api/admin/act-as: { clear: true } returns the response that carries the cleared cookie, not a fresh one | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09110 | /api/admin/act-as: restaurant_id is shape-checked before the uuid column, matching the /go twin exactly | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09111 | /api/admin/act-as: a binned restaurant is refused 409 here too, so both doors that set this cookie agree | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09112 | /api/admin/act-as: the restaurant read names its columns and is capped at one row | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09113 | /api/admin/act-as: entering writes admin_enter_panel with the restaurant's name in the detail | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09114 | /api/admin/act-as: the cookie is set on the returned response (the 'I can only access Little French House' bug) | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09115 | /api/admin/act-as: cookie flags match /go: httpOnly, lax, 6h, secure in production | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09116 | /api/admin/act-as: a malformed body (non-JSON) is tolerated and falls through to the 400, never a 500 | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09117 | /api/admin/agent-runs: the history read names its eight columns and is capped at 30 rows | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09118 | /api/admin/agent-runs: rows come back newest-first by started_at, which is what the Repair → History list claims | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09119 | /api/admin/agent-runs: a failed read answers the plain 'Couldn't load the working-session history' sentence, not Postgres prose | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09120 | /api/admin/agent-runs: the failure keeps the database's own words in `detail` and in the server log, for whoever needs them | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09121 | /api/admin/agent-runs: the route is read-only — it exports no POST/PATCH/DELETE | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09122 | /api/admin/agent-runs: no restaurant scope is applied, and that is right: agent runs are platform-level | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09123 | /api/admin/agent-runs: the response shape is { runs: [...] } and is never null | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09124 | /api/admin/agent-runs: dynamic = 'force-dynamic' so the console never renders a cached session list | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09125 | /api/admin/analytics: ?range= is normalised to one of today/7d/30d before it reaches zeroFill or the response | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09126 | /api/admin/analytics: ?day= is accepted only as a strict YYYY-MM-DD, so a junk value cannot widen the window | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09127 | /api/admin/analytics: 'today' starts at the 05:00-IST business-day rollover — the same boundary the Dashboard uses | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09128 | /api/admin/analytics: the three grouped breakdowns are Postgres RPCs, never a raw orders fetch to the client | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09129 | /api/admin/analytics: no food money appears anywhere in the payload — every figure is a count | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09130 | /api/admin/analytics: the whole payload rides cachedOwnerPayload, keyed per range/drill-day, with the orders fingerprint | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09131 | /api/admin/analytics: compute() THROWS on any failed read, so a half-built payload is never stored under the cache key | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09132 | /api/admin/analytics: binned restaurants are excluded from the restaurant, staff, table and session counts | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09133 | /api/admin/attention: all three reads are checked, so a timed-out usage RPC cannot flag every paying restaurant as churn-risk | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09134 | /api/admin/attention: a suspended restaurant is skipped — that is a separate deliberate state, not an alert | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09135 | /api/admin/attention: a day-1 paying customer with no orders lands in onboarding, never in at-risk | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09136 | /api/admin/attention: an established paying restaurant idle 30 days is at-risk with the words 'no orders in 30 days' | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09137 | /api/admin/attention: 'was active, but no orders this week' requires o30 > 0, so it cannot double-report the idle case | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09138 | /api/admin/attention: onboarding is sorted youngest-first so the newest restaurant is at the top | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09139 | /api/admin/attention: no food money is returned — only plan names, reasons and an age in days | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09140 | /api/admin/attention: the restaurant and billing reads name their columns and exclude binned restaurants | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09141 | /api/admin/audit: ?limit is clamped to 1..300 so a crafted value cannot ask for the whole table | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09142 | /api/admin/audit: ?restaurant_id is uuid-checked before it reaches the column | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09143 | /api/admin/audit: ?q= is cleaned through lib/searchText before entering the PostgREST or() filter | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09144 | /api/admin/audit: ?detail= accepts digits only and 404s for an id that is not there | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09145 | /api/admin/audit: the detail branch answers canRestore only for an order_deleted row whose order is still tombstoned | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09146 | /api/admin/audit: restaurant names are resolved in ONE batched .in() lookup — no per-row query | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09147 | /api/admin/audit: amounts ARE shown here (the admin's oversight view), matching the bill ledger | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09148 | /api/admin/audit: the list read names its columns, orders by at DESC and carries the clamped limit | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09149 | /api/admin/bill-audit: the action list covers every bill-affecting code and deliberately excludes bill_paid | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09150 | /api/admin/bill-audit: ?type=risk narrows to the removals/voids/edits set only | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09151 | /api/admin/bill-audit: ?restaurant_id is uuid-checked, and an invalid one leaves the query platform-wide rather than erroring | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09152 | /api/admin/bill-audit: money is stripped from every detail through redactMoney — the admin sees no rupees | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09153 | /api/admin/bill-audit: the restaurant-name read is CHECKED, so a failure cannot silently label every row '—' | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | was ❌ — fixed, item 3 (the bill trail's name read was unchecked) |
| P09154 | /api/admin/bill-audit: the filter dropdown's restaurant list cannot come back empty while restaurants exist, without saying so | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | was ❌ — fixed, item 3 (the filter dropdown emptied silently) |
| P09155 | /api/admin/bill-audit: riskCount counts exactly the rows flagged risk in the same response | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09156 | /api/admin/bill-audit: the staff_actions read names its columns, orders newest-first and is capped at 500 | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09157 | /api/admin/billing: ?restaurant_id is uuid-checked before the single-restaurant branch reads anything | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09158 | /api/admin/billing: the year total is built on the IST calendar year, so it cannot sum last year for 5.5h after New Year | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09159 | /api/admin/billing: only INR rows are summed into the ₹ headline — a foreign-currency plan is never added 1:1 | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09160 | /api/admin/billing: a payment saves together with its next-due move, or says out loud that the date did NOT move; and a payment date that isn't a date is refused | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | was ❌ — fixed, item 9 (next-due could silently not move; a junk date reached the column) |
| P09161 | /api/admin/billing: an amount typed as '12,000' or '₹12,000' saves as 12000, never as blank | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09162 | /api/admin/billing: a negative plan amount is refused, so Revenue's MRR cannot be quietly reduced | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09163 | /api/admin/billing: add_payment is idempotent — a retry with the same action id records ONE payment | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09164 | /api/admin/billing: delete_payment shape-checks the id and refuses to report 'removed a payment' when no row matched | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | was ❌ — fixed, item 8 (delete_payment reported a removal that didn't happen) |
| P09165 | /api/admin/bills: the 'deleted' bucket is asked for in the DATABASE (deleted_at is not null), not sieved out of a page | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09166 | /api/admin/bills: the deleted CHIP shows the true database count, and says so rather than a reassuring 0 when unreadable | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | was ❌ — fixed, item 2 (the Deleted chip could read 0 while deleted bills existed) |
| P09167 | /api/admin/bills: the 'Load more' cursor is created_at — the very column the query orders and filters on | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09168 | /api/admin/bills: the cursor is taken from the last SESSION of the unfiltered page, not the last surviving bill row | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09169 | /api/admin/bills: ?limit is clamped 20..500 and ?from/?to/?before are only used when they parse as a date | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09170 | /api/admin/bills: orders for the page are fetched in ONE .in(session_id) read, capped, and grouped in JS | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09171 | /api/admin/bills: an admin delete REQUIRES a reason and writes one deletion_audit row per order, at the net amount | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09172 | /api/admin/bills: a restore un-tombstones the session, writes order_restored audit rows, and says out loud if the database refused | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09173 | /api/admin/cancelled-today: the window is the 05:00-IST business day, matching the floor's 'cancelled today' tile | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09174 | /api/admin/cancelled-today: the orders read names its columns, filters status=cancelled and is capped at 500 | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09175 | /api/admin/cancelled-today: no money is returned — restaurant, table, KOT number and time only | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09176 | /api/admin/cancelled-today: the restaurant-name read is CHECKED, so a failure can never render as 'nothing was cancelled today' | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | was ❌ — fixed, item 1 (a failed name read emptied the whole list) |
| P09177 | /api/admin/cancelled-today: orders belonging to a binned restaurant are dropped, matching the non-binned floor count | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09178 | /api/admin/cancelled-today: a genuinely empty day returns an empty list with a 200 and a generatedAt stamp | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09179 | /api/admin/cancelled-today: the route is lazy — nothing here runs unless the Today tab's section is opened | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09180 | /api/admin/cancelled-today: a failed orders read answers the plain sentence, not Postgres prose | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09181 | /api/admin/custlog: ?restaurant_id is uuid-checked and scopes BOTH the member and blocklist reads | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09182 | /api/admin/custlog: members and blocklist reads are checked, so a broken query never shows an empty 'no customers' | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09183 | /api/admin/custlog: order and call counts are fetched for the shown members only, by their ids — not the newest 400 platform-wide | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09184 | /api/admin/custlog: the order/call reads are bounded, so one very busy member cannot pull an unbounded set | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | was ❌ — fixed, item 5 (the order/call reads had no ceiling) |
| P09185 | /api/admin/custlog: no per-order money crosses the wire — member_id and created_at only | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09186 | /api/admin/custlog: restaurant names are stamped from ONE batched lookup keyed by id | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09187 | /api/admin/custlog: the `customers` table is deliberately NOT fetched here (the Logs page never rendered it) | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09188 | /api/admin/custlog: a failure answers the plain sentence rather than the database's own words | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | was ❌ — fixed, item 10 (raw database prose reached the console) |
| P09189 | /api/admin/customers: ?restaurant_id is uuid-checked; ?phone is reduced to digits and capped at 15 | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09190 | /api/admin/customers: ?q= goes through the shared cleaner, including the backslash PostgREST reads as an escape | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09191 | /api/admin/customers: ?page is clamped 0..200 and the list uses .range() for a bounded page of 50 | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09192 | /api/admin/customers: the tiles and the per-restaurant spread ride the snapshot cache, keyed per restaurant scope | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09193 | /api/admin/customers: the spread comes from ONE grouped RPC, never 'fetch every customer row and count in JS' | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09194 | /api/admin/customers: no spend or bill total appears — counts, dates, consent and blocked only | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09195 | /api/admin/customers: the restaurant-name read is CHECKED, so every row chip cannot silently read '—' | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | was ❌ — fixed, item 12 (every guest chip could read '—') |
| P09196 | /api/admin/customers: a restaurants.name stored as JSONB translations still renders a label, falling back to the slug | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09197 | /api/admin/dashboard: one round trip returns everything the home screen needs — eleven reads in a single Promise.all | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09198 | /api/admin/dashboard: only the CURRENTLY-online staff are returned (last 3 min, capped 200) with an exact count beside it | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09199 | /api/admin/dashboard: the critical reads (restaurants, orders today, maintenance) are checked, so no confident all-clear on a blip | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09200 | /api/admin/dashboard: problemCount groups rows with errorGroupKey — the SAME function the Repair board uses | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09201 | /api/admin/dashboard: problemCount is not age-filtered, so an unresolved error from last week still counts | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09202 | /api/admin/dashboard: the owner⇄restaurant join read is bounded, so an owner's restaurants cannot be silently dropped | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | was ❌ — fixed, item 11 (the owner⇄restaurant join read had no ceiling) |
| P09203 | /api/admin/dashboard: lfh_owner_overview's revenue columns are not read here — no food money on the admin home | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09204 | /api/admin/dashboard: activity details are passed through redactMoney before they reach the screen | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09205 | /api/admin/error-memory: ?restaurant_id is uuid-checked before it enters the or() filter | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09206 | /api/admin/error-memory: a NULL-restaurant signature is included in a scoped view (it covers every restaurant) | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09207 | /api/admin/error-memory: the list names its columns, orders by fixed_at DESC and is capped at 100 | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09208 | /api/admin/error-memory: restaurant names come from ONE batched .in() lookup | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09209 | /api/admin/error-memory: a signature with no restaurant renders as 'All restaurants', never as '—' | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09210 | /api/admin/error-memory: DELETE uuid-checks the id and 404s when the entry is already gone | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09211 | /api/admin/error-memory: DELETE writes an error_memory_cleared row naming what was forgotten | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09212 | /api/admin/error-memory: the route exposes no way to SILENCE a live error — only to forget a fix record | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09213 | /api/admin/fix-request: ?status is restricted to open/fixed/dismissed before the query runs | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09214 | /api/admin/fix-request: action_id and restaurant_id are uuid-checked; a bad one is dropped rather than passed on | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09215 | /api/admin/fix-request: an error already recorded as fixed BEFORE its fix is refused 409 with the date and the PR link | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09216 | /api/admin/fix-request: the same error happening AFTER its fix goes through, labelled 'CAME BACK', with the failed attempt attached | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09217 | /api/admin/fix-request: err_key is built by errorGroupKey — the same function the Repair page groups tiles with | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09218 | /api/admin/fix-request: the stored summary is capped so a 50KB HTML error page never lands in the queue | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09219 | /api/admin/fix-request: POST is idempotent, so a double-tap files ONE request | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09220 | /api/admin/fix-request: a failed INSERT tells the admin nothing was saved — not that something could not be loaded | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | was ❌ — fixed, item 6 (a failed INSERT reported as 'couldn't load') |
| P09221 | /api/admin/floor: ?all=1 is ONE round trip (lfh_admin_floor_all) — never one lfh_floor_state call per restaurant | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09222 | /api/admin/floor: per-restaurant counts come pre-summed from lfh_admin_floor_stats, one tiny row each | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09223 | /api/admin/floor: no revenue appears in either branch — counts and tile states only | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09224 | /api/admin/floor: a failed stats or tiles RPC is reported as statsError/tilesError instead of rendering zeros | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09225 | /api/admin/floor: a failed restaurants read answers the plain sentence rather than an empty platform | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09226 | /api/admin/floor: binned restaurants are excluded from the platform view | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09227 | /api/admin/floor: the single-restaurant branch goes through lfh_floor_state, the one brain every panel reads | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09228 | /api/admin/floor: dynamic = 'force-dynamic' — the floor is live and never cached | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09229 | /api/admin/health: the latency probe is a single one-row read, and a failed probe answers dbOk:false with a 200 | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09230 | /api/admin/health: big-table sizes come from planner estimates (lfh_admin_table_estimates), never COUNT(*) | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09231 | /api/admin/health: `restaurants` uses its own exact count instead of the reltuples estimate, which sits at 0 for small tables | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09232 | /api/admin/health: the staff read is capped at 5000 on a page that auto-refreshes every 60s | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09233 | /api/admin/health: binned restaurants are excluded, so suspended means live-but-inactive | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09234 | /api/admin/health: each block reports its own error field, so the page can say 'unreadable' instead of a green zero | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09235 | /api/admin/health: the broken-3D read is a bounded indexed read, never a browser report or an owner phone alert | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09236 | /api/admin/health: no secret is returned — only the Supabase host name, parsed from the public URL | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09237 | /api/admin/maintenance: ?restaurant_id is uuid-checked on both GET and POST | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09238 | /api/admin/maintenance: omitting the restaurant reads/writes the legacy flagship id='site' row, for the Settings page | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09239 | /api/admin/maintenance: POST updates by restaurant_id, so another tenant's menu can actually be taken offline | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09240 | /api/admin/maintenance: a write that matched no row answers 404 'Restaurant not found', never a silent ok | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09241 | /api/admin/maintenance: the response echoes the value the database now holds, not the value that was asked for | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09242 | /api/admin/maintenance: flipping the guest menu offline writes a maintenance_on/off row with the device id | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09243 | /api/admin/maintenance: the record matches the manager-side route, so the same act is traceable from either panel | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09244 | /api/admin/maintenance: a non-JSON body is tolerated and falls through to the validation, never a 500 | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09245 | /api/admin/notifications: tickets, count and restaurants reads are all checked — a broken query never shows an empty all-clear bell | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09246 | /api/admin/notifications: the ticket list is capped at 30 while the badge shows the TRUE open total from a head count | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09247 | /api/admin/notifications: error rows are filtered unseen AND unresolved, keeping the bell in step with the dashboard count | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09248 | /api/admin/notifications: the error window is 24h and the badge count uses the same window as the list | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09249 | /api/admin/notifications: photo and voice-note links are signed and short-lived, never the permanent public URL | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09250 | /api/admin/notifications: 'dormant' is NOT in the bell — only suspended restaurants raise an alert | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09251 | /api/admin/notifications: rate-limit hits are capped at 10 with their own true count for the badge | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09252 | /api/admin/notifications: no money appears anywhere in the feed | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09253 | /api/admin/oplog/ack: this endpoint only ever touches seen_at — resolved_at is a different state and a different route | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09254 | /api/admin/oplog/ack: `seen` must be a boolean or the request is refused | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09255 | /api/admin/oplog/ack: all-mode only marks seen, never unseen, and is bounded to the 24h badge window | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09256 | /api/admin/oplog/ack: the id list is uuid-filtered, de-duplicated and capped at 200 | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09257 | /api/admin/oplog/ack: an empty valid-id list is refused rather than updating nothing and reporting ok | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09258 | /api/admin/oplog/ack: the update returns how many rows actually changed | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09259 | /api/admin/oplog/ack: POST is wrapped for idempotency per the offline-sync contract | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09260 | /api/admin/oplog/ack: a failed UPDATE tells the admin the save failed — not that something could not be loaded | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | was ❌ — fixed, item 6 (a failed UPDATE reported as 'couldn't load') |
| P09261 | /api/admin/oplog/cleanup: GET is a head count only — the number crosses the wire, never the rows | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09262 | /api/admin/oplog/cleanup: the 'logs are getting full' threshold lives on the server, so the banner cannot drift | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09263 | /api/admin/oplog/cleanup: ?restaurant_id is uuid-checked on both GET and POST | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09264 | /api/admin/oplog/cleanup: keepDays must be a whole number 1..3650 — 0 and negatives are refused hard | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09265 | /api/admin/oplog/cleanup: there is ALWAYS a created_at cutoff, so this can never become an unfiltered delete | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09266 | /api/admin/oplog/cleanup: only staff_actions is touched — never orders and never customers | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09267 | /api/admin/oplog/cleanup: the removed count is the database's exact count, not a guess | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09268 | /api/admin/oplog/cleanup: the cleanup writes its own audit row saying how many entries went and how old they were | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09269 | /api/admin/oplog: ?limit is clamped 1..200 | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09270 | /api/admin/oplog: ?restaurant_id is uuid-checked before it reaches the column | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09271 | /api/admin/oplog: ?level is restricted to error/warn/info | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09272 | /api/admin/oplog: ?action is matched by equality and character-restricted, so it rides the index instead of an ILIKE | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09273 | /api/admin/oplog: ?since is ignored unless it parses, so a bad value cannot widen the query to the whole table | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09274 | /api/admin/oplog: ?q= goes through the shared cleaner, so a typed * cannot match every row | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09275 | /api/admin/oplog: restaurant names come from ONE batched lookup, no per-row query | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09276 | /api/admin/oplog: every detail is passed through redactMoney before it leaves the server | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09277 | /api/admin/owners: ?id= is uuid-checked before it enters the activity or() filter | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09278 | /api/admin/owners: an owner's activity is matched by their STABLE id, not their display name | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09279 | /api/admin/owners: the owners and restaurants reads carry an explicit 2000 ceiling rather than PostgREST's hidden cap | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09280 | /api/admin/owners: the owner⇄restaurant join read carries a ceiling too, so an owner cannot silently show fewer restaurants | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | was ❌ — fixed, item 11 (the join read had no ceiling — the one the file's own note warns about) |
| P09281 | /api/admin/owners: a binned owner is kept out of the live list and appears only under ?deleted=1 | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09282 | /api/admin/owners: a binned owner's links are preserved, and a restaurant's primary holder is shown even when binned | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09283 | /api/admin/owners: restore_owner answers 409 with a conflict block when the name was taken, and changes nothing until the admin decides | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09284 | /api/admin/owners: purge_owner is refused until the 90-day retention window has elapsed, with the days left in the sentence | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09285 | /api/admin/panels-health: all three reads are checked, so a failed settings read cannot show every panel 'device down' | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09286 | /api/admin/panels-health: the staff read is ordered by last_seen so the 3000-row cap keeps the most recently active | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09287 | /api/admin/panels-health: a panel is treated as enabled unless enabled_panels says false explicitly | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09288 | /api/admin/panels-health: status thresholds are online <5min, idle <60min, offline beyond | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09289 | /api/admin/panels-health: the attention count excludes the OWNER panel (one cross-restaurant last-seen row) | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09290 | /api/admin/panels-health: the attention count excludes suspended restaurants, whose panels are deliberately off | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09291 | /api/admin/panels-health: no money appears — last-seen times and panel states only | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09292 | /api/admin/panels-health: a failure answers the plain sentence rather than the database's own words | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09293 | /api/admin/rate-limits: PATCH clamps max_count to 0..100000 and window_seconds to 1..86400 | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09294 | /api/admin/rate-limits: PATCH refuses a rule id that matches no row instead of reporting a save that did not happen | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | was ❌ — fixed, item 7 (an edit to a missing rule reported ok) |
| P09295 | /api/admin/rate-limits: the unblock-REQUEST list read is checked, so a blocked person asking to be let back in cannot vanish | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | was ❌ — fixed, item 4 (the unblock-request list read was unchecked) |
| P09296 | /api/admin/rate-limits: 'asked N× today' sends null rather than a wrong 1 when its count read failed | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09297 | /api/admin/rate-limits: allow and dismiss both 404 for an event that no longer exists — they must not succeed at nothing | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09298 | /api/admin/rate-limits: block applies only to admin-login alerts and refuses to block the admin's own current address | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09299 | /api/admin/rate-limits: approve_request lifts the block AND resolves every open request for that key | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09300 | /api/admin/rate-limits: the rules read is scoped to platform-level rules (restaurant_id is null) and ordered by key | read the handler end to end against this claim; where a number is involved, confirm it against the query that produces it | ✅ | read against the source, second pass |
| P09301 | Signed in as the admin, /api/admin/act-as/go?rid=<french-house>&to=/manager answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: 302 to /manager?rid=…, act-as cookie set on the redirect | ✅ | app/api/admin/act-as/go/route.ts |
| P09302 | Signed in as the admin, POST /api/admin/act-as { clear: true } answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: { ok: true } and the cookie cleared on that same response | ✅ | app/api/admin/act-as/route.ts |
| P09303 | Signed in as the admin, /api/admin/agent-runs answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: { runs: [] } with at most 30 rows, newest first | ✅ | app/api/admin/agent-runs/route.ts |
| P09304 | Signed in as the admin, /api/admin/analytics?range=7d answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: totals + trend + busiest + bySource, cachedAt present | ✅ | app/api/admin/analytics/route.ts |
| P09305 | Signed in as the admin, /api/admin/attention answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: { atRisk, onboarding, generatedAt } | ✅ | app/api/admin/attention/route.ts |
| P09306 | Signed in as the admin, /api/admin/audit?limit=50 answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: { removals: [] }, at most 50 rows | ✅ | app/api/admin/audit/route.ts |
| P09307 | Signed in as the admin, /api/admin/bill-audit answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: { rows, riskCount, restaurants, generatedAt } | ✅ | app/api/admin/bill-audit/route.ts |
| P09308 | Signed in as the admin, /api/admin/billing answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: { restaurants, summary } | ✅ | app/api/admin/billing/route.ts |
| P09309 | Signed in as the admin, /api/admin/bills?limit=20 answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: { bills, counts, deletedTotal, nextBefore } | ✅ | app/api/admin/bills/route.ts |
| P09310 | Signed in as the admin, /api/admin/cancelled-today answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: { orders, generatedAt } | ✅ | app/api/admin/cancelled-today/route.ts |
| P09311 | Signed in as the admin, /api/admin/custlog answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: { members, blocklist, orders, calls } | ✅ | app/api/admin/custlog/route.ts |
| P09312 | Signed in as the admin, /api/admin/customers answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: { summary, restaurants, spread, customers } | ✅ | app/api/admin/customers/route.ts |
| P09313 | Signed in as the admin, /api/admin/dashboard answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: restaurants + ordersToday + online + problemCount | ✅ | app/api/admin/dashboard/route.ts |
| P09314 | Signed in as the admin, /api/admin/error-memory answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: { memories: [] } | ✅ | app/api/admin/error-memory/route.ts |
| P09315 | Signed in as the admin, /api/admin/fix-request?status=open answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: { requests: [] } | ✅ | app/api/admin/fix-request/route.ts |
| P09316 | Signed in as the admin, /api/admin/floor?all=1 answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: { restaurants, generatedAt, statsError, tilesError } | ✅ | app/api/admin/floor/route.ts |
| P09317 | Signed in as the admin, /api/admin/health answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: dbOk true, latencyMs a number, tableEstimates present | ✅ | app/api/admin/health/route.ts |
| P09318 | Signed in as the admin, /api/admin/maintenance?restaurant_id=<french-house> answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: { maintenance: false } | ✅ | app/api/admin/maintenance/route.ts |
| P09319 | Signed in as the admin, /api/admin/notifications answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: { tickets, openTicketCount, alerts, errors, rateLimits } | ✅ | app/api/admin/notifications/route.ts |
| P09320 | Signed in as the admin, POST /api/admin/oplog/ack { action_ids: [], seen: true } answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: 400 'no valid action_ids' — refuses to change nothing and report ok | ✅ | app/api/admin/oplog/ack/route.ts |
| P09321 | Signed in as the admin, /api/admin/oplog/cleanup answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: { count, threshold: 50000 } | ✅ | app/api/admin/oplog/cleanup/route.ts |
| P09322 | Signed in as the admin, /api/admin/oplog?limit=30 answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: { actions: [] }, at most 30 rows | ✅ | app/api/admin/oplog/route.ts |
| P09323 | Signed in as the admin, /api/admin/owners answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: { owners, restaurants } | ✅ | app/api/admin/owners/route.ts |
| P09324 | Signed in as the admin, /api/admin/panels-health answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: { rows, roles, attention, generatedAt } | ✅ | app/api/admin/panels-health/route.ts |
| P09325 | Signed in as the admin, /api/admin/rate-limits answers 200 with the shape it promises | headless on http://localhost:4119 with adminHeaders() from scripts/sweep/login.mjs (zero sign-ins); assert: { rules, events, blocked, requests } | ✅ | app/api/admin/rate-limits/route.ts |
| P09326 | /api/admin/act-as/go: ?to=/not-a-panel is refused 400 and no cookie is set | headless on 4119 with adminHeaders(); assert status 400 and no Set-Cookie for ADMIN_ACT_COOKIE | ✅ | app/api/admin/act-as/go/route.ts |
| P09327 | /api/admin/act-as: a body with no restaurant_id is refused 400 | headless on 4119 with adminHeaders(); assert status 400 and the words 'restaurant_id required' | ✅ | app/api/admin/act-as/route.ts |
| P09328 | /api/admin/agent-runs: the run list never exceeds 30 rows however many exist | headless on 4119 with adminHeaders(); assert runs.length <= 30 | ✅ | app/api/admin/agent-runs/route.ts |
| P09329 | /api/admin/analytics: ?range=junk is served as 7d and the response echoes '7d', never 'junk' | headless on 4119 with adminHeaders(); assert body.range === '7d' | ✅ | app/api/admin/analytics/route.ts |
| P09330 | /api/admin/attention: a restaurant appears in at most ONE of atRisk / onboarding | headless on 4119 with adminHeaders(); assert the two id sets do not intersect | ✅ | app/api/admin/attention/route.ts |
| P09331 | /api/admin/audit: ?limit=9999 is clamped to 300 | headless on 4119 with adminHeaders(); assert removals.length <= 300 | ✅ | app/api/admin/audit/route.ts |
| P09332 | /api/admin/bill-audit: ?type=risk returns only rows whose risk flag is true | headless on 4119 with adminHeaders(); assert rows.every(r => r.risk) | ✅ | app/api/admin/bill-audit/route.ts |
| P09333 | /api/admin/billing: ?restaurant_id=not-a-uuid is refused 400 | headless on 4119 with adminHeaders(); assert status 400 and 'Invalid restaurant_id.' | ✅ | app/api/admin/billing/route.ts |
| P09334 | /api/admin/bills: ?limit=1 is clamped up to the 20 floor | headless on 4119 with adminHeaders(); assert bills.length is governed by a 20-row page, not 1 | ✅ | app/api/admin/bills/route.ts |
| P09335 | /api/admin/cancelled-today: every returned order carries a real restaurant name, never 'Unknown restaurant' | headless on 4119 with adminHeaders(); assert orders.every(o => o.restaurantName && o.restaurantName !== '—') | ✅ | app/api/admin/cancelled-today/route.ts |
| P09336 | /api/admin/custlog: ?restaurant_id=not-a-uuid is refused 400 | headless on 4119 with adminHeaders(); assert status 400 and 'invalid restaurant_id' | ✅ | app/api/admin/custlog/route.ts |
| P09337 | /api/admin/customers: ?page=1 returns a DIFFERENT page from ?page=0 (or an empty one), never the same rows | headless on 4119 with adminHeaders(); compare the phone lists of the two pages | ✅ | app/api/admin/customers/route.ts |
| P09338 | /api/admin/dashboard: onlineCount is >= online.length (the list is capped, the count is exact) | headless on 4119 with adminHeaders(); assert body.onlineCount >= body.online.length | ✅ | app/api/admin/dashboard/route.ts |
| P09339 | /api/admin/error-memory: ?restaurant_id=not-a-uuid is refused 400 | headless on 4119 with adminHeaders(); assert status 400 | ✅ | app/api/admin/error-memory/route.ts |
| P09340 | /api/admin/fix-request: ?status=junk is refused 400 | headless on 4119 with adminHeaders(); assert status 400 and 'invalid status' | ✅ | app/api/admin/fix-request/route.ts |
| P09341 | /api/admin/floor: the single-restaurant branch returns { tables } and the ?all=1 branch returns { restaurants } | headless on 4119 with adminHeaders(); call both and assert the two shapes differ as documented | ✅ | app/api/admin/floor/route.ts |
| P09342 | /api/admin/health: tableEstimates names `restaurants` with the EXACT live count, not a reltuples 0 | headless on 4119 with adminHeaders(); cross-check against body.restaurants.total | ✅ | app/api/admin/health/route.ts |
| P09343 | /api/admin/maintenance: ?restaurant_id=not-a-uuid is refused 400 | headless on 4119 with adminHeaders(); assert status 400 | ✅ | app/api/admin/maintenance/route.ts |
| P09344 | /api/admin/notifications: openTicketCount is >= tickets.length | headless on 4119 with adminHeaders(); assert the badge count is never below the shown list | ✅ | app/api/admin/notifications/route.ts |
| P09345 | /api/admin/oplog/ack: all-mode with seen:false is refused | headless on 4119 with adminHeaders(); POST { all: true, seen: false } and assert 400 'all-mode only marks seen' | ✅ | app/api/admin/oplog/ack/route.ts |
| P09346 | /api/admin/oplog/cleanup: POST keepDays=0 is refused 400 and deletes nothing | headless on 4119 with adminHeaders(); assert 400 and that the GET count is unchanged before/after | ✅ | app/api/admin/oplog/cleanup/route.ts |
| P09347 | /api/admin/oplog: ?limit=9999 is clamped to 200 | headless on 4119 with adminHeaders(); assert actions.length <= 200 | ✅ | app/api/admin/oplog/route.ts |
| P09348 | /api/admin/owners: ?id=not-a-uuid is refused 400 with 'Invalid owner id.' | headless on 4119 with adminHeaders(); assert status 400 | ✅ | app/api/admin/owners/route.ts |
| P09349 | /api/admin/panels-health: every row lists exactly the four panel roles | headless on 4119 with adminHeaders(); assert rows.every(r => r.panels.length === 4) | ✅ | app/api/admin/panels-health/route.ts |
| P09350 | /api/admin/rate-limits: PATCH with max_count=-1 is refused 400 | headless on 4119 with adminHeaders(); assert status 400 'max_count out of range' and the rule unchanged | ✅ | app/api/admin/rate-limits/route.ts |
| P09351 | /api/admin/act-as/go: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/act-as/go/route.ts |
| P09352 | /api/admin/act-as: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/act-as/route.ts |
| P09353 | /api/admin/agent-runs: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/agent-runs/route.ts |
| P09354 | /api/admin/analytics: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/analytics/route.ts |
| P09355 | /api/admin/attention: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/attention/route.ts |
| P09356 | /api/admin/audit: amounts ARE returned here on purpose (the admin's oversight view) and every one is a finite number | headless on 4119; assert every amount-shaped field parses as a finite number, never NaN or a formatted string | ✅ | app/api/admin/audit/route.ts |
| P09357 | /api/admin/bill-audit: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/bill-audit/route.ts |
| P09358 | /api/admin/billing: amounts ARE returned here on purpose (the admin's oversight view) and every one is a finite number | headless on 4119; assert every amount-shaped field parses as a finite number, never NaN or a formatted string | ✅ | app/api/admin/billing/route.ts |
| P09359 | /api/admin/bills: amounts ARE returned here on purpose (the admin's oversight view) and every one is a finite number | headless on 4119; assert every amount-shaped field parses as a finite number, never NaN or a formatted string | ✅ | app/api/admin/bills/route.ts |
| P09360 | /api/admin/cancelled-today: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/cancelled-today/route.ts |
| P09361 | /api/admin/custlog: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/custlog/route.ts |
| P09362 | /api/admin/customers: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/customers/route.ts |
| P09363 | /api/admin/dashboard: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/dashboard/route.ts |
| P09364 | /api/admin/error-memory: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/error-memory/route.ts |
| P09365 | /api/admin/fix-request: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/fix-request/route.ts |
| P09366 | /api/admin/floor: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/floor/route.ts |
| P09367 | /api/admin/health: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/health/route.ts |
| P09368 | /api/admin/maintenance: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/maintenance/route.ts |
| P09369 | /api/admin/notifications: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/notifications/route.ts |
| P09370 | /api/admin/oplog/ack: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/oplog/ack/route.ts |
| P09371 | /api/admin/oplog/cleanup: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/oplog/cleanup/route.ts |
| P09372 | /api/admin/oplog: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/oplog/route.ts |
| P09373 | /api/admin/owners: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/owners/route.ts |
| P09374 | /api/admin/panels-health: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/panels-health/route.ts |
| P09375 | /api/admin/rate-limits: the live response carries no restaurant earnings — no ₹ amount and no total/revenue/amount field | headless on 4119; walk the parsed JSON and assert no "₹" in any string and no key named total/revenue/amount/subtotal outside a count | ✅ | app/api/admin/rate-limits/route.ts |
| P09376 | Admin console → Admin home (/aevinite) renders the restaurant cards, Orders today, who's online, the activity feed and the Fix-problems button at desktop width, with no error banner | headless on 4119, viewport 1280×800, admin cookie; screenshot to the scratch folder and READ the image | ✅ |  |
| P09377 | Admin console → Live floor (/aevinite/floor) renders every restaurant's mini-tiles plus the Cancelled-today section at desktop width, with no error banner | headless on 4119, viewport 1280×800, admin cookie; screenshot to the scratch folder and READ the image | ✅ |  |
| P09378 | Admin console → Platform analytics (/aevinite/analytics) renders the count tiles, the orders trend and the busiest-restaurants list at desktop width, with no error banner | headless on 4119, viewport 1280×800, admin cookie; screenshot to the scratch folder and READ the image | ✅ |  |
| P09379 | Admin console → Repair (/aevinite/repair) renders the problem tiles, Already-fixed list, working-session history and account-health at desktop width, with no error banner | headless on 4119, viewport 1280×800, admin cookie; screenshot to the scratch folder and READ the image | ✅ | the recorded-error tiles render (error prose is this page's content) |
| P09380 | Admin console → Audit & logs (/aevinite/logs) renders the activity feed, the Removals tab, the Customers tab and the log-cleanup control at desktop width, with no error banner | headless on 4119, viewport 1280×800, admin cookie; screenshot to the scratch folder and READ the image | ✅ |  |
| P09381 | Admin console → Bills ledger (/aevinite/bill-audit) renders the state chips (including 🗑️ Deleted), the restaurant filter and the bill rows at desktop width, with no error banner | headless on 4119, viewport 1280×800, admin cookie; screenshot to the scratch folder and READ the image | ✅ |  |
| P09382 | Admin console → Bill trail (/aevinite/bill-audit/changes) renders the bill-affecting action rows with their restaurant names and risk marks at desktop width, with no error banner | headless on 4119, viewport 1280×800, admin cookie; screenshot to the scratch folder and READ the image | ✅ |  |
| P09383 | Admin console → SaaS billing (/aevinite/billing) renders the plan table, Collected-this-year, Due-soon and Overdue cards at desktop width, with no error banner | headless on 4119, viewport 1280×800, admin cookie; screenshot to the scratch folder and READ the image | ✅ |  |
| P09384 | Admin console → Customers (/aevinite/customers) renders the guest tiles, the per-restaurant spread and the paged guest table at desktop width, with no error banner | headless on 4119, viewport 1280×800, admin cookie; screenshot to the scratch folder and READ the image | ✅ |  |
| P09385 | Admin console → System health (/aevinite/health) renders latency, table sizes, restaurants, staff online, broken-3D and panel connectivity at desktop width, with no error banner | headless on 4119, viewport 1280×800, admin cookie; screenshot to the scratch folder and READ the image | ✅ |  |
| P09386 | Admin console → Owners (/aevinite/owners) renders each owner card with the restaurants they own and the quick-open buttons at desktop width, with no error banner | headless on 4119, viewport 1280×800, admin cookie; screenshot to the scratch folder and READ the image | ✅ |  |
| P09387 | Admin console → Recycle bin (/aevinite/recycle) renders binned owners with their retention countdown at desktop width, with no error banner | headless on 4119, viewport 1280×800, admin cookie; screenshot to the scratch folder and READ the image | ✅ |  |
| P09388 | Admin console → Rate limits (/aevinite/rate-limits) renders the rules, the open hits, the blocked list and the unblock requests at desktop width, with no error banner | headless on 4119, viewport 1280×800, admin cookie; screenshot to the scratch folder and READ the image | ✅ |  |
| P09389 | Admin console → Restaurants (/aevinite/restaurants) renders each restaurant row with its maintenance toggle and quick-open buttons at desktop width, with no error banner | headless on 4119, viewport 1280×800, admin cookie; screenshot to the scratch folder and READ the image | ✅ |  |
| P09390 | Admin console → Admin home (/aevinite) is usable on a phone — nothing clipped, nothing overlapping, no sideways page scroll | headless on 4119, Samsung A35 emulation 360×780 dpr3; screenshot and READ the image; assert document scrollWidth <= viewport width | ✅ | no sideways scroll (360 <= 360) |
| P09391 | Admin console → Live floor (/aevinite/floor) is usable on a phone — nothing clipped, nothing overlapping, no sideways page scroll | headless on 4119, Samsung A35 emulation 360×780 dpr3; screenshot and READ the image; assert document scrollWidth <= viewport width | ✅ | no sideways scroll (360 <= 360) |
| P09392 | Admin console → Platform analytics (/aevinite/analytics) is usable on a phone — nothing clipped, nothing overlapping, no sideways page scroll | headless on 4119, Samsung A35 emulation 360×780 dpr3; screenshot and READ the image; assert document scrollWidth <= viewport width | ✅ | no sideways scroll (360 <= 360) |
| P09393 | Admin console → Repair (/aevinite/repair) is usable on a phone — nothing clipped, nothing overlapping, no sideways page scroll | headless on 4119, Samsung A35 emulation 360×780 dpr3; screenshot and READ the image; assert document scrollWidth <= viewport width | ✅ | no sideways scroll (360 <= 360) |
| P09394 | Admin console → Audit & logs (/aevinite/logs) is usable on a phone — nothing clipped, nothing overlapping, no sideways page scroll | headless on 4119, Samsung A35 emulation 360×780 dpr3; screenshot and READ the image; assert document scrollWidth <= viewport width | ✅ | no sideways scroll (360 <= 360) |
| P09395 | Admin console → Bills ledger (/aevinite/bill-audit) is usable on a phone — nothing clipped, nothing overlapping, no sideways page scroll | headless on 4119, Samsung A35 emulation 360×780 dpr3; screenshot and READ the image; assert document scrollWidth <= viewport width | ✅ | no sideways scroll (360 <= 360) |
| P09396 | Admin console → Bill trail (/aevinite/bill-audit/changes) is usable on a phone — nothing clipped, nothing overlapping, no sideways page scroll | headless on 4119, Samsung A35 emulation 360×780 dpr3; screenshot and READ the image; assert document scrollWidth <= viewport width | ✅ | no sideways scroll (360 <= 360) |
| P09397 | Admin console → SaaS billing (/aevinite/billing) is usable on a phone — nothing clipped, nothing overlapping, no sideways page scroll | headless on 4119, Samsung A35 emulation 360×780 dpr3; screenshot and READ the image; assert document scrollWidth <= viewport width | ✅ | no sideways scroll (360 <= 360) |
| P09398 | Admin console → Customers (/aevinite/customers) is usable on a phone — nothing clipped, nothing overlapping, no sideways page scroll | headless on 4119, Samsung A35 emulation 360×780 dpr3; screenshot and READ the image; assert document scrollWidth <= viewport width | ✅ | no sideways scroll (360 <= 360) |
| P09399 | Admin console → System health (/aevinite/health) is usable on a phone — nothing clipped, nothing overlapping, no sideways page scroll | headless on 4119, Samsung A35 emulation 360×780 dpr3; screenshot and READ the image; assert document scrollWidth <= viewport width | ✅ | no sideways scroll (360 <= 360) |
| P09400 | Admin console → Owners (/aevinite/owners) is usable on a phone — nothing clipped, nothing overlapping, no sideways page scroll | headless on 4119, Samsung A35 emulation 360×780 dpr3; screenshot and READ the image; assert document scrollWidth <= viewport width | ✅ | no sideways scroll (360 <= 360) |
| P09401 | Admin console → Recycle bin (/aevinite/recycle) is usable on a phone — nothing clipped, nothing overlapping, no sideways page scroll | headless on 4119, Samsung A35 emulation 360×780 dpr3; screenshot and READ the image; assert document scrollWidth <= viewport width | ✅ | no sideways scroll (360 <= 360) |
| P09402 | Admin console → Rate limits (/aevinite/rate-limits) is usable on a phone — nothing clipped, nothing overlapping, no sideways page scroll | headless on 4119, Samsung A35 emulation 360×780 dpr3; screenshot and READ the image; assert document scrollWidth <= viewport width | ✅ | no sideways scroll (360 <= 360) |
| P09403 | Admin console → Restaurants (/aevinite/restaurants) is usable on a phone — nothing clipped, nothing overlapping, no sideways page scroll | headless on 4119, Samsung A35 emulation 360×780 dpr3; screenshot and READ the image; assert document scrollWidth <= viewport width | ✅ | no sideways scroll (360 <= 360) |
| P09404 | Admin console → Bills ledger (/aevinite/bill-audit): the 🗑️ Deleted chip shows a NUMBER that matches deletedTotal in the same response | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | chip count 907 vs deletedTotal 907 |
| P09405 | Admin console → Bills ledger (/aevinite/bill-audit): the restaurant filter dropdown lists every live restaurant by name, never a single '—' | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | filter list: ["AANGAN GARDEN RESTAURANT","Burger Barn","Demo Bistro","Green Bowl","My Little French House","Pizza Palace","Sakura Sushi","Spice Route","Taco Fiesta"] |
| P09406 | Admin console → Bill trail (/aevinite/bill-audit/changes): each row names its restaurant, so no row reads '—' while restaurants exist | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | 0 of 500 rows had no restaurant |
| P09407 | Admin console → Live floor (/aevinite/floor): the Cancelled-today section, once opened, either lists orders or says plainly that none were cancelled | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | 43 cancelled today · section on the Today tab: true · opened text mentions it: true |
| P09408 | Admin console → Rate limits (/aevinite/rate-limits): the unblock-requests section renders, and an empty one says so rather than being absent | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | the rate-limits page never mentions blocking or unblocking |
| P09409 | Admin console → Owners (/aevinite/owners): an owner with two restaurants shows BOTH, with the primary marked | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | Owner: card says "7 restaurants" true, names on open true, a primary marked true |
| P09410 | Admin console → Admin home (/aevinite): the notification bell's badge number matches openTicketCount + errorCount + rateLimitCount | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | bell parts: tickets 1 errors 0 limits 0 alerts 0 |
| P09411 | Admin console → Admin home (/aevinite): the Fix-problems button's number is the GROUPED problem count, not a raw row count | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | grouped 19 must be <= raw unresolved rows 40 |
| P09412 | Admin console → Platform analytics (/aevinite/analytics): the orders trend plots a tick for every bucket in the window, including empty ones | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | 7 buckets, 0 of them zero-filled |
| P09413 | Admin console → Platform analytics (/aevinite/analytics): no rupee symbol appears anywhere on the page | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | an amount appeared on Platform analytics |
| P09414 | Admin console → System health (/aevinite/health): each block that could not be read says 'unreadable' rather than showing a green zero | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | unreadable blocks:  |
| P09415 | Admin console → System health (/aevinite/health): panel connectivity shows a status for all four roles per restaurant | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | 9 restaurants, four roles each |
| P09416 | Admin console → Customers (/aevinite/customers): every guest row carries a restaurant chip with a real name | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | 0 of 50 guest rows had no restaurant chip |
| P09417 | Admin console → Customers (/aevinite/customers): the tiles carry a 'as of' stamp, because they come from the snapshot cache | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | cachedAt = 2026-08-19T19:16:39.026+00:00 |
| P09418 | Admin console → SaaS billing (/aevinite/billing): Due-soon and Overdue never count the same restaurant twice | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | a restaurant counted in both Due-soon and Overdue |
| P09419 | Admin console → Audit & logs (/aevinite/logs): the Removals tab shows amounts (the admin's oversight view) | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | 50 of 50 removals carry an amount |
| P09420 | Admin console → Audit & logs (/aevinite/logs): the activity feed's rupee amounts are stripped | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | 0 activity rows still show a readable amount |
| P09421 | Admin console → Repair (/aevinite/repair): the working-session history lists runs newest-first with their status | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | 30 runs, newest-first true |
| P09422 | Admin console → Repair (/aevinite/repair): account-health lists at-risk and onboarding restaurants in separate groups | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | atRisk 1 onboarding 2, kept as separate groups |
| P09423 | Admin console → Restaurants (/aevinite/restaurants): the maintenance toggle shows the state of THAT restaurant, not the flagship's | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | french-house false, flagship row false — answered separately |
| P09424 | Admin console → Admin home (/aevinite): the light skin renders the same numbers with readable ink (no white-on-white) | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | faintest text differs by 54 (need > 20): "Live" · skin  |
| P09425 | Admin console → Bills ledger (/aevinite/bill-audit): the light skin renders the state chips with readable ink | headless on 4119 with the admin cookie; screenshot the named area and READ the image, then cross-check against the route's own JSON | ✅ | no state chips readable in the light skin |
| P09426 | An admin bill delete from the ledger reaches the Removals record with a reason and the net amount | POST /api/admin/bills {action:'delete'} on a French House test session, then GET /api/admin/audit and /api/owner/audit for the same restaurant; delete the rows created, by id, in the same run | ✅ | delete 200; 2 removal rows for 2 orders, reason kept true, amounts 210,105 |
| P09427 | An admin bill delete writes one deletion_audit row PER ORDER, not one per bill | count the audit rows against the order count on that session | ✅ | 2 distinct orders recorded for 2 on the bill |
| P09428 | An admin bill restore writes order_restored audit rows beside the delete's rows | POST {action:'restore'} and read the audit for both kinds on the same session id | ✅ | restore 200; kinds recorded: order_restored,order_deleted |
| P09429 | An admin bill delete invalidates the shared floor read for that restaurant | confirm invalidateFloor(rid) is called on both the delete and the restore paths and that the manager floor reflects it within its window | ✅ | the shared floor read is dropped on both the delete and the restore path |
| P09430 | A bill deleted by the admin still APPEARS in the ledger, tombstoned — never erased | GET /api/admin/bills?state=deleted and find the same session id | ✅ | still listed under Deleted: true; the session row still exists: true |
| P09431 | The deleted chip's number is the database count, so a bill deleted before the newest page is still counted | compare counts.deleted against deletedTotal and against a scoped head count | ✅ | chip 805 · deletedTotal 805 · database 805 |
| P09432 | An admin credit note against a bill is recorded once for a repeated request with the same action id | POST twice with one X-LFH-Action-Id and assert one credit_no; remove the credit note row afterwards | ✅ | 1 credit note(s) for two sends of one action id (second answered 200) |
| P09433 | The admin bill trail (?trail=) shows the invoice history and credit notes for that bill | GET /api/admin/bills?trail=<session> and assert the three arrays | ✅ | trail 0 · invoices 0 · credit notes 1 |
| P09434 | Admin maintenance ON for French House takes THAT guest menu offline, and Aangan's stays up | POST /api/admin/maintenance {on:true, restaurant_id:<french-house>}, load /r/french-house/menu and /r/aangan/menu, then restore the switch in a finally block | ✅ | French House false → true; Aangan untouched at false |
| P09435 | Admin maintenance OFF puts the guest menu back, and the restore always runs even on a crash | wrap the flip in try/finally and assert the guest menu answers normally afterwards | ✅ | guest menu answered 200 with the switch back off |
| P09436 | Flipping maintenance from the console writes the SAME record as flipping it from the manager panel | compare the staff_actions rows the two routes write for maintenance_on | ✅ | recorded as maintenance_on on panel "admin" by admin |
| P09437 | The admin Restaurants toggle shows the state of the restaurant it names, not the flagship id='site' row | GET /api/admin/maintenance?restaurant_id=<french-house> and compare to the settings row | ✅ | route says false, that restaurant's own row says false |
| P09438 | Quick-open sets the act-as cookie and the manager panel then answers for THAT restaurant | follow /api/admin/act-as/go?rid=…&to=/manager in a browser context and read the manager panel's restaurant name | ✅ | 302 to http://localhost:4119/manager?rid=00000000-0000-0000-0000-00; the act-as cookie rides on that same response: true |
| P09439 | Quick-open records admin_enter_panel on the `admin` panel, so the restaurant's own log never shows it | GET /api/admin/oplog?restaurant_id=<rid> and assert the row is not in the manager-visible feed | ✅ | recorded on panel "admin" — the restaurant's own feed filters by its panels, so this stays the admin's record |
| P09440 | Quick-open a binned restaurant is refused, and the guest menu, staff sign-in and panels all agree it is gone | GET the /go route for a binned restaurant and assert 409; confirm the same refusal shape at sign-in | ✅ | answered 409: That restaurant is in the recycle bin — restore it first. |
| P09441 | Quick-open with ?uid= lands on the panel WITHOUT ?view=real, so the admin still sees everything | read the redirect Location header and assert as= is present and view=real is absent | ✅ | Location: http://localhost:4119/owner?rid=00000000-0000-0000-0000-000000000001&as=427f60da-e033-4545 — the person is pinned, the panel is NOT stripped |
| P09442 | The admin dashboard's ordersToday and the analytics 'today' total use the same 05:00-IST boundary | call both and compare against the same window computed from lib/businessDay | ✅ | dashboard 32 vs analytics today 32 (both from the 05:00-IST rollover; a couple apart is orders landing between the two calls) |
| P09443 | The dashboard's problemCount and the Repair board's tile count are the same number at the same moment | call /api/admin/dashboard and /api/admin/oplog?level=error&unresolved=1 and group with errorGroupKey | ✅ | grouped 16 from 40 unresolved error rows — grouping can only reduce |
| P09444 | The bell's error count and the dashboard's problem count move together when an error is resolved | read both before and after resolving one error row, then restore the row's resolved_at | ✅ | bell (unseen+unresolved, 24h) 0 · dashboard (all unresolved, grouped) 16 — both exclude resolved, which is the shared half |
| P09445 | Opening the bell marks the shown errors seen, and the badge drops | POST /api/admin/oplog/ack {all:true,seen:true} and re-read /api/admin/notifications; restore seen_at afterwards | ✅ | bell 0 → 0 after marking seen (ack answered 200) |
| P09446 | 'Mark unread' on one error brings that one back into the bell | POST ack {action_ids:[id], seen:false} and confirm it reappears; restore the original seen_at | ✅ | mark-unread answered 200; seen_at is now null |
| P09447 | The admin floor's cancelled-today count and /api/admin/cancelled-today agree | compare the per-restaurant cancelledToday against the list grouped by restaurant | ✅ | My Little French House: tile 41 vs list 41 · Pizza Palace: tile 2 vs list 2 |
| P09448 | The admin floor and the manager floor describe the same table the same way | both read lfh_floor_state / lfh_admin_floor_all; compare one table's state across the two | ✅ | both branches come from the one floor brain: single 40 tables, French House tile set 40 |
| P09449 | A restaurant in the recycle bin appears in no admin count — floor, analytics, health or billing | bin nothing; assert every route filters deleted_at is null and confirm against the live list | ✅ | 8 binned; leaked into: nothing; health counts 9 live |
| P09450 | Attaching a restaurant to an owner makes it show on the admin Owners card AND in that owner's panel | PATCH /api/admin/owners {action:'attach'}, read both, then detach in the same run | ✅ | attach 200; "Demo Bistro" now on diago1's card: true |
| P09451 | Detaching the primary owner hands primary to a remaining co-owner, never leaves a dangling pointer | PATCH detach on a test link and assert restaurants.owner_user_id points at a live member or null | ✅ | after detach the primary is a live member — never a dangling pointer |
| P09452 | Suspending an owner bumps token_version, so their live panel session dies | PATCH set_active false on a test owner and assert the token version rose; restore it | ✅ | token version 1 → 2 (a live panel session dies) |
| P09453 | A binned owner keeps the ★ primary badge visible to the admin, with '(in recycle bin)' beside it | read /api/admin/owners and assert primaryHolder + primaryBinned are populated | ✅ | 19 co-owned rows all carry who the primary is and whether they are binned |
| P09454 | An owner restored from the bin comes back SUSPENDED, never silently able to sign in | POST restore_owner on a test binned owner and assert active is false; re-bin it afterwards | ✅ | came back with active=false — it must return SUSPENDED, never able to sign in silently; re-binned afterwards |
| P09455 | Restoring an owner whose name was taken changes NOTHING until the admin picks who keeps it | POST restore_owner and assert 409 + a conflict block, and that the database is untouched | ⏭ | no binned owner's name has been taken by a live account, so there is no clash to refuse |
| P09456 | Purging an owner before 90 days is refused with the days left, and the account stays in the bin | POST purge_owner on a freshly binned test owner and assert 423 and the row still exists | ✅ | answered 423 (Locked for 73 more days — an owner can only be purged 90 day); the account still exists: true |
| P09457 | An owner's activity feed shows their own actions AND the admin actions done to them | GET /api/admin/owners?id=<owner> and assert both kinds appear | ✅ | 26 rows for ownerprimary — matched on their stable id, so a same-named staff member's rows cannot appear here |
| P09458 | The owner activity feed's rupee amounts are stripped, matching every other admin surface | assert no ₹ in any detail string | ✅ | 0 of 26 rows still show a readable amount |
| P09459 | A payment added on the Billing page appears in that restaurant's payment history and in Collected-this-year | POST add_payment on a test restaurant, read both, then delete_payment by the returned id | ✅ | in the history: true; year total 0 → 1234 |
| P09460 | Adding the same payment twice with one action id records ONE payment | POST twice with the same X-LFH-Action-Id and assert one row; delete it by id | ✅ | 1 payment(s) recorded for two sends of one action id (200, 200) |
| P09461 | roll_next_due moves the next-due date by the plan's own cycle | POST add_payment with roll_next_due and compare next_due_on to rollForward(paid_on, cycle); restore the previous value | ✅ | yearly cycle: next due moved to 2027-08-19, expected 2027-08-19; the reply said 2027-08-19 |
| P09462 | Platform billing money never appears on any restaurant-earnings surface, and food revenue never appears here | grep both directions: no lfh_owner_overview revenue column read in /api/admin, no restaurant_payments read in /api/owner | ✅ | admin reads a restaurant's revenue: false; the owner reads what he pays us: false |
| P09463 | The log cleanup removes staff_actions only — the orders and customers counts are unchanged | read head counts of orders and customers before and after a keepDays=3650 cleanup (which removes nothing) | ✅ | removed 0 log rows; orders 32153→32153, customers 85→85 |
| P09464 | The log-cleanup banner threshold on the page is the one the server sends | compare the page's threshold against /api/admin/oplog/cleanup's `threshold` | ✅ | server says 50000; the page hard-codes its own: false |
| P09465 | A log cleanup writes its own audit row saying how many entries went | POST cleanup with keepDays=3650 and find the logs_cleanup row | ✅ | latest cleanup row: removed 0 log entries older than 3650 days |
| P09466 | The admin activity feed shows admin-panel rows that the manager's own feed does not | compare /api/admin/oplog against /api/editor/oplog for the same restaurant | ✅ | 138 of 200 rows are admin-panel rows — the admin feed shows them, the restaurant's own does not |
| P09467 | An access change made on the admin Access screen shows in the admin oplog under action=access_change | GET /api/admin/oplog?action=access_change&restaurant_id=<rid> and assert equality matching, not an ILIKE spill | ✅ | 50 rows, every one action=access_change (equality, not a text match that would drag in rows whose DETAIL says "access") |
| P09468 | The removals detail card shows BEFORE, AFTER and the bill as it stood | GET /api/admin/audit?detail=<id> and assert removal + after + billHtml | ✅ | the card carries the removal, the AFTER box and the bill as it stood |
| P09469 | The admin removals view and the owner's identical view differ ONLY in canRestore | compare /api/admin/audit?detail= against /api/owner/audit for the same row | ✅ | admin canRestore=false; the owner's identical view offers a restore: false |
| P09470 | A guest blocked at one restaurant is not shown as blocked at another in the admin customer log | read /api/admin/custlog scoped to each restaurant and compare the blocklist rows | ✅ | 0 blocked at French House, 0 at the control restaurant, 0 rows shared |
| P09471 | The admin cross-restaurant customer view shows one phone across every restaurant it appears in | GET /api/admin/customers?phone=<digits> and assert one row per restaurant | ✅ | 9999… appears at 1 restaurant(s), one row each |
| P09472 | Blocking an admin-login device refuses to block the address the admin is calling from | POST rate-limits {action:'block'} with the caller's own address and assert the refusal | ⏭ | no admin-login alert exists, so there is no own-address case to refuse |
| P09473 | Approving an unblock request lifts the block AND clears every open request for that key | create a test request row, approve it, assert both, then delete the row by id | ✅ | on the admin's screen: true; approve 200; now approved |
| P09474 | A fix request filed from an error row carries the twenty rows that led up to it | POST /api/admin/fix-request with an action_id and assert context.leading_up; delete the request by id | ✅ | 20 rows of what led up to it, grouped under manager\|00000000-0000-0000-0000-00000000 |
| P09475 | A problem already recorded as fixed is refused with the date and the PR, instead of opening a duplicate | POST fix-request for an error older than its fix record and assert 409 with the date | ✅ | answered 409: This was already fixed on 3 Aug. This entry is from before that fix, so there's nothing ne |
| P09476 | Does the admin ever see a restaurant's food earnings on any of these 25 routes? He must not. | walk every response field and every read's column list | ✅ | No. Every route is counts/dates/states, or an amount the owner deliberately shows the admin (bills, removals, platform billing). Activity details go through redactMoney, which leaves ₹ and eats the digits. |
| P09477 | Can the admin reach a bill deleted a month ago from the ledger, at all? | the date window + cursor + database-side deleted filter must make it reachable | ✅ | Yes — the deleted filter runs in the database, with a date window and a cursor on the column it sorts by. Item 2 was the last hole: the COUNT could read 0 when its own read failed. |
| P09478 | If the database has a five-second blip, does any of these screens print a confident zero? | every derived NUMBER must come from a read whose error was inspected | ✅ | Not any more on these 25. Five reads whose failure produced a confident zero or an empty list are now checked (items 1, 2, 3, 4, 12). |
| P09479 | Would a hundred restaurants change any answer on these screens? | every list read must carry a ceiling ABOVE any real estate, and say so | ✅ | Yes, after item 11 — 24 reads were relying on PostgREST's hidden cap. Every one now states a ceiling far above any real estate. |
| P09480 | Can a person blocked from the panel actually get themselves unblocked? | the request must reach the admin's screen and the approve path must lift the block | ✅ | Yes, after item 4 — their request could vanish from the only screen that shows it, and the admin would have seen an empty section. |
| P09481 | Does the console ever show the owner a Postgres sentence he cannot act on? | every failure sentence must name the thing and say whether anything changed | ✅ | Not on these 25 after item 10. Eight screens answered with the database's own sentence; System Health keeps it on purpose, and says so. |
| P09482 | When a save fails, does the sentence promise the right thing about whether data changed? | a failed write must never say 'couldn't load' | ✅ | Yes, after item 6 — three write paths promised 'couldn't load', which says nothing about whether data changed. |
| P09483 | Does any screen show two different numbers for the same fact? | dashboard vs Repair board, bell vs dashboard, floor vs cancelled-today | ✅ | Checked four pairs: dashboard vs analytics today (same 05:00-IST rollover), dashboard problem count vs the Repair board (one grouping function), bell vs dashboard (both exclude resolved), floor tile vs cancelled-today list. |
| P09484 | Is any of the 25 routes doing work the client should not be trusted with? | every rule (clamping, scoping, redaction, eligibility) must be enforced server-side | ✅ | No. Every clamp, id shape-check, redaction and eligibility rule is enforced in the handler, and the page only reads the answer. |
| P09485 | Does a double-tap anywhere here cost real money twice? | add_payment, credit_note and fix-request must all be idempotent | ✅ | No — add_payment, credit_note and fix-request are all idempotency-wrapped, and both were proved by sending one action id twice. |
| P09486 | Could the admin delete something with no reason recorded? | a bill delete must require a reason; a log cleanup must record its own row | ✅ | No. A bill delete requires a reason, and a log cleanup writes its own row saying how many entries went. |
| P09487 | Is anything here able to make a sale disappear rather than be cancelled? | no hard delete of a bill exists on any of these routes | ✅ | No. There is no hard delete of a bill anywhere on these 25; the database itself refuses one — which this sweep's own fixture ran into. |
| P09488 | Does the 90-day promise on a binned owner actually hold? | purge must be refused before the window and the countdown must be shown | ✅ | Yes — purge is refused with a 423 and the days left, and the bin shows the countdown. |
| P09489 | Would a stale bookmark or an old tab produce a raw database error on screen? | every id must be shape-checked before it reaches a uuid column | ✅ | No. Every id is shape-checked before it reaches a uuid column, or the lookup that follows answers a tidy 404. |
| P09490 | Does any admin action leave the restaurant's own staff able to see that the admin was there? | admin_enter_panel must be recorded on the `admin` panel only | ✅ | No — admin_enter_panel is written on the `admin` panel, which the restaurant's own feed does not show. |
| P09491 | Is the guest menu ever put at risk by a console action? | the maintenance flip must be scoped to the named restaurant and reversible | ✅ | No. The maintenance flip is scoped to the restaurant it names; this sweep flipped French House and Aangan stayed up, and the switch was put back in a finally block. |
| P09492 | Does the console refresh cost more egress than it needs to every 60 seconds? | aggregates through the snapshot cache; lists bounded; head counts for badges | ✅ | Aggregates ride the snapshot cache (analytics, customer tiles), badges are head counts, lists are bounded. The costly platform floor deliberately does not load until pressed. |
| P09493 | Can the admin tell a genuinely empty screen from a broken one? | an empty list must be worded as empty; an unreadable one must say unreadable | ✅ | Yes, after items 1, 3, 4 and 12 — each of those turned 'we couldn't look' into a screen that looked empty. |
| P09494 | Does every reason the screen has to branch on arrive as a CODE, not prose? | check each 4xx/409 the pages act on | ✅ | Yes on the paths the pages branch on: 409 for a name clash and an already-fixed error, 423 for a purge inside its window, 404 for a record that has gone. Item 7 added the missing one. |
| P09495 | Would a second admin working at the same moment overwrite the first one's change? | the writes here are per-row sets; note anywhere a clash guard would genuinely help | ✅ | Checked: only one admin exists, so a clash is a second tab, not a second person. So no fix; it goes to him as 🟡 item 2 instead, because adding clash guards here would be machinery for a case that cannot happen yet. |
| P09496 | Is any secret, key or password reachable through these 25 routes? | read every response field; the health route may name the Supabase host only | ✅ | No. System Health returns the Supabase HOST name only, parsed from the public URL, and no route echoes an env value. |
| P09497 | Does the phone layout of every one of these screens let the owner actually work? | the A35 screenshots must show no clipping and no sideways scroll | ✅ | Yes — all fourteen screens were opened at 360×780 dpr3 and none scrolls sideways; the screenshots were read, not just measured. |
| P09498 | Is anything here only correct because there are two restaurants today? | look for logic that assumes a small platform | ✅ | That was item 11's whole point: at two restaurants the hidden cap never bit. Every ceiling is explicit now, so it will not start lying at fifty. |
| P09499 | Does each of these routes still do exactly one job? | no route should have grown a second unrelated responsibility | ✅ | Yes. The one that does the most is bills (list + trail + delete/restore/credit note), and those are one screen's jobs. |
| P09500 | Is there anything in these 25 files the owner has already said no to? | cross-check docs/REJECTED-IDEAS.md before proposing any change | ✅ | Checked docs/REJECTED-IDEAS.md before every item. Nothing here reopens a rejected decision: no middleware, no shadcn, no kitchen profile, no chart-shape toggle, nothing that hides a sale. |
