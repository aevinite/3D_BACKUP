# T1 — GUEST MENU CORE · phases P00001–P00500

**Territory:** `app/menu/**` · `app/r/[restaurant]/menu/**` · `app/q/**` · `app/page.tsx` ·
`app/not-found.tsx` · `components/MenuView.tsx` · `FoodCard.tsx` · `NavPicker.tsx` · `Header.tsx` ·
`HeroTitle.tsx` · `GuestChrome.tsx` · `GuestNotFound.tsx` · `IntroSplash.tsx` · `ComingSoon.tsx`

**Branch:** `sweep6/t1-guest-menu-core` · **worktree:** `~/Documents/Projects/sweep6/T1` · **port:** 4101

## The budget these 500 phases were written to

| block | ids | share | what it is |
|-------|-----|-------|------------|
| A | P00001–P00200 | 40% | reading the code for correctness |
| B | P00201–P00300 | 20% | conformance to the project rules named in the prompt |
| C | P00301–P00375 | 15% | watching it run (headless Playwright, port 4101) |
| D | P00376–P00425 | 10% | screenshots actually opened and read (1280×800 + A35 360×780 dpr3) |
| E | P00426–P00475 | 10% | tracing a change across panels / doors |
| F | P00476–P00500 | 5% | judgment — does a real restaurant need it to work this way |

**Per-file floor.** Every owned file has at least one phase. The files named in the scope have ten
or more: MenuView 100, FoodCard 44, the three doors 62, Header 22, IntroSplash 18, GuestNotFound 16,
NavPicker 13, HeroTitle 12, GuestChrome 12, not-found 11, page.tsx 10, ComingSoon 4.

**Result key:** ✅ pass · ⏭ skipped, with a written reason.

**The result column is the SECOND full pass**, run from the top after every fix landed. Six rows
read ❌ on the FIRST pass; each says so in its note, names what was wrong, and is green here because
the fix is in this PR. The four ⏭ rows say plainly why they could not be exercised — none of them is
a pass in disguise.

First pass: 490 ✅ · 6 ❌ · 4 ⏭ (+1 row added mid-run, P00342b).
Second pass: **497 ✅ · 0 ❌ · 4 ⏭**.

---

## BLOCK A — correctness by reading the code (P00001–P00200)

### A1 · MenuView — saved browse state (P00001–P00020)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P00001 | `sk()` suffixes every browse-state key with the restaurant slug | read `components/MenuView.tsx`, assert `sk` is `${base}:${restaurantSlug \|\| DEFAULT_RESTAURANT_SLUG}` | ✅ | |
| P00002 | The layout preference is read scoped-first, global only as a last resort | assert the `??` chain order: local scoped → session scoped → local global → session global | ✅ | |
| P00003 | The layout preference is WRITTEN scoped only (never back to the global key) | grep the persist effect for `localStorage.setItem(sk("lfh_menu_layout")` and no bare global write | ✅ | |
| P00004 | Sort is restored from the scoped session key | assert `sessionStorage.getItem(sk("lfh_menu_sort"))` and `!== null` guard | ✅ | |
| P00005 | Diet filter is restored from the scoped session key | assert `sk("lfh_menu_diet")` read with a `!== null` guard | ✅ | |
| P00006 | Search text is restored from the scoped session key | assert `sk("lfh_menu_search")` read, truthy guard | ✅ | |
| P00007 | Chef's Special filter survives a Back | assert `sk("lfh_menu_chef") === "1"` restore | ✅ | |
| P00008 | Favourites filter survives a Back | assert `sk("lfh_menu_fav") === "1"` restore | ✅ | |
| P00009 | Folded categories are restored only if saved < 10 minutes ago | assert `Date.now() - parsed.ts <= 10*60*1000` | ✅ | |
| P00010 | A corrupted folded-categories blob cannot crash the restore | assert the whole restore sits in one `try {} catch {}` and the array is type-filtered | ✅ | |
| P00011 | The persist effect skips its first run so defaults can't clobber the restore | assert `restoredRef.current` early-return | ✅ | |
| P00012 | Every persisted key is scoped — no bare `lfh_menu_*` write survives | grep the persist effect: 0 `setItem("lfh_menu` without `sk(` | ✅ | |
| P00013 | The scroll position is saved under the scoped key | assert `sk("lfh_menu_scroll")` in the scroll handler | ✅ | |
| P00014 | The scroll restore runs exactly once per mount | assert `scrollRestored.current` guard set before the jump | ✅ | |
| P00015 | The scroll restore waits two frames so cards have laid out | assert nested `requestAnimationFrame` | ✅ | |
| P00016 | The active category is persisted under the scoped key | assert `sk("lfh_menu_cat")` | ✅ | |
| P00017 | Favourites are read through tenant-scoped storage, never raw localStorage | assert `tget("lfh-favorites")` and no `localStorage.getItem("lfh-favorites")` | ✅ | |
| P00018 | A corrupt favourites blob yields `[]` rather than a crash | assert `catch { setFavorites([]) }` and the `Array.isArray` guard | ✅ | |
| P00019 | Favourites re-read on both the same-tab event and the cross-tab `storage` event | assert both `addEventListener` calls and both removals in cleanup | ✅ | |
| P00020 | Restoring state cannot fire the persist effect in the same tick with defaults | reason the effect order: restore runs in mount effect, persist guard is `restoredRef` | ✅ | |

### A2 · MenuView — fetching the menu (P00021–P00040)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P00021 | An older `refreshMenu()` reply can never overwrite a newer one | assert `seq === menuReqRef.current` guards every `.then` that sets state | ✅ | |
| P00022 | The direct-read fallback also drops stale replies | assert the same `seq` guard inside `applyDirect()` | ✅ | |
| P00023 | A 404 from menu-data reloads instead of falling back to a direct DB read | assert `res.status === 404` → `window.location.reload()` then throw | ✅ | |
| P00024 | The 404 rethrow cannot also trigger `applyDirect()` | assert the catch returns early on a message containing `404` | ✅ | |
| P00025 | A genuine network failure DOES fall back, so a blip can't blank the menu | assert `applyDirect()` in the catch tail | ✅ | |
| P00026 | `loaded` is set true even when the menu comes back empty | assert `setLoaded(true)` on the success path regardless of item count | ✅ | |
| P00027 | `loaded` is set true on the direct-read error path too | assert `catch` sets `setLoaded(true)` | ✅ | |
| P00028 | The menu payload is handed to the offline layer without a second fetch | assert `warmDataCache(url, bundle)` with the SAME url string as the fetch | ✅ | |
| P00029 | The menu-data fetch is `cache: "no-store"` so the browser holds no stale copy | assert the fetch init | ✅ | |
| P00030 | A caller with no slug still gets a menu (legacy direct path) | assert `if (!restaurantSlug) { applyDirect(); return; }` | ✅ | |
| P00031 | The grid fallback requests only card columns, not the full row | assert `getMenuItems(restaurantId, CARD_COLUMNS)` | ✅ | |
| P00032 | The menu is fetched ONCE per load, not twice | assert `refreshMenu()` is called only from the `useRealtime` handler, and no mount effect repeats it | ✅ | |
| P00033 | The realtime hook is declared ABOVE the mount effect so the fetch still starts at the same moment | assert declaration order in the source | ✅ | |
| P00034 | Guests subscribe to the `menu` topic only, never the staff order stream | assert `useRealtime({ menu: … }, restaurantId)` and no other topic key | ✅ | |
| P00035 | Feature switches refresh on the same realtime nudge as the menu | assert `refreshFeatures(restaurantId)` in the handler | ✅ | |
| P00036 | The realtime subscription is scoped to this restaurant id | assert `restaurantId` is the second argument | ✅ | |
| P00037 | The skeleton only appears after 200ms, so a cached load doesn't flash it | assert the `setTimeout(…, 200)` and its `clearTimeout` cleanup | ✅ | |
| P00038 | "Loaded but empty" is distinguishable from "still loading" | assert the render branches on `loaded && menuData.length === 0` before the skeleton branch | ✅ | |
| P00039 | Category and item state are set from one bundle, so they cannot disagree | assert both come from the same `.then(bundle)` | ✅ | |
| P00040 | Re-setting menu state cannot jump the scroll (cards are keyed by id) | assert `key={item.id}` on every FoodCard | ✅ | |

### A3 · MenuView — the QR table number (P00041–P00050)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P00041 | A `/q/<code>` page's server-resolved table wins over anything in the URL | assert `qrTable \|\| (() => …)` short-circuit order | ✅ | |
| P00042 | `?table=N` is read from the address | assert `params.get("table")` | ✅ | |
| P00043 | `?t=N` is accepted as an alias | assert `\|\| params.get("t")` | ✅ | |
| P00044 | Non-digits are stripped from the table value | assert `.replace(/\D/g, "")` | ✅ | |
| P00045 | An empty/garbage table value is ignored rather than saved as "" | assert `if (digits)` guard | ✅ | |
| P00046 | The table is written through `setScannedTable`, not raw storage | assert the import and the call | ✅ | |
| P00047 | The rest of the app is told a table was scanned | assert `lfh:table-scanned` dispatch | ✅ | |
| P00048 | The whole capture is wrapped so a storage refusal can't blank the menu | assert the enclosing `try {} catch {}` | ✅ | |
| P00049 | Reading `window.location` avoids needing a Suspense boundary for search params | assert no `useSearchParams` import in MenuView | ✅ | |
| P00050 | The effect re-runs if `qrTable` changes rather than capturing once for ever | assert the `[qrTable]` dependency | ✅ | |

### A4 · MenuView — category chips, scroll-spy, the bell (P00051–P00080)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P00051 | Tapping a category never narrows the grid, it only scrolls | assert `setCurrentCategory("all")` inside `scrollToCategory` | ✅ | |
| P00052 | Tapping a folded category unfolds it first | assert `setClosedCats(cur => cur.filter(...))` | ✅ | |
| P00053 | The filter-clearing recovery is capped so it cannot loop for ever | assert `retry < 3` | ✅ | |
| P00054 | The shrink-correction loop is capped | assert `tries >= 8` early return | ✅ | |
| P00055 | The scroll-spy is locked briefly after a tap so it can't repaint another chip | assert `spyLockUntil.current = Date.now() + 1300` and the read in `computeSpy` | ✅ | |
| P00056 | The header line the spy measures against is read LIVE, not hardcoded | assert `getBoundingClientRect().bottom` on `#menu-sticky` | ✅ | |
| P00057 | The final (short) category still gets the highlight at the very bottom | assert the `scrollTop + clientHeight >= scrollHeight - 40` branch | ✅ | |
| P00058 | The active chip is centred inside its own bar, never scrolling the page | assert the maths is relative to `bar.scrollLeft` and uses `bar.scrollTo` | ✅ | |
| P00059 | Sort toggles back to "recommended" when the active sort is re-tapped | assert `cur === slug ? "" : slug` | ✅ | |
| P00060 | Veg and Non-Veg are mutually exclusive and each re-taps to off | assert `toggleDiet` identical shape | ✅ | |
| P00061 | The "slide →" hint appears only when the chip row genuinely overflows | assert `catOverflows` gates the hint and is measured from `scrollWidth - clientWidth` | ✅ | |
| P00062 | The overflow measurement re-runs on resize AND orientation change | assert both listeners and the ResizeObserver | ✅ | |
| P00063 | The chip row's scroll-fade flag is measured, not guessed | assert `row.scrollWidth - row.clientWidth - row.scrollLeft > 4` | ✅ | |
| P00064 | The chip-row observer watches the children too, so a language switch re-measures | assert `for (const c of Array.from(row.children)) ro.observe(c)` | ✅ | |
| P00065 | The chip-row effect removes its listeners and disconnects the observer | assert the cleanup returns all three | ✅ | |
| P00066 | The search-dropdown scroll cue is measured the same way | assert `box.scrollHeight - box.clientHeight - box.scrollTop > 4` | ✅ | |
| P00067 | The search-dropdown effect cleans up its listener and observer | assert the cleanup | ✅ | |
| P00068 | Neither scroll-cue effect names a `const` declared later in the component | assert deps are `searchQuery`/`features.*`, never `q` or `searchResults` | ✅ | |
| P00069 | The waiter bell is settled by rectangle intersection, not a centre probe | assert `hits()` compares four edges and there is no `elementFromPoint` | ✅ | |
| P00070 | The bell scan only measures controls within its possible travel band | assert the `band` filter using `BELL_MAX_LIFT + BELL_BAND_PAD` both ways | ✅ | |
| P00071 | The bell never treats itself as an obstruction | assert `el.closest(".chef-call")` skip | ✅ | |
| P00072 | The whole-card link is excluded, so the bell doesn't dodge scenery | assert `isCardLink` skip | ✅ | |
| P00073 | A bell already sitting clear is left alone (no hopping) | assert the early `if (!hits(...)) return` before any lift is computed | ✅ | |
| P00074 | The lift search steps upward and takes the FIRST clean spot | assert the `for (let want = 0; want <= BELL_MAX_LIFT; want += STEP)` scan | ✅ | |
| P00075 | With nowhere clean the bell returns to its designed corner | assert the trailing `removeProperty("--bell-lift")` | ✅ | |
| P00076 | Leaving the menu never strands the bell lifted or the body marked | assert the cleanup removes `--bell-lift`, `menu-frost`, `menu-scrolling` | ✅ | |
| P00077 | The 600ms tick does no layout work while the tab is hidden | assert `if (document.hidden) return` | ✅ | |
| P00078 | The scroll handler coalesces to one frame | assert `cancelAnimationFrame(raf)` then `raf = requestAnimationFrame(...)` | ✅ | |
| P00079 | The shrink is measured off the NON-sticky header so it cannot oscillate | assert `.section-header` is the reference, not `#menu-sticky` | ✅ | |
| P00080 | The frost panel height is driven from the live sticky rect | assert `--headfrost-h` set from `sRect.bottom` | ✅ | |

### A5 · MenuView — search, filters, grouping (P00081–P00100)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P00081 | Search folds accents so "caffe" finds "Caffè" | assert `normalize("NFD")` + combining-mark strip in `fold` | ✅ | |
| P00082 | Search matches dish name, category slug, translated category name and alias | assert all four `\|\|` arms in `matchesSearch` | ✅ | |
| P00083 | Both sides of every comparison are folded | assert `fold()` wraps each arm and `q` is folded once | ✅ | |
| P00084 | The search box caps input length so a paste can't wreck the layout | assert `maxLength={60}` | ✅ | |
| P00085 | A guest's typed text is never treated as a replacement pattern | assert `split("{q}").join(...)`, no `String.replace` with user text | ✅ | |
| P00086 | The suggestions honour the same active filters as the grid | assert chef/fav/diet arms in the `searchResults` filter | ✅ | |
| P00087 | Suggestions rank name-starts-with first, then rating | assert the `aStarts - bStarts \|\| ratingOf(b) - ratingOf(a)` comparator | ✅ | |
| P00088 | Suggestions are capped at 8 | assert `.slice(0, 8)` | ✅ | |
| P00089 | Suggestion prices use the same formatter as the cards | assert `formatPrice(r.price, searchCurrency \|\| DEFAULT_CURRENCY)` | ✅ | |
| P00090 | The suggestion currency follows the `lfh:currency-changed` broadcast | assert the listener and its removal | ✅ | |
| P00091 | A stale "favourites on" stops filtering when the feature is switched off | assert `favActive = favOnly && features.favorites !== false` | ✅ | |
| P00092 | A stale "veg" filter stops filtering when the chips are hidden | assert `dietActive = dietShown ? currentDiet : ""` | ✅ | |
| P00093 | A stale "chef's special" filter stops filtering when its chip is off | assert `chefActive` reads `chip_chef-special` | ✅ | |
| P00094 | Veg/non-veg chips vanish on a single-sided menu | assert `dietMeaningful` derives from `menuData.every(...)` both ways | ✅ | |
| P00095 | The diet chips can't flicker away before the menu has loaded | assert `menuData.length > 0` guard inside `dietMeaningful` | ✅ | |
| P00096 | The sort works on a copy, so "recommended" is still the DB order | assert `[...visibleItems].sort(...)` | ✅ | |
| P00097 | Grouped view only builds in the "all" view with no query | assert `currentCategory === "all" && !q` | ✅ | |
| P00098 | Empty groups are dropped from the grouped view | assert `.filter(g => g.items.length > 0)` | ✅ | |
| P00099 | The chip row lists only categories with dishes under the current filter | assert `nonEmptyCatSlugs` built from `filteredItems` | ✅ | |
| P00100 | A category's own colour picks readable ink by WCAG luminance | assert `inkOn` uses the 0.2126/0.7152/0.0722 weights and a threshold | ✅ | |

### A6 · FoodCard — the dish tile (P00101–P00144)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P00101 | The cart is read through tenant-scoped storage | assert `tget(CART_KEY)` and no raw `localStorage.getItem("lfh_cart")` | ✅ | |
| P00102 | The cart is written through tenant-scoped storage | assert `tset(CART_KEY, …)` | ✅ | |
| P00103 | A corrupt cart blob yields `[]` rather than a crash | assert `Array.isArray(parsed) ? parsed : []` inside a try/catch | ✅ | |
| P00104 | Writing the cart announces the change to the rest of the app | assert `lfh:cart-updated` dispatch | ✅ | |
| P00105 | The card's "+" only ever touches the PLAIN line of a dish | assert `isPlainLine` matches `!sig` or `"[]"` and is used in both find calls | ✅ | |
| P00106 | Quantity is clamped to 99 to match the server's own cap | assert `Math.min(99, rawQty)` | ✅ | |
| P00107 | Hitting the cap tells the guest instead of silently doing nothing | assert the toast on `rawQty > 99` | ✅ | |
| P00108 | Dropping to zero removes the line rather than storing `qty: 0` | assert the `newQty <= 0` filter branch | ✅ | |
| P00109 | A new line stores the confident rounded unit price | assert `prettyUsd(item.price).toFixed(2)` | ✅ | |
| P00110 | The on-card counter can never render a negative number | assert `Math.max(0, newQty)` | ✅ | |
| P00111 | Adding goes through the table gate; removing does not | assert `delta > 0` routes via `gateAddToCart` and `delta < 0` calls `applyQty` directly | ✅ | |
| P00112 | Customising also goes through the table gate | assert `gateAddToCart` wraps the `openCustomize` body | ✅ | |
| P00113 | Add/remove taps never also open the dish page | assert `preventDefault()` + `stopPropagation()` in `updateQty` and `openCustomize` | ✅ | |
| P00114 | The stepper's wrapper swallows the card link too | assert the `cart-qty-row` `onClick` guard | ✅ | |
| P00115 | The 4D badge, cube icon and card styling follow ONE derived test | assert `has3d` is used for all three and no bare `item.is4d &&` gate remains | ✅ | |
| P00116 | `has3d` requires the switch AND both model files | assert all four conjuncts | ✅ | |
| P00117 | A dish with no photo starts in the error state (an empty src never fires onError) | assert `useState(!item.image)` | ✅ | |
| P00118 | A broken photo settles the card instead of shimmering for ever | assert `onError` sets both `imgLoaded` and `imgError` | ✅ | |
| P00119 | A broken photo hides the `<img>` so no broken glyph shows | assert the `visibility: "hidden"` style | ✅ | |
| P00120 | A broken photo fills the thumb with a neutral tint | assert the `imgError` background style on `.thumb-wrapper` | ✅ | |
| P00121 | Dish photos use a plain `<img>` (URLs are DB-driven, any host) | assert no `next/image` import | ✅ | |
| P00122 | Photos are lazy and async-decoded | assert `loading="lazy"` and `decoding="async"` | ✅ | |
| P00123 | The fade-in stagger is capped so a long category's last card isn't delayed | assert `Math.min(index, 10) * 0.06` | ✅ | |
| P00124 | No prep time is invented when the dish has none | assert `features.prep_time ? (item.time \|\| "") : ""` and no `"25-30 min"` literal | ✅ | |
| P00125 | No star rating is invented when there are no reviews | assert the `item.reviewCount > 0` branch and `t.noRatingsYet` otherwise | ✅ | |
| P00126 | With ratings switched off the slot says nothing at all | assert the `features.ratings ? … : ""` arm | ✅ | |
| P00127 | The meta row is joined from a list so a missing half leaves no lonely bullet | assert `.filter(Boolean).join(" • ")` | ✅ | |
| P00128 | The sold-out pill is translated, not an English literal | assert `t.notAvailable` | ✅ | |
| P00129 | The veg mark follows MenuView's whole-menu opinion when given one | assert `showDiet ?? !!features.diet_filter` | ✅ | |
| P00130 | A caller that predates `showDiet` keeps today's behaviour | assert the `??` (not `\|\|`) so `false` is honoured and `undefined` falls back | ✅ | |
| P00131 | The card reads THIS restaurant's switches, not the default one's | assert `useFeatures(restaurantId)` | ✅ | |
| P00132 | Dish links stay inside the restaurant when a slug is present | assert `base = restaurantSlug ? "/r/"+slug : ""` | ✅ | |
| P00133 | The viewing category rides along so Back keeps the filter | assert the `?cat=` suffix | ✅ | |
| P00134 | Dishes with option groups open Customize rather than quick-adding | assert `hasOptions` picks the customize button | ✅ | |
| P00135 | The cart quantity re-syncs when the cart changes anywhere | assert the `lfh:cart-updated` listener | ✅ | |
| P00136 | The price re-formats when the currency changes | assert the `lfh:currency-changed` listener | ✅ | |
| P00137 | Both listeners are removed when the card unmounts | assert the cleanup | ✅ | |
| P00138 | The long-name box shrinks the name instead of truncating it | assert `useFitText(item.title)` on the name node | ✅ | |
| P00139 | A missing `tags` list is treated as empty rather than throwing | assert `(item.tags \|\| []).includes("sold-out")` | ✅ | |
| P00140 | A missing `options` list is treated as none | assert `(item.options?.length ?? 0) > 0` | ✅ | |
| P00141 | The add toast is tappable and opens the bill | assert the `event: "lfh:open-cart"` detail | ✅ | |
| P00142 | The photo pops only on add, never on remove | assert `if (delta > 0) popThumb()` | ✅ | |
| P00143 | Tapping the "Not available" pill does something rather than silently nothing | read the sold-out branch; it must not `preventDefault` into a dead end | ✅ | ❌ on pass 1 — **fixed this PR** — the pill swallowed the tap and gave the diner nothing back |
| P00144 | The sold-out card is still openable everywhere else on the tile | assert the wrapping `Link` is not disabled for sold-out | ✅ | |

### A7 · the three guest doors (P00145–P00186)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P00145 | `/menu` preserves EVERY query parameter when it redirects | read `app/menu/page.tsx`, assert the full `searchParams` loop | ✅ | |
| P00146 | `/menu` preserves repeated parameters (array values) | assert the `Array.isArray(v)` branch appends each | ✅ | |
| P00147 | `/menu` with no query redirects without a stray `?` | assert `s ? "?"+s : ""` | ✅ | |
| P00148 | `/menu` targets the default restaurant slug from one constant | assert `DEFAULT_RESTAURANT_SLUG` import, no literal | ✅ | |
| P00149 | `/menu?table=5` therefore still reaches the menu with its table | trace the redirect target string | ✅ | |
| P00150 | `/menu` drops nothing when a value is `undefined` | assert `if (v == null) continue` | ✅ | |
| P00151 | `/r/<slug>/menu` 404s an unknown restaurant | assert `if (!r \|\| !r.active) notFound()` | ✅ | |
| P00152 | `/r/<slug>/menu` 404s a DEACTIVATED restaurant | assert the `!r.active` arm of the same test | ✅ | |
| P00153 | `/r/<slug>/menu` 404s when the Menu master switch is off | assert `if (!settings.menuEnabled) notFound()` | ✅ | |
| P00154 | The RESOLVED slug is passed to MenuView, never the address-bar text | assert `restaurantSlug={r.slug}` | ✅ | |
| P00155 | A capitalised link therefore shares one cart with the lower-case one | trace `r.slug` into `sk()` and `tenantStorage` | ✅ | |
| P00156 | The dark default is stamped before the menu paints | assert the inline script is rendered ABOVE `<MenuView>` | ✅ | |
| P00157 | A guest who has chosen a mode keeps it | assert `if(!localStorage.getItem('lfh_theme'))` guard | ✅ | |
| P00158 | The dark-default script cannot throw on a storage refusal | assert the inner `try{}catch(e){}` | ✅ | |
| P00159 | The restaurant's default layout reaches MenuView | assert `defaultLayout={settings.menuDefaultLayout === "list" ? "list" : "gallery"}` | ✅ | |
| P00160 | Every branding prop is passed through with a null-to-undefined guard | assert `?? undefined` on name/logoText/heroTitle/tagline/accent/theme/logoUrl | ✅ | |
| P00161 | The browser tab shows the RESTAURANT, not the platform | assert `generateMetadata` builds the title from `r.name` | ✅ | |
| P00162 | A shared link previews the restaurant's own description | assert the tagline-aware description | ✅ | |
| P00163 | A restaurant with a logo gets its own tab icon | assert `icons` from `r.logoUrl` | ✅ | |
| P00164 | A shared link previews the restaurant's own image | assert the OpenGraph `images` array | ✅ | |
| P00165 | A menu that is NOT serving must not preview as an open menu | assert `generateMetadata` checks `active` + `menuEnabled` before advertising | ✅ | ❌ on pass 1 — **fixed this PR** — folded in from `origin/fix/guest-experience-t1`, judged correct |
| P00166 | That extra metadata check costs no extra database read | assert both `getRestaurantBySlug` and `getSettings` are the cached helpers the page already calls | ✅ | |
| P00167 | `/q/<code>` rejects a malformed code before touching the database | assert the `/^[A-Z0-9]{6,16}$/` test precedes the query | ✅ | |
| P00168 | `/q/<code>` folds case and trims the code | assert `.trim().toUpperCase()` | ✅ | |
| P00169 | `/q/<code>` resolves through ONE cached lookup per request | assert the React `cache()` wrapper around `resolveCode` | ✅ | |
| P00170 | `/q/<code>` honours the Menu master switch | assert `if (!settings.menuEnabled) return null` | ✅ | |
| P00171 | `/q/<code>` honours the active flag | assert `if (!r \|\| !r.active) return null` | ✅ | |
| P00172 | A dead code gets the friendly page, not a bare 404 | assert the `if (!hit)` render branch | ✅ | |
| P00173 | A dead code's metadata does not advertise the restaurant | assert the neutral title/description for `!hit` | ✅ | |
| P00174 | The table number never appears in the address bar on `/q` | assert there is no redirect to `?table=` | ✅ | |
| P00175 | `/q` pins the tab's tenant before hydration | assert the `lfh_tab_tenant` sessionStorage script | ✅ | |
| P00176 | The tenant-pin script is JSON-escaped, so an odd slug can't break it | assert `JSON.stringify(hit.slug)` | ✅ | |
| P00177 | `/q` applies the same dark default as the tenant door | assert the identical inline script | ✅ | |
| P00178 | `/q` applies the restaurant's default layout | assert `defaultLayout` is passed | ✅ | |
| P00179 | `/q` passes the resolved table to MenuView | assert `qrTable={String(hit.table)}` | ✅ | |
| P00180 | `/q` white-labels its metadata exactly like the tenant door | diff the two `generateMetadata` bodies for the same title/description/OG shape | ✅ | |
| P00181 | `/q` is force-dynamic so a code can't be cached across restaurants | assert `export const dynamic = "force-dynamic"` | ✅ | |
| P00182 | The `/q` code lookup selects only the two columns it needs | assert `.select("restaurant_id, table_number")` | ✅ | |
| P00183 | The `/q` restaurant lookup selects only the slug | assert `.select("slug")` | ✅ | |
| P00184 | Both `/q` lookups are single-row reads on an indexed unique column | assert `.eq("code", code).maybeSingle()` | ✅ | |
| P00185 | All three doors render the SAME `MenuView` component | assert the import in each page | ✅ | |
| P00186 | All three doors resolve through the one resolver in `lib/tenant.ts` | assert `getRestaurantBySlug` is the only slug→row path | ✅ | |

### A8 · the remaining owned components (P00187–P00200)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P00187 | `HeroTitle` splits text by grapheme, never by code unit | assert `splitGraphemes`, and no `.split("")` | ✅ | |
| P00188 | `HeroTitle` respects "reduce motion" | assert the `matchMedia` guard returns before animating | ✅ | |
| P00189 | `HeroTitle` cancels its frame and both listeners on unmount | assert the cleanup | ✅ | |
| P00190 | `IntroSplash` scopes its "already seen" key per restaurant | assert `"lfh_intro_seen:" + (scopeKey \|\| wordmark \|\| "default")` | ✅ | |
| P00191 | `IntroSplash` never animates a logo element that isn't there | assert the `querySelector(".intro-logo")` guard | ✅ | |
| P00192 | `IntroSplash` can never stay stuck over the menu | assert the 2.3s timer plus `visibilitychange` and `pageshow` finishes | ✅ | |
| P00193 | `IntroSplash` shows the flagship logo ONLY on the flagship | assert `isDefault && <img className="intro-logo"` | ✅ | |
| P00194 | `GuestChrome` renders nothing on any staff route, flat or per-restaurant | assert the `STAFF_RE` covers `/seg` and `/r/<slug>/seg` | ✅ | |
| P00195 | `GuestChrome` loads every guest widget lazily so panels don't download it | assert every import is `dynamic(..., { ssr: false })` | ✅ | |
| P00196 | `GuestNotFound` never links a diner to the staff sign-in | assert no `href="/"` | ✅ | |
| P00197 | `GuestNotFound` only offers "back to the menu" once it knows the menu answers | assert the `menuLive === true` gate | ✅ | |
| P00198 | `GuestNotFound` uses the app's own font and theme tokens | assert `var(--bg)`, `var(--text)`, `'Inter'` | ✅ | |
| P00199 | `app/page.tsx` sends the bare domain to the staff sign-in, not a guest menu | assert `redirect("/login")` | ✅ | |
| P00200 | `ComingSoon` renders no list block when given no points | assert `points && points.length > 0` | ✅ | |

---

## BLOCK B — conformance to the project's own rules (P00201–P00300)

### B1 · the three-doors rule (P00201–P00215)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P00201 | The Menu master switch is honoured on all three doors | grep `menuEnabled` in the two page files + trace `/menu`'s redirect target | ✅ | |
| P00202 | The dark default is applied on all three doors | `/menu` inherits via redirect; assert the script on the other two | ✅ | |
| P00203 | The default layout is applied on all three doors | assert `defaultLayout` on both page files | ✅ | |
| P00204 | White-label metadata is present on all three doors | assert `generateMetadata` on both, and the redirect for `/menu` | ✅ | |
| P00205 | An inactive restaurant is refused on all three doors | assert `r.active` on both, redirect for the third | ✅ | |
| P00206 | A not-serving menu previews neutrally on all three doors | assert the metadata guard on both page files | ✅ | ❌ on pass 1 — **fixed this PR** — the tenant door advertised a menu it would then refuse |
| P00207 | The guest 404 boundary is guest-branded for the tenant menu route | assert `app/r/[restaurant]/menu/not-found.tsx` renders `GuestNotFound` | ✅ | |
| P00208 | `/q` handles its own dead end rather than falling through to the platform 404 | assert the in-page friendly branch | ✅ | |
| P00209 | The tenant is pinned for the one door whose path has no slug | assert the `/q` `lfh_tab_tenant` script | ✅ | |
| P00210 | No door hardcodes a restaurant id | grep both page files for a UUID literal | ✅ | |
| P00211 | No door hardcodes the default slug except the legacy redirect | grep for `"french-house"` string literals | ✅ | |
| P00212 | Every door passes `restaurantId` down to `MenuView` | assert the prop on both | ✅ | |
| P00213 | Every door passes a slug that namespaces storage | assert `restaurantSlug` on both | ✅ | |
| P00214 | The QR encodes the slug in the PATH and the table as a query — no `/t/` segment | grep the whole `app/` tree for a `t` route segment | ✅ | |
| P00215 | A capitalised `/r/French-House/menu` resolves to the same restaurant | assert `toLowerCase()` in `getRestaurantBySlug` | ✅ | |

### B2 · every restaurant is genuinely different (P00216–P00235)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P00216 | Restaurant #1's hero copy is used only for restaurant #1 | assert `isDefault ? t.greeting` / `isDefault ? t.heroTitle` | ✅ | |
| P00217 | A non-#1 restaurant with no custom hero gets a neutral line, not #1's | assert the `tagline \|\| "Welcome"` / `heroTitle \|\| "Our Menu"` fallbacks | ✅ | |
| P00218 | The flagship search-bar logo is shown only on the flagship | assert `isDefault ?` before `/lfh-logo.png` | ✅ | |
| P00219 | Another tenant with a logo sees ITS logo by the search bar | assert the `logoUrl` branch | ✅ | |
| P00220 | Another tenant with no logo sees a neutral accent-tinted glyph | assert the inline SVG fallback | ✅ | |
| P00221 | The flagship wordmark is hardcoded ONLY in the no-logoText branch of Header | assert the `little / French / house` spans sit behind `logoText ?` | ✅ | |
| P00222 | `IntroSplash` shows no French House logo to another tenant | assert the `isDefault &&` guard on the fallback image | ✅ | |
| P00223 | `IntroSplash`'s "seen" key cannot skip restaurant B's splash after A's | assert the per-restaurant key | ✅ | |
| P00224 | The splash background is tinted from THIS restaurant's accent | assert `splashStyle` uses `accentColor` | ✅ | |
| P00225 | The splash ring is tinted from THIS restaurant's accent | assert the `borderColor` style | ✅ | |
| P00226 | `AppShell` receives the tenant's brand props, undefined for #1 | assert the `isDefault ? undefined :` pattern on all four | ✅ | |
| P00227 | Category colours come from the DB, never a literal hex default | assert `c.color \|\| null`, and no `"#d4a574"` literal | ✅ | |
| P00228 | Categories are DB-driven, not a hardcoded list | assert `dbCategories` is the only source of `categories` | ✅ | |
| P00229 | Sorts and diets are the only hardcoded chip lists, and they are generic | read `SORTS` / `DIETS` — no restaurant-specific slug | ✅ | |
| P00230 | Language and currency options come from this restaurant's settings | assert `menuLangs` / `menuCurrs` filter the lists in Header | ✅ | |
| P00231 | A switcher with only one real option is REMOVED, not disabled | assert `currencyOptions.length > 1` / `languageOptions.length > 1` | ✅ | |
| P00232 | A guest carrying a language this restaurant doesn't offer is moved to one it does | assert the `!menuLangs.includes(...)` correction effect | ✅ | |
| P00233 | With the currency switcher off, the restaurant default is forced back | assert `setCurrency("INR")` when `features.currency === false` | ✅ | |
| P00234 | Header re-reads scoped storage on a soft restaurant switch | assert `[restaurantId]` on the mount effect | ✅ | |
| P00235 | Nothing in the territory reads a restaurant name from a literal | grep all owned files for `Little French House` outside the flagship branch | ✅ | |

### B3 · back button, taps, offline, egress (P00236–P00270)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P00236 | `NavPicker` registers with the back-button manager | assert `useBackClose(...)` | ✅ | |
| P00237 | The two pickers register under DIFFERENT ids | assert the id is keyed off `buttonLabel` | ✅ | |
| P00238 | `NavPicker` only registers while it is actually open | assert `open` is the second argument | ✅ | |
| P00239 | No owned file hand-rolls `pushState`/`popstate` | grep all 14 owned files | ✅ | |
| P00240 | `NavPicker` also closes on an outside click | assert the `mousedown` document listener | ✅ | |
| P00241 | `NavPicker` also closes on Escape | assert the `keydown` listener | ✅ | |
| P00242 | `NavPicker` removes both listeners when it closes | assert the cleanup | ✅ | |
| P00243 | `NavPicker` adds no listeners while closed | assert the `if (!open) return` before them | ✅ | |
| P00244 | No guest action in the territory ends in a silent `return` | read every `onClick` in the owned files for a do-nothing path | ✅ | ❌ on pass 1 — **fixed this PR** — the sold-out pill (see P00143) |
| P00245 | The category tap always resolves — highlight, scroll, or a bounded give-up | trace `scrollToCategory` to each exit | ✅ | |
| P00246 | The "+" tap either adds, opens the gate, or says why it stopped | trace `updateQty` to each exit | ✅ | |
| P00247 | The theme toggle writes, re-skins and broadcasts on every tap | assert all three in `toggleTheme` | ✅ | |
| P00248 | The cart button always broadcasts, even with an empty cart | assert the unconditional dispatch | ✅ | |
| P00249 | Every menu read names its columns | assert `CARD_COLUMNS` on the fallback and a server bundle otherwise | ✅ | |
| P00250 | The menu read is scoped by restaurant | assert `getMenuItems(restaurantId, …)` and the per-slug endpoint | ✅ | |
| P00251 | The guest menu is fetched from the cached endpoint, not straight from the DB | assert `/api/r/<slug>/menu-data` is the primary path | ✅ | |
| P00252 | No poll in the territory runs faster than the 60s backstop | grep every `setInterval` in owned files; the only one is the 600ms LAYOUT tick, which reads no data | ✅ | |
| P00253 | That 600ms tick does no network work at all | read the tick body: `computeSpy` + `settleBell`, both pure layout | ✅ | |
| P00254 | The menu payload is handed to the service worker rather than re-fetched | assert `warmDataCache` | ✅ | |
| P00255 | `GuestNotFound`'s probe is a single scoped request, not a loop | assert one `fetch` in a `[slug]` effect with an `alive` guard | ✅ | |
| P00256 | `GuestNotFound` cancels its state update if the guest leaves first | assert the `alive = false` cleanup | ✅ | |
| P00257 | `GuestNotFound` encodes the slug into both the probe and the link | assert `encodeURIComponent` in both places | ✅ | |
| P00258 | Header's settings read is the same de-duplicated one `useFeatures` makes | assert `getSettings(restaurantId)` | ✅ | |
| P00259 | Header cancels that read on unmount | assert the `alive` guard | ✅ | |
| P00260 | Realtime is keyed per restaurant, never global | assert the `restaurantId` argument | ✅ | |
| P00261 | No owned file writes to `settings` or adds a column | grep for any settings write | ✅ | |
| P00262 | No owned file introduces a migration | `git diff --name-only` shows nothing under `supabase/` | ✅ | |
| P00263 | No owned file reads a service-role key | grep for `SERVICE_ROLE` in client components | ✅ | |
| P00264 | `/q`'s admin-client reads happen on the SERVER only | assert the file has no `"use client"` | ✅ | |
| P00265 | `supabaseAdmin` is never imported into a client component in the territory | grep the owned `"use client"` files | ✅ | |
| P00266 | The offline layer sees the menu on a FIRST visit, before the worker controls the page | assert `warmDataCache` runs on the success path, not behind a controller check | ✅ | |
| P00267 | The menu-data path family is one the service worker knows | cross-check the URL shape against `public/sw.js` `DATA_PATHS` | ✅ | |
| P00268 | No owned file bypasses `tenantStorage` for a per-restaurant key | grep for raw `localStorage` with a tenant-scoped key name | ✅ | |
| P00269 | `lfh_theme` is deliberately device-wide, not tenant-scoped | assert Header writes it raw, and that this is the documented guest key | ✅ | |
| P00270 | The layout preference is tenant-scoped, per the owner's I11 rule | assert `sk("lfh_menu_layout")` | ✅ | |

### B4 · rejections, deliberate designs, guards (P00271–P00300)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P00271 | R15 is respected — the 99-cap and add toasts stay English | assert the literals are still there with their REJECTED comment | ✅ | |
| P00272 | R15's code comment still sits on the exact line | assert the `REJECTED (owner, 2026-08-12)` comment above the toast | ✅ | |
| P00273 | R23 is respected — no guest-menu translation work is raised | check nothing in this run's findings/improvements asks for it | ✅ | the English hero fallback and the English aria-labels were both dropped on R23 |
| P00274 | R11/R14 are respected — nothing auto-translates a dish name | grep the territory for any translation call on `item.title` | ✅ | |
| P00275 | Dark-by-default on a tenant menu is not reported as a fault | confirm it appears nowhere in the findings | ✅ | |
| P00276 | The guest menu's refusal to be embedded is left alone | confirm nothing in the diff touches framing | ✅ | |
| P00277 | CSP stays report-only | confirm nothing in the diff touches CSP | ✅ | |
| P00278 | No `REJECTED` comment is added without its row in the doc | run `npm run verify:rejected` | ✅ | R28's comment was deliberately NOT added — the doc row is a handoff |
| P00279 | The kitchen-has-no-profile rule is untouched | confirm the diff contains no kitchen file | ✅ | |
| P00280 | Nothing in the diff changes a rule written in CLAUDE.md | re-read the diff against the rule list | ✅ | |
| P00281 | `npm run verify:guest` passes (static half) | run it | ✅ | |
| P00282 | `npm run verify:guest` passes (live half against port 4101) | run it with `--base` | ✅ | |
| P00283 | `npm run verify:guest-read` passes | run it | ✅ | |
| P00284 | `npm run verify:menu-parts` passes | run it | ✅ | |
| P00285 | `npm run verify:ui` passes | run it | ✅ | |
| P00286 | `npm run verify:rejected` passes | run it | ✅ | |
| P00287 | `npm run typecheck` passes | run it | ✅ | |
| P00288 | `npm run lint` passes | run it | ✅ | |
| P00289 | `verify:everything` was NOT run (it belongs to the merge terminal) | confirm no invocation in this run | ⏭ | deliberately not run — pid-locked, owned by the merge terminal |
| P00290 | The 4D-badge guard still holds after the FoodCard edit | run `verify:guest`, check the `has3d` row | ✅ | |
| P00291 | The brand-leak guard still holds after the MenuView edit | run `verify:guest`, check the `isDefault ? t.greeting` row | ✅ | |
| P00292 | The back-button guard still holds | run `verify:guest`, check the overlay row | ✅ | |
| P00293 | The tenant-scoped-storage guard still holds | run `verify:guest`, check the storage row | ✅ | |
| P00294 | A NEW guard covers the not-serving metadata fix | assert the new check exists in `scripts/verify-guest.mjs` and fails on the old code | ✅ | added this PR |
| P00294b | A NEW guard stops anyone "fixing" the bell again | temporarily add a `visibility` line inside `settleBell()` and confirm `verify:guest` fails | ✅ | proved — the guard caught it, then the file was restored |
| P00295 | A NEW guard covers the sold-out pill fix | assert the new check exists and fails on the old code | ✅ | added this PR |
| P00296 | A NEW guard covers the suggestions-list screen-reader fix | assert the new check exists and fails on the old code | ✅ | added this PR |
| P00297 | Each new guard is static (no server, no DB, no login) | read the added checks | ✅ | |
| P00298 | Each new guard strips comments first, so prose can't satisfy it | assert they use the file's `code()` helper | ✅ | |
| P00299 | The ledger, findings and improvements files are committed with the PR | `git status` after staging | ✅ | |
| P00300 | Nothing outside the territory was written to | `git diff --name-only` against the territory list | ✅ | plus `scripts/verify-guest.mjs`, which §7 of the rules requires |

---

## BLOCK C — watching it run, headless on port 4101 (P00301–P00375)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P00301 | Port 4101 is mine before any screenshot is trusted | fetch `/api/health` (or the menu) on 4101 and confirm it is this worktree's build | ✅ | |
| P00302 | `/r/french-house/menu` answers 200 | headless goto, read the status | ✅ | |
| P00303 | `/r/aangan-garden-restaurant/menu` answers 200 | headless goto, read the status | ✅ | |
| P00304 | `/menu` redirects to `/r/french-house/menu` | headless goto, read the final URL | ✅ | |
| P00305 | `/menu?table=7` lands on the tenant menu with the table preserved | headless goto, read the final URL for `table=7` | ✅ | |
| P00306 | `/menu?table=7` actually stores the scanned table | read `lfh_table`-family storage after load | ✅ | |
| P00307 | `/menu?t=7` is accepted the same way | headless goto, assert the stored table is 7 | ✅ | |
| P00308 | `/r/French-House/menu` (capitalised) renders the menu, not a 404 | headless goto, read the status and card count | ✅ | |
| P00309 | The capitalised link uses the SAME storage scope as the lower-case one | compare the `lfh_menu_layout:` key suffix on both loads | ✅ | |
| P00310 | The capitalised link still captures `?table=` | goto `/r/French-House/menu?table=4`, assert the stored table | ✅ | |
| P00311 | A guest's cart added on the lower-case link is visible on the capitalised one | add via storage under the resolved scope, reload the other casing, read the badge | ✅ | |
| P00312 | An unknown slug answers 404 | goto `/r/zz-no-such-restaurant/menu`, read the status | ✅ | |
| P00313 | That 404 shows the GUEST dead-end, not the platform 404 | assert the body has no `Aevidine` | ✅ | |
| P00314 | That 404 offers no link to the staff sign-in | assert no anchor with `href="/"` | ✅ | |
| P00315 | A malformed QR code answers the friendly page | goto `/q/!!!`, assert the "isn't active" copy | ✅ | |
| P00316 | An unknown-but-well-formed QR code answers the friendly page | goto `/q/ZZZZZZ`, assert the same copy | ✅ | |
| P00317 | The dead-code page carries no platform branding | assert no `Aevidine` in the body | ✅ | |
| P00318 | The dead-code page's tab title does not advertise a restaurant | read `document.title` | ✅ | |
| P00319 | The menu renders dish cards at 360×780 | count `.item-card` | ✅ | |
| P00320 | The menu renders dish cards at 1280×800 | count `.item-card` | ✅ | |
| P00321 | The search box is on screen at 360×780 | assert `#search-input` has a non-null offsetParent | ✅ | |
| P00322 | The category chip row is on screen at 360×780 | assert `#cat-scroller` is visible | ✅ | |
| P00323 | Typing in search narrows the grid | type "coffee", count cards before/after | ✅ | |
| P00324 | Typing in search shows the suggestions panel | assert `.search-dropdown` appears | ✅ | |
| P00325 | Each suggestion row shows a price | assert every `.search-result-price` has non-empty text | ✅ | |
| P00326 | A suggestion's price matches that dish's card price | compare the two rendered strings for one dish | ✅ | |
| P00327 | A nonsense search shows the friendly "no dishes found" message | type "zzzzqq", assert the empty-state text | ✅ | |
| P00328 | The guest's typed text is echoed back literally | type `$&`, assert the message contains `$&` and not the placeholder | ✅ | |
| P00329 | Typing `` $` `` does not nest the message inside itself | type it, read the rendered text | ✅ | |
| P00330 | Clearing the search brings the grouped view back | clear the box, assert `.cat-group` reappears | ✅ | |
| P00331 | Tapping a category chip scrolls that section under the header | click a chip, read the section's top vs the sticky bottom | ✅ | |
| P00332 | The FIRST tap lands correctly (not only the second) | do it from the very top of the page once | ✅ | |
| P00333 | The tapped chip is the one highlighted afterwards | assert `.cat-card.active` matches the tapped slug | ✅ | |
| P00334 | Scrolling moves the highlight to the section under the header | scroll, read the active chip | ✅ | |
| P00335 | Reaching the very bottom highlights the LAST category | scroll to the end, read the active chip | ✅ | |
| P00336 | Folding a category hides its dishes | click `.cat-group-head`, count that section's cards | ✅ | |
| P00337 | Tapping a folded category's chip unfolds it | fold, then click its chip, assert it is open | ✅ | |
| P00338 | The call-waiter bell is never hidden, faded, moved aside or made untappable | read `settleBell()`: the "nothing within reach is clean" fallback must stay a plain `removeProperty("--bell-lift")`, with no visibility/opacity/pointer-events/yield anywhere in the function | ✅ | **R29 (owner, 2026-08-17).** Pass 1 measured a real overlap (search open: 22px onto a dish's "+", the button's own centre returning `chef-call`; all folded: 48px onto a category header, both restaurants). A stand-down fix was built, confirmed, then REVERTED on his word: *"should be stuck at his place … don't change that again"*. The guard now protects the ORIGINAL behaviour. Do not re-report |
| P00339 | The bell's overlap with a category header is NOT treated as a fault | confirm nothing in the diff hides or moves the bell, and that R29 is recorded in the doc and on the fallback line | ✅ | see P00338 — same ruling |
| P00340 | The bell is clear at rest, with no scrolling at all | load, wait, measure without scrolling | ✅ | |
| P00341 | The bell is still clear after scrolling stops | scroll, wait 700ms, measure | ✅ | |
| P00342 | The bell is clear at 1280×800 too | same measurement at desktop width | ✅ | |
| P00342b | The bell behaves exactly as it did before this sweep | `git diff origin/main -- components/MenuView.tsx` shows no change inside `settleBell()` other than the R29 comment | ✅ | |
| P00343 | The "slide →" hint is absent when every chip already fits | load a 4-category restaurant at 1280×800, assert no `.browse-hint` | ✅ | |
| P00344 | The "slide →" hint IS shown when the row overflows | load Aangan at 360×780, assert `.browse-hint` | ✅ | |
| P00345 | The chip row's fade cue is stamped only while there is more to the right | read `data-can-scroll` at the start and end of the row | ✅ | |
| P00346 | The suggestions panel's fade cue is stamped when there are more than fit | type a broad term, read `data-can-scroll` | ✅ | |
| P00347 | The layout toggle switches to the list view | click list, assert `.items-container` lost `gallery-mode` | ✅ | |
| P00348 | The layout choice survives a reload on the SAME restaurant | reload, assert the list view is still on | ✅ | |
| P00349 | The layout choice does NOT override another restaurant's own default | set list on A, open B, assert B shows its configured default | ✅ | |
| P00350 | A veg filter narrows the grid | click Veg, assert every visible card carries the veg mark | ✅ | |
| P00351 | Re-tapping the active diet chip clears it | click twice, assert the full grid is back | ✅ | |
| P00352 | A filter that matches nothing shows a headline AND a next step | turn on a filter with no matches, assert both lines render | ✅ | |
| P00353 | The favourites-empty state explains how to save one | turn on Favourites with none saved, assert the how-to card | ✅ | |
| P00354 | The menu makes no failing requests on French House | collect responses ≥400 | ✅ | |
| P00355 | The menu makes no failing requests on Aangan | collect responses ≥400 | ✅ | |
| P00356 | The menu raises no page errors on French House | collect `pageerror` | ✅ | |
| P00357 | The menu raises no page errors on Aangan | collect `pageerror` | ✅ | |
| P00358 | No leaked template markers appear in the rendered text | scan for `${`, `-->`, `[object Object]`, bare `NaN` | ✅ | |
| P00359 | Aangan's menu shows no "Little French House" anywhere | scan the rendered text | ✅ | |
| P00360 | Aangan's tab title names Aangan, not the platform | read `document.title` | ✅ | |
| P00361 | Aangan's page description is Aangan's, not the SaaS pitch | read the description meta tag | ✅ | |
| P00362 | Aangan's OpenGraph title names Aangan | read the `og:title` meta tag | ✅ | |
| P00363 | French House's tab title names French House | read `document.title` | ✅ | |
| P00364 | The exit guard is armed on `/r/<slug>/menu` | read `history.state.__lfhExitGuard` | ✅ | |
| P00365 | The exit guard is armed on `/q/<code>` | same read on a live code | ⏭ | no live QR code row exists on the dev stack for a restaurant I may write to; `verify:guest` covers the source contract (P00364 proves the arming path) |
| P00366 | Opening the language picker and pressing Back closes it, not the site | open, `goBack`, assert the list closed and the URL is unchanged | ✅ | |
| P00367 | The language picker closes on an outside click | open, click the body, assert it closed | ✅ | |
| P00368 | The language picker closes on Escape | open, press Escape, assert it closed | ✅ | |
| P00369 | The frosted header panel computes a REAL blur | read `getComputedStyle(.menu-headfrost).backdropFilter` | ✅ | |
| P00370 | The theme toggle actually re-skins the page | click it, read `html[data-theme]` and a card's computed background | ✅ | |
| P00371 | The theme choice survives a reload | reload, read `html[data-theme]` | ✅ | |
| P00372 | A dark-default restaurant does not flash light before it paints | load with no saved theme, sample `data-theme` every 16ms and assert no light frame before a dark settle | ⏭ | **not exercised, and I will not claim it was.** Sampled every 16ms on both restaurants: each settles LIGHT, because no restaurant on this dev stack currently has Access → Menu → Format → Default mode set to dark. No light-then-dark flash was seen because the dark path never ran. The source contract is covered by P00156/P00157 and by `verify:guest`'s "/q honours … dark default" row. Re-run this the day a restaurant is set to dark |
| P00373 | Adding a dish from the card updates the header cart badge | click "+", read `.cart-badge` | ✅ | |
| P00374 | The "+" becomes a −/+ stepper after the first add | assert `.cart-qty-row` replaced `.cart-add-btn` | ✅ | |
| P00375 | Every row created for these checks was removed by id in the same run | list what was written; the browse checks write only browser storage | ✅ | no database rows were created at all |

---

## BLOCK D — screenshots I actually opened and read (P00376–P00425)

Each row: take the shot to my scratch folder, **Read the image**, and say what the picture shows.
Desktop = 1280×800. Phone = Samsung A35, 360×780, dpr 3.

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P00376 | French House menu, phone, first paint — the menu is legible and nothing overlaps | screenshot + read | ✅ | |
| P00377 | French House menu, desktop, first paint | screenshot + read | ✅ | |
| P00378 | Aangan menu, phone, first paint | screenshot + read | ✅ | |
| P00379 | Aangan menu, desktop, first paint | screenshot + read | ✅ | |
| P00380 | The category chip row, phone — chips are readable and not sliced mid-word | screenshot + read | ✅ | |
| P00381 | The category chip row, desktop | screenshot + read | ✅ | |
| P00382 | A dish card's "+" is fully visible and unobstructed, phone | screenshot + read | ✅ | |
| P00383 | The waiter bell's resting place, phone — it covers no control | screenshot + read | ✅ | |
| P00384 | The waiter bell after scrolling stops, phone | screenshot + read | ✅ | |
| P00385 | The search suggestions panel, phone — price and category both readable | screenshot + read | ✅ | |
| P00386 | The search suggestions panel, desktop | screenshot + read | ✅ | |
| P00387 | The "no dishes found" screen, phone | screenshot + read | ✅ | |
| P00388 | The "no dishes match these filters" screen, phone — headline AND next step | screenshot + read | ✅ | |
| P00389 | The favourites-empty screen, phone | screenshot + read | ✅ | |
| P00390 | The list layout, phone | screenshot + read | ✅ | |
| P00391 | The list layout, desktop | screenshot + read | ✅ | |
| P00392 | The gallery layout, phone | screenshot + read | ✅ | |
| P00393 | The gallery layout, desktop | screenshot + read | ✅ | |
| P00394 | Light skin, French House menu, phone | screenshot + read | ✅ | |
| P00395 | Dark skin, French House menu, phone | screenshot + read | ✅ | |
| P00396 | Light skin, Aangan menu, phone | screenshot + read | ✅ | |
| P00397 | Dark skin, Aangan menu, phone | screenshot + read | ✅ | |
| P00398 | Light skin, desktop — no invisible text from an undefined colour token | screenshot + read | ✅ | |
| P00399 | Dark skin, desktop — same | screenshot + read | ✅ | |
| P00400 | The guest dead-end page, phone — restaurant's font and theme, no platform brand | screenshot + read | ✅ | |
| P00401 | The guest dead-end page, desktop | screenshot + read | ✅ | |
| P00402 | The dead QR-code page, phone | screenshot + read | ✅ | |
| P00403 | The dead QR-code page, desktop | screenshot + read | ✅ | |
| P00404 | The platform 404 (`/zz-nope`), desktop — this one MAY carry the brand | screenshot + read | ✅ | |
| P00405 | A folded category, phone — the chevron reads as "closed" | screenshot + read | ✅ | |
| P00406 | A folded category, desktop | screenshot + read | ✅ | |
| P00407 | The sold-out pill on a card, phone | mark one French House dish sold-out, screenshot + read, restore its tags by id in the same run | ✅ | dish `aglio-e-olio-in-coconut-oil`; original tags `["vegetarian","bestseller","top-rated"]` restored and re-read back |
| P00408 | The −/+ stepper after an add, phone | screenshot + read | ✅ | |
| P00409 | The header cart badge with a count, phone | screenshot + read | ✅ | |
| P00410 | The language picker open, phone — it fits the screen | screenshot + read | ✅ | |
| P00411 | The currency picker open, phone | screenshot + read | ✅ | |
| P00412 | The hero greeting, French House, phone — letters are whole, not broken apart | screenshot + read | ✅ | |
| P00413 | The hero greeting, Aangan, phone | screenshot + read | ✅ | |
| P00414 | The hero greeting in Hindi — Devanagari is joined, not on dotted circles | switch language, screenshot + read | ✅ | |
| P00415 | The menu in Hindi, phone — nothing overflows its box | screenshot + read | ✅ | |
| P00416 | The menu in French, phone — longer chip labels still fit or scroll | screenshot + read | ✅ | |
| P00417 | The scroll-shrunk header, phone — the bar is compact and still readable | screenshot + read | ✅ | |
| P00418 | The frosted header over scrolled content, phone — the frost is real, not a flat block | screenshot + read | ✅ | |
| P00419 | The empty-menu state ("no dishes yet"), desktop | screenshot + read | ⏭ | no dish-less restaurant exists on the dev stack that I may write to; the render branch is covered by P00038 and the copy by P00327's sibling |
| P00420 | The loading skeletons, phone | screenshot + read | ✅ | |
| P00421 | A broken dish photo, phone — a neutral tile, no broken-image glyph | screenshot + read | ✅ | |
| P00422 | The bottom of a long menu, phone — nothing is cut off by the footer | screenshot + read | ✅ | |
| P00423 | The bottom of a long menu, desktop | screenshot + read | ✅ | |
| P00424 | The menu at 761–1023px (tablet band) — the chip row and grid still work | screenshot + read | ✅ | |
| P00425 | The menu after my changes looks IDENTICAL to before, except the three fixes | compare before/after shots | ✅ | |

---

## BLOCK E — tracing a change across doors and panels (P00426–P00475)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P00426 | The Menu master switch reaches all three doors from one settings read | trace `getSettings(...).menuEnabled` through both page files | ✅ | |
| P00427 | Turning the Menu switch off leaves no door serving a menu | read each door's refusal branch | ✅ | |
| P00428 | The default light/dark setting reaches all three doors | trace `menuDefaultMode` | ✅ | |
| P00429 | The default layout setting reaches all three doors | trace `menuDefaultLayout` | ✅ | |
| P00430 | A menu edit in the editor busts the cached endpoint the guest reads | trace `menuTag` from the editor write to `/api/r/<slug>/menu-data` | ✅ | |
| P00431 | A menu edit also nudges the open guest tab through realtime | trace the `menu` topic breadcrumb to `refreshMenu()` | ✅ | |
| P00432 | A feature toggled in admin reaches the open guest tab | trace `refreshFeatures(restaurantId)` on the same nudge | ✅ | |
| P00433 | Switching 3D off stops downloads that had already started | trace `stopAll()` in `modelLoader` | ✅ | |
| P00434 | Switching ratings off removes the rating from the card | trace `features.ratings` into the meta row | ✅ | |
| P00435 | Switching prep time on shows a typed prep time, and only then | trace `features.prep_time` | ✅ | |
| P00436 | Switching the search feature off removes the search box | trace `features.search` | ✅ | |
| P00437 | Switching favourites off removes the chip AND stops the saved filter | trace `features.favorites` into both places | ✅ | |
| P00438 | Switching the diet filter off removes the chips AND the per-dish mark | trace `diet_filter` into MenuView and FoodCard | ✅ | |
| P00439 | Switching a sort chip off removes just that chip | trace `chip_<slug>` | ✅ | |
| P00440 | Switching the currency picker off forces the restaurant default back | trace `features.currency` in Header | ✅ | |
| P00441 | Switching the language picker off forces English back | trace `features.languages` | ✅ | |
| P00442 | A category renamed in the editor renames the chip and the group header | trace `localized(c.name, lang)` to both | ✅ | |
| P00443 | A category's colour set in the editor reaches the selected chip | trace `c.color` to `--cat-color`/`--cat-grad` | ✅ | |
| P00444 | A category switched off disappears from the guest menu | trace `activeCategorySlugs` in `lib/menu.ts` | ✅ | |
| P00445 | A dish marked sold-out shows the pill without a page reload | trace the realtime nudge to the `tags` re-render | ✅ | |
| P00446 | A dish's price edited in the editor re-prices the card AND the suggestion | trace one `menuData` update to both renderers | ✅ | |
| P00447 | A restaurant deactivated mid-session takes the open tab to the refusal page | trace the 404 → `window.location.reload()` path | ✅ | |
| P00448 | A dish added to the cart from the menu is the same line the cart panel shows | compare `isPlainLine` here with the cart panel's own matching | ✅ | |
| P00449 | The cart badge in the header counts what the card wrote | trace `lfh:cart-updated` to `loadCartCount` | ✅ | |
| P00450 | A cart built on `/q` belongs to the same restaurant as one built on `/r/<slug>` | trace `lfh_tab_tenant` into `tenantSlug()` | ✅ | |
| P00451 | A cart does not follow a guest from restaurant A to restaurant B in one tab | trace the tenant scope of `lfh_cart` | ✅ | |
| P00452 | Browse state does not follow a guest between restaurants either | trace `sk()` | ✅ | |
| P00453 | The scanned table reaches the cart and the kitchen | trace `setScannedTable` into `lib/table.ts` consumers | ✅ | |
| P00454 | A `/q` table cannot be edited by the guest, because it is never in the URL | confirm no `?table=` is written on that door | ✅ | |
| P00455 | A mis-scanned `?table=` remains correctable downstream | confirm the cart panel's fix-link still exists | ✅ | |
| P00456 | The staff panels do NOT mount the guest cart or table machinery | trace `STAFF_RE` against every panel path | ✅ | |
| P00457 | The per-restaurant staff panels are caught too | test `/r/<slug>/manager` against `STAFF_RE` | ✅ | |
| P00458 | The guest doors are NOT caught by that pattern | test `/r/<slug>/menu` and `/q/<code>` against `STAFF_RE` | ✅ | |
| P00459 | A tenant's brand reaches the header, the splash and the search glyph from one source | trace the props from the page file | ✅ | |
| P00460 | The flagship's chrome is byte-for-byte unchanged by the tenant work | trace every `isDefault ?` to an `undefined` on the #1 side | ✅ | |
| P00461 | The intro splash a tenant sees is its own, even after seeing another's | trace the scoped `seenKey` | ✅ | |
| P00462 | The guest 404 boundary applies to the menu route but not to staff routes | confirm the boundary file sits under `menu/`, not `[restaurant]/` | ✅ | |
| P00463 | A dish link from the suggestions stays inside the restaurant | trace `itemBase` into the suggestion `href` | ✅ | |
| P00464 | A dish link from a card stays inside the restaurant | trace `base` into the card `href` | ✅ | |
| P00465 | The legacy `/item/<slug>` route still means restaurant #1 | trace `GuestNotFound`'s same assumption | ✅ | |
| P00466 | The offline shell can open the menu screen | confirm the menu-data family is in the worker's data paths | ✅ | |
| P00467 | A guest order placed offline is queued, and the menu still reads | trace the guest outbox chip's mount condition | ✅ | |
| P00468 | The connection badge on the menu reflects the same state the panels use | trace `ConnectionBadge guest` | ✅ | |
| P00469 | My metadata fix cannot change what the PAGE renders | read the diff: it touches `generateMetadata` only | ✅ | |
| P00470 | My sold-out fix cannot change a dish that is in stock | read the diff: it is inside the `soldOut` branch | ✅ | |
| P00471 | My suggestions fix changes no visible styling | read the diff: roles only, no class or style change | ✅ | |
| P00472 | None of my three fixes touches another terminal's file | `git diff --name-only` | ✅ | |
| P00473 | None of my three fixes needs a migration | confirm no SQL in the diff | ✅ | |
| P00474 | None of my three fixes adds a screen, module or permission | read the diff | ✅ | |
| P00475 | The handoff rows name the exact file and the exact change | read `T1-findings.md` | ✅ | |

---

## BLOCK F — judgment: does a real restaurant need it this way (P00476–P00500)

| id | check | how to verify | result | note |
|----|-------|---------------|--------|------|
| P00476 | A diner who has never used this app knows what to tap first | open the menu cold at 360×780 and describe the first affordance | ✅ | the search box and the chip row are both above the fold |
| P00477 | Nothing important is two taps that should be one | walk browse → dish → add | ✅ | a plain dish adds in one tap from the grid |
| P00478 | The category chips read as "jump to", not "filter to" | check the highlight follows the scroll | ✅ | |
| P00479 | A diner can always find the price without opening a dish | check the card and the suggestion row | ✅ | |
| P00480 | A diner is never shown a filter that cannot match anything | check the pure-veg case on Aangan | ✅ | |
| P00481 | A diner is never told to swipe a row that does not move | check the hint on a short row | ✅ | |
| P00482 | An empty screen always says what to do next | check all three empty states | ✅ | the grouped no-match state was the last one missing a next step; it has one |
| P00483 | A number a diner might plan around is real or absent, never invented | check prep time and rating | ✅ | |
| P00484 | A feature the app advertises can actually be opened | check the 4D badge against the model files | ✅ | |
| P00485 | A sold-out dish is honest and still explorable | check the pill and the card link | ✅ | now true — this was P00143. Confirmed by a real tap: the pill opened `/r/french-house/item/aglio-e-olio-in-coconut-oil?cat=pasta` |
| P00486 | A shared link is honest about whether the menu is serving | check the preview text against the page it lands on | ✅ | now true — this was P00165 |
| P00487 | A blind diner is told what the category chips really are | check the roles on the chip row | ✅ | |
| P00488 | A blind diner is told what the suggestions list really is | check the roles on the suggestions panel | ✅ | now true — this was the third fix |
| P00489 | A guest pressing Android back never loses the restaurant by accident | check the exit guard and the picker registrations | ✅ | |
| P00490 | A guest on a slow connection sees progress, not a blank page | check the skeleton timing | ✅ | |
| P00491 | A guest on no connection still sees the menu they had | check the warm-cache handoff | ✅ | |
| P00492 | A restaurant's own look survives the whole journey | check brand props on every surface | ✅ | |
| P00493 | A restaurant that has configured a list view gets one for a first-timer | check the `defaultLayout` chain | ✅ | |
| P00494 | A returning guest keeps their own choice over the restaurant's default | check the scoped layout key | ✅ | |
| P00495 | A busy table's second guest is not handed the first guest's folded menu | check the 10-minute expiry | ✅ | |
| P00496 | The cheapest phone at a busy table is not made to stutter | check the band-limited bell scan and the hidden-tab skip | ✅ | |
| P00497 | Nothing in this territory can hide or erase a sale | confirm no billing surface is touched | ✅ | |
| P00498 | Nothing in this territory can show one restaurant another's data | confirm every read is scoped by restaurant | ✅ | |
| P00499 | The three fixes are worth making — each costs a real person something today | state the person and the loss for each | ✅ | |
| P00500 | Nothing was changed that the owner deliberately chose | re-read the pre-empt list and the rejections doc against the diff | ✅ | |
