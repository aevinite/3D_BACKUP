{
  "name": "lfh-admin-control-room",
  "version": "1.0.0",
  "private": true,
  "description": "Admin control room — the owner's master panel. Embeds the guest menu, editor, kitchen and tablet behind one switcher, shows live status, and owns the maintenance switch. Local on :4004; ADMIN_PASSWORD-lockable.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "node -e \"console.log('No build step needed — Express server runs as-is.')\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "express": "^4.21.2"
  }
}
