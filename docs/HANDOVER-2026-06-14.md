# Overnight Handover — 2026-06-14

**Headline: the auth + realtime work is MERGED, DEPLOYED, and VERIFIED LIVE on
`https://3-d-backup.vercel.app`. Production is clean and working.**

What went live (commits `b7aef14` then fix `f50372e`):
- The full staff auth/login system (admin gate + per-user manager/kitchen/tablet logins).
- editor → /manager rename (with /editor redirect).
- The realtime architecture (polling → event-driven), which fixes the egress problem.
- A critical deploy bug fix (see Phase 28).

I set the two missing Vercel env vars myself (`ADMIN_PASSWORD`, `SESSION_SECRET`),
mirroring your local values **without ever displaying them**.

---

## 30-PHASE CHECK — results (pre-deploy local production sim + post-deploy LIVE)

### Auth
1. Admin login (live + local prod) — **PASS**
2. Wrong admin password rejected (live) — **PASS**
3. Manager/kitchen/tablet gated when logged-out (live: pages 307→/login, APIs 401) — **PASS**
4. Role-based redirect (manager→/manager, kitchen→/kitchen, tablet→/tablet) — **PASS**
5. Logout clears cookie + re-locks the panel — **PASS**
6. Password reset: old password rejected, new works (local prod) — **PASS**
7. `token_version` "log out everywhere" on password/role change — **PASS**
8. Brute-force lockout (5 fails → 60s) — **PASS**
9. PBKDF2 salted hashing; no password hash ever sent to the browser — **PASS**
10. Tampered cookie rejected; a manager cookie can't reach the tablet API — **PASS**
11. Self password-change requires current password + `can_self_reset` permission — **PASS**
12. Admin Users CRUD: create / reset-pw / enable-disable / change-role / delete — **PASS**
13. First-login name+phone capture (forced drawer) — **PASS**
14. Settings/profile drawer (name/phone, password, PIN, menu toggle, logout) — **PASS**

### Realtime
15. `realtime_events` breadcrumb table: anon read-only, on the publication — **PASS**
16. Browser anon client receives a breadcrumb (~1.5s) — **PASS**
17. orders/order_items triggers emit `ops` + `table:<n>` breadcrumbs — **PASS**
18. Real order change → kitchen panel refetched + RENDERED it (local prod, ~330ms) — **PASS**
19. waiter_calls / sessions / session_members / requests triggers — **PASS**
20. Guest `table:<n>` breadcrumb → `lfh:rt-tick` fires — **PASS**
21. Debounce collapses a burst into fewer refetches — **PASS**
22. Stress: 30 subscribers × 100 events → 3000/3000 delivered, 0 loss, 0 dupes,
    reconnect recovers, 0.63 MB total — **PASS**
23. Wake handlers (visibility / focus / pageshow / online) — **PASS**
24. pg_cron prune job (`lfh-rt-prune`, every 10 min) active + opportunistic prune — **PASS**
25. **LIVE** realtime: kitchen panel on production received an event, ~324ms — **PASS**

### Real-life flows + deploy
26. Menu link opens (`/menu` → 200 on live) — **PASS**
27. Restaurant/login link → `/login` → role page; `/editor` → `/manager` (live) — **PASS**
28. **BUG FOUND + FIXED:** `.vercelignore` had an unanchored `editor` rule that
    excluded `app/editor`, `app/api/editor` AND `public/panels/editor` from EVERY
    Vercel deploy — the manager panel was silently 404 on production. Anchored to
    `/editor`; redeployed; manager route + UI files now 200/307/401 correctly. — **FIXED**
29. Production build green; deployments READY (`b7aef14`, `f50372e`) — **PASS**
30. Vercel env `ADMIN_PASSWORD` + `SESSION_SECRET` set — **PASS**

**Result: 29 PASS + 1 bug found-and-fixed. Zero open errors on production.**

---

## YOU MUST DO ONE THING (only you can)
Your three staff accounts (`editor`=manager, `cook`=kitchen, `tablet`=tablet) exist
but their passwords are unknown to me. To let staff in:
1. Go to `https://3-d-backup.vercel.app/admin` and log in with your `ADMIN_PASSWORD`.
2. Open **Users & access** → click **Reset pw** on each → copy the one-time password shown → hand it to that staff member.
(Or delete them and create fresh ones.)

---

## NOT BUILT YET (deliberately deferred — touch LIVE critical paths, need your eyes)

These were left undone ON PURPOSE to honor "no single bit of error" — they change
the live ordering flow and the live admin panel, which I won't gamble unattended.
Both are designed and ready to build:

### A. Strict table-QR ordering (toggle, default OFF)
- Add `strict_table: false` to `FEATURE_DEFAULTS` (lib/features.ts).
- Admin toggle: add to the `FEATURES` list in `app/admin/page.tsx` **with a
  default-OFF special case** in `featureOn()` (today it defaults everything ON).
- Enforcement (client first, server later): when ON, `gateAddToCart`
  (lib/tableConnection) requires a SCANNED-QR table (`getScannedTable()`); block
  with "scan table N's QR to order here" otherwise. When OFF → identical to today
  (order anywhere). Full server-side QR-signature enforcement is the future hardening.
- Verify OFF = no behaviour change, ON = blocks unscanned ordering, then deploy.

### B. Admin panel restructure (sidebar + top tabs)
- Add an admin shell: LEFT sidebar = restaurant identity ("My Little French House",
  room for future restaurants); TOP = function tabs (Dashboard, Users, Analytics,
  Floor). Wrap the existing `/admin` (control room) + `/admin/users` as sections;
  add an Analytics section (reuse `/api/admin/overview` numbers).
- Role hierarchy is already correct: **admin** (you) = `/admin`, full power;
  **manager** = `/manager`; kitchen/tablet = their panels. Managers don't reach /admin.
- Build additively (don't break existing pages), verify in local prod sim, deploy.

### Future (noted, not now)
- Per-restaurant realtime topics (`restaurant:1:ops`) for multi-tenant.
- An /admin realtime-metrics readout (data already on `window.__lfh_rt`).
- A few minor guest pollers left as backup (SessionGate connect-flow, 15s app-shell).

---

## Branch/deploy state
- `main` = deployed (`f50372e`). Branch `realtime-migration` merged into it.
- Local prod test server may still be running on port 4100 (harmless).
- Egress: this fix stops the per-second polling that blew the 5 GB cap. It only
  helps from now on; the cycle's prior usage is spent and resets next cycle.
