# REJECTED IDEAS — things the owner has already said NO to

**Read this BEFORE suggesting any improvement, and before "fixing" anything that looks odd.**
An idea on this list has already been decided. Re-suggesting it wastes the owner's time and — worse —
invites someone to "fix" a thing that is deliberate.

> **The rule (owner, 2026-08-07):**
> *"everything I reject also should be written in the comment in the code."*
> *"While suggesting something and doing, at that time, you have to make sure I have already said no for it. So you don't repeat the same thing again."*
>
> So every rejection lands in **two** places: a row here, **and** a comment at the exact line in the
> code someone would otherwise change. `npm run verify:rejected` fails if a rejection listed here has
> no comment at its code site.

## How to add one

1. Add a row to the table below with the date and the owner's own words where you have them.
2. Put a comment at the code site starting with `REJECTED (owner, <date>):` and one line of why.
3. Run `npm run verify:rejected`.

Never delete a row. If the owner changes his mind, move it to **Reversed** at the bottom with the new
date — the history is the point.

---

## Rejected

| # | idea | owner's decision | code site |
|---|---|---|---|
| R1 | Make FREE table tiles shorter / denser so they stop being mostly blank space | **NO** (2026-08-07): *"I want free table to look empty only"* — a free tile looking empty is the design, not a defect. Do not shrink it, do not fill it, do not "use the space". | `public/panels/tablet/app.js` → `tileHtml()` |
| R2 | Hide the "Your tables 1–30 · 30 tables" section strip when the waiter holds the whole floor | **NO** (2026-08-07) — not wanted. | `public/panels/tablet/app.js` → `renderMySection()` |
| R3 | Collapse an EMPTY kitchen column ("New — Nothing here") on a phone to reclaim ~101px | **NO** (2026-08-07) — not wanted. The three columns stay whatever is in them. | `public/panels/kitchen/app.js` → `renderColumns()` |
| R4 | Widen the 💳 Mark-paid / ⏻ Close tap targets on a floor tile (they are 22–25px) | **NO** (2026-08-07): *"Don't do fix number four"* — the size is accepted; the two-step confirm is the answer to mis-taps. | `public/panels/tablet/app.js` → `tileHtml()` acts row |
| R5 | Give the kitchen a "ready for N minutes" ageing signal on the Ready column | **NO** (2026-08-07) — not picked. The cooking-age warning is enough. | `public/panels/kitchen/app.js` → `ageClass()` |
| R6 | Restate `TILE_MIN_PX` (the 44px tappability floor) in the waiter tablet alongside the per-row constants | **NO** (2026-08-07) — not picked. | `public/panels/tablet/app.js` → `FLOOR_PER_ROW_*` block |
| R7 | Any kind of PROFILE on the kitchen panel — including the one-time "👋 Finish setup" card (settled 2026-08-08: `LFH_NO_PROFILE_AT_ALL`) — a profile page, a pay record, a "👤 Profile" button, a personal-settings door | **NO, STANDING** (owner, 2026-07-29, re-confirmed 2026-08-05 and again **2026-08-07**: *"Kitchen panel will not have profile or stuff like that. I have already told this"*). This was asked twice and offered back to him a third time as an "improvement" — that is the mistake this whole file exists to stop. The kitchen keeps its login, its PIN and its action log; it has no profile of any kind and no button that says Profile. | `lib/staffProfileShared.ts` → `PROFILE_ROLES` · `public/panels/kitchen/app.js` → `LFH_SUPPRESS_SETTINGS_BTN` |
| R9 | Show how long a party has been merged ("⇄ with T7 · 40m") so a forgotten merge stands out | **NO** (2026-08-11): *"I don't implement improvement number three. We will ignore improvement number three. We will skip it."* | `public/panels/editor/app.js` → the `ft-merge` chip in `floorTileHtml()` |
| R10 | Put the money beside the floor header's "To pay" count ("To pay 2 · ₹1,659") | **NO** (2026-08-11): *"We don't need improvement number six."* The count stays a count. **Where the line is (2026-08-20):** he ASKED for a money split on the admin console's Bills → CLOSED UNPAID tile (*"we have 2 option in close out also … one with the food was made to could as loss one is food was not made and cancelled so no loss detected"*) and it was built. Different screen, different reader, different question — an oversight ledger read after service, not the live floor a manager works off during it. This row still stands for the manager floor; do not read the admin change as permission for it. | `public/panels/editor/app.js` → `floorStatsHtml()` |
| R11 | Stop the floor header's 🧾 KOT ▾ / ⚡ QO/P pair wrapping onto a second line (it costs ~60px above the tables at 1280px) | **NO** (2026-08-11): *"Don't solve p four. P four is perfectly fine. We don't need that."* The header's current layout is accepted as it is — do not re-flow it to keep the buttons on the title's line, and do not raise it again as a finding. | `public/panels/editor/style.css` → `.floor-head` / `.floor-head-acts` |
| R12 | Make the dish PRICE (and the muted meta line, and a sold-out dish's greyed price) obey their CSS colours by beating the `a *, .item-card-link * { color: inherit !important }` reset — reported by guest sweep T1, 2026-08-12, measured: the price computes as `--text` while `--accent` sits unused on the same element | **NO** (owner, 2026-08-12): *"I don't think P1 is a problem like I don't know like what are you saying like it's not a problem … I don't think it's required … because bill will be black and white colour will stand out."* The plain-text price is accepted on every restaurant, in both skins. Do not add `!important`, do not re-report it. | `app/globals.css` → the comment above `.dish-price` |
| R13 | Make the guest search-suggestion panel use the restaurant's own colours instead of the hardcoded `#1b120c` brown + `rgba(212,165,116,…)` gold — reported by guest sweep T1 as a white-label leak, measured identical on green-bowl, sakura-sushi, spice-route and aangan | **NO** (owner, 2026-08-12): *"P3 is completely trash. I want … all the panels and all that stuff write in a code as a comment that should be this colour only like golden and little french house theme one."* GOLDEN + the little-French-house theme is the intended house style here and on the panels. Do not swap these for `var(--accent)`, do not re-report it. | `app/globals.css` → the comment above `.search-dropdown` |
| R14 | An automatic translator for DISH names, so a guest browsing in Hindi can search a dish in Hindi (offered as sweep idea I6, 2026-08-12 — the third time this has been raised) | **NO** (owner, 2026-08-12): *"we will not do auto translator … I will tell you only, I will add the translated word, and we will fix that and keep that for the menu … So don't show it as a problem again."* He supplies the words himself when he wants them; they go in `searchAlias`. Never build it, never offer it. | `lib/menu.ts` → the dish-translation decision block |
| R15 | Translate the three remaining English app labels on the guest menu — the "Maximum 99 per dish" toast, the "<dish> added / tap to view your bill" toast, and the offline strip (then "Your order is saved and will send by itself"; the wording was corrected 2026-08-29 to "Anything you send is saved and goes by itself", because the queue also holds a call for staff and the old sentence promised an order even when nothing was queued — the REJECTION is unchanged and it is still an English literal, never wired to lib/i18n.ts) | **NO** (owner, 2026-08-12, asked directly): *"No — English is fine for these."* Distinct from R11: these are our own labels, not dish names. Leave them as English literals; do not move them into `lib/i18n.ts`. | `components/FoodCard.tsx` → the 99-cap toast · `components/OfflineNotice.tsx` → `msg` |
| R16 | Give the GUEST menu a longer offline window than the 2 hours staff get, so a diner who returns after a few hours still sees the menu they were reading (T1 improvement 7, 2026-08-12) | **NO** (owner, 2026-08-12): *"there is no off-line limit for diner. Diner should be online."* The window exists for staff continuity; one number covers everyone. Do not add a per-family window, do not re-report it. | `public/sw.js` → the comment above `MAX_STALE_MS` |
| R17 | Refresh the currency conversion rates from a live FX feed instead of the hand-edited table (T1 improvement 12, 2026-08-12) | **NO** (owner, 2026-08-12): *"leave it and make sure you don't show as improvement or error again … this will be like this only."* No scheduled fetch, no stored per-restaurant rate. Not a finding. | `lib/format.ts` → the comment above `CURRENCIES` |
| R18 | A SECOND health check that says WHICH part is broken — `/api/health/deep`, reporting the database, file storage and the live-updates setting separately, watched by an hourly UptimeRobot monitor (shipped 2026-08-12 as improvement I13, removed 2026-08-13) | **NO** (owner, 2026-08-13): *"then we don't need it remove it"* — said after the setup was explained: it needs a second monitor created in HIS UptimeRobot account, and he does not want to run it. A health page nobody watches is dead code, so the page went too. **Do not re-add it, and do not "improve" `/api/health` by folding storage / config checks into it instead** — that is the same idea wearing a different hat, and it would also make the 5-minute probe expensive, which is the thing it was deliberately trimmed for on 2026-08-06. `/api/health` itself STAYS exactly as it is: the panels' offline screen (`public/offline.html`) uses it to tell "this device lost signal" apart from "the restaurant's server is down", and the test suite pings it. | `app/api/health/route.ts` → the header comment |
| R19 | Change the two sign-in screens from `100vh` to `100dvh`, so the card stops sitting low on Android and the on-screen keyboard stops covering "Sign in" | **NO** (owner, 2026-08-13) — raised by the T12 phone sweep; he judged it not a problem in practice: *"i don't thing 7th is problem"*. | `app/login/LoginForm.tsx` → the `<main style={{ minHeight: "100vh" }}>` (same call covers `app/staff-login/page.tsx`) |
| R20 | Make the owner top-bar chip name the PAGE you are on instead of always reading "Owner overview" | **NO** (owner, 2026-08-13): *"why you need to know where we are you can click 3 line and know or on top there is many written like heading and stuff"*. | `components/owner/OwnerShell.tsx` → the `.owx-scope` chip |
| R21 | Give the KITCHEN panel the same plain-words error wording the manager and waiter panels have (a shared `errText()` that tells "no internet", "the system is very busy" and a clash apart, instead of `Failed: <code>`) — offered as sweep idea I6, 2026-08-12 | **NO** (owner, 2026-08-13): *"i don't thing i6 is require mainly in kitchen panel is only use for kot print"*. The kitchen screen's job is the KOT, not editing values, so the wording gap it would close is one that screen never reaches. Leave the toasts as they are; do not lift `errText()` into a shared file for the kitchen's sake, and do not re-report the kitchen as the odd one out of the three panels. | `public/panels/kitchen/app.js` → the comment above `act()` |
| R22 | Widen the tap area of the ✕ that closes the full-screen panel sheets (⚡ QO/P, ＋ Take order, Add-a-dish, Discount) — it measures 25×22px at 360, against a ~44px guideline, and sits in the corner a right thumb reaches last (T12 phone sweep, 2026-08-13, with a zoomed screenshot) | **NO** (owner, 2026-08-13): *"x is fine"* — said after looking at the screenshot. The phone's BACK button also closes every one of these sheets, so nobody is trapped. Do not pad it, do not resize it, and do not re-report the panel close ✕ as a tap-target fault. | `public/panels/editor/style.css` → `.tbl-modal-close` |
| R23 | RAISING the guest menu's half-finished translation as work — either "the ordering screens (cart, table gate, order strip, bill, waiter-call, rating) are still hardcoded English" or "Arabic renders as disconnected letters in the animated hero" (T15 wording sweep findings F1 and F2, 2026-08-13) | **NO — it stays as it is, and it stays off the list** (owner, 2026-08-14): *"you don't have to remove option … what I wanted is just don't suggest that improvement right now."* **The language picker and the currency picker REMAIN on the guest menu** — an earlier reading of this removed them and that was reverted the same day; the Access → Menu → Format → Languages/Currencies Single/Multiple control is live and meaningful. What is rejected is bringing the unfinished translation back to him as a problem or an improvement. Both facts are true and both are parked: only 4 files call `useTranslation()`, and per-grapheme `inline-block` spans cannot join Arabic. Do not re-report either, and do not start on them unasked. Extends R14 (no auto-translator) and R15 (English is fine for our own labels). **UPDATE (owner, 2026-09-21) — he asked for part of this himself, so it is no longer parked in full:** *"welcome all day cafe bakery and categories and names in categories name in item chef special favorite and all that language should be changed … if somewhere the language is not that change perfectly you will go inside the code and change that."* That is DISPLAY translation of the guest menu's own words, and migration 405 adds the columns it needs (`menu_items.title_i18n`, `restaurants.hero_i18n`, `restaurants.tagline_i18n`), read through the existing `localized()`. R14 still stands and is NOT touched: nothing translates automatically — the columns hold words somebody supplies, which is exactly what he said he would do. Still parked: the ordering screens (cart, table gate, order strip, bill, waiter-call, rating) and the Arabic hero letter-joining. Do not raise either unasked. | `lib/i18n.ts` → `useTranslation` · `components/HeroTitle.tsx` → `split` · `components/IntroSplash.tsx` → `intro-word` · `lib/brandText.ts` → `splitGraphemes` · `app/globals.css` → `greet-badge` |
| R24 | Give `/login` and `/staff-login` the SAME brand mark — `/login` draws a text ✦ while `/staff-login` uses the real SVG mark `/brand/aevidine-mark.svg` | **NO** (owner, 2026-08-14): *"for improvement 4 idk whats fix like we don't need/require one i guess"* — offered by the T11 visual sweep as a consistency fix; he does not want it. The two sign-in screens keep their own marks. Do not re-offer this as a "one product" or "brand consistency" improvement. | `app/login/LoginForm.tsx` (the ✦) · `app/staff-login/LoginForm.tsx` (the SVG mark) |
| R25 | Add the missing colours to the floor legend — a tile can turn AMBER (new order) or PINK (on the pass) with no legend entry, so the strip explains 3 of the 5 colours | **NO, and he said it TWICE** — the strip stays short. The state word is still in every tile's tooltip and in the table's own detail popup, so nothing is unknowable; the legend is deliberately not a full key. **Refused a THIRD time (2026-08-17)**, when the T5 sweep listed it for him again: *"whatever you left for my decision, I don't think we don't need that, so we will not do any of that stuff"*. Recorded here 2026-08-15 after the T11 sweep nearly re-reported it as a bug: the decision existed only as a code comment, which is exactly how a settled thing gets offered back. | `public/panels/editor/app.js` → `const LEG` · `public/panels/tablet/app.js` → `const LEG` |
| R26 | Make the printed documents fit a 58mm roll — the KOT's body is a fixed 280px built for 66/80mm paper, so on 58mm the table name, the "— no <ingredient>" line and the ⚠ AVOID allergy box run past the paper edge (measured by T15, 2026-08-15, by rendering billdoc's real output at both widths; the BILL itself is clean at 58mm and 80mm) | **NO — keep it as it is** (owner, 2026-08-16): *"for different restaurant we use different printer and all that stuff so right now keep IT AS IT IS."* Each restaurant is set up with a printer that suits it — the flagship and Aangan both run POS-80 (80mm) — so the document is built for 66/80mm on purpose and the right answer to a narrow roll is the right printer, not a document that tries to fit every width and reads worse on all of them. Do not add responsive print CSS, do not shrink the KOT body, and **do not re-report the 58mm overflow as a bug**. If a client ever arrives with 58mm hardware, he will say so and it becomes a real piece of work. | `public/panels/billdoc.js` → `kotDocHtml` |
| R27 | Give the restaurant a way to DELETE a bill — the grantable "Delete a bill" permission (owner or manager) | **NO** (owner, 2026-08-16): *"I don't want to give permission to the restaurant owner to delete the bill because he will fake the bill and delete the bill … so what can we do that the restaurant doesn't cheat, and at the same time we can keep the track?"* Cancel is the only route out of a bill for anyone at the restaurant; a cancel is a ₹0 sale that stays visible with its reason, person and time. The Aevidine admin console keeps a soft delete for support work. Do not re-add the row. | `lib/accessTree.ts` → the note where the `delete_bill` row was · `app/api/editor/[...path]/route.ts` → `canDeleteBill()` |
| R28 | Put a CEILING on the guest menu's background 3D preload — a cap, a first-N window, an "only what's on screen" queue, a data-saver mode or a per-restaurant setting (guest sweep T1 improvement I2: a restaurant modelling 30 dishes would have every diner's phone pull ~60 MB for dishes nobody opens) | **NO** (owner, 2026-08-16): *"don't do the sixth one any time soon like in the code, also reject that it is rejected by me."* A diner must never wait when they tap a dish. Measured, so nobody re-discovers it as new: 2 GLB requests on a French House menu open (it has 2 such dishes; Aangan has none), and `lib/modelLoader` already evicts past 40 MB. Do not re-report the unbounded preload as an egress or performance problem. | `components/MenuView.tsx` → the preload queue, at `const isAllView` |
| R29 | Make the call-waiter bell get out of the way when it has nowhere clean to sit — stand down, hide while searching, fade, yield its taps, step sideways, or raise `BELL_MAX_LIFT` (guest sweep T1: with the search suggestions open the bell overlaps a dish's "+" by 22px and `elementFromPoint` at the button's own centre returns `chef-call`; with every category folded it overlaps a `.cat-group-head` by 48px — both restaurants, 360x780) | **NO** (owner, 2026-08-17), after being shown the measurements and the built fix, which was reverted: *"i want like previous bell of call waiter should be stuck at his place we can scrool and click the thing make sure don't change that again."* The bell is a fixed thing in a fixed corner a guest can always find, and scrolling moves the page under it. Do not re-report the overlap as a fault. **RE-CONFIRMED AND WIDENED (owner, 2026-09-20):** *"why is the bell like flickering… at one point it just goes up and down… the waiter bell is look very unprofessional"*. The dodging machinery that survived the first ruling is now GONE — `settleBell()`, the `--bell-lift` variable, `body.menu-scrolling` (shrink to 72%, fade to 45%, drop 26px) and the perpetual `@keyframes chefFloat` bob. Measured at 390px before removing it: **107 px of travel with the guest sitting perfectly still**, shrinking to 36px wide at 45% opacity on every scroll — and it still finished sitting on a `.cat-group-head`, so it never achieved the thing it cost that for. Measured after: **0 px of travel**, nothing under the bell at rest or after scrolling, and the nearest dish "+" tappable at every scroll position. Do not re-add a float, a step-aside, a fade, a shrink or a lift. | `components/MenuView.tsx` → the `OBITUARY — THE BELL-DODGING MACHINERY IS GONE` note in the scroll effect · `app/globals.css` → `.chef-call` and the obituary where `body.menu-scrolling`/`chefFloat` stood |
| R30 | Give the guest hero a NEUTRAL TRANSLATED fallback — a pair of keys in `lib/i18n.ts` so a restaurant with no custom hero greets a Hindi or Arabic diner in their own language instead of the English "Welcome" / "Our Menu" | **NO** (owner, 2026-08-17): *"i want english only for all."* Extends R15 and R23. The English literals stay for every restaurant and every language. Do not add the keys, and do not use `t.greeting`/`t.heroTitle` there — those hold restaurant #1's own copy ("BONJOUR", "All-Day Café & Bakery") and leaking them onto another tenant is the bug that line exists to prevent. | `components/MenuView.tsx` → the `<HeroTitle>` in the hero block |
| R31 | A short "Order" label on the floor tile's take-order button, for the band where "＋ Take order" cannot fit (added by the T3 sweep 2026-08-06, restored by the T5 sweep 2026-08-17 after a later change had made the rung unreachable) | **NO** (owner, 2026-08-17), shown the real tile wearing it: *"instead of order is written that should be just a plus icon, nothing else, and it should stay like that"* — and about the tile as a whole: *"at the end of the day, UI should stay like this. I don't care if you increase the size of or maybe whatever you do. But make sure UI should stay like this."* So the button has exactly TWO faces: "＋ Take order" while it fits, and a bare **＋** when it does not — on a crowded tile, on a dense floor, and on a finished table. The SIZE may change; a third, worded face may not. Do not re-add one, and do not report the bare ＋ on a narrow tile as a bug. | `public/panels/editor/app.js` → `floorTileHtml()` · `public/panels/editor/style.css` → `.ft-take-x` |
| R32 | Put a one-tap 🍽️ Serve-all on the floor tile of a table whose food is all cooked, so serving stops costing two taps (offered as T5 improvement I3, 2026-08-17) | **NO** (owner, 2026-08-17): *"whatever you left for my decision, I don't think we don't need that, so we will not do any of that stuff."* Serving stays two taps — the tile, then 🍽️ Serve all inside it. This also protects the tile's action row, which he has already ruled on twice (R4, and "never make take order button small"): a fifth control on an 89px tile would have to take room from the take-order button. Do not add a serve control to the tile. | `public/panels/editor/app.js` → `floorTileHtml()` |
| R33 | A way out of a table that has a bill but no live tickets (every ticket cancelled) — either a line naming what to do next, or a ⏻ that ends an empty party (offered as T5 improvement I5, 2026-08-17) | **NO** (owner, 2026-08-17): *"whatever you left for my decision, I don't think we don't need that, so we will not do any of that stuff."* The screen already says what is true — "Nothing on this table — N cancelled tickets, kept in Bills" — and that is where it stops. Nothing is lost or double-charged: the bill is in the record and the table is usable. Do not add a prompt, and do not add a close button for an empty party (no table ends itself — mig 254). | `public/panels/editor/app.js` → `voidNote` (in `tablePanelParts()`) |
| R34 | Make the owner's **Pay Later** page hide itself and say "Pay Later isn't enabled for your restaurant" when the pay-later module reads off — offered and built as sweep-6 item 4 (the route already sends `moduleOff`, and the Customers screen shows exactly that sentence in the same situation) | **NO** (owner, 2026-08-18), and it was reverted the same hour: *"If there is no person in pay later, then you shouldn't shut it off. This is the wrong thing. Like, if the feature is on by me, it will stay on."* The page's job is to show the book, never to decide whether Pay Later exists. Do not re-add the branch, do not grey the tiles out, and do not re-report the unread `moduleOff` flag as a bug. **What he DID want from that area is separate and is built: the pay-later option must not appear at payment time when the module is off** — that is `khataOn()` / `tabletKhataOn()` in the two panels, checked live 2026-08-18 and correct. | `app/owner/khata/page.tsx` → the comment above the `<h1>Pay Later</h1>` |
| R35 | Give the credit book an **"oldest first"** ordering control, so an owner can work the oldest tabs down (sweep-6 item 13) | **NO** (owner, 2026-08-18): *"We don't need the thirteenth one."* The book stays ordered by how much is owed. It could not have been done honestly from this screen in any case — the list is bounded to the biggest 500 debts, so "oldest" of that slice is not the oldest on the book; a truthful version would have to move the ordering into `lfh_khata_outstanding`. The per-row age colouring (amber past 30 days, red past 60) is the part he kept. | `app/owner/khata/page.tsx` → the comment above the Pay Later search box |
| R36 | Show the owner WHICH owner-panel sections are switched OFF for him — the ✓/✗ chips on Settings → "What's enabled" (T13 sweep offered it as problem 3: the card listed 6 of 9 sections, so with Menu switched off the sidebar item vanished, /owner/menu said "ask your administrator", and this card said nothing) | **NO** (owner, 2026-08-18): *"owner can't know which option are not given to them only admin should know that"*. What is withheld from a restaurant is Aevidine's business alone — a ✗ tells the owner a feature exists that he has not been given, which is an invitation to ask for it. The card now lists ONLY what is ON and shows no off-state at all. Do not re-add the ✗, do not grey a withheld section, do not add a count ("6 of 9"), and do not re-report the missing ones as a gap. **What DID survive:** the map holds all nine sections, so a section he genuinely HAS can never be missing a chip — the card used to under-report what he had, which is the opposite fault. | `app/owner/settings/page.tsx` → the `REJECTED (owner, 2026-08-18)` note above `SECTION_LABEL`, **and the same rule on all six owner PAGES** (`customers`, `issues`, `inventory`, `manager` from 2026-08-31; `activity`, `staff`, `menu` from 2026-09-01) → each carries a `A SECTION YOU DO NOT HAVE SIMPLY IS NOT THERE` note above its `redirect("/owner")` / `router.replace("/owner")`. Extended on his word: *"it will not even show that option… it will not only show 'unable to access', that there is a feature which contains inventory"* (2026-08-31) and *"okay, give me permission"* (2026-09-01). Pay Later is the one exception, under R34. Guarded by `verify:owner-money` items 24 and 34. |
| R37 | Brand a SECOND copy of a guest's bill — the "Reprint · Duplicate" band across the top of the printed bill (shipped 2026-08-17, mig 333) | **NO** (owner, 2026-08-19): *"in the printing bill I don't even want the reprinted bill shown in the bill … and make the guard also in code like never change that to reprint thing and stuff"*. A guest asking for their bill again is service, not an incident — branding it makes the guest's own copy look like a lesser document. Do not re-add a band, a watermark, a "(copy)" in the document name, or a small-print line. The `reprint` flag no longer exists on bill data at all, so a caller cannot pass one. **What stays:** the KITCHEN TICKET keeps its big `*** Reprint · Duplicate ***` banner (his own ask, 2026-08-04, re-confirmed the same day — *"bill only keep kot banner"*), because a cook who mistakes a duplicate for a fresh order cooks the food twice. Guarded by `npm run verify:bill-reprint`. | `public/panels/billdoc.js` → `vband` (where the band was), `public/panels/editor/app.js` → `billPrintLabel()`, `public/panels/tablet/app.js` → `printTableBill()` |
| R38 | Record a reprinted bill anywhere — an Audit row, an Activity-log line, a counter, or a badge | **NO** (owner, 2026-08-19): *"I don't want reprinted bill shown anywhere like on audit also because it's not any kind of problem which needs to be audited"*. `sessions.bill_printed_at` (mig 333) survives for ONE purpose only: the button reads "Reprint" instead of "Print" once paper exists. It writes nothing to the record. **The other half stays exactly as it was:** *"reopen will be noted in the audit also"* — reopening a bill still requires a reason, is still audited (`invoice_voided`), the new invoice number is still logged, and the before → after row is still written. Guarded by `npm run verify:bill-reprint`. | `app/api/editor/[...path]/route.ts` → `sessions/:id/bill-printed` |
| R39 | Ask WHY on the print path — the "This invoice was voided. Re-issuing assigns a NEW number — why are you re-issuing it?" prompt that appeared when a reopened bill was printed | **NO** (owner, 2026-08-19): *"when I click print bill why it show like I wanna reopen the bill and stuff, it should not happen like this … reprinting should also not ask any question"*. The reason had already been given, one step earlier, in the reopen picker — which requires it and records it — so this asked the same question twice and made pressing Print feel like reopening the bill. The server now takes the reopen's own reason as the re-issue reason, so nothing is lost from the record. Do not add a prompt, a confirm or an "are you sure" to any bill-print path. **What stays:** the "who is this bill for?" sheet after a reopen, pre-filled — his words, same day: *"reopening can also change name and number … when you click print after reopen it asks for no and name again and the old one is autofilled"*. That is the bill's own field, not a why-question. | `public/panels/editor/app.js` → `generateInvoice()` |

| R40 | Enlarge the connection pill (the green **Live** chip) on the panel top bars into a 44px tap target — it measures 89 × 24.5 on the kitchen bar at every width, the last control there under the finger target once the rest of the bar was fixed | **NO** (owner, 2026-08-20): *"do your recommendation"* — said to a decision list whose recommendation was to leave both this and R41 exactly as they are. He was given both ways to fix it and the cost of each. Making the chip genuinely 44px tall grows it in **three** panels at once (kitchen, tablet and manager all load `connbadge.js`); keeping it visually identical means extending its invisible hit area over the 🔔 beside it, which trades a harmless miss for a harmful one. A mis-tap here opens or closes an information popover — the cheapest mis-tap on the screen. **Extends R4 and R22**: the third time this same judgement has been asked for. Do not re-measure it as a fault, do not pad it, do not report it in a sweep. | `public/panels/connbadge.js` → `.lfh-conn` |
| R41 | Give the kitchen top bar's 🚫 Sold out (36 × 44) and ⋯ menu (33 × 44) buttons a 44px **width** on a phone — they already meet the 44px height at every width | **NO** (owner, 2026-08-20): *"do your recommendation"* — same decision list, same answer. The height fix (2026-08-19, at every width) stands and is guarded by `npm run verify:ready-tile`; only the WIDTH is short, and only under 760px — above that these two measure 108px and hidden. This bar has wrapped to **four rows at 360px** before, and widening its buttons is precisely what brings that back: a bar that wraps costs a cook more than a narrow icon does. **Extends R4 and R22.** Do not add a `min-width` to the phone block. | `public/panels/kitchen/style.css` → `.top-actions .btn` |
| R42 | The four-pill status strip across the top of **System health** — `Database 26ms · 9 restaurants live · 1 staff online now · 1 open issue` | **NO** (owner, 2026-08-20): *"we don't want the top thing. Shows four things here is the image"*, sent with a screenshot of that exact strip, and in the same breath: *"you can optimize the whole system health care also. Right now, I'm not able to see, like, properly, like, how the thing is in system health."* A pill is a number with no sentence — "26ms" only means something to someone who already knows what good looks like — and this was the only summary the page had. It is replaced by a plain one-line verdict plus a labelled list where every check says what it means and where to go. Do not re-add a pill strip, a chip row, or a KPI band to this page. **Nothing was dropped:** the database timing and the complaint count, the only two facts the pills carried that were not already further down, are rows in that list. | `app/aevinite/health/page.tsx` → the `REBUILT 2026-08-20` note in the header |
| R43 | An amber warning light for an open complaint — the `1 open issue` pill going amber on System health whenever anything is unanswered (offered as T17 decision 16) | **NO** (owner, 2026-08-20): *"this is not need"* — his answer to that decision, given for the whole strip R42 removes. It was also the right call on its own merits: with ten restaurants there is nearly always one open complaint, so this light was amber on almost every load, which is how a person is taught to ignore amber. The Complaints row now carries a plain dot and a link straight to Repair; only an unusual pile-up (10 or more) is worth a colour. Do not colour a single open complaint, here or anywhere. **Same family as item 14 and R40/R41:** a warning that is always up is not a warning. | `app/aevinite/health/page.tsx` → the `NOT AMBER FOR ONE OPEN COMPLAINT` note on the Complaints check |
| R44 | Free a cancelled bill's number so the day's series has no gaps — "give the number back" when every order on a table was cancelled, or when a table takes a number and never orders | **NO** (owner, 2026-08-16, re-confirmed 2026-08-22 after he asked what the law wants): gaps are correct; **reuse** is the fault. CGST **Rule 46(b)** wants a consecutive serial unique for the financial year, **Rule 49** puts the identical requirement on a **Bill of Supply** — which is the document this app prints for a composition-scheme restaurant, so for those clients `bill_no` is the *statutory* number on the paper and not an internal reference — and **Rule 56** requires a cancelled document to be retained WITH its number. Hand that number to the next customer and two documents share one number, which an audit reads as *"a sale was deleted and the next one slid into its slot"*: the suppression pattern `docs/COMPLIANCE-GUARDRAILS.md` exists to prevent. A gap is explainable in one sentence; a duplicate is not explainable at all. **Measured 2026-08-22:** 840 sessions hold a number whose every order was cancelled and 250 hold one with no order — untidy, and not a fault: the Audit, the admin bill ledger and the signed chain (mig 332) account for all of them. Related: the printed sheet already shows the internal number ONLY when there is no invoice number to show instead, so the gappy one stays off a customer's paper without any counter changing. | `lib/billLedger.ts` → `rollUpBill()`, `public/panels/billdoc.js` → `billNo` |
| R45 | Move a section's remaining narrative out of `CLAUDE.md` into `docs/CLAUDE-DETAIL.md` to make headroom under the 24,000-byte guard — the file sits 56 bytes below it, so the next rule anyone adds turns the checks red | **NO** (2026-08-22): *"i don't want them"* — offered as a 🟡 decision at the end of the T29 sweep and refused. The budget stays, the sections stay where they are, and nobody spends an afternoon re-filing rules. **And raising the number is not the loophole** — the guard's own comment records that raising it is how the last budget stopped working. When it does trip, whoever is adding a rule makes room inside their own edit and says so. | `scripts/verify-doc-pointers.mjs` → `BYTE_BUDGET` |
| R46 | Make the print helper's done/failed report refuse a job that NO computer has claimed, so it matches the document fetch in the same file (drop the `job.agent_id &&` guard) | **NO** (2026-08-22): *"i don't want them"* — offered as a 🟡 decision at the end of the T29 sweep and refused. It is a consistency tidy-up with **no reachable path** behind it: a helper only ever learns a job id by claiming it, and claiming sets the owner; removing that computer nulls the owner but also kills its token, so it can no longer report anything. No named person is worse off today, and the blast radius of getting it wrong is paper. Leave the guard exactly as written. | `app/api/print-agent/[...path]/route.ts` → `job.agent_id` |
| R47 | A "✕ Cancel this bill" button — cancelling a whole bill as ONE act, instead of cancelling its KOTs one at a time (offered as the T5 sweep's recommendation, 2026-08-26, with the compliance research behind it) | **NO** (owner, 2026-08-26), after reading that research and deciding the opposite way: *"if there is a bill there is 1 KOT and it has been cancelled. It can go in the cancel bill. I agree to you like that, so there will not be bill cancellation. Only there will be only KOT cancellation and which will be going on audit section. Let's keep it like that."* **A bill is never cancelled as an act — it BECOMES cancelled when every KOT on it is cancelled**, which is what `billState()` already derives. That is his design and it is compliant on its own terms: nothing disappears, every KOT void carries who/when/why into the Audit, and migration 331 already refuses an invoice number to a bill whose every order is cancelled. Do not add a bill-level cancel, do not add a bill-level cancel reason, and do not re-offer this with the "one clean audit row" argument — he has heard it and chosen the KOT trail instead. | `public/panels/editor/app.js` → `billState()` · `public/panels/editor/app.js` → `cancelOrder()` |
| R48 | Reword the eight panel refusals that say **"No restaurant scope — open this panel from the admin console."** (two of them the bare "No restaurant scope.") into plain words — offered and built as the T27 sweep's item 3, 2026-08-27 | **NO** (owner, 2026-08-28): *"if you make everthing perfect the no 3 will not even happen"* — refused on REACHABILITY, not on the wording. He is right, and the reason is worth keeping rather than re-deriving: `lib/panelScope.ts` → `panelRestaurantId` returns `g.user.restaurant_id \|\| DEFAULT_RESTAURANT_ID` for **any signed-in staff member**, so `rid` can never be null for a waiter, a manager or a cook. The only reader who can reach these eight sentences is the **ADMIN super-user who opened a panel directly instead of through the console** — one person, who already knows what "scope" means. The commit was reverted. Do not reword them, and do not re-report them as jargon. **The word "scope" stays WATCHED by `verify:wording`** — the allowance there names these eight exact sentences, so a NEW "scope" on a screen a real person reads still fails. | `app/api/editor/[...path]/route.ts` → `panelRestaurantId` · `app/api/kitchen/[...path]/route.ts` → `panelRestaurantId` · `app/api/tablet/[...path]/route.ts` → `panelRestaurantId` · `app/api/inventory/[...path]/route.ts` → `panelRestaurantId` · `app/api/issue-media/route.ts` → `panelRestaurantId` · `scripts/verify-wording.mjs` → `ALLOWED` |
| R49 | Name the bill's number row after the DOCUMENT it sits on — "Serial no" instead of "Invoice" on a composition-scheme **Bill of Supply** (shipped and shown to him 2026-08-28) | **NO** (owner, 2026-08-28), on seeing it: *"KEEP INVOICE"*. The reasoning for the change was real — a composition dealer may not issue a tax invoice, and **CGST Rule 49** calls that number a *consecutive serial number*, so the sheet read BILL OF SUPPLY at the top and INVOICE three lines below. He looked at it and chose the plain word anyway: everyone at the restaurant, and every other screen in this product, calls it the invoice number, and **a second name for the same number on one document teaches staff there are two things when there is one.** Do not re-introduce "Serial no", "Bill of supply no", "Document no" or any per-document label. **What he DOES control, and already did:** the PREFIX — Settings › Billing → "Invoice prefix", default `INV` — so a composition restaurant that wants `BOS/2026-27/000118` can already have it without renaming the row. Guarded by `npm run verify:print-paper` §3m, which fails the build if the label changes on any document. | `public/panels/billdoc.js` → the `d.invNo` row |
| R50 | Print the mig-332 **verification line** on a guest's bill — one line under the total reading `Verification 1042 · a3f9c1d2e4b5`, so whoever holds the receipt can check the sale against the tamper-evident chain themselves (built 2026-08-17 off his own *"do both 11 and 12"*, wired to the data and rendered for him side by side on 2026-08-28) | **NO** (owner, 2026-08-28), after seeing exactly what it added on a real bill: *"DON'T DO 19TH ONE"*. Same reason as **R37** eleven days earlier — **a guest's copy of their own bill does not carry extra marks.** The argument for it was genuine (Germany's KassenSichV prints the signature on the receipt; after the PetPooja raids it is the strongest answer to "prove you cannot hide a sale") and he heard it and chose the cleaner paper. **The chain itself is untouched and still runs on every issued bill** — it stays in the records, where the Audit, the admin bill ledger and `docs/NUMBERING.md` already point. Do NOT re-add a verification line, a chain reference, a QR code carrying one, or a "scan to verify" note. The flag and both data fields were DELETED rather than left switched off, because a field that silently does nothing is how a band gets drawn again. Guarded by `npm run verify:print-paper` §3d, which fails the build if any of it reaches the sheet in any shape. | `public/panels/billdoc.js` → where the verification line was, above `.foot` |
| R51 | Turn the waiter tablet's split entry on the BILLING screen back into a button/tile — it is a small grey line at the bottom of the payment sheet, and a tile is more eye-catching (raised by me on 2026-08-29, because the ⇄ Split payment TILE had been added on 2026-08-21 precisely so split stopped being buried) | **NO** (owner, 2026-08-30): *"yes a small line is ok"* — asked directly, with the trade-off named. He had already designed it that way the day before: *"you can only split with the kot option or small written if you want split on billing at bottom"*. Splitting is not a WAY TO PAY — every tile on that grid answers "how did the money come in?" — so it does not belong among them. It stays a quiet line at the bottom, and it is still one tap from the same screen, which is what the 2026-08-21 change was actually about. Do not re-offer it as a discoverability improvement. | `public/panels/tablet/app.js` → the `.pay-splitline` block in `openPaymentMethodModal` |
| R52 | A **Columns / Bars / Line** picker on a chart — letting a person choose the shape of the owner dashboard's "Who earns more" card, and by extension any new chart (the segmented toggle that used to sit on that card) | **NO** (owner, 2026-07-31, PR #632): *"why option to toggle from bar column and stuff remove option — we have make already till 9 restaurant column then bars, we decide that"*. The right shape is a property of the DATA, not a preference — he had already set the boundary (≤9 restaurants → vertical columns, 10+ → the ranked horizontal bar), so the buttons were three ways for somebody to make the chart worse, sitting where the numbers should be; "Line" was also the same picture as the card beside it. **Never add a chart-type picker to a new chart** — choose the shape from the count and render only that. Pairs with the dynamic-chart rule, which already decides empty vs sparse vs dense automatically rather than by asking. | `components/owner/Charts.tsx` → `COLUMNS_MAX` (the comment there carries this decision). The older `ToggleChart` Bar/Line control on the reports pages is a DIFFERENT, pre-existing component he has NOT been asked about — do not retro-fit this to it. |
| R53 | Put **"Who serves which table"** back on the live floor — a 👥 button on the Table view header, or the same card opened as a popup from the floor (`openSectionsModal()`, built and never wired to anything) | **NO** (owner, 2026-07-31): *"I do not want this option completely on top"*. It had been on the floor header; he moved it to Settings → Access on 2026-07-30 and refused the floor placement outright the next day. Its ONE home is Settings → Access, where the waiter rota belongs. **The reason the popup was written no longer exists either:** it was there because Settings was gated by `edit_settings`, leaving a manager with only `table_assign` no way in — that is now solved properly by the tab opening for `edit_settings|table_assign|print_setup` with every card inside individually gated, so such a manager gets in and sees that one card. Do not re-add it to the floor, the header, or the ☰ drawer. The unreferenced popup, its repaint helper and its `.sec-modal-wide` styles were deleted 2026-09-03. | `public/panels/editor/app.js` → the `REJECTED (owner, 2026-07-31)` note in the openSectionsModal obituary, and the existing note above the floor action buttons ("the 👥 'Who serves what' button used to sit here") |
| R54 | Make **"Resolve all"** SKIP the problems set to come back later — so clearing the board never touches anything parked with "Remind me later" (offered as sweep #8 T18 decision 17, with the argument that parking something means "I have not handled this yet" while Resolve all means "I have") | **NO** (owner, 2026-09-04), asked directly and answered directly: *"resolve all means resolve, you have to one by one resolve all the problems as simple as that"*. Resolve all clears **every** open problem in scope, waiting ones included — the whole point of the button is that he does not have to go and find them one at a time, and a version that quietly left some behind would send him hunting for exactly the ones it skipped. The honesty half was kept instead: the confirm SAYS it will also clear the parked reports and how many (item 8 of the same run). Do not add a skip, a checkbox, or a second "…and the waiting ones?" button. | `app/aevinite/repair/page.tsx` → the `Resolve all` confirm · `app/api/admin/resolve-error/route.ts` → the `body.all === true` branch |
| R55 | Remember the restaurant's table count on the phone so the BILL's table box can still check an upper bound while offline — because a diner who opens the menu with no signal never reads `tableCount`, so the box accepts anything and the refusal only arrives when the saved order finally sends (offered as sweep #9 T3 item 11, 2026-09-14) | **NO** (owner, 2026-09-14), and the reason corrects the premise rather than the cost: *"diner will never type the table no due to qr it will already be there, the type one is for the admin when he can access any table through typing, otherwise user/guest will scan qr and they will be locked to that particular table"*. **A guest does not type a table number.** The QR carries it, `getScannedTable()` pre-fills it and a held session LOCKS the field read-only — so the case this would protect (a guest typing a number above the floor plan, offline) is not a case a guest is in. The typed box is the ADMIN/staff route to reach any table, and staff are not the people this offline queue is for. Do not add a remembered table count, a cached `tableCount`, or an offline upper-bound check to the guest bill — and do not re-report the offline box as missing its bound. **What was kept instead:** the refusal that arrives later now says something true and actionable (item 1 of the same run — `unknown_table` shares `off_plan_table`'s sentence), and the guest doors now draw the same table line the database does (item 10). | `components/CartPanel.tsx` → `cart-table` (the bill's table field, with the REJECTED note directly above it) · `lib/table.ts` → `validateTable`'s `tableCount > 0` bound |
| R56 | Hide the `print_sent_by_admin` rows from the owner's Activity log, the way every other Aevidine action is hidden — move them onto the `admin` panel so `/api/owner/oplog` drops them (T28 sweep #9: 12 such rows on French House's feed, reading *"Aevidine admin — bill #32 for table 26 sent to ZZ-Virt-Counter on sweep-pc"*) | **NO** (owner, 2026-08-20 — the rule these rows were built to keep: *"in the admin panel, in the owner panel, in the manager panel, in the kitchen print panel — everywhere"*; re-decided by T28 on 2026-09-15 on his delegation, after he picked the item). This row is the exception that shows where the admin-is-invisible rule STOPS, and it is the only one. It exists because a physical sheet of paper came out of a machine at a paying client's shop, and `verify:print-helper` §5 requires it in those words — *"the admin viewing a restaurant's panel prints NOTHING at their shop unless deliberately forced, AND THAT IS AUDITED"*. Hide it and paper appears at their counter with nothing at their end explaining it; they would reasonably conclude their own staff did it, which is a WORSE answer than the truth. The three log rows that WERE corrected on 2026-09-15 (a feature switch, the maintenance switch, a test print) are all things Aevidine CHANGED on their screens — the rule was never about an object appearing in their physical world. Do not re-raise it. | `app/api/tablet/[...path]/route.ts` · `app/api/editor/[...path]/route.ts` · `app/api/kitchen/[...path]/route.ts` — each at its `print_sent_by_admin` log line |
| R57 | Removing, hiding, flagging off or otherwise letting the 3D **callout tags** disappear from the 3D dish screen — and the other half of the same instruction, showing those tags INSIDE an AR session | **NO, STANDING** (owner, 2026-09-20): *"the tAGS WHERE MAIN THING AFTER ADDING IT BACK NEVER REMOVE THEM THEY WERE THE MAIN LOOK FOR 3D"* — said after they had been gone for eighteen days for every restaurant except #1, not by decision but as a side effect of closing a folder-name collision (they were stored in a file named after the folder; they now live on the dish row, migration 402). There is no switch for them and there must not be one: if a change means a dish shows no cards, the change is wrong. **The one exception is his own, from the same message** — *"in the android also in ar i don't want tags i just want tags in 3d"* — so they are hidden for the length of a live AR session ONLY, by `body.ar-presenting`, and hidden rather than unmounted, because a card removed from the DOM cannot be brought back by the reveal that owns it. Do not add a feature flag, an admin toggle, a per-restaurant setting or a "tags off while loading" state; do not move them back into `config.json`; do not add a second source "as a fallback". Beware the silent way to break this: `runFullSequence`'s reset clears every card BY SELECTOR and then walks the tag list to restore them, so any reveal that runs with an empty list hides all three for good. Guarded by `npm run verify:3d-viewer` (9 checks) and `.claude/rules/3d-viewer.md`. | `app/globals.css` → `ar-presenting` · `app/view/[folder]/ViewerClient.tsx` → `handleARStatus` · `lib/menu.ts` → `modelTags` |
| R58 | Rebuild the guest BILL line as a flex row with the price centred against the dish's details, put the quantity/Edit controls behind a tap, truncate the options/note with an ellipsis, or print a per-line "⚠ contains …" warning | **NO** (owner, 2026-09-20). He measured it himself first — *"the bill … doesn't look that professional, in the line some are going down"* — and the cause was the price being centred against a column whose height depends on what the guest chose: off his own screenshot the price sat **91px** below one dish's name and **65px** below the next. Shown five formats, he chose the hybrid: *"FROM D4 I LIKE THE THINNESS OF THE ORDER THING AND FROM D5 I LIKE THIS TICKET DESIGN SO DO THAT, IN D4 THERE SHOULD BE EDIT AND QUANTY BUTTON FOR EVERY DISH IF POSSIBLE ADD IMAGE LIKE D3"*, plus *"i want i order to look in line with the alergy and the not that they have added and ALL"* and *"remove it contain milk thing from there like they don't need to be show while that time"*. So the line is a THREE-COLUMN GRID — photo · qty+name+chips+allergen dots · price locked to the name's row — with the options/removals/note on a wrapping full-width line and − / qty / + / Edit / bin on EVERY dish. Measured after: name↔price drift **3px**, one price right-edge, 0 truncated lines, 0 warning lines. The allergen information is NOT lost: the dot on the name line still wears its red ring for anything the guest ticked, and the order-wide warning under the allergy chips is untouched. **EXTENDED (owner, 2026-09-21):** *"you have not shipped the hybrid one properly, in the hybrid one I am unable to see that ticket thing … ship the exact one that we have decided"* — d6 is TWO halves and only the rows had shipped. The lines sit on a TICKET: a card with a torn top edge, a letterspaced "Your order" heading, dotted rules between dishes, and **the totals on the same sheet**. Do not float the lines on the page background again, and do not remove the torn edge or the heading. The totals being on the ticket is not a regression of the sticky-foot fix — it is a better one: they sit near the top of the sheet, visible on open, so nothing needs pinning except Place Order. | `components/CartPanel.tsx` → the `cart-item` block and `.cart-ticket` · `app/globals.css` → `.cart-item` and `.cart-ticket` |
| R59 | Tint the guest menu's `.page` with a second `color-mix` of the accent, or give the brand bar a different background from the page | **NO** (owner, 2026-09-20): *"WHY DOES THE HERO SECTION IS LOOKING LIKE THIS LIKE SEPRATE FROM THE OTHER COLOUR THING SHIOULD SYNC AND ALL IT SHOULD NOT LOOK DIFF"*. `accentBackground()` used `color-mix(in srgb, <accent> 6%, var(--bg))` as its base, but a tenant's `--bg` is ALREADY `color-mix(in srgb, <accent> 6%, #ffffff)` — the accent applied twice. MEASURED on `aevidine` (#e0a920) at 390px: html/body and `.nav` painted `#fdfaf2` while `.page` painted `#fbf5e5`, so the hero sat in a visibly warmer box with a hard edge top and bottom. The base is now plain `var(--bg)` and the brand bar is painted with the identical string, so the whole top of the menu is ONE surface — verified in light AND dark. The radial glow stays; it fades to transparent and makes no edge. If a tenant's canvas should be tinted, tint `--bg` in `accentCanvasCss` so every surface moves together. | `lib/accent.ts` → `accentBackground` · `components/AppShell.tsx` → `navWashCss` |
| R60 | Show the guest's "You might also like" dish photos in a landscape box | **NO** (owner, 2026-09-20): *"in the you might also like this option the images are cut out from top and bottom, fix that, it looks not that good"*. It was the only landscape dish photo in the guest menu — 114×80 against square photos, so `object-fit: cover` kept the middle band and threw away **30% of every picture's height**. Every other dish image in the app is square (`.dish-thumb` 86×86, `.gallery-mode .thumb-wrapper` `aspect-ratio: 1`, search 42, pairing 46, confirm 88, peek 44). `.related-img` is now `aspect-ratio: 1`. | `app/globals.css` → `.related-img` |

## Reversed (the owner changed his mind — newest first)

| # | idea | what changed | code site |
|---|---|---|---|
| — | **"Zero typing": the helper pairs itself and a signed-in person presses Allow in a browser ON THE SHOP'S OWN PC** (mig 368, chosen by him on 2026-08-27 between "type 6 digits" and "click Allow": *"zero typing one, yeah"*) | **REVERSED (owner, 2026-09-13), by mig 380.** Reading the setup back: *"when I'm setting up helper it tells me to login wtf, helper is separate thing it will work on the pc itself then why login and which role to login"*, then the ruling: *"still instead of login make something else otherwise the waiter will also do that printing thing and make completely diff login not this"*, then the shape, in his own words: *"you can generate code for each restaurant from printing menu and like the helper ask for that code and that generated code only works for 10 min."* **His objection is the correct one and it is not about typing:** the Allow page asked a restaurant's counter machine for a STAFF login, and the staff login is the waiter's login, so it could never be the door to deciding where a whole restaurant's paper comes out. A `print_setup` switch was doing the separating, and a switch is not a door. **What he accepted in exchange, knowingly:** typing six characters once per computer, and a code that IS a real secret for its ten minutes (the old one was not) — priced down by a ten-minute life, one use, one live code per restaurant, hashing at rest, a guessing wall, and an audit row that never carries the code. **What stays rejected is the thing that was actually wrong**: no screen may ask a person to sign in ON the computer at the printer, and no door anywhere may mint a standing printing credential to be carried to a machine by hand (that was `newcode`, retired at mig 368 and DELETED at mig 380 along with `POST /agents` and the panel's `this-computer` create branch). | `lib/printSetupCode.ts` (which replaced `lib/printPair.ts`) · `app/api/print-agent/[...path]/route.ts` → `pair/claim` · the deleted `app/pair` + `app/api/pair` · `supabase/migrations/380_a_setup_code_replaces_the_allow_page.sql`. Guarded by `npm run verify:print-helper` (11 sabotage cases) and `verify:printing-sweep` sections 6c and 24. |
| — | The "how to print" setup guide inside the KITCHEN panel, and a mode toggle on the Printing board | **REVERSED (2026-08-31), and it was never a `##` row — recorded here because two guards were asserting it.** Previously (2026-08-29): *"inside the kitchen screen also kitchen panel also there is how to print… remove that completely"* — right at the time, because a cook's screen could not BE the printer (the admin named one person), so the guide was instructions for something the screen would refuse. Latest (2026-08-31): *"kitchen panel will always be on and there will be guide for it."* The model changed underneath the old rule — the kitchen screen is now the DEFAULT printer for kitchen slips, with nobody named and nothing switched on — so the guide is instructions for the machine the sheet is standing on. Same day he removed the mechanism toggle: *"in admin panel also we don't need toggle."* Both guards were **inverted, not deleted**: the kitchen panel must now REACH the guide, and neither board may render a mode toggle or a switch-confirmation strip. **NARROWED AGAIN (owner, 2026-09-13):** *"I want both separate — on top of printer there should be 2 menu, one for screen printing by chrome kiosk and one for helper, and they should have colour of red or green according to they are on and off."* What stays rejected is the **stored** mode (`settings.modules.printing.mode`, `writeMode`, a `mode` verb on either route, a switch-confirmation strip) — that is the thing that could disagree with the paper. What is NO LONGER rejected is showing one way at a time: the admin board now opens on two status cards that are DERIVED from the routes, post nothing, and can both read green at once. `verify:print-helper` asserts all of it (no stored mode · the picker exists with ON/OFF words · it posts nothing · both setups reachable). | `public/panels/kitchen/app.js` → the 🖨 sheet's "Set this screen up to print" row · `scripts/verify-print-queue.mjs` · `scripts/verify-print-helper.mjs` |
| R8 | Make the manager floor's "→ N more" chip say how many of the OFF-SCREEN tables need attention | **MOOT** (2026-08-15) — not a change of mind, the thing it was about was **deleted**. He refused the smarter chip on 2026-08-11 because waiter sections already decide who sees which tables. On 2026-08-15 he removed the sideways scroll entirely ("there shouldn't be horizontal scroll anywhere") and set fixed counts per screen — 2 on a phone, 4 turned, his number from ~10 inches up — so no table is ever off the right edge and the chip, its wrapper and its measuring pass are gone. Nothing to re-suggest and nothing left to guard. | *(was `public/panels/editor/app.js` → `syncFloorMore()`, removed)* |
| Billing reminders in Aevidine | 2026-07-07 | *"don't add billing reminder **because billing thing is not even added**"* — a SEQUENCING rejection, not a permanent one: you cannot remind someone about a billing system that does not exist. Worth re-asking the day Aevidine has subscription billing. Recovered 2026-09-03 from a capture whose nightly refine never ran. |
| — | **"The owner panel holds NO printing controls"** — a rule written into `app/owner/settings/page.tsx` on 2026-08-18 and kept ever since: the owner is not standing at the printer, the two switches are the admin's, and the per-device answer belongs to the computer the printer is attached to. So that card was read-only, and it named only the kitchen-slip printer. | **NARROWED (owner, 2026-09-14).** His words: *"On the manager panel and on the owner panel, when the helper is on, in the settings of both panels you could able to see that everything is connected and everything is live. And if not connected, you could able to see. … They can also test from there — print a KOT, print a bill, or print a banquet bill."* **The rule itself is intact and still enforced:** the owner panel changes NO setting — no route, no switch, no entitlement, no row. What it gained is a STATEMENT (the three papers, live, in plain words, from the same `paperStatus()` the manager board reads) and one ACTION that alters nothing: a test print. The owner is the person standing at the counter asking "is the printer working?", and until now the only way to find out was to ring up a real order. What stays rejected: putting the routes, the entitlement, `auto_print_kot`, or a "which screen prints" picker on the owner panel — those are still the admin's, and the card still says so in words. | `app/owner/settings/page.tsx` → the `KITCHEN PRINTING` block and the `REJECTED` note above it · `app/api/owner/printing/route.ts` → the note over `POST` |
| — | **The "Kitchen · KOT printing" card on the manager's Settings** — an auto-print toggle, a "🖨 Preview a sample KOT" that opened Chrome's print dialog on that screen, and a paragraph telling the reader to launch Chrome in kiosk-printing mode | **DELETED (2026-09-14), under his standing rule** *"a new way replaces the old one"* (2026-08-29) and his own description of this exact fault: *"there is two printing things, one is working and one is just showing."* Its own comment claimed it was *"hidden from everyone in this panel by his 2026-07-31 decision"* — **it never was**: `settingsSections()` hides a row only when `managerSettingsOff()` names it, and that function can only ever return `tables · users · access` (`lib/accessTree → MANAGER_SETTINGS`); the string "kitchen" does not appear in that file at all. So every manager had been seeing both cards since the Printing board shipped. Three reasons, not one: it is the old way beside the new one; its toggle wrote `auto_print_kot` with **no permission check**, so a manager deliberately not given "May set the printers up" could still stop the kitchen printing; and its instructions were wrong, naming one of the two ways as if it were the only one. Its live control is the Kitchen slips line on the Printing row (the SAME decision — `syncKotSwitch`), and its sample print is replaced by the three Test buttons there, which print through the real route on the real paper. Do not put a printing card back on that list. Guarded by `npm run verify:print-helper`, which also fails if any settings row CLAIMS to be hidden by a permission that cannot hide it. | `public/panels/editor/app.js` → the obituary in `SETTINGS_SECTIONS` and the one where `previewSampleKOT()` was |
| — | **"May set the printers up (from their own computer)"** — the `print_setup` permission (mig 367, his own ask 2026-08-27: *"that device is connected to the printer, so it will be easy for THAT device to set up the printer, instead of the admin"*). It gave ONE named manager a Printing page in their own Settings that could register that computer, show its ten-minute setup code, rename or unlink it, and choose which printer got the kitchen slips, the bills and the banquet sheets. | **REVERSED (owner, 2026-09-14).** Asked directly which of his two rulings won — this permission, or his newer *"in the manager panel and the owner panel there shouldn't be able to change it, just for them to see that a computer is online, printer is online, whichever printer we have set up all online, you are good to go"* — he answered: **"That setup will be done by me only, and maybe I was talking about the screen."** So the permission row is DELETED from the Access screen rather than switched off (a switch that changes nothing when you turn it on is the dead switch the access rebuild existed to remove), the panel's four setup verbs (`setup-code`, `this-computer`, `unlink`, `route`) are DELETED from `app/api/editor` rather than gated — a verb that still mints a printing credential is a door, and a door nobody can currently open is still a door — and `maySetup` is a constant `false` on that route. **What the manager panel keeps, each for its own reason:** the three papers shown LIVE with a **Test** print (that is the ask); the computers listed read-only with whether each is awake; the **kitchen-screen launcher**, which is the "screen" half he named in the same breath and which holds no secret and moves nobody's paper; and `print_clear`, a different amount of trust he asked for by name one day earlier. **What the owner panel keeps:** the same live rows and the same Test, and still no control of any kind. Do not put a printer control on a restaurant's panel again. Guarded by `npm run verify:print-helper` and `.github/scripts/verify-unowned-routes.mjs`. | `lib/accessTree.ts` → the obituary where the row was · `app/api/editor/[...path]/route.ts` → `const maySetup = false` and the `b0 !== "test"` refusal · `public/panels/editor/app.js` → the status-screen note in `formPrinting` |
