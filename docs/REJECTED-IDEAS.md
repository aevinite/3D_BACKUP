# REJECTED IDEAS — things the owner has already said NO to

**Read this BEFORE suggesting any improvement, and before "fixing" anything that looks odd.**
An idea on this list has already been decided. Re-suggesting it wastes the owner's time and — worse —
invites someone to "fix" a thing that is deliberate.

> **The rule (owner, 2026-08-07):**
> *"everything I reject also should be written in the comment in the code."*
> *"While suggesting something and doing, at that time, you have to make sure I have already said no for it. So you don't repeat the same thing again."*
>
> So every rejection lands in **two** places: a row here, **and** a comment at the exact line in the
> code someone would otherwise change. `npm run verify:rejected` fails if a rejection listed here has
> no comment at its code site.

## How to add one

1. Add a row to the table below with the date and the owner's own words where you have them.
2. Put a comment at the code site starting with `REJECTED (owner, <date>):` and one line of why.
3. Run `npm run verify:rejected`.

Never delete a row. If the owner changes his mind, move it to **Reversed** at the bottom with the new
date — the history is the point.

---

## Rejected

| # | idea | owner's decision | code site |
|---|---|---|---|
| R1 | Make FREE table tiles shorter / denser so they stop being mostly blank space | **NO** (2026-08-07): *"I want free table to look empty only"* — a free tile looking empty is the design, not a defect. Do not shrink it, do not fill it, do not "use the space". | `public/panels/tablet/app.js` → `tileHtml()` |
| R2 | Hide the "Your tables 1–30 · 30 tables" section strip when the waiter holds the whole floor | **NO** (2026-08-07) — not wanted. | `public/panels/tablet/app.js` → `renderMySection()` |
| R3 | Collapse an EMPTY kitchen column ("New — Nothing here") on a phone to reclaim ~101px | **NO** (2026-08-07) — not wanted. The three columns stay whatever is in them. | `public/panels/kitchen/app.js` → `renderColumns()` |
| R4 | Widen the 💳 Mark-paid / ⏻ Close tap targets on a floor tile (they are 22–25px) | **NO** (2026-08-07): *"Don't do fix number four"* — the size is accepted; the two-step confirm is the answer to mis-taps. | `public/panels/tablet/app.js` → `tileHtml()` acts row |
| R5 | Give the kitchen a "ready for N minutes" ageing signal on the Ready column | **NO** (2026-08-07) — not picked. The cooking-age warning is enough. | `public/panels/kitchen/app.js` → `ageClass()` |
| R6 | Restate `TILE_MIN_PX` (the 44px tappability floor) in the waiter tablet alongside the per-row constants | **NO** (2026-08-07) — not picked. | `public/panels/tablet/app.js` → `FLOOR_PER_ROW_*` block |
| R7 | Any kind of PROFILE on the kitchen panel — including the one-time "👋 Finish setup" card (settled 2026-08-08: `LFH_NO_PROFILE_AT_ALL`) — a profile page, a pay record, a "👤 Profile" button, a personal-settings door | **NO, STANDING** (owner, 2026-07-29, re-confirmed 2026-08-05 and again **2026-08-07**: *"Kitchen panel will not have profile or stuff like that. I have already told this"*). This was asked twice and offered back to him a third time as an "improvement" — that is the mistake this whole file exists to stop. The kitchen keeps its login, its PIN and its action log; it has no profile of any kind and no button that says Profile. | `lib/staffProfileShared.ts` → `PROFILE_ROLES` · `public/panels/kitchen/app.js` → `LFH_SUPPRESS_SETTINGS_BTN` |
| R9 | Show how long a party has been merged ("⇄ with T7 · 40m") so a forgotten merge stands out | **NO** (2026-08-11): *"I don't implement improvement number three. We will ignore improvement number three. We will skip it."* | `public/panels/editor/app.js` → the `ft-merge` chip in `floorTileHtml()` |
| R10 | Put the money beside the floor header's "To pay" count ("To pay 2 · ₹1,659") | **NO** (2026-08-11): *"We don't need improvement number six."* The count stays a count. **Where the line is (2026-08-20):** he ASKED for a money split on the admin console's Bills → CLOSED UNPAID tile (*"we have 2 option in close out also … one with the food was made to could as loss one is food was not made and cancelled so no loss detected"*) and it was built. Different screen, different reader, different question — an oversight ledger read after service, not the live floor a manager works off during it. This row still stands for the manager floor; do not read the admin change as permission for it. | `public/panels/editor/app.js` → `floorStatsHtml()` |
| R11 | Stop the floor header's 🧾 KOT ▾ / ⚡ QO/P pair wrapping onto a second line (it costs ~60px above the tables at 1280px) | **NO** (2026-08-11): *"Don't solve p four. P four is perfectly fine. We don't need that."* The header's current layout is accepted as it is — do not re-flow it to keep the buttons on the title's line, and do not raise it again as a finding. | `public/panels/editor/style.css` → `.floor-head` / `.floor-head-acts` |
| R12 | Make the dish PRICE (and the muted meta line, and a sold-out dish's greyed price) obey their CSS colours by beating the `a *, .item-card-link * { color: inherit !important }` reset — reported by guest sweep T1, 2026-08-12, measured: the price computes as `--text` while `--accent` sits unused on the same element | **NO** (owner, 2026-08-12): *"I don't think P1 is a problem like I don't know like what are you saying like it's not a problem … I don't think it's required … because bill will be black and white colour will stand out."* The plain-text price is accepted on every restaurant, in both skins. Do not add `!important`, do not re-report it. | `app/globals.css` → the comment above `.dish-price` |
| R13 | Make the guest search-suggestion panel use the restaurant's own colours instead of the hardcoded `#1b120c` brown + `rgba(212,165,116,…)` gold — reported by guest sweep T1 as a white-label leak, measured identical on green-bowl, sakura-sushi, spice-route and aangan | **NO** (owner, 2026-08-12): *"P3 is completely trash. I want … all the panels and all that stuff write in a code as a comment that should be this colour only like golden and little french house theme one."* GOLDEN + the little-French-house theme is the intended house style here and on the panels. Do not swap these for `var(--accent)`, do not re-report it. | `app/globals.css` → the comment above `.search-dropdown` |
| R14 | An automatic translator for DISH names, so a guest browsing in Hindi can search a dish in Hindi (offered as sweep idea I6, 2026-08-12 — the third time this has been raised) | **NO** (owner, 2026-08-12): *"we will not do auto translator … I will tell you only, I will add the translated word, and we will fix that and keep that for the menu … So don't show it as a problem again."* He supplies the words himself when he wants them; they go in `searchAlias`. Never build it, never offer it. | `lib/menu.ts` → the dish-translation decision block |
| R15 | Translate the three remaining English app labels on the guest menu — the "Maximum 99 per dish" toast, the "<dish> added / tap to view your bill" toast, and the offline strip ("Your order is saved and will send by itself") | **NO** (owner, 2026-08-12, asked directly): *"No — English is fine for these."* Distinct from R11: these are our own labels, not dish names. Leave them as English literals; do not move them into `lib/i18n.ts`. | `components/FoodCard.tsx` → the 99-cap toast · `components/OfflineNotice.tsx` → `msg` |
| R16 | Give the GUEST menu a longer offline window than the 2 hours staff get, so a diner who returns after a few hours still sees the menu they were reading (T1 improvement 7, 2026-08-12) | **NO** (owner, 2026-08-12): *"there is no off-line limit for diner. Diner should be online."* The window exists for staff continuity; one number covers everyone. Do not add a per-family window, do not re-report it. | `public/sw.js` → the comment above `MAX_STALE_MS` |
| R17 | Refresh the currency conversion rates from a live FX feed instead of the hand-edited table (T1 improvement 12, 2026-08-12) | **NO** (owner, 2026-08-12): *"leave it and make sure you don't show as improvement or error again … this will be like this only."* No scheduled fetch, no stored per-restaurant rate. Not a finding. | `lib/format.ts` → the comment above `CURRENCIES` |
| R18 | A SECOND health check that says WHICH part is broken — `/api/health/deep`, reporting the database, file storage and the live-updates setting separately, watched by an hourly UptimeRobot monitor (shipped 2026-08-12 as improvement I13, removed 2026-08-13) | **NO** (owner, 2026-08-13): *"then we don't need it remove it"* — said after the setup was explained: it needs a second monitor created in HIS UptimeRobot account, and he does not want to run it. A health page nobody watches is dead code, so the page went too. **Do not re-add it, and do not "improve" `/api/health` by folding storage / config checks into it instead** — that is the same idea wearing a different hat, and it would also make the 5-minute probe expensive, which is the thing it was deliberately trimmed for on 2026-08-06. `/api/health` itself STAYS exactly as it is: the panels' offline screen (`public/offline.html`) uses it to tell "this device lost signal" apart from "the restaurant's server is down", and the test suite pings it. | `app/api/health/route.ts` → the header comment |
| R19 | Change the two sign-in screens from `100vh` to `100dvh`, so the card stops sitting low on Android and the on-screen keyboard stops covering "Sign in" | **NO** (owner, 2026-08-13) — raised by the T12 phone sweep; he judged it not a problem in practice: *"i don't thing 7th is problem"*. | `app/login/LoginForm.tsx` → the `<main style={{ minHeight: "100vh" }}>` (same call covers `app/staff-login/page.tsx`) |
| R20 | Make the owner top-bar chip name the PAGE you are on instead of always reading "Owner overview" | **NO** (owner, 2026-08-13): *"why you need to know where we are you can click 3 line and know or on top there is many written like heading and stuff"*. | `components/owner/OwnerShell.tsx` → the `.owx-scope` chip |
| R21 | Give the KITCHEN panel the same plain-words error wording the manager and waiter panels have (a shared `errText()` that tells "no internet", "the system is very busy" and a clash apart, instead of `Failed: <code>`) — offered as sweep idea I6, 2026-08-12 | **NO** (owner, 2026-08-13): *"i don't thing i6 is require mainly in kitchen panel is only use for kot print"*. The kitchen screen's job is the KOT, not editing values, so the wording gap it would close is one that screen never reaches. Leave the toasts as they are; do not lift `errText()` into a shared file for the kitchen's sake, and do not re-report the kitchen as the odd one out of the three panels. | `public/panels/kitchen/app.js` → the comment above `act()` |
| R22 | Widen the tap area of the ✕ that closes the full-screen panel sheets (⚡ QO/P, ＋ Take order, Add-a-dish, Discount) — it measures 25×22px at 360, against a ~44px guideline, and sits in the corner a right thumb reaches last (T12 phone sweep, 2026-08-13, with a zoomed screenshot) | **NO** (owner, 2026-08-13): *"x is fine"* — said after looking at the screenshot. The phone's BACK button also closes every one of these sheets, so nobody is trapped. Do not pad it, do not resize it, and do not re-report the panel close ✕ as a tap-target fault. | `public/panels/editor/style.css` → `.tbl-modal-close` |
| R23 | RAISING the guest menu's half-finished translation as work — either "the ordering screens (cart, table gate, order strip, bill, waiter-call, rating) are still hardcoded English" or "Arabic renders as disconnected letters in the animated hero" (T15 wording sweep findings F1 and F2, 2026-08-13) | **NO — it stays as it is, and it stays off the list** (owner, 2026-08-14): *"you don't have to remove option … what I wanted is just don't suggest that improvement right now."* **The language picker and the currency picker REMAIN on the guest menu** — an earlier reading of this removed them and that was reverted the same day; the Access → Menu → Format → Languages/Currencies Single/Multiple control is live and meaningful. What is rejected is bringing the unfinished translation back to him as a problem or an improvement. Both facts are true and both are parked: only 4 files call `useTranslation()`, and per-grapheme `inline-block` spans cannot join Arabic. Do not re-report either, and do not start on them unasked. Extends R14 (no auto-translator) and R15 (English is fine for our own labels). | `lib/i18n.ts` → `useTranslation` |
| R24 | Give `/login` and `/staff-login` the SAME brand mark — `/login` draws a text ✦ while `/staff-login` uses the real SVG mark `/brand/aevidine-mark.svg` | **NO** (owner, 2026-08-14): *"for improvement 4 idk whats fix like we don't need/require one i guess"* — offered by the T11 visual sweep as a consistency fix; he does not want it. The two sign-in screens keep their own marks. Do not re-offer this as a "one product" or "brand consistency" improvement. | `app/login/LoginForm.tsx` (the ✦) · `app/staff-login/LoginForm.tsx` (the SVG mark) |
| R25 | Add the missing colours to the floor legend — a tile can turn AMBER (new order) or PINK (on the pass) with no legend entry, so the strip explains 3 of the 5 colours | **NO, and he said it TWICE** — the strip stays short. The state word is still in every tile's tooltip and in the table's own detail popup, so nothing is unknowable; the legend is deliberately not a full key. **Refused a THIRD time (2026-08-17)**, when the T5 sweep listed it for him again: *"whatever you left for my decision, I don't think we don't need that, so we will not do any of that stuff"*. Recorded here 2026-08-15 after the T11 sweep nearly re-reported it as a bug: the decision existed only as a code comment, which is exactly how a settled thing gets offered back. | `public/panels/editor/app.js` → `const LEG` · `public/panels/tablet/app.js` → `const LEG` |
| R26 | Make the printed documents fit a 58mm roll — the KOT's body is a fixed 280px built for 66/80mm paper, so on 58mm the table name, the "— no <ingredient>" line and the ⚠ AVOID allergy box run past the paper edge (measured by T15, 2026-08-15, by rendering billdoc's real output at both widths; the BILL itself is clean at 58mm and 80mm) | **NO — keep it as it is** (owner, 2026-08-16): *"for different restaurant we use different printer and all that stuff so right now keep IT AS IT IS."* Each restaurant is set up with a printer that suits it — the flagship and Aangan both run POS-80 (80mm) — so the document is built for 66/80mm on purpose and the right answer to a narrow roll is the right printer, not a document that tries to fit every width and reads worse on all of them. Do not add responsive print CSS, do not shrink the KOT body, and **do not re-report the 58mm overflow as a bug**. If a client ever arrives with 58mm hardware, he will say so and it becomes a real piece of work. | `public/panels/billdoc.js` → `kotDocHtml` |
| R27 | Give the restaurant a way to DELETE a bill — the grantable "Delete a bill" permission (owner or manager) | **NO** (owner, 2026-08-16): *"I don't want to give permission to the restaurant owner to delete the bill because he will fake the bill and delete the bill … so what can we do that the restaurant doesn't cheat, and at the same time we can keep the track?"* Cancel is the only route out of a bill for anyone at the restaurant; a cancel is a ₹0 sale that stays visible with its reason, person and time. The Aevidine admin console keeps a soft delete for support work. Do not re-add the row. | `lib/accessTree.ts` → the note where the `delete_bill` row was · `app/api/editor/[...path]/route.ts` → `canDeleteBill()` |
| R28 | Put a CEILING on the guest menu's background 3D preload — a cap, a first-N window, an "only what's on screen" queue, a data-saver mode or a per-restaurant setting (guest sweep T1 improvement I2: a restaurant modelling 30 dishes would have every diner's phone pull ~60 MB for dishes nobody opens) | **NO** (owner, 2026-08-16): *"don't do the sixth one any time soon like in the code, also reject that it is rejected by me."* A diner must never wait when they tap a dish. Measured, so nobody re-discovers it as new: 2 GLB requests on a French House menu open (it has 2 such dishes; Aangan has none), and `lib/modelLoader` already evicts past 40 MB. Do not re-report the unbounded preload as an egress or performance problem. | `components/MenuView.tsx` → the preload queue, at `const isAllView` |
| R29 | Make the call-waiter bell get out of the way when it has nowhere clean to sit — stand down, hide while searching, fade, yield its taps, step sideways, or raise `BELL_MAX_LIFT` (guest sweep T1: with the search suggestions open the bell overlaps a dish's "+" by 22px and `elementFromPoint` at the button's own centre returns `chef-call`; with every category folded it overlaps a `.cat-group-head` by 48px — both restaurants, 360x780) | **NO** (owner, 2026-08-17), after being shown the measurements and the built fix, which was reverted: *"i want like previous bell of call waiter should be stuck at his place we can scrool and click the thing make sure don't change that again."* The bell is a fixed thing in a fixed corner a guest can always find, and scrolling moves the page under it. Do not re-report the overlap as a fault. | `components/MenuView.tsx` → `settleBell()`, at the "NOTHING within reach is clean" fallback |
| R30 | Give the guest hero a NEUTRAL TRANSLATED fallback — a pair of keys in `lib/i18n.ts` so a restaurant with no custom hero greets a Hindi or Arabic diner in their own language instead of the English "Welcome" / "Our Menu" | **NO** (owner, 2026-08-17): *"i want english only for all."* Extends R15 and R23. The English literals stay for every restaurant and every language. Do not add the keys, and do not use `t.greeting`/`t.heroTitle` there — those hold restaurant #1's own copy ("BONJOUR", "All-Day Café & Bakery") and leaking them onto another tenant is the bug that line exists to prevent. | `components/MenuView.tsx` → the `<HeroTitle>` in the hero block |
| R31 | A short "Order" label on the floor tile's take-order button, for the band where "＋ Take order" cannot fit (added by the T3 sweep 2026-08-06, restored by the T5 sweep 2026-08-17 after a later change had made the rung unreachable) | **NO** (owner, 2026-08-17), shown the real tile wearing it: *"instead of order is written that should be just a plus icon, nothing else, and it should stay like that"* — and about the tile as a whole: *"at the end of the day, UI should stay like this. I don't care if you increase the size of or maybe whatever you do. But make sure UI should stay like this."* So the button has exactly TWO faces: "＋ Take order" while it fits, and a bare **＋** when it does not — on a crowded tile, on a dense floor, and on a finished table. The SIZE may change; a third, worded face may not. Do not re-add one, and do not report the bare ＋ on a narrow tile as a bug. | `public/panels/editor/app.js` → `floorTileHtml()` · `public/panels/editor/style.css` → `.ft-take-x` |
| R32 | Put a one-tap 🍽️ Serve-all on the floor tile of a table whose food is all cooked, so serving stops costing two taps (offered as T5 improvement I3, 2026-08-17) | **NO** (owner, 2026-08-17): *"whatever you left for my decision, I don't think we don't need that, so we will not do any of that stuff."* Serving stays two taps — the tile, then 🍽️ Serve all inside it. This also protects the tile's action row, which he has already ruled on twice (R4, and "never make take order button small"): a fifth control on an 89px tile would have to take room from the take-order button. Do not add a serve control to the tile. | `public/panels/editor/app.js` → `floorTileHtml()` |
| R33 | A way out of a table that has a bill but no live tickets (every ticket cancelled) — either a line naming what to do next, or a ⏻ that ends an empty party (offered as T5 improvement I5, 2026-08-17) | **NO** (owner, 2026-08-17): *"whatever you left for my decision, I don't think we don't need that, so we will not do any of that stuff."* The screen already says what is true — "Nothing on this table — N cancelled tickets, kept in Bills" — and that is where it stops. Nothing is lost or double-charged: the bill is in the record and the table is usable. Do not add a prompt, and do not add a close button for an empty party (no table ends itself — mig 254). | `public/panels/editor/app.js` → `voidNote` (in `tablePanelParts()`) |
| R34 | Make the owner's **Pay Later** page hide itself and say "Pay Later isn't enabled for your restaurant" when the pay-later module reads off — offered and built as sweep-6 item 4 (the route already sends `moduleOff`, and the Customers screen shows exactly that sentence in the same situation) | **NO** (owner, 2026-08-18), and it was reverted the same hour: *"If there is no person in pay later, then you shouldn't shut it off. This is the wrong thing. Like, if the feature is on by me, it will stay on."* The page's job is to show the book, never to decide whether Pay Later exists. Do not re-add the branch, do not grey the tiles out, and do not re-report the unread `moduleOff` flag as a bug. **What he DID want from that area is separate and is built: the pay-later option must not appear at payment time when the module is off** — that is `khataOn()` / `tabletKhataOn()` in the two panels, checked live 2026-08-18 and correct. | `app/owner/khata/page.tsx` → the comment above the `<h1>Pay Later</h1>` |
| R35 | Give the credit book an **"oldest first"** ordering control, so an owner can work the oldest tabs down (sweep-6 item 13) | **NO** (owner, 2026-08-18): *"We don't need the thirteenth one."* The book stays ordered by how much is owed. It could not have been done honestly from this screen in any case — the list is bounded to the biggest 500 debts, so "oldest" of that slice is not the oldest on the book; a truthful version would have to move the ordering into `lfh_khata_outstanding`. The per-row age colouring (amber past 30 days, red past 60) is the part he kept. | `app/owner/khata/page.tsx` → the comment above the Pay Later search box |
| R36 | Show the owner WHICH owner-panel sections are switched OFF for him — the ✓/✗ chips on Settings → "What's enabled" (T13 sweep offered it as problem 3: the card listed 6 of 9 sections, so with Menu switched off the sidebar item vanished, /owner/menu said "ask your administrator", and this card said nothing) | **NO** (owner, 2026-08-18): *"owner can't know which option are not given to them only admin should know that"*. What is withheld from a restaurant is Aevidine's business alone — a ✗ tells the owner a feature exists that he has not been given, which is an invitation to ask for it. The card now lists ONLY what is ON and shows no off-state at all. Do not re-add the ✗, do not grey a withheld section, do not add a count ("6 of 9"), and do not re-report the missing ones as a gap. **What DID survive:** the map holds all nine sections, so a section he genuinely HAS can never be missing a chip — the card used to under-report what he had, which is the opposite fault. | `app/owner/settings/page.tsx` → the `REJECTED (owner, 2026-08-18)` note above `SECTION_LABEL` |
| R37 | Brand a SECOND copy of a guest's bill — the "Reprint · Duplicate" band across the top of the printed bill (shipped 2026-08-17, mig 333) | **NO** (owner, 2026-08-19): *"in the printing bill I don't even want the reprinted bill shown in the bill … and make the guard also in code like never change that to reprint thing and stuff"*. A guest asking for their bill again is service, not an incident — branding it makes the guest's own copy look like a lesser document. Do not re-add a band, a watermark, a "(copy)" in the document name, or a small-print line. The `reprint` flag no longer exists on bill data at all, so a caller cannot pass one. **What stays:** the KITCHEN TICKET keeps its big `*** Reprint · Duplicate ***` banner (his own ask, 2026-08-04, re-confirmed the same day — *"bill only keep kot banner"*), because a cook who mistakes a duplicate for a fresh order cooks the food twice. Guarded by `npm run verify:bill-reprint`. | `public/panels/billdoc.js` → `vband` (where the band was), `public/panels/editor/app.js` → `billPrintLabel()`, `public/panels/tablet/app.js` → `printTableBill()` |
| R38 | Record a reprinted bill anywhere — an Audit row, an Activity-log line, a counter, or a badge | **NO** (owner, 2026-08-19): *"I don't want reprinted bill shown anywhere like on audit also because it's not any kind of problem which needs to be audited"*. `sessions.bill_printed_at` (mig 333) survives for ONE purpose only: the button reads "Reprint" instead of "Print" once paper exists. It writes nothing to the record. **The other half stays exactly as it was:** *"reopen will be noted in the audit also"* — reopening a bill still requires a reason, is still audited (`invoice_voided`), the new invoice number is still logged, and the before → after row is still written. Guarded by `npm run verify:bill-reprint`. | `app/api/editor/[...path]/route.ts` → `sessions/:id/bill-printed` |
| R39 | Ask WHY on the print path — the "This invoice was voided. Re-issuing assigns a NEW number — why are you re-issuing it?" prompt that appeared when a reopened bill was printed | **NO** (owner, 2026-08-19): *"when I click print bill why it show like I wanna reopen the bill and stuff, it should not happen like this … reprinting should also not ask any question"*. The reason had already been given, one step earlier, in the reopen picker — which requires it and records it — so this asked the same question twice and made pressing Print feel like reopening the bill. The server now takes the reopen's own reason as the re-issue reason, so nothing is lost from the record. Do not add a prompt, a confirm or an "are you sure" to any bill-print path. **What stays:** the "who is this bill for?" sheet after a reopen, pre-filled — his words, same day: *"reopening can also change name and number … when you click print after reopen it asks for no and name again and the old one is autofilled"*. That is the bill's own field, not a why-question. | `public/panels/editor/app.js` → `generateInvoice()` |

| R40 | Enlarge the connection pill (the green **Live** chip) on the panel top bars into a 44px tap target — it measures 89 × 24.5 on the kitchen bar at every width, the last control there under the finger target once the rest of the bar was fixed | **NO** (owner, 2026-08-20): *"do your recommendation"* — said to a decision list whose recommendation was to leave both this and R41 exactly as they are. He was given both ways to fix it and the cost of each. Making the chip genuinely 44px tall grows it in **three** panels at once (kitchen, tablet and manager all load `connbadge.js`); keeping it visually identical means extending its invisible hit area over the 🔔 beside it, which trades a harmless miss for a harmful one. A mis-tap here opens or closes an information popover — the cheapest mis-tap on the screen. **Extends R4 and R22**: the third time this same judgement has been asked for. Do not re-measure it as a fault, do not pad it, do not report it in a sweep. | `public/panels/connbadge.js` → `.lfh-conn` |
| R41 | Give the kitchen top bar's 🚫 Sold out (36 × 44) and ⋯ menu (33 × 44) buttons a 44px **width** on a phone — they already meet the 44px height at every width | **NO** (owner, 2026-08-20): *"do your recommendation"* — same decision list, same answer. The height fix (2026-08-19, at every width) stands and is guarded by `npm run verify:ready-tile`; only the WIDTH is short, and only under 760px — above that these two measure 108px and hidden. This bar has wrapped to **four rows at 360px** before, and widening its buttons is precisely what brings that back: a bar that wraps costs a cook more than a narrow icon does. **Extends R4 and R22.** Do not add a `min-width` to the phone block. | `public/panels/kitchen/style.css` → `.top-actions .btn` |
| R42 | The four-pill status strip across the top of **System health** — `Database 26ms · 9 restaurants live · 1 staff online now · 1 open issue` | **NO** (owner, 2026-08-20): *"we don't want the top thing. Shows four things here is the image"*, sent with a screenshot of that exact strip, and in the same breath: *"you can optimize the whole system health care also. Right now, I'm not able to see, like, properly, like, how the thing is in system health."* A pill is a number with no sentence — "26ms" only means something to someone who already knows what good looks like — and this was the only summary the page had. It is replaced by a plain one-line verdict plus a labelled list where every check says what it means and where to go. Do not re-add a pill strip, a chip row, or a KPI band to this page. **Nothing was dropped:** the database timing and the complaint count, the only two facts the pills carried that were not already further down, are rows in that list. | `app/aevinite/health/page.tsx` → the `REBUILT 2026-08-20` note in the header |
| R43 | An amber warning light for an open complaint — the `1 open issue` pill going amber on System health whenever anything is unanswered (offered as T17 decision 16) | **NO** (owner, 2026-08-20): *"this is not need"* — his answer to that decision, given for the whole strip R42 removes. It was also the right call on its own merits: with ten restaurants there is nearly always one open complaint, so this light was amber on almost every load, which is how a person is taught to ignore amber. The Complaints row now carries a plain dot and a link straight to Repair; only an unusual pile-up (10 or more) is worth a colour. Do not colour a single open complaint, here or anywhere. **Same family as item 14 and R40/R41:** a warning that is always up is not a warning. | `app/aevinite/health/page.tsx` → the `NOT AMBER FOR ONE OPEN COMPLAINT` note on the Complaints check |
| R44 | Free a cancelled bill's number so the day's series has no gaps — "give the number back" when every order on a table was cancelled, or when a table takes a number and never orders | **NO** (owner, 2026-08-16, re-confirmed 2026-08-22 after he asked what the law wants): gaps are correct; **reuse** is the fault. CGST **Rule 46(b)** wants a consecutive serial unique for the financial year, **Rule 49** puts the identical requirement on a **Bill of Supply** — which is the document this app prints for a composition-scheme restaurant, so for those clients `bill_no` is the *statutory* number on the paper and not an internal reference — and **Rule 56** requires a cancelled document to be retained WITH its number. Hand that number to the next customer and two documents share one number, which an audit reads as *"a sale was deleted and the next one slid into its slot"*: the suppression pattern `docs/COMPLIANCE-GUARDRAILS.md` exists to prevent. A gap is explainable in one sentence; a duplicate is not explainable at all. **Measured 2026-08-22:** 840 sessions hold a number whose every order was cancelled and 250 hold one with no order — untidy, and not a fault: the Audit, the admin bill ledger and the signed chain (mig 332) account for all of them. Related: the printed sheet already shows the internal number ONLY when there is no invoice number to show instead, so the gappy one stays off a customer's paper without any counter changing. | `lib/billLedger.ts` → `rollUpBill()`, `public/panels/billdoc.js` → `billNo` |
| R45 | Move a section's remaining narrative out of `CLAUDE.md` into `docs/CLAUDE-DETAIL.md` to make headroom under the 24,000-byte guard — the file sits 56 bytes below it, so the next rule anyone adds turns the checks red | **NO** (2026-08-22): *"i don't want them"* — offered as a 🟡 decision at the end of the T29 sweep and refused. The budget stays, the sections stay where they are, and nobody spends an afternoon re-filing rules. **And raising the number is not the loophole** — the guard's own comment records that raising it is how the last budget stopped working. When it does trip, whoever is adding a rule makes room inside their own edit and says so. | `scripts/verify-doc-pointers.mjs` → `BYTE_BUDGET` |
| R46 | Make the print helper's done/failed report refuse a job that NO computer has claimed, so it matches the document fetch in the same file (drop the `job.agent_id &&` guard) | **NO** (2026-08-22): *"i don't want them"* — offered as a 🟡 decision at the end of the T29 sweep and refused. It is a consistency tidy-up with **no reachable path** behind it: a helper only ever learns a job id by claiming it, and claiming sets the owner; removing that computer nulls the owner but also kills its token, so it can no longer report anything. No named person is worse off today, and the blast radius of getting it wrong is paper. Leave the guard exactly as written. | `app/api/print-agent/[...path]/route.ts` → `job.agent_id` |

## Reversed (the owner changed his mind — newest first)

| # | idea | what changed | code site |
|---|---|---|---|
| R8 | Make the manager floor's "→ N more" chip say how many of the OFF-SCREEN tables need attention | **MOOT** (2026-08-15) — not a change of mind, the thing it was about was **deleted**. He refused the smarter chip on 2026-08-11 because waiter sections already decide who sees which tables. On 2026-08-15 he removed the sideways scroll entirely ("there shouldn't be horizontal scroll anywhere") and set fixed counts per screen — 2 on a phone, 4 turned, his number from ~10 inches up — so no table is ever off the right edge and the chip, its wrapper and its measuring pass are gone. Nothing to re-suggest and nothing left to guard. | *(was `public/panels/editor/app.js` → `syncFloorMore()`, removed)* |
