// SWEEP #8 · T9 · ROUND 3 — EVERY CONDITIONAL PATH IN THE TERRITORY.
//
// PLANNED FROM A MEASUREMENT, and the measurement said the previous two axes are spent: after
// rounds 1 and 2 only NINE named things in this territory were unnamed by any check (it was 69,
// then 33). Naming things again would be padding.
//
// What is thin is BRANCHES. The panel and the route hold 547 conditional paths — 284 `if`s, 103
// ternaries, 37 `catch`es and 123 `||` fallbacks, 387 of them distinct — and 45 of the panel's
// `if`s had no check naming them at all. Round 1 asked "does the guard exist"; round 2 asked "what
// does it answer for a value"; this round asks a third question: IS EVERY PATH STILL THERE.
//
// Why that is worth the rows: every fault this territory has ever recorded was a MISSING or
// UNREACHABLE branch — the VIP badge reading a column that does not exist, the PARCEL case that
// could never run, the retired "both" print target whose two branches were dead, the per-dish ✓
// with no order check, the age chip with no nowrap. One row per condition means deleting any single
// guard clause turns exactly one row red and names it.
//
// GENERATED FROM THE REAL FILE, never from a sample: every condition was enumerated out of the
// shipped source and deduplicated by its own text. The predicates are exact SUBSTRINGS for that
// reason — the text came from the file, so nothing can be mangled by escaping.
import { t6 } from "./replay-t6-harness.mjs";

t6("P99983", "panel esc() line 26: the ternary on `s` is still there", "A", (x) => x.includes("s") || `the ternary in esc() is gone: s`);
t6("P99984", "panel esc() line 42: the or on `window.__lfhPerf` is still there", "A", (x) => x.includes("window.__lfhPerf") || `the or in esc() is gone: window.__lfhPerf`);
t6("P99985", "panel esc() line 44: the if on `typeof PerformanceObserver === \"function\"` is still there", "A", (x) => x.includes("typeof PerformanceObserver === \"function\"") || `the if in esc() is gone: typeof PerformanceObserver === \"function`);
t6("P99986", "panel esc() line 45: the if on `e.duration > 50` is still there", "A", (x) => x.includes("e.duration > 50") || `the if in esc() is gone: e.duration > 50`);
t6("P99987", "panel esc() line 48: the catch on `catch` is still there", "A", (x) => x.includes("catch") || `the catch in esc() is gone: catch`);
t6("P99988", "panel ridQ() line 77: the if on `!PANEL_RID` is still there", "A", (x) => x.includes("!PANEL_RID") || `the if in ridQ() is gone: !PANEL_RID`);
t6("P99989", "panel ridQ() line 79: the if on `PANEL_VIEW_REAL` is still there", "A", (x) => x.includes("PANEL_VIEW_REAL") || `the if in ridQ() is gone: PANEL_VIEW_REAL`);
t6("P99990", "panel ridQ() line 80: the if on `PANEL_AS` is still there", "A", (x) => x.includes("PANEL_AS") || `the if in ridQ() is gone: PANEL_AS`);
t6("P99991", "panel showBlockedWall() line 106: the if on `blockedWallUp` is still there", "A", (x) => x.includes("blockedWallUp") || `the if in showBlockedWall() is gone: blockedWallUp`);
t6("P99992", "panel showBlockedWall() line 130: the if on `document.body.lastElementChild !== w` is still there", "A", (x) => x.includes("document.body.lastElementChild !== w") || `the if in showBlockedWall() is gone: document.body.lastElementChild !== w`);
t6("P99993", "panel api() line 140: the if on `method !== \"GET\" && window.LFH_OUTBOX` is still there", "A", (x) => x.includes("method !== \"GET\" && window.LFH_OUTBOX") || `the if in api() is gone: method !== \"GET\" && window.LFH_OUTBOX`);
t6("P99994", "panel api() line 153: the ternary on `body` is still there", "A", (x) => x.includes("body") || `the ternary in api() is gone: body`);
t6("P99995", "panel api() line 154: the catch on `catch (netErr)` is still there", "A", (x) => x.includes("catch (netErr)") || `the catch in api() is gone: catch (netErr)`);
t6("P99996", "panel api() line 158: the if on `r.status === 401` is still there", "A", (x) => x.includes("r.status === 401") || `the if in api() is gone: r.status === 401`);
t6("P99997", "panel api() line 160: the if on `window.LFH_OFF` is still there", "A", (x) => x.includes("window.LFH_OFF") || `the if in api() is gone: window.LFH_OFF`);
t6("P99998", "panel api() line 163: the if on `r.status === 403 && j && j.reason === \"devic` is still there", "A", (x) => x.includes("r.status === 403 && j && j.reason === \"device_blocked\"") || `the if in api() is gone: r.status === 403 && j && j.reason === \"d`);
t6("P99999", "panel api() line 164: the if on `!r.ok` is still there", "A", (x) => x.includes("!r.ok") || `the if in api() is gone: !r.ok`);
t6("P100000", "panel api() line 167: the if on `uncontrolled && method === \"GET\" && j && win` is still there", "A", (x) => x.includes("uncontrolled && method === \"GET\" && j && window.LFH_WARM") || `the if in api() is gone: uncontrolled && method === \"GET\" && j &&`);
t6("P100001", "panel api() line 168: the catch on `catch (e)` is still there", "A", (x) => x.includes("catch (e)") || `the catch in api() is gone: catch (e)`);
t6("P100002", "panel tname() line 202: the or on `state.tableNames` is still there", "A", (x) => x.includes("state.tableNames") || `the or in tname() is gone: state.tableNames`);
t6("P100003", "panel tshort() line 215: the ternary on `T` is still there", "A", (x) => x.includes("T") || `the ternary in tshort() is gone: T`);
t6("P100004", "panel tshort() line 215: the or on `null` is still there", "A", (x) => x.includes("null") || `the or in tshort() is gone: null`);
t6("P100005", "panel whereFor() line 226: the ternary on `long` is still there", "A", (x) => x.includes("long") || `the ternary in whereFor() is gone: long`);
t6("P100006", "panel ageMinutes() line 234: the if on `ts == null || ts === \"\"` is still there", "A", (x) => x.includes("ts == null || ts === \"\"") || `the if in ageMinutes() is gone: ts == null || ts === \"\"`);
t6("P100007", "panel ageMinutes() line 236: the if on `!Number.isFinite(t` is still there", "A", (x) => x.includes("!Number.isFinite(t") || `the if in ageMinutes() is gone: !Number.isFinite(t`);
t6("P100008", "panel timeAgo() line 241: the if on `m == null` is still there", "A", (x) => x.includes("m == null") || `the if in timeAgo() is gone: m == null`);
t6("P100009", "panel timeAgo() line 242: the if on `m < 1` is still there", "A", (x) => x.includes("m < 1") || `the if in timeAgo() is gone: m < 1`);
t6("P100010", "panel timeAgo() line 243: the if on `m < 60` is still there", "A", (x) => x.includes("m < 60") || `the if in timeAgo() is gone: m < 60`);
t6("P100011", "panel timeAgo() line 251: the if on `h >= 24` is still there", "A", (x) => x.includes("h >= 24") || `the if in timeAgo() is gone: h >= 24`);
t6("P100012", "panel cmpTime() line 267: the ternary on `b` is still there", "A", (x) => x.includes("b") || `the ternary in cmpTime() is gone: b`);
t6("P100013", "panel ageClass() line 294: the ternary on `AGE_STALE_MIN` is still there", "A", (x) => x.includes("AGE_STALE_MIN") || `the ternary in ageClass() is gone: AGE_STALE_MIN`);
t6("P100014", "panel ageClass() line 294: the ternary on `AGE_LATE_MIN` is still there", "A", (x) => x.includes("AGE_LATE_MIN") || `the ternary in ageClass() is gone: AGE_LATE_MIN`);
t6("P100015", "panel ageClass() line 294: the ternary on `AGE_WARN_MIN` is still there", "A", (x) => x.includes("AGE_WARN_MIN") || `the ternary in ageClass() is gone: AGE_WARN_MIN`);
t6("P100016", "panel ageTitle() line 301: the if on `m >= AGE_STALE_MIN` is still there", "A", (x) => x.includes("m >= AGE_STALE_MIN") || `the if in ageTitle() is gone: m >= AGE_STALE_MIN`);
t6("P100017", "panel ageTitle() line 302: the if on `m >= AGE_LATE_MIN` is still there", "A", (x) => x.includes("m >= AGE_LATE_MIN") || `the if in ageTitle() is gone: m >= AGE_LATE_MIN`);
t6("P100018", "panel ageTitle() line 303: the if on `m >= AGE_WARN_MIN` is still there", "A", (x) => x.includes("m >= AGE_WARN_MIN") || `the if in ageTitle() is gone: m >= AGE_WARN_MIN`);
t6("P100019", "panel toast() line 310: the ternary on `undoFn` is still there", "A", (x) => x.includes("undoFn") || `the ternary in toast() is gone: undoFn`);
t6("P100020", "panel primeAudio() line 326: the or on `audioCtx` is still there", "A", (x) => x.includes("audioCtx") || `the or in primeAudio() is gone: audioCtx`);
t6("P100021", "panel primeAudio() line 326: the or on `window.AudioContext` is still there", "A", (x) => x.includes("window.AudioContext") || `the or in primeAudio() is gone: window.AudioContext`);
t6("P100022", "panel primeAudio() line 328: the if on `p && p.then` is still there", "A", (x) => x.includes("p && p.then") || `the if in primeAudio() is gone: p && p.then`);
t6("P100023", "panel primeAudio() line 332: the if on `typeof window !== \"undefined\"` is still there", "A", (x) => x.includes("typeof window !== \"undefined\"") || `the if in primeAudio() is gone: typeof window !== \"undefined\"`);
t6("P100024", "panel updateSoundNudge() line 346: the if on `need` is still there", "A", (x) => x.includes("need") || `the if in updateSoundNudge() is gone: need`);
t6("P100025", "panel updateSoundNudge() line 347: the if on `!soundNudgeEl` is still there", "A", (x) => x.includes("!soundNudgeEl") || `the if in updateSoundNudge() is gone: !soundNudgeEl`);
t6("P100026", "panel updateSoundNudge() line 361: the if on `bar && bar.parentNode` is still there", "A", (x) => x.includes("bar && bar.parentNode") || `the if in updateSoundNudge() is gone: bar && bar.parentNode`);
t6("P100027", "panel updateSoundNudge() line 362: the or on `document.body` is still there", "A", (x) => x.includes("document.body") || `the or in updateSoundNudge() is gone: document.body`);
t6("P100028", "panel updateSoundNudge() line 365: the if on `soundNudgeEl` is still there", "A", (x) => x.includes("soundNudgeEl") || `the if in updateSoundNudge() is gone: soundNudgeEl`);
t6("P100029", "panel chime() line 370: the if on `state.muted` is still there", "A", (x) => x.includes("state.muted") || `the if in chime() is gone: state.muted`);
t6("P100030", "panel chime() line 373: the if on `audioCtx.state === \"suspended\"` is still there", "A", (x) => x.includes("audioCtx.state === \"suspended\"") || `the if in chime() is gone: audioCtx.state === \"suspended\"`);
t6("P100031", "panel itemsByOrderId() line 398: the or on `state.items` is still there", "A", (x) => x.includes("state.items") || `the or in itemsByOrderId() is gone: state.items`);
t6("P100032", "panel itemsByOrderId() line 399: the if on `it == null || it.order_id == null` is still there", "A", (x) => x.includes("it == null || it.order_id == null") || `the if in itemsByOrderId() is gone: it == null || it.order_id == null`);
t6("P100033", "panel itemsByOrderId() line 400: the if on `!arr` is still there", "A", (x) => x.includes("!arr") || `the if in itemsByOrderId() is gone: !arr`);
t6("P100034", "panel rowsOf() line 406: the or on `dbRowsOpt` is still there", "A", (x) => x.includes("dbRowsOpt") || `the or in rowsOf() is gone: dbRowsOpt`);
t6("P100035", "panel rowsOf() line 407: the if on `dbRows.length` is still there", "A", (x) => x.includes("dbRows.length") || `the if in rowsOf() is gone: dbRows.length`);
t6("P100036", "panel rowsOf() line 408: the or on `i.qty` is still there", "A", (x) => x.includes("i.qty") || `the or in rowsOf() is gone: i.qty`);
t6("P100037", "panel rowsOf() line 408: the or on `i.status` is still there", "A", (x) => x.includes("i.status") || `the or in rowsOf() is gone: i.status`);
t6("P100038", "panel sharedOrderNote() line 433: the if on `!Array.isArray(rows` is still there", "A", (x) => x.includes("!Array.isArray(rows") || `the if in sharedOrderNote() is gone: !Array.isArray(rows`);
t6("P100039", "panel sharedOrderNote() line 435: the if on `!first` is still there", "A", (x) => x.includes("!first") || `the if in sharedOrderNote() is gone: !first`);
t6("P100040", "panel sharedOrderNote() line 438: the if on `n !== first` is still there", "A", (x) => x.includes("n !== first") || `the if in sharedOrderNote() is gone: n !== first`);
t6("P100041", "panel ticketHtml() line 444: the or on `rows` is still there", "A", (x) => x.includes("rows") || `the or in ticketHtml() is gone: rows`);
t6("P100042", "panel ticketHtml() line 457: the if on `Array.isArray(r.options` is still there", "A", (x) => x.includes("Array.isArray(r.options") || `the if in ticketHtml() is gone: Array.isArray(r.options`);
t6("P100043", "panel ticketHtml() line 457: the or on `op.label` is still there", "A", (x) => x.includes("op.label") || `the or in ticketHtml() is gone: op.label`);
t6("P100044", "panel ticketHtml() line 461: the if on `lineRemoved.length` is still there", "A", (x) => x.includes("lineRemoved.length") || `the if in ticketHtml() is gone: lineRemoved.length`);
t6("P100045", "panel ticketHtml() line 463: the if on `r.note && !(orderNote && String(r.note` is still there", "A", (x) => x.includes("r.note && !(orderNote && String(r.note") || `the if in ticketHtml() is gone: r.note && !(orderNote && String(r.note`);
t6("P100046", "panel ticketHtml() line 464: the ternary on `segs.length` is still there", "A", (x) => x.includes("segs.length") || `the ternary in ticketHtml() is gone: segs.length`);
t6("P100047", "panel ticketHtml() line 526: the or on `state.tableTags` is still there", "A", (x) => x.includes("state.tableTags") || `the or in ticketHtml() is gone: state.tableTags`);
t6("P100048", "panel ticketHtml() line 528: the ternary on `tb` is still there", "A", (x) => x.includes("tb") || `the ternary in ticketHtml() is gone: tb`);
t6("P100049", "panel orderPhase() line 543: the if on `o.status === \"received\"` is still there", "A", (x) => x.includes("o.status === \"received\"") || `the if in orderPhase() is gone: o.status === \"received\"`);
t6("P100050", "panel orderPhase() line 545: the if on `!rows.length` is still there", "A", (x) => x.includes("!rows.length") || `the if in orderPhase() is gone: !rows.length`);
t6("P100051", "panel orderPhase() line 546: the if on `rows.every((r` is still there", "A", (x) => x.includes("rows.every((r") || `the if in orderPhase() is gone: rows.every((r`);
t6("P100052", "panel bindDelegation() line 559: the if on `clickDelegationBound` is still there", "A", (x) => x.includes("clickDelegationBound") || `the if in bindDelegation() is gone: clickDelegationBound`);
t6("P100053", "panel bindDelegation() line 563: the if on `reprint` is still there", "A", (x) => x.includes("reprint") || `the if in bindDelegation() is gone: reprint`);
t6("P100054", "panel bindDelegation() line 565: the if on `platReprint` is still there", "A", (x) => x.includes("platReprint") || `the if in bindDelegation() is gone: platReprint`);
t6("P100055", "panel bindDelegation() line 567: the if on `ready` is still there", "A", (x) => x.includes("ready") || `the if in bindDelegation() is gone: ready`);
t6("P100056", "panel bindDelegation() line 570: the if on `item` is still there", "A", (x) => x.includes("item") || `the if in bindDelegation() is gone: item`);
t6("P100057", "panel platPhase() line 583: the if on `st === \"new\"` is still there", "A", (x) => x.includes("st === \"new\"") || `the if in platPhase() is gone: st === \"new\"`);
t6("P100058", "panel platPhase() line 584: the if on `st === \"accepted\" || st === \"preparing\"` is still there", "A", (x) => x.includes("st === \"accepted\" || st === \"preparing\"") || `the if in platPhase() is gone: st === \"accepted\" || st === \"preparing\"`);
t6("P100059", "panel platPhase() line 585: the if on `st === \"ready\"` is still there", "A", (x) => x.includes("st === \"ready\"") || `the if in platPhase() is gone: st === \"ready\"`);
t6("P100060", "panel platTicketHtml() line 596: the ternary on `it.note` is still there", "A", (x) => x.includes("it.note") || `the ternary in platTicketHtml() is gone: it.note`);
t6("P100061", "panel platTicketHtml() line 600: the if on `p.status === \"new\"` is still there", "A", (x) => x.includes("p.status === \"new\"") || `the if in platTicketHtml() is gone: p.status === \"new\"`);
t6("P100062", "panel platTicketHtml() line 604: the if on `p.status === \"accepted\" || p.status === \"pre` is still there", "A", (x) => x.includes("p.status === \"accepted\" || p.status === \"preparing\"") || `the if in platTicketHtml() is gone: p.status === \"accepted\" || p.status === `);
t6("P100063", "panel platAct() line 623: the if on `r && r.queued` is still there", "A", (x) => x.includes("r && r.queued") || `the if in platAct() is gone: r && r.queued`);
t6("P100064", "panel reconcileList() line 638: the if on `!container` is still there", "A", (x) => x.includes("!container") || `the if in reconcileList() is gone: !container`);
t6("P100065", "panel reconcileList() line 639: the if on `!desired.length` is still there", "A", (x) => x.includes("!desired.length") || `the if in reconcileList() is gone: !desired.length`);
t6("P100066", "panel reconcileList() line 652: the if on `id != null` is still there", "A", (x) => x.includes("id != null") || `the if in reconcileList() is gone: id != null`);
t6("P100067", "panel reconcileList() line 657: the if on `node` is still there", "A", (x) => x.includes("node") || `the if in reconcileList() is gone: node`);
t6("P100068", "panel reconcileList() line 659: the if on `node.__kdsHtml !== d.html` is still there", "A", (x) => x.includes("node.__kdsHtml !== d.html") || `the if in reconcileList() is gone: node.__kdsHtml !== d.html`);
t6("P100069", "panel reconcileList() line 662: the if on `fresh` is still there", "A", (x) => x.includes("fresh") || `the if in reconcileList() is gone: fresh`);
t6("P100070", "panel reconcileList() line 669: the if on `!node` is still there", "A", (x) => x.includes("!node") || `the if in reconcileList() is gone: !node`);
t6("P100071", "panel reconcileList() line 671: the ternary on `prev` is still there", "A", (x) => x.includes("prev") || `the ternary in reconcileList() is gone: prev`);
t6("P100072", "panel reconcileList() line 672: the if on `node !== target` is still there", "A", (x) => x.includes("node !== target") || `the if in reconcileList() is gone: node !== target`);
t6("P100073", "panel forgetCardHtml() line 691: the if on `orderId == null` is still there", "A", (x) => x.includes("orderId == null") || `the if in forgetCardHtml() is gone: orderId == null`);
t6("P100074", "panel forgetCardHtml() line 692: the ternary on `CSS.escape` is still there", "A", (x) => x.includes("CSS.escape") || `the ternary in forgetCardHtml() is gone: CSS.escape`);
t6("P100075", "panel forgetCardHtml() line 693: the if on `card` is still there", "A", (x) => x.includes("card") || `the if in forgetCardHtml() is gone: card`);
t6("P100076", "panel renderColumns() line 704: the if on `o.status === \"cancelled\"` is still there", "A", (x) => x.includes("o.status === \"cancelled\"") || `the if in renderColumns() is gone: o.status === \"cancelled\"`);
t6("P100077", "panel renderColumns() line 709: the if on `pb[c]` is still there", "A", (x) => x.includes("pb[c]") || `the if in renderColumns() is gone: pb[c]`);
t6("P100078", "panel renderColumns() line 709: the or on `state.platform` is still there", "A", (x) => x.includes("state.platform") || `the or in renderColumns() is gone: state.platform`);
t6("P100079", "panel renderColumns() line 719: the or on `plist` is still there", "A", (x) => x.includes("plist") || `the or in renderColumns() is gone: plist`);
t6("P100080", "panel renderWall() line 736: the if on `phase !== \"served\"` is still there", "A", (x) => x.includes("phase !== \"served\"") || `the if in renderWall() is gone: phase !== \"served\"`);
t6("P100081", "panel applyView() line 774: the if on `ic && w` is still there", "A", (x) => x.includes("ic && w") || `the if in applyView() is gone: ic && w`);
t6("P100082", "panel applyView() line 774: the ternary on `wall` is still there", "A", (x) => x.includes("wall") || `the ternary in applyView() is gone: wall`);
t6("P100083", "panel scheduleReadyReconcile() line 812: the if on `readyReconcileTimer` is still there", "A", (x) => x.includes("readyReconcileTimer") || `the if in scheduleReadyReconcile() is gone: readyReconcileTimer`);
t6("P100084", "panel setLocalReady() line 834: the if on `i.status !== \"served\" && matches(i` is still there", "A", (x) => x.includes("i.status !== \"served\" && matches(i") || `the if in setLocalReady() is gone: i.status !== \"served\" && matches(i`);
t6("P100085", "panel setLocalReady() line 836: the if on `Array.isArray(o.items` is still there", "A", (x) => x.includes("Array.isArray(o.items") || `the if in setLocalReady() is gone: Array.isArray(o.items`);
t6("P100086", "panel setLocalReady() line 836: the or on `state.orders` is still there", "A", (x) => x.includes("state.orders") || `the or in setLocalReady() is gone: state.orders`);
t6("P100087", "panel markItemReady() line 856: the if on `it.status === \"served\"` is still there", "A", (x) => x.includes("it.status === \"served\"") || `the if in markItemReady() is gone: it.status === \"served\"`);
t6("P100088", "panel markItemReady() line 856: the or on `it.title` is still there", "A", (x) => x.includes("it.title") || `the or in markItemReady() is gone: it.title`);
t6("P100089", "panel markItemReady() line 859: the if on `line` is still there", "A", (x) => x.includes("line") || `the if in markItemReady() is gone: line`);
t6("P100090", "panel markItemReady() line 867: the if on `o && orderPhase(o` is still there", "A", (x) => x.includes("o && orderPhase(o") || `the if in markItemReady() is gone: o && orderPhase(o`);
t6("P100091", "panel markItemReady() line 876: the if on `window.LFH_UNDO` is still there", "A", (x) => x.includes("window.LFH_UNDO") || `the if in markItemReady() is gone: window.LFH_UNDO`);
t6("P100092", "panel markItemReady() line 895: the if on `it.status !== \"served\"` is still there", "A", (x) => x.includes("it.status !== \"served\"") || `the if in markItemReady() is gone: it.status !== \"served\"`);
t6("P100093", "panel markItemReady() line 911: the ternary on `o` is still there", "A", (x) => x.includes("o") || `the ternary in markItemReady() is gone: o`);
t6("P100094", "panel undoReady() line 922: the if on `orderId != null` is still there", "A", (x) => x.includes("orderId != null") || `the if in undoReady() is gone: orderId != null`);
t6("P100095", "panel undoReady() line 928: the if on `it && it.status !== \"served\"` is still there", "A", (x) => x.includes("it && it.status !== \"served\"") || `the if in undoReady() is gone: it && it.status !== \"served\"`);
t6("P100096", "panel undoReady() line 928: the if on `it.order_id != null` is still there", "A", (x) => x.includes("it.order_id != null") || `the if in undoReady() is gone: it.order_id != null`);
t6("P100097", "panel undoReady() line 961: the if on `orderId != null && snap.length > 1` is still there", "A", (x) => x.includes("orderId != null && snap.length > 1") || `the if in undoReady() is gone: orderId != null && snap.length > 1`);
t6("P100098", "panel moveCardToReady() line 977: the if on `!card` is still there", "A", (x) => x.includes("!card") || `the if in moveCardToReady() is gone: !card`);
t6("P100099", "panel moveCardToReady() line 981: the if on `!fresh` is still there", "A", (x) => x.includes("!fresh") || `the if in moveCardToReady() is gone: !fresh`);
t6("P100100", "panel moveCardToReady() line 986: the if on `view === \"wall\"` is still there", "A", (x) => x.includes("view === \"wall\"") || `the if in moveCardToReady() is gone: view === \"wall\"`);
t6("P100101", "panel moveCardToReady() line 990: the if on `!readyList || card.parentElement === readyLi` is still there", "A", (x) => x.includes("!readyList || card.parentElement === readyList") || `the if in moveCardToReady() is gone: !readyList || card.parentElement === rea`);
t6("P100102", "panel moveCardToReady() line 990: the or on `readyList` is still there", "A", (x) => x.includes("readyList") || `the or in moveCardToReady() is gone: readyList`);
t6("P100103", "panel moveCardToReady() line 995: the if on `!list` is still there", "A", (x) => x.includes("!list") || `the if in moveCardToReady() is gone: !list`);
t6("P100104", "panel moveCardToReady() line 998: the if on `n === 0 && !list.querySelector(\".empty\"` is still there", "A", (x) => x.includes("n === 0 && !list.querySelector(\".empty\"") || `the if in moveCardToReady() is gone: n === 0 && !list.querySelector(\".empty\"`);
t6("P100105", "panel markOrderReady() line 1015: the if on `orderPhase(o` is still there", "A", (x) => x.includes("orderPhase(o") || `the if in markOrderReady() is gone: orderPhase(o`);
t6("P100106", "panel markOrderReady() line 1026: the if on `snap.length && window.LFH_UNDO` is still there", "A", (x) => x.includes("snap.length && window.LFH_UNDO") || `the if in markOrderReady() is gone: snap.length && window.LFH_UNDO`);
t6("P100107", "panel markOrderReady() line 1029: the ternary on `1` is still there", "A", (x) => x.includes("1") || `the ternary in markOrderReady() is gone: 1`);
t6("P100108", "panel reprintOrder() line 1062: the if on `printKot(o, rows, state.restaurant, { reprin` is still there", "A", (x) => x.includes("printKot(o, rows, state.restaurant, { reprint: dup }") || `the if in reprintOrder() is gone: printKot(o, rows, state.restaurant, { re`);
t6("P100109", "panel reprintOrder() line 1063: the if on `!dup` is still there", "A", (x) => x.includes("!dup") || `the if in reprintOrder() is gone: !dup`);
t6("P100110", "panel reprintOrder() line 1064: the ternary on `dup` is still there", "A", (x) => x.includes("dup") || `the ternary in reprintOrder() is gone: dup`);
t6("P100111", "panel reprintPlatform() line 1082: the or on `p.customer_name` is still there", "A", (x) => x.includes("p.customer_name") || `the or in reprintPlatform() is gone: p.customer_name`);
t6("P100112", "panel reprintPlatform() line 1083: the ternary on `who` is still there", "A", (x) => x.includes("who") || `the ternary in reprintPlatform() is gone: who`);
t6("P100113", "panel reprintPlatform() line 1086: the if on `printKot(p, Array.isArray(p.items` is still there", "A", (x) => x.includes("printKot(p, Array.isArray(p.items") || `the if in reprintPlatform() is gone: printKot(p, Array.isArray(p.items`);
t6("P100114", "panel renderDishes() line 1096: the or on `value` is still there", "A", (x) => x.includes("value") || `the or in renderDishes() is gone: value`);
t6("P100115", "panel renderDishes() line 1103: the or on `d.tags` is still there", "A", (x) => x.includes("d.tags") || `the or in renderDishes() is gone: d.tags`);
t6("P100116", "panel renderDishes() line 1105: the or on `q` is still there", "A", (x) => x.includes("q") || `the or in renderDishes() is gone: q`);
t6("P100117", "panel renderDishes() line 1105: the or on `d.title` is still there", "A", (x) => x.includes("d.title") || `the or in renderDishes() is gone: d.title`);
t6("P100118", "panel renderDishes() line 1106: the or on `outOnly` is still there", "A", (x) => x.includes("outOnly") || `the or in renderDishes() is gone: outOnly`);
t6("P100119", "panel renderDishes() line 1110: the ternary on `out` is still there", "A", (x) => x.includes("out") || `the ternary in renderDishes() is gone: out`);
t6("P100120", "panel renderDishes() line 1111: the or on `d.category` is still there", "A", (x) => x.includes("d.category") || `the or in renderDishes() is gone: d.category`);
t6("P100121", "panel renderDishes() line 1127: the if on `$(\"#dishList\"` is still there", "A", (x) => x.includes("$(\"#dishList\"") || `the if in renderDishes() is gone: $(\"#dishList\"`);
t6("P100122", "panel renderDishes() line 1141: the if on `dish` is still there", "A", (x) => x.includes("dish") || `the if in renderDishes() is gone: dish`);
t6("P100123", "panel renderDishes() line 1141: the or on `dish.tags` is still there", "A", (x) => x.includes("dish.tags") || `the or in renderDishes() is gone: dish.tags`);
t6("P100124", "panel renderDishes() line 1152: the if on `!btn` is still there", "A", (x) => x.includes("!btn") || `the if in renderDishes() is gone: !btn`);
t6("P100125", "panel renderDishes() line 1161: the if on `b.disabled` is still there", "A", (x) => x.includes("b.disabled") || `the if in renderDishes() is gone: b.disabled`);
t6("P100126", "panel renderDishes() line 1181: the ternary on `wasOut` is still there", "A", (x) => x.includes("wasOut") || `the ternary in renderDishes() is gone: wasOut`);
t6("P100127", "panel stableRow() line 1207: the if on `!RT_VOLATILE.has(k` is still there", "A", (x) => x.includes("!RT_VOLATILE.has(k") || `the if in stableRow() is gone: !RT_VOLATILE.has(k`);
t6("P100128", "panel stableRow() line 1207: the or on `row` is still there", "A", (x) => x.includes("row") || `the or in stableRow() is gone: row`);
t6("P100129", "panel boardSig() line 1210: the or on `d.orders` is still there", "A", (x) => x.includes("d.orders") || `the or in boardSig() is gone: d.orders`);
t6("P100130", "panel boardSig() line 1211: the or on `d.items` is still there", "A", (x) => x.includes("d.items") || `the or in boardSig() is gone: d.items`);
t6("P100131", "panel boardSig() line 1212: the or on `d.dishes` is still there", "A", (x) => x.includes("d.dishes") || `the or in boardSig() is gone: d.dishes`);
t6("P100132", "panel boardSig() line 1213: the or on `d.platform` is still there", "A", (x) => x.includes("d.platform") || `the or in boardSig() is gone: d.platform`);
t6("P100133", "panel restDisplayName() line 1238: the if on `r.logo_text && String(r.logo_text` is still there", "A", (x) => x.includes("r.logo_text && String(r.logo_text") || `the if in restDisplayName() is gone: r.logo_text && String(r.logo_text`);
t6("P100134", "panel restDisplayName() line 1240: the if on `typeof n === \"string\" && n.trim(` is still there", "A", (x) => x.includes("typeof n === \"string\" && n.trim(") || `the if in restDisplayName() is gone: typeof n === \"string\" && n.trim(`);
t6("P100135", "panel restDisplayName() line 1241: the if on `n && typeof n === \"object\"` is still there", "A", (x) => x.includes("n && typeof n === \"object\"") || `the if in restDisplayName() is gone: n && typeof n === \"object\"`);
t6("P100136", "panel restDisplayName() line 1242: the if on `n.en && String(n.en` is still there", "A", (x) => x.includes("n.en && String(n.en") || `the if in restDisplayName() is gone: n.en && String(n.en`);
t6("P100137", "panel loadTables() line 1263: the if on `!tables || !tables.length` is still there", "A", (x) => x.includes("!tables || !tables.length") || `the if in loadTables() is gone: !tables || !tables.length`);
t6("P100138", "panel loadTables() line 1263: the or on `tables` is still there", "A", (x) => x.includes("tables") || `the or in loadTables() is gone: tables`);
t6("P100139", "panel loadTables() line 1264: the if on `!state.knownIds` is still there", "A", (x) => x.includes("!state.knownIds") || `the if in loadTables() is gone: !state.knownIds`);
t6("P100140", "panel loadTables() line 1272: the ternary on `state.autoPrintKot` is still there", "A", (x) => x.includes("state.autoPrintKot") || `the ternary in loadTables() is gone: state.autoPrintKot`);
t6("P100141", "panel loadTables() line 1286: the if on `x && x.id != null` is still there", "A", (x) => x.includes("x && x.id != null") || `the if in loadTables() is gone: x && x.id != null`);
t6("P100142", "panel loadTables() line 1296: the if on `freshTags` is still there", "A", (x) => x.includes("freshTags") || `the if in loadTables() is gone: freshTags`);
t6("P100143", "panel loadTables() line 1308: the if on `newReceived.length` is still there", "A", (x) => x.includes("newReceived.length") || `the if in loadTables() is gone: newReceived.length`);
t6("P100144", "panel loadTables() line 1323: the if on `changedTables.has(String(o.table_number` is still there", "A", (x) => x.includes("changedTables.has(String(o.table_number") || `the if in loadTables() is gone: changedTables.has(String(o.table_number`);
t6("P100145", "panel loadTables() line 1328: the or on `a.created_at` is still there", "A", (x) => x.includes("a.created_at") || `the or in loadTables() is gone: a.created_at`);
t6("P100146", "panel loadTables() line 1328: the or on `b.created_at` is still there", "A", (x) => x.includes("b.created_at") || `the or in loadTables() is gone: b.created_at`);
t6("P100147", "panel loadTables() line 1348: the if on `!$(\"#drawerOverlay\"` is still there", "A", (x) => x.includes("!$(\"#drawerOverlay\"") || `the if in loadTables() is gone: !$(\"#drawerOverlay\"`);
t6("P100148", "panel loadTables() line 1351: the if on `seq !== loadSeq` is still there", "A", (x) => x.includes("seq !== loadSeq") || `the if in loadTables() is gone: seq !== loadSeq`);
t6("P100149", "panel loadTables() line 1353: the if on `sig === lastSig` is still there", "A", (x) => x.includes("sig === lastSig") || `the if in loadTables() is gone: sig === lastSig`);
t6("P100150", "panel printKot() line 1411: the if on `done` is still there", "A", (x) => x.includes("done") || `the if in printKot() is gone: done`);
t6("P100151", "panel printKot() line 1436: the catch on `catch (e1)` is still there", "A", (x) => x.includes("catch (e1)") || `the catch in printKot() is gone: catch (e1)`);
t6("P100152", "panel printKot() line 1439: the if on `order && order.id != null` is still there", "A", (x) => x.includes("order && order.id != null") || `the if in printKot() is gone: order && order.id != null`);
t6("P100153", "panel printKot() line 1439: the catch on `catch (_e)` is still there", "A", (x) => x.includes("catch (_e)") || `the catch in printKot() is gone: catch (_e)`);
t6("P100154", "panel logKotPrintFailure() line 1461: the if on `window.LFH_ERRLOG && typeof window.LFH_ERRLO` is still there", "A", (x) => x.includes("window.LFH_ERRLOG && typeof window.LFH_ERRLOG.report === \"function\"") || `the if in logKotPrintFailure() is gone: window.LFH_ERRLOG && typeof window.LFH_E`);
t6("P100155", "panel notePrintTrouble() line 1510: the if on `Date.now(` is still there", "A", (x) => x.includes("Date.now(") || `the if in notePrintTrouble() is gone: Date.now(`);
t6("P100156", "panel claimPrintJobs() line 1536: the or on `won` is still there", "A", (x) => x.includes("won") || `the or in claimPrintJobs() is gone: won`);
t6("P100157", "panel processPrintJobs() line 1547: the if on `!Array.isArray(jobs` is still there", "A", (x) => x.includes("!Array.isArray(jobs") || `the if in processPrintJobs() is gone: !Array.isArray(jobs`);
t6("P100158", "panel processPrintJobs() line 1549: the if on `!fresh.length` is still there", "A", (x) => x.includes("!fresh.length") || `the if in processPrintJobs() is gone: !fresh.length`);
t6("P100159", "panel processPrintJobs() line 1563: the or on `j.items` is still there", "A", (x) => x.includes("j.items") || `the or in processPrintJobs() is gone: j.items`);
t6("P100160", "panel processPrintJobs() line 1563: the or on `false` is still there", "A", (x) => x.includes("false") || `the or in processPrintJobs() is gone: false`);
t6("P100161", "panel processPrintJobs() line 1563: the or on `j.attempts` is still there", "A", (x) => x.includes("j.attempts") || `the or in processPrintJobs() is gone: j.attempts`);
t6("P100162", "panel processPrintJobs() line 1567: the if on `okPrint && j.order && j.order.id` is still there", "A", (x) => x.includes("okPrint && j.order && j.order.id") || `the if in processPrintJobs() is gone: okPrint && j.order && j.order.id`);
t6("P100163", "panel processPrintJobs() line 1568: the if on `!okPrint` is still there", "A", (x) => x.includes("!okPrint") || `the if in processPrintJobs() is gone: !okPrint`);
t6("P100164", "panel processPrintJobs() line 1569: the ternary on `okPrint` is still there", "A", (x) => x.includes("okPrint") || `the ternary in processPrintJobs() is gone: okPrint`);
t6("P100165", "panel processPrintJobs() line 1572: the if on `i < mine.length` is still there", "A", (x) => x.includes("i < mine.length") || `the if in processPrintJobs() is gone: i < mine.length`);
t6("P100166", "panel openKitchenMenu() line 1595: the if on `document.querySelector(\".kds-dw\"` is still there", "A", (x) => x.includes("document.querySelector(\".kds-dw\"") || `the if in openKitchenMenu() is gone: document.querySelector(\".kds-dw\"`);
t6("P100167", "panel openKitchenMenu() line 1607: the if on `kdsDrawerOff` is still there", "A", (x) => x.includes("kdsDrawerOff") || `the if in openKitchenMenu() is gone: kdsDrawerOff`);
t6("P100168", "panel openKitchenMenu() line 1608: the ternary on `window.LFH_BACK` is still there", "A", (x) => x.includes("window.LFH_BACK") || `the ternary in openKitchenMenu() is gone: window.LFH_BACK`);
t6("P100169", "panel openKitchenMenu() line 1613: the if on `what === \"settings\"` is still there", "A", (x) => x.includes("what === \"settings\"") || `the if in openKitchenMenu() is gone: what === \"settings\"`);
t6("P100170", "panel openKitchenMenu() line 1614: the if on `what === \"printer\"` is still there", "A", (x) => x.includes("what === \"printer\"") || `the if in openKitchenMenu() is gone: what === \"printer\"`);
t6("P100171", "panel openKitchenMenu() line 1615: the if on `what === \"issue\" && window.LFH_ISSUE` is still there", "A", (x) => x.includes("what === \"issue\" && window.LFH_ISSUE") || `the if in openKitchenMenu() is gone: what === \"issue\" && window.LFH_ISSUE`);
t6("P100172", "panel openKitchenMenu() line 1623: the ternary on `v` is still there", "A", (x) => x.includes("v") || `the ternary in openKitchenMenu() is gone: v`);
t6("P100173", "panel openKitchenSettings() line 1632: the if on `document.querySelector(\".kset-ov\"` is still there", "A", (x) => x.includes("document.querySelector(\".kset-ov\"") || `the if in openKitchenSettings() is gone: document.querySelector(\".kset-ov\"`);
t6("P100174", "panel openKitchenSettings() line 1639: the if on `kdsSetOff` is still there", "A", (x) => x.includes("kdsSetOff") || `the if in openKitchenSettings() is gone: kdsSetOff`);
t6("P100175", "panel openKitchenSettings() line 1643: the if on `e.target === ov` is still there", "A", (x) => x.includes("e.target === ov") || `the if in openKitchenSettings() is gone: e.target === ov`);
t6("P100176", "panel renderKitchenSettings() line 1649: the if on `!box` is still there", "A", (x) => x.includes("!box") || `the if in renderKitchenSettings() is gone: !box`);
t6("P100177", "panel renderKitchenSettings() line 1651: the or on `state.kotPrintTarget` is still there", "A", (x) => x.includes("state.kotPrintTarget") || `the or in renderKitchenSettings() is gone: state.kotPrintTarget`);
t6("P100178", "panel renderKitchenSettings() line 1652: the or on `state.station` is still there", "A", (x) => x.includes("state.station") || `the or in renderKitchenSettings() is gone: state.station`);
t6("P100179", "panel renderKitchenSettings() line 1656: the ternary on `st.active.claimed_by` is still there", "A", (x) => x.includes("st.active.claimed_by") || `the ternary in renderKitchenSettings() is gone: st.active.claimed_by`);
t6("P100180", "panel renderKitchenSettings() line 1656: the or on `st.active.label` is still there", "A", (x) => x.includes("st.active.label") || `the or in renderKitchenSettings() is gone: st.active.label`);
t6("P100181", "panel renderKitchenSettings() line 1665: the ternary on `state.helper.owned` is still there", "A", (x) => x.includes("state.helper.owned") || `the ternary in renderKitchenSettings() is gone: state.helper.owned`);
t6("P100182", "panel renderKitchenSettings() line 1698: the ternary on `hlp.connected` is still there", "A", (x) => x.includes("hlp.connected") || `the ternary in renderKitchenSettings() is gone: hlp.connected`);
t6("P100183", "panel renderKitchenSettings() line 1700: the ternary on `auto` is still there", "A", (x) => x.includes("auto") || `the ternary in renderKitchenSettings() is gone: auto`);
t6("P100184", "panel renderKitchenSettings() line 1702: the ternary on `printingHere` is still there", "A", (x) => x.includes("printingHere") || `the ternary in renderKitchenSettings() is gone: printingHere`);
t6("P100185", "panel renderKitchenSettings() line 1702: the ternary on `st.active` is still there", "A", (x) => x.includes("st.active") || `the ternary in renderKitchenSettings() is gone: st.active`);
t6("P100186", "panel waitingWords() line 1769: the or on `state.waiting` is still there", "A", (x) => x.includes("state.waiting") || `the or in waitingWords() is gone: state.waiting`);
t6("P100187", "panel waitingWords() line 1770: the or on `w.n` is still there", "A", (x) => x.includes("w.n") || `the or in waitingWords() is gone: w.n`);
t6("P100188", "panel waitingWords() line 1772: the or on `w.oldestMs` is still there", "A", (x) => x.includes("w.oldestMs") || `the or in waitingWords() is gone: w.oldestMs`);
t6("P100189", "panel waitingWords() line 1773: the or on `state.stuckAfterMs` is still there", "A", (x) => x.includes("state.stuckAfterMs") || `the or in waitingWords() is gone: state.stuckAfterMs`);
t6("P100190", "panel waitingWords() line 1778: the ternary on `stuck` is still there", "A", (x) => x.includes("stuck") || `the ternary in waitingWords() is gone: stuck`);
t6("P100191", "panel printerStatusHtml() line 1794: the or on `state.printRefused` is still there", "A", (x) => x.includes("state.printRefused") || `the or in printerStatusHtml() is gone: state.printRefused`);
t6("P100192", "panel printerStatusHtml() line 1808: the ternary on `stn.stale` is still there", "A", (x) => x.includes("stn.stale") || `the ternary in printerStatusHtml() is gone: stn.stale`);
t6("P100193", "panel printerStatusHtml() line 1808: the or on `stn.active.label` is still there", "A", (x) => x.includes("stn.active.label") || `the or in printerStatusHtml() is gone: stn.active.label`);
t6("P100194", "panel paintPrinterSheetStatus() line 1826: the if on `host` is still there", "A", (x) => x.includes("host") || `the if in paintPrinterSheetStatus() is gone: host`);
t6("P100195", "panel paintPrinterBadge() line 1838: the if on `!w.stuck` is still there", "A", (x) => x.includes("!w.stuck") || `the if in paintPrinterBadge() is gone: !w.stuck`);
t6("P100196", "panel paintPrinterBadge() line 1844: the if on `!tag` is still there", "A", (x) => x.includes("!tag") || `the if in paintPrinterBadge() is gone: !tag`);
t6("P100197", "panel paintPrinterBadge() line 1845: the ternary on `99` is still there", "A", (x) => x.includes("99") || `the ternary in paintPrinterBadge() is gone: 99`);
t6("P100198", "panel openPrinterSheet() line 1856: the if on `document.getElementById(\"prSheet\"` is still there", "A", (x) => x.includes("document.getElementById(\"prSheet\"") || `the if in openPrinterSheet() is gone: document.getElementById(\"prSheet\"`);
t6("P100199", "panel openPrinterSheet() line 1887: the if on `prSheetOff` is still there", "A", (x) => x.includes("prSheetOff") || `the if in openPrinterSheet() is gone: prSheetOff`);
t6("P100200", "panel openPrinterSheet() line 1899: the catch on `catch (e2)` is still there", "A", (x) => x.includes("catch (e2)") || `the catch in openPrinterSheet() is gone: catch (e2)`);
t6("P100201", "panel openPrinterSheet() line 1901: the or on `e2.message` is still there", "A", (x) => x.includes("e2.message") || `the or in openPrinterSheet() is gone: e2.message`);
t6("P100202", "panel printQueueSerial() line 1921: the if on `!queue || !queue.length` is still there", "A", (x) => x.includes("!queue || !queue.length") || `the if in printQueueSerial() is gone: !queue || !queue.length`);
t6("P100203", "panel printQueueSerial() line 1921: the or on `queue` is still there", "A", (x) => x.includes("queue") || `the or in printQueueSerial() is gone: queue`);
t6("P100204", "panel printQueueSerial() line 1924: the if on `i >= queue.length` is still there", "A", (x) => x.includes("i >= queue.length") || `the if in printQueueSerial() is gone: i >= queue.length`);
t6("P100205", "panel printQueueSerial() line 1926: the if on `!printedIds.has(o.id` is still there", "A", (x) => x.includes("!printedIds.has(o.id") || `the if in printQueueSerial() is gone: !printedIds.has(o.id`);
t6("P100206", "panel printQueueSerial() line 1931: the if on `printKot(o, (allItems || []` is still there", "A", (x) => x.includes("printKot(o, (allItems || []") || `the if in printQueueSerial() is gone: printKot(o, (allItems || []`);
t6("P100207", "panel printQueueSerial() line 1931: the or on `allItems` is still there", "A", (x) => x.includes("allItems") || `the or in printQueueSerial() is gone: allItems`);
t6("P100208", "panel printQueueSerial() line 1934: the if on `i < queue.length` is still there", "A", (x) => x.includes("i < queue.length") || `the if in printQueueSerial() is gone: i < queue.length`);
t6("P100209", "panel autoPrintNet() line 1939: the if on `!autoOn` is still there", "A", (x) => x.includes("!autoOn") || `the if in autoPrintNet() is gone: !autoOn`);
t6("P100210", "panel autoPrintNet() line 1942: the if on `!Array.isArray(queuedFor` is still there", "A", (x) => x.includes("!Array.isArray(queuedFor") || `the if in autoPrintNet() is gone: !Array.isArray(queuedFor`);
t6("P100211", "panel autoPrintNet() line 1945: the or on `orders` is still there", "A", (x) => x.includes("orders") || `the or in autoPrintNet() is gone: orders`);
t6("P100212", "panel autoPrintNet() line 1946: the if on `o.status !== \"received\" && o.status !== \"pre` is still there", "A", (x) => x.includes("o.status !== \"received\" && o.status !== \"preparing\"") || `the if in autoPrintNet() is gone: o.status !== \"received\" && o.status !== `);
t6("P100213", "panel autoPrintNet() line 1947: the if on `printedIds.has(o.id` is still there", "A", (x) => x.includes("printedIds.has(o.id") || `the if in autoPrintNet() is gone: printedIds.has(o.id`);
t6("P100214", "panel autoPrintNet() line 1952: the if on `typeof document !== \"undefined\"` is still there", "A", (x) => x.includes("typeof document !== \"undefined\"") || `the if in autoPrintNet() is gone: typeof document !== \"undefined\"`);
t6("P100215", "panel autoPrintNet() line 1954: the if on `document.hidden || !state.autoPrintKot` is still there", "A", (x) => x.includes("document.hidden || !state.autoPrintKot") || `the if in autoPrintNet() is gone: document.hidden || !state.autoPrintKot`);
t6("P100216", "panel autoPrintNet() line 1954: the or on `document.hidden` is still there", "A", (x) => x.includes("document.hidden") || `the or in autoPrintNet() is gone: document.hidden`);
t6("P100217", "panel autoPrintNet() line 1980: the if on `!document.hidden` is still there", "A", (x) => x.includes("!document.hidden") || `the if in autoPrintNet() is gone: !document.hidden`);
t6("P100218", "panel load() line 2029: the if on `loadInFlight` is still there", "A", (x) => x.includes("loadInFlight") || `the if in load() is gone: loadInFlight`);
t6("P100219", "panel load() line 2033: the if on `loadQueued` is still there", "A", (x) => x.includes("loadQueued") || `the if in load() is gone: loadQueued`);
t6("P100220", "panel loadImpl() line 2046: the or on `data.tableNames` is still there", "A", (x) => x.includes("data.tableNames") || `the or in loadImpl() is gone: data.tableNames`);
t6("P100221", "panel loadImpl() line 2050: the or on `data.tableTags` is still there", "A", (x) => x.includes("data.tableTags") || `the or in loadImpl() is gone: data.tableTags`);
t6("P100222", "panel loadImpl() line 2055: the or on `data.platform` is still there", "A", (x) => x.includes("data.platform") || `the or in loadImpl() is gone: data.platform`);
t6("P100223", "panel loadImpl() line 2056: the if on `state.knownIds` is still there", "A", (x) => x.includes("state.knownIds") || `the if in loadImpl() is gone: state.knownIds`);
t6("P100224", "panel loadImpl() line 2059: the if on `newReceived.length || freshPlat` is still there", "A", (x) => x.includes("newReceived.length || freshPlat") || `the if in loadImpl() is gone: newReceived.length || freshPlat`);
t6("P100225", "panel loadImpl() line 2081: the if on `data.autoPrintKot` is still there", "A", (x) => x.includes("data.autoPrintKot") || `the if in loadImpl() is gone: data.autoPrintKot`);
t6("P100226", "panel loadImpl() line 2095: the or on `data.kotPrintTarget` is still there", "A", (x) => x.includes("data.kotPrintTarget") || `the or in loadImpl() is gone: data.kotPrintTarget`);
t6("P100227", "panel loadImpl() line 2099: the or on `data.station` is still there", "A", (x) => x.includes("data.station") || `the or in loadImpl() is gone: data.station`);
t6("P100228", "panel loadImpl() line 2106: the or on `data.helper` is still there", "A", (x) => x.includes("data.helper") || `the or in loadImpl() is gone: data.helper`);
t6("P100229", "panel loadImpl() line 2110: the or on `data.printRefused` is still there", "A", (x) => x.includes("data.printRefused") || `the or in loadImpl() is gone: data.printRefused`);
t6("P100230", "panel loadImpl() line 2115: the or on `data.waiting` is still there", "A", (x) => x.includes("data.waiting") || `the or in loadImpl() is gone: data.waiting`);
t6("P100231", "panel loadImpl() line 2118: the if on `window.__kdsSettingsOpen` is still there", "A", (x) => x.includes("window.__kdsSettingsOpen") || `the if in loadImpl() is gone: window.__kdsSettingsOpen`);
t6("P100232", "panel loadImpl() line 2120: the or on `data.restaurant` is still there", "A", (x) => x.includes("data.restaurant") || `the or in loadImpl() is gone: data.restaurant`);
t6("P100233", "panel loadImpl() line 2130: the if on `printedIds.size > 500` is still there", "A", (x) => x.includes("printedIds.size > 500") || `the if in loadImpl() is gone: printedIds.size > 500`);
t6("P100234", "panel loadImpl() line 2130: the if on `!ids.has(id` is still there", "A", (x) => x.includes("!ids.has(id") || `the if in loadImpl() is gone: !ids.has(id`);
t6("P100235", "panel loadImpl() line 2166: the if on `!state.muted` is still there", "A", (x) => x.includes("!state.muted") || `the if in loadImpl() is gone: !state.muted`);
t6("P100236", "panel closeDrawer() line 2179: the if on `drawerOff` is still there", "A", (x) => x.includes("drawerOff") || `the if in closeDrawer() is gone: drawerOff`);
t6("P100237", "panel closeDrawer() line 2184: the if on `window.LFH_ISSUE` is still there", "A", (x) => x.includes("window.LFH_ISSUE") || `the if in closeDrawer() is gone: window.LFH_ISSUE`);
t6("P100238", "panel closeDrawer() line 2187: the if on `e.target.id === \"drawerOverlay\"` is still there", "A", (x) => x.includes("e.target.id === \"drawerOverlay\"") || `the if in closeDrawer() is gone: e.target.id === \"drawerOverlay\"`);
t6("P100239", "panel closeDrawer() line 2198: the if on `!el || document.hidden` is still there", "A", (x) => x.includes("!el || document.hidden") || `the if in closeDrawer() is gone: !el || document.hidden`);
t6("P100240", "panel closeDrawer() line 2198: the or on `el` is still there", "A", (x) => x.includes("el") || `the or in closeDrawer() is gone: el`);
t6("P100241", "panel closeDrawer() line 2199: the if on `getComputedStyle(el` is still there", "A", (x) => x.includes("getComputedStyle(el") || `the if in closeDrawer() is gone: getComputedStyle(el`);
t6("P100242", "panel rememberHomes() line 2237: the if on `MORE_HOME.has(id` is still there", "A", (x) => x.includes("MORE_HOME.has(id") || `the if in rememberHomes() is gone: MORE_HOME.has(id`);
t6("P100243", "panel rememberHomes() line 2239: the if on `el && el.parentElement` is still there", "A", (x) => x.includes("el && el.parentElement") || `the if in rememberHomes() is gone: el && el.parentElement`);
t6("P100244", "panel buildMoreMenu() line 2245: the if on `!bar || !btn || morePop` is still there", "A", (x) => x.includes("!bar || !btn || morePop") || `the if in buildMoreMenu() is gone: !bar || !btn || morePop`);
t6("P100245", "panel buildMoreMenu() line 2245: the or on `bar` is still there", "A", (x) => x.includes("bar") || `the or in buildMoreMenu() is gone: bar`);
t6("P100246", "panel buildMoreMenu() line 2245: the or on `btn` is still there", "A", (x) => x.includes("btn") || `the or in buildMoreMenu() is gone: btn`);
t6("P100247", "panel buildMoreMenu() line 2271: the if on `!morePop || morePop.hidden` is still there", "A", (x) => x.includes("!morePop || morePop.hidden") || `the if in buildMoreMenu() is gone: !morePop || morePop.hidden`);
t6("P100248", "panel buildMoreMenu() line 2271: the or on `morePop` is still there", "A", (x) => x.includes("morePop") || `the or in buildMoreMenu() is gone: morePop`);
t6("P100249", "panel buildMoreMenu() line 2274: the if on `moreBackOff` is still there", "A", (x) => x.includes("moreBackOff") || `the if in buildMoreMenu() is gone: moreBackOff`);
t6("P100250", "panel buildMoreMenu() line 2283: the if on `morePop.hidden` is still there", "A", (x) => x.includes("morePop.hidden") || `the if in buildMoreMenu() is gone: morePop.hidden`);
t6("P100251", "panel buildMoreMenu() line 2290: the if on `row && row.dataset.for !== \"muteBtn\" && e.ta` is still there", "A", (x) => x.includes("row && row.dataset.for !== \"muteBtn\" && e.target.closest(\"button\"") || `the if in buildMoreMenu() is gone: row && row.dataset.for !== \"muteBtn\" && `);
t6("P100252", "panel buildMoreMenu() line 2294: the if on `!e.target.closest(\"#morePop\"` is still there", "A", (x) => x.includes("!e.target.closest(\"#morePop\"") || `the if in buildMoreMenu() is gone: !e.target.closest(\"#morePop\"`);
t6("P100253", "panel buildMoreMenu() line 2296: the if on `e.key === \"Escape\"` is still there", "A", (x) => x.includes("e.key === \"Escape\"") || `the if in buildMoreMenu() is gone: e.key === \"Escape\"`);
t6("P100254", "panel syncMoreMenu() line 2304: the if on `!acts || !btn` is still there", "A", (x) => x.includes("!acts || !btn") || `the if in syncMoreMenu() is gone: !acts || !btn`);
t6("P100255", "panel syncMoreMenu() line 2304: the or on `acts` is still there", "A", (x) => x.includes("acts") || `the or in syncMoreMenu() is gone: acts`);
t6("P100256", "panel syncMoreMenu() line 2309: the if on `phone` is still there", "A", (x) => x.includes("phone") || `the if in syncMoreMenu() is gone: phone`);
t6("P100257", "panel syncMoreMenu() line 2311: the if on `!morePop` is still there", "A", (x) => x.includes("!morePop") || `the if in syncMoreMenu() is gone: !morePop`);
t6("P100258", "panel syncMoreMenu() line 2314: the if on `el && el.parentElement !== row` is still there", "A", (x) => x.includes("el && el.parentElement !== row") || `the if in syncMoreMenu() is gone: el && el.parentElement !== row`);
t6("P100259", "panel syncMoreMenu() line 2317: the if on `window.__kdsCloseMore` is still there", "A", (x) => x.includes("window.__kdsCloseMore") || `the if in syncMoreMenu() is gone: window.__kdsCloseMore`);
t6("P100260", "panel syncMoreMenu() line 2321: the if on `!el || el.parentElement === acts` is still there", "A", (x) => x.includes("!el || el.parentElement === acts") || `the if in syncMoreMenu() is gone: !el || el.parentElement === acts`);
t6("P100261", "panel syncMoreMenu() line 2355: the if on `window.LFH_OFF && window.LFH_OFF.isOfflineEr` is still there", "A", (x) => x.includes("window.LFH_OFF && window.LFH_OFF.isOfflineErr(e") || `the if in syncMoreMenu() is gone: window.LFH_OFF && window.LFH_OFF.isOffli`);
t6("P100262", "panel syncMoreMenu() line 2356: the if on `window.LFH_OFF && window.LFH_OFF.isBusyErr &` is still there", "A", (x) => x.includes("window.LFH_OFF && window.LFH_OFF.isBusyErr && window.LFH_OFF.isBusyErr(e") || `the if in syncMoreMenu() is gone: window.LFH_OFF && window.LFH_OFF.isBusyE`);
t6("P100263", "panel syncMoreMenu() line 2363: the if on `window.LFH_RT` is still there", "A", (x) => x.includes("window.LFH_RT") || `the if in syncMoreMenu() is gone: window.LFH_RT`);
t6("P100264", "panel syncMoreMenu() line 2374: the if on `fullTimer` is still there", "A", (x) => x.includes("fullTimer") || `the if in syncMoreMenu() is gone: fullTimer`);
t6("P100265", "panel syncMoreMenu() line 2425: the if on `!document.hidden || state.autoPrintKot` is still there", "A", (x) => x.includes("!document.hidden || state.autoPrintKot") || `the if in syncMoreMenu() is gone: !document.hidden || state.autoPrintKot`);
t6("P100266", "panel syncMoreMenu() line 2432: the if on `window.LFH_RT.catchUp` is still there", "A", (x) => x.includes("window.LFH_RT.catchUp") || `the if in syncMoreMenu() is gone: window.LFH_RT.catchUp`);
t6("P100267", "panel backoffPoll() line 2452: the if on `document.hidden || navigator.onLine === fals` is still there", "A", (x) => x.includes("document.hidden || navigator.onLine === false") || `the if in backoffPoll() is gone: document.hidden || navigator.onLine === `);
t6("P100268", "panel backoffPoll() line 2499: the if on `!w || (!w.higherView && !sim` is still there", "A", (x) => x.includes("!w || (!w.higherView && !sim") || `the if in backoffPoll() is gone: !w || (!w.higherView && !sim`);
t6("P100269", "panel backoffPoll() line 2499: the or on `w` is still there", "A", (x) => x.includes("w") || `the or in backoffPoll() is gone: w`);
t6("P100270", "panel backoffPoll() line 2501: the or on `sim` is still there", "A", (x) => x.includes("sim") || `the or in backoffPoll() is gone: sim`);
t6("P100271", "panel backoffPoll() line 2526: the ternary on `w.asName` is still there", "A", (x) => x.includes("w.asName") || `the ternary in backoffPoll() is gone: w.asName`);
t6("P100272", "panel backoffPoll() line 2534: the if on `simB` is still there", "A", (x) => x.includes("simB") || `the if in backoffPoll() is gone: simB`);
t6("P100273", "panel backoffPoll() line 2544: the if on `home` is still there", "A", (x) => x.includes("home") || `the if in backoffPoll() is gone: home`);
t6("P100274", "panel backoffPoll() line 2546: the if on `restLink && restLink.tagName === \"A\"` is still there", "A", (x) => x.includes("restLink && restLink.tagName === \"A\"") || `the if in backoffPoll() is gone: restLink && restLink.tagName === \"A\"`);
t6("P100275", "panel backoffPoll() line 2549: the if on `restEl` is still there", "A", (x) => x.includes("restEl") || `the if in backoffPoll() is gone: restEl`);
t6("P100276", "panel backoffPoll() line 2550: the if on `me && me.textContent !== t` is still there", "A", (x) => x.includes("me && me.textContent !== t") || `the if in backoffPoll() is gone: me && me.textContent !== t`);
t6("P100277", "panel backoffPoll() line 2550: the or on `restEl.textContent` is still there", "A", (x) => x.includes("restEl.textContent") || `the or in backoffPoll() is gone: restEl.textContent`);
t6("P100278", "route gate() line 42: the if on `!g.ok` is still there", "R", (x) => x.includes("!g.ok") || `the if in gate() is gone: !g.ok`);
t6("P100279", "route touchStationSafe() line 57: the if on `!dev` is still there", "R", (x) => x.includes("!dev") || `the if in touchStationSafe() is gone: !dev`);
t6("P100280", "route touchStationSafe() line 58: the catch on `catch` is still there", "R", (x) => x.includes("catch") || `the catch in touchStationSafe() is gone: catch`);
t6("P100281", "route tableTagMap() line 73: the if on `!(await tableTagsLadder(rid` is still there", "R", (x) => x.includes("!(await tableTagsLadder(rid") || `the if in tableTagMap() is gone: !(await tableTagsLadder(rid`);
t6("P100282", "route tableTagMap() line 77: the or on `rows` is still there", "R", (x) => x.includes("rows") || `the or in tableTagMap() is gone: rows`);
t6("P100283", "route tableTagMap() line 80: the if on `t && tag` is still there", "R", (x) => x.includes("t && tag") || `the if in tableTagMap() is gone: t && tag`);
t6("P100284", "route must() line 93: the if on `r.error` is still there", "R", (x) => x.includes("r.error") || `the if in must() is gone: r.error`);
t6("P100285", "route blockedForRead() line 129: the if on `hit && Date.now(` is still there", "R", (x) => x.includes("hit && Date.now(") || `the if in blockedForRead() is gone: hit && Date.now(`);
t6("P100286", "route blockedForRead() line 133: the if on `blockMemo.size > 500` is still there", "R", (x) => x.includes("blockMemo.size > 500") || `the if in blockedForRead() is gone: blockMemo.size > 500`);
t6("P100287", "route blockedForRead() line 133: the if on `Date.now(` is still there", "R", (x) => x.includes("Date.now(") || `the if in blockedForRead() is gone: Date.now(`);
t6("P100288", "route GET() line 142: the if on `g instanceof NextResponse` is still there", "R", (x) => x.includes("g instanceof NextResponse") || `the if in GET() is gone: g instanceof NextResponse`);
t6("P100289", "route GET() line 152: the if on `!rid` is still there", "R", (x) => x.includes("!rid") || `the if in GET() is gone: !rid`);
t6("P100290", "route GET() line 154: the if on `await blockedForRead(deviceIdFrom(req` is still there", "R", (x) => x.includes("await blockedForRead(deviceIdFrom(req") || `the if in GET() is gone: await blockedForRead(deviceIdFrom(req`);
t6("P100291", "route GET() line 162: the if on `path.join(\"/\"` is still there", "R", (x) => x.includes("path.join(\"/\"") || `the if in GET() is gone: path.join(\"/\"`);
t6("P100292", "route GET() line 173: the ternary on `g.user` is still there", "R", (x) => x.includes("g.user") || `the ternary in GET() is gone: g.user`);
t6("P100293", "route GET() line 173: the ternary on `simulate` is still there", "R", (x) => x.includes("simulate") || `the ternary in GET() is gone: simulate`);
t6("P100294", "route GET() line 211: the ternary on `wantJobs` is still there", "R", (x) => x.includes("wantJobs") || `the ternary in GET() is gone: wantJobs`);
t6("P100295", "route GET() line 255: the or on `platform_channels` is still there", "R", (x) => x.includes("platform_channels") || `the or in GET() is gone: platform_channels`);
t6("P100296", "route GET() line 258: the if on `platL.effective` is still there", "R", (x) => x.includes("platL.effective") || `the if in GET() is gone: platL.effective`);
t6("P100297", "route GET() line 258: the if on `kOn(\"zomato\"` is still there", "R", (x) => x.includes("kOn(\"zomato\"") || `the if in GET() is gone: kOn(\"zomato\"`);
t6("P100298", "route GET() line 258: the if on `kOn(\"swiggy\"` is still there", "R", (x) => x.includes("kOn(\"swiggy\"") || `the if in GET() is gone: kOn(\"swiggy\"`);
t6("P100299", "route GET() line 258: the if on `kOn(\"website\"` is still there", "R", (x) => x.includes("kOn(\"website\"") || `the if in GET() is gone: kOn(\"website\"`);
t6("P100300", "route GET() line 259: the if on `parcL.effective` is still there", "R", (x) => x.includes("parcL.effective") || `the if in GET() is gone: parcL.effective`);
t6("P100301", "route GET() line 306: the if on `target.kind === \"screen\"` is still there", "R", (x) => x.includes("target.kind === \"screen\"") || `the if in GET() is gone: target.kind === \"screen\"`);
t6("P100302", "route GET() line 307: the if on `target.kind === \"off\" || target.kind === \"co` is still there", "R", (x) => x.includes("target.kind === \"off\" || target.kind === \"computer\"") || `the if in GET() is gone: target.kind === \"off\" || target.kind ===`);
t6("P100303", "route GET() line 308: the or on `id` is still there", "R", (x) => x.includes("id") || `the or in GET() is gone: id`);
t6("P100304", "route GET() line 327: the if on `station.mine` is still there", "R", (x) => x.includes("station.mine") || `the if in GET() is gone: station.mine`);
t6("P100305", "route GET() line 359: the ternary on `mayI.ok` is still there", "R", (x) => x.includes("mayI.ok") || `the ternary in GET() is gone: mayI.ok`);
t6("P100306", "route GET() line 361: the or on `table_names` is still there", "R", (x) => x.includes("table_names") || `the or in GET() is gone: table_names`);
t6("P100307", "route GET() line 368: the catch on `catch (e)` is still there", "R", (x) => x.includes("catch (e)") || `the catch in GET() is gone: catch (e)`);
t6("P100308", "route GET() line 369: the if on `worthLogging(e` is still there", "R", (x) => x.includes("worthLogging(e") || `the if in GET() is gone: worthLogging(e`);
t6("P100309", "route postImpl() line 413: the if on `emptyIdSegment(b` is still there", "R", (x) => x.includes("emptyIdSegment(b") || `the if in postImpl() is gone: emptyIdSegment(b`);
t6("P100310", "route postImpl() line 422: the or on `g.user.name` is still there", "R", (x) => x.includes("g.user.name") || `the or in postImpl() is gone: g.user.name`);
t6("P100311", "route postImpl() line 427: the if on `await deviceBlocked(dev, rid` is still there", "R", (x) => x.includes("await deviceBlocked(dev, rid") || `the if in postImpl() is gone: await deviceBlocked(dev, rid`);
t6("P100312", "route postImpl() line 436: the if on `clash` is still there", "R", (x) => x.includes("clash") || `the if in postImpl() is gone: clash`);
t6("P100313", "route postImpl() line 442: the if on `overwrite` is still there", "R", (x) => x.includes("overwrite") || `the if in postImpl() is gone: overwrite`);
t6("P100314", "route postImpl() line 447: the if on `a === \"issue\"` is still there", "R", (x) => x.includes("a === \"issue\"") || `the if in postImpl() is gone: a === \"issue\"`);
t6("P100315", "route postImpl() line 451: the ternary on `ib` is still there", "R", (x) => x.includes("ib") || `the ternary in postImpl() is gone: ib`);
t6("P100316", "route postImpl() line 451: the or on `subject` is still there", "R", (x) => x.includes("subject") || `the or in postImpl() is gone: subject`);
t6("P100317", "route postImpl() line 452: the or on `name` is still there", "R", (x) => x.includes("name") || `the or in postImpl() is gone: name`);
t6("P100318", "route postImpl() line 452: the or on `username` is still there", "R", (x) => x.includes("username") || `the or in postImpl() is gone: username`);
t6("P100319", "route postImpl() line 453: the or on `role` is still there", "R", (x) => x.includes("role") || `the or in postImpl() is gone: role`);
t6("P100320", "route postImpl() line 456: the ternary on `Error` is still there", "R", (x) => x.includes("Error") || `the ternary in postImpl() is gone: Error`);
t6("P100321", "route postImpl() line 461: the if on `a === \"orders\" && c === \"accept\"` is still there", "R", (x) => x.includes("a === \"orders\" && c === \"accept\"") || `the if in postImpl() is gone: a === \"orders\" && c === \"accept\"`);
t6("P100322", "route postImpl() line 466: the if on `!cur` is still there", "R", (x) => x.includes("!cur") || `the if in postImpl() is gone: !cur`);
t6("P100323", "route postImpl() line 479: the if on `a === \"orders\" && c === \"ready\"` is still there", "R", (x) => x.includes("a === \"orders\" && c === \"ready\"") || `the if in postImpl() is gone: a === \"orders\" && c === \"ready\"`);
t6("P100324", "route postImpl() line 504: the if on `a === \"orders\" && c === \"unready\"` is still there", "R", (x) => x.includes("a === \"orders\" && c === \"unready\"") || `the if in postImpl() is gone: a === \"orders\" && c === \"unready\"`);
t6("P100325", "route postImpl() line 507: the ternary on `body` is still there", "R", (x) => x.includes("body") || `the ternary in postImpl() is gone: body`);
t6("P100326", "route postImpl() line 511: the ternary on `d` is still there", "R", (x) => x.includes("d") || `the ternary in postImpl() is gone: d`);
t6("P100327", "route postImpl() line 514: the if on `!snap.length` is still there", "R", (x) => x.includes("!snap.length") || `the if in postImpl() is gone: !snap.length`);
t6("P100328", "route postImpl() line 525: the if on `wasNow === undefined` is still there", "R", (x) => x.includes("wasNow === undefined") || `the if in postImpl() is gone: wasNow === undefined`);
t6("P100329", "route postImpl() line 526: the if on `wasNow === \"served\"` is still there", "R", (x) => x.includes("wasNow === \"served\"") || `the if in postImpl() is gone: wasNow === \"served\"`);
t6("P100330", "route postImpl() line 527: the if on `wasNow === d.prev` is still there", "R", (x) => x.includes("wasNow === d.prev") || `the if in postImpl() is gone: wasNow === d.prev`);
t6("P100331", "route postImpl() line 528: the if on `!groups.has(d.prev` is still there", "R", (x) => x.includes("!groups.has(d.prev") || `the if in postImpl() is gone: !groups.has(d.prev`);
t6("P100332", "route postImpl() line 531: the if on `!groups.size` is still there", "R", (x) => x.includes("!groups.size") || `the if in postImpl() is gone: !groups.size`);
t6("P100333", "route postImpl() line 545: the ternary on `0` is still there", "R", (x) => x.includes("0") || `the ternary in postImpl() is gone: 0`);
t6("P100334", "route postImpl() line 545: the ternary on `anyActive` is still there", "R", (x) => x.includes("anyActive") || `the ternary in postImpl() is gone: anyActive`);
t6("P100335", "route postImpl() line 550: the ternary on `1` is still there", "R", (x) => x.includes("1") || `the ternary in postImpl() is gone: 1`);
t6("P100336", "route postImpl() line 556: the if on `a === \"items\" && c === \"status\"` is still there", "R", (x) => x.includes("a === \"items\" && c === \"status\"") || `the if in postImpl() is gone: a === \"items\" && c === \"status\"`);
t6("P100337", "route postImpl() line 558: the if on `![\"received\", \"preparing\", \"ready\", \"served\"` is still there", "R", (x) => x.includes("![\"received\", \"preparing\", \"ready\", \"served\"].includes(status") || `the if in postImpl() is gone: ![\"received\", \"preparing\", \"ready\", \"ser`);
t6("P100338", "route postImpl() line 573: the if on `!item` is still there", "R", (x) => x.includes("!item") || `the if in postImpl() is gone: !item`);
t6("P100339", "route postImpl() line 574: the if on `item.order_id` is still there", "R", (x) => x.includes("item.order_id") || `the if in postImpl() is gone: item.order_id`);
t6("P100340", "route postImpl() line 593: the if on `a === \"platform\" && c === \"status\"` is still there", "R", (x) => x.includes("a === \"platform\" && c === \"status\"") || `the if in postImpl() is gone: a === \"platform\" && c === \"status\"`);
t6("P100341", "route postImpl() line 595: the if on `![\"accepted\", \"preparing\", \"ready\", \"handed_` is still there", "R", (x) => x.includes("![\"accepted\", \"preparing\", \"ready\", \"handed_over\"].includes(status") || `the if in postImpl() is gone: ![\"accepted\", \"preparing\", \"ready\", \"han`);
t6("P100342", "route postImpl() line 600: the if on `!owns` is still there", "R", (x) => x.includes("!owns") || `the if in postImpl() is gone: !owns`);
t6("P100343", "route postImpl() line 606: the if on `owns.status === \"new\"` is still there", "R", (x) => x.includes("owns.status === \"new\"") || `the if in postImpl() is gone: owns.status === \"new\"`);
t6("P100344", "route postImpl() line 608: the if on `!(s.data && s.data.kitchen_can_accept_platfo` is still there", "R", (x) => x.includes("!(s.data && s.data.kitchen_can_accept_platform") || `the if in postImpl() is gone: !(s.data && s.data.kitchen_can_accept_pl`);
t6("P100345", "route postImpl() line 613: the if on `error` is still there", "R", (x) => x.includes("error") || `the if in postImpl() is gone: error`);
t6("P100346", "route postImpl() line 621: the if on `a === \"dishes\" && c === \"sold-out\"` is still there", "R", (x) => x.includes("a === \"dishes\" && c === \"sold-out\"") || `the if in postImpl() is gone: a === \"dishes\" && c === \"sold-out\"`);
t6("P100347", "route postImpl() line 626: the if on `value` is still there", "R", (x) => x.includes("value") || `the if in postImpl() is gone: value`);
t6("P100348", "route postImpl() line 650: the if on `a === \"print-jobs\" && b === \"claim\"` is still there", "R", (x) => x.includes("a === \"print-jobs\" && b === \"claim\"") || `the if in postImpl() is gone: a === \"print-jobs\" && b === \"claim\"`);
t6("P100349", "route postImpl() line 652: the if on `!ids.length` is still there", "R", (x) => x.includes("!ids.length") || `the if in postImpl() is gone: !ids.length`);
t6("P100350", "route postImpl() line 669: the if on `!may.ok` is still there", "R", (x) => x.includes("!may.ok") || `the if in postImpl() is gone: !may.ok`);
t6("P100351", "route postImpl() line 682: the if on `!gate.ok` is still there", "R", (x) => x.includes("!gate.ok") || `the if in postImpl() is gone: !gate.ok`);
t6("P100352", "route postImpl() line 691: the if on `a === \"print-station\" && b === \"take\"` is still there", "R", (x) => x.includes("a === \"print-station\" && b === \"take\"") || `the if in postImpl() is gone: a === \"print-station\" && b === \"take\"`);
t6("P100353", "route postImpl() line 695: the if on `!(st2?.auto_print_kot === true && st2?.auto_` is still there", "R", (x) => x.includes("!(st2?.auto_print_kot === true && st2?.auto_print_kot_allowed === true") || `the if in postImpl() is gone: !(st2?.auto_print_kot === true && st2?.a`);
t6("P100354", "route postImpl() line 701: the if on `!mayTake.ok` is still there", "R", (x) => x.includes("!mayTake.ok") || `the if in postImpl() is gone: !mayTake.ok`);
t6("P100355", "route postImpl() line 709: the if on `a === \"print-station\" && b === \"release\"` is still there", "R", (x) => x.includes("a === \"print-station\" && b === \"release\"") || `the if in postImpl() is gone: a === \"print-station\" && b === \"release\"`);
t6("P100356", "route postImpl() line 720: the if on `a === \"print-jobs\" && c === \"done\"` is still there", "R", (x) => x.includes("a === \"print-jobs\" && c === \"done\"") || `the if in postImpl() is gone: a === \"print-jobs\" && c === \"done\"`);
t6("P100357", "route postImpl() line 726: the if on `!r.found` is still there", "R", (x) => x.includes("!r.found") || `the if in postImpl() is gone: !r.found`);
t6("P100358", "route postImpl() line 727: the if on `okPrint` is still there", "R", (x) => x.includes("okPrint") || `the if in postImpl() is gone: okPrint`);
t6("P100359", "route postImpl() line 738: the ternary on `r.reprint` is still there", "R", (x) => x.includes("r.reprint") || `the ternary in postImpl() is gone: r.reprint`);
t6("P100360", "route postImpl() line 738: the ternary on `null` is still there", "R", (x) => x.includes("null") || `the ternary in postImpl() is gone: null`);
t6("P100361", "route postImpl() line 747: the ternary on `r.parked` is still there", "R", (x) => x.includes("r.parked") || `the ternary in postImpl() is gone: r.parked`);
t6("P100362", "route postImpl() line 760: the if on `a === \"printer-events\" && path.length === 1` is still there", "R", (x) => x.includes("a === \"printer-events\" && path.length === 1") || `the if in postImpl() is gone: a === \"printer-events\" && path.length ==`);
t6("P100363", "route postImpl() line 762: the or on `kind` is still there", "R", (x) => x.includes("kind") || `the or in postImpl() is gone: kind`);
t6("P100364", "route postImpl() line 763: the if on `!kinds.includes(kind` is still there", "R", (x) => x.includes("!kinds.includes(kind") || `the if in postImpl() is gone: !kinds.includes(kind`);
t6("P100365", "route postImpl() line 773: the or on `own.printer` is still there", "R", (x) => x.includes("own.printer") || `the or in postImpl() is gone: own.printer`);
t6("P100366", "route postImpl() line 777: the ternary on `aboutPrinter` is still there", "R", (x) => x.includes("aboutPrinter") || `the ternary in postImpl() is gone: aboutPrinter`);
t6("P100367", "route postImpl() line 779: the if on `open && open.length` is still there", "R", (x) => x.includes("open && open.length") || `the if in postImpl() is gone: open && open.length`);
t6("P100368", "route postImpl() line 780: the ternary on `note` is still there", "R", (x) => x.includes("note") || `the ternary in postImpl() is gone: note`);
t6("P100369", "route postImpl() line 780: the or on `count` is still there", "R", (x) => x.includes("count") || `the or in postImpl() is gone: count`);
