#!/bin/bash
# =====================================================================
#  Little French House — open your LEARNING GUIDE.
#  On a Mac: just DOUBLE-CLICK this file. It opens the guide in your
#  browser. (No server, no internet needed — it works offline.)
#  If double-clicking ever says "permission denied", run this once in
#  Terminal:  chmod +x run.command
# =====================================================================
cd "$(dirname "$0")"
open "index.html"
